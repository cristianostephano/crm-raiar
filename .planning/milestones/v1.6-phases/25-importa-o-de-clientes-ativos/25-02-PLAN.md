---
phase: 25-importa-o-de-clientes-ativos
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - lib/importacao/typesAtivo.ts
  - lib/importacao/modeloAtivo.ts
  - lib/importacao/annotarLinhaAtivo.ts
  - lib/importacao/confirmarAtivo.ts
  - lib/importacao/confirmar.ts
  - app/actions/importacaoAtivos.ts
  - tests/importacao/ativos-vocabulario.test.ts
  - tests/importacao/annotarLinhaAtivo.test.ts
  - tests/importacao/confirmarAtivo.test.ts
autonomous: true
requirements: [ATIVO-01, ATIVO-03, ATIVO-04]

must_haves:
  truths:
    - "Uma linha da planilha de ativos sem um dos campos obrigatórios vira erro daquela linha, com uma frase que nomeia o campo que faltou — e as outras linhas do mesmo lote continuam válidas (ATIVO-03)"
    - "Uma linha cuja razão social já existe no banco, ou colide com outra linha do próprio lote, é marcada como possível duplicado antes de qualquer gravação (ATIVO-04)"
    - "Uma linha com erro nunca é promovida a possível duplicado — erro tem precedência"
    - "Confirmar o lote chama `importar_clientes_ativos_lote` exatamente uma vez e devolve, para a tela, quantos entraram e o motivo agrupado de cada linha pulada"
    - "Só o Supervisor consegue validar ou confirmar um lote de ativos"
    - "Existe UMA única lista que decide, ao mesmo tempo, quais campos são obrigatórios e qual frase aparece quando cada um falta"
  artifacts:
    - lib/importacao/typesAtivo.ts
    - lib/importacao/modeloAtivo.ts
    - lib/importacao/annotarLinhaAtivo.ts
    - lib/importacao/confirmarAtivo.ts
    - app/actions/importacaoAtivos.ts
  key_links:
    - "`SYSTEM_FIELDS_ATIVO` alimenta ao mesmo tempo o modelo de planilha, a sugestão/obrigatoriedade de mapeamento e a checagem de campo faltando na anotação — nunca uma segunda cópia da regra"
    - "A leitura de cidades usa o leitor paginado `getTodasCidades()`, nunca uma consulta direta à tabela (a tabela tem 5571 linhas e uma consulta direta trunca em 1000)"
    - "A detecção de duplicado é `findDuplicates` de `lib/importacao/dedupe.ts`, reusada verbatim (ATIVO-04)"
    - "O formato de carga da RPC é o mesmo `RpcClienteRow` já usado pela importação de clientes — `etapa` e `status_acompanhamento` são constantes dentro do SQL, nunca campos da carga"
---

<objective>
Construir a camada pura + as ações de servidor da nova importação de clientes ativos: o vocabulário de campos (com a obrigatoriedade ampliada), o gerador do modelo de planilha, a anotação linha a linha, o planejamento de confirmação e as duas ações de servidor (validar e confirmar).

Purpose: é aqui que ATIVO-03 (erro por linha, nunca trava o lote) e ATIVO-04 (duplicado detectado com a mesma lógica de sempre) ficam realmente resolvidos. A tela do plano 25-03 é só a superfície disso.

Output: 4 módulos puros novos, 1 ação de servidor nova, 1 ajuste mínimo de exportação num módulo existente, e 3 arquivos de teste unitário.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-importa-o-de-clientes-ativos/25-01-SUMMARY.md

# Vocabulário e anotação da importação ORIGINAL de clientes (fonte de campos, forma de validação)
@lib/importacao/types.ts
@lib/importacao/annotarLinha.ts
@lib/importacao/confirmar.ts
@app/actions/importacao.ts

# Espelho estrutural do conjunto de arquivos irmão (planilha de CNPJ em massa)
@lib/importacao/typesCnpj.ts
@lib/importacao/annotarLinhaCnpj.ts
@app/actions/importacaoCnpj.ts

# Camada genérica de mapeamento já parametrizada por lista de campos
@lib/importacao/mapping.ts
@lib/importacao/modeloCnpj.ts
</context>

<source_audit>
Ver a auditoria de cobertura completa (GOAL / REQ / RESEARCH / CONTEXT) em `25-01-PLAN.md`, seção `<source_audit>` — ela vale para os três planos da fase. Este plano cobre ATIVO-01 (obrigatoriedade ampliada, frequência de visita fora), ATIVO-03 (anotação por linha) e ATIVO-04 (reuso da detecção de duplicado).
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vocabulário de campos da planilha de ativos + gerador do modelo</name>
  <files>lib/importacao/typesAtivo.ts, lib/importacao/modeloAtivo.ts, tests/importacao/ativos-vocabulario.test.ts</files>
  <behavior>
    - campos: `SYSTEM_FIELDS_ATIVO` tem 16 definições, nas mesmas chaves e na mesma ordem de `SYSTEM_FIELDS`, e nenhuma chave referente a frequência de visita ou dia fixo.
    - obrigatorios: exatamente 9 definições têm `required` verdadeiro — razão social, CNPJ, CEP, rua, número, cidade, estado, responsável e contato.
    - invariante: toda definição obrigatória tem uma frase de campo faltando não vazia, e nenhuma definição opcional tem essa frase — é isso que garante uma fonte única de obrigatoriedade.
    - naoregride: as mesmas 16 definições de `SYSTEM_FIELDS` continuam com apenas 2 obrigatórias (razão social e responsável) — a lista antiga não é tocada por este plano.
    - sugestao: `suggestMapping` avaliado contra o vocabulário novo continua reconhecendo os apelidos de cabeçalho de sempre (por exemplo o apelido de vendedor apontando para responsável, e o de município apontando para cidade).
    - obrigatoriosfaltando: `requiredFieldsFaltando` com mapeamento vazio devolve as 9 definições obrigatórias; com as 9 mapeadas devolve lista vazia.
    - modelo: `buildModeloAtivos()` gera uma planilha com 16 colunas cujos rótulos são exatamente os rótulos do vocabulário, na ordem, mais uma linha de exemplo com 16 células preenchidas.
  </behavior>
  <action>
Criar `lib/importacao/typesAtivo.ts`.

Declarar `SystemFieldDefinitionAtivo`, estendendo `SystemFieldDefinition<SystemField>` (de `lib/importacao/types.ts`) com uma propriedade opcional de texto para a frase exibida quando aquele campo obrigatório está em branco. Estender em vez de criar um tipo paralelo é o que mantém a lista compatível com a camada genérica de mapeamento (`suggestMapping`, `applyMapping`, `requiredFieldsFaltando`, `ColumnMappingTable`), que já é parametrizada por lista de campos desde a Fase 17.

Declarar `SYSTEM_FIELDS_ATIVO`: as MESMAS 16 chaves de `SYSTEM_FIELDS`, na MESMA ordem e com os MESMOS rótulos — a planilha de ativos pede os mesmos campos de cliente, o que muda é quais deles são exigidos. Marcar como obrigatórios exatamente 9: razão social, CNPJ, CEP, rua, número, cidade, estado, responsável e contato. Os outros 7 (nome fantasia, complemento, categoria, telefone, email, produtos consumidos, número de lojas) ficam opcionais.

Cada um dos 9 obrigatórios carrega a sua própria frase de campo faltando, escrita com a concordância correta em português (o gênero muda por campo — não dá para gerar a frase a partir do rótulo). Usar o mesmo tom das frases já existentes na importação de clientes.

Escrever no cabeçalho do arquivo, como comentário, a justificativa de cada fronteira de obrigatoriedade, porque ela vai ser conferida pelo dono do projeto no checkpoint do plano 25-03:
- Os 9 obrigatórios desta lista são, juntos, EXATAMENTE a condição que `cliente_ativo_pronto_para_ganho` (a RPC do plano 25-01) exige para gravar — mesma lista, mesma cardinalidade, sem subconjunto nem superconjunto. Uma linha sem qualquer um deles é recusada pelo banco de qualquer jeito; exigir na tela só antecipa a recusa com uma explicação melhor.
- Razão social, CNPJ e os 5 campos de endereço: cópia dos guards de ganho das migrations 0018/0025.
- Responsável: a coluna é obrigatória no esquema da tabela desde a migration 0002; sem ela a linha não pode nem ser criada. Por ser `not null`, uma linha sem responsável que chegasse ao banco abortaria o LOTE INTEIRO por violação de restrição, não só a própria linha — por isso ela também entra no filtro da RPC do 25-01, e não fica só nesta lista.
- Contato: nomeado literalmente no requisito ATIVO-01. É anulável na tabela, então o banco NÃO a recusaria sozinho — sem a exigência aqui e no filtro da RPC, a linha entraria calada com contato nulo, sem erro e sem sinal na tela.
- Frequência de visita e dia fixo NÃO aparecem neste vocabulário, por decisão travada — são definidos depois, individualmente, pela ficha/Agenda da Fase 24. Um cliente importado por aqui nasce sem frequência e por isso aparece na seção "Sem dia fixo definido" da Agenda; isso é o comportamento esperado, não uma pendência.
- Registrar também que esta lista é a AUTORIDADE ÚNICA de obrigatoriedade deste fluxo: não existe um esquema de validação paralelo mantido à mão. Isso fecha um risco registrado na STATE.md (duas fontes de obrigatoriedade concordando só por convenção entre os dois assistentes de importação).
- Registrar que trocar um campo de obrigatório para opcional (ou o contrário) é uma mudança de UMA linha deste arquivo mais o ajuste do caso de teste correspondente.

Nenhuma validação de formato/dígito verificador de CNPJ em lugar nenhum — só presença. É convenção travada do projeto desde a Fase 18.

Criar `lib/importacao/modeloAtivo.ts`, espelho literal de `lib/importacao/modeloCnpj.ts`: exporta `buildModeloAtivos()`, deriva os cabeçalhos dos rótulos de `SYSTEM_FIELDS_ATIVO` (nunca digitados à mão), escreve uma linha de exemplo por campo, e usa a escrita em modo seguro para navegador (o botão de baixar modelo embrulha o resultado num objeto binário no cliente — nada de manipulador de rota nem de buffer de servidor). Não sanitiza: o modelo é gerado pelo próprio sistema, nunca é entrada do usuário. Os valores de exemplo podem reusar os mesmos de `lib/importacao/modelo.ts`.

Criar `tests/importacao/ativos-vocabulario.test.ts` no molde de `tests/importacao/cnpj-vocabulario.test.ts` (testes puros, sem Supabase), cobrindo os comportamentos declarados acima.
  </action>
  <verify>
    <automated>npx vitest run tests/importacao/ativos-vocabulario.test.ts tests/importacao/modelo.test.ts tests/importacao/mapping.test.ts</automated>
  </verify>
  <done>Os três arquivos existem; o teste novo passa; `modelo.test.ts` e `mapping.test.ts` (da importação antiga) passam sem edição, provando que a lista antiga não foi tocada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Anotação linha a linha da planilha de ativos</name>
  <files>lib/importacao/annotarLinhaAtivo.ts, tests/importacao/annotarLinhaAtivo.test.ts</files>
  <behavior>
    - completa: uma linha com os 9 obrigatórios preenchidos e uma categoria/produto/responsável que existem nas listas de consulta resulta em situação "ok", sem motivos, e com todos os identificadores resolvidos.
    - faltando: uma linha sem CNPJ resulta em situação "erro" cujo único motivo é a frase de CNPJ faltando declarada no vocabulário — e uma linha sem CEP produz a frase de CEP, provando que a frase vem da lista, não de um mapa paralelo.
    - soespacos: um campo obrigatório preenchido só com espaços conta como faltando.
    - multiplosmotivos: uma linha com três obrigatórios em branco acumula os três motivos, sem repetição.
    - responsavelnaoencontrado: um responsável preenchido mas que não casa com nenhum vendedor produz um motivo DIFERENTE do de responsável em branco (um diz que não foi informado, o outro que não foi encontrado).
    - categoriainexistente: uma categoria preenchida que não existe na lista vira motivo de erro nomeando o valor lido.
    - produtos: uma célula de produtos com dois valores separados por vírgula resolve dois identificadores; um valor inexistente entre eles vira motivo de erro nomeando aquele valor.
    - estadocidade: uma sigla de estado fora da lista fixa de siglas vira erro; uma cidade que não existe para um estado válido vira erro; uma cidade válida é normalizada para o nome canônico.
    - sanitizacao: uma célula que começa com um caractere de fórmula de planilha é neutralizada antes de qualquer comparação.
    - loteindependente: anotar um lote de 3 linhas onde só a do meio está incompleta devolve "ok", "erro", "ok" — nenhuma linha contamina a outra (ATIVO-03).
  </behavior>
  <action>
Criar `lib/importacao/annotarLinhaAtivo.ts` como módulo PURO — sem cliente Supabase, sem diretiva de servidor/cliente — exatamente a disciplina de `lib/importacao/annotarLinha.ts`: todas as listas de consulta (vendedores, categorias, produtos, cidades) chegam já estreitadas pelo chamador, o que torna o arquivo totalmente testável com listas fabricadas, sem variáveis de ambiente.

Reusar tipos em vez de duplicar: `MappedRow`, `ResolvedRow`, `VendedorLookup`, `CidadeLookup` e `AnnotarLinhaLookups` vêm de `lib/importacao/annotarLinha.ts`. A planilha de ativos usa exatamente as mesmas 16 chaves e produz exatamente a mesma forma resolvida — declarar cópias desses tipos criaria duas verdades que só concordariam por convenção. Registrar isso em comentário.

Exportar `AnnotatedRowAtivo` (situação "ok" ou "erro", lista de motivos, forma resolvida), `annotarLinhaAtivo(linha, lookups)` e `annotarLoteAtivos(linhas, lookups)`.

Corpo da anotação, na ordem:
1. Sanear toda célula definida com `sanitizeCell` (de `lib/clientes/exportacao`) ANTES de qualquer resolução ou comparação — mesma guarda contra injeção de fórmula já aplicada nos dois fluxos irmãos.
2. Campos obrigatórios: percorrer `SYSTEM_FIELDS_ATIVO`, filtrando as definições obrigatórias, e para cada uma cujo valor saneado esteja ausente ou só com espaços, acrescentar a frase de campo faltando declarada NA PRÓPRIA definição. Este laço é o único lugar que decide obrigatoriedade — não criar esquema de validação paralelo, não importar o esquema da importação antiga.
3. Categoria (opcional): quando preenchida, casar por nome sem diferenciar caixa/acento (mesma função de normalização de `lib/importacao/dedupe.ts` já usada em `annotarLinha.ts`); sem casamento, motivo de erro nomeando o valor lido.
4. Produtos consumidos (opcional, multivalor): separar por vírgula ou ponto e vírgula, aparar, descartar vazios, casar cada pedaço; cada pedaço sem casamento vira um motivo próprio.
5. Responsável: quando o valor está em branco, o laço do passo 2 já produziu a frase de campo faltando — NÃO acrescentar nada aqui, para não duplicar motivo. Quando está preenchido, casar primeiro por email e depois por "nome sobrenome" (mesma ordem de `findVendedor` em `annotarLinha.ts`); sem casamento, um motivo NOVO e distinto, que diz que o responsável informado não foi encontrado e cita o valor lido. Distinguir os dois casos é uma melhoria deliberada sobre a importação antiga, que confunde os dois numa frase só.
6. Estado e cidade: reusar a lista fixa de siglas (`UFS` de `lib/clientes/ufs`) e `cidadeValida`/`cidadeCanonica` (de `lib/clientes/cidadeValida`), com a mesma forma de `annotarLinha.ts`: as duas checagens só rodam quando o valor está presente, porque a ausência já foi tratada no passo 2.
7. Remover motivos repetidos antes de devolver; situação é "erro" quando sobrou pelo menos um motivo, senão "ok". A situação de possível duplicado NÃO é decidida aqui — ela precisa do lote inteiro e é aplicada pela ação de servidor, exatamente como na importação antiga.
8. Forma resolvida: montar `ResolvedRow` como `annotarLinha.ts` faz — campos de texto aparados e virando valor nulo quando ficam vazios (texto vazio em estado viola uma restrição de verificação do banco e derrubaria a linha), estado normalizado para maiúscula, cidade normalizada para o nome canônico quando casa.

Criar `tests/importacao/annotarLinhaAtivo.test.ts` no molde de `tests/importacao/annotarLinha.test.ts`: listas de consulta fabricadas, sem Supabase, cobrindo os comportamentos declarados acima. Incluir pelo menos um caso que prove que a frase de campo faltando lida no motivo é literalmente a mesma string declarada na definição correspondente de `SYSTEM_FIELDS_ATIVO` (lendo-a da lista, não repetindo o texto no teste) — é essa asserção que trava a fonte única de obrigatoriedade.
  </action>
  <verify>
    <automated>npx vitest run tests/importacao/annotarLinhaAtivo.test.ts tests/importacao/annotarLinha.test.ts</automated>
  </verify>
  <done>`annotarLinhaAtivo.ts` existe e é puro (nenhum import de `@/lib/supabase/...`, nenhuma diretiva); o teste novo passa; `annotarLinha.test.ts` passa sem edição, provando que a anotação antiga não foi tocada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Planejamento de confirmação + ações de servidor de validar e confirmar</name>
  <files>lib/importacao/confirmarAtivo.ts, lib/importacao/confirmar.ts, app/actions/importacaoAtivos.ts, tests/importacao/confirmarAtivo.test.ts</files>
  <behavior>
    - reconciliainserido: uma carga de 2 linhas cujo retorno da RPC traz as duas como inseridas devolve 2 importados e nenhum motivo de pulada.
    - reconciliaincompleto: uma linha devolvida pela RPC como incompleta vira uma pulada agrupada sob um motivo que fala de dado obrigatório faltando ao gravar.
    - reconciliaduplicado: uma linha devolvida como duplicada vira uma pulada agrupada sob o motivo de duplicado encontrado ao confirmar.
    - reconciliaausente: uma linha enviada que não aparece de forma alguma no retorno é contada como pulada (nunca como importada), sob o mesmo motivo de duplicado encontrado ao confirmar.
    - razaorepetida: quando duas linhas da carga têm a mesma razão social e a RPC devolve uma inserção só para ela, no máximo uma é contada como importada; a outra vira pulada.
    - fundemotivos: dois grupos com o mesmo texto de motivo são somados num só, nunca listados duas vezes.
  </behavior>
  <action>
Editar `lib/importacao/confirmar.ts` com UMA mudança mínima e sem efeito de comportamento: exportar a função que converte uma linha resolvida na forma de carga da RPC (hoje ela é privada) e exportar as duas constantes de texto de motivo de duplicado (a de pulado na revisão e a de encontrado ao confirmar). Nenhuma outra linha desse arquivo muda. O objetivo é que o fluxo de ativos reuse a MESMA conversão e o MESMO vocabulário de motivo em vez de manter uma segunda cópia que só concordaria por convenção. Rodar os testes existentes desse módulo sem editá-los é a prova de que nada quebrou.

Criar `lib/importacao/confirmarAtivo.ts`, módulo puro (sem Supabase, sem diretiva), com:
- Um tipo para a linha devolvida pela RPC do plano 25-01: razão social, identificador possivelmente nulo, e um texto de situação restrito a inserido, duplicado ou incompleto. Os nomes das propriedades seguem o formato de coluna do Postgres, igual ao que `confirmar.ts` já faz para a carga.
- Uma constante de texto para o motivo de linha recusada por dado obrigatório faltando no momento da gravação, explicando em linguagem simples que faltou razão social, CNPJ ou endereço completo. Este é um motivo de rede de segurança: uma linha nesse estado deveria ter sido barrada na revisão; se aparecer aqui, houve mudança de dado entre revisar e confirmar.
- `reconciliarAtivos(carga, retornados)`, que devolve a lista de importados (por razão social) e os grupos de puladas. Percorrer a carga na ordem, casando cada item com uma entrada do retorno que ainda não foi consumida (conjunto de consumidos, para que duas linhas com a mesma razão social nunca contem duas inserções). Situação inserida vira importado; duplicada vira pulada sob a constante já existente de duplicado encontrado ao confirmar; incompleta vira pulada sob a constante nova; ausência total de casamento vira pulada sob a constante de duplicado encontrado ao confirmar (mesmo tratamento defensivo que `reconcileImportados` já dá hoje).
- `fundirGruposDeMotivosAtivos(a, b)`, somando quantidades por texto de motivo. Declarada localmente e não importada do fluxo de CNPJ de propósito: a Fase 26 vai remover as telas antigas de importação, e criar dependência de um módulo que pode desaparecer transformaria uma limpeza de menu numa quebra de compilação.

Criar `app/actions/importacaoAtivos.ts` com diretiva de servidor, espelho estrutural de `app/actions/importacaoCnpj.ts` somado à lógica de consulta de `app/actions/importacao.ts`. Atenção: num módulo com diretiva de servidor toda exportação de VALOR precisa ser função assíncrona — as duas ações abaixo cumprem isso; tipos podem ser exportados normalmente (são apagados na compilação).

- `ValidatedRowAtivo`: índice da linha, situação ("ok", "duplicado" ou "erro"), motivos, nome parecido opcional, e a forma resolvida.
- `validarLoteAtivos(linhas)`: somente leitura, NUNCA escreve nem invalida cache. Abertura idêntica às irmãs — ler o usuário da sessão; sem sessão devolve código de não autenticado; ler o papel na tabela de perfis; se não for supervisor devolve código de proibido; só então qualquer leitura de dado. Em paralelo: categorias ativas, produtos ativos, perfis (identificador, nome, sobrenome, email), razões sociais já existentes em `clientes` (consulta estreita, sem NENHUM filtro manual de dono — para o Supervisor a regra de leitura já devolve a base inteira), e a lista completa de cidades pelo leitor paginado `getTodasCidades()`. Usar o leitor paginado é obrigatório: uma consulta direta à tabela de cidades trunca em 1000 linhas e faz a validação rejeitar cidade real, bug já corrigido uma vez neste projeto. Falha em qualquer uma dessas leituras vira erro do lote inteiro, de propósito. Depois: anotar o lote, rodar `findDuplicates` (de `lib/importacao/dedupe.ts`, reusada verbatim — ATIVO-04) sobre as razões sociais resolvidas contra as existentes, e montar o resultado com precedência erro sobre duplicado sobre ok. O texto do motivo de duplicado segue o mesmo formato já usado na importação antiga, citando o nome parecido.
- `confirmarLoteAtivos(linhas, decisoes)`: mesma abertura de sessão/papel. Reler as razões sociais existentes (a mesma consulta estreita) e chamar `planConfirmacao` de `lib/importacao/confirmar.ts` — reusada verbatim, não copiada: ela já implementa exatamente a política exigida (linha com erro é pulada por cada motivo; linha duplicada segue a decisão por linha, com pular como padrão; linha marcada explicitamente para importar nunca é reexcluída pela reconferência; linha "ok" que virou duplicada entre revisar e confirmar é pulada). Se não sobrou nada para gravar, devolver zero importados e os grupos de puladas, sem tocar no banco. Senão, chamar `importar_clientes_ativos_lote` EXATAMENTE uma vez, passando a carga inteira (nunca um laço linha a linha), reconciliar o retorno com `reconciliarAtivos`, fundir os grupos de motivos e invalidar o cache da tela de clientes. Erro da RPC vira código genérico, sem gravação parcial reportada como sucesso.

Criar `tests/importacao/confirmarAtivo.test.ts` cobrindo os comportamentos declarados acima com dados fabricados (módulo puro, sem Supabase). As duas ações de servidor não são invocáveis diretamente pelo executor de testes (dependem de um escopo de requisição do Next), mesma limitação já registrada no projeto — a cobertura delas vem do checkpoint humano do plano 25-03.
  </action>
  <verify>
    <automated>npx vitest run tests/importacao/confirmarAtivo.test.ts tests/importacao/confirmar.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Os quatro arquivos existem/foram editados; o teste novo passa; `confirmar.test.ts` passa SEM edição (prova de que a mudança em `confirmar.ts` foi só de visibilidade de exportação); a verificação de tipos do projeto inteiro passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → ação de servidor | As linhas mapeadas e o mapa de decisões por linha são dado enviado pelo cliente, nunca entrada de autorização |
| ação de servidor → Postgres | A trava de papel na ação é conforto de interface; a trava real é a da RPC (plano 25-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-07 | Elevation of Privilege | `validarLoteAtivos` / `confirmarLoteAtivos` | high | mitigate | Checagem de sessão e de papel de supervisor ANTES de qualquer leitura de dado, mesma ordem já usada nas ações irmãs; a trava real permanece dentro da RPC |
| T-25-08 | Tampering | Injeção de fórmula por célula de planilha | medium | mitigate | `sanitizeCell` aplicada a toda célula definida antes de qualquer resolução ou comparação, em `annotarLinhaAtivo` |
| T-25-09 | Spoofing | Identificador de responsável/categoria/produto vindo do navegador | medium | mitigate | Os identificadores da carga são sempre os que a validação resolveu contra o banco; nenhuma célula crua vira identificador |
| T-25-10 | Information Disclosure | Consulta de duplicados devolvendo clientes de outros vendedores | low | accept | A consulta é estreita (só razão social) e escopada pela regra de leitura; para o Supervisor, ver a base inteira É o comportamento exigido pela detecção de duplicado |
| T-25-11 | Tampering | Divergência entre a obrigatoriedade da tela e a do banco | medium | mitigate | Uma lista única (`SYSTEM_FIELDS_ATIVO`) decide obrigatoriedade e frase; o subconjunto exigido pelo banco é um subconjunto declarado dela, com a justificativa escrita no cabeçalho |
| T-25-SC | Tampering | instalações npm/pip/cargo | high | accept | Nenhum pacote novo neste plano — só módulos e bibliotecas já presentes em `package.json` |
</threat_model>

<verification>
1. `npx vitest run tests/importacao/` — toda a pasta de testes de importação VERDE (os arquivos antigos provam ausência de regressão nas duas importações existentes).
2. `npx tsc --noEmit` — sem erro de tipo.
3. `npm run lint` — sem aviso novo.
4. Conferência manual de leitura: `lib/importacao/annotarLinhaAtivo.ts` e `lib/importacao/confirmarAtivo.ts` não importam nada de `@/lib/supabase/` e não têm diretiva de servidor/cliente.
</verification>

<success_criteria>
- [ ] Linha com campo obrigatório em branco vira erro só daquela linha, com frase que nomeia o campo (ATIVO-03)
- [ ] Frase de campo faltando vem da MESMA lista que decide a obrigatoriedade — não há segunda fonte (risco registrado na STATE.md fechado)
- [ ] Duplicado (contra o banco e dentro do próprio lote) é sinalizado antes de gravar, reusando `findDuplicates` (ATIVO-04)
- [ ] Confirmar chama `importar_clientes_ativos_lote` uma única vez com o lote inteiro
- [ ] Cada linha pulada tem um motivo agrupado e legível, inclusive as recusadas pelo banco no momento da gravação
- [ ] Frequência de visita não aparece em nenhum ponto do vocabulário nem da carga (ATIVO-01)
- [ ] Testes antigos de importação passam sem edição
</success_criteria>

<output>
Criar `.planning/phases/25-importa-o-de-clientes-ativos/25-02-SUMMARY.md` ao terminar.
</output>
