---
phase: 25-importa-o-de-clientes-ativos
plan: 03
type: execute
wave: 3
depends_on: ["25-02"]
files_modified:
  - components/importacao/AtivoPreviewTable.tsx
  - components/importacao/AtivoImportSummary.tsx
  - components/importacao/AtivoImportWizard.tsx
  - app/(app)/clientes/importar-ativos/page.tsx
  - components/layout/AppSidebar.tsx
  - tests/importacao/ativo-preview-table.test.tsx
  - tests/importacao/ativo-import-summary.test.tsx
  - tests/importacao/AppSidebar.test.tsx
autonomous: false
requirements: [ATIVO-01, ATIVO-02, ATIVO-03, ATIVO-04]

must_haves:
  truths:
    - "O Supervisor vê uma entrada nova no menu, 'Importar Clientes Ativos', que leva a uma tela própria; o Vendedor não vê essa entrada e é redirecionado se tentar a rota direto"
    - "A tela tem os mesmos 3 passos das outras importações — enviar planilha, mapear colunas, revisar — e o botão de continuar do passo 2 fica travado enquanto faltar mapear um campo obrigatório (ATIVO-01)"
    - "Na revisão, cada linha aparece com OK, erro (com o motivo) ou possível duplicado, e uma linha ruim nunca impede as boas (ATIVO-03/ATIVO-04)"
    - "Uma linha marcada como possível duplicado pode ser importada mesmo assim ou pulada, por decisão do Supervisor, antes de gravar (ATIVO-04)"
    - "Depois de confirmar, a tela mostra quantos clientes ativos entraram, quantas linhas foram puladas e por quê, e avisa que a frequência de visita ainda precisa ser definida individualmente"
    - "Um cliente importado por esta tela aparece na coluna final do funil com situação de ganho (ATIVO-02)"
    - "As entradas antigas 'Importar clientes', 'Importar CNPJ' e 'Importar frequências' continuam exatamente como estavam"
  artifacts:
    - components/importacao/AtivoImportWizard.tsx
    - components/importacao/AtivoPreviewTable.tsx
    - components/importacao/AtivoImportSummary.tsx
    - app/(app)/clientes/importar-ativos/page.tsx
  key_links:
    - "O assistente passa `SYSTEM_FIELDS_ATIVO` para as três funções genéricas de mapeamento e para a tabela de mapeamento — sem isso o passo 2 ofereceria e exigiria a lista antiga de campos"
    - "As decisões por linha vivem no assistente (não na tabela de revisão), porque a ação de confirmar precisa lê-las"
    - "A entrada nova de menu fica na seção de Administração, depois das três existentes, com ícone distinto dos três já usados"
---

<objective>
Entregar a tela "Importar Clientes Ativos": assistente de 3 passos, tabela de revisão própria, tela de conclusão própria, rota protegida e entrada nova no menu — fechando os quatro requisitos da fase de ponta a ponta.

Purpose: é o único plano da fase que o dono do projeto consegue enxergar e usar. Tudo que os planos 25-01 e 25-02 construíram só vira valor aqui.

Output: 3 componentes novos, 1 rota nova, 1 entrada de menu, 3 arquivos de teste, e uma verificação humana de ponta a ponta com a planilha real da equipe.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-importa-o-de-clientes-ativos/25-02-SUMMARY.md

# Molde funcional (assistente que CRIA clientes, com decisão por linha em duplicado)
@components/importacao/ImportWizard.tsx
@components/importacao/ImportPreviewTable.tsx
@components/importacao/ImportSummary.tsx

# Molde estrutural do conjunto de arquivos irmão (planilha de CNPJ em massa)
@components/importacao/CnpjImportWizard.tsx
@app/(app)/clientes/importar-cnpj/page.tsx

# Peças compartilhadas já parametrizadas por lista de campos
@components/importacao/ColumnMappingTable.tsx
@lib/importacao/preview.ts
@components/layout/AppSidebar.tsx
</context>

<source_audit>
Ver a auditoria de cobertura completa (GOAL / REQ / RESEARCH / CONTEXT) em `25-01-PLAN.md`, seção `<source_audit>` — ela vale para os três planos da fase. Este plano é o que fecha funcionalmente ATIVO-01, ATIVO-02, ATIVO-03 e ATIVO-04 na interface.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tabela de revisão e tela de conclusão da importação de ativos</name>
  <files>components/importacao/AtivoPreviewTable.tsx, components/importacao/AtivoImportSummary.tsx, tests/importacao/ativo-preview-table.test.tsx, tests/importacao/ativo-import-summary.test.tsx</files>
  <behavior>
    - colunas: a tabela de revisão mostra, por linha, o número da linha, a razão social, o CNPJ, cidade e sigla de estado, a situação e a coluna de ação.
    - resumo: acima da tabela aparece uma frase com o total de linhas e a quebra entre válidas, com erro e possíveis duplicados.
    - motivos: uma linha com situação de erro exibe os motivos junto do selo de situação.
    - acaosoduplicado: os botões de importar mesmo assim / pular aparecem SÓ em linha com situação de possível duplicado; linha válida e linha com erro não têm ação.
    - decisaocontrolada: clicar em importar mesmo assim dispara o retorno de mudança de decisão com o índice da linha e a escolha, sem guardar estado próprio.
    - paginacao: com mais de 50 linhas, a tabela mostra a primeira página e os controles de navegação; com 50 ou menos, os controles não aparecem.
    - vazio: lote sem nenhuma linha mostra o estado vazio, nunca uma tabela sem corpo.
    - conclusaocontagem: a tela de conclusão mostra a contagem de clientes ativos importados e a de linhas puladas, e lista cada motivo com a quantidade.
    - conclusaoaviso: a tela de conclusão sempre exibe o aviso de que a frequência de visita não é definida por esta planilha e precisa ser definida individualmente.
    - conclusaobotoes: os dois botões de ação disparam os retornos recebidos por propriedade.
  </behavior>
  <action>
Criar `components/importacao/AtivoPreviewTable.tsx` como componente de cliente, cópia estrutural de `components/importacao/ImportPreviewTable.tsx` com duas diferenças deliberadas:
- Uma coluna a mais, o CNPJ, entre razão social e cidade/estado. O CNPJ é obrigatório nesta planilha e é o campo que o Supervisor mais precisa conferir visualmente antes de gravar — sem ele na tabela, a revisão de uma planilha de carteira fica cega justamente no dado novo.
- O tipo de linha vem de `ValidatedRowAtivo` (`app/actions/importacaoAtivos.ts`), não da importação antiga.

Tudo o mais é igual e deve ser reusado, nunca reescrito: fatia de 50 linhas por página sobre estado simples de React (o projeto não tem biblioteca de tabela instalada e adicionar uma exigiria um checkpoint de legitimidade de pacote), reinício da paginação quando o conjunto de linhas muda, `summaryCounts` e `statusBorderClass` de `lib/importacao/preview.ts` (nunca um segundo vocabulário de cor de situação), e a decisão por linha como propriedade CONTROLADA (o componente lê e reporta, nunca guarda).

Criar `components/importacao/AtivoImportSummary.tsx` como componente de cliente, cópia estrutural de `components/importacao/ImportSummary.tsx` com três diferenças deliberadas:
- O rótulo do primeiro cartão fala de clientes ATIVOS importados, para o Supervisor não confundir com a importação de prospecção.
- Um bloco de aviso fixo, sempre visível (não condicional), explicando em linguagem simples que esta planilha não define a frequência de visita, que os clientes importados entram como ativos sem recorrência, e que eles vão aparecer na seção de clientes sem dia fixo definido dentro da Agenda até alguém definir a frequência. Este aviso é o elo declarado entre esta fase e a Fase 24 e não pode ser omitido.
- O texto do cabeçalho fala em importação de clientes ativos concluída.

Manter os mesmos dois botões de ação (ver clientes / importar outra planilha) como retornos recebidos por propriedade, e o mesmo tamanho de destaque numérico já usado (o tamanho maior é uma exceção travada só para os cartões do painel, não vale aqui).

Criar `tests/importacao/ativo-preview-table.test.tsx` e `tests/importacao/ativo-import-summary.test.tsx` nos moldes de `tests/importacao/cnpj-preview-table.test.tsx` e `tests/importacao/cnpj-import-summary.test.tsx`. A extensão de arquivo precisa ser a de componente para o ambiente de navegador simulado ser ativado — a configuração do executor de testes restringe esse ambiente a esse padrão de nome.

Aviso de armadilha já registrada no projeto: ao escrever comentários nesses componentes, não repetir literalmente termos que os próprios testes procuram como marca de ausência — a Fase 19 teve um falso positivo exatamente assim. Preferir descrever o conceito.
  </action>
  <verify>
    <automated>npx vitest run tests/importacao/ativo-preview-table.test.tsx tests/importacao/ativo-import-summary.test.tsx tests/importacao/import-summary.test.tsx</automated>
  </verify>
  <done>Os dois componentes e os dois testes novos existem; os testes novos passam; `import-summary.test.tsx` (da importação antiga) passa sem edição.</done>
</task>

<task type="auto">
  <name>Task 2: Assistente de 3 passos, rota protegida e entrada nova no menu</name>
  <files>components/importacao/AtivoImportWizard.tsx, app/(app)/clientes/importar-ativos/page.tsx, components/layout/AppSidebar.tsx, tests/importacao/AppSidebar.test.tsx</files>
  <action>
Criar `components/importacao/AtivoImportWizard.tsx` como componente de cliente, cópia estrutural de `components/importacao/ImportWizard.tsx` (o molde funcional certo, porque este fluxo CRIA clientes e tem decisão por linha em duplicado, ao contrário do fluxo de CNPJ, que só atualiza), trocando exatamente estes pontos:

- Nome do arquivo do modelo baixado: um nome próprio que deixe claro que é a planilha de clientes ativos.
- Gerador do modelo: `buildModeloAtivos()` de `lib/importacao/modeloAtivo.ts`.
- Vocabulário de campos: passar `SYSTEM_FIELDS_ATIVO` para `suggestMapping` (na sugestão inicial por cabeçalho), para `applyMapping`, para `requiredFieldsFaltando` (nas duas chamadas: o aviso do passo 2 e o estado desabilitado do botão de continuar) e para a propriedade de lista de campos de `ColumnMappingTable`. Sem isso o passo 2 ofereceria e exigiria a lista antiga. Seguir o padrão de parametrização já usado em `components/importacao/CnpjImportWizard.tsx`.
- Ações de servidor: `validarLoteAtivos` e `confirmarLoteAtivos` de `app/actions/importacaoAtivos.ts`.
- Componentes de revisão e conclusão: `AtivoPreviewTable` e `AtivoImportSummary` (Task 1).
- Textos: cabeçalho do passo 3, rótulo do botão em andamento e mensagens de erro devem falar de clientes ativos, não de clientes genéricos. A mensagem de falha ao confirmar precisa dizer que nenhum cliente foi criado.

Preservar sem mudança de forma: o estado por preocupação (arquivo, conteúdo lido, mapeamento, linhas mapeadas, linhas validadas, decisões por linha, confirmação), a marcação síncrona da bandeira de carregamento antes de cada chamada assíncrona (padrão do projeto para o rótulo de progresso aparecer já no próximo desenho), o efeito que dispara a validação ao chegar no passo 3 com um lote novo, e o fato de as decisões por linha viverem AQUI e não na tabela — a ação de confirmar precisa recebê-las.

Criar `app/(app)/clientes/importar-ativos/page.tsx`, cópia estrutural de `app/(app)/clientes/importar-cnpj/page.tsx`: componente de servidor assíncrono, duplo redirecionamento (para a tela de login sem sessão, para a raiz quando o papel não for supervisor), título próprio na página e o assistente novo. Registrar em comentário que o redirecionamento é conforto de interface — a trava real é a trava de papel dentro da RPC somada à checagem de papel das duas ações de servidor.

Editar `components/layout/AppSidebar.tsx`: acrescentar UMA entrada nova ao final da lista de links da seção de Administração, apontando para a rota nova, com o rótulo "Importar Clientes Ativos" e um ícone da biblioteca já instalada que seja distinto dos três já usados nas outras importações (documento com seta para cima, documento com números, e setas circulares). Uma escolha adequada é o selo com marca de conferido, que carrega a ideia de cliente já fechado. Registrar em comentário o mesmo raciocínio que as entradas vizinhas já registram: símbolos diferentes porque as telas fazem coisas diferentes.

Não tocar, não renomear e não remover nenhuma das três entradas existentes — a limpeza do menu é escopo declarado da Fase 26 e fazer aqui deixaria o Supervisor sem caminho para regularizar clientes incompletos antes de a substituta estar provada em produção.

Editar `tests/importacao/AppSidebar.test.tsx` acrescentando três casos no molde exato dos casos que já existem para a entrada de CNPJ: a entrada nova aparece para o Supervisor com a rota correta; não aparece para o Vendedor; e aparece DEPOIS da entrada de CNPJ na ordem do documento. Não alterar nenhum caso existente.
  </action>
  <verify>
    <automated>npx vitest run tests/importacao/AppSidebar.test.tsx && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>O assistente, a rota e a entrada de menu existem; os três casos novos do menu passam junto com todos os antigos, sem edição dos antigos; a verificação de tipos e o linter passam sem erro nem aviso novo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — verificação humana de ponta a ponta da importação de clientes ativos</name>
  <action>Pausar a execução e conduzir o dono do projeto pelos 7 blocos de verificação abaixo, na ordem. O bloco 2 (conferência da lista de nove colunas obrigatórias contra a planilha real da equipe) é o de maior valor: um campo obrigatório que a planilha real não tem trava o assistente no passo 2 e inviabiliza a importação. Se o dono pedir mudança nessa lista, aplicá-la em `lib/importacao/typesAtivo.ts` (uma linha por campo) mais o caso de teste correspondente em `tests/importacao/ativos-vocabulario.test.ts`, e repetir o bloco 2.</action>
  <what-built>
A tela "Importar Clientes Ativos" está completa e ligada de ponta a ponta: entrada nova no menu (seção Administração), rota protegida só para Supervisor, assistente de 3 passos com modelo de planilha para baixar, mapeamento de colunas, revisão linha a linha com OK / erro / possível duplicado, decisão por linha nos duplicados, e tela de conclusão com contagens e motivos. A gravação usa a RPC nova, que cria cada cliente já como ativo (situação de ganho, etapa final) sem passar pelas 7 etapas do funil.
  </what-built>
  <how-to-verify>
Rode `npm run dev` e entre com a sua conta de Supervisor.

**1. Menu e acesso**
- No menu da esquerda, seção Administração, confira que existe uma entrada nova "Importar Clientes Ativos", com ícone diferente das outras três, e que as três antigas ("Importar clientes", "Importar frequências", "Importar CNPJ") continuam lá, com os mesmos nomes.
- Clique na entrada nova. Deve abrir a tela com o título de importação de clientes ativos.

**2. Modelo de planilha — CONFIRA A LISTA DE COLUNAS OBRIGATÓRIAS**
- Clique em "Baixar modelo de planilha" e abra o arquivo.
- Devem existir 16 colunas. Nove delas são obrigatórias: Razão social, CNPJ, CEP, Rua, Número, Cidade, Estado, Responsável (vendedor) e Contato.
- **Esta é a pergunta mais importante deste checkpoint:** essa lista de nove bate com a planilha real da equipe (`Listagem_Clientes_por_Vendedor.xlsx`)? Se a planilha real NÃO tiver uma dessas colunas, o assistente vai travar no passo 2 e você não vai conseguir importar. Se faltar alguma, diga qual — trocar um campo de obrigatório para opcional é uma linha de código.
- Se sobrar alguma coluna que você acha que TAMBÉM deveria ser obrigatória (por exemplo Telefone ou Email), diga também.

**3. Caminho feliz + erro por linha + duplicado (ATIVO-01/02/03/04)**
- Monte uma planilha de teste pequena com 4 linhas: (a) uma linha completa com razão social inédita; (b) uma linha sem CNPJ; (c) uma linha com a Cidade em branco; (d) uma linha repetindo a razão social de um cliente que já existe no sistema.
- Envie, avance para o mapeamento (as colunas devem vir pré-associadas sozinhas) e vá para a revisão.
- Esperado: a linha (a) marcada como OK; as linhas (b) e (c) marcadas com erro, cada uma dizendo qual campo faltou; a linha (d) marcada como possível duplicado, com os botões de importar mesmo assim / pular.
- Deixe a (d) como "Pular" e clique em confirmar.
- Esperado na tela de conclusão: 1 cliente ativo importado, 3 linhas puladas, com os motivos listados um a um, e o aviso sobre frequência de visita visível.

**4. O cliente entrou como ATIVO, não no começo do funil (ATIVO-02)**
- Clique em "Ver clientes". O cliente importado deve aparecer na ÚLTIMA coluna do funil ("1ª venda concluída"), marcado como ganho — nunca em "Aguardando contato".
- Abra a ficha dele e confira que os dados da planilha estão lá (endereço, CNPJ, contato, responsável).

**5. O elo com a Agenda (Fase 24)**
- Vá em Agenda, visão de Lista. O cliente importado deve aparecer na seção "Sem dia fixo definido" — ele entrou sem frequência de visita, de propósito, e é assim que o sistema lembra você de definir uma depois.

**6. Vendedor não vê nem entra**
- Saia e entre com uma conta de Vendedor.
- A entrada "Importar Clientes Ativos" NÃO deve aparecer no menu.
- Digite a rota `/clientes/importar-ativos` direto na barra de endereço: você deve ser mandado de volta para a tela inicial.

**7. Nada quebrou nas telas antigas**
- Abra "Importar clientes", "Importar CNPJ" e "Importar frequências". As três devem abrir normalmente, como antes.
  </how-to-verify>
  <resume-signal>Digite "aprovado" se os 7 blocos passaram, ou descreva o que deu errado (e, no bloco 2, quais colunas obrigatórias precisam mudar).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → rota `/clientes/importar-ativos` | Rota adivinhável; o redirecionamento por papel é conforto, não a fronteira real |
| navegador → ações de servidor de ativos | Linhas e decisões por linha são dado enviado pelo cliente |
| menu → visibilidade por papel | Esconder link é enfeite de experiência, nunca autorização |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-12 | Elevation of Privilege | `app/(app)/clientes/importar-ativos/page.tsx` | medium | mitigate | Duplo redirecionamento no componente de servidor (sem sessão e sem papel de supervisor), somado à checagem de papel nas duas ações e à trava dentro da RPC — três camadas, a última sendo a real |
| T-25-13 | Information Disclosure | Entrada nova no menu | low | mitigate | A entrada nova entra na seção de Administração, que já é renderizada só quando o papel é supervisor; caso de teste novo prova que o Vendedor não a vê |
| T-25-14 | Tampering | Decisão por linha (importar mesmo assim) enviada pelo navegador | medium | accept | Só afeta linha marcada como possível duplicado, e a decisão é do próprio Supervisor; a restrição de unicidade de razão social no banco continua sendo a rede final |
| T-25-15 | Denial of Service | Planilha muito grande travando o navegador na revisão | low | mitigate | Tabela de revisão pagina em 50 linhas por página, mesmo comportamento já em produção nos fluxos irmãos |
| T-25-SC | Tampering | instalações npm/pip/cargo | high | accept | Nenhum pacote novo neste plano — componentes e ícones vêm do que já está em `package.json` |
</threat_model>

<verification>
1. `npx vitest run tests/importacao/` — pasta inteira VERDE.
2. `npx tsc --noEmit` e `npm run lint` — sem erro nem aviso novo.
3. `npx playwright test e2e/importar-guard.spec.ts e2e/importar-frequencias-guard.spec.ts` — os dois roteiros antigos continuam válidos (esta fase não muda as rotas antigas). Se falharem por causa das contas de teste apagadas, registrar no SUMMARY como bloqueio herdado, nunca como regressão desta fase.
4. Checkpoint humano aprovado (Task 3).
5. Ao marcar os requisitos em `REQUIREMENTS.md`: ATIVO-01..04 só passam a Completo DEPOIS deste plano — a mesma convenção multi-plano já travada nas Fases 9, 19, 20 e 22.
</verification>

<success_criteria>
- [ ] Supervisor importa uma planilha de ativos e os clientes válidos entram direto como ganho, na etapa final, sem passar pelo funil (ATIVO-01/ATIVO-02)
- [ ] Linha com dado obrigatório faltando vira erro só daquela linha, na mesma tela de revisão das outras importações (ATIVO-03)
- [ ] Possível duplicado é sinalizado na revisão, com decisão por linha, antes de gravar (ATIVO-04)
- [ ] A tela de conclusão avisa que a frequência de visita ainda precisa ser definida individualmente
- [ ] Vendedor não vê a entrada no menu e não consegue abrir a rota
- [ ] As três entradas antigas de importação continuam intactas (limpeza é da Fase 26)
- [ ] Checkpoint humano aprovado, incluindo a conferência da lista de colunas obrigatórias contra a planilha real da equipe
</success_criteria>

<output>
Criar `.planning/phases/25-importa-o-de-clientes-ativos/25-03-SUMMARY.md` ao terminar.
</output>
