---
phase: 26-importa-o-de-clientes-em-prospec-o-e-limpeza-de-menu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0028_remover_atualizar_cnpj_e_frequencia_lote.sql
  - tests/importacao/rls-cnpj-lote.test.ts
  - tests/importacao/rls-frequencia-lote.test.ts
autonomous: false
requirements: [MENU-01, MENU-02]

must_haves:
  truths:
    - "Nenhum usuário (nem Supervisor, nem Vendedor) consegue mais chamar `atualizar_cnpj_lote` ou `atualizar_frequencia_visita_lote` pela API — as duas funções deixam de existir no banco (MENU-01/MENU-02, 'removida do sistema por completo' inclui a camada de banco, decisão do dono do projeto)."
    - "A importação de clientes em prospecção (`importar_clientes_lote`) e a importação de clientes ativos (`importar_clientes_ativos_lote`, Fase 25) continuam funcionando exatamente como antes do push."
    - "A suíte de testes não contém mais nenhum arquivo que exercite as duas funções removidas."
  artifacts:
    - "supabase/migrations/0028_remover_atualizar_cnpj_e_frequencia_lote.sql — migration nova, aditiva, contendo exatamente duas instruções de remoção de função e nenhuma instrução de criação"
  key_links:
    - "A remoção usa a assinatura completa (`p_atualizacoes jsonb`) de cada função — remoção sem assinatura é ambígua e pode atingir uma sobrecarga errada (mesma disciplina já documentada na migration 0018)."
    - "Os dois testes de integração ao vivo removidos são os ÚNICOS arquivos de teste que citam essas funções (grep exaustivo confirmado na pesquisa) — deixá-los no repositório transformaria o push numa quebra permanente da suíte."
---

<objective>
Remover do banco de dados as duas funções de gravação em massa que ficaram órfãs quando a Fase 25 entregou a planilha de "Importar Clientes Ativos": `atualizar_cnpj_lote` (migration 0020) e `atualizar_frequencia_visita_lote` (migration 0017).

Purpose: MENU-01 e MENU-02 exigem que "Importar CNPJ" e "Importar frequências" saiam do sistema **por completo**. O dono do projeto confirmou explicitamente que isso inclui apagar as duas funções do banco, não apenas esconder as telas. Enquanto elas existirem, permanecem chamáveis diretamente pela API mesmo sem tela nenhuma apontando para elas.

Output: uma migration nova (0028) com exatamente duas remoções de função, os dois testes de integração ao vivo dessas funções removidos, e a confirmação humana de que o push chegou em produção sem levar junto nenhuma função ainda em uso.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-importa-o-de-clientes-em-prospec-o-e-limpeza-de-menu/26-RESEARCH.md

# Assinaturas exatas a remover (ler para confirmar antes de escrever a migration)
@supabase/migrations/0017_atualizar_frequencia_visita_lote.sql
@supabase/migrations/0020_atualizar_cnpj_lote.sql

# Convenção de migration deste projeto (cabeçalho comentado + rastro para o plano)
@supabase/migrations/0023_endereco_opcional_na_importacao.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0028 — remover as duas funções de gravação em massa órfãs</name>
  <files>supabase/migrations/0028_remover_atualizar_cnpj_e_frequencia_lote.sql</files>
  <action>
Antes de escrever: abrir `supabase/migrations/0017_atualizar_frequencia_visita_lote.sql` e `supabase/migrations/0020_atualizar_cnpj_lote.sql` e confirmar a assinatura de cada função na linha de declaração. As duas recebem um único parâmetro `p_atualizacoes jsonb`.

Criar o arquivo novo `supabase/migrations/0028_remover_atualizar_cnpj_e_frequencia_lote.sql` contendo, na parte executável, EXATAMENTE duas instruções e nada mais:

- remoção condicional (`drop function if exists`) de `atualizar_cnpj_lote` com a assinatura completa `(p_atualizacoes jsonb)`
- remoção condicional (`drop function if exists`) de `atualizar_frequencia_visita_lote` com a assinatura completa `(p_atualizacoes jsonb)`

Regras não negociáveis para este arquivo:

- Sempre com a assinatura entre parênteses. Remoção sem assinatura é ambígua e pode atingir uma sobrecarga que não é a pretendida — mesma disciplina já documentada na migration 0018 deste projeto.
- Sempre `if exists`, para o push não abortar se alguém já tiver removido a função manualmente.
- Zero instrução de criação de qualquer tipo (função, tabela, política, tipo). Esta migration só remove (MENU-01/MENU-02).
- Nenhuma outra função pode ser citada na parte executável do arquivo. Em particular, as duas RPCs de importação que continuam em produção (a de prospecção, migration 0019, e a de ativos, migration 0027) NÃO podem aparecer em nenhuma instrução — só no comentário de cabeçalho, como referência do que ficou intacto. <!-- planner-discipline-allow: importar_clientes -->
- Nunca editar 0017 nem 0020 (migrations já aplicadas; convenção do projeto em `CLAUDE.md`: só migration nova).
- Toda linha de comentário deste arquivo precisa começar com dois hifens no início da linha (sem bloco `/* */`), porque as checagens mecânicas deste plano filtram comentários por esse prefixo.

Cabeçalho comentado, na forma dos outros arquivos de `supabase/migrations/` (ver 0023 como molde), explicando em português: por que as duas funções ficaram órfãs (a planilha de "Importar Clientes Ativos" da Fase 25 substituiu o caso de uso das duas planilhas avulsas), que a decisão de apagá-las do banco — e não só esconder as telas — foi tomada explicitamente pelo dono do projeto, quais funções continuam intocadas, e a linha `-- Source: .planning/phases/26-importa-o-de-clientes-em-prospec-o-e-limpeza-de-menu/26-01-PLAN.md`.

Não rodar `supabase db push` nesta task — o push é a Task 3, feita pelo dono do projeto.
  </action>
  <verify>
    <automated>M=supabase/migrations/0028_remover_atualizar_cnpj_e_frequencia_lote.sql; test -f "$M" && B=$(grep -vE '^\s*--' "$M"); [ "$(printf '%s\n' "$B" | grep -c 'drop function if exists')" = "2" ] && [ "$(printf '%s\n' "$B" | grep -c 'p_atualizacoes jsonb')" = "2" ] && [ "$(printf '%s\n' "$B" | grep -ciE '\bcreate\b')" = "0" ] && [ "$(printf '%s\n' "$B" | grep -c 'importar_clientes')" = "0" ] && [ "$(printf '%s\n' "$B" | grep -c 'atualizar_cnpj_lote')" = "1" ] && [ "$(printf '%s\n' "$B" | grep -c 'atualizar_frequencia_visita_lote')" = "1" ] && echo GATE_OK</automated>
  </verify>
  <done>O arquivo 0028 existe; sua parte executável tem exatamente 2 remoções condicionais de função, ambas com a assinatura `(p_atualizacoes jsonb)`, cada função citada uma única vez, zero instruções de criação e zero menções às RPCs de importação que continuam em uso. As migrations 0017 e 0020 permanecem byte a byte inalteradas (`git diff --stat` não as lista).</done>
</task>

<task type="auto">
  <name>Task 2: Remover os dois testes de integração ao vivo das funções apagadas</name>
  <files>tests/importacao/rls-cnpj-lote.test.ts, tests/importacao/rls-frequencia-lote.test.ts</files>
  <action>
Apagar os dois arquivos de teste que exercitam diretamente as funções removidas na Task 1, usando `git rm`:

- `tests/importacao/rls-cnpj-lote.test.ts`
- `tests/importacao/rls-frequencia-lote.test.ts`

Esses são os únicos arquivos de teste do repositório que chamam essas duas funções (grep exaustivo confirmado na pesquisa da fase, seção "Full Deletion Inventory"). Eles testam apenas as funções apagadas — não há nada neles a preservar em outro lugar. Mantê-los transformaria o push da Task 3 numa quebra permanente da suíte.

Não apagar nenhum outro arquivo nesta task. Os demais arquivos das duas telas antigas (rotas, componentes, ações de servidor, vocabulários, testes de unidade) são responsabilidade do plano 26-04 — apagar qualquer um deles aqui deixaria o projeto sem compilar entre os dois planos.

Depois de apagar, confirmar por busca que nenhum outro arquivo dentro de `tests/` cita as duas funções.
  </action>
  <verify>
    <automated>[ ! -f tests/importacao/rls-cnpj-lote.test.ts ] && [ ! -f tests/importacao/rls-frequencia-lote.test.ts ] && [ "$(grep -rl 'atualizar_cnpj_lote' tests/ 2>/dev/null | wc -l)" = "0" ] && [ "$(grep -rl 'atualizar_frequencia_visita_lote' tests/ 2>/dev/null | wc -l)" = "0" ] && [ -f app/actions/importacaoCnpj.ts ] && [ -f app/actions/importacaoFrequencia.ts ] && echo GATE_OK</automated>
  </verify>
  <done>Os dois arquivos de teste não existem mais; nenhum arquivo dentro de `tests/` cita as funções removidas; os arquivos das telas antigas (ações de servidor de CNPJ e de frequências) continuam no lugar, intocados, para o plano 26-04 removê-los.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Checkpoint — o dono do projeto aplica a migration 0028 em produção</name>
  <files>supabase/migrations/0028_remover_atualizar_cnpj_e_frequencia_lote.sql</files>
  <action>
Parar a execução e apresentar ao dono do projeto as instruções abaixo. Não tentar rodar `supabase db push` pelo assistente — a convenção deste projeto (Fases 18, 19, 24 e 25) é que toda escrita de schema em produção é feita pelo próprio dono. Só retomar depois do sinal de retomada.
  </action>
  <what-built>
A migration `0028_remover_atualizar_cnpj_e_frequencia_lote.sql` está escrita e commitada, removendo do banco as duas funções de gravação em massa que sobraram sem tela: a de CNPJ em massa e a de frequência de visita em massa. Os dois testes que dependiam delas foram apagados.
  </what-built>
  <how-to-verify>
Este passo precisa de você porque é uma escrita em produção — o assistente não aplica migration sozinho neste projeto (mesmo padrão das Fases 18, 19, 24 e 25).

1. No terminal, na pasta do projeto, garanta o CLI do Supabase na versão fixada pelo projeto (`2.111.0` — versões 2.112.0+ têm um bug conhecido de validação de schema que já derrubou push antes neste projeto).
2. Rode `supabase db push`.
3. Confirme que a saída aplica a migration `0028` e nenhuma outra.
4. Depois do push, no SQL Editor do painel do Supabase, rode esta consulta e confira o resultado:

   `select proname from pg_proc where proname in ('atualizar_cnpj_lote','atualizar_frequencia_visita_lote','importar_clientes_lote','importar_clientes_ativos_lote') order by proname;`

   O resultado ESPERADO tem exatamente 2 linhas: `importar_clientes_ativos_lote` e `importar_clientes_lote`. Se aparecer qualquer uma das duas primeiras, o push não aplicou. Se FALTAR alguma das duas últimas, PARE e avise — significa que a migration removeu algo que ainda está em uso, e precisamos reverter antes de qualquer outra coisa.
5. Abra o sistema no navegador e confirme que a tela "Importar Clientes Ativos" (menu Administração) ainda abre normalmente. Ela ainda está no menu neste momento — a limpeza do menu é o plano 26-04.
  </how-to-verify>
  <resume-signal>Escreva "aprovado" se a consulta devolveu exatamente as 2 linhas esperadas, ou descreva o que apareceu de diferente.</resume-signal>
  <done>A migration 0028 foi aplicada em produção pelo dono do projeto; a consulta de catálogo devolveu exatamente as duas funções de importação que continuam em uso, e nenhuma das duas funções removidas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente autenticado → PostgREST/RPC | Qualquer usuário autenticado pode chamar qualquer função exposta pelo PostgREST, mesmo sem tela apontando para ela |
| repositório → banco de produção | O `supabase db push` é a única porta de escrita de schema; roda com credencial de administrador |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Elevation of Privilege | `atualizar_cnpj_lote` / `atualizar_frequencia_visita_lote` expostas pelo PostgREST sem nenhuma tela | medium | mitigate | Task 1 remove as duas funções do banco; superfície chamável deixa de existir (hoje elas já checam `is_supervisor()`, então o risco residual era baixo — a remoção fecha a superfície morta em vez de confiar só no guard) |
| T-26-02 | Denial of Service | Migration 0028 removendo por engano uma função ainda em uso (importação de prospecção ou de ativos) | high | mitigate | As remoções carregam a assinatura completa `(p_atualizacoes jsonb)`; a checagem mecânica da Task 1 exige exatamente 2 remoções, zero criações e zero menções às RPCs de importação na parte executável; o checkpoint humano (Task 3) confere por consulta ao catálogo que as duas RPCs de importação continuam existindo depois do push |
| T-26-03 | Tampering | Edição de migration já aplicada (0017/0020) em vez de migration nova | medium | mitigate | A ação da Task 1 proíbe editar 0017/0020 explicitamente; o critério de conclusão exige que `git diff --stat` não liste esses dois arquivos |
| T-26-SC | Tampering | instalações npm/pip/cargo | low | accept | Esta fase não instala nenhum pacote (pesquisa da fase: "Package Legitimacy Audit is not applicable — no packages are installed in this phase"); nenhum checkpoint de legitimidade é necessário |
</threat_model>

<verification>
1. `git diff --stat` mostra exatamente um arquivo novo (a migration 0028) e dois arquivos apagados (os dois testes de integração ao vivo) — nenhum outro arquivo tocado.
2. As migrations 0017, 0019, 0020 e 0027 não aparecem em `git diff --stat`.
3. Depois do push (Task 3), a consulta ao catálogo do Postgres devolve exatamente `importar_clientes_ativos_lote` e `importar_clientes_lote` para a lista das 4 funções conferidas.
4. `npx vitest run tests/importacao/ --reporter=basic` não reclama de arquivo de teste faltando (os dois removidos simplesmente não aparecem mais na lista).

Observação sobre a suíte: conforme os Blockers em `.planning/STATE.md`, as contas semente de teste (`vendedor.a+test` / `vendedor.b+test`) foram apagadas, então todo teste de integração contra o banco ao vivo já falha hoje por "Invalid login credentials" — isso é anterior a este plano e não deve ser tratado como regressão dele. Este plano não acrescenta nenhum teste que dependa dessas contas.
</verification>

<success_criteria>
- A migration 0028 existe, remove exatamente as duas funções órfãs pela assinatura completa e não cria nada.
- Os dois testes de integração ao vivo dessas funções foram removidos, e nenhum arquivo em `tests/` cita mais as duas funções.
- O push foi aplicado em produção pelo dono do projeto e a conferência de catálogo confirmou: as duas funções órfãs sumiram; a importação de prospecção e a de ativos continuam existindo.
- Nenhum arquivo de tela/rota/ação das duas planilhas antigas foi tocado por este plano (isso é do plano 26-04).
</success_criteria>

<output>
Create `.planning/phases/26-importa-o-de-clientes-em-prospec-o-e-limpeza-de-menu/26-01-SUMMARY.md` when done
</output>
