---
phase: 20-calend-rio-da-agenda-m-s-semana-e-dia
plan: 04
type: execute
wave: 3
depends_on: ["20-01", "20-02", "20-03"]
files_modified:
  - components/agenda/AgendaCalendarioToolbar.tsx
  - components/agenda/AgendaCalendario.tsx
  - tests/agenda/agenda-calendario-toolbar.test.tsx
  - tests/agenda/agenda-calendario.test.tsx
autonomous: true
requirements: [AGD-08, AGD-09, AGD-11]

must_haves:
  truths:
    - "AGD-08 (D-02): a barra de ferramentas tem um seletor segmentado com quatro opções — Lista, Dia, Semana, Mês — e a opção ativa é visualmente distinta das outras três"
    - "AGD-08 (D-02): a barra tem botão de voltar, rótulo do período, botão de avançar e um botão 'Hoje'; clicar em avançar/voltar move exatamente um dia, uma semana ou um mês conforme o modo ativo"
    - "AGD-08: clicar em 'Hoje' devolve a referência para a data de hoje, de qualquer modo e de qualquer distância"
    - "AGD-08 (D-09): não existe mini-calendário lateral — a barra de ferramentas simples da Variante A é o que ficou"
    - "AGD-08: na visão de Lista a barra mostra só o seletor de visão; navegação de data e legenda não aparecem, porque não significam nada para a Lista"
    - "AGD-09 (D-03): clicar num dia da grade de mês abre um diálogo com a lista completa daquele dia, montada com o MESMO componente de dia do plano 20-02 — nenhuma lista de item nova"
    - "AGD-11: o modo Dia mostra o cabeçalho com a data por extenso e, abaixo, a lista de itens daquele dia"
    - "O agrupamento por data é calculado UMA vez, sobre a lista recebida, e é o mesmo consumido pelas três visões e pelo diálogo do dia"
    - "O contêiner não busca dado nem chama ação de servidor — recebe a lista já buscada e já estreitada, e repassa para cima os dois pedidos de ação (abrir ficha do cliente, concluir item)"
    - "A legenda explica as três marcas visuais (prospecção, visita, atrasado) usando os mesmos símbolos de cor das visões"
  artifacts:
    - "components/agenda/AgendaCalendarioToolbar.tsx — seletor de visão, navegação de data, botão Hoje e legenda"
    - "components/agenda/AgendaCalendario.tsx — contêiner que guarda a data de referência e o dia do diálogo, calcula o agrupamento uma vez e escolhe qual visão renderizar"
    - "tests/agenda/agenda-calendario-toolbar.test.tsx e tests/agenda/agenda-calendario.test.tsx"
  key_links:
    - "a barra de ferramentas precisa continuar montada mesmo quando a visão ativa é a Lista — se ela sumir junto com a grade, o usuário entra no Calendário e não tem como voltar"
    - "o agrupamento por data precisa ser calculado uma vez, no contêiner, e passado às três visões; recalcular dentro de cada visão abre a porta para uma delas receber um conjunto diferente do da outra"
    - "o diálogo do dia precisa reusar o componente de dia do plano 20-02, que por sua vez reusa o cartão da Lista — é a cadeia inteira da decisão D-05, e quebrá-la em qualquer elo produz um segundo cartão de item no projeto"
    - "os passos de navegação precisam vir da função de navegação do plano 20-01, para que o passo de mês seja mês (e não 30 dias) e o de semana seja semana"
    - "a data de referência é estado do calendário, não da tela — mantê-la aqui é o que permite ao plano 20-05 acrescentar o alternador sem herdar controle de data"
---

<objective>
Montar a barra de ferramentas da Variante A (seletor de visão + navegação de data + botão "Hoje" + legenda) e o contêiner que amarra as três visões: guarda a data de referência, calcula o agrupamento por data uma única vez, escolhe qual visão renderizar e abre o diálogo com a lista completa do dia quando alguém clica numa célula da grade de mês.

Purpose: até aqui existem três visões soltas que não sabem navegar. Este plano é o que transforma peças em calendário — e é onde a decisão D-03 fecha o ciclo: o clique num dia da grade de mês abre a lista completa daquele dia usando o mesmo componente da visão de Dia, que por sua vez usa o mesmo cartão da Lista. Um cartão de item no projeto inteiro, três lugares que o mostram.

Output: dois componentes de cliente novos e dois arquivos de teste. Ainda sem tocar em `AgendaList.tsx` — a fiação na tela é o plano 20-05.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@components/agenda/AgendaList.tsx
@components/agenda/ConcluirItemDialog.tsx
@lib/agenda/itens.ts
@.planning/sketches/003-agenda-calendario/index.html
@.planning/phases/20-calend-rio-da-agenda-m-s-semana-e-dia/20-01-PLAN.md
@.planning/phases/20-calend-rio-da-agenda-m-s-semana-e-dia/20-02-PLAN.md
@.planning/phases/20-calend-rio-da-agenda-m-s-semana-e-dia/20-03-PLAN.md
</context>

<interface_context>
**Consumido** (plano 20-01, `lib/agenda/itens.ts`):

```ts
export type CalendarioModo = "dia" | "semana" | "mes"
export type AgendaVisao = "lista" | CalendarioModo
export function agruparPorData(itens: AgendaItem[]): Map<string, AgendaItem[]>
export function itensDoDia(porData: Map<string, AgendaItem[]>, dia: Date): AgendaItem[]
export function navegarData(referencia: Date, modo: CalendarioModo, passo: 1 | -1): Date
export function rotuloDoPeriodo(referencia: Date, modo: CalendarioModo): string
```

**Consumido** (planos 20-02 e 20-03):

```tsx
export function AgendaCalendarioDia(props: {
  itens: AgendaItem[]; showResponsavel: boolean
  onOpen: (item: AgendaItem) => void; onConcluir: (item: AgendaItem) => void; now?: Date
}): JSX.Element

export function AgendaCalendarioSemana(props: {
  referencia: Date; porData: Map<string, AgendaItem[]>
  onOpenItem: (item: AgendaItem) => void; now?: Date
}): JSX.Element

export function AgendaCalendarioMes(props: {
  referencia: Date; porData: Map<string, AgendaItem[]>
  onSelecionarDia: (dia: Date) => void; now?: Date
}): JSX.Element
```

**Publicado por este plano** — o plano 20-05 consome exatamente isto:

```tsx
// components/agenda/AgendaCalendarioToolbar.tsx
export function AgendaCalendarioToolbar(props: {
  visao: AgendaVisao
  onVisaoChange: (visao: AgendaVisao) => void
  rotulo: string | null              // null na visão de Lista → nav e legenda não aparecem
  onAnterior: () => void
  onProximo: () => void
  onHoje: () => void
}): JSX.Element

// components/agenda/AgendaCalendario.tsx
export function AgendaCalendario(props: {
  visao: AgendaVisao
  onVisaoChange: (visao: AgendaVisao) => void
  itens: AgendaItem[]                       // JÁ estreitados pelo filtro de vendedor
  showResponsavel: boolean
  onOpenCliente: (clienteId: string) => void
  onConcluirItem: (item: AgendaItem) => void
  now?: Date
}): JSX.Element
```

Contrato de composição do contêiner: ele renderiza **sempre** a barra de ferramentas, e a grade só quando a visão não é a Lista. Quando a visão é a Lista, ele devolve apenas a barra — quem desenha a Lista continua sendo `AgendaList.tsx`, logo abaixo (plano 20-05).

**Primitivos de interface já instalados e usados aqui** (nada a instalar): `Button` (variantes `default`/`outline`/`ghost`, tamanhos `sm`/`icon`), `Dialog` com `DialogContent`/`DialogHeader`/`DialogTitle`, e os ícones de seta esquerda/direita da biblioteca de ícones já presente.
</interface_context>

<source_audit>
Ver `<source_audit>` completo em `20-01-PLAN.md`. Este plano cobre G2, G3 (diálogo do dia), AGD-08, AGD-09 (abertura da lista do dia), AGD-11 (cabeçalho da visão de dia), D-02, D-03, D-07 e D-09.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Barra de ferramentas — seletor de visão, navegação de data, botão Hoje e legenda</name>
  <files>components/agenda/AgendaCalendarioToolbar.tsx, tests/agenda/agenda-calendario-toolbar.test.tsx</files>
  <read_first>
    - `.planning/sketches/003-agenda-calendario/index.html`, Variante A: as classes `.toolbar`, `.segmented`, `.segmented button.active`, `.date-nav`, `.date-nav .label`, `.today-btn` e `.legend`/`.dot` — a referência visual. Reparar na ORDEM dos quatro botões do seletor no bloco `#a-view-switch` (Lista, Dia, Semana, Mês) e no fato de a navegação de data ficar à esquerda e o seletor à direita, com quebra de linha permitida.
    - **Variante B foi rejeitada (D-09):** nada de mini-calendário lateral. Ignorar todo o bloco `#variant-b` do esboço.
    - `components/ui/button.tsx` — as variantes e tamanhos realmente disponíveis neste projeto (o seletor segmentado é montado com botões, não existe primitivo de segmentado aqui).
    - `components/agenda/AgendaList.tsx`, o cabeçalho da tela (linhas ~268-303) — para saber onde esta barra vai encaixar depois e não duplicar título nem filtro.
    - `app/globals.css` — os símbolos de cor. O esboço traz valores crus **que não devem ser copiados**.
  </read_first>
  <behavior>
    - Renderiza quatro botões de visão, na ordem Lista, Dia, Semana, Mês, cada um com o rótulo correspondente.
    - O botão da visão ativa é distinguível dos outros três (variante visual diferente) e anuncia seu estado de seleção de forma acessível.
    - Clicar num botão de visão dispara o retorno de chamada com o valor daquela visão.
    - Quando o rótulo do período é nulo (visão de Lista), a navegação de data e a legenda NÃO são renderizadas, e o seletor de visão continua visível.
    - Quando há rótulo, aparecem: botão de voltar, o rótulo, botão de avançar e o botão "Hoje".
    - Cada um dos três botões de navegação dispara o retorno de chamada correspondente, e nenhum dispara outro.
    - Os botões de seta têm rótulo acessível (não dependem só do ícone).
    - A legenda mostra três marcas — prospecção, visita e atrasado — com um ponto colorido cada.
  </behavior>
  <action>
    Escrever os casos de teste primeiro (RED), depois o componente.

    Criar `components/agenda/AgendaCalendarioToolbar.tsx` como componente de cliente. Comentário de cabeçalho registrando: (a) que é a Variante A do esboço aprovado, e que a Variante B (mini-calendário lateral) foi explicitamente rejeitada pelo dono do projeto — não reintroduzir; (b) que a barra continua montada na visão de Lista mostrando só o seletor, porque é ela o único caminho de volta ao Calendário; (c) que o componente é apresentacional puro — não guarda data, não decide passo de navegação, só dispara os retornos de chamada recebidos.

    Seletor segmentado: um contêiner em linha com borda, cantos arredondados, superfície de cartão e espaçamento interno mínimo, contendo quatro botões pequenos. O ativo usa a variante cheia e os inativos a variante fantasma. Cada botão declara seu estado de seleção de forma acessível (atributo de pressionado ou papel de aba com selecionado — escolher um e aplicar aos quatro), para que o teste consiga afirmar qual está ativo sem depender de classe de estilo. A lista das quatro opções é uma constante local com valor e rótulo, iterada — não quatro blocos copiados.

    Navegação de data e legenda ficam num bloco condicionado à existência do rótulo. Navegação: botão de ícone com seta para a esquerda, o rótulo do período centralizado com largura mínima (para o texto não fazer os botões pularem ao trocar de mês), botão de ícone com seta para a direita, e o botão "Hoje" com variante de contorno e tamanho pequeno. Os dois botões de seta recebem rótulo acessível descrevendo a ação ("Período anterior" / "Próximo período").

    Legenda: três marcas em linha, texto minúsculo e esmaecido, cada uma com um ponto redondo pequeno — esmaecido para prospecção, primário para visita, de destaque de erro para atrasado. Traduzir as cores do esboço para os símbolos do projeto; **nenhum valor cru copiado**.

    Disposição geral: navegação à esquerda, seletor à direita, com quebra de linha permitida e espaçamento entre os grupos, como no esboço. A legenda vai abaixo, ocupando a linha inteira.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint components/agenda tests/agenda && npx vitest run tests/agenda/agenda-calendario-toolbar.test.tsx && node -e "const fs=require('fs');const raw=fs.readFileSync('components/agenda/AgendaCalendarioToolbar.tsx','utf8');const src=raw.replace(/\/\*[\s\S]*?\*\//g,'').replace(/^[^\n]*\/\/.*$/gm,'');if(!raw.startsWith('\"use client\"'))throw new Error('precisa ser componente de cliente');for(const r of ['Lista','Dia','Semana','Mes','Hoje'])if(!new RegExp(r,'i').test(src))throw new Error('falta o rotulo '+r);const cores=src.match(/#[0-9a-fA-F]{3,8}\b/g)||[];if(cores.length)throw new Error('valor de cor cru copiado do esboco: '+cores.join(','));if(/mini|miniCal|Calendar\b/.test(src))throw new Error('a Variante B (mini-calendario lateral) foi rejeitada — D-09');if(/useState|useEffect/.test(src))throw new Error('a barra e apresentacional: nao guarda data nem visao');for(const p of ['use server','@/app/actions','@/lib/supabase'])if(src.includes(p))throw new Error('componente apresentacional nao pode importar '+p);if(!/aria-pressed|aria-selected/.test(src))throw new Error('o botao ativo precisa anunciar selecao de forma acessivel');if(!/aria-label/.test(src))throw new Error('os botoes de seta precisam de rotulo acessivel');console.log('OK barra de ferramentas')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/agenda/AgendaCalendarioToolbar.tsx` renderiza os quatro botões de visão na ordem Lista, Dia, Semana, Mês, iterados a partir de uma constante local.
    - O botão ativo é distinguível e anuncia seleção de forma acessível; o teste afirma qual está ativo sem depender de classe de estilo.
    - Com rótulo nulo, navegação e legenda não são renderizadas e o seletor continua visível (caso em teste).
    - Com rótulo, os três botões de navegação existem, cada um dispara só o seu retorno de chamada, e os de seta têm rótulo acessível.
    - A legenda mostra as três marcas com os símbolos de cor do projeto; nenhum valor cru do esboço no arquivo.
    - Nenhum vestígio de mini-calendário lateral; o componente não guarda estado.
    - `npx tsc --noEmit` e `npx eslint components/agenda tests/agenda` limpos.
  </acceptance_criteria>
  <done>A barra de ferramentas da Variante A existe, com os quatro modos e a navegação de data, sem guardar nenhum estado próprio.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contêiner do calendário — data de referência, agrupamento único, troca de visão e diálogo do dia</name>
  <files>components/agenda/AgendaCalendario.tsx, tests/agenda/agenda-calendario.test.tsx</files>
  <read_first>
    - `components/agenda/ConcluirItemDialog.tsx`, o bloco do diálogo (abertura controlada de fora, `DialogContent`/`DialogHeader`/`DialogTitle`) — o padrão de diálogo deste projeto, a ser espelhado no diálogo do dia.
    - `components/agenda/AgendaList.tsx` INTEIRO — quem hoje é dono da busca, do filtro e dos dois diálogos; este contêiner precisa encaixar como filho dela sem disputar nenhuma dessas responsabilidades.
    - `lib/agenda/itens.ts` — assinaturas do agrupamento por data, itens do dia, navegação e rótulo de período.
    - Os blocos `<interface_context>` de `20-02-PLAN.md` e `20-03-PLAN.md` — as assinaturas exatas das três visões consumidas.
    - `.planning/sketches/003-agenda-calendario/index.html`, o bloco `#a-view-dia` (cabeçalho do dia acima da lista) e a função de simulação `openDay` (clicar num dia da grade de mês abre a lista completa daquele dia).
  </read_first>
  <behavior>
    - Com a visão "lista", renderiza a barra de ferramentas sem rótulo de período e NÃO renderiza nenhuma grade.
    - Com a visão "mes", renderiza a barra com o rótulo do mês e a grade de mês; com "semana", a grade de semana; com "dia", o cabeçalho da data por extenso seguido da lista daquele dia.
    - Clicar em avançar no modo mês muda o rótulo para o mês seguinte; no modo semana, para a semana seguinte; no modo dia, para o dia seguinte. Voltar faz o inverso.
    - Depois de navegar para longe, clicar em "Hoje" traz a referência de volta para a data do `now` recebido.
    - Trocar de visão preserva a data de referência (ir para outubro no mês e trocar para semana mostra uma semana de outubro, não a semana de hoje).
    - O agrupamento por data é calculado uma vez a partir da lista recebida e é o mesmo objeto passado às visões — trocar de visão não recalcula sobre um conjunto diferente.
    - Clicar num dia da grade de mês abre um diálogo cujo título é a data por extenso daquele dia e cujo corpo é a lista de itens daquele dia; fechar o diálogo o remove.
    - O diálogo aberto por um dia sem itens mostra a mensagem de dia vazio (o mesmo estado vazio da visão de dia).
    - Concluir um item de dentro do diálogo dispara o retorno de chamada de conclusão com o item certo; abrir um item dispara o de abrir ficha com o identificador de cliente certo.
    - Clicar num cartão da visão de semana dispara o retorno de chamada de abrir ficha com o identificador de cliente certo.
    - O contêiner não chama nenhuma ação de servidor.
  </behavior>
  <action>
    Escrever os casos de teste primeiro (RED), depois o componente.

    Criar `components/agenda/AgendaCalendario.tsx` como componente de cliente. Comentário de cabeçalho registrando: (a) que este é o dono da DATA DE REFERÊNCIA e do dia aberto no diálogo, e de mais nada — a busca, o filtro por vendedor, a ficha do cliente e a janela de conclusão continuam sendo da tela (`AgendaList.tsx`), que passa os dois pedidos de ação para cima; (b) que a lista recebida já vem estreitada pelo filtro de vendedor, e que por isso o agrupamento calculado aqui é automaticamente coerente com o que a Lista mostra — recalcular a partir de outra fonte é a falha registrada no Pitfall 10; (c) que a barra de ferramentas é renderizada mesmo na visão de Lista, porque é o único caminho de volta.

    Estado: a data de referência (iniciada com o `now` recebido) e o dia aberto no diálogo (nulo quando fechado). Nada mais.

    Agrupamento: calculado uma vez por renderização a partir da lista recebida, memorizado por ela, e passado às visões de semana e mês; a visão de dia recebe a lista já extraída pela função de itens do dia.

    Renderização: sempre a barra de ferramentas, recebendo a visão, o repasse de troca de visão, o rótulo (nulo quando a visão é a Lista, senão o rótulo do período para o modo atual) e três tratadores — voltar e avançar chamando a função de navegação do módulo puro com o modo atual e o passo correspondente, e "Hoje" devolvendo a referência para o `now`. Abaixo, um bloco condicional por modo:
    - modo mês: a grade de mês, com o retorno de chamada de seleção de dia guardando o dia no estado do diálogo;
    - modo semana: a grade de semana, com o retorno de chamada de abertura de item repassando o identificador de cliente do item para cima;
    - modo dia: um cabeçalho com o rótulo do período no modo dia, e abaixo a visão de dia alimentada pelos itens daquele dia;
    - visão Lista: nada além da barra.

    Diálogo do dia: montado com o primitivo de diálogo já usado no projeto, com abertura derivada de o dia do diálogo não ser nulo e fechamento zerando esse estado. Título: o rótulo do período no modo dia para o dia guardado. Corpo: a mesma visão de dia usada no modo dia, alimentada pelos itens daquele dia. Comentar explicitamente que o corpo é o MESMO componente, e que essa é a cadeia da decisão D-05 — a lista do dia dentro do diálogo, a visão de dia e a Lista da tela mostram todas o mesmo cartão de item; criar aqui uma segunda forma de listar item quebraria a única coisa que o requisito AGD-11 pede.

    Não criar contagem, filtro, ordenação ou busca nenhuma aqui.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint components/agenda tests/agenda && npx vitest run tests/agenda/agenda-calendario.test.tsx tests/agenda/agenda-calendario-toolbar.test.tsx tests/agenda/agenda-calendario-dia.test.tsx tests/agenda/agenda-calendario-semana.test.tsx tests/agenda/agenda-calendario-mes.test.tsx && node -e "const fs=require('fs');const raw=fs.readFileSync('components/agenda/AgendaCalendario.tsx','utf8');const src=raw.replace(/\/\*[\s\S]*?\*\//g,'').replace(/^[^\n]*\/\/.*$/gm,'');if(!raw.startsWith('\"use client\"'))throw new Error('precisa ser componente de cliente');for(const n of ['AgendaCalendarioToolbar','AgendaCalendarioMes','AgendaCalendarioSemana','AgendaCalendarioDia','agruparPorData','itensDoDia','navegarData','rotuloDoPeriodo'])if(!src.includes(n))throw new Error('precisa compor/consumir '+n);if((src.match(/AgendaCalendarioDia/g)||[]).length<3)throw new Error('a visao de dia precisa ser reusada no diaogo do dia tambem (D-03/D-05)');if(!/Dialog/.test(src))throw new Error('falta o diaogo aberto pelo clique no dia da grade de mes');if(/AgendaItemRow/.test(src))throw new Error('o conteiner nao monta linha de item — quem faz isso e a visao de dia');if(/filtrarPorVendedor|agruparAgenda/.test(src))throw new Error('o filtro e o agrupamento em secoes continuam sendo da tela, nao do calendario');for(const p of ['use server','@/app/actions','@/lib/supabase','getAgendaAction'])if(src.includes(p))throw new Error('o conteiner nao pode buscar dado nem chamar acao de servidor: '+p);if(!/useMemo/.test(src))throw new Error('o agrupamento por data precisa ser calculado uma vez e memorizado');const lista=fs.readFileSync('components/agenda/AgendaList.tsx','utf8');if(lista.includes('AgendaCalendario'))throw new Error('AgendaList.tsx e do plano 20-05, nao deste');console.log('OK conteiner do calendario')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/agenda/AgendaCalendario.tsx` guarda apenas a data de referência e o dia do diálogo; não busca dado, não chama ação de servidor, não filtra e não reagrupa em seções.
    - A barra de ferramentas é renderizada em todas as visões, inclusive na Lista (caso em teste).
    - Os três modos renderizam a visão correspondente; a visão de dia vem com o cabeçalho da data por extenso.
    - Avançar/voltar movem um dia, uma semana ou um mês conforme o modo (três casos em teste); "Hoje" devolve a referência para a data do `now`.
    - Trocar de visão preserva a data de referência (caso em teste).
    - O agrupamento por data é calculado uma vez e memorizado pela lista recebida.
    - Clicar num dia da grade de mês abre o diálogo com o título da data por extenso e a lista daquele dia; o corpo é o MESMO componente da visão de dia (verificado mecanicamente por reuso).
    - Concluir e abrir a partir do diálogo, e abrir a partir de um cartão da semana, disparam os retornos de chamada com o item/identificador certos.
    - `AgendaList.tsx` NÃO foi tocado neste plano.
    - Todos os arquivos de teste de calendário dos planos 20-02, 20-03 e deste passam juntos; `npx tsc --noEmit` e `npx eslint components/agenda tests/agenda` limpos.
  </acceptance_criteria>
  <done>O calendário existe como uma peça só, navegável, com as três visões e o diálogo do dia — falta apenas plugá-lo na tela da Agenda.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| propriedades → estado local | O contêiner recebe itens já escopados pela RLS e já estreitados pelo filtro de vendedor; guarda apenas datas. Nenhum dado sensível nasce aqui. |
| retornos de chamada → tela | Abrir a ficha do cliente e concluir item são pedidos repassados para cima; a execução real continua nas ações de servidor já existentes, que revalidam do lado do servidor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-10 | Elevation of Privilege | retorno de chamada de conclusão vindo do diálogo do dia | medium | mitigate | O contêiner apenas repassa o item; quem executa continua sendo `concluirTarefaProspeccao`/`concluirVisita` em `app/actions/agenda.ts`, intocadas, que revalidam a entrada no servidor e rodam sob RLS. Verificado mecanicamente pela proibição de importar ações de servidor aqui. |
| T-20-11 | Information Disclosure | agrupamento recalculado sobre outra fonte | medium | mitigate | Um agrupamento derivado de conjunto diferente do filtrado exporia ao Supervisor filtrado (ou ao vendedor) itens fora do recorte esperado. Mitigado por: cálculo único memorizado sobre a lista recebida, proibição mecânica de reimportar o filtro por vendedor aqui, e caso de teste que confere as três visões contra o mesmo conjunto. |
| T-20-12 | Denial of Service | navegação de data ilimitada | low | accept | Navegar para 1997 apenas desenha uma grade vazia — não há busca por período nesta fase (isso é a Fase 21). Custo constante. Sem mitigação. |
| T-20-SC | Tampering | instalação de pacotes (npm) | high | mitigate | Zero instalação: diálogo, botões e ícones já existem no projeto. Qualquer necessidade de instalar algo aqui é desvio de escopo e exige o portão humano de legitimidade antes. |
</threat_model>

<verification>
- `npx vitest run tests/agenda/` verde por inteiro — os cinco arquivos de calendário novos mais todos os antigos, estes últimos sem edição.
- `npx tsc --noEmit` e `npx eslint components/agenda tests/agenda` limpos.
- `components/agenda/AgendaList.tsx`, `AgendaItemRow.tsx` e `ConcluirItemDialog.tsx` não aparecem em `git diff --stat`.
- `git diff --stat` mostra exatamente quatro arquivos, todos novos.
</verification>

<success_criteria>
O calendário está montado e navegável fora da tela: barra da Variante A com quatro modos, navegação de data com passo correto por modo, botão "Hoje", legenda, as três visões plugadas ao mesmo agrupamento calculado uma vez, e o diálogo que abre a lista completa de um dia reusando o mesmo componente da visão de dia.
</success_criteria>

<output>
Create `.planning/phases/20-calend-rio-da-agenda-m-s-semana-e-dia/20-04-SUMMARY.md` when done
</output>
