---
phase: quick-260819-m8q
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: false
requirements: [QUICK-260819-m8q]
files_modified:
  - supabase/migrations/0023_endereco_opcional_na_importacao.sql
  - tests/clientes/endereco-opcional.test.ts
  - lib/importacao/types.ts
  - lib/validations/importacao.ts
  - lib/importacao/annotarLinha.ts
  - lib/importacao/confirmar.ts
  - tests/importacao/annotarLinha.test.ts
  - tests/importacao/confirmar.test.ts
  - lib/clientes/rotuloLocalizacao.ts
  - tests/clientes/rotulo-localizacao.test.ts
  - lib/supabase/queries/clientes.ts
  - lib/clientes/exportacao.ts
  - components/clientes/ClienteCard.tsx
  - components/clientes/KanbanBoard.tsx
  - components/clientes/ClienteDetailSheet.tsx
  - components/clientes/FiltersPopover.tsx
  - components/importacao/ImportPreviewTable.tsx
  - tests/clientes/filters-popover.test.tsx

must_haves:
  truths:
    - "Uma planilha de importação em massa com CEP, Rua, Número, Cidade e Estado em branco é aceita: a linha aparece como 'ok' na revisão e o cliente é criado."
    - "O cadastro manual (Novo cliente) continua recusando salvar sem os 5 campos, com as mesmas mensagens de antes — createClienteSchema fica byte-a-byte igual."
    - "Um cliente sem cidade/estado aparece normalmente na lista/kanban com os rótulos 'Sem cidade'/'Sem estado' — nunca some da tela, nunca imprime 'null' ou 'undefined'."
    - "O filtro de Estado da tela de Clientes tem a opção 'Sem estado', e o filtro de Cidade tem a opção 'Sem cidade', e cada uma encontra exatamente os clientes com aquele campo vazio."
    - "Gravar estado como string vazia continua sendo recusado pelo banco; só o valor nulo é aceito."
  artifacts:
    - supabase/migrations/0023_endereco_opcional_na_importacao.sql
    - lib/clientes/rotuloLocalizacao.ts
    - tests/clientes/endereco-opcional.test.ts
    - tests/clientes/rotulo-localizacao.test.ts
  key_links:
    - "annotarLinha devolve valor NULO (nunca string vazia) nos 5 campos quando a célula está em branco — string vazia em `estado` viola chk_estado_valido e derruba a linha inteira no insert."
    - "ResolvedRow -> RpcClienteRow -> jsonb_to_recordset de importar_clientes_lote: o JSON precisa carregar nulo de verdade, senão o valor chega ao Postgres como texto vazio."
    - "ClienteListItem.cidade/estado (agora anuláveis) alimenta DUAS leituras que precisam mudar juntas: o rótulo do card (ClienteCard) e o comparador do filtro (clienteAtendeFiltros). Mudar só uma faz o card imprimir 'null' OU o cliente sumir do filtro."
    - "createClienteSchema e updateClienteSchema permanecem inalterados — depois que o NOT NULL sai do banco, essa camada Zod passa a ser a ÚNICA coisa que ainda garante endereço no cadastro e na edição manual."
---

<objective>
Tornar CEP, Rua, Número, Cidade e Estado opcionais **apenas** no fluxo de importação em massa de clientes ("Importar clientes"), mantendo-os obrigatórios no cadastro manual.

Purpose: hoje uma planilha com endereço incompleto é recusada linha a linha, e o time precisa importar a base existente mesmo sem endereço completo. O cadastro manual continua exigindo tudo porque lá o vendedor tem a informação na mão.

Output:
- Migration nova tornando as 5 colunas anuláveis em `clientes`.
- Camada de importação (vocabulário de campos, esquema de linha, anotação, montagem do lote) aceitando as 5 células em branco e gravando valor nulo.
- Camada de exibição/filtro tratando cidade/estado nulos com rótulo próprio, sem esconder o cliente.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.claude/CLAUDE.md

Arquivos-fonte que o executor precisa ler antes de editar:
@supabase/migrations/0002_clientes_and_funil.sql
@supabase/migrations/0007_cidades_e_estado_valido.sql
@supabase/migrations/0019_importar_clientes_lote_cnpj_nome_fantasia.sql
@lib/importacao/types.ts
@lib/importacao/annotarLinha.ts
@lib/importacao/confirmar.ts
@lib/validations/importacao.ts
@lib/validations/cliente.ts
@lib/supabase/queries/clientes.ts
@components/clientes/FiltersPopover.tsx
@components/clientes/ClienteCard.tsx
@tests/clientes/estado-constraint.test.ts
</context>

<decisoes_travadas>
Estas decisões vêm do Discuss e **não devem ser reabertas** durante a execução:

- **D-01 — Escopo:** só o fluxo de importação em massa passa a aceitar as 5 colunas em branco. O formulário de cadastro manual continua exigindo os 5. `createClienteSchema` (`lib/validations/cliente.ts`) **não muda**.
- **D-02 — Consequência no banco:** as colunas `cep`/`rua`/`numero`/`cidade`/`estado` em `clientes` viram anuláveis, porque a mesma tabela recebe linhas dos dois fluxos. A obrigatoriedade do cadastro manual passa a ser garantida só pela validação Zod daquele formulário.
- **D-03 — Visibilidade:** cliente sem cidade/estado **sempre** aparece na lista/kanban, com rótulo "Sem cidade"/"Sem estado". Nunca escondido. O filtro de Cidade/Estado ganha opção própria para encontrar esses clientes depois.
- **D-04 — Constraint de UF:** `estado` nulo não pode violar `chk_estado_valido`. O CHECK atual é `check (estado in (...))`; em Postgres um valor nulo faz a expressão devolver desconhecido, e um CHECK só reprova quando devolve falso — portanto nulo já passa e a constraint **não precisa ser alterada**. String vazia continua sendo reprovada, e isso é desejado.
- **D-05 — Zero `security definer` novo.** Nenhuma RPC nova, nenhuma policy nova nesta task.

**Consequência conhecida e aceita (documentar no SUMMARY, não "consertar" por conta própria):** `updateClienteSchema` também fica inalterado. Isso significa que, ao abrir a ficha de um cliente importado sem endereço e clicar em "Salvar alterações", o formulário vai pedir os 5 campos antes de deixar salvar. Isso é o comportamento pretendido de D-01 (regularização na edição manual), não um defeito. Se o dono do projeto quiser afrouxar isso depois, é uma decisão separada.
</decisoes_travadas>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0023 — as 5 colunas de endereço viram anuláveis em `clientes`</name>
  <files>supabase/migrations/0023_endereco_opcional_na_importacao.sql, tests/clientes/endereco-opcional.test.ts</files>
  <action>
Criar `supabase/migrations/0023_endereco_opcional_na_importacao.sql` (arquivo NOVO — nunca editar 0001-0022, que já estão aplicadas; regra do CLAUDE.md e Pitfall 11 do projeto).

Conteúdo da migration:
1. Cabeçalho em comentário no mesmo estilo das migrations 0013/0018/0022: o que muda, por quê (D-01/D-02), e a referência a este plano.
2. Um único `alter table clientes` removendo a exigência de valor obrigatório das cinco colunas `cep`, `rua`, `numero`, `cidade`, `estado` — cinco cláusulas `alter column ... drop not null` no mesmo statement.
3. Um bloco de comentário explicando D-04: `chk_estado_valido` (migration 0007) é `check (estado in (...))`; um valor nulo torna a expressão desconhecida e um CHECK do Postgres só reprova quando a expressão é falsa, então nulo já é aceito sem nenhuma alteração de constraint. Texto vazio continua reprovado, e isso é a trava que impede a camada de importação de gravar texto vazio por engano. **Nenhum `alter constraint`, nenhum `drop constraint`, nenhum `validate constraint` nesta migration.**
4. Nenhuma alteração em `importar_clientes_lote`: a função já repassa os cinco campos vindos de `jsonb_to_recordset`, que produz valor nulo quando a chave do JSON vem nula. Registrar isso em comentário para quem vier depois não recriar a função à toa.

Criar `tests/clientes/endereco-opcional.test.ts` seguindo o molde de `tests/clientes/estado-constraint.test.ts` (mesmos imports de `tests/helpers/supabase-test-clients`, mesmo `afterEach` que apaga por id via `serviceClient()`), com DUAS diferenças deliberadas:
- Usar **`serviceClient()`** para os inserts, nunca `signInAs`. O projeto tem um limite de taxa conhecido em `signInWithPassword` (ver Blockers na STATE.md) e o que este arquivo prova é um fato de SCHEMA, não de RLS — `serviceClient()` evita o limite completamente.
- Precisa de um `responsavel` válido: ler um id qualquer de `profiles` via `serviceClient()` no início do teste, em vez de derivar de uma sessão.

Três casos:
- Insert direto com `cep`, `rua`, `numero`, `cidade` e `estado` todos nulos **é aceito** e cria a linha (prova que a exigência de valor obrigatório saiu).
- Insert direto com `estado` em texto vazio **é recusado** com violação de CHECK e não cria linha (prova que `chk_estado_valido` continua sendo a trava contra texto vazio — D-04).
- Insert direto com `estado` nulo e `cidade` preenchida **é aceito** (prova que os campos são independentes entre si).

Este arquivo fica VERMELHO até a Task 2 aplicar a migration no banco — é esperado e está anotado no cabeçalho do arquivo, mesmo padrão do cabeçalho de `estado-constraint.test.ts`.
  </action>
  <verify>
    <automated>grep -v -- '^--' supabase/migrations/0023_endereco_opcional_na_importacao.sql | grep -c 'drop not null'</automated>
    <expected>5</expected>
    <automated>test -f tests/clientes/endereco-opcional.test.ts &amp;&amp; echo OK</automated>
    <note>`npx vitest run tests/clientes/endereco-opcional.test.ts` fica VERMELHO até a Task 2 — não tratar como falha nesta task.</note>
  </verify>
  <done>A migration existe, tem exatamente 5 cláusulas de remoção da exigência de valor obrigatório fora de comentário, não toca em nenhuma constraint nem em nenhuma função, e o arquivo de teste de integração existe com os 3 casos.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Aplicar a migration 0023 no banco hospedado (dono do projeto)</name>
  <action>PAUSAR. Esta task não é executável pelo agente — o dono do projeto aplica a migration manualmente com a CLI do Supabase pinada em 2.111.0, conforme as instruções abaixo, e só então a Task 3 começa.</action>
  <what-built>A migration `0023_endereco_opcional_na_importacao.sql` está escrita e commitada, mas ainda NÃO foi aplicada no banco hospedado.</what-built>
  <why-human>Aplicar migration em produção neste projeto é sempre feito pelo dono (precedente das Fases 18 e 19: o classificador de modo automático bloqueia a tentativa do executor). O executor não tem como rodar `supabase db push` sozinho.</why-human>
  <how-to-verify>
1. No terminal, na raiz do projeto, aplicar a migration com a CLI do Supabase **pinada em 2.111.0** (as versões 2.112.0+ têm um defeito conhecido de validação de schema que este projeto já enfrentou nas Fases 13/17/18/19):

   `npx supabase@2.111.0 db push`

2. Confirmar que a saída lista `0023_endereco_opcional_na_importacao.sql` como aplicada, sem erro.

3. Rodar o teste de integração que a Task 1 criou — ele estava vermelho e agora precisa ficar verde:

   `npx vitest run tests/clientes/endereco-opcional.test.ts`

   Esperado: 3 testes passando. Se o caso do texto vazio falhar (ou seja: o banco ACEITOU `estado` em texto vazio), parar e reportar — isso significaria que `chk_estado_valido` não está mais valendo, e o resto do plano depende dele.
  </how-to-verify>
  <resume-signal>Responda "aplicada" (com o resultado dos 3 testes) ou descreva o erro.</resume-signal>
  <verify>
    <automated>npx vitest run tests/clientes/endereco-opcional.test.ts</automated>
  </verify>
  <done>A migration 0023 aparece como aplicada na saída do push e os 3 casos de `tests/clientes/endereco-opcional.test.ts` passam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Camada de importação aceita as 5 células em branco e grava valor nulo</name>
  <files>lib/importacao/types.ts, lib/validations/importacao.ts, lib/importacao/annotarLinha.ts, lib/importacao/confirmar.ts, components/importacao/ImportPreviewTable.tsx, tests/importacao/annotarLinha.test.ts, tests/importacao/confirmar.test.ts</files>
  <behavior>
    - Uma linha só com razão social e responsável preenchidos, e as 5 células de endereço em branco, devolve `status: "ok"` e lista de motivos vazia.
    - Nessa mesma linha, os 5 campos de `resolved` vêm com valor NULO — nunca texto vazio (texto vazio em `estado` seria recusado pelo banco).
    - `planConfirmacao` transforma essa linha em uma entrada do lote com os 5 campos nulos, e ela entra em `rowsToInsert` (não é pulada).
    - Uma linha ainda sem razão social continua sendo erro, com o mesmo motivo de antes.
    - Uma linha ainda sem responsável continua sendo erro, com o mesmo motivo de antes.
    - Uma linha com `estado` preenchido mas fora das 27 siglas de UF continua sendo erro, com o mesmo motivo de antes.
    - Uma linha com cidade preenchida que não existe para o estado informado continua sendo erro, com o mesmo motivo de antes.
  </behavior>
  <action>
Escrever/ajustar os testes ANTES da implementação (os casos acima em `tests/importacao/annotarLinha.test.ts` e `tests/importacao/confirmar.test.ts`), rodar, ver falhar, depois implementar.

**`lib/importacao/types.ts`** — em `SYSTEM_FIELDS`, as entradas `cep`, `rua`, `numero`, `cidade` e `estado` passam a ter `required: false`. Sobram exatamente DOIS campos marcados como obrigatórios: `razaoSocial` e `responsavel`. Atualizar o comentário de cabeçalho do arquivo, que hoje descreve um conjunto de 7 campos mínimos espelhando `createClienteSchema` — reescrevê-lo para explicar que, desde esta task, o vocabulário da importação em massa deixou de espelhar as regras do cadastro manual nesses 5 campos (D-01/D-02), e por quê. Esse comentário é a documentação principal da divergência; não deixar ele mentindo.

Efeito de segunda ordem já coberto pelo código existente: `requiredFieldsFaltando` (`lib/importacao/mapping.ts`) é genérica e lê `field.required`, então o assistente de importação para de exigir o mapeamento dessas 5 colunas sozinho. **Não editar `mapping.ts`.**

**`lib/validations/importacao.ts`** — em `createImportRowSchema`, os 5 campos de endereço deixam de exigir tamanho mínimo e passam a ser opcionais, como `complemento`/`contato`/`telefone` já são. `razaoSocial` e `responsavel` ficam exatamente como estão. Com isso, a lista `ENDERECO_FIELDS` e o mapeamento de motivo de endereço ficam sem uso — conferir com `grep -rn ENDERECO_FIELDS --include=*.ts .` que o único consumidor é `annotarLinha.ts`, e então remover a exportação junto com o uso. Atualizar o comentário de cabeçalho do arquivo pela mesma razão do item anterior.

**`lib/importacao/annotarLinha.ts`**:
- No tipo `ResolvedRow`, os campos `cep`, `rua`, `numero`, `cidade` e `estado` passam a admitir valor nulo (`string | null`), como `complemento` já admite.
- Remover o ramo do passo 5 que traduzia campo de endereço faltando em motivo de erro, e a constante de motivo correspondente. Os ramos de razão social e de responsável continuam intactos.
- Na montagem de `resolved`: cada um dos 5 campos apara espaços e vira valor nulo quando fica vazio — **nunca texto vazio**. Isso é o elo crítico: texto vazio em `estado` viola `chk_estado_valido` e derrubaria a linha no insert. Usar a mesma forma já usada logo abaixo para `cnpj`/`nomeFantasia` (valor aparado quando tem conteúdo, valor nulo quando não tem).
- `cidade` mantém a canonicalização IBGE quando casa; quando não casa, cai para o valor saneado aparado, e para valor nulo se esse valor estiver vazio.
- `estado` mantém a normalização para sigla maiúscula quando é UF válida; senão cai para o valor saneado aparado, e para valor nulo se vazio.
- As checagens do passo 4b (UF válida, cidade existente para o estado) já rodam só quando o valor está presente — **não mexer nelas**. Uma linha em branco simplesmente não as aciona.
- Deixar um comentário curto no passo 5 explicando por que endereço saiu da lista de exigências (D-01) e por que o valor precisa ser nulo e não texto vazio (D-04).

**`lib/importacao/confirmar.ts`** — no tipo `RpcClienteRow`, os 5 campos passam a admitir valor nulo. `toRpcClienteRow` continua repassando direto, sem `?? ""` em lugar nenhum: o JSON precisa chegar ao `jsonb_to_recordset` com nulo de verdade.

**`components/importacao/ImportPreviewTable.tsx`** — a célula de localização hoje é `{linha.resolved.cidade || "—"}/{linha.resolved.estado || "—"}`. Isso já degrada bem com valor nulo (vira travessão), então só conferir que continua compilando com o tipo anulável; não trocar por outro texto.
  </action>
  <verify>
    <automated>npx vitest run tests/importacao/annotarLinha.test.ts tests/importacao/confirmar.test.ts tests/importacao/mapping.test.ts tests/importacao/modelo.test.ts tests/importacao/preview.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -vE '^\s*(\*|//|/\*)' lib/importacao/types.ts | grep -c 'required: true'</automated>
    <expected>2</expected>
  </verify>
  <done>Uma linha de planilha sem os 5 campos de endereço é anotada como "ok", chega a `rowsToInsert` com os 5 campos nulos, e todos os motivos de erro anteriores (razão social, responsável, UF inválida, cidade inexistente) continuam sendo produzidos nos mesmos casos. `npx tsc --noEmit` limpo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Lista, ficha, exportação e filtro tratam cidade/estado nulos com rótulo próprio</name>
  <files>lib/clientes/rotuloLocalizacao.ts, tests/clientes/rotulo-localizacao.test.ts, lib/supabase/queries/clientes.ts, lib/clientes/exportacao.ts, components/clientes/ClienteCard.tsx, components/clientes/KanbanBoard.tsx, components/clientes/ClienteDetailSheet.tsx, components/clientes/FiltersPopover.tsx, tests/clientes/filters-popover.test.tsx</files>
  <behavior>
    - `rotuloCidadeEstado("São Paulo", "SP")` devolve `"São Paulo/SP"`.
    - `rotuloCidadeEstado(null, "SP")` devolve `"Sem cidade/SP"`.
    - `rotuloCidadeEstado("Santos", null)` devolve `"Santos/Sem estado"`.
    - `rotuloCidadeEstado(null, null)` devolve `"Sem cidade/Sem estado"`.
    - Texto só com espaços em branco é tratado igual a valor nulo nos quatro casos acima.
    - `clienteAtendeFiltros` com o filtro de estado na opção "sem estado" devolve verdadeiro só para cliente com estado nulo/vazio, e falso para cliente com UF preenchida.
    - `clienteAtendeFiltros` com o filtro de cidade na opção "sem cidade" devolve verdadeiro só para cliente com cidade nula/vazia.
    - `clienteAtendeFiltros` sem filtro nenhum devolve verdadeiro para um cliente com cidade e estado nulos (D-03: nunca escondido).
    - `clienteAtendeFiltros` com filtro de cidade em um nome real continua comparando por igualdade exata sem diferenciar maiúsculas (comportamento existente, não pode regredir).
  </behavior>
  <action>
Escrever/ajustar os testes ANTES da implementação, rodar, ver falhar, depois implementar.

**Criar `lib/clientes/rotuloLocalizacao.ts`** — módulo puro, sem nenhum import de Supabase nem de `next/headers`, para poder ser importado tanto de componente de cliente quanto de servidor (mesma disciplina de `lib/clientes/completude.ts`, cujo cabeçalho explica o motivo). Exporta:
- duas constantes com os rótulos de ausência, para cidade e para estado, de forma que a tela e o filtro nunca escrevam o texto duas vezes;
- `rotuloCidadeEstado(cidade: string | null, estado: string | null): string`, montando o par separado por barra e trocando cada lado ausente pelo seu rótulo. Valor só com espaços conta como ausente.
Comentário de cabeçalho registrando que este módulo é a autoridade ÚNICA desses dois rótulos (D-03) e que a tela, a ficha e o filtro devem todos ler daqui — dois lugares escrevendo o mesmo texto à mão é como o filtro e o card acabam discordando.

**`lib/supabase/queries/clientes.ts`** — os campos passam a admitir valor nulo nos tipos de leitura, refletindo o schema: `ClienteListItem.cidade`/`estado`; `ClienteDetalhe.cep`/`rua`/`numero`/`cidade`/`estado`; `ClienteExportRow.cep`/`rua`/`numero`/`cidade`/`estado`; e os tipos de linha crua correspondentes (`ClienteRow`, `ClienteDetalheRow`, `ClienteExportQueryRow`). Nenhuma mudança de comportamento: os mapeamentos já repassam o valor direto. **Não inserir `?? ""` nesta camada** — a decisão de como mostrar ausência é da tela, não da leitura (mesma postura já documentada em `getDiario` para o nome do autor).

**`lib/clientes/exportacao.ts`** — o auxiliar `cell()` já converte valor nulo em célula vazia, então a planilha exportada não precisa de mudança de lógica; só conferir que compila com os tipos anuláveis. Uma coluna de endereço vazia na planilha é o resultado correto.

**`components/clientes/ClienteCard.tsx`** — o tipo `ClienteCardData` passa a admitir cidade/estado nulos, e a linha que hoje monta o par com barra passa a chamar `rotuloCidadeEstado`.

**`components/clientes/KanbanBoard.tsx`** — `toCardData` repassa os valores como estão (sem `?? ""`). Conferir que a chamada a `clienteAtendeFiltros` continua compilando com o tipo anulável.

**`components/clientes/ClienteDetailSheet.tsx`** — em `toFormValues`, os campos de endereço passam a cair para texto vazio quando o valor lido é nulo (`cliente.cep ?? ""` e equivalentes; `estado` mantém a conversão de tipo já existente aplicada sobre o valor com queda para texto vazio). Isso é obrigatório: passar valor nulo para um campo de formulário do React o torna não-controlado e dispara aviso no console. **Não mexer em `updateClienteSchema`** — a ficha continuar exigindo os 5 campos ao salvar é o comportamento pretendido de D-01, já registrado no bloco de decisões travadas deste plano.

**`components/clientes/FiltersPopover.tsx`** — ganha as duas opções de busca por ausência (D-03):
- Duas constantes-sentinela novas, ao lado da sentinela `SEM_FILTRO` que já existe: uma para "estado ausente" e outra para "cidade ausente". Seguir a mesma convenção de nome com dois sublinhados nas pontas, e o mesmo motivo já comentado no arquivo (texto vazio é reservado pelo Base UI para o estado de placeholder).
- No campo de escolha de Estado, um item novo logo abaixo de "Todos os estados", rotulado com a constante de rótulo de estado ausente vinda de `rotuloLocalizacao.ts`.
- No campo de busca de Cidade: ele passa a ficar habilitado quando há qualquer Estado escolhido no rascunho, incluindo a sentinela de estado ausente. A lista visível sempre começa com uma entrada correspondente ao rótulo de cidade ausente; quando o Estado escolhido é uma UF de verdade, ela é seguida pelos nomes vindos da RPC. Quando o Estado escolhido é a sentinela de ausente, a lista tem só essa entrada. O efeito que chama `buscarCidadesComClientes` precisa de uma guarda: só dispara quando o Estado do rascunho é uma das 27 siglas de `UFS` — mandar a sentinela para a RPC é um pedido inútil ao servidor.
- `clienteAtendeFiltros` passa a receber cidade/estado anuláveis e ganha os dois ramos de ausência: filtro na sentinela de estado ausente casa só quando o cliente não tem estado (nulo ou só espaços); filtro na sentinela de cidade ausente casa só quando o cliente não tem cidade. Fora desses ramos, a comparação atual (igualdade exata sem diferenciar maiúsculas para cidade, igualdade direta para estado) fica intacta, lendo o valor com queda para texto vazio. **Sem filtro ativo, um cliente sem cidade/estado passa** — esse é o critério literal de D-03 e precisa ter teste próprio.
- `contarFiltrosAtivos` não precisa de mudança: as sentinelas são texto não vazio e já contam como filtro ativo. Confirmar com teste.

**`tests/clientes/filters-popover.test.tsx`** — acrescentar os casos de `clienteAtendeFiltros` listados em `<behavior>` e um caso de renderização provando que a opção de estado ausente aparece na lista do campo de Estado. Não remover nenhum caso existente: os testes de ordem no DOM, de cascata e de comparação exata de cidade são proteções contra regressões reais já encontradas neste arquivo.
  </action>
  <verify>
    <automated>npx vitest run tests/clientes/rotulo-localizacao.test.ts tests/clientes/filters-popover.test.tsx tests/clientes/exportacao.test.ts tests/clientes/incompleto.test.ts tests/clientes/estado-cidade-fields.test.tsx</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npx eslint</automated>
  </verify>
  <done>Existe um módulo único dono dos rótulos de ausência; card, ficha e filtro leem dele; o filtro tem opções para achar cliente sem cidade e sem estado; um cliente sem cidade/estado passa por `clienteAtendeFiltros` quando nenhum filtro está ativo. `npx tsc --noEmit` e `npx eslint` limpos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planilha do Supervisor → `annotarLinha` → `importar_clientes_lote` | conteúdo de célula não confiável atravessa aqui; já saneado por `sanitizeCell`, e a RPC é guardada por `is_supervisor()` |
| formulário de cadastro/edição → Server Action → `clientes` | entrada não confiável; revalidada por Zod no servidor e delimitada pela RLS de `clientes` |
| navegador → filtro da tela de Clientes | filtragem puramente em memória sobre um conjunto que a RLS já delimitou; não cruza fronteira nova |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-M8Q-01 | Tampering | `clientes` (colunas de endereço) | medium | mitigate | Remover a exigência de valor obrigatório enfraquece uma garantia de integridade no nível do banco para o cadastro manual. Mitigado mantendo `createClienteSchema` e `updateClienteSchema` intocados (Task 3 e Task 4 proíbem explicitamente editá-los) e mantendo a revalidação no servidor dentro de `createCliente`/`updateCliente` |
| T-M8Q-02 | Tampering | `annotarLinha` → `chk_estado_valido` | medium | mitigate | Se a camada de importação gravasse texto vazio em vez de valor nulo, `chk_estado_valido` reprovaria e a importação quebraria de forma confusa. Mitigado exigindo valor nulo em `resolved`/`RpcClienteRow` (Task 3) e provando os dois lados no banco real (Task 1: nulo aceito, texto vazio recusado) |
| T-M8Q-03 | Elevation of Privilege | `importar_clientes_lote` | low | accept | O caminho relaxado é alcançável só por quem passa no guard `is_supervisor()` da RPC, que não muda nesta task. Nenhuma policy nova, nenhuma RPC nova, nenhuma exceção `security definer` nova (D-05) |
| T-M8Q-04 | Information Disclosure | filtro "Sem cidade"/"Sem estado" | low | accept | O filtro roda inteiramente em memória sobre o conjunto de cards que a RLS de `clientes` já delimitou; não existe leitura nova no servidor. Um Vendedor continua vendo só os próprios clientes |
| T-M8Q-05 | Denial of Service | `cidades_com_clientes_por_estado` | low | mitigate | A guarda de UF no efeito do filtro (Task 4) impede que a sentinela de estado ausente vire chamada de RPC inútil a cada abertura do popover |
| T-M8Q-SC | Tampering | instalações npm/pip/cargo | low | accept | Esta task **não instala nenhum pacote novo**. Se o executor concluir que algum pacote é necessário, deve PARAR e reportar — instalação exige checkpoint humano bloqueante de legitimidade |
</threat_model>

<verification>
Depois das 4 tasks, verificação manual no navegador (o dono do projeto executa):

1. **Importação com endereço em branco:** montar uma planilha pequena (2-3 linhas) com Razão social e Responsável preenchidos e CEP, Rua, Número, Cidade e Estado em branco. Ir em "Importar clientes", subir o arquivo, mapear as colunas. Esperado: o assistente **não** exige mapear as 5 colunas de endereço, e na revisão as linhas aparecem como "ok", sem o motivo de endereço faltando. Confirmar a importação. Esperado: os clientes são criados.
2. **Cadastro manual continua exigindo:** clicar em "Novo cliente", preencher só razão social e responsável, tentar salvar. Esperado: o formulário recusa e pede CEP, Rua, Número, Cidade e Estado, com as mesmas mensagens de antes.
3. **Cliente sem cidade/estado aparece na tela:** localizar no kanban um dos clientes importados no passo 1. Esperado: o card aparece normalmente na coluna "Aguardando contato" e mostra "Sem cidade/Sem estado". Não pode aparecer "null", "undefined" nem barra solta.
4. **Filtro encontra esses clientes:** abrir "Filtros", escolher "Sem estado" no campo de Estado, aplicar. Esperado: só os clientes sem estado ficam na tela. Limpar e escolher uma UF real com "Sem cidade" no campo de Cidade — esperado: só os clientes daquele estado sem cidade. Limpar tudo — esperado: os clientes sem endereço voltam a aparecer junto com todos os outros.
5. **Verificar nas duas contas:** repetir os passos 3 e 4 com uma conta de Vendedor (não só Supervisor), confirmando que o Vendedor enxerga os próprios clientes sem endereço normalmente.
6. **Ficha do cliente:** abrir a ficha de um cliente importado sem endereço. Esperado: os campos de endereço aparecem vazios (não com "null"), e tentar "Salvar alterações" pede os 5 campos — comportamento pretendido de D-01, registrado no bloco de decisões travadas.
</verification>

<success_criteria>
- Migration 0023 aplicada no banco hospedado; `tests/clientes/endereco-opcional.test.ts` verde com os 3 casos.
- Uma linha de planilha sem os 5 campos de endereço é anotada como "ok" e chega ao lote com os 5 valores nulos.
- Os motivos de erro anteriores da importação (razão social, responsável, UF fora das 27 siglas, cidade inexistente para o estado) continuam sendo produzidos nos mesmos casos.
- `lib/validations/cliente.ts` está **byte a byte idêntico** ao que era antes desta task (`git diff --stat` não lista o arquivo).
- Card, ficha, exportação e filtro tratam cidade/estado nulos sem imprimir valor cru na tela e sem esconder o cliente.
- O filtro tem uma opção que encontra cliente sem estado e outra que encontra cliente sem cidade.
- `npx tsc --noEmit` e `npx eslint` limpos.
- Nenhuma dependência npm nova; nenhuma exceção `security definer` nova (continuam 4 no código-base).
</success_criteria>

<output>
Ao terminar, criar `.planning/quick/260819-m8q-tornar-cep-rua-numero-cidade-e-estado-ca/260819-m8q-SUMMARY.md` registrando:
- que `createClienteSchema` e `updateClienteSchema` ficaram intocados de propósito (D-01), e a consequência conhecida na edição da ficha de cliente importado sem endereço;
- que `chk_estado_valido` não precisou de alteração porque um CHECK do Postgres já aceita valor nulo, e que texto vazio continua reprovado — sendo essa a trava que obriga a importação a gravar nulo;
- o novo módulo dono dos rótulos "Sem cidade"/"Sem estado" e o fato de card/ficha/filtro lerem todos dele;
- as duas sentinelas novas do filtro e a guarda de UF antes de chamar a RPC de cidades.
</output>
