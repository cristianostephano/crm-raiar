---
phase: 10-desativa-o-de-membro-da-equipe
plan: 06
type: execute
wave: 5
depends_on: ["10-05"]
files_modified:
  - app/(app)/equipe/page.tsx
autonomous: false
requirements: [EQP-01, EQP-02, EQP-03, EQP-04]
user_setup: []

must_haves:
  truths:
    - "The real /equipe screen renders the Status column and the per-row actions — the feature is reachable by a Supervisor, not just present in the repository."
    - "The page passes the logged-in user's id down, which is what lets the list hide Desativar on the caller's own row (D-01)."
    - "The Supervisor-only redirect guard, the Convidar header and the empty state are unchanged."
    - "A production build succeeds, proving no server-only module leaked into a Client Component."
    - "A real deactivated member is genuinely refused at the login screen, and a real reactivated member can log in again — the two verifications 10-VALIDATION.md marks manual-only."
  artifacts:
    - app/(app)/equipe/page.tsx
  key_links:
    - "`page.tsx` -> `EquipeList`: the page is the only place that knows the session user's id, and it has never passed it downstream before; without this new prop D-01's client-side control silently does nothing."
    - "`profiles.ativo` -> the page's `select()`: the column must be added to the select list or every row renders as Inativo/undefined."
    - "manual login attempt -> Auth `ban_duration`: the only part of EQP-03 that no automated test in this phase can reach."
---

<objective>
Wire the feature into the real screen, then have a human confirm the two things no test in this phase can reach: that a deactivated member is actually refused at the login screen, and that a reactivated member can log in again.

The page edit itself is small and composition-only, exactly as `10-UI-SPEC.md` frames it — no new screen, no layout change, no new route. The weight of this plan is the verification: `10-VALIDATION.md` lists two manual-only rows, both about the Supabase Auth ban, because banning a real `auth.users` row is not safely repeatable against the live free-tier project from an automated suite.

Purpose: every automated test in this phase proves the Postgres half of EQP-03 (access stops on the next request). The Auth half — "não consegue mais entrar no sistema", read literally as the login screen refusing them — needs one human, once.
Output: an edited `app/(app)/equipe/page.tsx`, a signed-off live verification, and a full green suite proving the verification account was restored.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/10-desativa-o-de-membro-da-equipe/10-UI-SPEC.md
@.planning/phases/10-desativa-o-de-membro-da-equipe/10-VALIDATION.md
@.planning/phases/10-desativa-o-de-membro-da-equipe/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire EquipeList into the Gerenciar equipe page</name>

  <read_first>
    - `app/(app)/equipe/page.tsx` — the whole file (96 lines). Lines 18-37 are the Supervisor-only redirect guard; line 39-42 is the `profiles` select; lines 53-81 are the inline table this task replaces; lines 82-93 are the empty state that stays.
    - `.planning/phases/10-desativa-o-de-membro-da-equipe/10-PATTERNS.md`, section "`app/(app)/equipe/page.tsx`" — the two changes, and the explicit list of what stays untouched.
    - `components/equipe/EquipeList.tsx` and `lib/equipe/membros.ts` — created in 10-05: the props (`members`, `currentUserId`) and the `EquipeMember` shape.
    - `.planning/phases/10-desativa-o-de-membro-da-equipe/10-UI-SPEC.md` §1 — the column order the list now owns.
  </read_first>

  <files>app/(app)/equipe/page.tsx</files>

  <action>
Two changes, nothing else.

Change 1 — extend the members query's column list to include `ativo`, so the select reads `id, nome, sobrenome, email, role, ativo`. Without this the list renders every row as inactive. Type the result as `EquipeMember[]` (imported from `@/lib/equipe/membros`) where the local variable is declared, so a future column rename fails the type-check here rather than silently at runtime.

Change 2 — replace the whole `<div className="overflow-hidden rounded-lg border">…</div>` block containing the inline `<table>` (currently lines 54-81) with a single `<EquipeList members={members ?? []} currentUserId={user.id} />`. The `currentUserId` prop is new plumbing this file has never had: `user.id` is already in scope from the existing `auth.getUser()` call at the top, and passing it down is what allows the list to hide the Desativar action on the caller's own row (D-01). Add a one-line comment at the prop saying exactly that, and adding that the RPC refuses self-deactivation regardless — this prop is an affordance, not the control.

Then delete what became dead in this file: the `ROLE_LABELS` constant and the `Badge` import, both of which moved into `EquipeList` in 10-05. Leave `redirect`, `InviteUserForm` and `createClient` imports alone.

Do not touch: the Supervisor-only redirect guard (lines 18-37), the `<h1>Gerenciar equipe</h1>` + `InviteUserForm` header row, the `hasMembers` branch, or the "Nenhum membro cadastrado ainda" empty state. This screen's authorization story does not change in this phase — the redirect stays UX-only and `profiles`' SELECT policy stays the real boundary.

Finish by running a production build (`npm run build`). It is the gate that catches a Client Component accidentally reaching a server-only module — which in this phase would mean the `service_role` key path, the single most expensive mistake available here.
  </action>

  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npm run lint` reports no new errors.
    - `npm run build` completes successfully.
    - `grep -c 'id, nome, sobrenome, email, role, ativo' "app/(app)/equipe/page.tsx"` returns 1.
    - `grep -c 'currentUserId={user.id}' "app/(app)/equipe/page.tsx"` returns 1.
    - `grep -c '<table' "app/(app)/equipe/page.tsx"` returns 0 — the table now lives entirely in `EquipeList`.
    - `grep -c 'ROLE_LABELS' "app/(app)/equipe/page.tsx"` returns 0 — the map moved out with the markup.
    - `grep -c 'Nenhum membro cadastrado ainda' "app/(app)/equipe/page.tsx"` returns 1 — the empty state survived.
    - `grep -c 'redirect("/")' "app/(app)/equipe/page.tsx"` returns 1 — the Supervisor-only guard survived.
    - `grep -rl "supabase/admin" --include=*.tsx .` returns nothing — no Client Component or page reaches the service_role factory.
    - `npx vitest run tests/equipe` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>npx tsc --noEmit && npm run build && npx vitest run tests/equipe</automated>
  </verify>

  <done>A Supervisor opening /equipe sees the Status column and the per-row Desativar/Reativar actions, with no action on their own row, and the app still builds for production.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live verification — including the two checks no test can perform</name>

  <read_first>
    - `.planning/phases/10-desativa-o-de-membro-da-equipe/10-VALIDATION.md`, "Manual-Only Verifications" table — the two rows this checkpoint exists to close, and why they are manual.
    - `.planning/phases/10-desativa-o-de-membro-da-equipe/10-UI-SPEC.md`, Copywriting Contract — the exact wording the reviewer is checking against.
    - `tests/auth/rls-roles.test.ts` lines 15-28 — `SEED_ACCOUNTS.vendedorB`'s email and password, the disposable identity this walkthrough uses.
  </read_first>

  <files>none — this task modifies no file; it pauses for a human walkthrough of the running app</files>

  <action>
Stop and hand control to the project owner. Start the dev server (`npm run dev`) so the app is actually reachable before pausing, confirm the Supervisor account can log in, and then present the walkthrough below verbatim — including the warning that steps 8 and 9 are mandatory because they restore the disposable account the automated suite depends on. Do not proceed to Task 3, and do not self-approve, until the owner replies with the resume signal. If the owner reports a failure at any step, capture exactly what they saw and route it back to the plan that owns that surface (dialog copy and row behaviour: 10-05; error codes and the Auth ban: 10-04; RPC guards and reassignment: 10-01) rather than patching the symptom here.
  </action>

  <what-built>
The whole feature is now live locally: a `Status` column and a `Ações` column on "Gerenciar equipe", a deactivation dialog that always asks for a replacement vendedor, one-click reactivation, and a Server Action that deactivates in Postgres and then blocks the login through Supabase Auth.

Everything provable by machine is already proven: reassignment of only in-progress clientes, preservation of won/lost clientes, the self-deactivation refusal, the loss of access on the very next request with a still-valid session, and the fact that the Auth ban value persists and can be cleared.

Two things remain that no automated test in this phase can reach, both flagged as manual-only in `10-VALIDATION.md`: whether a deactivated person is actually turned away at the **login screen**, and whether reactivating them actually lets them back in. Everything else below is a quick visual pass against the approved design contract.
  </what-built>

  <how-to-verify>
Use the disposable test account **`vendedor.b+test@raiar.local`** (password `TestVendedorB!2026`) as the person you deactivate. Do not use a real team member. Steps 7 and 8 restore that account and are **not optional** — leaving it deactivated will break the project's automated test suite.

Start the app with `npm run dev` and log in as the Supervisor.

1. Open "Gerenciar equipe". The table should now have six columns in this order: Nome, Sobrenome, Papel, Status, E-mail, Ações. Every row should show a plain grey-outlined badge reading "Ativo" or "Inativo" — no green, no red.
2. Look at your own row. It should have **no** Desativar button at all (an em dash or an empty cell). That is deliberate: you can never switch yourself off.
3. Click the Desativar (crossed-out eye) button on the `vendedor.b+test@raiar.local` row. The dialog should show the person's name in the title, followed by a four-sentence plain-language explanation ending with "Você pode reativar o acesso quando quiser."
4. Open the "Vendedor substituto" dropdown. It should list only active vendedores, and it must **not** list the person you are deactivating. Per D-03 this picker is always asked for, even when the person has nothing in progress to hand over — if you deactivate someone with zero in-progress clientes later, expect the exact same dialog, not a shortcut.
5. Without picking anyone, click "Desativar". It should refuse with "Selecione um vendedor substituto antes de confirmar." and nothing should change.
6. Now pick a substitute and click "Desativar". Expect: the dialog closes, a green banner appears saying how many in-progress clientes were transferred and to whom, and that row's badge flips to "Inativo" with the action swapping to a plain-eye "Reativar" button. Then open the clientes/kanban screen and confirm the transfer is real: the in-progress clientes of that vendedor now belong to the substitute, and any won ("ganho") or lost ("perdido") clientes of theirs are still credited to them.
7. **Manual check 1 of 2 — this is the one nothing else can test.** Log out. On the login screen, try to log in as `vendedor.b+test@raiar.local` with password `TestVendedorB!2026`. The **login itself must fail**, with an error on the login screen. It is not enough for the app to look empty after logging in — if the login succeeds at all, the Auth ban did not take effect and this check has failed.
8. Log back in as the Supervisor, go to "Gerenciar equipe", and click "Reativar" on that row. Expect: no confirmation dialog, a single click, a green banner reading "{nome} reativado(a)." and the badge back to "Ativo".
9. **Manual check 2 of 2.** Log out and log in again as `vendedor.b+test@raiar.local` with the same password. It must now **succeed**. (This is also what the automated suite re-checks in the next task, so if it fails here, say so rather than continuing.)

In plain language: you are checking that switching someone off actually locks them out of the front door, that switching them back on actually unlocks it, that their in-progress work moved to whoever you chose, and that their finished deals stayed theirs.
  </how-to-verify>

  <resume-signal>Type "approved" if steps 1-9 all behaved as described, or describe exactly which step misbehaved and what you saw instead.</resume-signal>

  <verify>
    <human-check>Reviewer confirms steps 1-9, in particular that step 7's login attempt was refused and step 9's login attempt succeeded.</human-check>
  </verify>

  <done>A human has confirmed on the running app that a deactivated member is turned away at the login screen and a reactivated member is let back in, and that the visible behaviour matches the approved design contract.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the verification account was restored and the suite is green</name>

  <read_first>
    - `tests/auth/rls-roles.test.ts` — every test in it starts with `signInAs(...)`, so the file cannot pass unless `vendedor.b+test@raiar.local` can complete a real sign-in.
    - `tests/equipe/schema-desativacao.test.ts` — its first assertion (narrowed in 10-02) requires all three seeded accounts to be `ativo`.
    - `.planning/phases/10-desativa-o-de-membro-da-equipe/10-VALIDATION.md` — the Sampling Rate section: the full suite must be green before `/gsd-verify-work`.
  </read_first>

  <files>none — verification only, no file is modified by this task</files>

  <action>
Run the full suite with `npm test` and confirm it is green.

Two specific files carry meaning beyond "tests pass", and the SUMMARY must call them out:
  - `tests/auth/rls-roles.test.ts` passing proves the deactivated verification account can complete a fresh sign-in again. That is an automated re-confirmation of the second manual check (unban restores login) and of RESEARCH assumption A1, arriving from a completely different direction than `tests/equipe/auth-ban.test.ts`'s API-level assertion.
  - `tests/equipe/schema-desativacao.test.ts` passing proves no seeded account was left deactivated by the walkthrough.

If either fails, the walkthrough left the verification account in a bad state: reactivate `vendedor.b+test@raiar.local` through the UI (or via `reativar_membro_equipe` plus an unban), then re-run before recording the phase as done. Do not "fix" it by editing the assertions.

In the SUMMARY, record the status of each `10-VALIDATION.md` Per-Task Verification Map row for this phase — which command proves it and whether it is green — and mark both Manual-Only rows as completed with the checkpoint approval as the evidence. Also note that RESEARCH assumptions A1 and A2 are now resolved (A2 by `tests/equipe/auth-ban.test.ts`, A1 by that test plus the live login in the checkpoint plus this suite run).
  </action>

  <acceptance_criteria>
    - `npm test` exits 0.
    - `npx vitest run tests/auth/rls-roles.test.ts` exits 0 — the verification account signs in again.
    - `npx vitest run tests/equipe` exits 0 — all six suites in the phase directory green together.
    - `npx vitest run tests/equipe/reassignment.test.ts -t "reassigns em_andamento"` exits 0 (EQP-01).
    - `npx vitest run tests/equipe/reassignment.test.ts -t "preserves closed"` exits 0 (EQP-04).
    - `npx vitest run tests/equipe/rls-desativar-membro.test.ts -t "last supervisor"` exits 0 (EQP-02).
    - `npx vitest run tests/equipe/rls-desativar-membro.test.ts -t "self"` exits 0 (EQP-02).
    - `npx vitest run tests/equipe/rls-desativar-membro.test.ts -t "is_supervisor"` exits 0 (EQP-03).
    - The SUMMARY lists every row of `10-VALIDATION.md`'s Per-Task Verification Map with its status.
  </acceptance_criteria>

  <verify>
    <automated>npm test</automated>
  </verify>

  <done>The full suite is green, the verification account is provably usable again, and every validation row for this phase — automated and manual — has a recorded status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /equipe route | Any authenticated user can request this URL |
| Client Component bundle -> browser | A production build is what proves nothing server-only crossed over |
| live verification account -> shared test suite | A test identity left banned breaks unrelated suites |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-14 | Elevation of Privilege | /equipe reachable by a Vendedor | high | mitigate | The existing Supervisor-only redirect is left untouched, and `profiles`' SELECT policy plus the RPC's own `is_supervisor()` guard remain the real boundaries; acceptance criteria assert the redirect line still exists |
| T-10-22 | Information Disclosure | service_role key crossing into the browser bundle | critical | mitigate | `npm run build` must succeed, and a repo-wide grep asserts no `.tsx` file imports the admin factory |
| T-10-24 | Denial of Service | verification account left banned after a partial walkthrough | medium | mitigate | Steps 8-9 of the checkpoint are mandatory, and Task 3 independently re-proves the account can sign in before the phase is recorded as done |
| T-10-25 | Repudiation | manual-only checks silently skipped | medium | mitigate | Blocking checkpoint with an explicit resume signal, plus a SUMMARY requirement to record both Manual-Only rows with the approval as evidence |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages in this plan; `10-RESEARCH.md`'s Package Legitimacy Audit records "Not applicable" for the phase |
</threat_model>

<verification>
- `npm run build` — succeeds.
- `npm test` — full suite green.
- All five pinned `-t` commands from `10-VALIDATION.md` exit 0.
- Both Manual-Only rows in `10-VALIDATION.md` recorded as completed, with the checkpoint approval named as the evidence.
- `10-VALIDATION.md`'s frontmatter can be moved to `nyquist_compliant: true` / `wave_0_complete: true` once this plan closes.
</verification>

<success_criteria>
- /equipe shows the Status column and per-row actions, with no action on the caller's own row.
- The page still redirects non-Supervisors and still shows the empty state when there are no members.
- A production build succeeds.
- A human has confirmed a deactivated member cannot log in and a reactivated one can.
- The full test suite is green and the verification account is usable again.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan's share)

| Symbol | Kind | Location |
|--------|------|----------|
| `EquipePage` | edited Server Component (fetches `ativo`, renders `EquipeList`, passes `currentUserId`) | `app/(app)/equipe/page.tsx` |
| Live verification sign-off (Auth ban blocks login; unban restores it) | recorded human approval | `10-06-SUMMARY.md` |
| Per-Task Verification Map status for all 6 rows | recorded validation status | `10-06-SUMMARY.md` |
</artifacts_produced>

<output>
Create `.planning/phases/10-desativa-o-de-membro-da-equipe/10-06-SUMMARY.md` when done.
</output>
