---
phase: 09-filtros-de-estado-e-cidade-estruturados
plan: 04
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - tests/clientes/filters-popover.test.tsx
  - components/clientes/FiltersPopover.tsx
  - components/clientes/ClienteToolbar.tsx
  - components/clientes/KanbanBoard.tsx
autonomous: true
requirements: [LOC-01, LOC-02, LOC-03]

must_haves:
  truths:
    - "LOC-03: no filtro de clientes, o controle de Estado renderiza ANTES do controle de Cidade (na ordem do JSX/DOM e de tabulação)"
    - "LOC-01: o filtro de Estado usa a constante fixa UFS (27 siglas), não mais a lista derivada dos clientes carregados (estadoOptions vira código morto e é removido)"
    - "LOC-02/D-02: o filtro de Cidade é um Combobox sobre cidades_por_estado(draft.estado), desabilitado até um Estado ser escolhido, e trocar de Estado limpa draft.cidade"
    - "Pitfall 4: clienteAtendeFiltros compara Cidade por igualdade exata case-insensível (não mais substring includes)"
  artifacts:
    - "components/clientes/FiltersPopover.tsx (reorder + UFS + Combobox de Cidade + exact-match; prop estadoOptions removida)"
    - "components/clientes/ClienteToolbar.tsx (deixa de repassar estadoOptions)"
    - "components/clientes/KanbanBoard.tsx (useMemo estadoOptions removido; deixa de passar a prop)"
    - "tests/clientes/filters-popover.test.tsx (ordem Estado-antes-de-Cidade + Cidade disabled sem Estado + exact-match de clienteAtendeFiltros)"
  key_links:
    - "estadoOptions é removido em cadeia: prop do FiltersPopover + prop do ClienteToolbar + useMemo do KanbanBoard — os três no mesmo task pra tsc não quebrar"
    - "FiltersPopover importa UFS direto; mantém o sentinela SEM_FILTRO ('Todos os estados'/'Todas as cidades') que só o contexto de filtro tem"
    - "Combobox de Cidade no filtro usa createClient().rpc('cidades_por_estado', { p_uf: draft.estado }) com wiring de draft-state próprio (não o EstadoCidadeFields, que é RHF)"
    - "clienteAtendeFiltros: trocar cliente.cidade.toLowerCase().includes(cidadeFiltro) por cliente.cidade.toLowerCase() === cidadeFiltro; contarFiltrosAtivos fica igual"
---

<objective>
Consertar o filtro de clientes (`FiltersPopover.tsx`) em quatro frentes que a pesquisa encontrou: (LOC-03) inverter a ordem para Estado antes de Cidade; (LOC-01) alimentar o Estado da constante fixa `UFS` em vez da lista derivada dos clientes carregados; (LOC-02/D-02) transformar Cidade num Combobox pesquisável sobre a RPC, desabilitado até escolher Estado; e (Pitfall 4) trocar o match de Cidade de substring para igualdade exata, já que Cidade agora é um valor escolhido de uma lista. Depende do 09-01 (RPC) e do 09-02 (UFS + combobox).

Purpose: hoje o filtro tem dois bugs reais (ordem trocada e match por substring) além do Estado depender de quais clientes estão carregados — o que faz uma UF sem clientes sumir da lista. Este plano deixa o filtro consistente com os formulários.

Output: `FiltersPopover.tsx` reescrito, a prop morta `estadoOptions` removida em cadeia (Toolbar + KanbanBoard), e um teste de contrato do popover.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-CONTEXT.md
@.planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-RESEARCH.md
@.planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-PATTERNS.md
@.planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-UI-SPEC.md
@components/clientes/FiltersPopover.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Teste de contrato do FiltersPopover — ordem, disable de Cidade e exact-match (RED)</name>
  <files>tests/clientes/filters-popover.test.tsx</files>
  <read_first>
    - components/clientes/FiltersPopover.tsx (estado atual: Cidade Input linhas 255-265 ANTES de Estado Select 267-290; clienteAtendeFiltros linhas 66-97 com includes na cidade 85-88; contarFiltrosAtivos 55-63; ClienteFiltros type 38-44; SEM_FILTRO)
    - .planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-UI-SPEC.md (§4 reorder + cascade + exact-match; Copywriting Contract dos placeholders)
    - .planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-PATTERNS.md (Fix 1-4 do FiltersPopover)
    - tests/importacao/AppSidebar.test.tsx (padrão RTL: @vitest-environment jsdom, render, screen, queries por texto/role)
    - tests/importacao/import-summary.test.tsx (padrão de assert por DOM order/container.querySelectorAll)
  </read_first>
  <action>
    Criar `tests/clientes/filters-popover.test.tsx` (`// @vitest-environment jsdom`) cobrindo o contrato-alvo (vai FALHAR/RED contra o FiltersPopover atual — Cidade é Input, ordem invertida, match por includes):

    (A) Teste de ordem (LOC-03): renderizar `<FiltersPopover>` com props mínimas (filtros=FILTROS_VAZIOS, onApply/onClear noop, categoriaOptions/produtoOptions/vendedorOptions=[], isSupervisor=false), abrir o popover (o gatilho é o botão "Filtros"), e asserir que, na ordem do DOM dentro do conteúdo do popover, o bloco com o Label "Estado" aparece ANTES do bloco com o Label "Cidade". Usar `container.querySelectorAll` sobre os labels ou comparar posições via `compareDocumentPosition`.

    (B) Teste de disable (D-02): com `draft.estado` vazio (nenhum Estado escolhido), o controle de Cidade está desabilitado (atributo disabled/aria-disabled ou placeholder "Escolha o Estado primeiro" presente).

    (C) Teste de exact-match (Pitfall 4): importar `{ clienteAtendeFiltros }` do próprio módulo e asserir que, com `filtros.cidade = "santos"`, um cliente com `cidade: "Santos"` PASSA e um cliente com `cidade: "Santos do Sul"` NÃO passa (prova que virou igualdade exata case-insensível, não substring). Fabricar o objeto cliente com a shape que clienteAtendeFiltros espera (categoria_id, produtos, cidade, estado, responsavel).

    Mockar `@/lib/supabase/client` (`vi.mock`) para o `createClient().rpc('cidades_por_estado')` resolver uma lista fixa, já que o Combobox de Cidade vai chamá-la quando um Estado estiver escolhido. Se a abertura do Popover ou a interação do Select for instável no jsdom, focar (A) e (B) no que é assertável de forma estável e manter (C) como teste puro de função (não precisa renderizar). Escrever o teste antes da implementação (RED).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tests/clientes/filters-popover.test.tsx` existe, tem `// @vitest-environment jsdom`, importa `FiltersPopover` e `clienteAtendeFiltros` de `@/components/clientes/FiltersPopover`, e mocka `@/lib/supabase/client`.
    - Cobre: ordem Estado-antes-de-Cidade (A), Cidade disabled sem Estado (B), exact-match de clienteAtendeFiltros (C: "Santos" passa, "Santos do Sul" não).
    - `npx tsc --noEmit` limpo. (Em runtime o teste está RED até o Task 2 — esperado.)
  </acceptance_criteria>
  <done>Teste de contrato do FiltersPopover escrito cobrindo ordem, disable e exact-match; RED contra a implementação atual.</done>
</task>

<task type="auto">
  <name>Task 2: FiltersPopover — reorder + UFS + Combobox de Cidade + exact-match, e remoção em cadeia de estadoOptions (GREEN)</name>
  <files>components/clientes/FiltersPopover.tsx, components/clientes/ClienteToolbar.tsx, components/clientes/KanbanBoard.tsx</files>
  <read_first>
    - components/clientes/FiltersPopover.tsx (arquivo inteiro; Fix 1-4)
    - components/clientes/ClienteToolbar.tsx (prop estadoOptions nas linhas ~48/63/87 — repassada ao FiltersPopover)
    - components/clientes/KanbanBoard.tsx (useMemo estadoOptions linhas 278-284; passagem da prop na linha ~612)
    - components/ui/combobox.tsx (API real do Combobox base-nova — LER antes de wire)
    - .planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-PATTERNS.md (Fix 1 reorder; Fix 2 UFS import + drop prop; Fix 3 Combobox disabled + cascade reset via draft-setter; Fix 4 exact-match)
    - .planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-UI-SPEC.md (§4 detalhes de placeholder/cópia; §5)
    - lib/clientes/ufs.ts (UFS); lib/supabase/client.ts (createClient)
  </read_first>
  <action>
    Editar `components/clientes/FiltersPopover.tsx`:
      Fix 1 (LOC-03): mover o bloco `flex flex-col gap-1.5` da Cidade (hoje linhas 255-265) para DEPOIS do bloco do Estado (hoje 267-290), invertendo a ordem — Estado passa a renderizar antes de Cidade. Manter o wrapper `flex flex-col gap-1.5` de cada bloco (padrão hand-rolled do popover, não FormField).
      Fix 2 (LOC-01): `import { UFS } from "@/lib/clientes/ufs"`; remover a prop `estadoOptions: string[]` da assinatura (linha 109/118); no map do Select de Estado, iterar `UFS` em vez de `estadoOptions` (`{UFS.map((uf) => <SelectItem key={uf} value={uf}>{uf}</SelectItem>)}`). Manter o sentinela `SEM_FILTRO` ("Todos os estados") como primeiro item — no filtro Estado continua opcional.
      Fix 3 (LOC-02/D-02): trocar o `<Input>` de Cidade por um `<Combobox>` (o de components/ui/combobox.tsx) sobre `cidades` obtidas de `createClient().rpc("cidades_por_estado", { p_uf: draft.estado })` num useEffect que reage a `draft.estado` (guard cancelled; lista vazia quando `!draft.estado`). `disabled={!draft.estado}`; placeholder "Escolha o Estado primeiro" (disabled) / "Todas as cidades" (habilitado, sentinela de "sem filtro"); busca "Buscar cidade..."; vazio "Nenhuma cidade encontrada". No `onValueChange` do Select de Estado, além de setar `estado`, LIMPAR `cidade` no draft: `setDraft((prev) => ({ ...prev, estado: value === SEM_FILTRO ? null : String(value), cidade: "" }))` (reset em cascata, D-02).
      Fix 4 (Pitfall 4): em `clienteAtendeFiltros`, trocar `if (cidadeFiltro && !cliente.cidade.toLowerCase().includes(cidadeFiltro)) return false` por igualdade exata: `if (cidadeFiltro && cliente.cidade.toLowerCase() !== cidadeFiltro) return false`. NÃO mexer em `contarFiltrosAtivos` (o `filtros.cidade.trim() !== ""` continua correto). O tipo `ClienteFiltros.cidade` permanece `string` (string vazia = sem filtro).

    Remover a prop morta `estadoOptions` em cadeia (no MESMO task, senão o tsc quebra):
      - `components/clientes/ClienteToolbar.tsx`: remover `estadoOptions` da destructuring, do type de props, e da passagem `<FiltersPopover ... estadoOptions={estadoOptions} />`.
      - `components/clientes/KanbanBoard.tsx`: remover o `useMemo` `estadoOptions` (linhas 278-284) e a passagem `estadoOptions={estadoOptions}` ao `<ClienteToolbar>` (linha ~612). Não mexer no `vendedorOptions` (esse continua derivado e usado).

    Depois de tudo, rodar o teste do Task 1 e confirmar GREEN.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/clientes/filters-popover.test.tsx && node -e "const fs=require('fs'); const strip=s=>s.split('\n').filter(l=>!l.trim().startsWith('//')&&!l.trim().startsWith('*')).join('\n'); const fp=strip(fs.readFileSync('components/clientes/FiltersPopover.tsx','utf8')); if(/estadoOptions/.test(fp)) throw new Error('estadoOptions ainda referenciado em FiltersPopover'); if(!/UFS/.test(fp)) throw new Error('FiltersPopover nao importa UFS'); if(/cliente\.cidade\.toLowerCase\(\)\.includes/.test(fp)) throw new Error('ainda usa includes na cidade (Pitfall 4)'); const kb=strip(fs.readFileSync('components/clientes/KanbanBoard.tsx','utf8')); if(/estadoOptions/.test(kb)) throw new Error('estadoOptions ainda em KanbanBoard'); const tb=strip(fs.readFileSync('components/clientes/ClienteToolbar.tsx','utf8')); if(/estadoOptions/.test(tb)) throw new Error('estadoOptions ainda em ClienteToolbar'); console.log('OK filtro reordenado, UFS, exact-match, estadoOptions removido em cadeia')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/clientes/filters-popover.test.tsx` passa (GREEN): Estado antes de Cidade, Cidade disabled sem Estado, exact-match.
    - FiltersPopover: Estado renderiza antes de Cidade; usa `UFS` (sem a prop estadoOptions); Cidade é um Combobox `disabled={!draft.estado}` sobre cidades_por_estado; trocar Estado limpa `cidade`; clienteAtendeFiltros usa igualdade exata (sem `.includes` na cidade).
    - `estadoOptions` não aparece mais em FiltersPopover.tsx, ClienteToolbar.tsx nem KanbanBoard.tsx (grep, ignorando comentários); o useMemo estadoOptions do KanbanBoard foi removido.
    - `npx tsc --noEmit` limpo e todo o `npx vitest run` continua verde (sem regressão em KanbanBoard/Toolbar).
  </acceptance_criteria>
  <done>Filtro com Estado antes de Cidade, Estado da constante UFS, Cidade como Combobox disable-até-Estado com reset, match exato; prop morta estadoOptions removida em cadeia; teste GREEN.</done>
</task>

</tasks>

<artifacts_produced>
Símbolos/arquivos que este plano modifica ou cria:

| Símbolo / Arquivo | Tipo | Status |
|-------------------|------|--------|
| `components/clientes/FiltersPopover.tsx` | Componente existente | Modificado: reorder, UFS, Combobox de Cidade, exact-match, prop estadoOptions removida (Task 2) |
| `clienteAtendeFiltros` (em FiltersPopover) | Função existente | Modificado: Cidade por igualdade exata (Task 2) |
| `components/clientes/ClienteToolbar.tsx` | Componente existente | Modificado: deixa de repassar estadoOptions (Task 2) |
| `components/clientes/KanbanBoard.tsx` | Componente existente | Modificado: useMemo estadoOptions removido (Task 2) |
| `tests/clientes/filters-popover.test.tsx` | Novo teste (jsdom, createClient mockado) | Criado (Task 1) |

Zero dependências npm novas. Consome UFS/combobox (09-02) e a RPC cidades_por_estado (09-01).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → RPC cidades_por_estado | O Combobox de Cidade do filtro chama a RPC direto pelo cliente do browser (RLS read-open) — leitura pública dos municípios. |
| (filtragem) | 100% em memória sobre o card set já carregado (Pitfall 7) — não há novo dado persistido nem nova fronteira de autorização; a RLS de clientes (inalterada) já limitou o que foi carregado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07 | Information Disclosure | filtro de Cidade/Estado em memória | low | accept | A filtragem roda sobre o conjunto que a RLS de clientes já restringiu ao papel do usuário (Vendedor só vê os próprios) — reordenar/estruturar o filtro não amplia visibilidade. Aceito. |
| T-09-06 | Information Disclosure | RPC cidades_por_estado no browser | low | accept | Devolve só a lista pública do IBGE sob RLS SELECT para authenticated. Aceito. |
| T-09-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` neste plano (Combobox é file copy já feito no 09-02). |
</threat_model>

<verification>
Checks de plano:
- `npx vitest run tests/clientes/filters-popover.test.tsx` verde + `npx vitest run` sem regressão.
- `npx tsc --noEmit` limpo.
- Grep: estadoOptions ausente nos 3 arquivos; UFS presente no FiltersPopover; nenhum `.includes` na comparação de cidade.
</verification>

<success_criteria>
- LOC-03: Estado aparece antes de Cidade no filtro.
- LOC-01: filtro de Estado alimentado pela constante UFS (não mais derivado dos clientes carregados).
- LOC-02/D-02: filtro de Cidade é Combobox pesquisável sobre a RPC, disable-até-Estado, com reset ao trocar.
- Pitfall 4: comparação de Cidade por igualdade exata.
- Código morto (estadoOptions + useMemo) removido em cadeia; sem regressão.
</success_criteria>

<output>
Create `.planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-04-SUMMARY.md` when done.
</output>
