---
phase: 09-filtros-de-estado-e-cidade-estruturados
plan: 05
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - components/clientes/ClienteQuickCreateForm.tsx
  - components/clientes/ClienteDetailSheet.tsx
autonomous: true
requirements: [LOC-01, LOC-02]

must_haves:
  truths:
    - "LOC-01/LOC-02: no cadastro rápido (ClienteQuickCreateForm), Estado é um Select das 27 UFs e Cidade é o Combobox pesquisável em cascata — não mais dois Inputs de texto livre"
    - "LOC-01/LOC-02: na edição (ClienteDetailSheet), Estado e Cidade usam os mesmos controles estruturados via EstadoCidadeFields"
    - "UI-SPEC §5: um cliente existente com estado legado fora das 27 siglas abre e edita normalmente (sem UI de aviso), e ao salvar com uma UF válida o valor é corrigido"
    - "UI-SPEC §3: a linha grid grid-cols-3 gap-3 (Complemento/Cidade/Estado) mantém layout, contagem e ordem de colunas — só os controles mudam"
  artifacts:
    - "components/clientes/ClienteQuickCreateForm.tsx (bloco Cidade/Estado Input → EstadoCidadeFields)"
    - "components/clientes/ClienteDetailSheet.tsx (bloco Cidade/Estado Input → EstadoCidadeFields)"
  key_links:
    - "EstadoCidadeFields substitui a linha inteira grid grid-cols-3 gap-3 (Complemento/Cidade/Estado) nos dois forms — não duplicar a cascata"
    - "os imports Select/SelectContent/etc. que ficarem sem uso após a troca devem ser limpos onde aplicável, sem quebrar os outros Selects (Responsável/Categoria/Status) que continuam"
    - "no ClienteDetailSheet, o valor legado de estado renderiza via items do Select (fallback silencioso), sem custom UI"
---

<objective>
Trocar, nos dois formulários de cliente (cadastro rápido e edição), os dois `<Input>` de texto livre de Cidade e Estado pelo componente compartilhado `EstadoCidadeFields` (criado no plano 09-03), aplicando os seletores estruturados de forma idêntica nas duas telas. Depende do 09-03 (EstadoCidadeFields + schema z.enum).

Purpose: é aqui que o usuário final passa a escolher Estado de uma lista e Cidade de uma busca em cascata ao cadastrar/editar um cliente — o coração do LOC-01/LOC-02 do ponto de vista de quem preenche o funil.

Output: `ClienteQuickCreateForm.tsx` e `ClienteDetailSheet.tsx` consumindo `EstadoCidadeFields`, sem duplicar a cascata.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-CONTEXT.md
@.planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-PATTERNS.md
@.planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-UI-SPEC.md
@components/clientes/ClienteQuickCreateForm.tsx
@components/clientes/ClienteDetailSheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ClienteQuickCreateForm usa EstadoCidadeFields no lugar dos Inputs de Cidade/Estado</name>
  <files>components/clientes/ClienteQuickCreateForm.tsx</files>
  <read_first>
    - components/clientes/ClienteQuickCreateForm.tsx (linha grid grid-cols-3 gap-3 nas linhas 228-268: Complemento/Cidade/Estado como Input; defaultValues 103-115; padrão do Select do Responsável 270-300)
    - components/clientes/EstadoCidadeFields.tsx (assinatura/props criadas no 09-03 — como passar control/form)
    - .planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-PATTERNS.md (bloco `ClienteQuickCreateForm.tsx`: substituir a linha, manter Complemento como 1ª coluna)
    - .planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-UI-SPEC.md (§3 mesma linha grid, mesmo FormItem/FormControl single-child)
  </read_first>
  <action>
    Editar SOMENTE `components/clientes/ClienteQuickCreateForm.tsx`. Substituir a linha `grid grid-cols-3 gap-3` inteira (hoje linhas 228-268, com os três FormField Complemento/Cidade/Estado como Input) por `<EstadoCidadeFields ... />`, passando as props que o componente do 09-03 espera (o `control={form.control}` e o `form`/setValue necessários para a cascata). Como o EstadoCidadeFields do 09-03 renderiza a linha grid inteira (Complemento + Cidade + Estado), a coluna Complemento continua sendo a primeira, com o mesmo `Input` — layout, contagem e ordem de colunas inalterados (UI-SPEC §3). Adicionar o import de `EstadoCidadeFields` de `@/components/clientes/EstadoCidadeFields`. Remover os imports agora sem uso APENAS se de fato nenhum outro campo os usar (o `Input` continua usado por razaoSocial/cep/numero/rua; os imports de Select continuam usados pelo Responsável) — não remover import ainda referenciado. Não mexer em defaultValues (estado/cidade continuam iniciando `""`), no onSubmit, nem no campo Responsável.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && node -e "const fs=require('fs'); const s=fs.readFileSync('components/clientes/ClienteQuickCreateForm.tsx','utf8'); if(!/EstadoCidadeFields/.test(s)) throw new Error('EstadoCidadeFields nao usado'); if(/name=\"cidade\"[\s\S]{0,120}<Input/.test(s)||/name=\"estado\"[\s\S]{0,120}<Input/.test(s)) throw new Error('Cidade/Estado ainda como Input de texto livre'); console.log('OK cadastro usa EstadoCidadeFields')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/clientes/ClienteQuickCreateForm.tsx` renderiza `<EstadoCidadeFields>` no lugar dos Inputs de Cidade/Estado; Complemento continua como 1ª coluna da mesma linha grid grid-cols-3 gap-3.
    - Não há mais `FormField name="cidade"` nem `FormField name="estado"` envolvendo um `<Input>` de texto livre neste arquivo.
    - Os campos Responsável/razaoSocial/cep/numero/rua/etc. permanecem inalterados; nenhum import em uso foi removido.
    - `npx tsc --noEmit` limpo e `npx vitest run` sem regressão.
  </acceptance_criteria>
  <done>Cadastro rápido usa o Select de UF + Combobox de Cidade em cascata via EstadoCidadeFields, mantendo o layout da linha de endereço.</done>
</task>

<task type="auto">
  <name>Task 2: ClienteDetailSheet usa EstadoCidadeFields; valor legado de Estado renderiza/edita normalmente</name>
  <files>components/clientes/ClienteDetailSheet.tsx</files>
  <read_first>
    - components/clientes/ClienteDetailSheet.tsx (linha grid grid-cols-3 gap-3 nas linhas 597-637: Complemento/Cidade/Estado como Input; toFormValues 113-132; padrão de items nos Selects de Responsável/Categoria 645-717 — por que passam items)
    - components/clientes/EstadoCidadeFields.tsx (assinatura/props do 09-03; suporte a valor fora-da-lista via items)
    - .planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-PATTERNS.md (bloco `ClienteDetailSheet.tsx`: mesma troca do cadastro + UI-SPEC §5 valor legado)
    - .planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-UI-SPEC.md (§5 estado legado fora de UFS: renderiza/edita normal, sem UI de aviso)
  </read_first>
  <action>
    Editar SOMENTE `components/clientes/ClienteDetailSheet.tsx`. Substituir a linha `grid grid-cols-3 gap-3` (hoje linhas 597-637, Complemento/Cidade/Estado como Input) por `<EstadoCidadeFields ... />`, com as mesmas props do Task 1 (control/form). A Sheet abre sempre com um valor pré-selecionado (via `toFormValues`), então o Select de Estado do EstadoCidadeFields precisa que o valor apareça mesmo antes de abrir o popup — garantir isso via a prop `items` do Select (mesmo motivo dos Selects de Responsável/Categoria deste arquivo, comentado nas linhas 93-105). UI-SPEC §5: se `cliente.estado` for um valor legado fora das 27 UFs (dado de teste antigo), ele apenas cai silenciosamente (o Select não acha o item e não exibe label — sem custom UI de aviso); ao o usuário escolher uma UF válida e salvar, o valor é corrigido. Não adicionar nenhum tratamento especial de "valor inválido". Não mexer na seção Funil (status/tarefas/histórico), no delete, nem nos outros campos. Limpar imports que ficarem sem uso apenas se realmente não forem mais referenciados por nenhum campo (Input/Select seguem usados por vários campos).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && node -e "const fs=require('fs'); const s=fs.readFileSync('components/clientes/ClienteDetailSheet.tsx','utf8'); if(!/EstadoCidadeFields/.test(s)) throw new Error('EstadoCidadeFields nao usado'); if(/name=\"cidade\"[\s\S]{0,140}<Input/.test(s)||/name=\"estado\"[\s\S]{0,140}<Input/.test(s)) throw new Error('Cidade/Estado ainda como Input de texto livre'); console.log('OK edicao usa EstadoCidadeFields')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/clientes/ClienteDetailSheet.tsx` renderiza `<EstadoCidadeFields>` no lugar dos Inputs de Cidade/Estado; Complemento segue 1ª coluna da mesma linha grid.
    - Não há mais `FormField name="cidade"`/`name="estado"` com `<Input>` de texto livre neste arquivo.
    - Um valor legado de estado fora de UFS abre a Sheet sem quebrar e sem UI de aviso (fallback silencioso do Select); as seções Funil/delete/demais campos ficam intactas.
    - `npx tsc --noEmit` limpo e `npx vitest run` sem regressão.
  </acceptance_criteria>
  <done>Edição usa o mesmo Select de UF + Combobox de Cidade via EstadoCidadeFields; valor legado renderiza/edita normalmente conforme UI-SPEC §5.</done>
</task>

</tasks>

<artifacts_produced>
Símbolos/arquivos que este plano modifica:

| Símbolo / Arquivo | Tipo | Status |
|-------------------|------|--------|
| `components/clientes/ClienteQuickCreateForm.tsx` | Componente existente | Modificado: Cidade/Estado Input → EstadoCidadeFields (Task 1) |
| `components/clientes/ClienteDetailSheet.tsx` | Componente existente | Modificado: Cidade/Estado Input → EstadoCidadeFields + §5 legado (Task 2) |

Zero dependências npm novas. Consome EstadoCidadeFields (09-03).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input do formulário → Server Action createCliente/updateCliente | Estado/Cidade escolhidos são enviados às Server Actions existentes; a re-validação server-side (estado ∈ UFS via Zod + cidade ∈ RPC) é do plano 09-06 — este plano só troca os controles de UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05 | Tampering (input validation) | UI dos forms de cadastro/edição | medium | mitigate | Os seletores estruturados reduzem erro de digitação, mas são UX — o backstop server-side (Zod z.enum + checagem cidade ∈ cidades_por_estado + constraint no banco) é garantido pelos planos 09-01/09-03/09-06. Este plano não é a fronteira de segurança. |
| T-09-08 | Denial of Service | Combobox de Cidade com ~645 municípios (SP) na edição | low | accept | Renderização virtual/scroll do popup do Combobox base-ui + filtragem por digitação; a verificação manual (VALIDATION.md) confirma responsividade com o dataset grande. Aceito. |
| T-09-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` neste plano. |
</threat_model>

<verification>
Checks de plano:
- `npx tsc --noEmit` limpo e `npx vitest run` sem regressão (suites de KanbanBoard/clientes intactas).
- Grep: `EstadoCidadeFields` usado nos dois arquivos; nenhum `FormField name="cidade"/"estado"` com `<Input>` de texto livre restante.
- Verificação manual (adiada para /gsd-verify-work, conforme VALIDATION.md Manual-Only): cadastrar e editar um cliente escolhendo UF e buscando a cidade, confirmando cascata e busca fluida com um Estado grande (SP).
</verification>

<success_criteria>
- LOC-01/LOC-02: cadastro e edição usam Select de UF + Combobox de Cidade em cascata (não mais texto livre).
- Layout da linha de endereço preservado (UI-SPEC §3); valor legado de Estado renderiza/edita sem aviso (UI-SPEC §5).
- Sem duplicação da cascata (ambos consomem EstadoCidadeFields); sem dependência npm nova; sem regressão.
</success_criteria>

<output>
Create `.planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-05-SUMMARY.md` when done.
</output>
