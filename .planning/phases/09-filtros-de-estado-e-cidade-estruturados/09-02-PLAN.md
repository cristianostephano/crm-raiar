---
phase: 09-filtros-de-estado-e-cidade-estruturados
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/clientes/ufs.ts
  - lib/clientes/normalizarEstado.ts
  - tests/clientes/estado-normalizacao.test.ts
  - components/ui/combobox.tsx
  - components/ui/input-group.tsx
autonomous: true
requirements: [LOC-01, LOC-02, LOC-04]

must_haves:
  truths:
    - "LOC-01: existe uma constante TypeScript UFS com exatamente as 27 siglas de UF, importável pelo schema Zod, pelos formulários e pelo filtro — sem objeto no banco"
    - "LOC-04: existe uma função pura normalizarEstado(raw) que mapeia variantes ('São Paulo', 'sp ', 'SP') para a sigla 'SP' por correspondência exata case/acento-insensível (D-01, sem fuzzy match), testável sem banco"
    - "LOC-02: o componente base shadcn combobox (components/ui/combobox.tsx) existe no preset base-nova (Base UI), pronto para ser o seletor pesquisável de Cidade, sem nenhuma dependência npm nova"
  artifacts:
    - "lib/clientes/ufs.ts (const UFS + type Uf)"
    - "lib/clientes/normalizarEstado.ts (função pura de normalização de Estado)"
    - "tests/clientes/estado-normalizacao.test.ts (unit test da normalização)"
    - "components/ui/combobox.tsx + components/ui/input-group.tsx (file copy shadcn base-nova)"
  key_links:
    - "UFS é `as const` para o Zod inferir z.enum e o type Uf ser o union literal das 27 siglas"
    - "normalizarEstado espelha o mapa nome→sigla usado no backfill SQL da migration 0007 (mesma fonte-verdade conceitual)"
    - "combobox instalado via `npx shadcn@latest add combobox` (file copy, não npm dep) — @base-ui/react já está instalado; trigger deve ter paridade visual com components/ui/select.tsx"
---

<objective>
Criar os blocos de frontend puros e independentes de que os consumidores das ondas 2 e 3 dependem: a constante `UFS` (27 siglas), a função pura `normalizarEstado` (normalização de Estado para LOC-04, testável sem banco), e o componente base `combobox` do shadcn (seletor pesquisável de Cidade). Nada aqui toca o banco nem depende do push da migration — por isso roda em paralelo com o plano 09-01, na mesma onda.

Purpose: separar as peças "sem I/O" (constante + função pura + primitivo de UI) do backend e dos consumidores permite testá-las isoladamente e deixá-las prontas antes que os formulários/filtro/importação as consumam.

Output: dois módulos em `lib/clientes/`, um teste unitário, e dois arquivos de componente shadcn copiados.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-CONTEXT.md
@.planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-RESEARCH.md
@.planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-PATTERNS.md
@.planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Constante UFS + função pura normalizarEstado, guiadas por teste unitário (RED → GREEN)</name>
  <files>lib/clientes/ufs.ts, lib/clientes/normalizarEstado.ts, tests/clientes/estado-normalizacao.test.ts</files>
  <read_first>
    - .planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-PATTERNS.md (bloco `lib/clientes/ufs.ts` com o array exato das 27 siglas; bloco `lib/clientes/normalizarEstado.ts` — analog é normalizeRazaoSocial)
    - .planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-RESEARCH.md (Pattern 3 UFS + z.enum; D-01 normalização simples sem fuzzy)
    - lib/importacao/dedupe.ts (precedente de função pura de normalização: stripDiacritics via NFD + filtro por codepoint, lowercase, trim — copiar o estilo, não o conteúdo)
    - tests/importacao/dedupe.test.ts (estilo de teste de função pura: describe/it/expect de "vitest", entradas fabricadas, sem .env.local)
  </read_first>
  <behavior>
    - Caso 1: `normalizarEstado('SP')` → 'SP'; `normalizarEstado('sp')` → 'SP'; `normalizarEstado(' sp ')` → 'SP' (trim + upper).
    - Caso 2 (nome por extenso, LOC-04): `normalizarEstado('São Paulo')` → 'SP'; `normalizarEstado('Sao Paulo')` → 'SP' (acento-insensível); `normalizarEstado('rio de janeiro')` → 'RJ'.
    - Caso 3 (sem match): `normalizarEstado('Estado Inexistente')` → devolve o valor em upper/trim ('ESTADO INEXISTENTE') sem lançar (D-01/discretion: não bloquear, não adivinhar sigla).
    - Caso 4 (constante): `UFS.length === 27`, contém 'SP','RJ','DF','MG', e não contém duplicatas; `UFS.includes('XX' as never)` é false.
  </behavior>
  <action>
    Criar `lib/clientes/ufs.ts` copiando VERBATIM o array do 09-PATTERNS.md: `export const UFS = [...] as const` com as 27 siglas na ordem alfabética exata (AC, AL, AP, AM, BA, CE, DF, ES, GO, MA, MT, MS, MG, PA, PB, PR, PE, PI, RJ, RN, RS, RO, RR, SC, SP, SE, TO) e `export type Uf = (typeof UFS)[number]`. O `as const` é obrigatório (o Zod do plano 09-03 depende dele para `z.enum(UFS)`).

    Criar `lib/clientes/normalizarEstado.ts` — módulo puro (sem import de Supabase, sem diretiva "use client"/"use server"), mesma forma dependency-free de `lib/importacao/dedupe.ts`. Expor `export function normalizarEstado(raw: string): string`. Passos: (1) computar uma chave de comparação = raw sem acentos (NFD + filtro de combining marks por codepoint, mesmo método do stripDiacritics do dedupe.ts — NÃO usar regex com caractere combinante literal), lowercase, espaços colapsados, trim; (2) se a chave, em upper, já for uma das 27 siglas de UFS, retornar essa sigla; (3) senão, procurar num mapa interno de NOME-POR-EXTENSO-normalizado → sigla (os 27 estados: "acre"→"AC", "alagoas"→"AL", ..., "sao paulo"→"SP", "rio de janeiro"→"RJ", "distrito federal"→"DF", etc.) — usar a MESMA normalização de chave nos nomes do mapa para o match ser acento/caso-insensível; (4) se achou, retornar a sigla; (5) senão, retornar `raw.trim().toUpperCase()` (fallback não-bloqueante de D-01). Os pares nome→sigla deste mapa são a fonte-verdade que o backfill SQL do plano 09-01 espelha.

    Criar `tests/clientes/estado-normalizacao.test.ts` cobrindo os 4 casos do bloco behavior, importando `{ normalizarEstado }` de `@/lib/clientes/normalizarEstado` e `{ UFS }` de `@/lib/clientes/ufs`. Estilo puro (vitest, sem Supabase, sem .env.local), igual dedupe.test.ts. Escrever o teste primeiro (RED contra os módulos ainda inexistentes), depois implementar até passar (GREEN).
  </action>
  <verify>
    <automated>npx vitest run tests/clientes/estado-normalizacao.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/clientes/estado-normalizacao.test.ts` passa com os 4 casos (sigla direta, nome por extenso com/sem acento, sem-match não-bloqueante, contrato da constante).
    - `lib/clientes/ufs.ts` exporta `UFS` (`as const`, 27 itens) e `type Uf`.
    - `lib/clientes/normalizarEstado.ts` é puro (sem `@/lib/supabase/*`, sem "use client"/"use server") e mapeia 'São Paulo'/'Sao Paulo'/'sp '/'SP' → 'SP'.
    - `npx tsc --noEmit` limpo.
  </acceptance_criteria>
  <done>UFS e normalizarEstado criados e cobertos por teste unitário puro; mapa nome→sigla alinhado ao backfill da migration 0007.</done>
</task>

<task type="auto">
  <name>Task 2: Instalar o componente base combobox do shadcn (preset base-nova) e conferir paridade visual com o Select</name>
  <files>components/ui/combobox.tsx, components/ui/input-group.tsx</files>
  <read_first>
    - .planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-UI-SPEC.md (Registry Safety; §2 paridade visual do trigger com SelectTrigger: h-8, text-sm, rounded-lg border-input bg-transparent, disabled:opacity-50)
    - .planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-PATTERNS.md (bloco `components/ui/combobox.tsx`: instalar via CLI, diff contra select.tsx, importar de @base-ui/react/combobox)
    - components/ui/select.tsx (referência de paridade: import de @base-ui/react/select, classes do trigger/popup/item, CheckIcon do indicador)
    - components.json (preset base-nova, registries {} — nenhum registry de terceiro)
  </read_first>
  <action>
    Rodar `npx shadcn@latest add combobox` para copiar `components/ui/combobox.tsx` (e sua dependência de arquivo `components/ui/input-group.tsx`) no preset `base-nova` já configurado. É um FILE COPY do registry oficial do shadcn — NÃO adiciona dependência npm (a única dep npm, `@base-ui/react`, já está instalada e em uso em `components/ui/select.tsx`). Não hand-escrever o arquivo do zero.

    Se por algum motivo o `shadcn add combobox` não estiver disponível para o preset, o fallback é escrever `components/ui/combobox.tsx` à mão sobre `@base-ui/react/combobox`, espelhando a estrutura de `components/ui/select.tsx` (mesmos subcomponentes Trigger/Popup/Item, mesmas classes) — consultar a doc do Base UI Combobox via Context7 (`@base-ui-components/react`) para a API exata dos subcomponentes.

    Após instalar, abrir `components/ui/combobox.tsx` e conferir (por diff mental contra select.tsx) que o trigger reusa as classes de paridade do UI-SPEC §2 (h-8/`data-[size=default]:h-8`, text-sm, rounded-lg, border-input, bg-transparent, disabled:opacity-50 disabled:cursor-not-allowed) e que o popup usa `bg-popover`, `rounded-lg`, `shadow-md`. Ajustar SOMENTE se o CLI gerar algo divergente do select — não reescrever o arquivo, só alinhar classes de trigger/popup se necessário. Não wire nenhum consumidor aqui (isso é dos planos 09-03/09-04); este task só garante que o primitivo existe e é visualmente compatível.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const fs=require('fs'); if(!fs.existsSync('components/ui/combobox.tsx')) throw new Error('combobox.tsx ausente'); const c=fs.readFileSync('components/ui/combobox.tsx','utf8'); if(!/@base-ui\/react/.test(c)) throw new Error('combobox nao usa @base-ui/react (preset errado?)'); const pkg=JSON.parse(fs.readFileSync('package.json','utf8')); const deps={...pkg.dependencies,...pkg.devDependencies}; if(!deps['@base-ui/react']) throw new Error('@base-ui/react deveria ja estar instalado'); console.log('OK combobox instalado sem dep npm nova')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/ui/combobox.tsx` e `components/ui/input-group.tsx` existem e importam de `@base-ui/react` (preset base-nova, não cmdk/Radix).
    - Nenhuma dependência npm nova foi adicionada ao `package.json` (só file copy; `@base-ui/react` já estava presente).
    - O trigger do combobox tem paridade de classe com `SelectTrigger` (h-8, text-sm, rounded-lg, border-input, bg-transparent, disabled:opacity-50).
    - `npx tsc --noEmit` limpo.
  </acceptance_criteria>
  <done>Componente base combobox (base-nova, Base UI) disponível em components/ui/, com paridade visual com o Select e zero dependência npm nova.</done>
</task>

</tasks>

<artifacts_produced>
Símbolos/arquivos que este plano cria:

| Símbolo / Arquivo | Tipo | Status |
|-------------------|------|--------|
| `UFS` / `type Uf` (`lib/clientes/ufs.ts`) | Constante `as const` das 27 siglas + type union | Criado (Task 1) |
| `normalizarEstado(raw)` (`lib/clientes/normalizarEstado.ts`) | Função pura de normalização (LOC-04) | Criado (Task 1) |
| `tests/clientes/estado-normalizacao.test.ts` | Unit test puro | Criado (Task 1) |
| `components/ui/combobox.tsx` | Componente base shadcn (Base UI) | Criado (Task 2) |
| `components/ui/input-group.tsx` | Dependência de arquivo do combobox | Criado (Task 2) |

Zero dependências npm novas. Nenhum consumidor é wired aqui.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Este plano só cria uma constante, uma função pura sem I/O e um file copy de componente. Nenhum input novo do usuário atravessa uma fronteira, nenhuma chamada de rede em runtime, nenhum dado persistido. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-SC | Tampering (supply chain) | `npx shadcn@latest add combobox` | low | accept | File copy do registry OFICIAL do shadcn; única dep npm (`@base-ui/react`) já instalada e em produção (`select.tsx`). RESEARCH Package Legitimacy Audit: zero pacotes novos instalados por esta fase — nenhum checkpoint de legitimidade aplicável (verificado via grep no package.json no verify). |
| T-09-04 | Tampering (input validation, downstream) | constante UFS como fonte de validação | low | mitigate | UFS é a fonte usada tanto no Zod (09-03) quanto na re-validação server-side (09-06) e na constraint do banco (09-01) — defesa em profundidade; esta constante sozinha é só UX, o backstop real é a constraint chk_estado_valido. |
</threat_model>

<verification>
Checks de plano:
- `npx vitest run tests/clientes/estado-normalizacao.test.ts` verde.
- `npx tsc --noEmit` limpo.
- Grep: `components/ui/combobox.tsx` existe e usa `@base-ui/react`; `package.json` sem dependência nova.
</verification>

<success_criteria>
- LOC-01: `UFS` disponível como constante `as const` das 27 siglas, importável por Zod/forms/filtro.
- LOC-04: `normalizarEstado` mapeia variantes comuns para a sigla correta (D-01), coberta por teste puro.
- LOC-02: `combobox` base do shadcn (base-nova) disponível, com paridade visual ao Select, sem dependência npm nova.
</success_criteria>

<output>
Create `.planning/phases/09-filtros-de-estado-e-cidade-estruturados/09-02-SUMMARY.md` when done.
</output>
