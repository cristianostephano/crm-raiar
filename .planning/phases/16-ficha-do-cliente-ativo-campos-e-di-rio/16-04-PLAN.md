---
phase: 16-ficha-do-cliente-ativo-campos-e-di-rio
plan: 04
type: execute
wave: 4
depends_on: ["16-02", "16-03"]
files_modified:
  - components/clientes/DiarioTimeline.tsx
  - components/clientes/ClienteDetailSheet.tsx
  - tests/clientes/diario-timeline.test.tsx
autonomous: false
requirements: [ATV-01, ATV-02, DIAR-01]

must_haves:
  truths:
    - "ATV-01 (critério de sucesso 1): a ficha de um cliente com status ganho mostra e permite editar Nome fantasia e CNPJ; a ficha de um cliente que não está ganho não mostra nenhum dos dois; e a tela de cadastro rápido continua sem pedir nenhum dos dois (arquivo literalmente intocado)"
    - "ATV-02 (critério de sucesso 2): a ficha de um cliente ganho mostra um campo de escolha de frequência de pedidos, alimentado pela lista gerenciada pelo Supervisor, com um texto de apoio SEMPRE visível dizendo que é informação de apoio e não gera alerta, cobrança ou tarefa na Agenda"
    - "ATV-02: o campo de frequência de pedidos não dispara nenhuma escrita imediata, não cria visita, não cria tarefa e não toca em nada da Agenda — ele é salvo junto do resto pelo botão de salvar que já existe"
    - "ATV-02: quando o vocabulário estiver vazio, a ficha diz isso em texto simples no lugar do campo de escolha, em vez de mostrar um seletor sem opções"
    - "DIAR-01 (critério de sucesso 3): a ficha mostra uma seção 'Diário' com as visitas e tarefas já concluídas, cada entrada com o resumo, a data e o autor, da mais recente para a mais antiga"
    - "DIAR-01: o diário aparece para qualquer cliente, independentemente do status — um cliente pode ter concluído tarefas de prospecção muito antes de virar ganho"
    - "DIAR-01: a seção 'Histórico' que já existe continua exatamente como está, ao lado do diário — o diário é adicional, nunca substituto"
    - "DIAR-01: o diário é carregado pela consulta própria criada no plano anterior, NUNCA filtrando no navegador a lista de histórico já carregada"
    - "Entrada de conclusão sem resumo real (caminho antigo da caixinha de concluir tarefa) renderiza igual a uma com resumo real — sem aviso, sem selo de 'faltando resumo', sem caso especial (decisão D1)"
    - "Cliente ganho antigo com os três campos em branco renderiza normalmente, sem aviso e sem bloquear o salvamento — dado histórico não é estado de erro"
    - "A supressão de lint documentada na ficha do cliente não piora: o arquivo continua com exatamente as duas supressões que já tinha e com um único efeito; o estado novo por cliente entra como UMA linha a mais no bloco de reset que já existe"
    - "Zero componente shadcn novo, zero dependência npm nova, zero tela nova, zero diálogo novo"
  artifacts:
    - "components/clientes/DiarioTimeline.tsx (novo) — componente de leitura, irmão estrutural do componente de histórico"
    - "três campos condicionais dentro da seção 'Dados do cliente' de components/clientes/ClienteDetailSheet.tsx"
    - "seção 'Diário' dentro da seção 'Funil' de components/clientes/ClienteDetailSheet.tsx, logo após 'Histórico'"
    - "tests/clientes/diario-timeline.test.tsx (novo)"
  key_links:
    - "o componente do diário não pode ter nenhum controle de escrita: a trilha de auditoria é a única tabela do projeto deliberadamente inescrevível pela aplicação, e um botão ali seria a primeira trinca nessa garantia"
    - "o campo de escolha desta ficha precisa receber a lista de itens explicitamente, como todos os outros campos de escolha deste mesmo arquivo já fazem — sem isso, um valor já selecionado na abertura da ficha não é resolvido para rótulo legível até o menu ser aberto uma vez (motivo documentado no próprio arquivo)"
    - "os três campos precisam continuar sendo preenchidos a partir do cliente lido mesmo quando não são renderizados: é isso que faz salvar a ficha de um cliente que deixou de ser ganho devolver os valores em vez de apagá-los"
    - "recarregar o diário nos mesmos pontos em que o histórico já é recarregado é o que impede que uma tarefa concluída dentro da própria ficha só apareça no diário depois de fechar e reabrir a ficha"
    - "acrescentar um segundo efeito em vez de uma linha no bloco de reset existente pioraria a supressão de lint que o ROADMAP desta fase pede explicitamente para não piorar"
---

<objective>
Fechar a fase na tela: um componente de leitura novo para o diário e as duas inserções na ficha do cliente — os três campos que só aparecem quando o cliente está ganho, e a seção "Diário" logo abaixo do histórico. Mais o teste de renderização do componente novo e a verificação humana no navegador.

Purpose: é o único plano da fase que o dono do projeto enxerga. Também é o que mexe no arquivo mais delicado do codebase — a ficha do cliente, que já carrega uma supressão de lint documentada e que o ROADMAP desta fase manda explicitamente não piorar. Por isso as mudanças aqui são cirúrgicas e verificadas mecanicamente: uma linha a mais no bloco de reset que já existe, nenhum efeito novo, nenhum controle de escrita no componente do diário, e nenhuma alteração no que já estava funcionando (histórico completo, caixinha de concluir tarefa, formulário de cadastro rápido).

Output: a ficha do cliente ativo completa, verificada por teste de renderização e por olho humano no navegador.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-ficha-do-cliente-ativo-campos-e-di-rio/16-UI-SPEC.md
@components/clientes/HistoricoTimeline.tsx
</context>

<source_audit>
Auditoria completa de cobertura das quatro fontes (GOAL / REQ / RESEARCH / CONTEXT) no `<source_audit>` do plano `16-01-PLAN.md` — conjunto de planos 16-01 a 16-04, 0 itens MISSING. Este plano cobre a camada de tela de ATV-01, ATV-02 e DIAR-01.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Componente de leitura do diário</name>
  <files>components/clientes/DiarioTimeline.tsx, tests/clientes/diario-timeline.test.tsx</files>
  <read_first>
    - components/clientes/HistoricoTimeline.tsx (já no contexto) — o irmão estrutural: componente de leitura pura, sem estado, sem diretiva de cliente, recebendo a lista pronta por propriedade, com estado vazio em texto apagado e formatação de data a partir da string ISO com as funções de data já usadas no projeto. Extrair a formatação de data literal e o comentário de cabeçalho que registra a ausência deliberada de controles de escrita.
    - components/agenda/AgendaItemRow.tsx — os dois selos de origem, verbatim: variante e ícone de cada um, e os dois rótulos. O componente novo reusa exatamente esses dois tratamentos, sem inventar variante nem ícone novo.
    - lib/supabase/queries/clientes.ts — o tipo de entrada do diário criado no plano 16-03 (cinco campos), que é a propriedade de entrada deste componente.
    - .planning/phases/16-ficha-do-cliente-ativo-campos-e-di-rio/16-UI-SPEC.md (já no contexto) — a seção do contrato de copy referente ao diário (rótulo, estado vazio, os dois rótulos de selo, formato de data, texto do corpo, linha de autor, texto de substituição do autor, ordenação) e o esboço estrutural do componente com as classes exatas. A copy é contrato: reproduzir literalmente, com acentuação.
    - tests/agenda/agenda-item-row.test.tsx — o molde mais próximo de teste de renderização de componente neste projeto (ambiente com DOM já configurado por padrão de nome de arquivo; biblioteca de teste de componentes já instalada).
  </read_first>
  <action>
    Criar `components/clientes/DiarioTimeline.tsx`, componente de LEITURA pura recebendo a lista de entradas por propriedade. Nenhum estado, nenhuma busca de dados, nenhuma diretiva de cliente (o irmão de histórico também não tem — só é renderizado dentro de um componente que já é de cliente).

    Comportamento, seguindo o esboço do contrato de interface:
      - lista vazia: uma linha de texto apagado com a copy exata do contrato (`Nenhuma visita ou tarefa concluída ainda.`), no mesmo tratamento visual que o estado vazio do histórico já usa;
      - lista com entradas: uma lista ordenada, uma entrada por item, cada item num contêiner com borda arredondada e o mesmo espaçamento interno já usado pelo bloco de adicionar tarefa da própria ficha (é reuso de classe existente, não estilo novo);
      - dentro de cada entrada, na ordem: uma linha com o selo de origem à esquerda e a data à direita; o texto do corpo; e a linha de autor;
      - selo de origem: para entrada de tarefa concluída, a variante, o ícone e o rótulo `Prospecção` copiados do componente de linha da Agenda; para entrada de visita concluída, a variante, o ícone e o rótulo `Visita` do mesmo lugar. Nada de variante nova, nada de ícone novo, nada de cor nova;
      - data: formatada a partir da string ISO com as mesmas funções de data e o mesmo formato literal que o componente de histórico já usa (é a mesma coluna de origem, então tem que exibir igual). Nunca construir data a partir da string com o construtor nativo — é o defeito de fuso horário já documentado no marco;
      - corpo: o texto da descrição, verbatim, sem prefixo e sem transformação. **Sem nenhum caso especial** entre um resumo escrito pelo vendedor e o texto genérico que o caminho antigo grava (decisão D1 do contrato de interface): o componente não distingue os dois e não mostra nenhum aviso de resumo faltando;
      - autor: o nome quando vier preenchido; a copy exata `Autor não identificado` quando vier nulo.

    Comentário de cabeçalho obrigatório, no tom do componente de histórico: registrar que é somente leitura, sem nenhum controle de escrita em lugar nenhum, porque a trilha de auditoria é escrita exclusivamente por gatilhos internos do banco e não tem permissão de escrita para a aplicação; e registrar que a ordenação vem pronta da consulta — este componente nunca reordena.

    Nenhum componente shadcn novo, nenhuma dependência npm nova.

    Criar `tests/clientes/diario-timeline.test.tsx`, teste de renderização (ambiente com DOM, pelo sufixo do arquivo). Casos exigidos, com estas palavras-chave nos nomes (sem acento):
      - **`vazio`**: lista vazia renderiza a copy exata do estado vazio e nenhum item de lista.
      - **`prospeccao`**: uma entrada de tarefa concluída renderiza o rótulo `Prospecção`.
      - **`visita`**: uma entrada de visita concluída renderiza o rótulo `Visita`.
      - **`data`**: a data renderizada corresponde exatamente ao formato do contrato para uma string ISO conhecida.
      - **`resumo`**: o texto da descrição aparece verbatim; e uma entrada com o texto genérico do caminho antigo aparece do mesmo jeito, sem nenhum aviso adicional (prova da decisão D1).
      - **`autor`**: o nome do autor aparece quando preenchido.
      - **`autornulo`**: a copy exata `Autor não identificado` aparece quando o nome do autor vier nulo.
      - **`ordem`**: a ordem renderizada é exatamente a ordem recebida por propriedade — o componente não reordena.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx vitest run tests/clientes/diario-timeline.test.tsx && node -e "const fs=require('fs');const c=fs.readFileSync('components/clientes/DiarioTimeline.tsx','utf8');for(const s of ['Nenhuma visita ou tarefa concluída ainda.','Prospecção','Visita','Autor não identificado','dd/MM/yyyy HH:mm','parseISO'])if(!c.includes(s))throw new Error('falta a copy/formato exato: '+s);if(/onClick|onChange|onSubmit|<Button|useState|useEffect/.test(c))throw new Error('o componente do diario e somente leitura, sem controle de escrita e sem estado');if(/new Date\(/.test(c))throw new Error('nunca construir data a partir da string ISO com o construtor nativo');if(/\.sort\(/.test(c))throw new Error('a ordem vem pronta da consulta; o componente nao reordena');if(/createClient|supabase/.test(c))throw new Error('o componente nao busca dado; recebe por propriedade');const t=fs.readFileSync('tests/clientes/diario-timeline.test.tsx','utf8');const n=(t.match(/\bit\(\s*[\"'][^\"']+/g)||[]).map(s=>s.toLowerCase());for(const k of ['vazio','prospeccao','visita','data','resumo','autornulo','autor','ordem'])if(!n.some(x=>x.includes(k)))throw new Error('diario-timeline.test.tsx: falta caso com a palavra '+k);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/clientes/DiarioTimeline.tsx` existe, é somente leitura (sem estado, sem efeito, sem botão, sem manipulador de evento, sem acesso a dados) e recebe as entradas por propriedade.
    - A copy é literal e acentuada: estado vazio, os dois rótulos de selo e o texto de substituição do autor.
    - A data é formatada com o mesmo formato do componente de histórico, a partir da string ISO com a função de análise do projeto — nunca com o construtor nativo.
    - Os dois selos reusam variante e ícone do componente de linha da Agenda; nenhuma variante, cor ou ícone novo.
    - O componente não reordena a lista.
    - `tests/clientes/diario-timeline.test.tsx` passa inteiro, com casos cujos nomes contêm `vazio`, `prospeccao`, `visita`, `data`, `resumo`, `autor`, `autornulo` e `ordem`.
    - `npx tsc --noEmit` limpo e `npm run lint` limpo.
  </acceptance_criteria>
  <done>Componente de leitura do diário pronto e testado, com copy de contrato e zero superfície de escrita.</done>
</task>

<task type="auto">
  <name>Task 2: Os três campos condicionais e a seção Diário na ficha do cliente</name>
  <files>components/clientes/ClienteDetailSheet.tsx</files>
  <read_first>
    - components/clientes/ClienteDetailSheet.tsx INTEIRO. É o arquivo mais delicado da fase; ler antes de escrever. Extrair, especificamente: (a) o bloco de estado local no topo do componente; (b) o comentário longo perto do topo que explica por que TODO campo de escolha deste arquivo precisa receber a lista de itens explicitamente — sem isso, um valor já selecionado na abertura não é resolvido para rótulo legível; (c) o efeito único, com o bloco de reset ao fechar (que carrega a supressão de lint documentada) e as quatro buscas independentes que ele dispara ao abrir; (d) a função que monta os valores iniciais do formulário a partir do cliente lido, e os valores padrão do formulário; (e) a posição exata do último campo da seção de dados do cliente e do separador que abre a seção de funil; (f) o bloco condicional de frequência de VISITA já existente dentro da seção de funil — é o molde de "campo que só aparece quando o cliente está ganho"; (g) as duas funções que já recarregam o histórico e onde elas são chamadas; (h) o bloco final da seção de funil, com o rótulo de histórico e o componente correspondente.
    - components/clientes/DiarioTimeline.tsx (criado no Task 1) — a propriedade de entrada.
    - lib/supabase/queries/clientes.ts — o tipo de entrada do diário e os três campos novos do tipo da ficha (plano 16-03).
    - app/actions/funil.ts — a Server Action do diário (plano 16-03).
    - app/actions/clientes.ts — a Server Action do catálogo de frequências (plano 16-02).
    - .planning/phases/16-ficha-do-cliente-ativo-campos-e-di-rio/16-UI-SPEC.md (já no contexto) — o contrato de copy dos três campos e o "Layout & Interaction Contract" inteiro, em especial os pontos 1 a 4 da subseção da ficha. A copy é contrato: reproduzir literalmente, com acentuação.
    - .planning/STATE.md — Deferred Items: a entrada sobre a supressão de lint deste arquivo ("não piorar").
  </read_first>
  <action>
    Todas as mudanças são neste único arquivo. Ordem sugerida.

    **1. Estado novo.** Duas variáveis de estado: a lista de entradas do diário (começando vazia) e a lista de opções do catálogo de frequências (começando vazia). Colocar as duas junto do bloco de estado da seção de funil / catálogos que já existe.

    **2. Bloco de reset ao fechar — exatamente UMA linha a mais.** Acrescentar apenas o reset da lista do diário, porque ela é dado POR CLIENTE. O catálogo de frequências **não** entra nesse bloco, pelo mesmo motivo que o catálogo de tipos de tarefa já não entra: é catálogo global, não muda ao trocar de cliente, e resetá-lo só causaria uma busca a mais.
    Proibições explícitas: não criar um segundo efeito; não remover nem mover a supressão de lint existente; não reescrever o bloco. Uma linha a mais, nada além disso. O ROADMAP desta fase pede literalmente para não piorar essa supressão.

    **3. Duas buscas a mais dentro do efeito que já existe**, ao lado das quatro que já estão lá, seguindo o mesmo padrão: cada uma independente, cada uma abandonando silenciosamente em caso de cancelamento ou erro, e o fracasso de uma nunca bloqueando as outras. Uma busca o diário do cliente; a outra busca o catálogo de frequências.

    **4. Valores do formulário.** Acrescentar os três campos aos valores padrão do formulário (string vazia) e à função que monta os valores a partir do cliente lido, com o padrão de vazio já usado pelos demais campos opcionais de texto. **Isto vale para qualquer cliente, ganho ou não** — é justamente o que faz um envio devolver o valor guardado em vez de apagá-lo quando os campos não estão sendo renderizados. Registrar esse motivo num comentário curto ali.

    **5. Os três campos condicionais**, dentro do conteúdo do formulário, imediatamente depois do último campo da seção de dados do cliente e ANTES do separador que abre a seção de funil. Renderizados apenas quando o status do cliente for ganho — mesma expressão de condição literal já usada pelo bloco de frequência de visita da seção de funil. Sem subtítulo novo, sem separador novo: os campos aparecem no fluxo dos demais, exatamente como o contrato de interface especifica.
      - Nome fantasia e CNPJ lado a lado, numa grade de duas colunas com o mesmo espaçamento das duas grades de duas colunas que o arquivo já tem. Rótulos exatos `Nome fantasia` e `CNPJ`. Nome fantasia sem texto de dica; CNPJ com o texto de dica exato do contrato, que é só uma pista visual de formato — **sem máscara de digitação e sem verificação de formato**.
      - Frequência de pedidos como campo de escolha, com rótulo exato `Frequência de pedidos` e texto de espaço vazio exato `Selecione a frequência`. **Passar a lista de itens explicitamente** ao campo de escolha, montada a partir do catálogo carregado — o próprio arquivo documenta por que isso é obrigatório aqui.
      - Abaixo do campo de escolha, o texto de apoio exato do contrato (`Informação de apoio — não gera alerta, cobrança ou tarefa na Agenda.`), no tratamento de texto apagado, **sempre visível** — não é mensagem de erro, não é dica escondida atrás de balão. Esse texto é a expressão literal do critério de aceitação do ATV-02.
      - **Estado de vocabulário vazio:** se o catálogo carregado não tiver nenhuma opção, renderizar no lugar do campo de escolha uma linha de texto apagado dizendo `Nenhuma frequência cadastrada ainda.` — mesmo tratamento que o bloco de produtos consumidos já usa quando não há produto cadastrado. Sem isso, o campo apareceria como um seletor que não abre nada. (Extensão defensiva do contrato, seguindo precedente existente no mesmo arquivo.)
      - Os três são salvos pelo botão de salvar que já existe. **Nenhum deles pode disparar escrita imediata ao mudar** — em particular, o campo de frequência de pedidos NÃO segue o padrão do campo de frequência de visita, que salva na hora por uma ação dedicada. Aqui é campo comum de formulário. Registrar essa diferença num comentário curto, porque os dois campos têm nome parecido e ficam a poucas linhas um do outro.

    **6. Recarga do diário.** Criar uma função de recarga do diário no mesmo molde da de histórico já existente, e chamá-la em todos os pontos onde a de histórico já é chamada. Motivo: concluir uma tarefa pela caixinha da própria ficha grava uma entrada que o diário mostra — sem a recarga, ela só apareceria depois de fechar e reabrir a ficha.
    **Proibido** derivar o diário filtrando a lista de histórico já carregada no navegador: a consulta própria do plano 16-03 é a única fonte, e o filtro por tipo vive no SQL.

    **7. Seção "Diário"** dentro da seção de funil, imediatamente DEPOIS do bloco de histórico que já existe: um contêiner no mesmo padrão do bloco de histórico, com o rótulo exato `Diário` e o componente criado no Task 1 recebendo a lista. Renderizado para QUALQUER cliente, sem condição de status — um cliente pode ter concluído tarefas de prospecção muito antes de virar ganho.
    O bloco de histórico existente fica **exatamente como está**: mesmo rótulo, mesmo componente, mesma posição. O diário é adicional, nunca substituto.

    **O que este task NÃO faz** (verificado mecanicamente): não altera o formulário de cadastro rápido; não altera a caixinha de concluir tarefa nem a função que ela chama (decisão D1); não altera o componente de histórico; não altera o quadro kanban nem o cartão do cliente; não cria diálogo, rota, tela ou componente shadcn novo; não instala nada.
  </action>
  <verify>
    <automated>test -z "$(git diff --name-only HEAD -- components/clientes/ClienteQuickCreateForm.tsx components/clientes/HistoricoTimeline.tsx components/clientes/KanbanBoard.tsx components/clientes/ClienteCard.tsx supabase/migrations/)" && npx tsc --noEmit && npm run lint && node -e "const fs=require('fs');const s=fs.readFileSync('components/clientes/ClienteDetailSheet.tsx','utf8');for(const c of ['Nome fantasia','CNPJ','Frequência de pedidos','Selecione a frequência','Informação de apoio — não gera alerta, cobrança ou tarefa na Agenda.','Nenhuma frequência cadastrada ainda.','Diário','DiarioTimeline','getDiarioAction','getFrequenciasPedido','nomeFantasia','frequenciaPedidos'])if(!s.includes(c))throw new Error('falta na ficha: '+c);if(!s.includes('HistoricoTimeline')||!s.includes('Histórico'))throw new Error('o bloco de historico existente precisa continuar intacto');const dis=(s.match(/eslint-disable/g)||[]).length;if(dis!==2)throw new Error('o arquivo precisa continuar com exatamente 2 supressoes de lint, achei '+dis);const ef=(s.match(/useEffect\(/g)||[]).length;if(ef!==1)throw new Error('nenhum efeito novo: esperado 1 useEffect, achei '+ef);const ganho=(s.match(/statusAcompanhamento\s*===\s*[\"']ganho[\"']/g)||[]).length;if(ganho<2)throw new Error('os campos novos precisam ser condicionais ao status ganho, como o bloco de frequencia de visita ja e');const iDiario=s.indexOf('<DiarioTimeline');const iHist=s.indexOf('<HistoricoTimeline');if(iDiario<0||iHist<0||iDiario<iHist)throw new Error('o diario vem DEPOIS do historico');const iLojas=s.indexOf('numeroDeLojas');const iSep=s.indexOf('Funil');const iNF=s.indexOf('nomeFantasia');if(!(iNF>iLojas))throw new Error('os campos novos vem depois do numero de lojas');if(!(iNF<iSep))throw new Error('os campos novos vem antes da secao de funil');if(!/refreshDiario/.test(s))throw new Error('falta a recarga do diario');const rh=(s.match(/refreshHistorico\(\)/g)||[]).length;const rd=(s.match(/refreshDiario\(\)/g)||[]).length;if(rd<rh-1)throw new Error('a recarga do diario precisa acontecer nos mesmos pontos da de historico');if(/historico[\s\S]{0,80}\.filter\(/.test(s))throw new Error('o diario nunca pode ser derivado filtrando o historico ja carregado no navegador');if(!/items=\{[^}]*frequencia/i.test(s)&&!/frequenciasPedido[\s\S]{0,400}items=/.test(s))throw new Error('o campo de escolha precisa receber a lista de itens explicitamente');if(/atualizarFrequenciaPedidos|salvarFrequenciaPedidos/.test(s))throw new Error('a frequencia de pedidos e campo comum de formulario, sem escrita imediata dedicada');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Os arquivos de cadastro rápido, histórico, quadro kanban, cartão de cliente e a pasta de migrations saem vazios em `git diff --name-only HEAD`.
    - A ficha contém a copy exata e acentuada dos três rótulos, do texto de espaço vazio, do texto de apoio, do estado de vocabulário vazio e do rótulo do diário.
    - Os três campos são condicionais ao status ganho e ficam depois do campo de número de lojas e antes da seção de funil.
    - A seção do diário vem depois do bloco de histórico, que continua intacto.
    - O arquivo continua com exatamente duas supressões de lint e exatamente um efeito.
    - Existe recarga do diário, chamada nos mesmos pontos em que a de histórico já é chamada.
    - O diário não é derivado por filtro sobre o histórico já carregado.
    - O campo de escolha da frequência recebe a lista de itens explicitamente e não dispara escrita imediata.
    - `npx tsc --noEmit` limpo e `npm run lint` limpo.
  </acceptance_criteria>
  <done>Ficha do cliente ativo completa: três campos condicionais salvos pelo caminho existente e seção Diário abaixo do histórico, sem piorar o débito de lint do arquivo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana no navegador — ficha do cliente ativo e diário</name>
  <action>Pausar a execução. Subir a aplicação (`npm run dev`) e pedir ao dono do projeto que percorra o roteiro abaixo no navegador. É a mesma etapa de verificação visual já praticada nas Fases 4, 9, 13 e 15, e é o único ponto desta fase em que um humano confere a experiência real. Não marcar a fase como concluída sem o sinal de aprovação.</action>
  <what-built>
    A ficha do cliente (a gaveta que abre quando você clica num card do funil) ganhou duas coisas.

    **1. Três campos que só aparecem quando o cliente já é seu.** Quando um cliente está com o status "Ganho", a ficha dele passa a mostrar, na parte de cima, três campos que não existiam: **Nome fantasia**, **CNPJ** e **Frequência de pedidos**. Enquanto o cliente ainda está sendo trabalhado, esses três campos simplesmente não aparecem — e o cadastro rápido de um cliente novo continua exatamente como estava, sem pedir nenhum deles. Essa é a ideia toda: perguntar essas coisas só no momento em que elas passam a fazer sentido.

    - Nome fantasia e CNPJ são texto livre. O CNPJ mostra um formato de exemplo como pista, mas **não** valida nada e não força máscara — se você digitar de outro jeito, ele aceita. Validar CNPJ de verdade está fora do escopo deste projeto por decisão anterior sua.
    - Frequência de pedidos é uma **escolha de lista**, e a lista é a que você gerencia em Configurações (a quinta aba, criada nos planos anteriores desta fase). Embaixo dela tem uma frase fixa: *"Informação de apoio — não gera alerta, cobrança ou tarefa na Agenda."* Ela está ali de propósito, sempre visível, porque essa é a regra: essa frequência é só uma anotação de contexto. **Ela não cria visita, não cria tarefa e não aparece na Agenda.** Quem manda na sua Agenda continua sendo só a frequência de VISITA, que fica logo abaixo, na parte do Funil.
    - Os três são salvos junto com o resto, no botão "Salvar alterações" que já existe. Nada é salvo sozinho enquanto você digita.

    **2. Um "Diário" na parte de baixo da ficha.** Logo abaixo do "Histórico" que já existia, tem uma seção nova chamada **Diário**. A diferença entre as duas, em uma frase: o Histórico registra *tudo* que aconteceu com o card (mudou de etapa, mudou de status, concluiu tarefa); o Diário mostra só **o que foi de fato feito com o cliente** — as visitas e as tarefas concluídas, cada uma com o resumo que a pessoa escreveu, a data e quem foi. Da mais recente para a mais antiga.

    Um detalhe honesto: as tarefas concluídas pela caixinha de marcar dentro da própria ficha (o caminho antigo, que não pede resumo) aparecem no Diário com um texto genérico, do tipo "Tarefa marcada como concluída", em vez de um resumo escrito. Isso é esperado — foi a decisão que você tomou no checkpoint da fase anterior, de deixar aquela caixinha como está. As conclusões feitas pela tela da Agenda, essas sim, aparecem com o resumo real.

    O Diário aparece para **qualquer** cliente, não só para os ganhos — porque um cliente pode ter tido várias tarefas de prospecção concluídas muito antes de virar venda.
  </what-built>
  <how-to-verify>
    1. Rode `npm run dev` e entre como **Supervisor**.
    2. **Configurações → Frequência de pedidos.** Confirme que a quinta aba existe e que os valores iniciais estão lá. Adicione um valor novo (por exemplo "Sob demanda") e desative um dos que vieram por padrão. Deixe pelo menos dois ativos.
    3. **Clientes → abra um cliente que NÃO esteja como Ganho.** Confirme que Nome fantasia, CNPJ e Frequência de pedidos **não aparecem** na ficha. O resto da ficha tem que estar igual ao que você já conhece.
    4. **Abra um cliente que ESTEJA como Ganho** (ou marque um como ganho, se não tiver nenhum — a etapa precisa ser "1ª venda concluída"). Confirme que os três campos aparecem, logo abaixo de "Número de lojas". Preencha Nome fantasia e CNPJ, escolha uma frequência de pedidos, clique em **Salvar alterações**, feche a gaveta e abra de novo: os três valores têm que estar lá.
    5. **Confirme que o valor desativado no passo 2 NÃO aparece** entre as opções de frequência de pedidos.
    6. **Confirme a frase de apoio** embaixo do campo de frequência de pedidos — ela tem que estar visível o tempo todo, sem você precisar clicar em nada.
    7. **Ponto que merece o seu olhar #1 — não confundir as duas frequências.** Na mesma ficha existem agora duas coisas com nome parecido: "Frequência de **pedidos**" (em cima, na parte de dados do cliente) e "Frequência de **visita**" (embaixo, na parte do Funil). A de visita é a que faz sua Agenda funcionar; a de pedidos é só anotação. Os nomes ficaram claros o suficiente lado a lado, ou você prefere outro rótulo para algum dos dois? Se quiser mudar, é barato agora.
    8. **Ponto que merece o seu olhar #2 — a Agenda não pode ter mudado.** Depois de definir uma frequência de pedidos, abra a **Agenda**. Confirme que **nada** apareceu lá por causa disso: nenhuma visita nova, nenhuma tarefa nova, nenhum aviso. Se aparecer qualquer coisa, pare e me avise — é exatamente o comportamento que este campo não pode ter.
    9. **Diário.** Na ficha de um cliente que já teve tarefa ou visita concluída, role até o fim: confirme que existem duas seções, "Histórico" e "Diário", nessa ordem. No Diário, cada entrada tem um selo ("Prospecção" ou "Visita"), a data e hora, o texto e o nome de quem fez. A mais recente em cima.
    10. **Diário vivo.** Ainda com a ficha aberta, marque uma tarefa como concluída pela caixinha. A entrada nova tem que aparecer no Diário **sem** você precisar fechar e reabrir a gaveta (vai aparecer com o texto genérico, conforme explicado acima).
    11. **Cliente sem nada concluído:** abra um cliente novo e confirme que o Diário mostra a frase "Nenhuma visita ou tarefa concluída ainda." em vez de ficar vazio ou quebrado.
    12. **Cliente ganho antigo:** se você tiver algum cliente que já era Ganho antes desta fase, abra a ficha dele. Os três campos têm que aparecer **em branco, sem nenhum aviso de erro**, e você tem que conseguir salvar a ficha normalmente mesmo deixando os três vazios. Isso é dado histórico, não é problema.
    13. **Entre como Vendedor** (uma das contas de vendedor) e confirme duas coisas: (a) você continua vendo só os próprios clientes; (b) o Diário dos seus clientes aparece normalmente, e o campo de frequência de pedidos mostra as opções (não pode aparecer vazio). Confirme também que o Vendedor **não** vê a tela de Configurações.
    14. **Cadastro rápido:** clique em "Novo cliente" e confirme que o formulário continua **sem** pedir Nome fantasia, CNPJ ou frequência de pedidos. Ele tem que estar exatamente como você já conhece.
  </how-to-verify>
  <resume-signal>Digite "approved" se tudo acima conferiu (e responda o ponto #1 sobre os nomes dos dois campos de frequência), ou descreva o que estiver diferente do esperado — em linguagem de sintoma ("o campo X não salvou", "apareceu uma visita na agenda"), que eu traduzo para a causa.</resume-signal>
</task>

</tasks>

<artifacts_produced>
## Artefatos deste plano (parte 4 de 4 — tela)

| Símbolo / Arquivo | Tipo | Status |
|-------------------|------|--------|
| `components/clientes/DiarioTimeline.tsx` | Componente de leitura novo | Criado |
| `components/clientes/ClienteDetailSheet.tsx` | Três campos condicionais + seção Diário + duas buscas + recarga do diário | Modificado |
| `tests/clientes/diario-timeline.test.tsx` | Teste de renderização novo | Criado, GREEN |

Débito e itens a registrar no SUMMARY:
- A supressão de lint documentada na ficha do cliente continua suprimida, não corrigida — a fase cumpriu o "não piorar" (uma linha a mais no bloco existente, nenhum efeito novo), mas o item segue aberto na lista de itens adiados do STATE.
- Registrar a resposta do dono ao ponto #1 do checkpoint (rótulos das duas frequências) e qualquer ajuste de copy decorrente.
- Registrar que a exportação do diário (IMP-02) e a definição de frequência de visita em massa (IMP-01) continuam sendo escopo da Fase 17.

Nenhum componente shadcn novo. Nenhuma dependência npm nova. Nenhuma tela, rota ou diálogo novo. Nenhuma migration.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texto livre de vendedores (resumos da trilha de auditoria) → renderização na ficha | Conteúdo escrito por um usuário e exibido a outro (o Supervisor lê o diário de qualquer vendedor). |
| catálogo escrito pelo Supervisor → opções renderizadas na ficha | Texto escrito por um usuário e exibido como opção de escolha a todo o time. |
| entrada dos três campos → gravação | Texto do usuário indo para a camada de gravação do plano 16-03. |
| interface → decisão de o que mostrar por status | A condição de status na tela é conveniência de experiência, nunca fronteira de autorização. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-26 | Tampering (script injetado) | resumo de vendedor ou nome de valor do catálogo renderizados na ficha | medium | mitigate | Todo o conteúdo é renderizado como texto por JSX, que escapa por padrão; nenhum ponto deste plano injeta HTML cru. Verificação mecânica adicional não é necessária porque nenhuma API de injeção de HTML é usada em nenhum arquivo do projeto — mas revisar isso é item obrigatório na revisão de código desta fase. |
| T-16-27 | Tampering (trilha de auditoria) | controle de escrita aparecendo no componente do diário | high | mitigate | O componente é somente leitura por construção; a verificação mecânica do Task 1 recusa manipulador de evento, botão, estado, efeito e qualquer acesso a dados dentro dele. A trilha continua sem permissão de escrita para a aplicação (migration 0002, inalterada). |
| T-16-28 | Information Disclosure | diário de cliente alheio aparecendo na tela por engano de renderização | high | mitigate | A tela nunca decide o que é visível: ela renderiza exatamente o que a consulta devolveu, e a consulta é escopada pela regra de leitura da trilha (provado no plano 16-03 nos três sentidos). Um Vendedor abrindo a ficha de cliente alheio já não consegue nem carregar o cliente. |
| T-16-29 | Tampering | condição de status na tela sendo confundida com fronteira de autorização | medium | mitigate | A condição de status decide apenas o que é renderizado; a gravação re-valida tudo no servidor (plano 16-03) e a regra de alteração de clientes é a fronteira real. Registrado em comentário no código para que uma revisão futura não trate a condição visual como garantia. |
| T-16-30 | Tampering (regra de negócio) | frequência de pedidos gerando efeito colateral na Agenda | high | mitigate | O campo é campo comum de formulário, salvo pelo caminho de gravação existente; a verificação mecânica recusa qualquer ação de escrita dedicada a ele, e o passo 8 do roteiro de verificação humana confere na Agenda que nada apareceu. É a expressão literal do critério de aceitação do ATV-02. |
| T-16-31 | Denial of Service (autoinfligido) | campo de escolha vazio deixando o cliente ganho sem como preencher a frequência | medium | mitigate | Estado de vocabulário vazio tratado explicitamente com texto simples, seguindo o precedente do bloco de produtos consumidos do mesmo arquivo; o vocabulário já nasce semeado no plano 16-01, então o estado vazio só ocorre se tudo for desativado. |
| T-16-32 | Denial of Service | falha de uma das buscas derrubando a ficha inteira | medium | mitigate | As duas buscas novas seguem o padrão de buscas independentes já usado pelas quatro existentes: erro é abandonado silenciosamente e nunca bloqueia as demais seções. |
| T-16-33 | Repudiation | entrada de diário sem autor sendo lida como "ninguém fez" | low | accept | O texto de substituição do contrato deixa explícito que o autor não foi identificado, em vez de omitir a linha. Casos assim só surgem se o perfil do autor deixar de existir; o registro em si permanece íntegro na trilha. |
| T-16-34 | Tampering (débito de qualidade) | piora da supressão de lint documentada no arquivo mais delicado do codebase | medium | mitigate | Verificação mecânica de contagem exata de supressões e de efeito único; instrução explícita de uma linha a mais no bloco existente. É um pedido literal do ROADMAP desta fase. |
| T-16-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` neste plano nem nesta fase inteira — zero pacotes novos, nada a auditar. Sem checkpoint de legitimidade aplicável. |
</threat_model>

<verification>
Checks rodados pelo executor ao final do plano:
- `git diff --name-only HEAD` vazio para cadastro rápido, componente de histórico, quadro kanban, cartão de cliente e pasta de migrations.
- `npx tsc --noEmit` limpo e `npm run lint` limpo.
- Check estrutural do componente do diário: copy e formato de data exatos, zero controle de escrita, zero estado/efeito, zero acesso a dados, zero reordenação, sem construtor nativo de data.
- Check estrutural da ficha: copy exata dos rótulos/textos, campos condicionais ao status ganho posicionados entre número de lojas e a seção de funil, diário depois do histórico, bloco de histórico intacto, exatamente duas supressões de lint, exatamente um efeito, recarga do diário nos mesmos pontos da de histórico, diário não derivado por filtro do histórico, lista de itens passada ao campo de escolha, nenhuma ação de escrita imediata para a frequência de pedidos.
- `npx vitest run tests/clientes/diario-timeline.test.tsx` verde.
- Checkpoint humano (Task 3): roteiro de 14 passos no navegador aprovado pelo dono, incluindo a confirmação de que a Agenda não mudou.
</verification>

<success_criteria>
- Critério 1: ficha de cliente ganho mostra e salva Nome fantasia e CNPJ; ficha de cliente não-ganho não os mostra; cadastro rápido intocado.
- Critério 2: ficha de cliente ganho mostra a frequência de pedidos como escolha da lista do Supervisor, com o texto de apoio sempre visível, e nada aparece na Agenda por causa dela.
- Critério 3: a ficha mostra o diário das visitas e tarefas concluídas, com resumo, data e autor, da mais recente para a mais antiga, e ele se atualiza sem fechar a gaveta.
- Critério 4: verificado na prática pelo Vendedor no roteiro humano, sobre a fronteira já provada por teste no plano 16-03.
- Cliente ganho antigo com campos em branco renderiza e salva normalmente, sem aviso de erro.
- O arquivo mais delicado do codebase saiu da fase sem débito de lint novo.
- Zero componente shadcn novo, zero dependência npm nova, zero tela/rota/diálogo novo.
</success_criteria>

<output>
Create `.planning/phases/16-ficha-do-cliente-ativo-campos-e-di-rio/16-04-SUMMARY.md` when done.
</output>
