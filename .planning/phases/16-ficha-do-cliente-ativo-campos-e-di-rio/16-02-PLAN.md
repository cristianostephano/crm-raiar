---
phase: 16-ficha-do-cliente-ativo-campos-e-di-rio
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - app/actions/listas.ts
  - components/configuracoes/ConfiguracoesTabs.tsx
  - lib/supabase/queries/clientes.ts
  - app/actions/clientes.ts
  - tests/configuracoes/frequencias-pedido-catalogo.test.ts
autonomous: true
requirements: [ATV-02]

must_haves:
  truths:
    - "ATV-02: o Supervisor gerencia a lista 'Frequência de pedidos' pela MESMA tela de Configurações onde já gerencia as outras quatro listas — adicionar, renomear, desativar e reativar, sem nenhum componente novo de interface e sem nenhuma tela nova"
    - "ATV-02: a tela de Configurações passa a ter exatamente cinco abas, na ordem de domínio já estabelecida (atributos do cliente primeiro, listas do funil depois)"
    - "ATV-02: existe um leitor do catálogo que devolve SÓ os valores ativos, em ordem alfabética — é ele que vai alimentar o campo de escolha na ficha do cliente (plano 16-04)"
    - "ATV-02: o leitor é acessível a qualquer usuário autenticado, Vendedor inclusive — provado contra o banco real com sessão de Vendedor"
    - "ATV-02: um valor desativado pelo Supervisor some da lista de opções ativas na leitura seguinte, sem que a linha seja apagada"
    - "Zero componente de interface novo: a aba nova é configuração declarativa sobre a infraestrutura genérica de lista editável que já existe desde a Fase 3"
    - "Zero dependência npm nova"
  artifacts:
    - "`ListaTabela` (app/actions/listas.ts) passa a incluir a lista nova, sem nenhuma outra mudança de comportamento nas três ações de escrita já existentes"
    - "quinta entrada na configuração de abas de components/configuracoes/ConfiguracoesTabs.tsx"
    - "`getFrequenciasPedidoAtivas()` em lib/supabase/queries/clientes.ts — leitor de catálogo ativo, irmão literal de `getCategoriasAtivas`/`getTiposTarefaAtivos`"
    - "`getFrequenciasPedido()` em app/actions/clientes.ts — envelope fino de Server Action, no molde de `getMotivosPerda`/`getTiposTarefa`"
    - "tests/configuracoes/frequencias-pedido-catalogo.test.ts (novo)"
  key_links:
    - "as três ações de escrita de lista editável (criar, renomear, trocar estado de ativo) já são genéricas sobre o nome da tabela: incluir a lista nova no tipo que as parametriza é o que faz a aba nova ganhar CRUD completo sem uma linha de lógica nova — se alguém escrever lógica específica para esta lista, a simetria com as outras quatro quebra e vira quatro caminhos para manter"
    - "o filtro de ativo no leitor é o que faz 'desativar' significar alguma coisa na ficha do cliente: sem ele, um valor desativado continuaria aparecendo no campo de escolha e a desativação viraria decoração"
    - "a ordenação alfabética no leitor é a única autoridade de ordem das opções — a tela não reordena nada"
    - "este leitor devolve SÓ os ativos, de propósito; a validação do plano 16-03 usa um leitor diferente (catálogo completo), porque um cliente que já guardou um valor desde então desativado ainda precisa conseguir salvar a ficha"
---

<objective>
Ligar a lista criada no plano 16-01 nos dois lugares onde ela precisa existir: a tela de Configurações do Supervisor (quinta aba, gerenciada pela mesma infraestrutura genérica que já atende as outras quatro listas) e um leitor de catálogo ativo que os planos seguintes usam para montar o campo de escolha na ficha do cliente.

Purpose: sem este plano, a lista existe no banco mas ninguém consegue mexer nela pela tela, e o campo da ficha não teria de onde tirar as opções. É também o plano que prova, na prática, que a decisão de desenho estava certa: uma lista editável nova neste projeto custa uma entrada de configuração e um leitor, não uma tela nova — porque a Fase 3 deixou a infraestrutura genérica pronta.

Output: quinta aba funcional em Configurações e um leitor de catálogo testado contra o banco real.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-ficha-do-cliente-ativo-campos-e-di-rio/16-UI-SPEC.md
@app/actions/listas.ts
@components/configuracoes/ConfiguracoesTabs.tsx
</context>

<source_audit>
Auditoria completa de cobertura das quatro fontes (GOAL / REQ / RESEARCH / CONTEXT) no `<source_audit>` do plano `16-01-PLAN.md` — conjunto de planos 16-01 a 16-04, 0 itens MISSING. Este plano cobre a parte de ATV-02 relativa à administração do vocabulário e à leitura das opções.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Quinta aba de lista editável em Configurações</name>
  <files>app/actions/listas.ts, components/configuracoes/ConfiguracoesTabs.tsx</files>
  <read_first>
    - app/actions/listas.ts (já no contexto) — o tipo `ListaTabela` (união de quatro nomes de tabela), o comentário que explica que as quatro compartilham a mesma forma, e as quatro funções (`getListaValores`, `createListaValor`, `updateListaValor`, `setListaValorAtivo`). Confirmar que TODAS as quatro já recebem a tabela por parâmetro e não têm nenhum ramo específico por tabela — é isso que torna esta task quase inteiramente declarativa.
    - components/configuracoes/ConfiguracoesTabs.tsx (já no contexto) — a estrutura `TABS` (quatro objetos com `tabela`, `label`, `inputPlaceholder`, `pluralAtivoLabel`), o comentário que documenta a ordem de domínio das abas, e o comentário longo que explica por que cada painel precisa continuar sendo mantido montado ao trocar de aba.
    - components/configuracoes/EditableListTab.tsx — confirmar que o componente é totalmente parametrizado pelas quatro propriedades acima e não tem nenhum caso especial por tabela. NÃO alterar este arquivo.
    - app/(app)/configuracoes/page.tsx — confirmar que o guard de papel da página é de nível de página e não precisa mudar. NÃO alterar este arquivo.
    - .planning/phases/16-ficha-do-cliente-ativo-campos-e-di-rio/16-UI-SPEC.md (já no contexto) — seção D2, parágrafo sobre a tela de administração.
  </read_first>
  <action>
    Duas edições pequenas, ambas declarativas. Nenhum arquivo novo, nenhum componente novo, nenhuma lógica nova.

    (A) `app/actions/listas.ts`: acrescentar o nome da tabela nova à união `ListaTabela`, como quinto membro. Atualizar o comentário de documentação do tipo, que hoje fala em "as 4 tabelas de lista editável", para refletir que agora são cinco e que a quinta chegou na Fase 16 para o campo de frequência de pedidos da ficha do cliente ativo. Nenhuma das quatro funções de leitura/escrita muda: elas já recebem a tabela por parâmetro e não têm ramo específico por tabela — se alguma precisar de um caso especial para a lista nova, é sinal de que algo saiu do padrão e deve ser reportado em vez de contornado.

    Não mexer nos caminhos revalidados pelas ações de escrita (`/configuracoes` e `/clientes`): a ficha do cliente vive dentro de `/clientes`, então o caminho que precisa ser revalidado quando o vocabulário muda já está coberto.

    (B) `components/configuracoes/ConfiguracoesTabs.tsx`: acrescentar uma quinta entrada à configuração de abas. Posição: **por último**, depois de "Motivos de perda". Justificar em comentário curto: a ordem existente é "atributos do cliente primeiro, listas do funil depois", e a frequência de pedidos é um atributo do cliente — mas ela só existe para o cliente já ganho, então entra como a mais específica das cinco, no fim, para não empurrar as quatro abas que o Supervisor usa no dia a dia.

    Copy exata da entrada nova:
      - rótulo da aba: `Frequência de pedidos`
      - texto de dica do campo de adicionar: `Nome da frequência`
      - rótulo de contagem no plural/feminino: `frequências ativas`

    O rótulo de contagem precisa concordar em gênero e número em português — é exatamente por isso que ele é passado pronto pelo chamador em vez de ser derivado, como o comentário existente do arquivo já registra.

    Nenhuma outra mudança no arquivo. Em particular, a propriedade que mantém cada painel montado ao trocar de aba continua em TODOS os painéis, incluindo o novo — removê-la de qualquer um reintroduz o defeito de recarregamento a cada clique já documentado no próprio arquivo.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && node -e "const fs=require('fs');const l=fs.readFileSync('app/actions/listas.ts','utf8');if(!l.includes('frequencias_pedido'))throw new Error('ListaTabela precisa incluir a lista nova');const m=l.match(/export\s+type\s+ListaTabela\s*=([\s\S]*?);/);if(!m)throw new Error('tipo ListaTabela nao encontrado');const membros=(m[1].match(/\|/g)||[]).length;if(membros!==5)throw new Error('esperado 5 membros na uniao ListaTabela, achei '+membros);const t=fs.readFileSync('components/configuracoes/ConfiguracoesTabs.tsx','utf8');const abas=(t.match(/tabela:\s*[\"']/g)||[]).length;if(abas!==5)throw new Error('esperado 5 abas configuradas, achei '+abas);if(!t.includes('frequencias_pedido'))throw new Error('a aba nova precisa apontar para a tabela nova');for(const c of ['Frequencia de pedidos','Frequência de pedidos'])if(t.includes(c)&&c.indexOf('ê')===-1)throw new Error('o rotulo da aba precisa estar acentuado');if(!t.includes('Frequência de pedidos'))throw new Error('falta o rotulo exato da aba');if(!t.includes('Nome da frequência'))throw new Error('falta o texto de dica exato');if(!t.includes('frequências ativas'))throw new Error('falta o rotulo de contagem exato');const km=(t.match(/keepMounted/g)||[]).length;if(km<1)throw new Error('os paineis precisam continuar montados ao trocar de aba');const e=fs.readFileSync('components/configuracoes/EditableListTab.tsx','utf8');if(/frequencias_pedido/.test(e))throw new Error('EditableListTab precisa continuar generico, sem caso especial por tabela');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `ListaTabela` tem exatamente cinco membros e inclui a tabela nova.
    - Nenhuma das quatro funções de `app/actions/listas.ts` ganhou ramo específico por tabela.
    - `ConfiguracoesTabs.tsx` tem exatamente cinco abas configuradas, a quinta em último lugar, com a copy exata: rótulo `Frequência de pedidos`, dica `Nome da frequência`, contagem `frequências ativas`.
    - A propriedade que mantém os painéis montados continua presente.
    - `EditableListTab.tsx` e `app/(app)/configuracoes/page.tsx` não foram modificados.
    - `npx tsc --noEmit` limpo e `npm run lint` limpo.
  </acceptance_criteria>
  <done>Quinta aba de lista editável configurada, com CRUD completo herdado da infraestrutura genérica da Fase 3, sem componente novo.</done>
</task>

<task type="auto">
  <name>Task 2: Leitor do catálogo ativo + Server Action, com teste de integração</name>
  <files>lib/supabase/queries/clientes.ts, app/actions/clientes.ts, tests/configuracoes/frequencias-pedido-catalogo.test.ts</files>
  <read_first>
    - lib/supabase/queries/clientes.ts — os três leitores de catálogo já existentes (`getTiposTarefaAtivos`, `getCategoriasAtivas`, `getProdutosAtivos`), incluindo o tipo `LookupOption` compartilhado, o filtro de ativo, a ordenação por nome, o retorno de lista vazia em caso de erro (nunca exceção) e o comentário que explica por que estes são leituras de catálogo completo e não derivadas do conjunto de cards já carregado. O leitor novo é irmão literal desses três.
    - app/actions/clientes.ts — a forma dos envelopes finos de Server Action já existentes neste arquivo (checagem de usuário autenticado e retorno `{ data } | { error: { code } }`) e como `getClienteDetalhe`/`atualizarFrequenciaVisita` estão organizados. Ver também `getMotivosPerda` em app/actions/funil.ts e `getTiposTarefa` em app/actions/tarefas.ts como os dois moldes mais próximos.
    - tests/configuracoes/rls-listas.test.ts — molde de teste de integração para lista editável: `SEED_ACCOUNTS`, `signInAs`, `serviceClient` só para semear/limpar, limpeza por id.
    - tests/configuracoes/rls-frequencias-pedido.test.ts (criado no plano 16-01) — não duplicar o que ele já prova (autorização de escrita); este arquivo prova o comportamento do LEITOR (filtro de ativo, ordem, acesso do Vendedor).
    - .planning/STATE.md — Blockers/Concerns: limite de tentativas de login; logar cada identidade uma vez por arquivo.
  </read_first>
  <action>
    (A) `lib/supabase/queries/clientes.ts` — acrescentar `getFrequenciasPedidoAtivas(): Promise<LookupOption[]>`, colocada imediatamente depois de `getProdutosAtivos` (junto dos outros leitores de catálogo, nunca no meio dos leitores de cliente). Corpo idêntico em forma aos três irmãos: cliente do servidor, seleção estreita das duas colunas de identificador e nome, filtro de ativo verdadeiro, ordenação crescente por nome, e retorno de lista vazia se der erro ou não vier dado — nunca lançar exceção, porque um catálogo indisponível não pode derrubar a ficha do cliente inteira.

    Comentário de documentação obrigatório, registrando três coisas: (1) que este é o catálogo que alimenta o campo de escolha "Frequência de pedidos" na ficha do cliente ativo (ATV-02); (2) que ele devolve SÓ os valores ativos, de propósito, porque um valor desativado pelo Supervisor não deve mais poder ser escolhido; e (3) — o ponto mais importante para quem vier depois — que a **validação** do valor enviado (plano 16-03) NÃO pode usar este leitor: um cliente que já tenha guardado um valor desde então desativado precisa continuar conseguindo salvar a ficha, então a validação usa uma leitura do catálogo completo. Confundir os dois é o defeito mais provável desta área.

    (B) `app/actions/clientes.ts` — acrescentar o envelope fino `getFrequenciasPedido()`, no molde exato dos envelopes já existentes: cliente do servidor, checagem de usuário autenticado devolvendo o código de não autenticado, e senão o resultado do leitor. Tipo de resultado nomeado e exportado, seguindo o padrão dos vizinhos. Nenhuma checagem de papel aqui: leitura de catálogo é aberta a qualquer autenticado (é o que faz o campo funcionar para o Vendedor), e a fronteira real continua sendo a regra de leitura da tabela.

    Nenhuma alteração em nenhuma outra função destes dois arquivos.

    (C) Criar `tests/configuracoes/frequencias-pedido-catalogo.test.ts`. Como as funções acima dependem do contexto de requisição do Next (leitura de cookies), elas não podem ser invocadas diretamente pelo Vitest — precedente já registrado no projeto desde a Fase 2. O teste então exercita **a mesma consulta** que o leitor faz, com sessões reais, provando o comportamento que importa. Semear via chave de serviço e limpar por id no fim.

    Casos exigidos, com estas palavras-chave nos nomes (sem acento):
      - **`ativos`**: semear um valor ativo e um inativo; a consulta com filtro de ativo devolve o ativo e NÃO devolve o inativo.
      - **`ordem`**: semear valores fora de ordem alfabética; o resultado vem em ordem alfabética crescente pelo nome.
      - **`vendedor`**: um Vendedor autenticado consegue executar essa mesma leitura e recebe os valores (é o que garante que o campo não fica vazio para ele).
      - **`desativar`**: depois de o Supervisor trocar o estado de ativo de um valor para falso, a leitura seguinte com filtro de ativo já não traz aquele valor — e a linha continua existindo na tabela (reler pela chave de serviço).
      - **`colunas`**: a leitura devolve exatamente os campos de identificador e nome, sem trazer a linha inteira (seleção estreita, convenção do projeto para manter o tráfego baixo no plano gratuito).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx vitest run tests/configuracoes/frequencias-pedido-catalogo.test.ts && node -e "const fs=require('fs');const q=fs.readFileSync('lib/supabase/queries/clientes.ts','utf8');if(!q.includes('getFrequenciasPedidoAtivas'))throw new Error('falta o leitor do catalogo');const bloco=q.slice(q.indexOf('getFrequenciasPedidoAtivas'));if(!/from\(\s*[\"']frequencias_pedido[\"']\s*\)/.test(bloco))throw new Error('o leitor precisa ler da tabela nova');if(!/eq\(\s*[\"']ativo[\"']\s*,\s*true\s*\)/.test(bloco))throw new Error('o leitor precisa filtrar so os ativos');if(!/order\(\s*[\"']nome[\"']/.test(bloco))throw new Error('o leitor precisa ordenar por nome');if(/select\(\s*[\"']\*/.test(bloco))throw new Error('selecao estreita de colunas, nunca aberta');const a=fs.readFileSync('app/actions/clientes.ts','utf8');if(!a.includes('getFrequenciasPedido'))throw new Error('falta a Server Action');const ab=a.slice(a.indexOf('export async function getFrequenciasPedido'));if(!/auth\.getUser\(\)/.test(ab.slice(0,900)))throw new Error('a Server Action precisa checar usuario autenticado');if(/is_supervisor|role\s*===\s*[\"']supervisor[\"']/.test(ab.slice(0,900)))throw new Error('leitura de catalogo e aberta a qualquer autenticado, sem checagem de papel');const t=fs.readFileSync('tests/configuracoes/frequencias-pedido-catalogo.test.ts','utf8');const n=(t.match(/\bit\(\s*[\"'][^\"']+/g)||[]).map(s=>s.toLowerCase());for(const k of ['ativos','ordem','vendedor','desativar','colunas'])if(!n.some(x=>x.includes(k)))throw new Error('frequencias-pedido-catalogo.test.ts: falta caso com a palavra '+k);if(!t.includes('SEED_ACCOUNTS')||!t.includes('signInAs'))throw new Error('o teste precisa usar sessoes reais');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `getFrequenciasPedidoAtivas()` existe em `lib/supabase/queries/clientes.ts`, lê da tabela nova com seleção estreita, filtra só os ativos, ordena por nome e devolve lista vazia em caso de erro.
    - O comentário de documentação do leitor registra explicitamente que a validação do plano 16-03 NÃO deve usá-lo.
    - `getFrequenciasPedido()` existe em `app/actions/clientes.ts`, checa usuário autenticado e não faz nenhuma checagem de papel.
    - `tests/configuracoes/frequencias-pedido-catalogo.test.ts` passa inteiro, com casos cujos nomes contêm `ativos`, `ordem`, `vendedor`, `desativar` e `colunas`, usando sessões reais.
    - `npx tsc --noEmit` limpo e `npm run lint` limpo.
  </acceptance_criteria>
  <done>Catálogo de frequências de pedido legível por qualquer autenticado, filtrado e ordenado, provado contra o banco real.</done>
</task>

</tasks>

<artifacts_produced>
## Artefatos deste plano (parte 2 de 4 — administração e leitura do vocabulário)

| Símbolo / Arquivo | Tipo | Status |
|-------------------|------|--------|
| `ListaTabela` (app/actions/listas.ts) | União estendida para cinco listas | Modificado |
| `TABS` (components/configuracoes/ConfiguracoesTabs.tsx) | Quinta aba configurada | Modificado |
| `getFrequenciasPedidoAtivas()` | Leitor de catálogo ativo | Criado |
| `getFrequenciasPedido()` | Server Action (envelope fino) | Criada |
| `tests/configuracoes/frequencias-pedido-catalogo.test.ts` | Teste novo | Criado, GREEN |

Contrato para os planos seguintes:
- `getFrequenciasPedido()` é o ÚNICO caminho que o plano 16-04 usa para montar as opções do campo de escolha na ficha. A tela não reordena nem filtra nada por conta própria.
- `getFrequenciasPedidoAtivas()` devolve SÓ ativos e **não** serve para validar entrada — o plano 16-03 usa uma leitura de catálogo completo para isso.

Nenhuma dependência npm nova. Nenhum componente de interface novo. Nenhuma migration.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `getFrequenciasPedido()` | Server Action nova de LEITURA, chamável por qualquer autenticado a partir do cliente. |
| Supervisor (tela de Configurações) → as três ações de escrita genéricas, agora parametrizáveis com a tabela nova | Superfície de escrita já existente que passa a aceitar um quinto valor de tabela. |
| valor do catálogo → interface | Texto escrito pelo Supervisor renderizado como opção para todo o time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-10 | Elevation of Privilege | Vendedor chamando as ações de escrita de lista editável com a tabela nova por parâmetro | high | mitigate | As três ações já rejeitam chamador não Supervisor antes de tentar a escrita (defesa em profundidade da Fase 3) e a fronteira real são as regras de escrita criadas no plano 16-01, provadas cross-papel por `rls-frequencias-pedido.test.ts`. Este plano não afrouxa nada: só acrescenta um valor à união de tabelas, sem tocar nas funções. |
| T-16-11 | Tampering | valor de tabela arbitrário injetado no parâmetro das ações genéricas | medium | mitigate | O parâmetro é uma união fechada de cinco nomes literais em TypeScript; do lado do banco, qualquer nome fora dessas cinco tabelas cai nas regras de acesso da tabela alvo (ou inexistência), nunca em acesso irrestrito. Nenhuma tabela sem regras de acesso existe no projeto. |
| T-16-12 | Information Disclosure | catálogo do time exposto a não autenticado pela Server Action | medium | mitigate | O envelope checa usuário autenticado antes de ler; a regra de leitura da tabela é concedida ao papel de autenticado, nunca ao público (provado pelo caso `anonimo` do plano 16-01). |
| T-16-13 | Denial of Service | erro de leitura do catálogo derrubando a ficha do cliente inteira | medium | mitigate | O leitor devolve lista vazia em caso de erro, nunca lança — mesmo comportamento dos três leitores de catálogo irmãos. A ficha degrada para "sem opções", não para tela quebrada; o plano 16-04 trata esse estado vazio explicitamente. |
| T-16-14 | Tampering | valor desativado continuando a ser oferecido como opção | medium | mitigate | Filtro de ativo dentro do leitor, verificado mecanicamente e provado pelo caso `desativar` do teste. |
| T-16-15 | Information Disclosure | leitura larga trazendo colunas desnecessárias e inflando o tráfego contra o limite do plano gratuito | low | mitigate | Seleção estreita das duas colunas, verificada mecanicamente e provada pelo caso `colunas`. |
| T-16-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` neste plano nem nesta fase inteira — zero pacotes novos, nada a auditar. Sem checkpoint de legitimidade aplicável. |
</threat_model>

<verification>
Checks rodados pelo executor ao final do plano:
- `npx tsc --noEmit` limpo.
- `npm run lint` limpo.
- Check estrutural: cinco membros na união de tabelas, cinco abas configuradas, copy exata da aba nova, painéis continuam montados, componente genérico de lista editável sem caso especial por tabela.
- Check estrutural do leitor: lê da tabela nova, filtra ativos, ordena por nome, seleção estreita; Server Action checa autenticação e não faz checagem de papel.
- `npx vitest run tests/configuracoes/frequencias-pedido-catalogo.test.ts` verde.
</verification>

<success_criteria>
- O Supervisor consegue adicionar, renomear, desativar e reativar valores de "Frequência de pedidos" pela tela de Configurações, na quinta aba, com a mesma experiência das outras quatro listas.
- O leitor de catálogo devolve só os valores ativos, em ordem alfabética, com seleção estreita, e funciona com sessão de Vendedor.
- Desativar um valor o tira das opções na leitura seguinte sem apagar a linha.
- Nenhum componente de interface novo, nenhuma tela nova, nenhuma dependência npm nova.
</success_criteria>

<output>
Create `.planning/phases/16-ficha-do-cliente-ativo-campos-e-di-rio/16-02-SUMMARY.md` when done.
</output>
