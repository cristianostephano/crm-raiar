---
phase: 19-planilhas-de-cnpj-e-nome-fantasia
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - lib/importacao/types.ts
  - lib/importacao/modelo.ts
  - lib/importacao/mapping.ts
  - lib/importacao/annotarLinha.ts
  - lib/importacao/confirmar.ts
  - tests/importacao/modelo.test.ts
  - tests/importacao/mapping.test.ts
  - tests/importacao/column-mapping-table.test.tsx
  - tests/importacao/annotarLinha.test.ts
  - tests/importacao/confirmar.test.ts
autonomous: true
requirements: [IMP-01, IMP-02]

must_haves:
  truths:
    - "IMP-01: `cnpj` existe como campo do vocabulário de campos do sistema, com o rótulo 'CNPJ' e marcado como NÃO obrigatório"
    - "IMP-02: `nomeFantasia` existe como campo do vocabulário de campos do sistema, com o rótulo 'Nome Fantasia' e marcado como NÃO obrigatório"
    - "IMP-01/IMP-02: os dois campos aparecem como opção no Select de mapeamento de coluna do passo 2 do assistente de importar clientes, sem nenhuma edição no componente de mapeamento (ele já deriva as opções da lista de campos recebida)"
    - "IMP-01/IMP-02: os dois campos aparecem como colunas no modelo de planilha baixável, cada um com um valor de exemplo plausível"
    - "IMP-01/IMP-02: um cabeçalho de planilha escrito de forma variada ('CNPJ', 'cnpj', 'C.N.P.J.', 'Nome Fantasia', 'nome_fantasia', 'fantasia') é sugerido automaticamente para o campo certo"
    - "IMP-01/IMP-02: a anotação de linha carrega os dois valores saneados até a linha resolvida, como texto opcional (nulo quando a célula está ausente ou vazia) — e NUNCA adiciona motivo de erro por causa deles, porque são opcionais"
    - "IMP-01/IMP-02: a carga da chamada em lote leva `cnpj` e `nome_fantasia` nas chaves exatas que a função do banco espera (nomes em caixa baixa com sublinhado, como todas as outras chaves da carga)"
    - "IMP-01/IMP-02: uma linha sem CNPJ e sem Nome Fantasia continua entrando normalmente — os dois campos são opcionais em toda a cadeia, do vocabulário até a carga"
    - "Nenhuma validação de formato/máscara/dígito verificador de CNPJ existe em nenhum ponto — só saneamento de célula, igual a qualquer outro campo de texto"
    - "Os 7 campos obrigatórios de sempre continuam sendo exatamente os mesmos 7 — nenhum campo virou obrigatório e nenhum deixou de ser"
    - "O vocabulário do fluxo de frequência (a segunda lista de campos, da Fase 17) NÃO foi tocado — ele continua com exatamente dois campos, e a checagem de pertencimento de apelido continua fazendo os apelidos novos de CNPJ/Nome Fantasia NÃO vazarem para aquela lista"
  artifacts:
    - "lib/importacao/types.ts — dois membros novos na união de chaves e duas entradas novas na lista de campos"
    - "lib/importacao/modelo.ts — dois valores de exemplo novos"
    - "lib/importacao/mapping.ts — apelidos de cabeçalho para as duas chaves novas"
    - "lib/importacao/annotarLinha.ts — dois campos novos na linha resolvida, preenchidos a partir da célula saneada"
    - "lib/importacao/confirmar.ts — duas chaves novas na forma da carga da chamada em lote e no mapeamento que a produz"
    - "tests/importacao/annotarLinha.test.ts + confirmar.test.ts + mapping.test.ts + modelo.test.ts + column-mapping-table.test.tsx — casos novos provando a cadeia inteira"
  key_links:
    - "a lista de campos do sistema é a fonte única que alimenta MODELO, MAPEAMENTO e TELA ao mesmo tempo — adicionar as duas entradas lá faz as três coisas de uma vez; qualquer tentativa de listar os campos novos separadamente em outro arquivo cria duas fontes de verdade que vão divergir"
    - "o mapa de valores de exemplo do modelo é tipado por chave da lista de campos, então o compilador OBRIGA a adicionar os dois exemplos — se `npx tsc --noEmit` passa, o modelo não pode ter esquecido nenhuma coluna"
    - "os dois campos precisam chegar à carga da chamada em lote com as chaves em caixa baixa e sublinhado (`cnpj`, `nome_fantasia`) — a função do banco lê o registro por NOME de chave; escrever `nomeFantasia` na carga faria o valor ser descartado sem erro nenhum"
    - "a tabela de apelidos é fixada na união de chaves da lista de 14 (agora 16) — a função de sugestão confere se a chave apontada pertence à lista de campos recebida antes de devolvê-la, então os apelidos novos NÃO contaminam o assistente de frequências nem o de CNPJ em massa (padrão travado na Fase 17)"
    - "os dois campos são opcionais: nenhuma entrada nova no schema de linha da importação e nenhum motivo de erro novo na anotação. Marcar qualquer um como obrigatório quebraria toda planilha existente que o dono já usa hoje"
---

<objective>
Fazer CNPJ e Nome Fantasia atravessarem toda a cadeia da planilha "Importar clientes" já existente: vocabulário de campos, modelo baixável, sugestão automática de coluna, opção no Select de mapeamento, resolução da linha e carga da chamada em lote — até virarem valor gravado nas colunas que o plano 19-01 acabou de abrir na função do banco.

Purpose: são duas colunas opcionais numa tela que já existe, mas o ponto de falha é justamente a cadeia ser longa. Se qualquer elo faltar, o Supervisor mapeia a coluna, a tela mostra tudo verde, a importação diz "sucesso", e o CNPJ simplesmente não está no cliente — sem nenhuma mensagem de erro. O plano existe para fechar os seis elos de uma vez e provar cada um com teste.

Output: cinco arquivos da camada de importação estendidos com duas entradas cada, e cinco arquivos de teste ampliados provando que o valor sai da célula da planilha e chega à carga da chamada com a chave certa.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@lib/importacao/types.ts
@lib/importacao/mapping.ts
@.planning/phases/19-planilhas-de-cnpj-e-nome-fantasia/19-01-SUMMARY.md
</context>

<interface_context>
Interfaces já existentes que este plano ESTENDE (nunca redefine):

- `lib/importacao/types.ts` exporta `SystemField` (união de chaves), `SystemFieldDefinition<K extends string = SystemField>` com `{ key; label; required; multi? }`, e `SYSTEM_FIELDS: SystemFieldDefinition[]` com 14 entradas, 7 delas obrigatórias.
- `lib/importacao/modelo.ts` exporta `buildModeloImportacao(): Uint8Array` e mantém internamente `EXAMPLE_VALUES: Record<(typeof SYSTEM_FIELDS)[number]["key"], string>` — mapa TOTAL por chave, o que faz o compilador exigir uma entrada por campo novo.
- `lib/importacao/mapping.ts` exporta `NAO_IMPORTAR`, `suggestMapping<K>(header, fields?)`, `applyMapping<K>(headers, rows, mapping)`, `requiredFieldsFaltando<K>(mapping, fields?)`, e mantém internamente `ALIASES: Record<string, SystemField>` (chave = cabeçalho normalizado sem acento/pontuação/caixa).
- `lib/importacao/annotarLinha.ts` exporta `MappedRow = Partial<Record<SystemField, string>>`, `ResolvedRow` (hoje com 14 campos, `complemento`/`contato`/`telefone`/`email`/`numeroDeLojas` já são `string | null`) e `annotarLinha(linha, lookups): AnnotatedRow`.
- `lib/importacao/confirmar.ts` exporta `RpcClienteRow` (chaves em caixa baixa com sublinhado, exatamente as que a função do banco lê), `PuladaGroup`, `planConfirmacao(...)` e `reconcileImportados(...)`; internamente `toRpcClienteRow(resolved: ResolvedRow): RpcClienteRow`.
- `components/importacao/ColumnMappingTable.tsx` já é genérico (`fields` opcional, padrão `SYSTEM_FIELDS`) — **não precisa de nenhuma edição**; ele deriva as opções do Select da lista recebida.

Interface publicada pelo plano 19-01 e consumida aqui: a função `importar_clientes_lote` no banco agora lê `cnpj` e `nome_fantasia` do registro de cada linha da carga.
</interface_context>

<source_audit>
Auditoria de cobertura completa das quatro fontes (GOAL / REQ / RESEARCH / CONTEXT) para a Fase 19 inteira: ver `<source_audit>` em `19-01-PLAN.md`. Este plano cobre G1, G2, IMP-01, IMP-02, L-01, L-02, L-03 e L-07.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vocabulário, modelo e sugestão automática de coluna</name>
  <files>lib/importacao/types.ts, lib/importacao/modelo.ts, lib/importacao/mapping.ts, tests/importacao/modelo.test.ts, tests/importacao/mapping.test.ts, tests/importacao/column-mapping-table.test.tsx</files>
  <read_first>
    - `lib/importacao/types.ts` inteiro (é curto) — o banner do topo lista NOMINALMENTE todos os consumidores do vocabulário; ele é o mapa desta tarefa.
    - `lib/importacao/modelo.ts` — o mapa de valores de exemplo é total por chave; o compilador vai reclamar se faltar entrada.
    - `lib/importacao/mapping.ts`, seção da tabela de apelidos e a função de sugestão — em especial o comentário que explica a checagem de pertencimento introduzida na Fase 17.
    - `tests/importacao/modelo.test.ts` e `tests/importacao/column-mapping-table.test.tsx` — os dois derivam as asserções da própria lista de campos, então passam sozinhos ao crescer a lista; o que precisa de ajuste são os TEXTOS de nome/comentário que dizem "14"/"quatorze".
    - `tests/importacao/frequencia-vocabulario.test.ts` — mostra o formato de teste de vocabulário já usado no projeto e o caso que prova que apelido de fora da lista não vaza.
  </read_first>
  <behavior>
    - A lista de campos do sistema passa a ter 16 entradas; as duas novas são `cnpj` (rótulo "CNPJ") e `nomeFantasia` (rótulo "Nome Fantasia"), ambas com `required` falso.
    - A contagem de campos obrigatórios continua exatamente 7, e o conjunto de chaves obrigatórias continua sendo o mesmo de antes.
    - O modelo baixável passa a ter 16 colunas, na mesma ordem da lista, com uma linha de exemplo de 16 células.
    - Sugestão automática: os cabeçalhos "CNPJ", "cnpj", "C.N.P.J." e "Cnpj" sugerem `cnpj`; "Nome Fantasia", "nome_fantasia", "NOME FANTASIA" e "fantasia" sugerem `nomeFantasia`.
    - Avaliados contra a lista de campos do fluxo de frequência (a de dois campos), os mesmos cabeçalhos "CNPJ" e "Nome Fantasia" continuam sugerindo o item de não importar — os apelidos novos não vazam para o outro vocabulário.
    - Nenhum apelido já existente mudou de destino: "vendedor" continua sugerindo `responsavel`, "uf" continua sugerindo `estado`, "empresa" continua sugerindo `razaoSocial`.
    - O Select de mapeamento passa a oferecer os 16 rótulos mais o item de não importar, sem nenhuma edição no componente de mapeamento.
  </behavior>
  <action>
    Escrever primeiro os casos de teste (RED), depois a implementação.

    Em `lib/importacao/types.ts`: acrescentar `cnpj` e `nomeFantasia` à união de chaves e duas entradas correspondentes à lista de campos, as duas com `required` em falso e sem `multi`. Colocá-las numa posição que faça sentido para quem preenche a planilha — logo depois de `razaoSocial`, já que são as duas outras formas de identificar a empresa. Atualizar o banner do topo do arquivo para refletir a contagem nova e mencionar que os dois campos novos são opcionais e mapeiam colunas que já existem em `clientes` desde a Fase 13.

    Em `lib/importacao/modelo.ts`: acrescentar os dois valores de exemplo ao mapa (um CNPJ plausível, escrito com a pontuação usual, e um nome fantasia plausível coerente com a razão social de exemplo já usada no arquivo). Não sanear nada aqui — o modelo é gerado pelo próprio sistema, o saneamento é do lado de quem lê célula do usuário, e o banner do arquivo já explica isso.

    Em `lib/importacao/mapping.ts`: acrescentar à tabela de apelidos as entradas normalizadas que cobrem as variações de cabeçalho listadas no bloco de comportamento. Lembrar que a normalização de cabeçalho já remove acento, pontuação e caixa, então "C.N.P.J." e "cnpj" normalizam para a mesma chave — não criar entradas redundantes. Não mexer na função de sugestão, na checagem de pertencimento, nem em nada mais do arquivo.

    Nos testes: em `tests/importacao/mapping.test.ts`, acrescentar casos para as sugestões novas e para a não-contaminação do vocabulário de frequência. Em `tests/importacao/modelo.test.ts` e `tests/importacao/column-mapping-table.test.tsx`, as asserções já derivam da lista de campos e continuam corretas — ajustar apenas os textos de nome de caso e comentários que ainda falam em "14"/"quatorze", para o arquivo não mentir sobre si mesmo. Não afrouxar nenhuma asserção existente.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/importacao/modelo.test.ts tests/importacao/mapping.test.ts tests/importacao/column-mapping-table.test.tsx tests/importacao/frequencia-vocabulario.test.ts && node -e "const fs=require('fs');const t=fs.readFileSync('lib/importacao/types.ts','utf8');for(const s of ['\"cnpj\"','\"nomeFantasia\"','label: \"CNPJ\"','label: \"Nome Fantasia\"'])if(!t.includes(s))throw new Error('types.ts: faltou '+s);const defs=[...t.matchAll(/\{\s*key:\s*\"([A-Za-z]+)\",\s*label:\s*\"([^\"]+)\",\s*required:\s*(true|false)/g)].map(m=>({key:m[1],label:m[2],req:m[3]==='true'}));if(defs.length!==16)throw new Error('esperado 16 definicoes de campo, achei '+defs.length);const obrig=defs.filter(d=>d.req).map(d=>d.key).sort();const esperado=['cep','cidade','estado','numero','razaoSocial','responsavel','rua'];if(obrig.join(',')!==esperado.join(','))throw new Error('o conjunto de campos obrigatorios mudou: '+obrig.join(','));for(const k of ['cnpj','nomeFantasia']){const d=defs.find(x=>x.key===k);if(!d)throw new Error('faltou a definicao de '+k);if(d.req)throw new Error(k+' precisa ser opcional');}const tf=fs.readFileSync('lib/importacao/typesFrequencia.ts','utf8');if(tf.includes('cnpj')||tf.includes('nomeFantasia'))throw new Error('o vocabulario de frequencia (Fase 17) nao pode ser tocado por este plano');const cmt=fs.readFileSync('components/importacao/ColumnMappingTable.tsx','utf8');if(cmt.includes('cnpj')||cmt.includes('nomeFantasia'))throw new Error('o componente de mapeamento nao precisa de edicao — ele deriva as opcoes da lista recebida');console.log('OK vocabulario 16 campos, 7 obrigatorios')"</automated>
  </verify>
  <acceptance_criteria>
    - A lista de campos do sistema tem exatamente 16 definições; exatamente 7 continuam obrigatórias e o conjunto de chaves obrigatórias é idêntico ao de antes.
    - `cnpj` e `nomeFantasia` existem na união de chaves e na lista, com os rótulos "CNPJ" e "Nome Fantasia", ambos opcionais.
    - `npx tsc --noEmit` limpo — o que prova, pelo tipo total do mapa de exemplos, que o modelo baixável ganhou as duas colunas.
    - `tests/importacao/modelo.test.ts` verde: cabeçalho do modelo igual aos 16 rótulos na ordem, e a linha de exemplo com 16 células.
    - `tests/importacao/mapping.test.ts` verde, com casos novos provando as sugestões de CNPJ e Nome Fantasia e provando que esses mesmos cabeçalhos, avaliados contra o vocabulário de dois campos da Fase 17, sugerem o item de não importar.
    - Nenhum apelido já existente mudou de destino (os casos antigos de `mapping.test.ts` passam sem edição de asserção).
    - `tests/importacao/column-mapping-table.test.tsx` verde sem edição de asserção — só de texto de nome/comentário.
    - `tests/importacao/frequencia-vocabulario.test.ts` continua verde; `lib/importacao/typesFrequencia.ts` e `components/importacao/ColumnMappingTable.tsx` NÃO foram editados.
  </acceptance_criteria>
  <done>Os dois campos novos existem no vocabulário único, aparecem no modelo baixável e no Select de mapeamento, e são reconhecidos automaticamente a partir do cabeçalho da planilha — sem contaminar o vocabulário do fluxo de frequência.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Levar os dois valores da célula até a carga da chamada em lote</name>
  <files>lib/importacao/annotarLinha.ts, lib/importacao/confirmar.ts, tests/importacao/annotarLinha.test.ts, tests/importacao/confirmar.test.ts</files>
  <read_first>
    - `lib/importacao/annotarLinha.ts` — em especial a forma da linha resolvida e como os campos opcionais já existentes (complemento, contato, telefone, email, número de lojas) são tratados: saneados e convertidos para nulo quando ausentes, sem nenhum motivo de erro associado. Os dois campos novos seguem exatamente esse molde.
    - `lib/importacao/confirmar.ts` — a forma da carga da chamada e a função que mapeia a linha resolvida para ela.
    - `lib/validations/importacao.ts` — o schema de linha da importação. Ele lista só o que é obrigatório mais os opcionais já checados; os dois campos novos NÃO entram nele, porque nenhuma regra de validação se aplica a eles.
    - `.planning/phases/19-planilhas-de-cnpj-e-nome-fantasia/19-01-SUMMARY.md` — confirmar os nomes exatos das duas chaves que a função do banco lê.
  </read_first>
  <behavior>
    - Uma linha mapeada com célula de CNPJ preenchida produz linha resolvida com esse texto (saneado) no campo de CNPJ.
    - Uma linha mapeada com célula de Nome Fantasia preenchida produz linha resolvida com esse texto (saneado) no campo de nome fantasia.
    - Uma linha sem essas duas células produz linha resolvida com nulo nos dois campos e continua com situação "ok" quando o resto está correto — os dois campos NUNCA geram motivo de erro.
    - Uma célula de CNPJ contendo só espaços em branco vira nulo, não string vazia.
    - Uma célula que começa com um caractere de risco de fórmula é saneada pelo mesmo caminho que todas as outras células já usam — nenhum caminho novo de leitura de célula sem saneamento é criado.
    - Um valor qualquer não-vazio de CNPJ é aceito, independentemente de formato — não existe motivo de erro de formato de CNPJ.
    - A carga da chamada em lote produzida a partir de uma linha resolvida traz os dois valores nas chaves em caixa baixa com sublinhado que a função do banco lê, e nulo quando a linha resolvida tem nulo.
    - Toda a contabilidade de linhas puladas e a re-checagem de duplicados continuam idênticas — os dois campos novos não participam de nenhuma delas.
  </behavior>
  <action>
    Escrever primeiro os casos de teste (RED), depois a implementação.

    Em `lib/importacao/annotarLinha.ts`: acrescentar dois campos de texto opcional à forma da linha resolvida, no mesmo estilo dos opcionais já existentes, e preenchê-los no objeto resolvido a partir da célula já saneada, com aparo de espaço e conversão para nulo quando o resultado ficar vazio. NÃO acrescentar nenhum motivo de erro, nenhuma entrada no schema de linha da importação, e nenhuma checagem de formato — os dois campos são puramente carregados. O saneamento já acontece antes, no passo que sanea a linha inteira; não criar um segundo ponto de saneamento.

    Em `lib/importacao/confirmar.ts`: acrescentar as duas chaves à forma da carga da chamada (nomes em caixa baixa com sublinhado, iguais aos que a função do banco lê — confirmar no resumo do plano 19-01) e preenchê-las no mapeamento que converte a linha resolvida para essa forma. Não tocar em nada da classificação de linhas, da contabilidade de puladas nem da reconciliação.

    Nos testes: em `tests/importacao/annotarLinha.test.ts`, casos novos cobrindo todos os itens do bloco de comportamento que dizem respeito à anotação, incluindo o caso de só espaços em branco e o de valor de formato livre aceito. Em `tests/importacao/confirmar.test.ts`, casos novos provando que os dois valores chegam à carga com as chaves certas e que uma linha resolvida com nulo produz nulo na carga. Não afrouxar nenhuma asserção existente.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint lib/importacao app/actions/importacao.ts && npx vitest run tests/importacao/annotarLinha.test.ts tests/importacao/confirmar.test.ts tests/importacao/preview.test.ts tests/importacao/dedupe.test.ts && node -e "const fs=require('fs');const a=fs.readFileSync('lib/importacao/annotarLinha.ts','utf8');const c=fs.readFileSync('lib/importacao/confirmar.ts','utf8');const v=fs.readFileSync('lib/validations/importacao.ts','utf8');if(!/cnpj:\s*string\s*\|\s*null/.test(a))throw new Error('annotarLinha.ts: a linha resolvida precisa de um campo cnpj de texto opcional');if(!/nomeFantasia:\s*string\s*\|\s*null/.test(a))throw new Error('annotarLinha.ts: a linha resolvida precisa de um campo nomeFantasia de texto opcional');if(!/cnpj:\s*string\s*\|\s*null/.test(c))throw new Error('confirmar.ts: a carga precisa da chave cnpj');if(!/nome_fantasia:\s*string\s*\|\s*null/.test(c))throw new Error('confirmar.ts: a chave da carga precisa ser nome_fantasia (caixa baixa com sublinhado), nunca nomeFantasia');if(/nomeFantasia\s*:/.test(c.split('toRpcClienteRow')[1]||''))throw new Error('confirmar.ts: a carga nao pode usar a grafia em caixa mista — a funcao do banco le por nome de chave');if(v.includes('cnpj')||v.includes('nomeFantasia'))throw new Error('o schema de linha da importacao nao deve ganhar entrada para campos que nao tem regra de validacao');if(/regexp|\\\\d\{2\}\\\\.|test\(.*cnpj/i.test(a))throw new Error('nenhuma validacao de formato de CNPJ pode existir na anotacao');const at=fs.readFileSync('tests/importacao/annotarLinha.test.ts','utf8');const ct=fs.readFileSync('tests/importacao/confirmar.test.ts','utf8');for(const [f,s] of [['annotarLinha.test.ts',at],['confirmar.test.ts',ct]])for(const k of ['cnpj','antasia'])if(!s.includes(k))throw new Error(f+': faltam casos cobrindo '+k);console.log('OK cadeia celula -> linha resolvida -> carga')"</automated>
  </verify>
  <acceptance_criteria>
    - A linha resolvida tem dois campos novos de texto opcional para CNPJ e nome fantasia, preenchidos a partir da célula saneada e nulos quando ausentes ou só com espaço.
    - Nenhum motivo de erro novo foi introduzido: uma linha sem os dois campos continua com situação "ok".
    - `lib/validations/importacao.ts` NÃO foi editado — os dois campos não têm regra de validação.
    - Nenhuma expressão de formato/máscara/regex de CNPJ existe na anotação.
    - A forma da carga da chamada em lote ganhou as duas chaves com a grafia exata que a função do banco lê (caixa baixa com sublinhado), e o mapeamento as preenche a partir da linha resolvida.
    - `tests/importacao/annotarLinha.test.ts` e `tests/importacao/confirmar.test.ts` verdes, com casos novos cobrindo todos os itens do bloco de comportamento.
    - `tests/importacao/preview.test.ts` e `tests/importacao/dedupe.test.ts` continuam verdes (nenhuma regressão na contabilidade e na detecção de duplicados).
    - `npx tsc --noEmit` e `npx eslint lib/importacao app/actions/importacao.ts` limpos, sem nenhum uso do tipo curinga proibido pelo CLAUDE.md.
  </acceptance_criteria>
  <done>O valor digitado numa célula de CNPJ ou de Nome Fantasia atravessa a anotação e chega à carga da chamada em lote com a chave que a função do banco lê — a cadeia inteira de IMP-01/IMP-02 fechada e provada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| célula de planilha → linha resolvida | Texto totalmente controlado pelo usuário entra no sistema pela primeira vez |
| linha resolvida → carga da chamada → função do banco | A carga é o único contrato entre a aplicação e a gravação |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-11 | Tampering | injeção de fórmula por célula de CNPJ/Nome Fantasia | high | mitigate | Os dois campos entram pelo passo que sanea a linha INTEIRA antes de qualquer resolução — nenhum caminho novo de leitura de célula é criado. Verificado mecanicamente pela ausência de um segundo ponto de saneamento e provado pelo caso de célula com caractere de risco de fórmula. |
| T-19-12 | Tampering | valor gravado no campo errado por grafia de chave | high | mitigate | A carga usa as chaves em caixa baixa com sublinhado que a função do banco lê; a verificação do Task 2 recusa a grafia em caixa mista. A função do banco lê o registro por NOME de chave, então a grafia errada faria o valor sumir sem erro — o risco é silencioso e por isso é gate mecânico, não revisão de olho. |
| T-19-13 | Denial of Service | planilha existente do dono deixar de importar por campo novo virar obrigatório | high | mitigate | Verificação mecânica de que o conjunto de chaves obrigatórias continua idêntico (exatamente 7, mesmas chaves) e de que os dois campos novos têm marcação de opcional. |
| T-19-14 | Tampering | contaminação do vocabulário do fluxo de frequência pelos apelidos novos | medium | mitigate | A função de sugestão já confere pertencimento à lista recebida (conserto travado na Fase 17); o caso de teste novo prova que "CNPJ" avaliado contra o vocabulário de dois campos sugere não importar. `typesFrequencia.ts` é verificado como não editado. |
| T-19-15 | Information Disclosure | CNPJ exposto a vendedor que não é dono do cliente | low | accept | Nenhuma leitura nova é introduzida por este plano; o CNPJ é uma coluna de `clientes` já protegida pela policy de leitura existente desde a Fase 13. Sem superfície nova. |
| T-19-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` neste plano — zero pacotes novos. Sem checkpoint de legitimidade aplicável. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo.
- `npx eslint lib/importacao app/actions/importacao.ts` limpo.
- `npx vitest run tests/importacao/modelo.test.ts tests/importacao/mapping.test.ts tests/importacao/column-mapping-table.test.tsx tests/importacao/annotarLinha.test.ts tests/importacao/confirmar.test.ts tests/importacao/preview.test.ts tests/importacao/dedupe.test.ts tests/importacao/frequencia-vocabulario.test.ts` — tudo verde.
- `git diff --name-only HEAD~N` não lista `lib/importacao/typesFrequencia.ts`, `components/importacao/ColumnMappingTable.tsx` nem `lib/validations/importacao.ts`.
</verification>

<success_criteria>
- Um Supervisor consegue mapear uma coluna da planilha para CNPJ e outra para Nome Fantasia no passo 2 do assistente de importar clientes.
- O modelo de planilha baixável já traz as duas colunas, com exemplo.
- O valor mapeado chega à carga da chamada em lote com a chave que a função do banco lê, e portanto vira coluna gravada no cliente novo.
- Uma planilha sem essas duas colunas continua importando exatamente como antes.
- Nenhuma validação de formato de CNPJ foi introduzida em nenhum ponto.
</success_criteria>

<output>
Create `.planning/phases/19-planilhas-de-cnpj-e-nome-fantasia/19-02-SUMMARY.md` when done
</output>
