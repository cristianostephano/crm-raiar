---
phase: 19-planilhas-de-cnpj-e-nome-fantasia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/{proximo_numero_livre}_importar_clientes_lote_cnpj_nome_fantasia.sql
  - supabase/migrations/{proximo_numero_livre_mais_um}_atualizar_cnpj_lote.sql
  - tests/importacao/rls-importar-lote.test.ts
  - tests/importacao/rls-cnpj-lote.test.ts
autonomous: false
requirements: [IMP-01, IMP-02, IMP-03]

must_haves:
  truths:
    - "IMP-01/IMP-02: `importar_clientes_lote` passa a aceitar `cnpj` e `nome_fantasia` no objeto de cada linha do lote e grava os dois valores no cliente novo criado — hoje a função IGNORA silenciosamente essas duas chaves, porque nenhuma delas existe na lista de colunas do `jsonb_to_recordset` nem na lista de colunas da instrução de criação"
    - "IMP-01/IMP-02: as duas colunas continuam opcionais em todo caminho — uma linha de planilha sem CNPJ e sem Nome Fantasia entra normalmente, com nulo nas duas colunas, exatamente como hoje"
    - "IMP-01/IMP-02: nenhuma validação de formato/dígito verificador de CNPJ existe no SQL — só presença ou ausência do valor, mesma postura já travada na Fase 18"
    - "IMP-01/IMP-02: todo o resto de `importar_clientes_lote` continua idêntico — guard `is_supervisor()`, pragma de conflito de variável, ignorar razão social já existente sem derrubar o lote, e a separação em DUAS instruções sequenciais (a de clientes e a de produtos consumidos), que é o conserto de visibilidade de RLS da migration 0006 e não pode ser desfeito"
    - "IMP-03: existe uma função nova `atualizar_cnpj_lote(p_atualizacoes jsonb)` que grava, numa única instrução, o CNPJ de vários clientes já 'ganho' de uma vez, devolvendo `(id, razao_social)` de cada cliente efetivamente alterado"
    - "IMP-03: `atualizar_cnpj_lote` é ESTRUTURALMENTE incapaz de criar cliente — existe exatamente uma instrução de alteração em todo o arquivo e nenhuma instrução de criação de linha"
    - "IMP-03: `atualizar_cnpj_lote` só altera clientes cujo `status_acompanhamento` já é 'ganho' — a cláusula de status na condição da alteração é o mecanismo literal disso"
    - "IMP-03: uma linha do lote que não casa com nenhum cliente apto simplesmente não afeta linha nenhuma e não aparece no retorno — não levanta erro nem reverte o lote inteiro (comportamento silencioso e por-linha, DIFERENTE do guard da Fase 18 que levanta exceção)"
    - "IMP-03: um vendedor chamando `atualizar_cnpj_lote` é recusado antes de qualquer gravação, pelo guard `is_supervisor()`; um chamador não autenticado também"
    - "IMP-03: `atualizar_cnpj_lote` recebe identificador de cliente já resolvido (`id uuid`), NUNCA razão social — o casamento por nome e a detecção de nome ambíguo acontecem fora do banco (plano 19-03), antes da função ser chamada"
    - "Nenhuma das duas funções tem elevação de privilégio — as duas rodam com os privilégios de quem chama, e a RLS de `clientes` continua sendo a fronteira real de autorização. O projeto tem exatamente 4 exceções documentadas de elevação e esta fase não abre uma quinta"
    - "Nenhuma das duas migrations mexe em estrutura de tabela — `clientes.cnpj` e `clientes.nome_fantasia` já existem como colunas opcionais desde a migration 0013 e continuam opcionais"
    - "As duas migrations foram aplicadas no projeto Supabase hospedado num ÚNICO `supabase db push`, com aprovação humana prévia, e as suítes de importação continuam verdes"
  artifacts:
    - "supabase/migrations/{proximo_numero_livre}_importar_clientes_lote_cnpj_nome_fantasia.sql (número derivado por `ls` na execução, nunca copiado deste plano)"
    - "supabase/migrations/{proximo_numero_livre_mais_um}_atualizar_cnpj_lote.sql (o número seguinte ao da primeira)"
    - "tests/importacao/rls-cnpj-lote.test.ts (novo) — prova os 5 critérios de sucesso estruturais de IMP-03 contra o banco real"
    - "tests/importacao/rls-importar-lote.test.ts (editado: um caso novo provando que CNPJ e Nome Fantasia chegam gravados no cliente criado)"
  key_links:
    - "`cnpj text` e `nome_fantasia text` precisam entrar em TRÊS lugares da função de importação: na lista de colunas do `jsonb_to_recordset`, na lista de colunas da instrução de criação, e na lista de valores selecionados. Faltando qualquer um dos três, a chave chega da tela, não dá erro nenhum, e o valor some em silêncio — que é exatamente a falha que IMP-01/IMP-02 existem para consertar"
    - "a separação da função de importação em DUAS instruções sequenciais (clientes primeiro, produtos consumidos depois, ligadas por arrays de PL/pgSQL) é o conserto de visibilidade de RLS documentado na migration 0006 — reencadear como duas expressões de escrita numa instrução só ressuscita o erro 42501 em toda importação que traga produtos"
    - "a cláusula `and clientes.status_acompanhamento = 'ganho'` em `atualizar_cnpj_lote` é o que faz o critério de sucesso 5 valer; sem ela a planilha passaria a poder mexer em cliente em andamento"
    - "`atualizar_cnpj_lote` recebe `id uuid` e não razão social — se algum dia a função passar a casar por nome dentro do banco, a detecção de nome ambíguo (o achado real da Fase 17) deixa de existir e a planilha volta a poder gravar no cliente errado"
    - "as duas funções precisam do pragma de conflito de variável, porque as duas declaram retorno em forma de tabela com nomes que colidem com colunas reais — sem ele o Postgres levanta ambiguidade (SQLSTATE 42702), o mesmo bug que a migration 0005 já consertou uma vez"
    - "as duas migrations vão para o MESMO `supabase db push` — um push só, um checkpoint humano só. Migrations já aplicadas nunca são editadas (regra do CLAUDE.md); 0006 e 0017 permanecem intocadas e a versão nova de `importar_clientes_lote` nasce num arquivo novo"
---

<objective>
Preparar a camada de banco das duas planilhas desta fase, num único push de produção: (1) recriar `importar_clientes_lote` aceitando e gravando `cnpj` e `nome_fantasia` — hoje essas duas chaves são silenciosamente descartadas pela função; (2) criar `atualizar_cnpj_lote`, a operação de gravação em massa do CNPJ de clientes já "ganho", espelhando o molde já provado de `atualizar_frequencia_visita_lote` da Fase 17. Mais os testes de integração que provam as duas coisas contra o banco real.

Purpose: é a fundação que os outros três planos da fase consomem. Sem a primeira migration, mapear uma coluna de CNPJ na tela de importar clientes seria teatro — o valor chegaria até o banco e desapareceria sem erro nenhum, que é o pior tipo de falha possível para um usuário não técnico. E a segunda migration é onde três dos cinco critérios de sucesso da fase são garantidos por construção do SQL, não por disciplina de tela: incapaz de criar cliente, só toca cliente "ganho", e linha que não casa não derruba o lote.

Output: duas migrations aplicadas no banco hospedado com aprovação humana prévia, um arquivo de teste de integração novo verde, e um arquivo de teste existente ampliado com o caso de CNPJ/Nome Fantasia.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/skills/Supabase-conventions/SKILL.md
@supabase/migrations/0006_fix_importar_clientes_lote_cte_rls_visibility.sql
@supabase/migrations/0017_atualizar_frequencia_visita_lote.sql
@tests/importacao/rls-frequencia-lote.test.ts
</context>

<source_audit>
## Multi-Source Coverage Audit — Fase 19 (conjunto completo: planos 19-01, 19-02, 19-03, 19-04)

### GOAL (ROADMAP, Fase 19 — 5 critérios de sucesso)

| # | Item | Plano | Status |
|---|------|-------|--------|
| G1 | Na planilha "Importar clientes", o Supervisor mapeia uma coluna para CNPJ (opcional) e o valor é gravado no cliente novo criado | 19-01 (a função de importação passa a aceitar e gravar a coluna) + 19-02 (vocabulário de campos, modelo, resolução da linha, carga da chamada) | COVERED |
| G2 | Idem para Nome Fantasia | 19-01 + 19-02 | COVERED |
| G3 | O Supervisor sobe uma planilha "CNPJ em massa" (Razão Social + CNPJ) e o sistema grava o CNPJ nos clientes já "ganho" correspondentes, casando por nome | 19-01 (função `atualizar_cnpj_lote`) + 19-03 (casamento por nome + ação de servidor) + 19-04 (assistente de 3 passos + rota + menu) | COVERED |
| G4 | Nome normalizado batendo em mais de um cliente vira erro de linha, e nenhum CNPJ é gravado no cliente errado | 19-03 (índice de clientes por chave normalizada, lista com 2+ resultados vira erro e identificador nulo) + 19-04 (motivo exibido na tabela de revisão) | COVERED |
| G5 | A planilha de CNPJ em massa é estruturalmente incapaz de criar cliente — só atualiza cliente existente com status "ganho" | 19-01 (uma instrução de alteração, zero de criação, cláusula de status) | COVERED |

### REQ (REQUIREMENTS.md — `phase_req_ids`: IMP-01, IMP-02, IMP-03)

| ID | Item | Plano | Status |
|----|------|-------|--------|
| IMP-01 | Planilha "Importar clientes" ganha CNPJ como coluna opcional | 19-01 (banco) + 19-02 (aplicação) | COVERED |
| IMP-02 | Planilha "Importar clientes" ganha Nome Fantasia como coluna opcional | 19-01 (banco) + 19-02 (aplicação) | COVERED |
| IMP-03 | Nova planilha "CNPJ em massa" para regularizar clientes já "ganho", casando por nome, nome ambíguo vira erro de linha, incapaz de criar cliente | 19-01 (banco) + 19-03 (lógica pura + ação) + 19-04 (tela) | COVERED |

### RESEARCH

Nenhum RESEARCH.md — o dono do projeto optou explicitamente por pular a etapa de pesquisa, por esta fase ser extensão direta de dois padrões já estabelecidos no próprio repositório (o assistente de importação das Fases 6-7 e a planilha em massa da Fase 17), não um domínio novo. Não há itens de pesquisa a cobrir.

### CONTEXT (decisões travadas repassadas pelo orquestrador)

Nenhum CONTEXT.md — o dono optou por pular a etapa de discussão, por já existir precedente forte no código que responde toda pergunta estrutural. As decisões travadas repassadas pelo orquestrador estão rastreadas abaixo como L-01..L-10.

| ID | Decisão travada | Plano | Status |
|----|-----------------|-------|--------|
| L-01 | Dois campos novos no vocabulário de campos do sistema: `cnpj` (rótulo "CNPJ") e `nomeFantasia` (rótulo "Nome Fantasia"), ambos opcionais | 19-02 | COVERED |
| L-02 | Os dois campos precisam chegar até uma coluna gravada de `clientes`, não só aparecer na tela — rastrear TODOS os consumidores do vocabulário | 19-01 (banco) + 19-02 (modelo, apelidos, resolução da linha, carga da chamada) | COVERED |
| L-03 | Cliente importado continua sempre nascendo em "Aguardando contato"; CNPJ/Nome Fantasia não interagem com o guard da Fase 18 | 19-01 (a função de importação não mexe em etapa nem em status) + 19-02 | COVERED |
| L-04 | A planilha "CNPJ em massa" espelha arquivo por arquivo a "Frequência em massa" da Fase 17 | 19-01 (migration) + 19-03 (camada pura + ação) + 19-04 (tela + rota + menu) | COVERED |
| L-05 | As 5 propriedades estruturais da função em massa (incapaz de criar, só "ganho", linha sem casamento não derruba o lote, sem elevação de privilégio, recebe identificador e não nome) | 19-01 | COVERED |
| L-06 | Tratamento de nome ambíguo copiado da lógica já funcionando da Fase 17, nunca redescoberto | 19-03 | COVERED |
| L-07 | Sem validação de formato de CNPJ em lugar nenhum desta fase | 19-01, 19-02, 19-03 | COVERED |
| L-08 | Duas colunas na planilha de CNPJ em massa: Razão Social + CNPJ. Nenhuma coluna extra | 19-03 (vocabulário de dois campos) + 19-04 (tabela de revisão) | COVERED |
| L-09 | Reusar o caminho existente de envio/mapeamento/leitura de arquivo, sem abrir caminho novo de saída de texto sem saneamento | 19-03 (saneamento de célula na anotação) + 19-04 (reuso da zona de arquivo e da tabela de mapeamento) | COVERED |
| L-10 | Gravação em lote sempre numa instrução única baseada em conjunto, nunca laço linha a linha (limite de ~10s da hospedagem) | 19-01 (uma instrução de alteração) + 19-03 (uma chamada só à função) | COVERED |

**Nenhum item MISSING nas quatro fontes.** Nenhum item foi adiado, reduzido a "versão simplificada" ou transformado em rótulo estático.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Escrever as duas migrations e os testes de integração (RED)</name>
  <files>supabase/migrations/{proximo_numero_livre}_importar_clientes_lote_cnpj_nome_fantasia.sql, supabase/migrations/{proximo_numero_livre_mais_um}_atualizar_cnpj_lote.sql, tests/importacao/rls-cnpj-lote.test.ts, tests/importacao/rls-importar-lote.test.ts</files>
  <read_first>
    - `supabase/migrations/0006_fix_importar_clientes_lote_cte_rls_visibility.sql` — é a versão ATUAL de `importar_clientes_lote` e a base literal da primeira migration nova. Ler o banner inteiro antes de tocar em qualquer coisa: ele explica por que a função está partida em duas instruções sequenciais.
    - `supabase/migrations/0017_atualizar_frequencia_visita_lote.sql` — é o molde literal da segunda migration nova. Espelhar a forma inteira, trocando o campo de frequência pelo campo de CNPJ.
    - `tests/importacao/rls-frequencia-lote.test.ts` — molde literal do arquivo de teste novo: mesmos auxiliares (`uniqueRazaoSocial`, `baseClienteFields`, `getUserId`, `cleanup`, semeadura de cliente "ganho" e de cliente em andamento via chave de serviço), mesma disciplina de sessão real.
    - `tests/importacao/rls-importar-lote.test.ts` — arquivo que ganha um caso novo; ler os auxiliares `baseRowFields`/`cleanup` já existentes para reusá-los em vez de duplicar.
    - `.planning/STATE.md` — item "SECURITY DEFINER exceptions" em Deferred Items (a fase não pode abrir uma quinta exceção) e o item de Blockers sobre o limite de ~10s da hospedagem.
  </read_first>
  <action>
    Derivar os dois números de migration na execução com `ls supabase/migrations/` — nunca copiar número deste plano. A primeira migration nova recebe o próximo número livre; a segunda, o número imediatamente seguinte. Nenhuma migration já existente pode ser editada (regra do CLAUDE.md): 0006 e 0017 ficam intocadas.

    **Migration A — `{n}_importar_clientes_lote_cnpj_nome_fantasia.sql`.** Copiar o corpo de `importar_clientes_lote` da migration 0006 e mudar EXATAMENTE três lugares, nada mais: (a) a lista de colunas do registro derivado do parâmetro em formato de documento ganha `cnpj text` e `nome_fantasia text` no fim; (b) a lista de colunas da instrução de criação de `clientes` ganha `cnpj` e `nome_fantasia`; (c) a lista de valores selecionados ganha `r.cnpj` e `r.nome_fantasia`, nas mesmas posições. Tudo o mais permanece byte a byte: o pragma de conflito de variável, o guard `is_supervisor()` com a mesma mensagem, a cláusula de ignorar razão social repetida, a captura dos identificadores em arrays de PL/pgSQL, a segunda instrução de produtos consumidos dirigida por `unnest`, e o retorno final. Escrever um banner de comentário no topo no mesmo tom dos outros arquivos: dizer que 0006 continua válida e não foi editada, que a única mudança é a passagem das duas colunas opcionais que já existem em `clientes` desde a 0013, e que a separação em duas instruções sequenciais é o conserto de visibilidade de RLS da 0006 e não pode ser desfeita.

    **Migration B — `{n+1}_atualizar_cnpj_lote.sql`.** Espelhar `atualizar_frequencia_visita_lote` da 0017 substituindo o campo de frequência pelo de CNPJ: assinatura `atualizar_cnpj_lote(p_atualizacoes jsonb)`, retorno em forma de tabela com identificador e razão social, linguagem PL/pgSQL, pragma de conflito de variável, guard `is_supervisor()` levantando exceção com mensagem em português (algo como "Somente supervisores podem atualizar CNPJ em massa"), e UMA única instrução de alteração de `clientes` que atribui a coluna de CNPJ a partir do registro derivado do parâmetro (`atualizacoes(id uuid, cnpj text)` — tipo texto simples, pois CNPJ não é enumeração), condicionada a casar o identificador E a `status_acompanhamento` já ser 'ganho', devolvendo identificador e razão social. Nenhuma cláusula de elevação de privilégio. Nenhuma expressão de máscara, regex ou verificação de dígito sobre o valor do CNPJ — presença é tudo que importa nesta fase, mesma postura já travada na Fase 18. Banner de comentário no topo enumerando, em português, as propriedades garantidas por construção (exatamente uma instrução de alteração e nenhuma de criação de linha; só cliente já 'ganho'; linha sem casamento não afeta nada e não aparece no retorno; sem elevação de privilégio; recebe identificador já resolvido, nunca nome).

    **Teste novo — `tests/importacao/rls-cnpj-lote.test.ts`.** Espelhar o arquivo de frequência. A chave de serviço serve SÓ para semear e limpar; toda asserção roda com sessão real do papel sob teste (um teste negativo rodando com a chave de serviço passaria sempre, porque ela ignora a RLS). Casos exigidos, com essas palavras no nome do caso: `supervisor` (grava CNPJ de dois clientes "ganho" numa chamada só e o retorno traz os dois), `sobrescreve` (um cliente que já tem CNPJ tem o valor substituído), `vendedor` (chamada recusada E nenhum CNPJ alterado na releitura), `naoganho` (um cliente em andamento no mesmo lote passa incólume — CNPJ segue nulo e o retorno traz só o "ganho"), `inexistente` (um identificador que não existe na base não derruba as demais linhas do lote), `naocria` (contagem total de clientes antes e depois de um lote só com identificador inexistente é a mesma), `anonimo` (chamador não autenticado é recusado).

    **Caso novo em `tests/importacao/rls-importar-lote.test.ts`.** Um caso com a palavra `cnpjfantasia` no nome: o Supervisor chama a função de importação com uma linha que traz `cnpj` e `nome_fantasia` preenchidos, e a releitura da linha criada (pela chave de serviço) mostra os dois valores gravados. Reusar `baseRowFields`, `uniqueRazaoSocial`, `createdClienteIds` e `cleanup` já existentes no arquivo — não duplicar auxiliar. Não afrouxar nenhuma asserção existente.

    Neste ponto os dois testes ficam VERMELHOS de propósito: `atualizar_cnpj_lote` ainda não existe no banco, e a função de importação ainda descarta as duas chaves em silêncio (é justamente por descartar sem erro que o caso `cnpjfantasia` falha na asserção de releitura, e não na chamada).
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const fs=require('fs');const dir='supabase/migrations';const all=fs.readdirSync(dir).filter(n=>n.endsWith('.sql')).sort();const a=all.filter(n=>n.includes('importar_clientes_lote_cnpj_nome_fantasia'));const b=all.filter(n=>n.includes('atualizar_cnpj_lote'));if(a.length!==1)throw new Error('esperado exatamente 1 arquivo de migration da funcao de importacao, achei '+a.length);if(b.length!==1)throw new Error('esperado exatamente 1 arquivo de migration da funcao em massa, achei '+b.length);if(all[all.length-1]!==b[0])throw new Error('a migration da funcao em massa precisa ser a de MAIOR numero da pasta: '+b[0]+' vs '+all[all.length-1]);if(all[all.length-2]!==a[0])throw new Error('as duas migrations novas precisam ser consecutivas e as duas ultimas da pasta');const strip=f=>fs.readFileSync(dir+'/'+f,'utf8').split('\n').filter(l=>!l.trim().startsWith('--')).join('\n').toLowerCase();const A=strip(a[0]);const B=strip(b[0]);for(const t of ['importar_clientes_lote','#variable_conflict use_column','is_supervisor()','jsonb_to_recordset','cnpj text','nome_fantasia text','r.cnpj','r.nome_fantasia','cliente_produtos','unnest(v_ids, v_razoes)','on conflict'])if(!A.includes(t))throw new Error('faltou na migration de importacao: '+t);if((A.match(/create\s+or\s+replace\s+function/g)||[]).length!==1)throw new Error('a migration de importacao precisa ter exatamente 1 definicao de funcao');if((A.match(/\binsert\s+into\s+clientes\b/g)||[]).length!==1)throw new Error('a migration de importacao precisa ter exatamente 1 instrucao de criacao em clientes');if((A.match(/\binsert\s+into\s+cliente_produtos\b/g)||[]).length!==1)throw new Error('a instrucao de produtos consumidos precisa continuar separada');for(const t of ['atualizar_cnpj_lote','#variable_conflict use_column','is_supervisor()','raise exception','jsonb_to_recordset','id uuid, cnpj text',\"status_acompanhamento = 'ganho'\",'returning clientes.id, clientes.razao_social'])if(!B.includes(t))throw new Error('faltou na migration da funcao em massa: '+t);if((B.match(/create\s+or\s+replace\s+function/g)||[]).length!==1)throw new Error('a migration da funcao em massa precisa ter exatamente 1 definicao de funcao');if((B.match(/\bupdate\s+clientes\b/g)||[]).length!==1)throw new Error('a funcao em massa precisa ter EXATAMENTE 1 instrucao de alteracao');if(/\binsert\s+into\b/.test(B))throw new Error('a funcao em massa nao pode ter nenhuma instrucao de criacao de linha (criterio de sucesso 5)');for(const [nome,body] of [['importacao',A],['em massa',B]]){if(/security\s+definer/.test(body))throw new Error('a migration de '+nome+' nao pode elevar privilegio (o projeto tem 4 excecoes e esta fase nao abre a quinta)');if(/alter\s+table|create\s+table|drop\s+table|create\s+policy|drop\s+policy|truncate|delete\s+from/.test(body))throw new Error('a migration de '+nome+' saiu do escopo: nao mexe em estrutura de tabela nem em policy');if(/\d{2}\.\d{3}\.\d{3}\/\d{4}-\d{2}/.test(body)||/regexp_|~\s*'/.test(body))throw new Error('nenhuma validacao de formato de CNPJ pode existir no SQL da migration de '+nome);}const t1=fs.readFileSync('tests/importacao/rls-cnpj-lote.test.ts','utf8');const n1=(t1.match(/\bit\(\s*[\\\"'][^\\\"']+/g)||[]).map(s=>s.toLowerCase());for(const k of ['supervisor','sobrescreve','vendedor','naoganho','inexistente','naocria','anonimo'])if(!n1.some(t=>t.includes(k)))throw new Error('rls-cnpj-lote.test.ts: falta caso com a palavra '+k);for(const t of ['SEED_ACCOUNTS','signInAs','serviceClient','anonClient','atualizar_cnpj_lote'])if(!t1.includes(t))throw new Error('rls-cnpj-lote.test.ts precisa usar '+t);const t2=fs.readFileSync('tests/importacao/rls-importar-lote.test.ts','utf8');const n2=(t2.match(/\bit\(\s*[\\\"'][^\\\"']+/g)||[]).map(s=>s.toLowerCase());if(!n2.some(t=>t.includes('cnpjfantasia')))throw new Error('rls-importar-lote.test.ts: falta o caso com a palavra cnpjfantasia');if(!t2.includes('nome_fantasia'))throw new Error('rls-importar-lote.test.ts: o caso novo precisa provar a gravacao de nome_fantasia');const gitless=fs.readFileSync(dir+'/0006_fix_importar_clientes_lote_cte_rls_visibility.sql','utf8');if(gitless.toLowerCase().includes('nome_fantasia'))throw new Error('a migration 0006 ja aplicada nunca pode ser editada');console.log('OK '+a[0]+' + '+b[0])"</automated>
  </verify>
  <acceptance_criteria>
    - Existem exatamente dois arquivos de migration novos, consecutivos, e são os dois de maior número da pasta (números derivados por `ls` na execução).
    - Nenhuma migration já existente foi editada — `git diff --name-only HEAD -- supabase/migrations/` lista só os dois arquivos novos, e a 0006 continua sem menção a `nome_fantasia`.
    - A migration de importação contém exatamente uma definição de função, exatamente uma instrução de criação em `clientes` e exatamente uma instrução de criação em `cliente_produtos` (a separação em duas instruções sequenciais foi preservada).
    - A migration de importação declara `cnpj text` e `nome_fantasia text` no registro derivado do parâmetro e seleciona `r.cnpj` e `r.nome_fantasia`; o pragma de conflito de variável, o guard `is_supervisor()`, a cláusula de conflito e o `unnest(v_ids, v_razoes)` continuam presentes.
    - A migration da função em massa contém exatamente uma definição de função, EXATAMENTE uma instrução de alteração de `clientes` e ZERO instruções de criação de linha.
    - A função em massa declara o registro derivado como `id uuid, cnpj text`, condiciona pela `status_acompanhamento = 'ganho'`, devolve identificador e razão social, e levanta exceção quando quem chama não é supervisor.
    - Nenhuma das duas migrations contém cláusula de elevação de privilégio, alteração/criação/remoção de tabela, criação/remoção de policy, comando destrutivo de dados, nem expressão de formato/máscara/regex de CNPJ.
    - `tests/importacao/rls-cnpj-lote.test.ts` existe, usa `signInAs`/`SEED_ACCOUNTS`/`serviceClient`/`anonClient`, e tem casos cujos nomes contêm `supervisor`, `sobrescreve`, `vendedor`, `naoganho`, `inexistente`, `naocria` e `anonimo`.
    - `tests/importacao/rls-importar-lote.test.ts` ganhou um caso com a palavra `cnpjfantasia` que prova a gravação das duas colunas; nenhuma asserção existente foi afrouxada.
    - `npx tsc --noEmit` limpo.
    - Os testes ainda FALHAM em runtime (RED) neste ponto — esperado até o Task 3.
  </acceptance_criteria>
  <done>Duas migrations escritas (números derivados na execução), um arquivo de teste de integração novo criado e um ampliado; tudo ainda RED, aguardando aprovação e push.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Aprovação humana — aplicar as duas migrations no banco Supabase de produção</name>
  <read_first>
    - os dois arquivos de migration criados no Task 1 (são o que será apresentado e aplicado)
    - `.planning/phases/18-cnpj-obrigat-rio-no-ganho/18-01-SUMMARY.md`, seção "Issues Encountered" — o push da Fase 18 foi bloqueado para o executor pelo classificador de permissão do ambiente e teve que ser rodado pelo dono; esperar o mesmo aqui.
  </read_first>
  <what-built>Duas migrations novas, ainda só em arquivo: uma que faz a importação de clientes por planilha passar a gravar CNPJ e Nome Fantasia (hoje esses dois valores seriam jogados fora sem aviso), e outra que cria a operação de gravar CNPJ em massa nos clientes que já são "ganho". Nenhuma das duas mexe na estrutura das tabelas — as colunas já existem e continuam opcionais.</what-built>
  <action>Pausar a execução. É a etapa de revisão antes de produção exigida pelo CLAUDE.md e já praticada em todas as fases que mexeram em schema (9, 10, 11, 12, 13, 15, 17, 18). Explicar ao dono do projeto, em linguagem simples e sem jargão, o que as duas migrations fazem no banco real e o que NÃO fazem (não apagam dado, não mudam a estrutura das tabelas, não deixam nenhum cliente inacessível, e a segunda é incapaz de criar cliente). Avisar também do intervalo esperado entre este push e a conclusão dos planos 19-02/19-03/19-04: a tela ainda não manda os campos novos, então nada muda visualmente até lá — é esperado e temporário, sem perda de dado. Só rodar o `supabase db push` (Task 3) depois do sinal "approved".</action>
  <how-to-verify>
    1. Ler os dois arquivos de migration novos em `supabase/migrations/` (o Claude vai mostrá-los e explicá-los em português simples).
    2. Confirmar que faz sentido: a primeira só adiciona duas informações opcionais no caminho de importação; a segunda só preenche o CNPJ de clientes que já estão marcados como "ganho".
    3. Confirmar que aceita aplicar as duas de uma vez no banco de produção (um `supabase db push` só).
  </how-to-verify>
  <resume-signal>Escreva "approved" para autorizar o push, ou descreva o que precisa mudar antes.</resume-signal>
  <acceptance_criteria>
    - O dono do projeto viu o conteúdo das duas migrations explicado em linguagem simples antes de qualquer escrita em produção.
    - A execução ficou parada até o sinal explícito de aprovação.
    - Nenhum `supabase db push` foi rodado antes deste sinal.
  </acceptance_criteria>
  <done>Aprovação humana registrada; o push do Task 3 está autorizado.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Aplicar as migrations (supabase db push) e levar os testes a GREEN</name>
  <files>tests/importacao/rls-cnpj-lote.test.ts, tests/importacao/rls-importar-lote.test.ts</files>
  <read_first>
    - `.planning/STATE.md` — a decisão de pinar o CLI do Supabase em `2.111.0` para o push (as versões `2.112.0`+ têm um bug de validação de schema conhecido do projeto), e o item de Blockers sobre o rate limit de login do Supabase Auth ao rodar a suíte inteira.
    - `.planning/phases/18-cnpj-obrigat-rio-no-ganho/18-01-SUMMARY.md` — a convenção exata de mitigação do rate limit (provar isoladamente os arquivos de risco em vez de depender de `npm test` inteiro).
  </read_first>
  <action>
    Rodar `npx -y supabase@2.111.0 db push` para aplicar as DUAS migrations de uma vez. Se o ambiente bloquear o comando (escrita direta em produção), NÃO contornar por fora do CLI: parar e devolver um checkpoint pedindo ao dono que rode o comando com a permissão dele — foi exatamente o que aconteceu na Fase 18 e a saída é a mesma.

    Depois do push, re-verificar de forma independente que as duas migrations estão remotas com `npx supabase migration list` antes de considerar qualquer teste como prova.

    Então rodar, isoladamente (um arquivo por vez, com pausa entre execuções para a janela de rate limit reabrir): `npx vitest run tests/importacao/rls-cnpj-lote.test.ts` e `npx vitest run tests/importacao/rls-importar-lote.test.ts`. As duas suítes precisam fechar verdes. Rodar também `npx vitest run tests/importacao/rls-frequencia-lote.test.ts` como alarme de regressão — a função de frequência não foi tocada e precisa continuar verde.

    Se algum caso falhar por asserção real (não por rate limit), consertar a MIGRATION (numa migration nova, jamais editando a que acabou de ser aplicada) ou o teste, conforme o que estiver errado, e repetir. Se falhar com mensagem de limite de requisições, reexecutar o arquivo depois de uma pausa — é o comportamento já documentado do projeto, não regressão desta fase.
  </action>
  <verify>
    <automated>npx supabase migration list && npx vitest run tests/importacao/rls-cnpj-lote.test.ts && npx vitest run tests/importacao/rls-importar-lote.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase migration list` mostra as duas migrations novas com número remoto correspondente (aplicadas no projeto hospedado).
    - `tests/importacao/rls-cnpj-lote.test.ts` fecha 100% verde: os sete casos exigidos passam contra o banco real.
    - `tests/importacao/rls-importar-lote.test.ts` fecha 100% verde, incluindo o caso `cnpjfantasia` — a releitura mostra `cnpj` e `nome_fantasia` gravados no cliente criado.
    - `tests/importacao/rls-frequencia-lote.test.ts` continua verde (nenhuma regressão na função em massa da Fase 17).
    - Nenhuma migration já aplicada foi editada durante o conserto de qualquer falha.
    - Qualquer falha residual restante é comprovadamente de limite de requisições de login, nunca de asserção.
  </acceptance_criteria>
  <done>As duas migrations aplicadas em produção com aprovação humana; a gravação de CNPJ/Nome Fantasia na importação e as cinco propriedades estruturais da operação em massa provadas contra o banco real.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → ação de servidor → função do banco | O conteúdo da planilha é texto totalmente controlado pelo usuário; chega como parâmetro em formato de documento até as duas funções |
| chamador → RLS de `clientes` | As duas funções rodam sem elevação de privilégio, então a policy de alteração de `clientes` continua avaliando cada linha |
| repositório → banco hospedado | O `supabase db push` é escrita direta em produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Elevation of Privilege | `atualizar_cnpj_lote` | critical | mitigate | Guard `is_supervisor()` levantando exceção antes de qualquer alteração, somado à ausência de cláusula de elevação de privilégio (verificada mecanicamente no Task 1) — a RLS de `clientes` continua avaliando cada linha. Provado pelos casos `vendedor` e `anonimo` de `tests/importacao/rls-cnpj-lote.test.ts`. |
| T-19-02 | Tampering | criação de cliente por uma planilha que só deveria atualizar | critical | mitigate | A função em massa tem exatamente uma instrução de alteração e zero instruções de criação de linha — garantido por construção do SQL e verificado mecanicamente por contagem no Task 1. Provado pelo caso `naocria` (contagem total de clientes idêntica antes e depois). |
| T-19-03 | Tampering | gravação de CNPJ em cliente que não é "ganho" | high | mitigate | Cláusula `and clientes.status_acompanhamento = 'ganho'` na condição da alteração, mesma forma já provada na Fase 17. Provado pelo caso `naoganho`. |
| T-19-04 | Tampering | perda silenciosa de CNPJ/Nome Fantasia na importação de clientes | high | mitigate | As duas colunas entram nos três lugares exigidos da função de importação (registro derivado, lista de colunas, lista de valores) — a verificação mecânica do Task 1 exige os três, e o caso `cnpjfantasia` prova a gravação por releitura do banco. Esta ameaça É o requisito: hoje o valor sumiria sem erro nenhum. |
| T-19-05 | Denial of Service | quebra da importação de produtos consumidos por regressão de visibilidade de RLS | high | mitigate | A separação em duas instruções sequenciais da migration 0006 é preservada e verificada por contagem (exatamente uma instrução de criação em `clientes` e exatamente uma em `cliente_produtos`). O caso já existente de produtos em `rls-importar-lote.test.ts` roda como alarme no Task 3. |
| T-19-06 | Denial of Service | lote grande estourando o limite de ~10s da hospedagem | medium | mitigate | Gravação sempre baseada em conjunto, numa instrução única, nunca laço linha a linha — garantido pela contagem de exatamente uma instrução de alteração no Task 1 e herdado do molde da 0017. |
| T-19-07 | Tampering | migrations aplicadas em produção sem revisão humana | medium | mitigate | Checkpoint bloqueante (Task 2) antes do `supabase db push`, exigido pelo CLAUDE.md e praticado em todas as fases de schema desde a 9. As duas migrations vão num push só, com um checkpoint só. |
| T-19-08 | Tampering | edição de migration já aplicada | medium | mitigate | As versões novas das funções nascem em arquivos novos; 0006 e 0017 ficam intocadas. Verificado mecanicamente no Task 1 (a 0006 não pode mencionar a coluna nova). |
| T-19-09 | Spoofing | identificador de cliente vindo direto de célula de planilha | medium | mitigate | A função em massa aceita `id uuid` já resolvido; a resolução por nome (e a recusa de nome ambíguo) acontece fora do banco, no plano 19-03, sobre uma leitura de `clientes` limitada pela RLS do chamador. Nenhum identificador da carga vem de célula. |
| T-19-10 | Information Disclosure | mensagem de erro revelando existência de cliente de outro vendedor | low | accept | A operação é exclusiva de Supervisor, papel que já enxerga a base inteira pela policy de leitura — não há informação a vazar para quem já pode vê-la. Risco residual nulo. |
| T-19-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` neste plano nem nesta fase — zero pacotes novos, nada a auditar. Sem checkpoint de legitimidade aplicável. |
</threat_model>

<verification>
- `npx supabase migration list` mostra as duas migrations novas aplicadas no projeto hospedado.
- `npx vitest run tests/importacao/rls-cnpj-lote.test.ts` — 100% verde.
- `npx vitest run tests/importacao/rls-importar-lote.test.ts` — 100% verde, incluindo o caso novo.
- `npx vitest run tests/importacao/rls-frequencia-lote.test.ts` — continua verde (alarme de regressão).
- `npx tsc --noEmit` limpo.
- `git diff --name-only HEAD~N -- supabase/migrations/` lista apenas os dois arquivos novos.
</verification>

<success_criteria>
- `importar_clientes_lote` grava `cnpj` e `nome_fantasia` quando eles vêm no lote, e continua aceitando linhas sem eles (nulo nas duas colunas).
- `atualizar_cnpj_lote` existe no banco hospedado, é exclusiva de Supervisor, só altera cliente já "ganho", é incapaz de criar cliente, e uma linha sem casamento não derruba o lote.
- Nenhuma das duas funções eleva privilégio; a RLS de `clientes` continua sendo a fronteira de autorização.
- Nenhuma mudança de estrutura de tabela e nenhuma validação de formato de CNPJ foi introduzida.
- Os planos 19-02 e 19-03 podem começar: a interface de banco que os dois consomem está publicada e provada.
</success_criteria>

<output>
Create `.planning/phases/19-planilhas-de-cnpj-e-nome-fantasia/19-01-SUMMARY.md` when done
</output>
