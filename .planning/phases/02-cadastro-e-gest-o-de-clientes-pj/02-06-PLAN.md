---
phase: 02-cadastro-e-gest-o-de-clientes-pj
plan: 06
type: execute
wave: 6
depends_on: [02-05]
files_modified:
  - components/clientes/ClienteDetailSheet.tsx
  - app/actions/clientes.ts
  - lib/validations/cliente.ts
  - lib/supabase/queries/clientes.ts
  - components/clientes/KanbanBoard.tsx
  - components/ui/sheet.tsx
  - components/ui/separator.tsx
  - tests/clientes/update-delete.test.ts
  - package.json
autonomous: true
requirements: [CLI-02, CLI-05, CLI-06]

must_haves:
  truths:
    - "Clicking (not dragging) a card opens a right-side detail Sheet showing the full client, where a Vendedor can fill in the previously-blank optional fields at any time and save (CLI-02)."
    - "A Vendedor can edit their own client's data but sees no 'Apagar cliente' button; a Supervisor can edit any client, reassign its responsável, and delete it (CLI-05/CLI-06)."
    - "Deleting a client asks for confirmation with the exact destructive copy and, on confirm, removes the client and its card from the board."
    - "A Vendedor cannot delete a client even via a direct API call — the DELETE is a no-op under RLS (proven by test)."
  artifacts:
    - components/clientes/ClienteDetailSheet.tsx
    - app/actions/clientes.ts
    - lib/validations/cliente.ts
  key_links:
    - "The card's onOpen slot (defined in 02-03) drives selectedClienteId state in KanbanBoard, which opens the Sheet and loads the full client via getClienteById — a plain click opens it, a drag does not (02-04 distinction preserved)."
    - "updateCliente/deleteCliente re-check the caller's role server-side AND rely on the clientes UPDATE/DELETE RLS from 02-01 as the real boundary — the hidden 'Apagar' button is UX only (CLAUDE.md 'nunca esconder só na UI')."
    - "The Sheet is built as a two-section shell (Dados do cliente now, Funil placeholder) so 02-07 fills the Funil section without restructuring it."
---

<objective>
Build the client detail/edit experience: a right-side Sheet opened by clicking a card, containing the "Dados do cliente" section (all required + optional fields editable), a Supervisor-only "Apagar cliente" action with a destructive confirmation, and the updateCliente/deleteCliente Server Actions. This is where CLI-02's "complete the rest later" actually happens and where CLI-05/CLI-06's edit/delete permission split becomes real.

Purpose: CLI-02 (fill optional fields whenever), CLI-05 (Supervisor edits/deletes all + reassigns responsável), CLI-06 (Vendedor edits own, cannot delete).
Output: `ClienteDetailSheet` (Dados section + delete), `updateCliente`/`deleteCliente` actions, and the full edit schema.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-UI-SPEC.md
@.planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-PATTERNS.md
@.planning/research/PITFALLS.md
@components/auth/InviteUserForm.tsx
@components/clientes/ClienteQuickCreateForm.tsx
@components/clientes/KanbanBoard.tsx
@app/actions/clientes.ts
@lib/validations/cliente.ts
@lib/supabase/queries/clientes.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: updateCliente + deleteCliente actions, edit schema, getClienteById (+ tests)</name>
  <files>app/actions/clientes.ts, lib/validations/cliente.ts, lib/supabase/queries/clientes.ts, tests/clientes/update-delete.test.ts</files>
  <read_first>
    - app/actions/clientes.ts (extend the existing file created in 02-02 — reuse its role-resolution + structured-error pattern)
    - lib/validations/cliente.ts (extend with the full edit schema; keep object-level superRefine, no per-field .refine — the zod v4 caveat)
    - .planning/research/PITFALLS.md (security: WITH CHECK on UPDATE prevents responsavel reassignment by a Vendedor; delete is Supervisor-only; test the negative case from a Vendedor session directly)
    - tests/clientes/rls-clientes.test.ts (reuse SEED_ACCOUNTS + signInAs to test as Vendedor A vs Supervisor)
  </read_first>
  <behavior>
    - updateClienteSchema covers id + all editable fields (required razão social/endereço/responsável; optional categoria, contato, telefone, email, produtos, número de lojas). Multi-value produtos syncs the cliente_produtos join.
    - updateCliente(values): re-validates; a Vendedor cannot change responsavel (server drops/ignores the field for non-Supervisors, and RLS WITH CHECK backstops it); returns structured { data } | { error: { code } } (duplicate_razao_social if the new razão social collides).
    - deleteCliente(id): returns { error: { code: 'forbidden' } } for a non-Supervisor caller AND is a no-op under RLS; on Supervisor caller deletes the client (cascade removes its tarefas/historico/cliente_produtos). revalidatePath('/clientes') on success.
    - getClienteById(id): returns the full client incl. its produto ids for the edit form; RLS-scoped (a Vendedor requesting a non-owned id gets nothing).
  </behavior>
  <action>
    Extend lib/validations/cliente.ts with updateClienteSchema. Extend app/actions/clientes.ts with updateCliente(values) and deleteCliente(id) following <behavior>, reusing the 02-02 caller-role resolution and 23505 -> duplicate_razao_social mapping; for a Vendedor, strip any responsavel change before the update. Add getClienteById(id) to lib/supabase/queries/clientes.ts. Write tests/clientes/update-delete.test.ts using SEED_ACCOUNTS: Vendedor A can UPDATE their own client's optional fields; Vendedor A cannot DELETE it (0 rows / forbidden); Supervisor CAN delete it; a Vendedor's attempt to set responsavel to Vendedor B is rejected/stripped. Clean up via serviceClient.
  </action>
  <verify>
    <automated>npx vitest run tests/clientes/update-delete.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - deleteCliente is a no-op for a Vendedor and succeeds for a Supervisor (behavior assertion, tested from real signed-in sessions).
    - updateCliente strips/blocks a Vendedor's responsavel change (behavior assertion).
    - updateClienteSchema uses object-level superRefine (source assertion).
  </acceptance_criteria>
  <done>Edit/delete data layer complete and permission-tested. Commit: `feat(02-06): updateCliente/deleteCliente actions + edit schema + RLS tests`.</done>
</task>

<task type="auto">
  <name>Task 2: ClienteDetailSheet (Dados section) + delete confirmation, opened from a card</name>
  <files>components/clientes/ClienteDetailSheet.tsx, components/clientes/KanbanBoard.tsx, components/ui/sheet.tsx, components/ui/separator.tsx, package.json</files>
  <read_first>
    - .planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-UI-SPEC.md (Client Detail/Edit Sheet: 480px desktop / full-width mobile; header = razão social title + close + Supervisor-only "Apagar cliente" ghost destructive button; Section 1 "Dados do cliente" fields incl. D-11 cidade/estado, Responsável Select Supervisor-can-reassign / Vendedor read-only; footer "Salvar alterações" primary; destructive confirmation copy for apagar)
    - components/clientes/ClienteQuickCreateForm.tsx (reuse the form field patterns, Select controlled-from-mount, banners, submit-disabled-while-submitting)
    - components/clientes/KanbanBoard.tsx (add selectedClienteId state + render the Sheet; the ClienteCard onOpen slot from 02-03 drives it; a drag must NOT open the sheet — reuse 02-04's click-vs-drag distinction)
  </read_first>
  <action>
    Install primitives: `npx shadcn@latest add sheet separator` (official registry). Build components/clientes/ClienteDetailSheet.tsx (Client Component): a right-side Sheet (480px desktop, full-width mobile) whose header shows the razão social (Heading 20px/600), a close control, and — only when callerRole==='supervisor' — an "Apagar cliente" ghost destructive button that opens a confirmation dialog with the exact copy "Tem certeza que deseja apagar o cliente {razão social}? Essa ação não pode ser desfeita e vai remover todo o histórico do funil." (Cancelar ghost / Apagar destructive) calling deleteCliente. Section 1 "Dados do cliente" (Heading + Separator below): a react-hook-form bound to updateClienteSchema rendering razão social, endereço (CEP, rua, número, complemento, cidade, estado — D-11), Responsável (Supervisor: Select to reassign; Vendedor: own name read-only), and the CLI-02 optionals (Categoria Select, Contato, Telefone, Email, Produtos consumidos multi-select checklist, Número de lojas numeric). Leave a clearly-marked placeholder region below Section 1 for the "Funil" section that 02-07 will fill (an empty section container + comment — do not stub fake funil UI). Footer: a single "Salvar alterações" primary button pinned to the bottom, calling updateCliente; success copy "Cliente salvo com sucesso.", duplicate copy "Já existe um cliente cadastrado com essa razão social.", generic copy "Não foi possível salvar as alterações. Tente novamente." In KanbanBoard, hold selectedClienteId state, load the full client with getClienteById when a card's onOpen fires (click, never drag), and render the Sheet; refresh the board after a successful save/delete.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build 2>&1 | grep -Eq "(Compiled successfully|✓ Compiled|Route)" && echo BUILD-OK</automated>
    <human-check>Click a card (do not drag) — the Sheet opens with the client's data. As Vendedor: fill previously-blank optional fields, Salvar — the Incompleto badge disappears; there is NO Apagar button. As Supervisor: reassign the responsável and save; open Apagar, confirm the destructive copy, apagar — the card leaves the board. Dragging a card still does NOT open the Sheet. (End-of-phase batch.)</human-check>
  </verify>
  <acceptance_criteria>
    - The "Apagar cliente" button renders only when callerRole==='supervisor' (source assertion, CLI-05/CLI-06).
    - The Responsável control is a reassign Select for Supervisor and read-only for Vendedor (source assertion, CLI-03/CLI-04).
    - Clicking opens the Sheet; a drag does not (source assertion — onOpen is the click path, distinct from dragHandleProps).
    - A clearly-labelled empty Funil-section container exists for 02-07 (source assertion).
    - Build/lint/typecheck pass.
  </acceptance_criteria>
  <done>Clients are fully editable in a Sheet with the correct edit/delete permission split. Commit: `feat(02-06): client detail Sheet (Dados) + supervisor delete confirmation`.</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- **Component** `components/clientes/ClienteDetailSheet.tsx` — two-section shell + Dados do cliente + delete confirmation
- **Server Actions** `updateCliente(values)`, `deleteCliente(id)` in `app/actions/clientes.ts`
- **Validation** `updateClienteSchema` in `lib/validations/cliente.ts`
- **Query** `getClienteById(id)` in `lib/supabase/queries/clientes.ts`
- **Board wiring** card click → selectedClienteId → Sheet in `components/clientes/KanbanBoard.tsx`
- **shadcn primitives** `sheet`, `separator`
- **Test** `tests/clientes/update-delete.test.ts`
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser edit form → updateCliente / deleteCliente Server Actions | Edits/deletes cross into the server; role + ownership must be enforced server-side, never by the hidden button alone. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-18 | Elevation of Privilege | deleteCliente by a Vendedor | high | mitigate | App-layer role check + clientes DELETE RLS `using (is_supervisor())` (02-01); the hidden button is UX only; negative-case test proves a Vendedor delete is a no-op (CLI-06). |
| T-02-19 | Elevation of Privilege | responsavel reassignment via updateCliente | high | mitigate | Vendedor's responsavel change stripped server-side + UPDATE WITH CHECK backstop (02-01); tested from a Vendedor session. |
| T-02-20 | Tampering | duplicate razão social on edit | medium | mitigate | 23505 mapped to duplicate_razao_social; the UNIQUE constraint is the guarantee (D-06). |
| T-02-21 | Information Disclosure | getClienteById for a non-owned id | high | mitigate | RLS-scoped read returns nothing for a Vendedor requesting another vendedor's client. |
</threat_model>

<verification>
- `npx vitest run tests/clientes/`, `npx tsc --noEmit`, `npm run lint`, `npm run build` green.
- Manual (end-of-phase batch): edit-as-Vendedor (no delete), edit+reassign+delete-as-Supervisor, click-opens/drag-doesn't.
</verification>

<success_criteria>
- CLI-02/CLI-05/CLI-06 satisfied: optional fields completable anytime; Supervisor edits/deletes/reassigns all; Vendedor edits own but cannot delete — enforced by RLS, not just the hidden UI.
</success_criteria>

<output>
Create `.planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-06-SUMMARY.md` when done.
</output>
