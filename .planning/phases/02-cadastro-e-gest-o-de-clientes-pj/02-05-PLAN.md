---
phase: 02-cadastro-e-gest-o-de-clientes-pj
plan: 05
type: execute
wave: 5
depends_on: [02-03]
files_modified:
  - app/(app)/clientes/page.tsx
  - components/clientes/ClienteToolbar.tsx
  - components/clientes/FiltersPopover.tsx
  - components/clientes/ClienteCard.tsx
  - components/clientes/KanbanBoard.tsx
  - lib/supabase/queries/clientes.ts
  - components/ui/tabs.tsx
  - components/ui/popover.tsx
  - components/ui/dropdown-menu.tsx
  - components/ui/checkbox.tsx
  - tests/clientes/incompleto.test.ts
  - package.json
autonomous: true
requirements: [CLI-07, CLI-02]

must_haves:
  truths:
    - "Any user finds a client fast by typing part of the razão social in the search box (text search filters by razão social only, D-07) even with the full team board loaded (CLI-07)."
    - "The 'Filtros' panel narrows the board by categoria, produto consumido, cidade, and estado; the vendedor/responsável filter appears only for the Supervisor (D-08/D-09)."
    - "A 'Todos' / 'Incompletos' tab switches the board to show only clients missing an optional field, without leaving the page (D-01); those cards carry a neutral 'Incompleto' badge (D-02)."
    - "Each card shows quick-action icons (phone / WhatsApp / schedule); when telefone is blank the icon renders disabled, not hidden, so the layout stays stable (D-03)."
  artifacts:
    - components/clientes/ClienteToolbar.tsx
    - components/clientes/FiltersPopover.tsx
    - lib/supabase/queries/clientes.ts
  key_links:
    - "Search/filter/sort/tab state lives in the client-side board (all RLS-visible cards are already loaded once), so filtering is instant and adds zero extra DB egress (Pitfall 7)."
    - "isClienteIncompleto() is a shared pure predicate used by BOTH the 'Incompleto' badge and the 'Incompletos' tab, so they can never disagree."
    - "The vendedor filter is gated on callerRole==='supervisor' — a Vendedor never sees it (D-09) and never needs it (their board is already only theirs, CLI-04)."
---

<objective>
Add the toolbar that makes a full-team board usable: text search by razão social (D-07), a Filtros popover (categoria / produto / cidade / estado, plus vendedor for Supervisor — D-08/D-09), a sort menu, and the "Todos / Incompletos" tabs (D-01) with the "Incompleto" badge (D-02). Finish the card anatomy with the quick-action icon row (D-03).

Purpose: CLI-07 (search/filter) — without it the Supervisor's "vê todos os clientes" view degrades fast (FEATURES.md flags this as a must-promote gap); and CLI-02's incremental-completion model needs the Incompletos view + badge to be discoverable.
Output: `ClienteToolbar`, `FiltersPopover`, the shared `isClienteIncompleto` predicate, and the completed compact-card anatomy.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-UI-SPEC.md
@.planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-CONTEXT.md
@.planning/research/PITFALLS.md
@app/(app)/clientes/page.tsx
@components/clientes/KanbanBoard.tsx
@components/clientes/ClienteCard.tsx
@lib/supabase/queries/clientes.ts
@lib/funil/etapas.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared incompleto predicate + query fields for filters</name>
  <files>lib/supabase/queries/clientes.ts, tests/clientes/incompleto.test.ts</files>
  <read_first>
    - .planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-CONTEXT.md (D-02 optional fields that make a cadastro "incompleto": categoria, contato, telefone, email, produtos consumidos, número de lojas)
    - .planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-UI-SPEC.md ("Incompleto" badge = neutral/outline, never blue/red; the Incompletos tab is a filtered view of the same list)
    - lib/supabase/queries/clientes.ts (extend the existing reader — do not fork it)
  </read_first>
  <behavior>
    - isClienteIncompleto(cliente) returns true when ANY of these is blank/empty: categoria_id, contato, telefone, email, numero_de_lojas, or the produtos list (empty). Returns false only when all are present.
    - The query returns, per card, the fields needed for filtering: categoria_id + categoria nome, produto ids/names, cidade, estado, responsavel id/name, plus the optional fields the predicate reads.
  </behavior>
  <action>
    Add an exported pure predicate isClienteIncompleto(cliente) to a shared module (lib/funil or lib/validations — colocate with cliente types) implementing the D-02 rule in <behavior>. Extend getClientesAgrupadosPorEtapa() to select the fields needed for both the predicate and the FiltersPopover options (categoria, produtos via cliente_produtos join, cidade, estado, responsavel). Write tests/clientes/incompleto.test.ts asserting: a client with all optionals blank is incompleto; filling every optional flips it to complete; a client missing only produtos is still incompleto.
  </action>
  <verify>
    <automated>npx vitest run tests/clientes/incompleto.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - isClienteIncompleto is a single exported pure function reused by badge + tab (source assertion).
    - Unit tests cover all-blank, all-filled, and missing-only-produtos (behavior assertion).
  </acceptance_criteria>
  <done>Shared incomplete-detection + filter fields ready. Commit: `feat(02-05): isClienteIncompleto predicate + filter query fields`.</done>
</task>

<task type="auto">
  <name>Task 2: Toolbar (search, filters, sort, tabs) + card badge/quick-actions</name>
  <files>components/clientes/ClienteToolbar.tsx, components/clientes/FiltersPopover.tsx, app/(app)/clientes/page.tsx, components/clientes/KanbanBoard.tsx, components/clientes/ClienteCard.tsx, components/ui/tabs.tsx, components/ui/popover.tsx, components/ui/dropdown-menu.tsx, components/ui/checkbox.tsx, package.json</files>
  <read_first>
    - .planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-UI-SPEC.md (Client List toolbar layout: search Input placeholder "Buscar por razão social...", "Filtros" outline button with a blue active dot when ≥1 filter, "Ordenar por" DropdownMenu options, Tabs "Todos"/"Incompletos" with blue active indicator; FiltersPopover contents + "Limpar filtros"/"Aplicar filtros"; quick-action icon row Phone/MessageCircle/CalendarClock, 44px touch target, disabled-not-hidden when blank; Copywriting Contract for every empty state)
    - components/auth/InviteUserForm.tsx (Select controlled-from-mount pattern, reused for filter Selects)
    - components/clientes/KanbanBoard.tsx (already a Client Component from 02-04 — filter state lives here or in the toolbar)
  </read_first>
  <action>
    Install the primitives: `npx shadcn@latest add tabs popover dropdown-menu checkbox` (official registry; if checkbox was already added by a later plan, the CLI is idempotent). Build components/clientes/ClienteToolbar.tsx (Client Component) holding: a search Input (placeholder "Buscar por razão social...", filters by razão social only per D-07), the "Filtros" outline Button opening components/clientes/FiltersPopover.tsx, an "Ordenar por" DropdownMenu with options "Mais recentes" / "Razão social (A-Z)" / "Parado há mais tempo", and shadcn Tabs "Todos" / "Incompletos". FiltersPopover contains: Categoria Select, Produto consumido multi-select (Checkbox list), Cidade Input, Estado Select, and — only when callerRole==='supervisor' — a Vendedor/responsável Select (D-09); footer "Limpar filtros" (ghost) / "Aplicar filtros" (primary, inside the popover only); show a small blue active-state dot on the Filtros button when ≥1 filter is applied. Lift the search/filter/sort/activeTab state into the client board and apply it in-memory over the already-loaded RLS-scoped card set (no re-query — Pitfall 7): search matches razão social; filters AND together; sort reorders within columns (for "Parado há mais tempo" use diasParado from 02-04's staleness module); the Incompletos tab keeps only cards where isClienteIncompleto is true. Pass incompleto into each ClienteCard (renders the neutral "Incompleto" badge in the top-right slot from 02-03). Add the quick-action icon row to ClienteCard: ghost Phone (tel:), MessageCircle (https://wa.me/…), CalendarClock icons using the client's telefone; when telefone is blank render the icon disabled/muted, not hidden. Wire the UI-SPEC empty states: Incompletos-with-nothing -> "Nenhum cadastro incompleto! Todos os clientes estão com os dados completos."; search/filters return nothing -> "Nenhum cliente encontrado com esses filtros." with a "Limpar filtros" link.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build 2>&1 | grep -Eq "(Compiled successfully|✓ Compiled|Route)" && echo BUILD-OK</automated>
    <human-check>Type part of a razão social — only matching cards remain. Open Filtros, pick a categoria — the board narrows and the blue dot appears; "Limpar filtros" resets it. As Supervisor the Vendedor filter is present; as Vendedor it is absent. Switch to "Incompletos" — only cards with a blank optional field show, each with the "Incompleto" badge; if none, the friendly empty state shows. A card with no telefone shows the phone icon greyed, not missing. (End-of-phase batch.)</human-check>
  </verify>
  <acceptance_criteria>
    - Search matches razão social only (source assertion — no contato/email in the search predicate, D-07).
    - The Vendedor filter renders only when callerRole==='supervisor' (source assertion, D-09).
    - Filtering is in-memory over the loaded set (source assertion — no new getClientes call on filter change, Pitfall 7).
    - Incompletos tab and the badge both use isClienteIncompleto (source assertion).
    - Build/lint/typecheck pass.
  </acceptance_criteria>
  <done>The board is searchable/filterable/sortable with a working Incompletos view and completed card anatomy. Commit: `feat(02-05): search, filters, sort, Incompletos tab, card quick-actions`.</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- **Component** `components/clientes/ClienteToolbar.tsx` — search + Filtros + Ordenar por + Todos/Incompletos tabs
- **Component** `components/clientes/FiltersPopover.tsx` — categoria/produto/cidade/estado (+ vendedor for Supervisor)
- **Predicate** `isClienteIncompleto(cliente)` (shared by badge + tab)
- **shadcn primitives** `tabs`, `popover`, `dropdown-menu`, `checkbox`
- **Card anatomy** `Incompleto` badge slot + quick-action icon row wired on `ClienteCard`
- **Test** `tests/clientes/incompleto.test.ts`
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| in-browser filter over loaded card set | Filtering never widens visibility — it only narrows an already-RLS-scoped set; it is a UX convenience, not an authorization boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16 | Information Disclosure | Vendedor filter option | low | mitigate | The vendedor filter is Supervisor-only in the UI, but the real boundary is RLS: a Vendedor's loaded set only ever contains their own cards, so no client-side filter could reveal another vendedor's data (CLI-04/D-09). |
| T-02-17 | Information Disclosure | quick-action tel:/wa.me links | low | accept | Links are built from the client's own telefone (already visible to the viewer under RLS); no new data exposed. |
</threat_model>

<verification>
- `npx vitest run tests/clientes/`, `npx tsc --noEmit`, `npm run lint`, `npm run build` green.
- Manual (end-of-phase batch): search/filter/sort/tabs behavior + Supervisor-only vendedor filter + disabled-not-hidden quick actions.
</verification>

<success_criteria>
- CLI-07 satisfied: fast text search + multi-dimension filters usable on a full-team board; CLI-02's incremental model made discoverable via the Incompletos view + badge.
</success_criteria>

<output>
Create `.planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-05-SUMMARY.md` when done.
</output>
