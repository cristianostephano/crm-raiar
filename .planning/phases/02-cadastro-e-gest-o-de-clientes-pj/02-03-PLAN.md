---
phase: 02-cadastro-e-gest-o-de-clientes-pj
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - components/clientes/ClienteCard.tsx
  - components/clientes/KanbanBoard.tsx
  - app/(app)/clientes/page.tsx
  - lib/supabase/queries/clientes.ts
autonomous: true
requirements: [FUN-01, CLI-04]

must_haves:
  truths:
    - "Opening /clientes shows the 7 fixed funnel stages as columns in order, each client rendered as a compact card in its current stage's column (FUN-01, D-05)."
    - "Each card shows razão social prominently and its categoria at a glance without opening the client (D-03/D-04)."
    - "A Supervisor's cards also show the responsável's name; a Vendedor's cards omit it (all cards are theirs) — UI-SPEC card anatomy."
    - "An empty column shows 'Nenhum cliente nesta etapa'; a brand-new vendedor with zero clients sees the whole-board empty state."
  artifacts:
    - components/clientes/ClienteCard.tsx
    - components/clientes/KanbanBoard.tsx
  key_links:
    - "KanbanBoard renders columns strictly from ETAPAS (lib/funil/etapas.ts), so column order/labels stay a single source of truth shared with the DB enum."
    - "ClienteCard is a pure presentational component whose prop contract is defined once here to include everything later plans need (badges, incompleto flag, quick-action icons, drag handle, onOpen) so 02-04/02-05 pass props rather than restructure it."
    - "The page passes callerRole down so the card conditionally renders the responsável line (Supervisor only)."
---

<objective>
Turn the simple grouped list from 02-02 into the real compact kanban board — 7 fixed columns (D-05), each holding compact client cards (D-03/D-04) — the primary screen of this phase. No drag yet (that is 02-04); this plan establishes the board layout, the column structure, and the reusable `ClienteCard` prop contract.

Purpose: D-05 explicitly folded the funnel into this phase so the owner sees clients "já organizados em colunas por etapa", not a flat list. This is that view.
Output: `KanbanBoard` (static 7-column layout) and `ClienteCard` (compact, presentational, full prop contract), wired into /clientes.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-UI-SPEC.md
@.planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-PATTERNS.md
@app/(app)/clientes/page.tsx
@lib/supabase/queries/clientes.ts
@lib/funil/etapas.ts
@components/ui/badge.tsx
@components/ui/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ClienteCard (compact, presentational, full prop contract)</name>
  <files>components/clientes/ClienteCard.tsx, lib/supabase/queries/clientes.ts</files>
  <read_first>
    - .planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-UI-SPEC.md (Card anatomy under "Client List / Kanban Board": Card title role 16px/600 razão social truncated 1 line; neutral/outline categoria Badge never blue; responsável line Supervisor-only 14px/400 muted; 8px padding; and the color rules — status colors are NOT blue)
    - components/ui/badge.tsx, components/ui/card.tsx (existing primitives to compose)
    - .planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-PATTERNS.md ("ClienteCard.tsx" — badge/row rendering analog from equipe/page.tsx)
  </read_first>
  <action>
    Build components/clientes/ClienteCard.tsx as a pure presentational Client Component. Define its full prop contract NOW so later plans do not restructure it: cliente (id, razaoSocial, categoriaNome | null, responsavelNome | null, etapa, statusAcompanhamento, cidade, estado), showResponsavel: boolean (Supervisor-only line), incompleto: boolean (badge slot — rendered from 02-05 data, prop exists now), isOverdue: boolean and overdueTooltip?: string (amber highlight slot — populated in 02-04, prop exists now), onOpen?: () => void (click-to-open-detail, wired in 02-06), and dragHandleProps?: object (spread onto the card root by 02-04's dnd-kit integration, optional/no-op now). Render: razão social as Card title (16px/600, truncate 1 line); categoria as a neutral/outline Badge (never blue); if showResponsavel, the responsável name as 14px/400 muted-foreground; reserve the top-right corner for the "Incompleto" badge (render only when incompleto is true) and a 4px amber left border + TriangleAlert slot when isOverdue (both inert until their data plans land). Compact: `sm` (8px) padding. Do NOT use `--primary` blue for any status/category badge. Extend lib/supabase/queries/clientes.ts so getClientesAgrupadosPorEtapa() joins categoria name and responsável name (narrow select) for the card display.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - ClienteCard exposes props incompleto, isOverdue, overdueTooltip, onOpen, dragHandleProps, showResponsavel (source assertion — later plans depend on this contract).
    - No `--primary`/`text-primary`/`bg-primary` class on the categoria or status badge (source assertion — status/category are never blue per UI-SPEC).
    - Razão social uses the 16px/600 truncate-1-line treatment.
  </acceptance_criteria>
  <done>Reusable compact card with its complete forward-looking prop contract. Commit: `feat(02-03): compact ClienteCard with full prop contract`.</done>
</task>

<task type="auto">
  <name>Task 2: KanbanBoard (7 fixed columns) wired into /clientes</name>
  <files>components/clientes/KanbanBoard.tsx, app/(app)/clientes/page.tsx</files>
  <read_first>
    - lib/funil/etapas.ts (ETAPAS drives the columns — never hardcode stage labels in the board)
    - .planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-UI-SPEC.md (Spacing exceptions: column min-width 280px, 16px gap between columns, 8px gap between cards, board scrolls horizontally below 7×280px; column header = etapa name Heading 20px/600 + neutral/outline count badge on a --secondary panel)
    - app/(app)/clientes/page.tsx (replace the simple grouped list from 02-02 with the board)
  </read_first>
  <action>
    Build components/clientes/KanbanBoard.tsx: map over ETAPAS to render 7 columns in order, each with min-width 280px, `md` (16px) inter-column gap, a --secondary column header showing the stage label (Heading 20px/600) plus a neutral/outline count badge, and the column's ClienteCards stacked with `sm` (8px) gaps. The board is horizontally scrollable as a set. Per-column empty state: "Nenhum cliente nesta etapa". Accept the grouped-by-etapa data and callerRole as props; pass showResponsavel = (callerRole === 'supervisor') into each ClienteCard. Update app/(app)/clientes/page.tsx to render KanbanBoard instead of the interim grouped list, keeping the heading + "Novo cliente" trigger toolbar; keep the whole-board empty state ("Nenhum cliente cadastrado ainda...") when there are zero clients across all stages.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build 2>&1 | grep -Eq "(Compiled successfully|✓ Compiled|Route)" && echo BUILD-OK</automated>
    <human-check>Open /clientes: 7 columns appear in funnel order with correct Portuguese labels; a newly created client sits in "Aguardando contato"; empty columns read "Nenhum cliente nesta etapa"; on a narrow window the board scrolls horizontally. As Supervisor the card shows the responsável name; as Vendedor it does not. (End-of-phase batch.)</human-check>
  </verify>
  <acceptance_criteria>
    - KanbanBoard derives its columns from ETAPAS (source assertion — no inline array of 7 stage strings in the component).
    - Build/lint/typecheck pass.
    - Column count badge and per-column empty-state copy present (source assertion).
  </acceptance_criteria>
  <done>The 7-column compact kanban is the /clientes view; clients render as cards in their stage. Commit: `feat(02-03): 7-column kanban board render on /clientes`.</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- **Component** `components/clientes/ClienteCard.tsx` — compact presentational card, full forward prop contract
- **Component** `components/clientes/KanbanBoard.tsx` — 7 fixed columns from `ETAPAS`
- **Query extension** `getClientesAgrupadosPorEtapa()` now joins categoria + responsável names
- **Page** `/clientes` renders the kanban board
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Server Component → PostgREST | The board renders whatever RLS returns; a Vendedor board can only ever contain their own cards. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-11 | Information Disclosure | responsável name on card | medium | mitigate | Card renders the responsável line only when callerRole==='supervisor'; the underlying rows are already RLS-scoped, so a Vendedor never receives another vendedor's card to render (CLI-04). |
| T-02-12 | Information Disclosure | joined categoria/responsável names in the query | low | accept | Names come from profiles/categorias which all authenticated users may read (0001 SELECT policy + lookup SELECT policy); no sensitive data exposed. |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint`, `npm run build` green.
- Manual (end-of-phase batch): board structure, stage placement, empty states, Supervisor-vs-Vendedor responsável line.
</verification>

<success_criteria>
- FUN-01 visible: every client is a card in one of the 7 fixed stages, laid out as the primary screen (D-05).
- ClienteCard's prop contract is complete enough that 02-04/02-05/02-06 add behavior by passing props, not by rewriting it.
</success_criteria>

<output>
Create `.planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-03-SUMMARY.md` when done.
</output>
