---
phase: 02-cadastro-e-gest-o-de-clientes-pj
plan: 07
type: execute
wave: 7
depends_on: [02-06, 02-01]
files_modified:
  - components/clientes/ClienteDetailSheet.tsx
  - components/clientes/PerdaMotivoDialog.tsx
  - components/clientes/HistoricoTimeline.tsx
  - app/actions/funil.ts
  - app/actions/tarefas.ts
  - lib/validations/cliente.ts
  - lib/supabase/queries/clientes.ts
  - components/ui/textarea.tsx
  - components/ui/calendar.tsx
  - tests/clientes/funil-status.test.ts
  - package.json
autonomous: true
requirements: [FUN-04, FUN-05, FUN-06, FUN-07, FUN-08, FUN-10]

must_haves:
  truths:
    - "The detail Sheet's 'Funil' section lets a user set the card status (em andamento / perdido / ganho); 'Ganho' is disabled with an explanatory tooltip unless the card is in '1ª venda concluída' (FUN-04/FUN-05)."
    - "Choosing 'Perdido' opens a modal that requires a motivo da perda before it can be confirmed; you cannot save a perdido card with no motivo (FUN-06)."
    - "Each card has a free-text observação (FUN-07) and a task checklist where every task has a tipo and its own data de conclusão, addable/checkable/removable (FUN-08)."
    - "Each client shows a read-only histórico timeline of stage/status changes and completed tasks with date & time, generated automatically with no manual entry (FUN-10)."
  artifacts:
    - components/clientes/PerdaMotivoDialog.tsx
    - components/clientes/HistoricoTimeline.tsx
    - app/actions/tarefas.ts
  key_links:
    - "Status changes go through the mover_card_funil RPC (02-01), so the ganho-only-final and perdido-requires-motivo rules are enforced server-side; the disabled 'Ganho' option and the required-motivo modal are UI courtesies over that DB guarantee (FUN-05/FUN-06)."
    - "The histórico timeline is a pure read of the historico table, which is populated automatically by the 02-01 triggers on stage/status change and task completion — no client writes to historico (FUN-10)."
    - "The Funil section fills the placeholder region left in ClienteDetailSheet by 02-06 without restructuring the Dados section."
---

<objective>
Complete the client detail Sheet with its "Funil" section: the status control (with the disabled-Ganho courtesy and the required-motivo perda modal), the free-text observação, the task checklist with per-task due dates, and the read-only automatic histórico timeline. This closes out every remaining FUN requirement and finishes the phase's vertical slice.

Purpose: FUN-04 (status), FUN-05 (ganho only in final stage), FUN-06 (motivo required on perda), FUN-07 (observação), FUN-08 (tarefas with data_conclusao), FUN-10 (automatic histórico).
Output: The Funil section inside `ClienteDetailSheet`, `PerdaMotivoDialog`, `HistoricoTimeline`, and the `tarefas`/status Server Actions.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-UI-SPEC.md
@.planning/research/PITFALLS.md
@.claude/skills/Supabase-conventions/SKILL.md
@components/clientes/ClienteDetailSheet.tsx
@app/actions/funil.ts
@lib/funil/etapas.ts
@lib/supabase/queries/clientes.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Status + tarefas Server Actions and their history side-effects (+ tests)</name>
  <files>app/actions/funil.ts, app/actions/tarefas.ts, lib/validations/cliente.ts, lib/supabase/queries/clientes.ts, tests/clientes/funil-status.test.ts</files>
  <read_first>
    - app/actions/funil.ts (extend the existing file from 02-04 — reuse the moverCard/RPC pattern)
    - .planning/research/ARCHITECTURE.md (mover_card_funil signature: it takes p_nova_etapa, p_novo_status, p_motivo_perda_id — for a status-only change pass the card's CURRENT etapa)
    - supabase/migrations/0002_clientes_and_funil.sql (the historico triggers already fire on status change and task completion — actions must NOT write historico themselves, FUN-10)
    - tests/clientes/rls-clientes.test.ts (SEED_ACCOUNTS + signInAs reuse)
  </read_first>
  <behavior>
    - marcarStatus(clienteId, novoStatus, motivoPerdaId?): calls mover_card_funil with the card's current etapa + novoStatus (+ motivo). Returns { error: { code: 'ganho_travado' } } when novoStatus='ganho' and etapa≠'primeira_venda'; { error: { code: 'motivo_obrigatorio' } } when novoStatus='perdido' and no motivo; { data } otherwise. A perdido-with-motivo change results in exactly one historico row (written by the trigger, not the action).
    - adicionarTarefa(clienteId, tipoTarefaId, dataConclusao?), toggleTarefa(tarefaId, concluida), removerTarefa(tarefaId): all RLS-gated via the parent client (a Vendedor cannot touch tasks on a non-owned client). Completing a task (false->true) produces exactly one historico row (trigger).
    - salvarObservacao is folded into updateCliente by extending updateClienteSchema with an optional observacao (FUN-07).
  </behavior>
  <action>
    Extend app/actions/funil.ts with marcarStatus per <behavior> (fetch current etapa first, then call mover_card_funil so the RPC's validation + the 02-01 CHECK constraints enforce FUN-05/FUN-06). Create app/actions/tarefas.ts with adicionarTarefa/toggleTarefa/removerTarefa calling supabase table ops (RLS on tarefas enforces access); revalidatePath('/clientes') after mutations. Add optional observacao to updateClienteSchema in lib/validations/cliente.ts and persist it in updateCliente (already exists from 02-06). Add getTarefas(clienteId) and getHistorico(clienteId) to lib/supabase/queries/clientes.ts (RLS-scoped, ordered — histórico newest first). Write tests/clientes/funil-status.test.ts using SEED_ACCOUNTS: marcarStatus ganho from a non-final stage -> error; perdido without motivo -> error; perdido with motivo -> success AND a new historico row exists for that client; toggleTarefa completing a task -> a new historico row exists. Clean up via serviceClient.
  </action>
  <verify>
    <automated>npx vitest run tests/clientes/funil-status.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - marcarStatus routes through mover_card_funil (source assertion — no raw status UPDATE in the action) so the DB rules apply.
    - Perdido-with-motivo and task-completion each create exactly one historico row via the trigger, not an explicit action insert (behavior assertion + source assertion that no action inserts into historico).
    - tarefas actions are RLS-gated (behavior assertion: a Vendedor cannot toggle a task on a non-owned client).
  </acceptance_criteria>
  <done>Status + tarefas data layer complete; automatic history proven. Commit: `feat(02-07): status/tarefas actions + auto-historico, tested`.</done>
</task>

<task type="auto">
  <name>Task 2: Funil section UI — status + observação + tarefas + histórico + perda modal</name>
  <files>components/clientes/ClienteDetailSheet.tsx, components/clientes/PerdaMotivoDialog.tsx, components/clientes/HistoricoTimeline.tsx, components/ui/textarea.tsx, components/ui/calendar.tsx, package.json</files>
  <read_first>
    - .planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-UI-SPEC.md (Section 2 "Funil": Status Select with green ganho / red perdido / neutral em-andamento badges; ganho disabled + tooltip "Disponível somente na etapa \"1ª venda concluída\"." unless final stage; perdido opens PerdaMotivoDialog; Observação Textarea; Tarefas checklist = Checkbox [blue checked] + tipo + Calendar-popover data_conclusao [red text if past & unchecked] + ghost delete + "+ Adicionar tarefa" outline; Histórico read-only timeline, plain, newest entries, timestamp + plain-language description; and the perda modal copy "Marcar {razão social} como perdido?" + required "Motivo da perda" select + "Confirmar perda" destructive; success toasts)
    - components/clientes/ClienteDetailSheet.tsx (fill the Funil placeholder region left by 02-06; do not touch the Dados section beyond adding the observação field if it belongs there)
    - components/clientes/PerdaMotivoDialog analog: components/auth/InviteUserForm.tsx (small form-in-dialog + Select controlled-from-mount pattern)
    - lib/funil/etapas.ts (ETAPA_FINAL to decide whether Ganho is enabled)
  </read_first>
  <action>
    Install primitives: `npx shadcn@latest add textarea calendar` (official registry; checkbox already installed in 02-05). Fill the Funil section (Heading 20px/600 + Separator) inside ClienteDetailSheet: a Status control showing em-andamento (neutral) / perdido (red) / ganho (green) — render the Ganho option disabled with the Tooltip copy "Disponível somente na etapa \"1ª venda concluída\"." unless the card's etapa === ETAPA_FINAL (FUN-05 courtesy over the DB guarantee); selecting Perdido opens components/clientes/PerdaMotivoDialog.tsx (a modal listing motivos_perda; "Confirmar perda" is disabled until a motivo is chosen — copy "Selecione o motivo da perda antes de salvar." on attempted early confirm; title "Marcar {razão social} como perdido?"; "Confirmar perda" destructive) which calls marcarStatus(clienteId,'perdido',motivoId); selecting em-andamento/ganho calls marcarStatus directly. Add the Observação Textarea (FUN-07) saved via the Sheet's existing "Salvar alterações" (updateCliente now persists observacao). Build the Tarefas checklist (FUN-08): each existing tarefa row = Checkbox (blue checked, toggleTarefa) + tipo label + a Calendar-in-Popover date picker bound to data_conclusao (render the date in red 14px when it is past and the task is unchecked) + a ghost delete icon (removerTarefa); a "+ Adicionar tarefa" outline button (never filled blue) appends a new tarefa (tipo Select from tipos_tarefa + date) via adicionarTarefa. Build components/clientes/HistoricoTimeline.tsx: a read-only vertical timeline from getHistorico — each entry = timestamp (14px/400 muted) + plain-language descricao (14px/400), no color/icons, newest first (FUN-10). Show success toasts "Card movido para \"{etapa}\"." is not this plan's; use the status/save success copy from the Copywriting Contract.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build 2>&1 | grep -Eq "(Compiled successfully|✓ Compiled|Route)" && echo BUILD-OK</automated>
    <human-check>Open a card not in the final stage — "Ganho" is disabled and its tooltip explains why; move the card to "1ª venda concluída" (drag) then reopen — "Ganho" is now selectable. Choose "Perdido" — the modal blocks confirming until a motivo is picked. Type an observação and Salvar — it persists on reopen. Add a tarefa with a past due date left unchecked — its date shows red; check it — it strikes/greys and a new histórico entry appears. The histórico lists stage/status/task events with date & time, newest first. (End-of-phase batch.)</human-check>
  </verify>
  <acceptance_criteria>
    - The Ganho option is disabled unless etapa === ETAPA_FINAL, with the tooltip copy (source assertion, FUN-05).
    - PerdaMotivoDialog's confirm is disabled until a motivo is selected (source assertion, FUN-06).
    - Observação persists via updateCliente; tarefas render tipo + data_conclusao with overdue red styling; HistoricoTimeline is read-only (no write controls) — source assertions.
    - Build/lint/typecheck pass.
  </acceptance_criteria>
  <done>The Funil section completes the detail Sheet; all FUN requirements are user-visible. Commit: `feat(02-07): funil section — status, observação, tarefas, histórico + perda modal`.</done>
</task>

</tasks>

<artifacts>
## Artifacts this plan produces
- **Component** Funil section added to `components/clientes/ClienteDetailSheet.tsx`
- **Component** `components/clientes/PerdaMotivoDialog.tsx` (required-motivo perda modal, FUN-06)
- **Component** `components/clientes/HistoricoTimeline.tsx` (read-only automatic timeline, FUN-10)
- **Server Action** `marcarStatus(clienteId, novoStatus, motivoPerdaId?)` in `app/actions/funil.ts`
- **Server Actions** `adicionarTarefa`, `toggleTarefa`, `removerTarefa` in `app/actions/tarefas.ts`
- **Validation** `observacao` added to `updateClienteSchema`
- **Queries** `getTarefas(clienteId)`, `getHistorico(clienteId)`
- **shadcn primitives** `textarea`, `calendar`
- **Test** `tests/clientes/funil-status.test.ts`
</artifacts>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser status/tarefa controls → funil/tarefas Server Actions → RPC / RLS-gated tables | Status and task writes cross into the server; the funil business rules and the parent-client ownership must be enforced server-side, not by the disabled option / modal alone. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-22 | Tampering | status='ganho' from a non-final stage | high | mitigate | marcarStatus routes through mover_card_funil + the 02-01 CHECK constraint; the disabled UI option is courtesy only (FUN-05), proven un-bypassable by the 02-01 funil-constraints test. |
| T-02-23 | Tampering | status='perdido' with no motivo | high | mitigate | RPC + CHECK constraint reject it; the modal is a UX prompt, not the boundary (FUN-06). |
| T-02-24 | Elevation of Privilege | tarefas on a non-owned client | medium | mitigate | tarefas RLS (parent-client EXISTS gate, 02-01); a Vendedor toggling another vendedor's task is a no-op (tested). |
| T-02-25 | Tampering | client writing to historico directly | low | mitigate | No user INSERT policy on historico; only the SECURITY DEFINER triggers write it (FUN-10 integrity). |
</threat_model>

<verification>
- `npx vitest run tests/clientes/`, `npx tsc --noEmit`, `npm run lint`, `npm run build` green.
- Manual (end-of-phase batch): ganho-disabled/enabled, perda-modal-requires-motivo, observação persistence, tarefa overdue styling + completion, automatic histórico.
</verification>

<success_criteria>
- FUN-04/05/06/07/08/10 all user-visible and DB-enforced; the phase's vertical slice (cadastro → board → move → find → edit → funil detail) is complete end-to-end.
</success_criteria>

<output>
Create `.planning/phases/02-cadastro-e-gest-o-de-clientes-pj/02-07-SUMMARY.md` when done.
</output>
