---
phase: 17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/supabase/queries/clientes.ts
  - lib/clientes/exportacaoDiario.ts
  - app/api/agenda/exportar-diario/route.ts
  - components/agenda/AgendaList.tsx
  - tests/clientes/exportacao-diario.test.ts
  - tests/clientes/rls-exportacao-diario.test.ts
autonomous: false
requirements: [IMP-02]

must_haves:
  truths:
    - "IMP-02 (critério de sucesso 5): Vendedor e Supervisor baixam, da tela de Agenda, uma planilha com o histórico de visitas e tarefas já concluídas"
    - "IMP-02 (visibilidade): o Vendedor recebe só as conclusões dos próprios clientes e o Supervisor recebe as de todo o time — e isso sai inteiramente da regra de leitura da trilha de auditoria que já existe desde a migration 0002, sem nenhuma regra nova e sem nenhuma checagem de papel escrita à mão"
    - "D3: o botão exporta SEMPRE tudo o que o chamador pode ver, ignorando o filtro de vendedor ativo na tela — e isso é estrutural, não disciplina: a rota de download não aceita parâmetro nenhum"
    - "Todo texto que vai para a planilha passa pelo saneador de célula que já existe desde a Fase 5 — inclusive os resumos de texto livre escritos pelos vendedores, que é exatamente o risco de injeção de fórmula registrado na research do marco e transferido do plano 16-03"
    - "O saneador NÃO é reimplementado: é importado do módulo de exportação de clientes, que é o único caminho compartilhado do projeto para isso"
    - "A planilha traz seis colunas, em ordem fixa, com o tipo de conclusão traduzido para os mesmos dois rótulos que a tela já usa e a data no mesmo formato que a tela já usa"
    - "Um diário vazio baixa uma planilha só com a linha de cabeçalho — nunca um erro"
    - "A leitura é estreita (seis campos) e o filtro por tipo acontece no SQL, nunca no navegador — mesma disciplina do leitor do diário da Fase 16 e do leitor de exportação de clientes da Fase 5"
    - "Nada do fluxo de importação de clientes nem do botão de exportar clientes do kanban é tocado por este plano"
  artifacts:
    - "getDiarioParaExportacao() e o tipo de linha correspondente, acrescentados a lib/supabase/queries/clientes.ts ao lado dos leitores de diário e de exportação que já vivem lá"
    - "lib/clientes/exportacaoDiario.ts (novo) — gerador de planilha puro, irmão de lib/clientes/exportacao.ts"
    - "app/api/agenda/exportar-diario/route.ts (novo) — rota de download, irmã de app/api/clientes/exportar/route.ts"
    - "botão 'Exportar meu diário' / 'Exportar diário do time' no cabeçalho de components/agenda/AgendaList.tsx"
    - "tests/clientes/exportacao-diario.test.ts (novo, puro) e tests/clientes/rls-exportacao-diario.test.ts (novo, integração)"
  key_links:
    - "a regra de leitura da trilha de auditoria condicionada ao cliente pai (migration 0002) é a ÚNICA fronteira do critério de sucesso 5 — qualquer filtro de dono ou checagem de papel escrito dentro do leitor faria a autorização deixar de ser única e passar a poder divergir da tela"
    - "o saneador de célula importado do módulo de exportação de clientes é o único ponto que fecha a injeção de fórmula; reimplementá-lo aqui criaria dois saneadores que podem divergir, que é literalmente o que o comentário daquele módulo proíbe"
    - "a rota de download não aceitar parâmetro é o que torna D3 verdadeiro por construção: sem parâmetro, não existe caminho pelo qual o filtro da tela possa vazar para o servidor nem pelo qual um chamador possa tentar ampliar o escopo"
    - "o botão precisa saber se quem está vendo é Supervisor só para escolher o RÓTULO; o escopo do que baixa nunca vem desse valor — vem do RLS. Se alguém trocar o rótulo por um filtro, a fronteira de autorização migra para o navegador"
    - "o índice composto de tipo e data da trilha de auditoria já existe desde a migration 0003 e é o que mantém esta leitura barata — não é preciso índice novo"
---

<objective>
Entregar o IMP-02 inteiro: o leitor do diário para exportação, o gerador da planilha, a rota de download e o botão na tela de Agenda — com a mesma regra de visibilidade da exportação de clientes e com o texto livre dos resumos sanitizado contra injeção de fórmula.

Purpose: é a metade independente da Fase 17. Não depende de nada do fluxo de planilha (IMP-01), não compartilha nenhum arquivo com ele, e por isso roda em paralelo com a migration do plano 17-01. É também o fechamento de um risco que veio sendo empurrado desde a Fase 16: os resumos que o vendedor escreve ao concluir uma visita são texto livre e vão para uma planilha, exatamente o cenário de injeção de fórmula que a v1.1 já tratou na exportação de clientes.

Output: um botão funcional na Agenda que baixa a planilha do diário, com dois arquivos de teste novos verdes.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-UI-SPEC.md
</context>

<source_audit>
O audit completo de cobertura das quatro fontes (GOAL / REQ / RESEARCH / CONTEXT) para o conjunto inteiro de planos da Fase 17 está em `17-01-PLAN.md`, seção `<source_audit>`. Este plano cobre integralmente o item **G5** (critério de sucesso 5), o requisito **IMP-02**, os itens de RESEARCH "reaproveita o caminho de exportação da Fase 5" e "injeção de fórmula em planilha nos resumos de texto livre", e as decisões **D2** (botão na Agenda) e **D3** (exportação ignora o filtro de vendedor) do contrato de interface.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Leitor do diário para exportação e gerador da planilha</name>
  <files>lib/supabase/queries/clientes.ts, lib/clientes/exportacaoDiario.ts, tests/clientes/exportacao-diario.test.ts, tests/clientes/rls-exportacao-diario.test.ts</files>
  <read_first>
    - lib/supabase/queries/clientes.ts — três funções, nesta ordem de importância: (1) `getClientesParaExportacao` (a partir da linha ~370), em especial o comentário que registra que a regra de linha é a fronteira INTEIRA e que não existe filtro de dono nem checagem de papel ali; a forma de tipo público + tipo interno de linha crua; e a forma de seleção estreita com relações embutidas. (2) `getDiario` (a partir da linha ~536) — o filtro por tipo feito no SQL, a ordenação decrescente, a resolução do nome do autor a partir do perfil com nulo quando não houver, e o retorno de lista vazia em erro. (3) `getHistorico`, só para confirmar que este arquivo nunca tem função de escrita na trilha de auditoria.
    - lib/clientes/exportacao.ts — o arquivo INTEIRO (82 linhas). Extrair: o comentário que declara o módulo puro e por quê; a função de saneamento de célula e o comentário que a declara "o único caminho compartilhado, nunca reimplementada por coluna"; o embrulho que converte campo anulável em texto sanitizado; a forma do gerador de planilha (objeto por linha com as chaves na ordem das colunas, conversão para folha, criação de pasta, escrita em formato de memória do Node); e o mapa de rótulos de status como precedente de tradução de valor de banco para rótulo de planilha.
    - supabase/migrations/0002_clientes_and_funil.sql — a definição da trilha de auditoria (linhas ~159-166) e a ÚNICA regra de acesso que ela tem (leitura condicionada ao cliente pai, linhas ~338-346), mais a ausência deliberada de regra de escrita. É essa regra que entrega o critério de sucesso 5 de graça.
    - supabase/migrations/0003_dashboard_aggregates.sql, linha ~208 — o índice composto de tipo e data da trilha de auditoria, que já existe e serve exatamente a esta consulta (filtro por tipo, ordenação por data). Nenhum índice novo é necessário.
    - supabase/migrations/0015_conclusao_com_resumo.sql — os dois gatilhos que gravam as entradas de tarefa concluída e de visita concluída, incluindo o texto genérico gravado quando não há resumo. São exatamente esses dois tipos que a exportação leva.
    - components/clientes/DiarioTimeline.tsx — os dois rótulos de tipo já usados na tela, o formato de data já usado, e o texto de substituição quando o autor não resolve. A planilha precisa usar exatamente os mesmos três, sem inventar variação.
    - tests/clientes/exportacao.test.ts — o molde do teste puro do gerador de planilha (como monta linhas de entrada, como lê a folha de volta para asserção, e como o caso de injeção de fórmula é escrito).
    - tests/clientes/rls-exportacao.test.ts — o molde do teste de integração do leitor: sessões reais, semeadura pela chave de serviço, asserção de que o Vendedor não vê o do outro.
    - tests/clientes/diario.test.ts — como a trilha de auditoria é semeada (só pela chave de serviço, porque não existe regra de escrita para usuários) e como os carimbos de data são controlados para provar ordenação.
    - .planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-UI-SPEC.md (já no contexto) — seção "IMP-02 — new/modified files", pontos 1 e 2: a forma do tipo de linha, a consulta com relações embutidas, e a exigência de rotear TODA coluna pelo embrulho compartilhado.
    - .planning/STATE.md — Blockers/Concerns: o limite de tentativas de login do Supabase Auth (logar cada identidade uma vez por arquivo).
  </read_first>
  <action>
    (A) **`lib/supabase/queries/clientes.ts` — leitor novo.** Acrescentar, imediatamente depois de `getDiario`, o tipo público de linha de exportação do diário e a função de leitura `getDiarioParaExportacao()`.

    Tipo de linha, com seis campos: razão social do cliente; nome do responsável pelo cliente (texto anulável); tipo da entrada, restrito por união literal aos dois valores de conclusão; a descrição (o resumo); o carimbo de criação; e o nome do autor (texto anulável).

    Função de leitura, **sem parâmetro nenhum** (D3 — não existe escopo a estreitar). Regras obrigatórias:
      - seleção estreita das colunas necessárias, com duas relações embutidas: a do cliente (trazendo razão social e, aninhado, o perfil do responsável) e a do autor da entrada. Como a trilha de auditoria e a tabela de clientes têm, cada uma, uma ligação com a tabela de perfis, a do autor precisa vir **apelidada** para não colidir com a aninhada. Se o PostgREST recusar por ambiguidade de relacionamento no teste de integração, usar a forma explícita com dica do nome da restrição de chave estrangeira (`historico_autor_id_fkey` para o autor e `clientes_responsavel_fkey` para o responsável) em vez de mudar o desenho — mesma postura do plano 16-03;
      - **filtro por tipo dentro do SQL**, restringindo aos dois tipos de conclusão — nunca trazer tudo e filtrar depois;
      - ordenação decrescente pelo carimbo de criação;
      - erro lança exceção com mensagem em português (mesma postura de `getClientesParaExportacao`, que é chamada de dentro de uma rota que já trata exceção — diferente de `getDiario`, que devolve lista vazia porque alimenta uma seção de ficha que não pode derrubar a tela);
      - nome do responsável e nome do autor montados a partir do perfil quando ele existir, e **nulos** quando não existir — a camada de dados nunca inventa texto de substituição;
      - **zero checagem de papel e zero filtro de dono.** Registrar isso em comentário, no mesmo tom do comentário equivalente de `getClientesParaExportacao`, apontando que a regra de leitura da trilha de auditoria condicionada ao cliente pai é a fronteira inteira do critério de sucesso 5.

    Comentário de documentação obrigatório registrando também: que esta leitura é irmã de `getDiario` mas de escopo diferente (aquela é de um cliente só, para a ficha; esta é de tudo o que o chamador enxerga, para download); e que ela não aceita parâmetro **de propósito**, porque o filtro de vendedor da tela de Agenda não deve atravessar para a exportação (D3).

    (B) **`lib/clientes/exportacaoDiario.ts` (novo).** Módulo puro irmão de `lib/clientes/exportacao.ts`: sem cliente de banco, sem diretiva de cliente/servidor, importável tanto de rota quanto de teste. Importa o saneador de célula **do módulo de exportação de clientes** — nunca reimplementa, nunca copia o corpo dele.

    Expõe um gerador de planilha que recebe a lista de linhas do leitor e devolve a planilha em formato de memória do Node, na mesma forma do gerador de clientes. Detalhes obrigatórios:
      - **seis colunas, em ordem fixa**, com os cabeçalhos exatos do contrato de copy: Cliente, Responsável, Tipo, Resumo, Data, Autor;
      - a coluna de tipo traduz os dois valores de banco para os dois rótulos já usados na tela, por um mapa constante no topo do arquivo (precedente do mapa de rótulos de status do gerador de clientes) — nunca comparação de texto solta no meio da montagem;
      - a coluna de data é **pré-formatada como texto** no mesmo formato que a tela já usa, com a biblioteca de datas do projeto, e a leitura do carimbo precisa usar a função de análise da biblioteca, nunca o construtor nativo de data a partir de texto (o caso de fuso já registrado nos Pitfalls do marco);
      - **toda coluna**, sem exceção — inclusive a de data e as duas de nome — passa pelo mesmo embrulho compartilhado que rota para o saneador. Escrever o comentário que registra o porquê no mesmo tom do módulo irmão: rotear tudo uniformemente é o que impede que uma coluna futura reabra o buraco em silêncio;
      - campo anulável vira texto vazio, mesma convenção do embrulho do módulo irmão;
      - nome da pasta da planilha conforme o contrato de copy.

    (C) **`tests/clientes/exportacao-diario.test.ts` (novo, puro — sem banco, sem login).** No molde de `tests/clientes/exportacao.test.ts`. Casos exigidos, com estas palavras-chave nos nomes (sem acento):
      - **`colunas`**: as seis colunas aparecem na ordem exata do contrato, com os cabeçalhos exatos.
      - **`tipo`**: os dois valores de banco viram os dois rótulos da tela; nenhum valor cru vaza para a planilha.
      - **`data`**: o carimbo vira texto no formato acordado (montar a entrada com um carimbo conhecido e afirmar o texto exato).
      - **`injecao`**: um resumo começando por cada um dos caracteres perigosos sai da planilha com o prefixo de escape aplicado. Este caso é o fechamento do risco herdado do plano 16-03 e não pode ser omitido.
      - **`injecaonome`**: uma razão social começando por caractere perigoso também sai escapada — prova que o saneamento não está aplicado só na coluna de resumo.
      - **`nulo`**: responsável e autor nulos viram célula vazia, nunca a palavra "null" nem texto inventado.
      - **`vazio`**: lista vazia gera uma planilha válida só com a linha de cabeçalho, sem lançar.

    (D) **`tests/clientes/rls-exportacao-diario.test.ts` (novo, integração contra o banco real).** No molde de `tests/clientes/rls-exportacao.test.ts` + `tests/clientes/diario.test.ts`. Semear pela chave de serviço (único caminho para inserir na trilha de auditoria) um cliente do Vendedor A com entradas de conclusão E entradas de mudança de etapa/status, e um cliente do Vendedor B também com entradas de conclusão. Limpar por identificador no fim.

    Casos exigidos, com estas palavras-chave nos nomes (sem acento):
      - **`vendedor`**: o Vendedor A recebe as conclusões do próprio cliente e **nenhuma** do cliente do Vendedor B. Primeira metade do critério de sucesso 5.
      - **`supervisor`**: o Supervisor recebe as conclusões dos dois clientes. Segunda metade.
      - **`filtro`**: as entradas de mudança de etapa e de status NÃO aparecem no resultado — só os dois tipos de conclusão.
      - **`ordem`**: as entradas vêm da mais recente para a mais antiga, comparando os carimbos semeados.
      - **`campos`**: uma entrada semeada traz razão social, nome do responsável, resumo e nome do autor corretos.
      - **`anonimo`**: um chamador não autenticado não recebe entrada nenhuma.

    Nenhum outro arquivo é criado ou alterado por esta tarefa.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx vitest run tests/clientes/exportacao-diario.test.ts && npx vitest run tests/clientes/rls-exportacao-diario.test.ts && node -e "const fs=require('fs');const q=fs.readFileSync('lib/supabase/queries/clientes.ts','utf8');const i=q.indexOf('export async function getDiarioParaExportacao');if(i<0)throw new Error('falta getDiarioParaExportacao');const d=q.slice(i,i+2600);if(/getDiarioParaExportacao\s*\(\s*[a-zA-Z]/.test(q.slice(i,i+120)))throw new Error('o leitor nao pode aceitar parametro (D3)');if(!/from\(\s*[\"']historico[\"']\s*\)/.test(d))throw new Error('o leitor le da trilha de auditoria');if(!/\.in\(/.test(d))throw new Error('o filtro por tipo precisa acontecer no SQL');for(const t of ['tarefa_concluida','visita_concluida'])if(!d.includes(t))throw new Error('falta o tipo '+t+' no filtro');if(!/order\(\s*[\"']criado_em[\"'][\s\S]{0,60}ascending:\s*false/.test(d))throw new Error('ordenacao decrescente por data');if(/select\(\s*[\"']\*/.test(d))throw new Error('selecao estreita, nunca aberta');if(/is_supervisor|role\s*===|eq\(\s*[\"']responsavel[\"']|eq\(\s*[\"']autor_id[\"']/.test(d))throw new Error('autorizacao sai da regra de leitura da trilha, nunca de checagem escrita a mao');if(/\.insert\(|\.update\(|\.delete\(|\.upsert\(/.test(d))throw new Error('o leitor e somente leitura');const e=fs.readFileSync('lib/clientes/exportacaoDiario.ts','utf8');if(!/from\s+[\"']@\/lib\/clientes\/exportacao[\"']/.test(e))throw new Error('o saneador precisa ser IMPORTADO do modulo de exportacao de clientes');if(/function\s+sanitizeCell/.test(e))throw new Error('o saneador nao pode ser reimplementado aqui');if(/@\/lib\/supabase|createClient|use client|use server/.test(e))throw new Error('o gerador precisa ser puro');for(const c of ['Cliente','Responsável','Tipo','Resumo','Data','Autor'])if(!e.includes(c))throw new Error('falta a coluna '+c);if(/new\s+Date\s*\(\s*[a-zA-Z_]/.test(e))throw new Error('nunca construtor nativo de data a partir de texto: usar a funcao de analise da biblioteca de datas');const corpo=e.slice(e.indexOf('sheetRows'));const linhasCel=(corpo.match(/cell\(/g)||[]).length;if(linhasCel<6)throw new Error('as SEIS colunas precisam passar pelo embrulho compartilhado, achei '+linhasCel);const p=fs.readFileSync('tests/clientes/exportacao-diario.test.ts','utf8');const np=(p.match(/\bit\(\s*[\"'][^\"']+/g)||[]).map(x=>x.toLowerCase());for(const k of ['colunas','tipo','data','injecaonome','injecao','nulo','vazio'])if(!np.some(x=>x.includes(k)))throw new Error('exportacao-diario.test.ts: falta caso com a palavra '+k);const r=fs.readFileSync('tests/clientes/rls-exportacao-diario.test.ts','utf8');const nr=(r.match(/\bit\(\s*[\"'][^\"']+/g)||[]).map(x=>x.toLowerCase());for(const k of ['vendedor','supervisor','filtro','ordem','campos','anonimo'])if(!nr.some(x=>x.includes(k)))throw new Error('rls-exportacao-diario.test.ts: falta caso com a palavra '+k);if(!r.includes('SEED_ACCOUNTS')||!r.includes('signInAs'))throw new Error('o teste de integracao precisa usar sessoes reais');if(!r.includes('serviceClient'))throw new Error('a trilha de auditoria so pode ser semeada pela chave de servico');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `getDiarioParaExportacao()` existe, **não aceita parâmetro**, lê da trilha de auditoria com seleção estreita, filtra os dois tipos de conclusão no SQL, ordena do mais recente para o mais antigo, não escreve nada e não contém nenhuma checagem de papel nem filtro de dono.
    - `lib/clientes/exportacaoDiario.ts` é puro, **importa** o saneador do módulo de exportação de clientes e não o reimplementa, e roteia as seis colunas pelo embrulho compartilhado.
    - As seis colunas têm os cabeçalhos exatos do contrato de copy, na ordem fixa; o tipo é traduzido por mapa constante; a data é pré-formatada como texto sem usar o construtor nativo a partir de texto.
    - `tests/clientes/exportacao-diario.test.ts` passa, com casos contendo `colunas`, `tipo`, `data`, `injecao`, `injecaonome`, `nulo` e `vazio`.
    - `tests/clientes/rls-exportacao-diario.test.ts` passa contra o banco real, com casos contendo `vendedor`, `supervisor`, `filtro`, `ordem`, `campos` e `anonimo`.
    - `npx tsc --noEmit` limpo e `npm run lint` limpo.
  </acceptance_criteria>
  <done>O diário sai do banco já filtrado e ordenado, e vira uma planilha de seis colunas com todo texto livre sanitizado.</done>
</task>

<task type="auto">
  <name>Task 2: Rota de download e botão "Exportar diário" na Agenda</name>
  <files>app/api/agenda/exportar-diario/route.ts, components/agenda/AgendaList.tsx</files>
  <read_first>
    - app/api/clientes/exportar/route.ts — o arquivo INTEIRO (78 linhas). Extrair: o comentário que registra por que uma rota existe neste projeto (cabeçalhos de download que uma ação de servidor não consegue definir) e por que ela precisa declarar tempo de execução do Node; a re-checagem de sessão devolvendo 401 (a rota fica fora da guarda do grupo de rotas); a forma da resposta com os dois cabeçalhos de tipo e de anexo; e o tratamento de exceção devolvendo 500 com a mensagem.
    - components/clientes/KanbanBoard.tsx, linhas ~352-400 e ~570-593 — o MOLDE LITERAL do botão e do manipulador: envio por requisição, tratamento de resposta não-ok, conversão para objeto binário, criação de endereço temporário, âncora sintética com atributo de download, clique programático, remoção da âncora e liberação do endereço; mais a forma do botão (variante de contorno, ícone, rótulo condicional ao papel, rótulo em voo).
    - components/agenda/AgendaList.tsx — o arquivo INTEIRO. Em especial: a lista de propriedades recebidas (o indicador de Supervisor já chega pronto), a estrutura do JSX devolvido (o primeiro elemento hoje é o cabeçalho com título e filtro), a forma dos pares de estado já existentes, e o efeito de carga com guarda de cancelamento. O botão novo entra ANTES do cabeçalho atual.
    - components/importacao/ImportWizard.tsx, linhas ~297-304 — a string de classes exata da faixa de erro em linha usada em todo o projeto. A faixa de erro da exportação reusa essa string verbatim.
    - .planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-UI-SPEC.md (já no contexto) — seção "IMP-02 — new/modified files", pontos 3 e 4 (rota e componente), a tabela de copy do IMP-02 (rótulos do botão nos dois papéis, rótulo em voo, texto da faixa de erro, nome do arquivo baixado), e a decisão D3.
  </read_first>
  <action>
    (A) **`app/api/agenda/exportar-diario/route.ts` (novo).** Cópia estrutural da rota de exportação de clientes, com quatro diferenças e nenhuma a mais:
      - declara tempo de execução do Node pelo mesmo motivo (a biblioteca de planilha precisa das APIs de memória do Node) — comentar isso;
      - re-checa a sessão e devolve 401 quando não houver, pelo mesmo motivo (a rota fica fora da guarda do grupo de rotas);
      - **não lê o corpo da requisição, não valida corpo nenhum e não aceita parâmetro nenhum.** Esta é a implementação literal de D3, e precisa vir com comentário explicando: como a rota não tem parâmetro, não existe caminho pelo qual o filtro de vendedor da tela possa atravessar para o servidor nem pelo qual um chamador possa tentar ampliar escopo. O escopo é sempre "tudo o que a regra de leitura deixa este usuário ver";
      - chama o leitor e o gerador do Task 1 e devolve a planilha com os dois cabeçalhos, com o nome de arquivo do contrato de copy (prefixo do diário + a data de hoje).

      Manter o verbo POST (mesmo do irmão) — a tela chama por requisição, não por navegação. **Zero checagem de papel dentro da rota**, exatamente como a rota irmã: comentar que qualquer ramo de papel aqui moveria a fronteira de autorização para fora da regra de linha.

    (B) **`components/agenda/AgendaList.tsx`.** Três acréscimos, e nada mais no arquivo muda:
      - **dois estados novos**: um sinalizador de exportação em voo e um texto de erro anulável, na mesma forma dos pares de estado que já existem no componente;
      - **um manipulador de exportação**, cópia estrutural do manipulador do kanban, com uma simplificação: **não existe etapa de coleta de identificadores** e o envio não leva corpo (D3). Em resposta não-ok ou exceção, gravar no estado o texto de erro do contrato de copy; em sucesso, converter para objeto binário, criar endereço temporário, disparar a âncora sintética com o nome de arquivo do contrato, remover a âncora e liberar o endereço. Sinalizador em voo desligado no bloco final, sempre;
      - **um bloco novo no topo do JSX devolvido**, antes do cabeçalho de título/filtro que já existe: a linha alinhada à direita com o botão (variante de contorno, ícone de download, desabilitado enquanto em voo) e, logo abaixo, a faixa de erro renderizada só quando houver erro, com a string de classes de erro já padronizada no projeto e o papel de alerta.

      Rótulo do botão: em voo mostra o rótulo em voo do contrato; fora de voo, escolhe entre o rótulo de Supervisor e o de Vendedor **apenas** pelo indicador que o componente já recebe como propriedade. Comentário obrigatório de uma linha registrando que esse indicador escolhe só o RÓTULO — o escopo do que baixa vem sempre da regra de leitura no servidor, nunca deste valor.

      Sem estado de desabilitado por contagem (diferente do kanban, que sabe quantos itens estão na tela): a Agenda não carrega contagem de diário, e buscar uma só para desabilitar um botão está fora de escopo. Diário vazio baixa uma planilha só com cabeçalho, que é um resultado autoexplicativo. Comentar isso.

      O filtro de vendedor da tela **não** é lido pelo manipulador. Nenhuma linha do efeito de carga, do agrupamento, das seções, da ficha do cliente ou da janela de conclusão muda.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx next build --no-lint 2>/dev/null || npx tsc --noEmit; node -e "const fs=require('fs');const r=fs.readFileSync('app/api/agenda/exportar-diario/route.ts','utf8');if(!/export\s+const\s+runtime\s*=\s*[\"']nodejs[\"']/.test(r))throw new Error('a rota precisa declarar tempo de execucao do Node');if(!/auth\.getUser\(\)/.test(r)||!/401/.test(r))throw new Error('a rota precisa re-checar sessao e devolver 401');if(/request\.json\(\)|await\s+request\.text\(\)|searchParams/.test(r))throw new Error('D3: a rota NAO pode ler corpo nem parametro');if(/vendedorId|responsavel|\bids\b/.test(r))throw new Error('D3: a rota nao pode conhecer escopo nenhum');if(/is_supervisor|role\s*===\s*[\"']supervisor[\"']/.test(r))throw new Error('zero checagem de papel na rota');if(!/getDiarioParaExportacao/.test(r)||!/buildDiarioWorkbook|exportacaoDiario/.test(r))throw new Error('a rota precisa chamar o leitor e o gerador do Task 1');if(!/Content-Disposition/.test(r)||!/attachment/.test(r))throw new Error('faltam os cabecalhos de download');if(!/diario_/.test(r))throw new Error('nome de arquivo fora do contrato de copy');const a=fs.readFileSync('components/agenda/AgendaList.tsx','utf8');if(!/\/api\/agenda\/exportar-diario/.test(a))throw new Error('o botao precisa chamar a rota nova');const h=a.slice(a.indexOf('exportar-diario')-1500,a.indexOf('exportar-diario')+900);if(/body:/.test(h))throw new Error('D3: o envio nao pode levar corpo');if(/vendedorFiltroId/.test(h))throw new Error('D3: o manipulador de exportacao nao pode ler o filtro de vendedor da tela');for(const s of ['Exportar meu diário','Exportar diário do time','Exportando','Não foi possível exportar o diário. Tente novamente.'])if(!a.includes(s))throw new Error('falta a copy exata: '+s);if(!/variant=\"outline\"[\s\S]{0,300}Download|Download[\s\S]{0,300}variant=\"outline\"/.test(a))throw new Error('o botao precisa ser de contorno com icone de download');if(!/role=\"alert\"[\s\S]{0,200}destructive/.test(a))throw new Error('a faixa de erro precisa usar o papel de alerta e as classes padronizadas');if(!/isSupervisor\s*\?/.test(a))throw new Error('o rotulo precisa alternar pelo indicador de Supervisor');const w=fs.readFileSync('components/clientes/KanbanBoard.tsx','utf8');if(!w.includes('/api/clientes/exportar'))throw new Error('o botao de exportar clientes do kanban nao pode ter sido tocado');console.log('OK')" && test -z "$(git diff --name-only HEAD -- components/importacao/ components/clientes/KanbanBoard.tsx app/api/clientes/ lib/clientes/exportacao.ts)"</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/agenda/exportar-diario/route.ts` declara tempo de execução do Node, re-checa sessão com 401, **não lê corpo nem parâmetro nenhum**, não tem checagem de papel, e devolve a planilha com cabeçalho de anexo e o nome de arquivo do contrato.
    - `AgendaList.tsx` tem o botão de contorno com ícone de download, rótulo alternando pelos dois textos do contrato conforme o papel, rótulo em voo, faixa de erro com o texto do contrato e papel de alerta.
    - O manipulador de exportação **não envia corpo** e **não lê o filtro de vendedor** da tela (D3), verificado mecanicamente.
    - `components/importacao/`, `KanbanBoard.tsx`, `app/api/clientes/` e `lib/clientes/exportacao.ts` saem vazios em `git diff --name-only HEAD`. **`supabase/migrations/` NÃO entra nesta checagem**: o plano 17-01 roda na mesma onda que este e cria uma migration; incluir o diretório aqui faria a verificação falhar por causa do trabalho legítimo do plano vizinho. Este plano simplesmente não abre nenhum arquivo de migration.
    - `npx tsc --noEmit` limpo e `npm run lint` limpo.
  </acceptance_criteria>
  <done>O botão existe na Agenda, chama uma rota sem parâmetro e baixa a planilha do diário.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana — baixar o diário de verdade, nos dois papéis</name>
  <action>Pausar a execução. Os testes automatizados provam a regra de visibilidade e o escape de fórmula contra o banco real, mas ninguém ainda abriu a planilha baixada num programa de planilha de verdade. Apresentar ao dono e aguardar o sinal.</action>
  <what-built>
    Foi acrescentado um botão **"Exportar meu diário"** (ou **"Exportar diário do time"**, se você estiver como Supervisor) no canto superior direito da tela de Agenda. Ele baixa uma planilha com tudo o que já foi **concluído** — visitas e tarefas de prospecção — com seis colunas: Cliente, Responsável, Tipo, Resumo, Data e Autor.

    Três coisas que valem saber sobre como ele se comporta:

    1. **Ele sempre exporta tudo o que você pode ver, independente do filtro de vendedor que estiver ativo na tela.** Se você é Supervisor e está com a Agenda filtrada em um vendedor só, o botão ainda baixa o diário do time inteiro. Isso foi uma decisão consciente (o botão significa "tudo que eu posso ver", sempre a mesma coisa), mas se você preferir que ele respeite o filtro da tela, é um ajuste pequeno — fale agora.

    2. **A Agenda mostra o que está pendente; a planilha traz o que já foi concluído.** São conjuntos opostos de propósito: a tela é a lista de afazeres, a planilha é o registro do que aconteceu. Não estranhe a Agenda estar vazia e a planilha vir cheia.

    3. **Texto que começa com sinal de igual, mais, menos ou arroba sai da planilha com uma aspa simples na frente.** Isso é proteção: sem ela, um resumo escrito como "=algo" seria interpretado como fórmula pelo Excel ao abrir o arquivo, o que é um caminho conhecido de ataque. A aspa aparece na célula; é esperado, não é defeito.
  </what-built>
  <how-to-verify>
    1. Suba o projeto (`npm run dev`) e entre como **Vendedor**. Vá em Agenda. O botão deve dizer **"Exportar meu diário"**.
    2. Clique. Um arquivo `diario_AAAA-MM-DD.xlsx` deve baixar. Abra-o.
    3. Confirme que aparecem só clientes **seus** — nenhum cliente de outro vendedor. Esta é a verificação mais importante do plano.
    4. Confirme as seis colunas na ordem: Cliente, Responsável, Tipo, Resumo, Data, Autor. A coluna Tipo deve mostrar "Prospecção" ou "Visita", nunca um código técnico. A coluna Data deve estar no formato dia/mês/ano hora:minuto.
    5. Confirme que os resumos são exatamente o que foi escrito ao concluir cada item — sem corte e sem texto acrescentado.
    6. Saia e entre como **Supervisor**. Vá em Agenda. O botão deve dizer **"Exportar diário do time"**.
    7. Clique e abra. Agora devem aparecer conclusões de **todos** os vendedores.
    8. Ainda como Supervisor, escolha um vendedor específico no filtro "Vendedor" da tela e clique em exportar de novo. **O arquivo deve continuar trazendo o time inteiro** — é o comportamento descrito no ponto 1 acima. Confirme que é isso que você quer.
    9. Se você tiver algum cliente sem nenhuma visita ou tarefa concluída, tudo bem — ele simplesmente não aparece na planilha. E se ninguém tiver concluído nada ainda, o arquivo baixa com só a linha de cabeçalho. Nenhum dos dois é erro.
  </how-to-verify>
  <resume-signal>Digite "approved" se o download funcionou nos dois papéis e a visibilidade está correta, ou descreva o que ficou diferente do esperado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trilha de auditoria → planilha | Texto livre escrito por vendedores (resumos) saindo do sistema para um arquivo que será aberto num programa de planilha. |
| navegador → rota de download | Requisição de um recurso que agrega dados de vários clientes de uma vez. |
| papel do usuário → escopo do download | O que cada papel pode baixar; a tela sabe o papel só para escolher o rótulo. |
| rota → grupo de rotas protegido | A rota fica fora da guarda de sessão do grupo `(app)`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-11 | Tampering (injeção em planilha) | resumos de texto livre saindo para o arquivo | high | mitigate | Toda coluna, sem exceção, roteada pelo embrulho compartilhado que chama o saneador **importado** do módulo de exportação de clientes — nunca reimplementado. Verificação mecânica exige o import, recusa uma reimplementação local e conta que as seis colunas passam pelo embrulho. Provado pelos casos `injecao` (resumo) e `injecaonome` (razão social), que cobrem duas colunas diferentes de propósito. Fecha o risco registrado como transferido no plano 16-03. |
| T-17-12 | Information Disclosure | Vendedor baixando o diário de clientes alheios | critical | mitigate | O leitor não tem filtro de dono nem checagem de papel; a regra de leitura da trilha de auditoria condicionada ao cliente pai (migration 0002, inalterada) é a fronteira inteira. Verificação mecânica recusa qualquer checagem escrita à mão dentro do leitor. Provado nos dois sentidos pelos casos `vendedor` e `supervisor`, com sessões reais. |
| T-17-13 | Elevation of Privilege | chamador ampliando o escopo do download por parâmetro | high | mitigate | Impossibilidade estrutural: a rota não lê corpo, não lê parâmetro de consulta e o leitor não aceita argumento. Verificado mecanicamente nos dois arquivos. É também a implementação literal de D3 — não há caminho para o filtro da tela nem para um escopo forjado atravessarem. |
| T-17-14 | Information Disclosure | chamador não autenticado baixando o diário | critical | mitigate | Re-checagem de sessão na própria rota devolvendo 401 (a rota fica fora da guarda do grupo de rotas), no molde da rota irmã. Provado pelo caso `anonimo` do teste de integração, que exercita a camada de dados sem sessão. |
| T-17-15 | Spoofing | tela decidindo escopo pelo indicador de papel que ela recebe | high | mitigate | O indicador escolhe apenas o RÓTULO do botão; o envio não leva corpo e a rota não o consulta. Verificação mecânica recusa corpo no envio e recusa leitura do filtro de vendedor dentro do manipulador. Comentário obrigatório no componente registra a distinção para quem mexer depois. |
| T-17-16 | Denial of Service | leitura larga da trilha de auditoria estourando tempo ou tráfego | medium | mitigate | Seleção estreita de seis campos, filtro por tipo no SQL e ordenação servidas pelo índice composto de tipo e data que já existe desde a migration 0003 — nenhum índice novo necessário. Verificação mecânica recusa seleção aberta e exige o filtro no SQL. Sem limite de linhas: volumes de MVP não justificam paginação, e um limite silencioso produziria exportação incompleta, que é pior que lenta. |
| T-17-17 | Tampering (trilha de auditoria) | escrita na trilha por este caminho | critical | mitigate | Verificação mecânica recusa qualquer operação de escrita dentro do leitor; a rota é somente leitura; nenhuma migration é tocada por este plano. A trilha continua sem regra de escrita para usuários — o teste de integração depende disso ao ter que semear pela chave de serviço. |
| T-17-18 | Information Disclosure | nome de autor/responsável de outro membro do time exposto | low | accept | A leitura de perfis é aberta a qualquer autenticado desde a migration 0001 (decisão anterior do projeto, time pequeno de dois papéis). A exportação não amplia isso — só mostra o autor de entradas que o chamador já podia ler. |
| T-17-19 | Denial of Service | falha da exportação derrubando a tela de Agenda | medium | mitigate | O manipulador trata resposta não-ok e exceção gravando texto de erro no estado, e desliga o sinalizador em voo no bloco final. Nenhuma linha do efeito de carga da agenda é tocada — uma falha de download não afeta a lista. |
| T-17-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` neste plano. A biblioteca de planilha e a de datas já estão no projeto e a de planilha já passou por checkpoint de legitimidade na Fase 5. Nada a auditar. |
</threat_model>

<verification>
- Os dois arquivos de teste novos passam, o puro sem banco e o de integração contra o banco real.
- Os casos `vendedor` e `supervisor` provam os dois lados do critério de sucesso 5 com sessões reais.
- Os casos `injecao` e `injecaonome` provam o fechamento do risco de injeção de fórmula em duas colunas distintas.
- A verificação mecânica confirma: leitor sem parâmetro e sem checagem de papel; gerador puro importando o saneador em vez de reimplementá-lo, com as seis colunas roteadas; rota sem corpo, sem parâmetro e sem checagem de papel; manipulador sem corpo e sem leitura do filtro da tela.
- `git diff --name-only HEAD` sai vazio para `components/importacao/`, `KanbanBoard.tsx`, `app/api/clientes/`, `lib/clientes/exportacao.ts` e `supabase/migrations/`.
- Checkpoint humano aprovado com download real feito nos dois papéis.
</verification>

<success_criteria>
- Vendedor e Supervisor conseguem baixar, da tela de Agenda, uma planilha do diário com a mesma regra de visibilidade da exportação de clientes.
- Nenhum resumo de texto livre sai da planilha capaz de virar fórmula.
- IMP-02 está entregue por inteiro.
</success_criteria>

<output>
Create `.planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-02-SUMMARY.md` when done
</output>
