---
phase: 17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/{proximo_numero_livre}_atualizar_frequencia_visita_lote.sql
  - tests/importacao/rls-frequencia-lote.test.ts
autonomous: false
requirements: [IMP-01]

must_haves:
  truths:
    - "IMP-01 (fundação de gravação): existe no banco uma operação única que recebe uma lista de clientes já identificados e grava a frequência de visita de todos eles de uma vez, numa só instrução — nunca uma volta por linha (limite de ~10s do plano gratuito da Vercel)"
    - "IMP-01 (critério de sucesso 3, garantido por construção): essa operação é estruturalmente incapaz de criar cliente — ela é uma instrução de atualização sobre linhas que já existem, e não há nenhuma instrução de criação em lugar nenhum do arquivo. Verificado mecanicamente, não por leitura"
    - "IMP-01 (critério de sucesso 3, segunda metade): a operação só altera clientes cujo status de acompanhamento é 'ganho'; um cliente em andamento ou perdido passa incólume, sem erro e sem alteração"
    - "IMP-01 (critério de sucesso 4): uma linha que não casa com nenhum cliente apto simplesmente não aparece no retorno — ela não derruba, não reverte e não bloqueia as demais linhas do mesmo lote"
    - "Só o Supervisor consegue executar a operação; o Vendedor é recusado ANTES de qualquer gravação, e a recusa é provada contra o banco real com sessão de Vendedor de verdade"
    - "A operação devolve exatamente quais clientes foram de fato alterados (identificador + razão social), que é o dado com que a camada de aplicação monta o resumo 'quantas entraram / quantas foram puladas'"
    - "A operação roda com os privilégios de quem chama — a regra de alteração de clientes da migration 0002 continua sendo a fronteira real, e o projeto continua com exatamente três exceções de privilégio elevado, nenhuma nova"
    - "Esta migration NÃO toca em clientes, visitas, tarefas nem no histórico: nenhuma coluna nova, nenhuma regra de acesso nova, nenhum gatilho novo. A coluna de frequência de visita já existe desde a migration 0013"
    - "A operação NÃO agenda visita nenhuma — igual à edição individual de frequência que já existe na ficha do cliente desde a Fase 13, que também nunca mexe em visitas"
    - "A migration foi aplicada no projeto Supabase hospedado com aprovação humana prévia, e as suítes de regressão de maior risco continuam verdes"
  artifacts:
    - "supabase/migrations/{proximo_numero_livre}_atualizar_frequencia_visita_lote.sql (número derivado por `ls` na execução, nunca copiado deste plano)"
    - "função `atualizar_frequencia_visita_lote(p_atualizacoes jsonb)` devolvendo tabela de (id uuid, razao_social text)"
    - "tests/importacao/rls-frequencia-lote.test.ts (novo)"
  key_links:
    - "a função de checagem de papel do projeto (`is_supervisor()`, migration 0001) é a ÚNICA autoridade do guard de Supervisor — comparar papel à mão dentro da função faria a fronteira divergir de `importar_clientes_lote`, que é o molde literal desta"
    - "a cláusula que restringe a atualização ao status 'ganho' é o que torna o critério de sucesso 3 verdadeiro por construção; tirá-la faria o fluxo de planilha definir cadência para cliente em prospecção, que é exatamente o que a fase existe para impedir"
    - "a ausência de elevação de privilégio é o que mantém a regra de alteração de clientes (migration 0002) valendo: com privilégio elevado, a função passaria por cima do RLS e o guard de papel viraria a única barreira — defesa em camada única"
    - "os identificadores de cliente enviados na carga vêm SEMPRE de uma consulta feita no servidor (plano 17-03), nunca de célula de planilha; é isso que impede que um identificador malformado aborte o lote inteiro na conversão de tipo"
    - "o pragma de resolução de conflito entre nome de coluna e nome de variável de saída é obrigatório aqui pelo mesmo motivo da migration 0005: os dois campos devolvidos têm o mesmo nome de colunas reais da tabela"
---

<objective>
Criar a operação de banco que grava, numa única instrução, a frequência de visita de vários clientes "ganho" de uma vez — mais o arquivo de teste de integração que prova, contra o banco real, que só o Supervisor a executa, que ela nunca cria cliente e que uma linha ruim não derruba o lote.

Purpose: é a fundação de gravação do IMP-01 e a única mudança de banco da Fase 17 inteira. Os planos 17-03, 17-04 e 17-05 são todos código de aplicação por cima dela. Ela vem primeiro porque três dos cinco critérios de sucesso da fase (só altera clientes existentes; só clientes "ganho"; uma linha ruim não trava o lote) são verdadeiros **por construção do SQL**, não por cuidado da tela — e é aqui que essa construção acontece.

Output: uma migration aplicada no banco hospedado com aprovação humana prévia, e um arquivo de teste novo verde contra o banco real.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/skills/Supabase-conventions/SKILL.md
@.planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-UI-SPEC.md
</context>

<source_audit>
## Multi-Source Coverage Audit — Fase 17 (conjunto completo de planos 17-01 a 17-05)

### GOAL (ROADMAP, Fase 17 — 5 critérios de sucesso)

| # | Item | Plano | Status |
|---|------|-------|--------|
| G1 | Supervisor envia planilha e define frequência de vários clientes "ganho" de uma vez, no mesmo fluxo de 4 passos da importação de clientes | 17-01 (gravação) + 17-03 (leitura/validação/confirmação) + 17-04 (tabela de revisão e resumo) + 17-05 (assistente, rota e menu) | COVERED |
| G2 | Na revisão, cada linha mostra se o cliente foi encontrado e se está apto; linhas problemáticas sinalizadas antes de qualquer gravação | 17-03 (anotação por linha, puramente leitura) + 17-04 (tabela de revisão com selo e motivo por linha) | COVERED |
| G3 | Ao confirmar, só a frequência de clientes existentes é alterada — nenhum cliente novo é criado | 17-01 (instrução de atualização, nunca de criação — verificado mecanicamente) + 17-03 (a ação só envia identificadores resolvidos no servidor) | COVERED |
| G4 | Uma linha ruim não trava o lote; o resumo final diz quantas entraram e quantas foram puladas | 17-01 (a cláusula de status simplesmente não casa a linha, sem erro) + 17-03 (reconciliação enviado x devolvido) + 17-04 (tela de resumo com os dois números e os motivos) | COVERED |
| G5 | Vendedor e Supervisor exportam o histórico de visitas/tarefas concluídas como planilha, com a mesma regra de visibilidade da exportação de clientes | 17-02 (leitor, gerador de planilha, rota de download e botão) | COVERED |

### REQ (REQUIREMENTS.md — phase_req_ids: IMP-01, IMP-02)

| ID | Plano | Status |
|----|-------|--------|
| IMP-01 | 17-01 (banco) + 17-03 (lógica pura + ações de servidor) + 17-04 (componentes de revisão/resumo) + 17-05 (assistente + rota + menu) | COVERED |
| IMP-02 | 17-02 (integral) | COVERED |

### RESEARCH (.planning/research/ARCHITECTURE.md + FEATURES.md + PITFALLS.md + SUMMARY.md — não existe RESEARCH.md de fase, por escolha explícita do dono)

| Item | Plano | Status |
|------|-------|--------|
| ARCHITECTURE: IMP-01 é RPC nova e própria, no molde não-elevado de `importar_clientes_lote`, com guard explícito de Supervisor | 17-01 | COVERED |
| ARCHITECTURE: IMP-01 reaproveita o PADRÃO DE TELA da importação da v1.1, não o fluxo de dados | 17-04 + 17-05 (componentes irmãos dos existentes; o assistente de clientes fica literalmente intocado) | COVERED |
| ARCHITECTURE: IMP-02 reaproveita o caminho de exportação da Fase 5, só com outra consulta de origem | 17-02 | COVERED |
| PITFALLS: injeção de fórmula em planilha nos resumos de texto livre (risco A4 da v1.1, transferido do 16-03) | 17-02 (reusa o saneador existente, nunca reimplementa) | COVERED |
| PITFALLS: limite de ~10s do plano gratuito da Vercel → gravação em lote, nunca volta por linha | 17-01 (instrução única) + 17-03 (uma chamada por lote) | COVERED |
| PITFALLS: uma linha ruim nunca trava o lote | 17-01 + 17-03 | COVERED |
| STATE (Deferred Items): o marco v1.3 não pode adicionar uma quarta exceção de privilégio elevado | 17-01 (verificado mecanicamente) | COVERED |
| STATE (Blockers): limite de tentativas de login do Supabase Auth na suíte completa | todas as tarefas com teste de integração (logar cada identidade uma vez por arquivo) | COVERED |

### CONTEXT (decisões vinculantes)

Não existe `17-CONTEXT.md` — o dono pulou o Discuss por escolha explícita. As decisões vinculantes vêm do `17-UI-SPEC.md` (aprovado, 6/6 dimensões), das "Notas para o planejamento" do ROADMAP e do `STATE.md`:

| Decisão travada | Plano | Status |
|-----------------|-------|--------|
| D1 — a importação de frequências ganha rota própria (`/clientes/importar-frequencias`), não uma aba na tela de importar clientes | 17-05 | COVERED |
| D2 — o botão de exportar diário mora no cabeçalho da tela de Agenda, espelhando o botão de exportar clientes do kanban | 17-02 | COVERED |
| D3 — a exportação do diário ignora o filtro de vendedor ativo na Agenda: sempre exporta tudo o que o chamador pode ver | 17-02 (a rota não aceita parâmetro nenhum — é estrutural, não disciplina) | COVERED |
| D4 — a tabela de mapeamento de colunas ganha UM parâmetro opcional, nunca um componente duplicado | 17-04 | COVERED |
| Copy exata dos dois fluxos (rótulos de passo, motivos por linha, textos de erro, rótulos do resumo, nome do arquivo e das colunas exportadas) | 17-02 (IMP-02) + 17-04/17-05 (IMP-01) | COVERED |
| Reuso literal e sem modificação de `FileDropzone`, do mapa de cor de status da revisão e do saneador de célula | 17-03 + 17-04 + 17-02 | COVERED |
| Zero componente novo de biblioteca de interface; zero dependência npm nova | todos os cinco (verificado mecanicamente) | COVERED |
| O assistente de importação de clientes, sua tabela de revisão e seu resumo ficam LITERALMENTE intocados | 17-04 + 17-05 (verificado por diff vazio) | COVERED |

### Lacunas encontradas na leitura do código real — ADITIVAS, flagradas para o dono

Três pontos que o `17-UI-SPEC.md` não cobre e que só apareceram ao ler o código de verdade. Nenhum deles reduz escopo; dois acrescentam segurança e um é uma consequência de comportamento que precisa ser vista pelo dono:

| # | Lacuna | Tratamento | Onde |
|---|--------|------------|------|
| L1 | Duas razões sociais diferentes podem normalizar para a mesma chave de comparação (é exatamente o que a detecção de duplicados da v1.1 existe para achar). O contrato de copy tem 5 motivos de linha e nenhum cobre "casou com mais de um cliente" — sem tratamento, a planilha gravaria a frequência no cliente errado, em silêncio | Motivo de linha NOVO, aditivo, no mesmo tom dos cinco existentes. Linha vira "erro", nada é gravado | 17-03, e o dono vê no checkpoint do 17-05 |
| L2 | A mesma razão social repetida em duas linhas da planilha, com frequências diferentes, produziria uma gravação não determinística (a instrução em lote escolhe uma das duas arbitrariamente) | A primeira ocorrência vence (mesma convenção da detecção de duplicados da v1.1) e as demais entram no resumo como puladas, com motivo próprio, também aditivo | 17-03, e o dono vê no checkpoint do 17-05 |
| L3 | Definir a frequência pela planilha NÃO agenda a primeira visita — é o mesmo comportamento da edição individual na ficha do cliente, que desde a Fase 13 nunca mexe em visitas. Consistente, mas é uma consequência que o dono precisa enxergar antes de rodar a planilha em massa | Comportamento mantido por consistência, e apresentado explicitamente ao dono no checkpoint humano deste plano | 17-01 (checkpoint) |

Nenhum item de fonte ficou sem plano. Não há item adiado.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Migration da operação de gravação em lote + arquivo de teste de integração</name>
  <files>supabase/migrations/{proximo_numero_livre}_atualizar_frequencia_visita_lote.sql, tests/importacao/rls-frequencia-lote.test.ts</files>
  <read_first>
    - supabase/migrations/0006_fix_importar_clientes_lote_cte_rls_visibility.sql — o MOLDE LITERAL desta função. Extrair, na ordem: a assinatura (um único parâmetro de carga em formato de documento, tabela de retorno), a declaração de linguagem procedural, o pragma de resolução de conflito entre nome de coluna e nome de variável de saída (linha 39) e por que ele existe, o guard de papel logo no início do corpo (linhas 44-46) com a mensagem de erro em português, e a ausência deliberada de qualquer marcação de elevação de privilégio. Ler também o comentário de cabeçalho inteiro (linhas 1-34): é lá que está registrado por que uma escrita em lote nunca vira volta por linha e por que instruções encadeadas dentro de um mesmo comando enxergam o mesmo instantâneo.
    - supabase/migrations/0005_fix_importar_clientes_lote_variable_conflict.sql — o comentário que explica o defeito de ambiguidade que o pragma resolve. Esta função tem exatamente o mesmo problema (os dois campos de retorno têm os mesmos nomes de colunas reais da tabela), então o pragma é obrigatório aqui também.
    - supabase/migrations/0013_cliente_ativo_e_frequencia_visita.sql, linhas 25-60 — a definição do tipo enumerado de frequência de visita (quatro valores, nesta ordem) e a coluna opcional correspondente em clientes. A coluna JÁ EXISTE: esta migration não cria coluna nenhuma.
    - supabase/migrations/0002_clientes_and_funil.sql — a regra de alteração de clientes (linhas ~196-200) com sua cláusula de gravação, que é a fronteira real que continua valendo por a função não ter elevação de privilégio; e a restrição de verificação que amarra o status 'ganho' à etapa final (linhas ~128-130), para confirmar o vocabulário exato da coluna de status.
    - supabase/migrations/0001_profiles_and_roles.sql — a função de checagem de papel do projeto, que é a única autoridade do guard.
    - app/actions/clientes.ts, a função de edição individual da frequência (a partir da linha ~411) — em especial o comentário que registra que ela NUNCA toca na tabela de visitas. É o precedente que esta operação em lote precisa espelhar: definir cadência não agenda visita.
    - tests/importacao/rls-importar-lote.test.ts — o molde literal do arquivo de teste: comentário de cabeçalho declarando o que está sendo provado, gerador de razão social única por execução, uso da chave de serviço só para semear e limpar (nunca para a asserção), acumulador de identificadores criados e rotina de limpeza, e a forma de obter o identificador do usuário a partir de um cliente autenticado.
    - tests/helpers/supabase-test-clients.ts e tests/auth/rls-roles.test.ts — assinaturas dos ajudantes e as contas semeadas.
    - .planning/STATE.md — Blockers/Concerns: o limite de tentativas de login do Supabase Auth (logar cada identidade UMA vez por arquivo); e Deferred Items: as três exceções de privilégio elevado que o projeto tem hoje, e a proibição explícita de uma quarta neste marco.
  </read_first>
  <action>
    (A) **Descobrir o próximo número livre de migration** com `ls supabase/migrations/` e nomear o arquivo novo `{numero}_atualizar_frequencia_visita_lote.sql`. Nunca reaproveitar número existente, nunca editar migration já aplicada.

    (B) **Escrever a função de gravação em lote.** Comentário de cabeçalho no mesmo tom do 0006, registrando: que ela existe para o fluxo de planilha do IMP-01; que é irmã da importação de clientes em lote mas materialmente diferente porque altera linha existente em vez de criar; e que três critérios de sucesso da fase são garantidos aqui por construção do SQL, não por disciplina da tela.

    A função:
      - Recebe **um único parâmetro**: a carga completa do lote, em formato de documento (o mesmo formato que a importação de clientes já usa). Nunca um parâmetro por linha, nunca um laço de chamadas.
      - Devolve uma **tabela com dois campos**: o identificador e a razão social de cada cliente que foi de fato alterado. É esse retorno que a camada de aplicação (plano 17-03) usa para reconciliar "enviado x gravado" e montar o resumo.
      - Declara linguagem procedural e **inclui o pragma de resolução de conflito** entre nome de coluna e nome de variável de saída, imediatamente após a abertura do corpo, exatamente como o 0006 faz — sem ele os dois campos de retorno tornam as referências às colunas reais ambíguas e a função falha em tempo de execução com erro de ambiguidade.
      - **NÃO** declara elevação de privilégio: roda como quem chama, para que a regra de alteração de clientes da migration 0002 continue sendo a fronteira real (defesa em duas camadas: guard de papel + regra de linha). Este é o padrão do projeto inteiro e o marco v1.3 está explicitamente proibido de abrir uma quarta exceção.
      - **Guard de papel logo na primeira linha do corpo**, antes de qualquer acesso a dado: se quem chama não for Supervisor, levantar exceção com mensagem em português (no molde da mensagem do 0006, adaptada ao verbo deste fluxo). Usar a função de checagem de papel do projeto — nunca comparar papel à mão consultando a tabela de perfis.
      - **Uma única instrução de atualização**, em lote, expandindo a carga em conjunto de registros com dois campos tipados: o identificador (tipo identificador universal) e a frequência (o tipo enumerado da migration 0013). A instrução casa o cliente pelo identificador **e** exige que o status de acompanhamento seja 'ganho'; devolve identificador e razão social das linhas efetivamente afetadas, e esse resultado é o retorno da função.
      - **Não existe nenhuma outra instrução no corpo.** Nada de criação de linha, nada de laço, nada de tratamento de exceção por linha, nada de escrita em visitas, tarefas ou histórico. Uma linha da carga que não case (cliente inexistente, ou existente mas não "ganho") simplesmente não afeta linha nenhuma — não levanta erro, não reverte o lote, não aparece no retorno. É exatamente assim que o critério de sucesso 4 ("uma linha ruim não trava o lote") fica verdadeiro sem nenhum código extra.

    Comentário no corpo, em duas linhas, registrando: (1) que a cláusula de status é o mecanismo literal do critério de sucesso 3 e não pode ser removida nem afrouxada; (2) que esta operação deliberadamente não agenda visita nenhuma, pelo mesmo motivo documentado na edição individual da frequência (definir cadência ≠ agendar visita; a próxima visita nasce da conclusão da anterior, Fase 15).

    (C) **`tests/importacao/rls-frequencia-lote.test.ts` (novo).** Integração contra o banco real, no molde literal de `tests/importacao/rls-importar-lote.test.ts`. A chave de serviço só semeia e limpa; toda asserção roda com sessão real do papel sob teste. Logar cada identidade UMA vez por arquivo.

    Cenário semeado uma vez pela chave de serviço, com razões sociais únicas por execução: um cliente do Vendedor A com status 'ganho' na etapa final e frequência nula; um segundo cliente do Vendedor A com status 'ganho' e frequência já preenchida com um valor diferente do que o teste vai enviar; um cliente do Vendedor A com status em andamento; e um cliente do Vendedor B com status 'ganho'. Limpar todos por identificador no fim.

    Casos exigidos, com estas palavras-chave nos nomes (sem acento):
      - **`supervisor`**: com sessão de Supervisor, enviar um lote com os dois clientes 'ganho' do Vendedor A; o retorno traz exatamente esses dois; relendo pela chave de serviço, os dois têm a frequência nova (inclusive o que já tinha uma diferente — sobrescrever é o comportamento esperado).
      - **`vendedor`**: com sessão de Vendedor, enviar um lote válido; a chamada é recusada com exceção, e relendo pela chave de serviço NENHUMA frequência mudou. A asserção precisa cobrir os dois lados (a recusa E a ausência de efeito).
      - **`naoganho`**: enviar no mesmo lote um cliente 'ganho' e o cliente em andamento; o retorno traz só o 'ganho'; relendo, a frequência do cliente em andamento continua exatamente como estava. Prova o critério de sucesso 3 na sua segunda metade.
      - **`inexistente`**: enviar no mesmo lote um cliente 'ganho' válido e um identificador universal que não existe na base; o retorno traz só o válido, a chamada NÃO levanta erro, e a frequência do válido foi gravada. Prova o critério de sucesso 4 ("uma linha ruim não trava o lote").
      - **`naocria`**: contar as linhas de clientes pela chave de serviço antes e depois de um lote que inclui um identificador inexistente; a contagem é idêntica. Prova o critério de sucesso 3 na sua primeira metade, com número, não com leitura de código.
      - **`semvisita`**: depois do caso do Supervisor, contar pela chave de serviço as visitas dos clientes alterados; continua zero. Prova que definir cadência em massa não agenda visita (lacuna L3).
      - **`anonimo`**: um cliente não autenticado chamando a operação é recusado.

    Nenhum outro arquivo é criado ou alterado por esta tarefa. `supabase db push` NÃO roda aqui — é a Task 3, depois da aprovação humana.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const dir='supabase/migrations';const f=fs.readdirSync(dir).filter(n=>n.endsWith('_atualizar_frequencia_visita_lote.sql'));if(f.length!==1)throw new Error('esperado exatamente 1 arquivo de migration novo, achei '+f.length);const s=fs.readFileSync(dir+'/'+f[0],'utf8');const low=s.toLowerCase();if(!/create\s+(or\s+replace\s+)?function\s+atualizar_frequencia_visita_lote/.test(low))throw new Error('falta a funcao atualizar_frequencia_visita_lote');if(!/returns\s+table\s*\(\s*id\s+uuid\s*,\s*razao_social\s+text\s*\)/.test(low))throw new Error('a tabela de retorno precisa ser (id uuid, razao_social text)');if(!/language\s+plpgsql/.test(low))throw new Error('precisa ser linguagem procedural');if(/security\s+definer/.test(low))throw new Error('esta funcao NAO pode ter privilegio elevado');if(!/#variable_conflict\s+use_column/.test(low))throw new Error('falta o pragma de resolucao de conflito coluna x variavel de saida');if(!/is_supervisor\s*\(\s*\)/.test(low))throw new Error('falta o guard de papel usando a funcao do projeto');if(!/raise\s+exception/.test(low))throw new Error('o guard precisa levantar excecao');const corpo=low.slice(low.indexOf('begin'));const iGuard=corpo.indexOf('is_supervisor');const iUpd=corpo.indexOf('update');if(iGuard<0||iUpd<0||iGuard>iUpd)throw new Error('o guard de papel precisa vir ANTES da instrucao de atualizacao');if(/insert\s+into/.test(low))throw new Error('nenhuma instrucao de criacao pode existir neste arquivo');if(/\bloop\b|\bfor\s+\w+\s+in\b/.test(low))throw new Error('gravacao em lote, nunca volta por linha');if(/\bdelete\s+from\b|\btruncate\b|\bdrop\s+table\b/.test(low))throw new Error('esta migration nao apaga nada');if(/\bvisitas\b|\btarefas\b|\bhistorico\b/.test(low))throw new Error('esta operacao nao toca em visitas, tarefas nem historico');if(/alter\s+table/.test(low))throw new Error('nenhuma coluna nova: a coluna de frequencia ja existe desde a 0013');if(/create\s+policy/.test(low))throw new Error('nenhuma regra de acesso nova nesta fase');if((low.match(/\bupdate\s+clientes\b/g)||[]).length!==1)throw new Error('precisa haver exatamente uma instrucao de atualizacao de clientes');if(!/status_acompanhamento\s*=\s*'ganho'/.test(low))throw new Error('falta a clausula que restringe a atualizacao a clientes ganho');if(!/jsonb_to_recordset|jsonb_array_elements/.test(low))throw new Error('a carga precisa ser expandida em conjunto de registros');if(!/frequencia_visita_enum/.test(low))throw new Error('a frequencia precisa ser tipada pelo enum da 0013');if(!/returning/.test(low))throw new Error('a instrucao precisa devolver as linhas efetivamente afetadas');const t=fs.readFileSync('tests/importacao/rls-frequencia-lote.test.ts','utf8');const n=(t.match(/\bit\(\s*[\"'][^\"']+/g)||[]).map(x=>x.toLowerCase());for(const k of ['supervisor','vendedor','naoganho','inexistente','naocria','semvisita','anonimo'])if(!n.some(x=>x.includes(k)))throw new Error('rls-frequencia-lote.test.ts: falta caso com a palavra '+k);if(!t.includes('SEED_ACCOUNTS')||!t.includes('signInAs'))throw new Error('o teste precisa usar sessoes reais');if(!t.includes('serviceClient'))throw new Error('semeadura e limpeza pela chave de servico');if(!t.includes('atualizar_frequencia_visita_lote'))throw new Error('o teste precisa chamar a operacao nova');console.log('OK')" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Existe exatamente um arquivo novo em `supabase/migrations/` terminando em `_atualizar_frequencia_visita_lote.sql`, com número maior que todos os existentes.
    - A função devolve `(id uuid, razao_social text)`, é procedural, tem o pragma de resolução de conflito, e **não** tem elevação de privilégio.
    - O guard de papel usa a função de checagem do projeto, levanta exceção, e aparece **antes** da instrução de atualização.
    - O arquivo contém **exatamente uma** instrução de atualização de clientes, com a cláusula de status 'ganho', e **zero** instruções de criação, laços, apagamentos, alterações de tabela ou regras de acesso.
    - O arquivo não menciona visitas, tarefas nem histórico.
    - `tests/importacao/rls-frequencia-lote.test.ts` existe com casos cujos nomes contêm `supervisor`, `vendedor`, `naoganho`, `inexistente`, `naocria`, `semvisita` e `anonimo`, usando sessões reais e semeadura pela chave de serviço.
    - `npx tsc --noEmit` limpo e `npm run lint` limpo. Os testes ainda NÃO passam nesta tarefa (a função só existe no banco depois da Task 3) — isso é esperado.
  </acceptance_criteria>
  <done>A migration e o teste existem no repositório, mecanicamente conformes, prontos para a revisão humana anterior ao push.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Aprovação humana — aplicar a migration no banco Supabase de produção</name>
  <action>Pausar a execução. É a etapa de revisão antes de produção exigida pelo CLAUDE.md e já praticada em todas as fases que mexeram em schema (9, 10, 11, 12, 13, 16). Apresentar ao dono do projeto, em linguagem simples, o que a migration faz no banco real, e só rodar o `supabase db push` (Task 3) depois do sinal "approved". Não prosseguir sem aprovação.</action>
  <what-built>
    Foi escrita uma instrução nova no banco que serve a uma coisa só: **receber uma lista de clientes e gravar a frequência de visita de todos eles de uma vez**. É o motor por trás da planilha de frequências em massa que você vai usar na tela (a tela em si vem nos próximos planos desta fase).

    O que ela faz, em português:

    1. **Primeiro pergunta quem está chamando.** Se não for um Supervisor, ela recusa na hora e não grava absolutamente nada. Vendedor não consegue usar esse caminho nem tentando por fora da tela.

    2. **Depois altera todo mundo de uma vez só**, numa única operação, em vez de um cliente por vez. Isso não é preciosismo: a hospedagem do site derruba qualquer operação que passe de ~10 segundos, e um por vez estouraria esse limite numa planilha grande. É a mesma regra que já valeu para a importação de clientes da versão anterior.

    3. **Ela só sabe alterar, não sabe criar.** Isso é literal, não é uma promessa: a instrução escrita ali é do tipo "atualize a linha que já existe". Não existe nenhuma instrução de criação no arquivo inteiro, e o teste conta as linhas de clientes antes e depois para provar que o número não mudou. Uma planilha com um cliente que não existe na base não cria esse cliente — ela simplesmente ignora aquela linha.

    4. **Ela só mexe em cliente que está com status "Ganho".** Se a planilha trouxer um cliente que ainda está em prospecção ou que foi perdido, aquela linha passa batida, sem erro e sem alteração. Frequência de visita é rotina de pós-venda; não faz sentido definir cadência para quem ainda nem comprou.

    5. **Uma linha ruim não derruba a planilha inteira.** Linha que não casa com ninguém simplesmente não entra no resultado. As boas são gravadas normalmente. No fim, a tela vai te dizer quantas entraram e quantas foram puladas, e por quê.

    O que a migration **não** faz: não cria coluna nenhuma (o campo de frequência já existe desde a Fase 13), não cria tabela, não cria nenhuma permissão especial que passe por cima das travas de acesso, não apaga nada, não instala biblioteca nenhuma e não mexe em visitas, tarefas nem no histórico.
  </what-built>
  <how-to-verify>
    1. Confirme que você está logado no Supabase CLI e com o projeto linkado (mesmo fluxo das migrations anteriores; se `supabase db push` pedir a senha do banco, é normal — tenha ela à mão). Lembrete da Fase 13: o CLI pode precisar ficar na versão `2.111.0`.
    2. Se quiser conferir o arquivo: abra a migration nova em `supabase/migrations/` (é a de número mais alto, terminando em `_atualizar_frequencia_visita_lote.sql`). Confirme que não existe nenhum `insert`, `delete`, `drop` ou `alter table` nele — só um `update`. São umas 30 linhas no total.

    **Três pontos que merecem o seu olhar antes de aprovar:**

    3. **Ponto #1 — definir a frequência pela planilha NÃO agenda a primeira visita.** Depois de rodar a planilha, os clientes ficam com a cadência definida, mas nenhuma visita aparece na Agenda até que alguém conclua alguma coisa com aquele cliente. Isso é de propósito e é exatamente o que já acontece hoje quando você edita a frequência na ficha de um cliente individualmente — a Fase 13 decidiu assim, e mudar só para o fluxo de planilha criaria dois comportamentos diferentes para a mesma ação. **Se você esperava que a planilha já enchesse a Agenda de visitas, fale agora** — dá para mudar, mas é uma decisão de produto, não um detalhe técnico.

    4. **Ponto #2 — a planilha sobrescreve frequência que já existe.** Um cliente que já estava como "mensal" e aparece na planilha como "semanal" vira semanal, sem aviso e sem confirmação por linha. A tela de revisão (próximos planos) vai te mostrar cada linha antes de gravar, então você vê o que vai acontecer — mas não haverá um alerta específico de "esse aqui já tinha uma frequência". Se quiser esse alerta, é melhor pedir agora.

    5. **Ponto #3 — cliente que não está "Ganho" é pulado em silêncio no banco.** No banco, a linha simplesmente não casa. Na tela, porém, ela vai aparecer marcada como erro na revisão, com o motivo escrito ("Cliente encontrado, mas não está com status Ganho"), antes de qualquer gravação — então você não fica sem saber. Confirme que essa é a divisão de trabalho que você espera: o banco é a trava final, a tela é quem te explica.
  </how-to-verify>
  <resume-signal>Digite "approved" para autorizar o `supabase db push` no banco de produção, ou descreva qualquer dúvida/ajuste antes de aplicar.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Aplicar a migration (supabase db push) e levar os testes a GREEN</name>
  <files>tests/importacao/rls-frequencia-lote.test.ts</files>
  <read_first>
    - o arquivo de migration criado no Task 1 (é o que será aplicado)
    - tests/helpers/supabase-test-clients.ts — conferir que `.env.local` tem as três chaves apontando para o projeto hospedado
    - .planning/STATE.md — Blockers/Concerns: o limite de tentativas de login do Supabase Auth no projeto ao vivo (não repetir login desnecessariamente dentro do mesmo arquivo), a linha de cliente com estado inválido deixada de propósito na base, e o pin do Supabase CLI em `2.111.0` usado no push da Fase 13
  </read_first>
  <action>
    Rodar `supabase db push` para aplicar a migration no projeto Supabase hospedado (mesmo fluxo das migrations 0002 a 0016). Se falhar por autenticação ou link, criar dinamicamente um gate de auth (pedir ao dono que rode `supabase login` / `supabase link` e forneça a senha do banco) e repetir — NÃO seguir adiante sem o push concluído: é o pré-requisito bloqueante do plano 17-03 e de todo o fluxo IMP-01.

    Se o push acusar colisão de numeração, renomear o arquivo desta fase para o próximo número livre real e repetir — nunca renomear nem editar uma migration já aplicada.

    Depois do push, rodar o arquivo de teste contra o banco real e levá-lo a GREEN. Se algum caso falhar, diagnosticar contra o SQL aplicado ANTES de mexer no teste. Suspeitos prováveis, em ordem:
      1. Erro de ambiguidade em tempo de execução ao chamar a função — o pragma de resolução de conflito não entrou, ou entrou no lugar errado (precisa vir logo depois da abertura do corpo). Corrigir com migration nova, nunca editando a já aplicada.
      2. Erro de conversão de tipo ao expandir a carga — a frequência enviada pelo teste não é um dos quatro valores do tipo enumerado da 0013, ou o identificador enviado não é um identificador universal bem formado. Conferir o que o teste está montando antes de mexer no SQL.
      3. Caso `vendedor` passando por engano — conferir que a asserção está usando sessão real de Vendedor e não a chave de serviço (com a chave de serviço o guard também recusa, mas por outro motivo, e o teste deixaria de provar o que promete).
      4. Caso `naoganho` alterando o cliente em andamento — a cláusula de status não entrou na instrução, ou entrou como filtro da expansão da carga em vez de condição da atualização.

    Rodar também, como regressão de menor custo, as suítes de maior risco de colisão com esta mudança: `tests/importacao/` inteira e `tests/clientes/frequencia-visita.test.ts` + `tests/clientes/frequencia-visita-edicao.test.ts`. Rodar em lotes pequenos por diretório, NUNCA `npm test` de uma vez — o limite de tentativas de login do Supabase Auth derruba a suíte completa numa execução única (defeito conhecido, registrado no STATE.md, não é regressão desta fase).
  </action>
  <verify>
    <automated>npx vitest run tests/importacao/rls-frequencia-lote.test.ts && npx vitest run tests/clientes/frequencia-visita.test.ts tests/clientes/frequencia-visita-edicao.test.ts && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` concluído com sucesso contra o projeto hospedado.
    - `tests/importacao/rls-frequencia-lote.test.ts` passa inteiro contra o banco real, incluindo os casos `vendedor` (recusa + zero efeito), `naocria` (contagem idêntica antes/depois) e `semvisita`.
    - As duas suítes de frequência da Fase 13 continuam verdes (nenhuma regressão na edição individual).
    - `npx tsc --noEmit` limpo e `npm run lint` limpo.
    - Nenhuma migration já aplicada foi editada ou renomeada.
  </acceptance_criteria>
  <done>A operação de gravação em lote existe no banco de produção, provada contra ele, e o plano 17-03 está desbloqueado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → ação de servidor → operação de banco | A carga do lote atravessa a fronteira; ela é DADO, nunca insumo de autorização. |
| operação de banco → tabela de clientes | Escrita em massa numa coluna de linhas que podem pertencer a qualquer vendedor. |
| papel do chamador → permissão de uso do fluxo | Só Supervisor; a checagem na tela é conforto, a checagem aqui é a trava. |
| planilha do usuário → tipo enumerado do banco | Texto arbitrário virando valor de um tipo fechado — conversão que pode abortar o comando inteiro. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Elevation of Privilege | Vendedor executando a gravação em massa por fora da tela | critical | mitigate | Guard de papel como primeira instrução do corpo, usando a função de checagem do projeto, antes de qualquer acesso a dado. Verificado mecanicamente (ordem guard-antes-da-atualização) e provado pelo caso `vendedor`, que exige recusa E ausência de efeito. |
| T-17-02 | Elevation of Privilege | função passando por cima das regras de linha por privilégio elevado | critical | mitigate | Ausência de marcação de elevação de privilégio, verificada mecanicamente. A regra de alteração de clientes da migration 0002 continua valendo como segunda camada: mesmo um Supervisor comprometido só altera o que aquela regra permite. Mantém o projeto em exatamente três exceções, respeitando a proibição registrada no STATE.md. |
| T-17-03 | Tampering | fluxo de planilha criando cliente novo em vez de só atualizar | critical | mitigate | Impossibilidade estrutural: uma única instrução de atualização, zero instruções de criação em todo o arquivo, verificado mecanicamente por contagem. Provado por número no caso `naocria` (contagem de linhas de clientes idêntica antes e depois de um lote com identificador inexistente). |
| T-17-04 | Denial of Service | uma linha ruim abortando o lote inteiro | high | mitigate | Linha que não casa não afeta linha nenhuma e não levanta erro (caso `inexistente`). O único caminho que ainda poderia abortar o comando é conversão de tipo malsucedida — mitigado em duas camadas: os identificadores vêm de consulta feita no servidor (plano 17-03), nunca de célula, e a frequência é re-validada contra o vocabulário fechado antes do envio. |
| T-17-05 | Tampering (integridade de dado) | definir cadência para cliente que ainda não comprou | high | mitigate | Cláusula de status na condição da atualização, verificada mecanicamente e provada pelo caso `naoganho`. É a metade de banco do critério de sucesso 3; a metade de tela (motivo escrito na revisão) é do plano 17-03. |
| T-17-06 | Denial of Service | gravação linha a linha estourando o limite de ~10s da hospedagem | high | mitigate | Instrução única em lote; verificação mecânica recusa laço e recusa mais de uma instrução de atualização. Mesma regra que já valeu para a importação de clientes da v1.1. |
| T-17-07 | Tampering | escrita indevida em visitas/tarefas/histórico a partir deste caminho | medium | mitigate | Verificação mecânica recusa qualquer menção a essas três tabelas no arquivo. O caso `semvisita` prova pelo lado do dado que nenhuma visita nasce da gravação em massa. A trilha de auditoria continua sem regra de escrita para usuários. |
| T-17-08 | Repudiation | alteração em massa sem rastro de quem fez | low | accept | A trilha de auditoria do projeto só registra mudança de etapa/status e conclusões (gatilhos da migration 0002/0015); frequência de visita nunca foi registrada nela, nem na edição individual da Fase 13. Registrar agora só para o fluxo de planilha criaria assimetria entre dois caminhos do mesmo campo. Risco aceito, consistente com o precedente. |
| T-17-09 | Information Disclosure | Supervisor descobrindo existência de cliente alheio pelo retorno da operação | low | accept | O Supervisor já enxerga todos os clientes pela regra de leitura da migration 0002 — o retorno não amplia nada. Para o Vendedor a questão não se coloca: ele é recusado antes de qualquer leitura. |
| T-17-10 | Denial of Service (autoinfligido) | migration abortando o push contra dado legado de produção | medium | mitigate | A migration não cria coluna, não cria restrição e não altera tabela — só define uma função. Não há dado legado capaz de fazê-la falhar. Verificado mecanicamente (ausência de alteração de tabela) e coberto pelo checkpoint humano antes do push. |
| T-17-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` neste plano nem nesta fase inteira — zero pacotes novos, nada a auditar. Checkpoint de legitimidade não se aplica. |
</threat_model>

<verification>
- A migration está aplicada no projeto hospedado e `tests/importacao/rls-frequencia-lote.test.ts` passa contra ele.
- Os sete casos exigidos existem e passam, cobrindo os dois lados de cada afirmação (recusa + ausência de efeito; retorno + releitura).
- O arquivo de migration é mecanicamente conforme: sem elevação de privilégio, sem instrução de criação, sem laço, sem alteração de tabela, sem regra de acesso, sem menção a visitas/tarefas/histórico, com exatamente uma instrução de atualização portando a cláusula de status.
- Nenhuma migration anterior foi editada (`git diff` sobre os arquivos 0001-0016 sai vazio).
- As duas suítes de frequência da Fase 13 continuam verdes.
</verification>

<success_criteria>
- Existe no banco de produção uma operação que grava a frequência de visita de vários clientes 'ganho' numa única instrução, executável só pelo Supervisor.
- Está provado por teste contra o banco real que ela não cria cliente, não altera cliente não-'ganho', não agenda visita e não é derrubada por uma linha ruim.
- O plano 17-03 está desbloqueado.
</success_criteria>

<output>
Create `.planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-01-SUMMARY.md` when done
</output>
