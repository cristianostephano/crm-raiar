---
phase: 17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio
plan: 04
type: execute
wave: 3
depends_on: ["17-03"]
files_modified:
  - components/importacao/ColumnMappingTable.tsx
  - components/importacao/FrequenciaPreviewTable.tsx
  - components/importacao/FrequenciaImportSummary.tsx
  - tests/importacao/column-mapping-table.test.tsx
  - tests/importacao/frequencia-preview-table.test.tsx
  - tests/importacao/frequencia-import-summary.test.tsx
autonomous: true
requirements: [IMP-01]

must_haves:
  truths:
    - "D4 cumprida por inteiro: a tabela de mapeamento de colunas passa a aceitar uma lista de campos opcional e serve as duas listas do projeto — **um** parâmetro novo, zero componente duplicado, e o ponto de uso existente no assistente de clientes não muda nem uma linha"
    - "IMP-01 (critério de sucesso 2): a tabela de revisão mostra, por linha, a razão social que veio na planilha, qual cliente foi encontrado, qual frequência será gravada, e o selo de situação com o motivo escrito por extenso"
    - "A tabela de revisão NÃO tem coluna de ação: este fluxo não tem estado de decisão por linha, então os botões de importar/pular não existem — não é que estejam escondidos, a coluna não é montada"
    - "IMP-01 (critério de sucesso 4, metade visual): a tela de resumo mostra os dois números (clientes atualizados e linhas puladas) e a lista de motivos com a quantidade por motivo"
    - "O mapa de cor de borda por situação e a contagem de resumo vêm do módulo de revisão que já existe, importados sem modificação — nenhum sistema de cor de status novo é introduzido"
    - "Nenhum componente novo de biblioteca de interface é instalado; nenhum arquivo do assistente de importação de clientes, da sua tabela de revisão ou do seu resumo é tocado"
  artifacts:
    - "components/importacao/ColumnMappingTable.tsx com um parâmetro opcional de lista de campos, retrocompatível"
    - "components/importacao/FrequenciaPreviewTable.tsx (novo) — cinco colunas, paginação de 50, linha de resumo"
    - "components/importacao/FrequenciaImportSummary.tsx (novo) — dois cartões de contagem, lista de motivos, dois botões"
    - "três arquivos de teste de renderização novos"
  key_links:
    - "a lista de campos precisa ser parâmetro OPCIONAL com valor padrão igual à lista de 14: se virar obrigatório, o ponto de uso existente no assistente de clientes quebra e a mudança deixa de ser retrocompatível"
    - "a tabela de revisão precisa mostrar o cliente encontrado vindo do BANCO, não a célula da planilha — é essa diferença que confirma visualmente ao Supervisor que o casamento foi o certo antes de gravar"
    - "importar o mapa de cor e a contagem do módulo de revisão em vez de copiá-los é o que impede dois vocabulários de cor de situação divergirem entre os dois fluxos de planilha"
    - "o resumo deriva a contagem de linhas puladas somando as quantidades dos grupos, nunca recebendo um número separado — dois números independentes acabariam discordando"
---

<objective>
Construir as três peças visuais reutilizáveis do fluxo de planilha de frequências: o parâmetro que faz a tabela de mapeamento de colunas servir as duas listas de campos (D4), a tabela de revisão de cinco colunas e a tela de resumo pós-gravação.

Purpose: separa a montagem de componentes da montagem do assistente. As três peças aqui são apresentacionais puras — recebem tudo por parâmetro, não chamam ação de servidor nenhuma, não têm efeito de carga — e por isso são testáveis por renderização isolada. O plano 17-05 só as encaixa.

Output: um componente existente estendido de forma retrocompatível, dois componentes novos e três arquivos de teste de renderização.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-UI-SPEC.md
@.planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-03-SUMMARY.md
</context>

<source_audit>
O audit completo de cobertura das quatro fontes para o conjunto inteiro de planos da Fase 17 está em `17-01-PLAN.md`, seção `<source_audit>`. Este plano cobre a metade visual reutilizável dos itens **G1**, **G2** e **G4**, o requisito **IMP-01**, e a decisão **D4** (metade de componente).
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: A tabela de mapeamento de colunas servindo as duas listas de campos (D4)</name>
  <files>components/importacao/ColumnMappingTable.tsx, tests/importacao/column-mapping-table.test.tsx</files>
  <read_first>
    - components/importacao/ColumnMappingTable.tsx — o arquivo INTEIRO (87 linhas). Extrair: o comentário que registra que o componente é uma casca controlada sem estado próprio; a forma das quatro propriedades atuais; os três cabeçalhos de coluna; o rótulo acessível montado por coluna; e a montagem das opções do campo de escolha a partir da lista de 14 mais o item sentinela de "não importar" ao final.
    - lib/importacao/mapping.ts (como ficou depois do plano 17-03) — as versões parametrizadas dos tipos de alvo e de mapeamento, e os apelidos retrocompatíveis. É a forma dessas duas versões que a assinatura das propriedades precisa acompanhar.
    - lib/importacao/types.ts (como ficou depois do plano 17-03) — a definição de campo parametrizada, com o padrão igual à união de 14.
    - components/importacao/ImportWizard.tsx, linhas ~339-348 — o ÚNICO ponto de uso existente do componente. Ele **não pode ser modificado**: se ele precisar de qualquer alteração para continuar compilando, a mudança não é retrocompatível e está errada.
    - tests/importacao/FileDropzone.test.tsx — molde de teste de renderização de componente neste projeto (ambiente de DOM, biblioteca de testes de renderização).
    - .planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-UI-SPEC.md (já no contexto) — decisão D4 e o ponto 5 da seção "IMP-01 — new files".
  </read_first>
  <action>
    (A) **`components/importacao/ColumnMappingTable.tsx`.** Parametrizar o componente pela chave de campo, com padrão igual à união de 14, e acrescentar **uma** propriedade opcional: a lista de definições de campo, com valor padrão igual à lista de 14. As propriedades de mapeamento e de mudança de mapeamento acompanham a mesma parametrização (usando as versões parametrizadas criadas no plano 17-03), para que o campo de escolha não aceite um alvo fora da lista recebida.

    Única mudança no corpo: a montagem das opções passa a percorrer a lista recebida em vez da lista de 14 fixa. O item sentinela de "não importar" continua sendo o último, sempre, e seu rótulo não muda. Nenhuma outra linha do arquivo é alterada — mesmos cabeçalhos, mesma prévia, mesmo rótulo acessível, mesmo comentário de casca controlada (estendido com uma frase registrando que o componente serve duas listas de campos desde a Fase 17, e que por isso a lista é parâmetro).

    **Retrocompatibilidade é o requisito, não um efeito colateral:** o ponto de uso existente no assistente de clientes não passa a propriedade nova, e precisa continuar compilando e se comportando exatamente igual. Se o compilador exigir uma conversão explícita para casar o valor padrão com o parâmetro genérico, usar conversão de tipo com comentário — nunca o tipo curinga proibido pelo CLAUDE.md.

    (B) **`tests/importacao/column-mapping-table.test.tsx` (novo).** Renderização isolada. Casos exigidos, com estas palavras-chave nos nomes (sem acento):
      - **`padrao`**: renderizado **sem** a propriedade nova, o campo de escolha de uma coluna oferece os 14 rótulos da lista de clientes mais o item de não importar. É a prova de retrocompatibilidade.
      - **`frequencia`**: renderizado **com** a segunda lista, o campo de escolha oferece exatamente os dois rótulos daquela lista mais o item de não importar — e **nenhum** rótulo da lista de 14 aparece.
      - **`sentinela`**: em ambos os casos, o item de não importar é o último da lista.
      - **`mudanca`**: escolher uma opção dispara a função de mudança com o índice da coluna e o alvo escolhido.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx vitest run tests/importacao/column-mapping-table.test.tsx && test -z "$(git diff --name-only HEAD -- components/importacao/ImportWizard.tsx components/importacao/ImportPreviewTable.tsx components/importacao/ImportSummary.tsx components/importacao/FileDropzone.tsx app/actions/importacao.ts supabase/migrations/)" && node -e "const fs=require('fs');const c=fs.readFileSync('components/importacao/ColumnMappingTable.tsx','utf8');if(!/fields\?\s*:/.test(c))throw new Error('a lista de campos precisa ser propriedade OPCIONAL');if(!/fields\s*=\s*SYSTEM_FIELDS/.test(c))throw new Error('o valor padrao precisa ser a lista de 14');if(/SYSTEM_FIELDS\.map\(/.test(c))throw new Error('as opcoes precisam percorrer a lista RECEBIDA, nunca a lista de 14 fixa');if(!/fields\.map\(/.test(c))throw new Error('as opcoes precisam percorrer a lista recebida');if(/SYSTEM_FIELDS_FREQUENCIA/.test(c))throw new Error('o componente nao pode conhecer a segunda lista: ela chega por parametro');const iSent=c.lastIndexOf('NAO_IMPORTAR');const iMap=c.indexOf('fields.map(');if(iSent<iMap)throw new Error('o item sentinela precisa continuar sendo o ultimo');if(/\bany\b/.test(c.replace(/\/\*[\s\S]*?\*\/|\/\/.*$/gm,'')))throw new Error('tipo curinga proibido pelo CLAUDE.md');const t=fs.readFileSync('tests/importacao/column-mapping-table.test.tsx','utf8');const n=(t.match(/\bit\(\s*[\"'][^\"']+/g)||[]).map(x=>x.toLowerCase());for(const k of ['padrao','frequencia','sentinela','mudanca'])if(!n.some(x=>x.includes(k)))throw new Error('column-mapping-table.test.tsx: falta caso com a palavra '+k);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - A lista de campos é propriedade **opcional** com valor padrão igual à lista de 14; as opções percorrem a lista **recebida**.
    - O componente não importa a segunda lista de campos — ela chega sempre por parâmetro.
    - O item sentinela de não importar continua sendo o último.
    - `git diff --name-only HEAD` sai vazio para `ImportWizard.tsx`, `ImportPreviewTable.tsx`, `ImportSummary.tsx`, `FileDropzone.tsx`, `app/actions/importacao.ts` e `supabase/migrations/`.
    - `tests/importacao/column-mapping-table.test.tsx` passa com os quatro casos exigidos, incluindo o caso `padrao` que prova a retrocompatibilidade.
    - `npx tsc --noEmit` limpo e `npm run lint` limpo.
  </acceptance_criteria>
  <done>D4 cumprida: um componente, um parâmetro novo, duas listas de campos, zero duplicação.</done>
</task>

<task type="auto">
  <name>Task 2: Tabela de revisão e tela de resumo do fluxo de frequências</name>
  <files>components/importacao/FrequenciaPreviewTable.tsx, components/importacao/FrequenciaImportSummary.tsx, tests/importacao/frequencia-preview-table.test.tsx, tests/importacao/frequencia-import-summary.test.tsx</files>
  <read_first>
    - components/importacao/ImportPreviewTable.tsx — o arquivo INTEIRO (196 linhas). É o molde estrutural. Extrair: o tamanho de página e a fatia por página em estado React simples (e o comentário que registra por que não é a biblioteca de tabela headless — ela não está no projeto e acrescentá-la exigiria checkpoint de legitimidade); o efeito que reinicia a paginação quando a lista de entrada muda; a linha de resumo acima da tabela; o estado vazio com borda tracejada; a aplicação da classe de borda por situação na linha; o selo com o rótulo de situação e os motivos juntados por separador; e a barra de paginação com os dois botões, renderizada só quando houver mais de uma página. **Este arquivo não pode ser modificado.**
    - components/importacao/ImportSummary.tsx — o arquivo INTEIRO (95 linhas). Molde estrutural do resumo. Extrair: a derivação interna da contagem de puladas somando as quantidades dos grupos (e a razão registrada no histórico do projeto para ela ser derivada e não recebida); a grade de dois cartões com as bordas coloridas à esquerda e os dois ícones; o formatador de inteiro em português; a seção de motivos renderizada só quando houver algum, com a concordância de singular/plural; os dois botões ao final; e o comentário que registra o uso deliberado do tamanho de exibição de 20 pixels em vez do dispensado do painel gerencial. **Este arquivo não pode ser modificado.**
    - lib/importacao/preview.ts — o arquivo INTEIRO (45 linhas). A contagem de resumo e o mapa de cor de borda por situação, com o comentário que proíbe inventar um sistema de cor novo. Este módulo é importado **sem modificação**: as situações deste fluxo são um subconjunto estrito das três dele, então a chave de duplicado simplesmente nunca é usada aqui.
    - lib/funil/frequencia.ts — o mapa de rótulos, para exibir a frequência resolvida.
    - app/actions/importacaoFrequencia.ts e lib/importacao/annotarLinhaFrequencia.ts (criados no plano 17-03) — a forma exata da linha validada e da linha resolvida que a tabela recebe, e a forma do retorno da confirmação que o resumo recebe.
    - tests/importacao/import-summary.test.tsx — molde do teste de renderização do resumo.
    - .planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-UI-SPEC.md (já no contexto) — pontos 6 e 7 da seção "IMP-01 — new files", e, na tabela de copy, as linhas do passo 3 (cabeçalhos das cinco colunas, célula de cliente encontrado sem casamento, rótulos dos selos, linha de resumo) e as linhas do resumo (título, rótulos dos dois cartões, cabeçalho e formato da lista de motivos, rótulos dos dois botões).
  </read_first>
  <action>
    (A) **`components/importacao/FrequenciaPreviewTable.tsx` (novo).** Irmão estrutural da tabela de revisão de clientes, com o mesmo tamanho de página, o mesmo efeito de reinício de paginação, o mesmo estado vazio e a mesma barra de paginação. Importa a contagem de resumo e o mapa de cor de borda do módulo de revisão existente, **sem modificá-lo**.

    Propriedades: apenas a lista de linhas validadas. **Nenhuma propriedade de decisão por linha e nenhuma função de mudança de decisão** — este fluxo não tem estado de decisão.

    Cinco colunas, na ordem e com os cabeçalhos exatos do contrato de copy:
      1. o número da linha (índice mais um, mesma convenção do irmão);
      2. a razão social **como veio na planilha** (o valor resolvido), com o mesmo peso de fonte de destaque do irmão, e travessão quando vazia;
      3. o cliente encontrado — a razão social **do banco** quando houver casamento, travessão quando não houver. Comentário obrigatório de uma linha registrando que esta coluna existe justamente para o Supervisor confirmar visualmente que o casamento foi o certo, e por isso ela nunca pode exibir a célula da planilha;
      4. a nova frequência, pelo mapa de rótulos do módulo único de frequência, travessão quando nula;
      5. a situação: o selo com o rótulo e, abaixo, os motivos juntados pelo mesmo separador do irmão.

    **Não existe coluna de ação.** Registrar em comentário que ela não é montada (não é escondida) porque este fluxo não tem estado de decisão por linha.

    Linha de resumo acima da tabela com o texto exato do contrato de copy (total, prontas para atualizar, com erro), montada a partir da contagem importada — a contagem de duplicados que aquela função também devolve simplesmente não é usada aqui, e vale um comentário de uma linha dizendo por quê.

    Rótulos dos selos: só os dois que se aplicam, num mapa constante no topo do arquivo.

    (B) **`components/importacao/FrequenciaImportSummary.tsx` (novo).** Irmão estrutural do resumo de clientes, com a mesma grade de dois cartões, os mesmos ícones e cores de borda, o mesmo formatador de inteiro e o mesmo tamanho de exibição das duas contagens. Diferenças: a copy dos dois cartões, do título e dos dois botões vem do contrato deste fluxo; e o segundo botão reinicia o assistente em vez de falar em importar.

    A contagem de linhas puladas é **derivada** somando as quantidades dos grupos recebidos — nunca recebida como número separado, exatamente como o irmão faz. As propriedades são: a contagem de atualizados, a lista de grupos de motivos, e as duas funções dos botões.

    (C) **`tests/importacao/frequencia-preview-table.test.tsx` (novo).** Casos exigidos, com estas palavras-chave nos nomes (sem acento):
      - **`colunas`**: os cinco cabeçalhos aparecem, na ordem do contrato; o cabeçalho de ação **não** aparece.
      - **`encontrado`**: uma linha com casamento mostra a razão social do banco na coluna de cliente encontrado, e a da planilha na coluna da planilha — as duas células com textos diferentes, para provar que não são a mesma fonte.
      - **`naoencontrado`**: uma linha sem casamento mostra travessão na coluna de cliente encontrado.
      - **`frequencia`**: a frequência resolvida aparece pelo rótulo de exibição, nunca pelo valor interno.
      - **`motivos`**: uma linha com dois motivos mostra os dois, juntados pelo separador.
      - **`resumo`**: a linha de resumo mostra os três números com o texto do contrato.
      - **`vazio`**: lista vazia mostra o estado vazio, não uma tabela sem linhas.
      - **`paginacao`**: com mais linhas do que cabe numa página, a barra de paginação aparece e avançar mostra a página seguinte; com poucas linhas, a barra não aparece.

      (D) **`tests/importacao/frequencia-import-summary.test.tsx` (novo).** Casos exigidos, com estas palavras-chave nos nomes (sem acento):
      - **`contagens`**: os dois números aparecem, e o de puladas é a soma das quantidades dos grupos (montar grupos cuja soma seja diferente do número de grupos, para o teste não passar por coincidência).
      - **`motivos`**: cada grupo vira uma linha com o motivo e a quantidade, com singular e plural corretos (montar um grupo de quantidade 1 e outro de quantidade maior).
      - **`semmotivos`**: sem grupos, a seção de motivos não é renderizada.
      - **`botoes`**: os dois botões existem com a copy do contrato e disparam as funções recebidas.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx vitest run tests/importacao/frequencia-preview-table.test.tsx tests/importacao/frequencia-import-summary.test.tsx && npx vitest run tests/importacao/import-summary.test.tsx && test -z "$(git diff --name-only HEAD -- components/importacao/ImportWizard.tsx components/importacao/ImportPreviewTable.tsx components/importacao/ImportSummary.tsx components/importacao/FileDropzone.tsx lib/importacao/preview.ts lib/importacao/confirmar.ts app/actions/importacao.ts supabase/migrations/)" && node -e "const fs=require('fs');const p=fs.readFileSync('components/importacao/FrequenciaPreviewTable.tsx','utf8');if(!/from\s+[\"']@\/lib\/importacao\/preview[\"']/.test(p))throw new Error('a cor de situacao e a contagem precisam vir do modulo de revisao existente');if(/const\s+STATUS_BORDER|border-l-amber-500/.test(p))throw new Error('nenhum sistema de cor de situacao novo pode ser criado aqui');if(/decisions|onDecisionChange|Importar mesmo assim|Pular/.test(p))throw new Error('este fluxo nao tem estado de decisao por linha');if(/<TableHead>\s*Ação\s*<\/TableHead>/.test(p))throw new Error('a coluna de acao nao pode existir');for(const c of ['Linha','Razão social (na planilha)','Cliente encontrado','Nova frequência','Status'])if(!p.includes(c))throw new Error('falta o cabecalho: '+c);if(!/prontas para atualizar/.test(p))throw new Error('linha de resumo fora do contrato de copy');if(!/FREQUENCIA_VISITA_LABELS/.test(p))throw new Error('a frequencia precisa ser exibida pelo mapa de rotulos do modulo unico');if(/[\"']Semanal[\"']|[\"']Quinzenal[\"']|[\"']Mensal[\"']/.test(p))throw new Error('os rotulos de frequencia nao podem ser redigitados aqui');const s=fs.readFileSync('components/importacao/FrequenciaImportSummary.tsx','utf8');if(!/reduce\(/.test(s))throw new Error('a contagem de puladas precisa ser DERIVADA somando os grupos');if(/puladasCount\s*:/.test(s))throw new Error('a contagem de puladas nunca pode ser recebida como propriedade');for(const c of ['Atualização concluída','Clientes atualizados','Linhas puladas','Motivos das linhas puladas','Ver clientes','Enviar outra planilha'])if(!s.includes(c))throw new Error('falta a copy exata: '+c);if(/Importar outra planilha|Clientes importados|Importação concluída/.test(s))throw new Error('copy do fluxo de clientes vazando para o de frequencias');for(const f of ['tests/importacao/frequencia-preview-table.test.tsx','tests/importacao/frequencia-import-summary.test.tsx']){const t=fs.readFileSync(f,'utf8');const n=(t.match(/\bit\(\s*[\"'][^\"']+/g)||[]).map(x=>x.toLowerCase());const req=f.includes('preview')?['colunas','encontrado','naoencontrado','frequencia','motivos','resumo','vazio','paginacao']:['contagens','motivos','semmotivos','botoes'];for(const k of req)if(!n.some(x=>x.includes(k)))throw new Error(f+': falta caso com a palavra '+k)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - A tabela de revisão importa a cor de situação e a contagem do módulo de revisão existente, sem criar mapa de cor novo e sem modificar aquele módulo.
    - A tabela tem os cinco cabeçalhos do contrato, **não** tem coluna de ação e **não** conhece decisão por linha.
    - A coluna de cliente encontrado mostra a razão social do banco, provado pelo caso `encontrado` com textos diferentes nas duas colunas.
    - A frequência é exibida pelo mapa de rótulos do módulo único, nunca por texto redigitado.
    - O resumo **deriva** a contagem de puladas somando os grupos, e usa a copy exata deste fluxo — sem vazamento da copy do fluxo de clientes.
    - `git diff --name-only HEAD` sai vazio para os quatro componentes do fluxo de clientes, para os dois módulos puros reusados, para as ações de clientes e para as migrations; `tests/importacao/import-summary.test.tsx` continua verde.
    - Os dois arquivos de teste novos passam com todos os casos exigidos.
    - `npx tsc --noEmit` limpo e `npm run lint` limpo.
  </acceptance_criteria>
  <done>As duas telas do meio e do fim do fluxo existem como componentes isolados, testados por renderização, prontas para o assistente encaixar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados validados → renderização | Texto vindo de planilha do usuário e do banco sendo renderizado na tela de revisão. |
| componente compartilhado → dois fluxos | Um mesmo componente de mapeamento servindo a importação de clientes (em produção) e a de frequências (nova). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-30 | Tampering | regressão silenciosa no assistente de importação de clientes ao estender o componente compartilhado | high | mitigate | A propriedade nova é opcional com valor padrão igual à lista de 14, e o ponto de uso existente fica **literalmente intocado** (diff vazio). O caso `padrao` prova por renderização que, sem a propriedade, as 14 opções continuam sendo oferecidas. |
| T-17-31 | Tampering (integridade de exibição) | Supervisor aprovando a gravação achando que casou o cliente certo | high | mitigate | A coluna de cliente encontrado exibe a razão social lida do BANCO, nunca a célula da planilha — verificado mecanicamente pela ausência de coluna de ação e provado pelo caso `encontrado`, que exige textos diferentes nas duas colunas. É a metade visual do critério de sucesso 2. |
| T-17-32 | Repudiation | resumo mostrando números que não batem entre si | medium | mitigate | A contagem de puladas é derivada somando os grupos, nunca recebida separadamente (verificação mecânica recusa a propriedade). O caso `contagens` monta grupos cuja soma difere do número de grupos, para o teste não passar por coincidência. |
| T-17-33 | Tampering (divergência de vocabulário) | um segundo sistema de cor de situação ou um segundo conjunto de rótulos de frequência nascendo aqui | medium | mitigate | Verificação mecânica exige o import do módulo de revisão, recusa a criação de mapa de cor local e recusa rótulos de frequência redigitados no componente. |
| T-17-34 | Information Disclosure | tabela de revisão exibindo cliente que o chamador não poderia ver | low | accept | As linhas exibidas vêm da ação de validação, cuja leitura já é estreitada pela regra de leitura de clientes e cujo acesso é restrito ao Supervisor — que enxerga todos os clientes de qualquer forma. O componente é apresentacional puro e não faz leitura própria. |
| T-17-35 | Denial of Service | planilha grande travando a tela de revisão | medium | mitigate | Fatia de 50 linhas por página em estado React simples, mesma abordagem do irmão em produção; o efeito de reinício de paginação evita ficar preso numa página inexistente após nova validação. Provado pelo caso `paginacao`. |
| T-17-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` neste plano: todos os componentes de interface usados já estão instalados desde as Fases 1 a 7. Nada a auditar. |
</threat_model>

<verification>
- Os três arquivos de teste novos passam; `tests/importacao/import-summary.test.tsx` continua verde.
- O caso `padrao` prova a retrocompatibilidade do componente compartilhado por renderização, não por leitura de código.
- `git diff --name-only HEAD` sai vazio para todo o assistente de importação de clientes, para os módulos puros reusados e para as migrations.
- A verificação mecânica confirma: propriedade opcional com padrão correto; opções percorrendo a lista recebida; ausência de coluna de ação e de estado de decisão; cor e contagem importadas; rótulos de frequência não redigitados; contagem de puladas derivada; copy exata dos dois contratos sem vazamento entre fluxos.
</verification>

<success_criteria>
- A tabela de mapeamento de colunas serve as duas listas de campos sem duplicação e sem regressão.
- A tabela de revisão de frequências e a tela de resumo existem, testadas por renderização isolada.
- O plano 17-05 está desbloqueado.
</success_criteria>

<output>
Create `.planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-04-SUMMARY.md` when done
</output>
