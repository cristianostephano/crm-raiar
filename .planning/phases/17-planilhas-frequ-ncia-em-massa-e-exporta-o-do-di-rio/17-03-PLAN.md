---
phase: 17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - lib/importacao/types.ts
  - lib/importacao/mapping.ts
  - lib/importacao/typesFrequencia.ts
  - lib/importacao/modeloFrequencia.ts
  - lib/importacao/annotarLinhaFrequencia.ts
  - lib/importacao/confirmarFrequencia.ts
  - app/actions/importacaoFrequencia.ts
  - tests/importacao/frequencia-vocabulario.test.ts
  - tests/importacao/annotarLinhaFrequencia.test.ts
  - tests/importacao/confirmarFrequencia.test.ts
autonomous: true
requirements: [IMP-01]

must_haves:
  truths:
    - "D4 (metade de lógica): a camada pura de mapeamento de colunas passa a servir DUAS listas de campos do sistema — a de 14 campos da importação de clientes e a de 2 campos da importação de frequências — sem nenhum componente nem função duplicada"
    - "D4 (não-regressão, provada por arquivo intocado): o arquivo de teste de mapeamento existente continua verde SEM UMA LINHA ALTERADA — é essa a prova de que a generalização não mexeu no comportamento da importação de clientes"
    - "IMP-01 (critério de sucesso 2): cada linha da planilha sai anotada dizendo se o cliente foi encontrado e se está apto, com o motivo escrito por extenso, ANTES de qualquer gravação — a anotação é função pura e não escreve nada em lugar nenhum"
    - "IMP-01 (critério de sucesso 3): os identificadores enviados para a gravação vêm SEMPRE de uma consulta feita no servidor, nunca de célula de planilha — é isso que impede que a planilha invente um cliente ou aponte para um identificador forjado"
    - "IMP-01 (critério de sucesso 4): linha com erro nunca é enviada; e das que são enviadas, a reconciliação entre o que foi mandado e o que o banco devolveu produz os dois números do resumo (quantas entraram, quantas foram puladas) com o motivo agrupado"
    - "O vocabulário de frequência de visita NÃO é redeclarado: vem inteiro do módulo único do projeto, como o comentário de cabeçalho daquele módulo exige das Fases 14 a 17"
    - "Toda célula vinda da planilha passa pelo saneador de célula compartilhado antes de qualquer comparação, mesmo guarda que a anotação de linha da importação de clientes já aplica"
    - "A comparação de razão social usa a MESMA normalização da detecção de duplicados da v1.1 — nunca uma segunda função de normalização"
    - "Uma razão social que casa com mais de um cliente vira erro e não grava nada (lacuna L1); a mesma razão social repetida na planilha grava só a primeira ocorrência e reporta as demais como puladas (lacuna L2)"
    - "A gravação é UMA chamada por lote, nunca uma por linha — limite de ~10s da hospedagem"
    - "Só o Supervisor executa as duas ações; a checagem de papel na camada de aplicação é conforto de interface, e a trava real continua sendo o guard dentro da operação de banco do plano 17-01"
  artifacts:
    - "lib/importacao/typesFrequencia.ts (novo) — as duas chaves de campo e a lista de definições da segunda lista"
    - "lib/importacao/modeloFrequencia.ts (novo) — gerador do modelo de planilha de duas colunas"
    - "lib/importacao/annotarLinhaFrequencia.ts (novo) — anotação pura por linha + anotação de lote com índice construído uma vez"
    - "lib/importacao/confirmarFrequencia.ts (novo) — planejamento da carga, reconciliação e agrupamento de motivos"
    - "app/actions/importacaoFrequencia.ts (novo) — as duas ações de servidor (validar e confirmar)"
    - "lib/importacao/types.ts e lib/importacao/mapping.ts generalizados de forma retrocompatível"
    - "três arquivos de teste novos"
  key_links:
    - "os identificadores de cliente só existem porque a ação de validação os resolveu contra o banco; se alguma linha da planilha pudesse fornecer um identificador direto, o critério de sucesso 3 deixaria de valer e a operação de banco receberia dado não confiável"
    - "a lista de clientes lida na validação é estreitada pela regra de leitura de clientes, não por filtro escrito à mão — um filtro manual aqui faria a fronteira divergir da que a operação de banco aplica no momento da gravação"
    - "a re-validação da frequência contra o vocabulário fechado ANTES do envio é o que impede que uma conversão de tipo malsucedida aborte o lote inteiro dentro do banco — sem ela, o critério de sucesso 4 tem um buraco"
    - "reconciliar enviado x devolvido é a única forma honesta de descobrir que um cliente deixou de estar apto entre a revisão e a confirmação; sem isso o resumo mentiria dizendo que gravou o que não gravou"
    - "a generalização da camada de mapeamento é o que torna D4 possível: sem ela, a tabela de mapeamento teria que ser duplicada, que é exatamente o que a decisão proíbe"
---

<objective>
Construir toda a camada não visual do fluxo de planilha de frequências: a segunda lista de campos do sistema, o modelo de planilha, a anotação por linha (encontrado? apto? frequência reconhecida?), o planejamento/reconciliação da gravação e as duas ações de servidor — mais a generalização retrocompatível da camada de mapeamento de colunas que D4 exige.

Purpose: é onde vivem os critérios de sucesso 2, 3 e 4 do lado da aplicação. Tudo aqui é função pura ou ação fina, testável sem tela, e é isso que permite que os planos 17-04 e 17-05 sejam quase só montagem visual. Também é aqui que se fecham as duas lacunas que a leitura do código real revelou e que o contrato de interface não cobria: razão social ambígua e linha repetida na planilha.

Output: sete arquivos de código (dois generalizados, cinco novos) e três arquivos de teste novos, todos verdes, sem uma linha alterada no fluxo de importação de clientes.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-UI-SPEC.md
@.planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-01-SUMMARY.md
</context>

<source_audit>
O audit completo de cobertura das quatro fontes para o conjunto inteiro de planos da Fase 17 está em `17-01-PLAN.md`, seção `<source_audit>`. Este plano cobre a metade de aplicação dos itens **G1**, **G2**, **G3** e **G4**, o requisito **IMP-01**, o item de RESEARCH "gravação em lote, nunca volta por linha", a decisão **D4** (metade de lógica) e as duas lacunas aditivas **L1** e **L2**.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Segunda lista de campos, modelo de planilha, e a camada de mapeamento servindo duas listas</name>
  <files>lib/importacao/types.ts, lib/importacao/mapping.ts, lib/importacao/typesFrequencia.ts, lib/importacao/modeloFrequencia.ts, tests/importacao/frequencia-vocabulario.test.ts</files>
  <read_first>
    - lib/importacao/types.ts — o arquivo INTEIRO (67 linhas). A união de 14 chaves, a interface de definição de campo (quatro propriedades), a lista de definições com a marcação de obrigatório nos 7 mínimos, e os dois tipos auxiliares de arquivo lido. Notar que a propriedade de chave é hoje tipada pela união de 14 — é exatamente esse ponto que impede uma segunda lista de existir.
    - lib/importacao/mapping.ts — o arquivo INTEIRO (143 linhas). Em especial: o valor sentinela de "não importar"; os dois tipos de alvo e de mapeamento por índice de coluna; a normalização de cabeçalho (por faixa de código de caractere, não por regex com caractere combinante); a tabela de apelidos e o fato de que ela devolve chaves da lista de 14; as três funções exportadas e o fato de que DUAS delas (sugestão e obrigatórios faltando) **já aceitam** uma lista de campos como segundo parâmetro com valor padrão. É essa metade pronta que D4 cita.
    - lib/importacao/modelo.ts — o arquivo INTEIRO (47 linhas). A derivação dos cabeçalhos a partir dos rótulos das definições, o mapa de valores de exemplo por chave, a escrita em modo seguro para navegador, e o comentário que registra por que o modelo NÃO é saneado (é gerado pelo sistema, não é entrada de usuário).
    - lib/funil/frequencia.ts — o arquivo INTEIRO (71 linhas). O comentário de cabeçalho que declara explicitamente que "as Fases 14 a 17 precisam importar daqui em vez de redeclarar essas strings" — este plano é literalmente o destinatário desse aviso. Extrair: a lista dos quatro valores, o tipo derivado, o mapa de rótulos, a lista pronta de itens e o guarda de tipo.
    - tests/importacao/mapping.test.ts — LER, para saber exatamente o que ele cobre. **Este arquivo NÃO pode ser modificado por esta tarefa** — mantê-lo intocado e verde é a prova mecânica de que a generalização é retrocompatível.
    - tests/importacao/modelo.test.ts — molde do teste do gerador de modelo.
    - .planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-UI-SPEC.md (já no contexto) — seção "IMP-01 — new files", pontos 1 e 2 (forma exata da segunda lista e do modelo), e a decisão D4.
  </read_first>
  <action>
    (A) **`lib/importacao/types.ts` — generalização retrocompatível.** Transformar a interface de definição de campo num tipo parametrizado pela chave, com **valor padrão igual à união de 14 chaves**. Consequência: toda menção existente sem parâmetro continua significando exatamente o que significa hoje, a lista de 14 continua tipada igual, e o gerador de modelo de clientes não muda nem uma linha. Nenhuma outra alteração neste arquivo: a união de 14 chaves **não ganha um 15º valor** (isso quebraria o mapa de valores de exemplo do modelo de clientes e misturaria os dois vocabulários). Comentário curto registrando que o parâmetro existe para permitir uma segunda lista de campos sem duplicar a interface, e apontando a segunda lista.

    (B) **`lib/importacao/mapping.ts` — generalização retrocompatível + um conserto.** Quatro mudanças:
      1. Introduzir versões parametrizadas dos dois tipos (alvo de mapeamento e mapeamento por índice de coluna), e **manter os dois nomes atuais como apelidos** da versão parametrizada com a união de 14 — assim nenhum consumidor existente muda.
      2. Parametrizar as três funções exportadas pela mesma chave, mantendo o valor padrão do segundo parâmetro apontando para a lista de 14. Onde o compilador exigir uma conversão explícita para casar o padrão com o parâmetro genérico, usar conversão de tipo com comentário justificando — nunca o tipo curinga proibido pelo CLAUDE.md.
      3. **Conserto obrigatório na sugestão de mapeamento:** hoje, depois de falhar o casamento por rótulo, a função consulta a tabela de apelidos e devolve o que achar. Com uma segunda lista de campos isso vira defeito — um cabeçalho "CEP" numa planilha de frequências devolveria uma chave que não existe naquela lista, e o campo de escolha da tela receberia um valor fora das suas opções. Passar a **conferir se o resultado do apelido pertence à lista de campos recebida**; se não pertencer, devolver o sentinela de "não importar". Para a lista de 14 essa conferência nunca dispara (todo apelido aponta para um campo dela), então o comportamento atual é preservado por construção — registrar isso em comentário.
      4. Nenhuma alteração na normalização de cabeçalho, na tabela de apelidos ou na aplicação do mapeamento além da parametrização de tipos.

    (C) **`lib/importacao/typesFrequencia.ts` (novo).** Irmão de `types.ts`, no mesmo tom de comentário de cabeçalho. Exporta a união das **duas** chaves (razão social e frequência de visita) e a lista de definições correspondente, **ambas obrigatórias**, com os rótulos exatos do contrato de interface. Comentário registrando: que os dois campos são obrigatórios porque sem razão social não há como achar o cliente e sem frequência não há o que gravar; e que a marcação de obrigatório é o que dirige, sem lógica nova, o estado desabilitado do botão de continuar do passo 2 (via a função de obrigatórios faltando, já genérica).

    (D) **`lib/importacao/modeloFrequencia.ts` (novo).** Espelho literal de `modelo.ts`, adaptado: cabeçalhos derivados dos rótulos da segunda lista, **uma** linha de exemplo com um nome de empresa plausível e uma das quatro frequências escritas com o rótulo de exibição (nunca o valor interno, porque o Supervisor vai preencher a planilha lendo o rótulo). Mesma escrita em modo seguro para navegador, mesmo comentário registrando que o modelo não é saneado por ser gerado pelo sistema. O rótulo de exemplo precisa vir do mapa de rótulos do módulo único de frequência — nunca digitado à mão no arquivo.

    (E) **`tests/importacao/frequencia-vocabulario.test.ts` (novo, puro).** Casos exigidos, com estas palavras-chave nos nomes (sem acento):
      - **`campos`**: a segunda lista tem exatamente duas definições, ambas obrigatórias, com as chaves e rótulos do contrato.
      - **`sugestao`**: um cabeçalho escrito como o rótulo do campo (com variação de caixa/acento/pontuação) é sugerido corretamente contra a segunda lista.
      - **`apelidoforadalista`**: um cabeçalho que a tabela de apelidos conhece mas que **não** pertence à segunda lista (por exemplo o de CEP) é sugerido como "não importar", nunca como uma chave inexistente naquela lista. É a prova do conserto.
      - **`apelidopreservado`**: o mesmo cabeçalho, avaliado contra a lista de 14, continua sendo sugerido para a chave de sempre. É a prova de que o conserto não regrediu a importação de clientes.
      - **`obrigatorios`**: com nenhuma coluna mapeada, a função de obrigatórios faltando devolve as duas definições da segunda lista; com as duas mapeadas, devolve lista vazia.
      - **`aplicar`**: a aplicação do mapeamento sobre linhas cruas produz objetos só com as duas chaves, descartando colunas marcadas como "não importar".
      - **`modelo`**: o modelo gerado tem exatamente duas colunas com os rótulos do contrato, uma linha de exemplo, e o valor de exemplo de frequência é um dos quatro rótulos de exibição.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx vitest run tests/importacao/mapping.test.ts tests/importacao/modelo.test.ts tests/importacao/frequencia-vocabulario.test.ts && test -z "$(git diff --name-only HEAD -- tests/importacao/mapping.test.ts tests/importacao/modelo.test.ts lib/importacao/modelo.ts lib/importacao/annotarLinha.ts lib/importacao/confirmar.ts lib/importacao/dedupe.ts lib/importacao/preview.ts app/actions/importacao.ts components/importacao/ supabase/migrations/)" && node -e "const fs=require('fs');const t=fs.readFileSync('lib/importacao/types.ts','utf8');if(!/interface\s+SystemFieldDefinition\s*<[^>]*=\s*SystemField\s*>/.test(t))throw new Error('a definicao de campo precisa ser parametrizada com padrao igual a uniao de 14');const uniao=t.slice(t.indexOf('export type SystemField'),t.indexOf('export interface'));if(/frequenciaVisita/.test(uniao))throw new Error('a uniao de 14 chaves NAO pode ganhar um 15o valor');const m=fs.readFileSync('lib/importacao/mapping.ts','utf8');if(!/MappingTargetOf|ColumnMappingOf/.test(m))throw new Error('faltam as versoes parametrizadas dos tipos de mapeamento');if(!/export\s+type\s+MappingTarget\s*=/.test(m)||!/export\s+type\s+ColumnMapping\s*=/.test(m))throw new Error('os dois nomes atuais precisam continuar existindo como apelidos');if(/\bany\b/.test(m.replace(/\/\*[\s\S]*?\*\/|\/\/.*$/gm,'')))throw new Error('tipo curinga proibido pelo CLAUDE.md');const sug=m.slice(m.indexOf('export function suggestMapping'));if(!/fields\.some|fields\.find[\s\S]{0,200}alias|new\s+Set\([\s\S]{0,80}fields/.test(sug))throw new Error('a sugestao precisa conferir se o apelido pertence a lista recebida');const f=fs.readFileSync('lib/importacao/typesFrequencia.ts','utf8');for(const k of ['razaoSocial','frequenciaVisita','Razão social','Frequência de visita'])if(!f.includes(k))throw new Error('falta na segunda lista: '+k);if((f.match(/required:\s*true/g)||[]).length!==2)throw new Error('os dois campos da segunda lista precisam ser obrigatorios');if(/required:\s*false/.test(f))throw new Error('nenhum campo opcional na segunda lista');const mo=fs.readFileSync('lib/importacao/modeloFrequencia.ts','utf8');if(!/@\/lib\/funil\/frequencia/.test(mo))throw new Error('o rotulo de exemplo precisa vir do modulo unico de frequencia');if(/@\/lib\/supabase|use client|use server/.test(mo)||/@\/lib\/supabase|use client|use server/.test(f))throw new Error('os modulos novos precisam ser puros');const s=fs.readFileSync('tests/importacao/frequencia-vocabulario.test.ts','utf8');const n=(s.match(/\bit\(\s*[\"'][^\"']+/g)||[]).map(x=>x.toLowerCase());for(const k of ['campos','sugestao','apelidoforadalista','apelidopreservado','obrigatorios','aplicar','modelo'])if(!n.some(x=>x.includes(k)))throw new Error('frequencia-vocabulario.test.ts: falta caso com a palavra '+k);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only HEAD` sai **vazio** para `tests/importacao/mapping.test.ts`, `tests/importacao/modelo.test.ts`, `lib/importacao/modelo.ts`, `lib/importacao/annotarLinha.ts`, `lib/importacao/confirmar.ts`, `lib/importacao/dedupe.ts`, `lib/importacao/preview.ts`, `app/actions/importacao.ts`, `components/importacao/` e `supabase/migrations/` — e os dois arquivos de teste existentes continuam verdes.
    - A definição de campo é parametrizada com padrão igual à união de 14; a união **não** ganhou nenhuma chave nova.
    - Os dois nomes de tipo de mapeamento atuais continuam existindo como apelidos; nenhum tipo curinga foi introduzido.
    - A sugestão de mapeamento confere a pertinência do apelido à lista recebida; provado nos dois sentidos pelos casos `apelidoforadalista` e `apelidopreservado`.
    - A segunda lista tem exatamente duas definições, ambas obrigatórias, com chaves e rótulos do contrato.
    - O modelo de frequências importa o rótulo de exemplo do módulo único de frequência.
    - `tests/importacao/frequencia-vocabulario.test.ts` passa com os sete casos exigidos.
    - `npx tsc --noEmit` limpo e `npm run lint` limpo.
  </acceptance_criteria>
  <done>A camada de mapeamento serve duas listas de campos, a segunda lista existe e o modelo de planilha de frequências é gerável — sem uma linha alterada no fluxo de clientes.</done>
</task>

<task type="auto">
  <name>Task 2: Anotação por linha — encontrado, apto, frequência reconhecida</name>
  <files>lib/importacao/annotarLinhaFrequencia.ts, tests/importacao/annotarLinhaFrequencia.test.ts</files>
  <read_first>
    - lib/importacao/annotarLinha.ts — o arquivo INTEIRO (255 linhas). É o molde. Extrair: o comentário de cabeçalho que declara o módulo puro e explica que as listas de consulta chegam já estreitadas pelo chamador; a **importação SÓ DE TIPO** da camada de consulta (é assim que o módulo permanece puro apesar de referenciar um tipo que mora num arquivo com cliente de banco); a função que saneia toda célula definida antes de qualquer resolução; a busca por nome usando a normalização da detecção de duplicados; e a montagem final devolvendo status derivado da existência de motivos.
    - lib/importacao/dedupe.ts — a função de normalização de razão social (linhas ~43-67), a lista de sufixos societários removidos e o comentário que avisa que o retorno é CHAVE DE COMPARAÇÃO e nunca deve ser exibido nem gravado. E `findDuplicates`, só para entender que duas razões sociais diferentes podem colidir na mesma chave — é exatamente essa possibilidade que gera a lacuna L1.
    - lib/funil/frequencia.ts — a lista dos quatro valores, o mapa de rótulos e o guarda de tipo. O reconhecimento da célula de frequência precisa aceitar o que o Supervisor escreveria de verdade (o RÓTULO, com variação de caixa e espaço), não só o valor interno.
    - lib/clientes/exportacao.ts — a função de saneamento de célula (linhas ~34-39) e o comentário que a declara o único caminho compartilhado.
    - lib/supabase/queries/clientes.ts, linha 65 — o tipo de status de acompanhamento (três valores). É de lá que vem, por importação SÓ DE TIPO.
    - lib/importacao/typesFrequencia.ts (criado no Task 1) — as duas chaves.
    - tests/importacao/annotarLinha.test.ts — molde do arquivo de teste puro.
    - .planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-UI-SPEC.md (já no contexto) — seção "IMP-01 — new files", ponto 3 (as quatro formas de tipo exigidas e a assinatura da anotação por linha) e, na tabela de copy, os **cinco** motivos de linha com o texto exato.
  </read_first>
  <action>
    (A) **`lib/importacao/annotarLinhaFrequencia.ts` (novo).** Módulo puro, irmão de `annotarLinha.ts`: nenhuma importação de valor vinda da camada de consulta (só importação de tipo), nenhuma diretiva de cliente/servidor, zero acesso a banco. Exporta:

      1. **Os quatro tipos** com as formas exatas do contrato de interface: a linha mapeada (parcial sobre as duas chaves); a forma de consulta de cliente (identificador, razão social, status de acompanhamento); a linha resolvida (identificador do cliente ou nulo, razão social como veio da planilha, frequência reconhecida ou nula); e a linha anotada (status de "ok" ou "erro", lista de motivos, e a linha resolvida).

      2. **Um índice de clientes por chave de comparação** e a função pura que o constrói. O índice mapeia cada chave normalizada para a **lista** de clientes que caem nela — deliberadamente uma lista, e não um cliente só, porque é a existência de mais de um que revela a ambiguidade da lacuna L1. Construir o índice UMA vez por lote é também o que impede que a anotação fique quadrática e estoure o limite de tempo da hospedagem numa planilha grande.

      3. **A anotação por linha**, com a assinatura exata do contrato (linha mapeada + lista de clientes), para o teste unitário e para quem ler o contrato. Internamente ela constrói o índice e delega à mesma rotina do lote — nunca duplica a regra.

      4. **A anotação de lote** (lista de linhas + lista de clientes), que constrói o índice uma vez e mapeia. É esta que a ação de servidor chama.

    Regras da anotação, em ordem:
      - sanear **toda** célula definida com o saneador compartilhado antes de qualquer comparação (mesma guarda de `annotarLinha.ts`);
      - razão social ausente ou vazia depois de aparar espaços → motivo "razão social não informada"; sem busca;
      - com razão social, procurar no índice pela chave normalizada da **mesma** função de normalização da detecção de duplicados (nunca uma segunda função): zero resultados → motivo "cliente não encontrado"; **mais de um resultado → motivo de ambiguidade (lacuna L1)**, e o identificador resolvido fica nulo; exatamente um resultado → guardar o identificador e conferir o status;
      - cliente encontrado mas com status diferente de "ganho" → motivo "encontrado, mas não está com status Ganho", e o identificador resolvido **volta a ficar nulo** (uma linha inapta nunca pode carregar identificador para a fase de gravação);
      - frequência ausente ou vazia → motivo "frequência não informada"; presente mas não reconhecida → motivo "frequência não reconhecida", com o valor cru interpolado no texto;
      - reconhecimento da frequência: aparar espaços, comparar sem diferenciar caixa e sem diferenciar acento (reusar a remoção de diacríticos que a normalização de razão social já faz, chamando a função existente — nunca reimplementar), aceitando tanto o valor interno quanto o **rótulo de exibição** dos quatro valores, ambos vindos do módulo único de frequência. Nunca redeclarar as strings;
      - motivos deduplicados, mesma disciplina de `annotarLinha.ts`;
      - status é "erro" quando houver qualquer motivo, "ok" quando não houver.

    **Texto exato dos cinco motivos do contrato de copy**, mais o sexto (lacuna L1), declarados como constantes no topo do arquivo — nunca literais soltos no meio da lógica. O sexto motivo é uma ADIÇÃO ao contrato de copy aprovado, no mesmo tom dos outros cinco, e precisa vir com comentário registrando que é adição consciente e por quê (sem ele, uma colisão de normalização gravaria a frequência no cliente errado, em silêncio): `Mais de um cliente encontrado com essa razão social — use o nome exato do cadastro`.

    (B) **`tests/importacao/annotarLinhaFrequencia.test.ts` (novo, puro).** Casos exigidos, com estas palavras-chave nos nomes (sem acento):
      - **`ok`**: linha com razão social que casa exatamente um cliente "ganho" e frequência escrita pelo rótulo → status "ok", zero motivos, identificador preenchido, frequência resolvida no valor interno.
      - **`variacao`**: a mesma linha com a razão social escrita com caixa/acento/sufixo societário diferentes e a frequência em caixa alta com espaços sobrando → continua "ok", mesmo identificador, mesma frequência.
      - **`semrazao`**: razão social vazia → erro com o motivo de razão social não informada, identificador nulo.
      - **`naoencontrado`**: razão social que não casa ninguém → erro com o motivo de não encontrado, identificador nulo.
      - **`ambiguo`**: dois clientes na lista que normalizam para a mesma chave → erro com o motivo de ambiguidade e identificador **nulo**. Prova da lacuna L1.
      - **`naoganho`**: cliente encontrado com status em andamento → erro com o motivo de status, e identificador **nulo** (afirmar o nulo explicitamente: é o que impede a linha de chegar à gravação).
      - **`semfrequencia`**: frequência vazia → erro com o motivo de não informada.
      - **`frequenciainvalida`**: frequência com um texto qualquer → erro cujo motivo contém o valor cru interpolado.
      - **`multiplosmotivos`**: linha sem razão social e sem frequência → dois motivos, sem repetição.
      - **`saneamento`**: célula começando por caractere perigoso não escapa do saneamento — a razão social resolvida sai com o prefixo de escape, e a linha não casa cliente nenhum.
      - **`lote`**: a anotação de lote sobre várias linhas devolve o mesmo resultado que chamar a anotação por linha para cada uma. Prova que as duas portas de entrada não divergem.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx vitest run tests/importacao/annotarLinhaFrequencia.test.ts && test -z "$(git diff --name-only HEAD -- lib/importacao/annotarLinha.ts lib/importacao/dedupe.ts lib/funil/frequencia.ts lib/clientes/exportacao.ts app/actions/importacao.ts components/importacao/ supabase/migrations/)" && node -e "const fs=require('fs');const a=fs.readFileSync('lib/importacao/annotarLinhaFrequencia.ts','utf8');if(/use client|use server/.test(a))throw new Error('o modulo precisa ser puro');const imports=a.slice(0,a.indexOf('export type')>0?a.indexOf('export type'):1200);if(/^\s*import\s+\{[^}]*\}\s+from\s+[\"']@\/lib\/supabase/m.test(imports))throw new Error('a referencia a camada de consulta precisa ser importacao SO DE TIPO');if(!/import\s+type\s+\{[^}]*StatusAcompanhamento/.test(a))throw new Error('falta a importacao so de tipo do status de acompanhamento');if(!/normalizeRazaoSocial/.test(a))throw new Error('a comparacao precisa usar a normalizacao da deteccao de duplicados');if(/function\s+normaliz/i.test(a))throw new Error('nunca uma segunda funcao de normalizacao');if(!/sanitizeCell/.test(a))throw new Error('toda celula precisa passar pelo saneador compartilhado');if(!/@\/lib\/funil\/frequencia/.test(a))throw new Error('o vocabulario de frequencia precisa vir do modulo unico');if(/[\"']semanal[\"']|[\"']quinzenal[\"']|[\"']mensal[\"']|[\"']nenhuma[\"']/.test(a))throw new Error('o vocabulario de frequencia nao pode ser redeclarado neste arquivo');if(!/Map\s*<\s*string\s*,\s*[A-Za-z]+\[\]\s*>|Map<string, ClienteFrequenciaLookup\[\]>/.test(a))throw new Error('o indice precisa mapear chave para LISTA de clientes (e o que revela a ambiguidade)');for(const c of ['Razão social não informada','Cliente não encontrado com essa razão social','não está com status','não reconhecida','Frequência de visita não informada','Mais de um cliente encontrado'])if(!a.includes(c))throw new Error('falta o motivo: '+c);if(!/export\s+function\s+annotarLinhaFrequencia/.test(a)||!/export\s+function\s+annotarLoteFrequencia/.test(a))throw new Error('faltam as duas portas de entrada');if(/\bany\b/.test(a.replace(/\/\*[\s\S]*?\*\/|\/\/.*$/gm,'')))throw new Error('tipo curinga proibido pelo CLAUDE.md');const t=fs.readFileSync('tests/importacao/annotarLinhaFrequencia.test.ts','utf8');const n=(t.match(/\bit\(\s*[\"'][^\"']+/g)||[]).map(x=>x.toLowerCase());for(const k of ['ok','variacao','semrazao','naoencontrado','ambiguo','naoganho','semfrequencia','frequenciainvalida','multiplosmotivos','saneamento','lote'])if(!n.some(x=>x.includes(k)))throw new Error('annotarLinhaFrequencia.test.ts: falta caso com a palavra '+k);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/importacao/annotarLinhaFrequencia.ts` é puro, referencia a camada de consulta apenas por importação de tipo, e não redeclara nenhuma das quatro strings de frequência.
    - O índice mapeia chave normalizada para **lista** de clientes; ambiguidade vira erro com identificador nulo (caso `ambiguo`).
    - Cliente não-"ganho" vira erro **com identificador nulo** (caso `naoganho` afirma o nulo explicitamente).
    - Os cinco motivos do contrato de copy mais o sexto (ambiguidade) existem como constantes, com o texto exato.
    - As duas portas de entrada (por linha e por lote) produzem resultados idênticos (caso `lote`).
    - `git diff --name-only HEAD` sai vazio para os arquivos do fluxo de clientes listados.
    - `tests/importacao/annotarLinhaFrequencia.test.ts` passa com os onze casos exigidos.
    - `npx tsc --noEmit` limpo e `npm run lint` limpo.
  </acceptance_criteria>
  <done>Cada linha da planilha sai anotada com encontrado/apto/frequência antes de qualquer gravação, e nenhuma linha inapta consegue carregar identificador adiante.</done>
</task>

<task type="auto">
  <name>Task 3: Planejamento da gravação, reconciliação e as duas ações de servidor</name>
  <files>lib/importacao/confirmarFrequencia.ts, app/actions/importacaoFrequencia.ts, tests/importacao/confirmarFrequencia.test.ts</files>
  <read_first>
    - lib/importacao/confirmar.ts — o arquivo INTEIRO (205 linhas). É o molde. Extrair: o comentário de cabeçalho que registra que nada ali persiste em lugar nenhum; a **importação só de tipo** vindo do arquivo de ações de servidor; o tipo de grupo de motivos puladas; a forma do planejamento (quais linhas viram carga, quais são excluídas e sob que motivo, contagem por motivo num mapa); e a reconciliação entre o que foi enviado e o que o banco devolveu, com o grupo extra de puladas.
    - app/actions/importacao.ts — o arquivo INTEIRO (289 linhas). Extrair: a forma exata do resultado das duas ações (união discriminada com dado ou erro codificado); a checagem de sessão seguida da checagem de papel lida da tabela de perfis, ANTES de qualquer leitura de dado; o comentário que registra que as linhas enviadas pelo navegador são DADO e nunca insumo de autorização; a leitura estreita e estreitada pela regra de leitura, com o aviso explícito de nunca acrescentar filtro de dono; a chamada única à operação de banco; a reconciliação; a revalidação de caminho; e a função privada que funde dois conjuntos de grupos de motivos somando quantidades por motivo.
    - lib/importacao/annotarLinhaFrequencia.ts (criado no Task 2) — os quatro tipos e a anotação de lote.
    - lib/funil/frequencia.ts — o guarda de tipo, para a re-validação defensiva antes do envio.
    - .planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-01-SUMMARY.md (já no contexto) — a assinatura REAL da operação de banco como ela ficou depois do push, e o formato exato da carga que ela espera. Usar o que está no resumo, não o que este plano supõe.
    - .planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-UI-SPEC.md (já no contexto) — seção "IMP-01 — new files", ponto 4: a forma das duas ações e o contrato de retorno da confirmação.
    - .planning/STATE.md — o precedente registrado desde a Fase 2 de que ações de servidor não podem ser invocadas diretamente pelo Vitest (dependem do contexto de requisição do Next). É por isso que TODA a lógica testável deste passo mora no módulo puro, e a ação fica sendo só orquestração fina.
  </read_first>
  <action>
    (A) **`lib/importacao/confirmarFrequencia.ts` (novo).** Módulo puro irmão de `confirmar.ts`: sem cliente de banco, sem diretiva, importando o tipo da linha validada do arquivo de ações **só como tipo**. Reusa o tipo de grupo de motivos de `confirmar.ts` — nunca declara um segundo. Exporta:

      1. **A forma de uma entrada da carga** para a operação de banco, com as chaves exatas que ela espera (conferir no resumo do plano 17-01), tipada com o tipo de frequência do módulo único.

      2. **O planejamento da carga.** Recebe as linhas validadas e devolve a carga a enviar, os grupos de puladas e a contagem de linhas puladas. Regras:
         - linha com status de erro → pulada; **cada motivo dela conta no seu próprio grupo**, e a linha conta uma vez só no total (mesma contabilidade de `confirmar.ts`);
         - linha "ok" mas com identificador nulo ou frequência nula → pulada por segurança, sob um motivo genérico próprio. Isso não deveria acontecer (a anotação garante), mas é a rede que impede um dado meio-resolvido de virar carga;
         - **linhas apontando para o mesmo identificador de cliente (lacuna L2)**: a primeira ocorrência entra na carga, as demais são puladas sob o motivo próprio de repetição. A ordem de entrada define quem é a primeira — mesma convenção da detecção de duplicados da v1.1. Sem isso, a instrução em lote escolheria arbitrariamente qual frequência gravar quando a planilha trouxesse o mesmo cliente duas vezes com valores diferentes;
         - **re-validação defensiva** da frequência com o guarda de tipo do módulo único antes de entrar na carga; o que não passar vira pulada sob o motivo de frequência não reconhecida. É esta guarda que impede uma conversão de tipo malsucedida de abortar o lote inteiro dentro do banco.

      3. **A reconciliação.** Recebe a carga enviada e as linhas que a operação de banco devolveu, e produz a lista de atualizados (razão social, para o resumo) e o grupo extra de puladas para tudo que foi enviado e não voltou — sob um motivo que diga a verdade: o cliente deixou de estar apto entre a revisão e a gravação. É o análogo exato da corrida que `confirmar.ts` já cobre para a inserção.

      4. **A fusão de grupos de motivos** somando quantidades por motivo — mesma lógica que hoje é privada no arquivo de ações de clientes; aqui ela nasce **exportada do módulo puro**, para poder ser testada.

    Dois motivos NOVOS declarados como constantes no topo, ambos adições conscientes ao contrato de copy, com comentário justificando cada um: `Cliente repetido na planilha — só a primeira linha foi considerada` (lacuna L2) e `Cliente não estava mais apto no momento da gravação` (corrida entre revisão e confirmação).

    Comentário de cabeçalho registrando, como o irmão faz, que nada aqui persiste: os grupos de puladas só existem no valor de retorno da requisição.

    (B) **`app/actions/importacaoFrequencia.ts` (novo, arquivo de ações de servidor).** Duas ações, ambas com a mesma abertura: cliente de servidor, checagem de sessão devolvendo o código de não autenticado, e checagem de papel lida da tabela de perfis devolvendo o código de proibido — **antes de qualquer leitura de dado**. Comentário registrando que essa checagem é a mesma disciplina da ação de importação de clientes, que ela é conforto de interface, e que a trava real é o guard dentro da operação de banco do plano 17-01.

      1. **Ação de validação** (somente leitura, nunca escreve nada): lê os clientes de forma **estreita** (identificador, razão social e status de acompanhamento) e **estreitada pela regra de leitura** — sem nenhum filtro de dono e sem nenhum filtro por status na consulta (o status precisa vir para que a linha possa dizer *por que* o cliente não está apto; filtrar aqui transformaria "não é ganho" em "não encontrado", que é uma mensagem errada). Comentar essa escolha. Chamar a anotação de lote e devolver as linhas validadas com o índice de linha, status, motivos e a linha resolvida. Nenhuma escrita, nenhuma revalidação de caminho.

      2. **Ação de confirmação**: mesma abertura; chamar o planejamento; se a carga estiver vazia, devolver imediatamente sem tocar no banco; senão chamar a operação de banco **exatamente uma vez**, com a carga inteira; em erro, devolver o código genérico; em sucesso, reconciliar, fundir os grupos de puladas e devolver `{ atualizados, puladas }`. Revalidar o caminho da lista de clientes ao final, como a ação irmã faz.

      Nunca um laço de chamadas ao banco. Nunca um identificador vindo direto de célula de planilha: os identificadores da carga são os que a ação de validação resolveu. Nenhuma exportação que não seja função assíncrona ou tipo (restrição do próprio Next para arquivos de ações de servidor) — é por isso que as constantes de motivo e a fusão de grupos moram no módulo puro.

    (C) **`tests/importacao/confirmarFrequencia.test.ts` (novo, puro).** Casos exigidos, com estas palavras-chave nos nomes (sem acento):
      - **`carga`**: linhas "ok" viram entradas de carga com as chaves exatas que a operação de banco espera, e nada mais.
      - **`erro`**: linha com status de erro não entra na carga; cada motivo dela vira um grupo, e a contagem total de linhas puladas conta a linha uma vez só.
      - **`repetido`**: três linhas apontando para o mesmo identificador → só a primeira entra na carga; as outras duas viram um grupo de puladas com quantidade dois, sob o motivo de repetição. Prova da lacuna L2.
      - **`incompleto`**: linha marcada "ok" mas com identificador nulo (ou frequência nula) não entra na carga e vira pulada. Prova da rede de segurança.
      - **`frequenciainvalida`**: linha marcada "ok" com uma frequência fora do vocabulário não entra na carga. Prova da re-validação defensiva que protege o lote.
      - **`reconcilia`**: enviadas três entradas e devolvidas duas → dois atualizados com as razões sociais devolvidas e um grupo extra de puladas com quantidade um, sob o motivo de deixou de estar apto.
      - **`funde`**: dois conjuntos de grupos com um motivo em comum viram um grupo só com as quantidades somadas.
      - **`vazio`**: nenhuma linha "ok" → carga vazia e nenhuma exceção.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx vitest run tests/importacao/confirmarFrequencia.test.ts tests/importacao/confirmar.test.ts && test -z "$(git diff --name-only HEAD -- lib/importacao/confirmar.ts app/actions/importacao.ts components/importacao/ supabase/migrations/)" && node -e "const fs=require('fs');const c=fs.readFileSync('lib/importacao/confirmarFrequencia.ts','utf8');if(/use client|use server/.test(c))throw new Error('o modulo precisa ser puro');if(!/import\s+type\s/.test(c))throw new Error('a referencia ao arquivo de acoes precisa ser importacao so de tipo');if(/from\s+[\"']@\/lib\/supabase/.test(c))throw new Error('nenhum cliente de banco no modulo puro');if(!/from\s+[\"']@\/lib\/importacao\/confirmar[\"']/.test(c))throw new Error('o tipo de grupo de motivos precisa ser reusado, nunca redeclarado');if(/type\s+PuladaGroup\s*=/.test(c))throw new Error('nao pode existir um segundo tipo de grupo de motivos');for(const s of ['Cliente repetido na planilha','não estava mais apto'])if(!c.includes(s))throw new Error('falta o motivo: '+s);if(!/isFrequenciaVisita/.test(c))throw new Error('falta a re-validacao defensiva com o guarda de tipo do modulo unico');const a=fs.readFileSync('app/actions/importacaoFrequencia.ts','utf8');if(!/^\s*[\"']use server[\"']/m.test(a))throw new Error('falta a diretiva de acoes de servidor');const val=a.slice(a.indexOf('export async function validarLoteFrequencia'),a.indexOf('export async function confirmarLoteFrequencia'));const con=a.slice(a.indexOf('export async function confirmarLoteFrequencia'));for(const [nome,bloco] of [['validar',val],['confirmar',con]]){if(!/auth\.getUser\(\)/.test(bloco))throw new Error(nome+': falta a checagem de sessao');if(!/role[\s\S]{0,120}supervisor/.test(bloco))throw new Error(nome+': falta a checagem de papel');const iRole=bloco.indexOf('supervisor');const iFrom=bloco.indexOf('.from(\"clientes\")');if(iFrom>=0&&iRole>iFrom)throw new Error(nome+': a checagem de papel precisa vir ANTES de qualquer leitura de dado')}if(/\.insert\(|\.update\(|\.delete\(|\.upsert\(|\.rpc\(/.test(val))throw new Error('a acao de validacao e somente leitura');if(/eq\(\s*[\"']responsavel[\"']/.test(a))throw new Error('nunca filtro de dono: a regra de leitura e a fronteira');if(/eq\(\s*[\"']status_acompanhamento[\"']/.test(val))throw new Error('a leitura de validacao nao pode filtrar por status: o status precisa chegar para explicar por que a linha nao esta apta');if((con.match(/\.rpc\(/g)||[]).length!==1)throw new Error('a gravacao precisa ser UMA chamada por lote, nunca uma por linha');if(/for\s*\([\s\S]{0,120}\.rpc\(|\.map\([\s\S]{0,120}\.rpc\(/.test(con))throw new Error('nenhum laco de chamadas ao banco');if(!/atualizar_frequencia_visita_lote/.test(con))throw new Error('a confirmacao precisa chamar a operacao de banco do plano 17-01');if(/\bany\b/.test(a.replace(/\/\*[\s\S]*?\*\/|\/\/.*$/gm,'')))throw new Error('tipo curinga proibido pelo CLAUDE.md');const t=fs.readFileSync('tests/importacao/confirmarFrequencia.test.ts','utf8');const n=(t.match(/\bit\(\s*[\"'][^\"']+/g)||[]).map(x=>x.toLowerCase());for(const k of ['carga','erro','repetido','incompleto','frequenciainvalida','reconcilia','funde','vazio'])if(!n.some(x=>x.includes(k)))throw new Error('confirmarFrequencia.test.ts: falta caso com a palavra '+k);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/importacao/confirmarFrequencia.ts` é puro, reusa o tipo de grupo de motivos do módulo irmão (não redeclara), e re-valida a frequência com o guarda de tipo antes de montar a carga.
    - Identificador repetido na planilha → só a primeira linha entra na carga (caso `repetido`); linha "ok" incompleta ou com frequência inválida nunca entra (casos `incompleto` e `frequenciainvalida`).
    - As duas ações checam sessão **e** papel antes de qualquer leitura de dado; a de validação não escreve nada e não filtra por dono nem por status.
    - A confirmação faz **exatamente uma** chamada à operação de banco, sem laço.
    - `git diff --name-only HEAD` sai vazio para `lib/importacao/confirmar.ts`, `app/actions/importacao.ts`, `components/importacao/` e `supabase/migrations/`; `tests/importacao/confirmar.test.ts` continua verde.
    - `tests/importacao/confirmarFrequencia.test.ts` passa com os oito casos exigidos.
    - `npx tsc --noEmit` limpo e `npm run lint` limpo.
  </acceptance_criteria>
  <done>A camada não visual do fluxo de planilha está completa: valida sem escrever, grava numa chamada só, e conta honestamente quantas linhas entraram e quantas foram puladas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planilha do Supervisor → anotação | Células arbitrárias, incluindo texto que um programa de planilha interpretaria como fórmula. |
| navegador → ações de servidor | As linhas validadas voltam do navegador para a confirmação; podem ter sido adulteradas no caminho. |
| ações de servidor → operação de banco | A carga do lote; conversão de tipo pode abortar o comando inteiro. |
| papel do chamador → permissão de uso do fluxo | Só Supervisor; a checagem aqui é conforto, a trava é no banco. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-20 | Tampering (integridade de dado) | frequência gravada no cliente errado por colisão de normalização de razão social | high | mitigate | O índice mapeia chave para LISTA de clientes; mais de um resultado vira erro com identificador nulo, e a linha nunca chega à carga. Verificado mecanicamente (forma do índice + presença do motivo) e provado pelo caso `ambiguo`. Lacuna L1, adição consciente ao contrato de copy. |
| T-17-21 | Tampering (integridade de dado) | gravação não determinística quando a planilha repete o mesmo cliente com frequências diferentes | high | mitigate | O planejamento deduplica por identificador antes de montar a carga: a primeira ocorrência vence e as demais entram no resumo como puladas com motivo próprio. Provado pelo caso `repetido`. Lacuna L2. |
| T-17-22 | Elevation of Privilege | linha da planilha carregando identificador de cliente forjado | critical | mitigate | Nenhum identificador vem de célula: eles são resolvidos exclusivamente contra a leitura de clientes feita no servidor, já estreitada pela regra de leitura. Uma linha inapta tem o identificador zerado antes de sair da anotação (caso `naoganho` afirma o nulo), e o planejamento descarta qualquer linha com identificador nulo (caso `incompleto`). Terceira camada: a operação de banco só casa identificador existente e "ganho". |
| T-17-23 | Elevation of Privilege | Vendedor usando as ações por fora da tela | high | mitigate | Checagem de sessão e de papel antes de qualquer leitura, nas duas ações, verificada mecanicamente inclusive na ORDEM (papel antes de leitura). É defesa em profundidade: a trava real é o guard da operação de banco, já provado contra o banco real no plano 17-01. |
| T-17-24 | Denial of Service | conversão de tipo malsucedida abortando o lote inteiro no banco | high | mitigate | Re-validação defensiva com o guarda de tipo do módulo único antes de a linha entrar na carga (caso `frequenciainvalida`), somada à anotação que já marcou a linha como erro na revisão. Duas camadas antes do banco. |
| T-17-25 | Denial of Service | gravação linha a linha estourando o limite de ~10s da hospedagem | high | mitigate | Verificação mecânica exige exatamente uma chamada à operação de banco na confirmação e recusa laço de chamadas. A anotação constrói o índice de clientes uma vez por lote, evitando custo quadrático numa planilha grande. |
| T-17-26 | Tampering (injeção em planilha) | célula perigosa atravessando a anotação sem escape | medium | mitigate | Toda célula definida passa pelo saneador compartilhado antes de qualquer comparação, mesma guarda de `annotarLinha.ts`; verificado mecanicamente e provado pelo caso `saneamento`. O saneador é importado, nunca reimplementado. |
| T-17-27 | Information Disclosure | Supervisor descobrindo clientes pela leitura de validação | low | accept | O Supervisor já enxerga todos os clientes pela regra de leitura da migration 0002 — a leitura de validação não amplia nada. A ação é recusada para qualquer outro papel antes da leitura. |
| T-17-28 | Repudiation | resumo mentindo sobre o que foi gravado | medium | mitigate | A lista de atualizados vem do que a operação de banco DEVOLVEU, nunca do que foi enviado; tudo que foi enviado e não voltou vira pulada sob um motivo verdadeiro. Provado pelo caso `reconcilia`. |
| T-17-29 | Tampering | regressão silenciosa no fluxo de importação de clientes ao generalizar a camada de mapeamento | high | mitigate | Os arquivos de teste existentes de mapeamento e de modelo ficam **literalmente intocados** e precisam continuar verdes (verificado por diff vazio + execução). O conserto da sugestão por apelido é provado neutro para a lista de 14 pelo caso `apelidopreservado`. Os oito arquivos do fluxo de clientes saem vazios no diff. |
| T-17-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` neste plano. Zero pacotes novos, nada a auditar. |
</threat_model>

<verification>
- Os três arquivos de teste novos passam; `tests/importacao/mapping.test.ts`, `tests/importacao/modelo.test.ts` e `tests/importacao/confirmar.test.ts` continuam verdes **sem uma linha alterada**.
- A verificação mecânica confirma: definição de campo parametrizada com padrão retrocompatível e união de 14 intacta; apelidos conferidos contra a lista recebida; módulos novos puros, sem redeclarar o vocabulário de frequência e sem segunda função de normalização; índice por lista; identificador nulo para linha inapta; deduplicação por identificador; re-validação defensiva; papel checado antes de leitura; exatamente uma chamada de gravação por lote.
- `git diff --name-only HEAD` sai vazio para todo o fluxo de importação de clientes, para os componentes e para as migrations.
</verification>

<success_criteria>
- A camada não visual do IMP-01 está completa e testada sem tela.
- Nenhuma linha inapta, ambígua, repetida ou com frequência inválida consegue chegar ao banco.
- O fluxo de importação de clientes continua idêntico, provado por arquivos de teste intocados e verdes.
- Os planos 17-04 e 17-05 estão desbloqueados.
</success_criteria>

<output>
Create `.planning/phases/17-planilhas-frequ-ncia-em-massa-e-exporta-o-do-di-rio/17-03-SUMMARY.md` when done
</output>
