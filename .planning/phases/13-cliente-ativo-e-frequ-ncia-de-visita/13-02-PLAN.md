---
phase: 13-cliente-ativo-e-frequ-ncia-de-visita
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - lib/funil/frequencia.ts
  - lib/supabase/queries/clientes.ts
  - app/actions/funil.ts
  - components/clientes/GanhoFrequenciaDialog.tsx
  - components/clientes/ClienteDetailSheet.tsx
  - tests/clientes/ganho-frequencia-dialog.test.tsx
autonomous: true
requirements: [VIS-01]

must_haves:
  truths:
    - "VIS-01: escolher 'Ganho' no Select de Status dentro do ClienteDetailSheet abre um diálogo pedindo a frequência de visita ANTES de qualquer gravação — a mudança de status não acontece se o diálogo for cancelado"
    - "VIS-01: o diálogo só permite confirmar depois que uma das quatro opções foi escolhida; o botão de confirmar nasce desabilitado"
    - "VIS-01: confirmar chama a mesma Server Action `marcarStatus` que o fluxo de 'perdido' já usa, passando a frequência adiante até o parâmetro `p_frequencia_visita` do RPC `mover_card_funil` — nunca um UPDATE cru em `clientes`"
    - "O vocabulário das quatro frequências vive em UM único módulo TypeScript (`lib/funil/frequencia.ts`), na ordem travada pelo UI-SPEC (Semanal, Quinzenal, Mensal, Nenhuma), reusado pelo diálogo desta fase e pelo controle permanente do plano 13-03"
    - "O botão de confirmar do diálogo usa a variante primária do Button, deliberadamente diferente do `PerdaMotivoDialog` (que é destrutivo) — marcar ganho é um desfecho positivo (UI-SPEC, seção Color)"
    - "Toda a copy do diálogo bate literalmente com o Copywriting Contract do 13-UI-SPEC.md, sem texto inventado"
    - "`ClienteDetalhe` passa a carregar `frequenciaVisita`, lida do banco no mesmo `select()` que já busca o resto da ficha — sem query extra"
    - "Depois de confirmar o ganho, o estado local do Sheet reflete a frequência escolhida, para que o controle permanente do plano 13-03 abra já mostrando o valor certo sem recarregar a ficha"
  artifacts:
    - "lib/funil/frequencia.ts (novo) — FREQUENCIAS_VISITA, FrequenciaVisita, FREQUENCIA_VISITA_LABELS, FREQUENCIA_VISITA_ITEMS"
    - "components/clientes/GanhoFrequenciaDialog.tsx (novo)"
    - "lib/supabase/queries/clientes.ts (modificado) — frequenciaVisita em ClienteDetalhe + coluna no select"
    - "app/actions/funil.ts (modificado) — marcarStatus com 4º parâmetro frequenciaVisita + código de erro frequencia_obrigatoria"
    - "components/clientes/ClienteDetailSheet.tsx (modificado) — ramo 'ganho' em handleStatusSelect, 3º parâmetro em handleStatusChange, render do diálogo novo"
    - "tests/clientes/ganho-frequencia-dialog.test.tsx (novo)"
  key_links:
    - "`handleStatusSelect` → `setGanhoDialogOpen(true)` com `return` antes de `handleStatusChange` — é o que garante que nada é gravado antes de o vendedor escolher a frequência (espelho exato do ramo 'perdido' que já existe)"
    - "`onConfirm={(frequencia) => handleStatusChange('ganho', undefined, frequencia)}` — o mesmo formato de retorno de `PerdaMotivoDialog`, que é o que faz o diálogo saber se fecha ou mostra erro"
    - "`p_frequencia_visita` no `supabase.rpc('mover_card_funil', ...)` dentro de `marcarStatus` — sem isso o guard do banco (13-01) recusa toda marcação de ganho"
    - "`frequencia_visita` acrescentado à string de `select()` de `getClienteById` — é o que o controle permanente do 13-03 vai ler"
---

<objective>
Ligar a exigência de frequência que o banco passou a impor (13-01) ao fluxo real do vendedor: um diálogo novo que aparece quando ele escolhe "Ganho" no Select de Status da ficha do cliente, espelhando estruturalmente o `PerdaMotivoDialog` que já existe para o "perdido", mais o encanamento de dados que leva a frequência escolhida até o RPC e traz o valor gravado de volta para a tela.

Purpose: depois do 13-01, o banco recusa marcar ganho sem frequência — este plano é o que devolve essa operação ao vendedor, agora capturando a cadência de pós-venda no mesmo gesto. Também cria o módulo de vocabulário das frequências que o plano 13-03 e as Fases 14-17 vão reusar, para o nome das opções nunca divergir entre telas.

Output: um componente de diálogo novo, um módulo de vocabulário, três arquivos existentes estendidos e um teste de render novo.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-cliente-ativo-e-frequ-ncia-de-visita/13-UI-SPEC.md
@components/clientes/PerdaMotivoDialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vocabulário das frequências, leitura da coluna nova e passagem da frequência até o RPC</name>
  <files>lib/funil/frequencia.ts, lib/supabase/queries/clientes.ts, app/actions/funil.ts</files>
  <read_first>
    - lib/funil/etapas.ts — o formato de módulo de vocabulário já usado neste projeto (array `as const` + type derivado + mapa de rótulos). O arquivo novo tem que seguir a mesma forma, não inventar outra.
    - lib/supabase/queries/clientes.ts, linhas ~60 a ~165 — o type `ClienteDetalhe`, o type `ClienteDetalheRow`, a string de `select()` de `getClienteById` e o objeto de mapeamento retornado. São os quatro pontos exatos a estender.
    - app/actions/funil.ts (arquivo inteiro, ~220 linhas) — `MarcarStatusErrorCode`, `MarcarStatusResult`, o corpo de `marcarStatus` (pré-checagens de `ganho_travado` e `motivo_obrigatorio`, a chamada `supabase.rpc('mover_card_funil', ...)` e o `revalidatePath`). O parâmetro novo entra espelhando exatamente como `motivoPerdaId` já é tratado.
    - .planning/phases/13-cliente-ativo-e-frequ-ncia-de-visita/13-UI-SPEC.md — seção "Copywriting Contract", tabela do diálogo novo: a linha "Select options (in this order)" trava a ordem e os rótulos; a linha "Validation error" trava a mensagem a reusar aqui.
    - .planning/phases/13-cliente-ativo-e-frequ-ncia-de-visita/13-01-SUMMARY.md — confirmar o nome final do enum no banco e a ordem/nome exatos dos parâmetros de `mover_card_funil` depois da recriação.
  </read_first>
  <action>
    Três arquivos.

    **(A) `lib/funil/frequencia.ts` (novo).** Módulo de vocabulário puro, sem nenhum import de Supabase, React ou Next — ele vai ser importado tanto por Client Component quanto por Server Action, então não pode arrastar dependência de servidor. Exportar:
      - `FREQUENCIAS_VISITA` — array `as const` com exatamente `semanal`, `quinzenal`, `mensal`, `nenhuma`, NESSA ordem (é a ordem de exibição travada no UI-SPEC, e é a mesma ordem do enum criado no 13-01).
      - `FrequenciaVisita` — type derivado do array (`(typeof FREQUENCIAS_VISITA)[number]`).
      - `FREQUENCIA_VISITA_LABELS` — `Record<FrequenciaVisita, string>` com os rótulos exatos `Semanal`, `Quinzenal`, `Mensal`, `Nenhuma`.
      - `FREQUENCIA_VISITA_ITEMS` — derivado dos dois acima, no formato `{ value, label }[]`, que é o formato que o `items` do Select do Base UI espera (ver o comentário sobre `items` em `ClienteDetailSheet.tsx`, ~linha 95).
      - `isFrequenciaVisita(value: unknown): value is FrequenciaVisita` — guarda de tipo simples, usada pela Server Action para re-validar o que vem do navegador.
      Comentar no topo que este é o vocabulário único das frequências de visita do projeto (ATV-03 no nível de código: um valor só, um lugar só) e que as Fases 14 a 17 devem importar daqui em vez de redeclarar as strings.

    **(B) `lib/supabase/queries/clientes.ts`.** Quatro edições cirúrgicas, sem tocar em mais nada do arquivo:
      1. Importar `FrequenciaVisita` do módulo novo.
      2. Acrescentar `frequenciaVisita: FrequenciaVisita | null` ao type `ClienteDetalhe`, no bloco de campos da seção Funil (junto de `statusAcompanhamento`/`motivoPerdaId`/`observacao`), com um comentário de uma linha dizendo que é a cadência de pós-venda (VIS-01/VIS-02/ATV-03) e que ela vale para qualquer status, mas só é editável na ficha quando o cliente está ganho.
      3. Acrescentar `frequencia_visita: FrequenciaVisita | null` ao type `ClienteDetalheRow` e a coluna `frequencia_visita` à string de `select()` de `getClienteById` — no fim da lista, sem reordenar as colunas existentes.
      4. Mapear `frequenciaVisita: row.frequencia_visita` no objeto retornado.
      NÃO acrescentar `nome_fantasia`, `cnpj` nem `frequencia_pedidos` aqui — essas três colunas são da Fase 16 e ler agora seria carregar dado que nenhuma tela desta fase usa.

    **(C) `app/actions/funil.ts`.** Estender `marcarStatus`:
      - Acrescentar `frequencia_obrigatoria` à união `MarcarStatusErrorCode`.
      - Acrescentar um 4º parâmetro opcional `frequenciaVisita?: FrequenciaVisita` à assinatura de `marcarStatus`, DEPOIS de `motivoPerdaId` (para não quebrar nenhuma chamada existente por posição).
      - Acrescentar uma pré-checagem logo depois da de `motivo_obrigatorio`, no mesmo formato: se `novoStatus === "ganho"` e a frequência estiver ausente ou não passar por `isFrequenciaVisita`, retornar `{ error: { code: "frequencia_obrigatoria", message: "Selecione a frequência de visita antes de confirmar." } }`. Essa string é a "Validation error" travada no Copywriting Contract do UI-SPEC — usar exatamente ela, sem reescrever. Comentar que esta pré-checagem é apenas uma mensagem mais amigável: o guard REAL é o do RPC criado no 13-01, que continua sendo o backstop se este caminho for contornado (mesma relação que o comentário existente já descreve entre as pré-checagens e as CHECK constraints).
      - Acrescentar `p_frequencia_visita: novoStatus === "ganho" ? frequenciaVisita : null` ao objeto passado para `supabase.rpc("mover_card_funil", ...)`, espelhando exatamente a forma condicional que `p_motivo_perda_id` já usa. Passar `null` fora do caso ganho é obrigatório: o RPC faz `coalesce`, então mandar `null` preserva o valor existente em vez de apagá-lo.
      - Não alterar `moverCard`, `getHistoricoAction` nem `getMotivosPerda`.

    Não criar nenhum componente neste task e não tocar em `ClienteDetailSheet.tsx` — isso é o Task 2.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint lib/funil/frequencia.ts lib/supabase/queries/clientes.ts app/actions/funil.ts && node -e "const fs=require('fs');const f=fs.readFileSync('lib/funil/frequencia.ts','utf8');for(const t of ['FREQUENCIAS_VISITA','FrequenciaVisita','FREQUENCIA_VISITA_LABELS','FREQUENCIA_VISITA_ITEMS','isFrequenciaVisita','as const'])if(!f.includes(t))throw new Error('lib/funil/frequencia.ts: falta '+t);const ordem=(f.match(/'(semanal|quinzenal|mensal|nenhuma)'/g)||[]).map(s=>s.replace(/'/g,''));const primeiras=[];for(const v of ordem){if(!primeiras.includes(v))primeiras.push(v)}if(primeiras.join(',')!=='semanal,quinzenal,mensal,nenhuma')throw new Error('ordem das frequencias travada pelo UI-SPEC quebrada: '+primeiras.join(','));for(const l of ['Semanal','Quinzenal','Mensal','Nenhuma'])if(!f.includes(l))throw new Error('falta o rotulo '+l);if(/from ['\"]@?[./]*(next|react|@supabase)/.test(f))throw new Error('o modulo de vocabulario tem que ser puro, sem import de next/react/supabase');const q=fs.readFileSync('lib/supabase/queries/clientes.ts','utf8');if(!q.includes('frequenciaVisita'))throw new Error('ClienteDetalhe precisa de frequenciaVisita');const sel=q.match(/\.select\(\s*[\s\S]{0,900}?\)/g)||[];if(!sel.some(s=>s.includes('frequencia_visita')))throw new Error('a coluna frequencia_visita nao entrou em nenhum select()');for(const proibida of ['nome_fantasia','frequencia_pedidos'])if(q.includes(proibida))throw new Error('coluna da Fase 16 nao deve ser lida aqui: '+proibida);const a=fs.readFileSync('app/actions/funil.ts','utf8');for(const t of ['frequencia_obrigatoria','p_frequencia_visita','isFrequenciaVisita','Selecione a frequencia de visita antes de confirmar.'.replace('frequencia','frequ\u00eancia')])if(!a.includes(t))throw new Error('app/actions/funil.ts: falta '+t);if(!/p_frequencia_visita\s*:\s*novoStatus\s*===\s*[\"']ganho[\"']/.test(a))throw new Error('p_frequencia_visita tem que ser condicional ao ganho, mandando null fora dele');console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/funil/frequencia.ts` existe, é um módulo puro (nenhum import de `next`, `react` ou `@supabase`), e exporta `FREQUENCIAS_VISITA` (array `as const`), `FrequenciaVisita`, `FREQUENCIA_VISITA_LABELS`, `FREQUENCIA_VISITA_ITEMS` e `isFrequenciaVisita`.
    - A ordem dos quatro valores no arquivo é `semanal, quinzenal, mensal, nenhuma`, e os quatro rótulos `Semanal`, `Quinzenal`, `Mensal`, `Nenhuma` estão presentes.
    - `ClienteDetalhe` tem `frequenciaVisita`, e `frequencia_visita` aparece na string de `select()` de `getClienteById`.
    - `lib/supabase/queries/clientes.ts` NÃO passa a ler `nome_fantasia` nem `frequencia_pedidos` (colunas da Fase 16).
    - `app/actions/funil.ts` tem o código de erro `frequencia_obrigatoria`, usa `isFrequenciaVisita` na pré-checagem, carrega a mensagem de validação exata do UI-SPEC, e envia `p_frequencia_visita` de forma condicional ao status ganho (null fora dele).
    - `npx tsc --noEmit` limpo e `npx eslint` limpo nos três arquivos.
  </acceptance_criteria>
  <done>Vocabulário único das frequências criado, `ClienteDetalhe` lendo a coluna nova e `marcarStatus` capaz de levar a frequência até o RPC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GanhoFrequenciaDialog e a ligação dele ao Select de Status da ficha</name>
  <files>components/clientes/GanhoFrequenciaDialog.tsx, components/clientes/ClienteDetailSheet.tsx, tests/clientes/ganho-frequencia-dialog.test.tsx</files>
  <read_first>
    - components/clientes/PerdaMotivoDialog.tsx (já no contexto, arquivo inteiro) — é o molde estrutural literal: props, estados locais (`selectedX`, `validationError`, `submitError`, `isSubmitting`), `handleOpenChange` que reseta ao fechar, `handleConfirm` com a guarda defensiva + try/catch, e a árvore JSX (DialogHeader/DialogTitle, bloco `flex flex-col gap-1.5` com Label + Select + slot de mensagem, bloco de `submitError`, DialogFooter com Cancelar + Confirmar).
    - .planning/phases/13-cliente-ativo-e-frequ-ncia-de-visita/13-UI-SPEC.md (já no contexto) — a tabela do Copywriting Contract do diálogo (cada linha é uma string literal a reproduzir), a nota da seção Color sobre a variante do botão de confirmar, a nota de Spacing sobre `gap-1.5`, e o "Interaction Contract" completo (pontos 1 a 4), que descreve exatamente as edições no `ClienteDetailSheet`.
    - components/clientes/ClienteDetailSheet.tsx — os pontos exatos a editar: o bloco de imports (~linhas 60-70), a declaração de estado `perdaDialogOpen` (~linha 192), o bloco de reset dentro do efeito (~linhas 236-251), `handleStatusChange` (~linhas 303-342), `handleStatusSelect` (~linhas 344-352) e o render do `PerdaMotivoDialog` no fim do arquivo (~linhas 1108-1115).
    - tests/clientes/estado-cidade-fields.test.tsx — o molde de teste de render deste projeto: diretiva `// @vitest-environment jsdom` na primeira linha, imports explícitos do `vitest` (não há globais), e a ressalva documentada de que interagir com o popup de Select do Base UI dentro do jsdom é instável — por isso as asserções ficam no que já está no DOM sem abrir o popup.
    - tests/setup.ts — o `cleanup()` global no `afterEach`.
  </read_first>
  <behavior>
    - Aberto para um cliente chamado "Padaria Central", o diálogo mostra o título `Marcar Padaria Central como ganho?`.
    - Com nada escolhido, o texto de apoio `Escolha a frequência de visita para este cliente.` está visível e o botão `Confirmar ganho` está desabilitado.
    - O botão `Confirmar ganho` NÃO carrega a variante destrutiva (é a variante primária) — é a diferença deliberada em relação ao `PerdaMotivoDialog`.
    - O botão `Cancelar` existe, está habilitado, e clicar nele pede o fechamento do diálogo sem chamar `onConfirm`.
    - O vocabulário exposto pelo módulo de frequências tem exatamente quatro itens, na ordem Semanal → Quinzenal → Mensal → Nenhuma.
    - O diálogo não tem estado de carregamento nenhum (as opções são estáticas, não vêm do banco).
  </behavior>
  <action>
    **(A) `components/clientes/GanhoFrequenciaDialog.tsx` (novo).** `"use client"`. Copiar a ESTRUTURA do `PerdaMotivoDialog` e aplicar as duas diferenças deliberadas que o UI-SPEC registra:
      - **Sem carregamento assíncrono**: as opções vêm de `FREQUENCIA_VISITA_ITEMS` (import de `@/lib/funil/frequencia`), não de uma Server Action. Portanto NÃO existe `useEffect`, nem estado de carregamento, nem estado de erro de carregamento, nem o texto de espera que o `PerdaMotivoDialog` mostra enquanto busca os motivos, nem a condição `isLoading` no `disabled` do botão.
      - **Botão de confirmar na variante primária**, nunca na destrutiva. É a decisão registrada na seção Color do UI-SPEC: marcar ganho é desfecho positivo. Na prática: não passar prop de variante para esse `Button` (o padrão do componente já é a primária).
      <!-- planner-discipline-allow: variant="destructive" -->

      Props: `open`, `onOpenChange`, `razaoSocial`, e `onConfirm: (frequencia: FrequenciaVisita) => Promise<{ error?: { message: string } } | undefined>`. O UI-SPEC descreve `onConfirm` recebendo uma string; tipar com `FrequenciaVisita` em vez de `string` é a mesma forma com tipagem estrita, exigida pelo CLAUDE.md — registrar isso num comentário de uma linha.

      Estado local: `selectedFrequencia`, `validationError`, `submitError`, `isSubmitting` — mesmos nomes/semântica do molde. `handleOpenChange(next)` reseta os três primeiros ao fechar (nunca um `useEffect` reagindo a `open`, pela mesma razão comentada no molde). `handleConfirm` mantém a guarda defensiva (botão já é `disabled`, mas um Enter perdido não pode passar), o `try/catch` e o fechamento em caso de sucesso.

      Copy — reproduzir LITERALMENTE, sem reescrever, do Copywriting Contract:
      - Título: `Marcar {razaoSocial} como ganho?`
      - Label: `Frequência de visita`, com `htmlFor` apontando para o id do trigger do Select (usar `ganho-frequencia-select`, no mesmo estilo de `perda-motivo-select`)
      - Placeholder do Select: `Selecione a frequência`
      - Opções: os rótulos de `FREQUENCIA_VISITA_ITEMS`, nessa ordem
      - Texto de apoio (mesmo slot em que o molde mostra a mensagem ociosa, ou seja, quando não há erro e nada foi escolhido): `Escolha a frequência de visita para este cliente.`
      - Erro de validação: `Selecione a frequência de visita antes de confirmar.`
      - Erro de gravação (fallback do catch): `Não foi possível salvar as alterações. Tente novamente.` — declarar como a constante `GENERIC_ERROR` do arquivo, igual ao molde
      - Cancelar: `Cancelar`
      - Confirmar: `Confirmar ganho` / `Salvando...`

      Estilo: o bloco do campo é `flex flex-col gap-1.5` e o `SelectTrigger` leva `className="w-full"` — idênticos ao molde (UI-SPEC, seção Spacing). Nenhuma classe de cor nova, nenhum tamanho de fonte novo. O `Select` NÃO precisa da prop `items`: ele sempre monta sem valor escolhido, que é exatamente o caso em que o comentário de `ClienteDetailSheet.tsx` (~linha 95) diz que `items` é dispensável.

    **(B) `components/clientes/ClienteDetailSheet.tsx`.** Cinco edições cirúrgicas, seguindo o Interaction Contract do UI-SPEC. Nada além disso neste arquivo — o controle permanente de frequência é do plano 13-03.
      1. Importar `GanhoFrequenciaDialog` (ao lado do import do `PerdaMotivoDialog`) e `FrequenciaVisita` de `@/lib/funil/frequencia`.
      2. Declarar `const [ganhoDialogOpen, setGanhoDialogOpen] = useState(false)` logo abaixo de `perdaDialogOpen`.
      3. No bloco de reset dentro do efeito de abertura/fechamento, acrescentar `setGanhoDialogOpen(false)` na mesma lista onde `setPerdaDialogOpen(false)` já está. NÃO acrescentar nenhuma diretiva de supressão de lint nova: a que já existe ali cobre o bloco inteiro, e o débito documentado desse arquivo não pode piorar nesta fase.
      4. `handleStatusChange` ganha um 3º parâmetro opcional `frequenciaVisita?: FrequenciaVisita`, repassado para `marcarStatus(cliente.id, novoStatus, motivoPerdaId, frequenciaVisita)`. No `setCliente` de sucesso, além de `statusAcompanhamento` e `motivoPerdaId` (que continuam como estão), atualizar `frequenciaVisita` para o valor escolhido quando o status novo for ganho, e preservar o valor anterior nos demais casos — mudar para "em andamento" ou "perdido" NÃO apaga a cadência gravada, do mesmo jeito que o RPC também não apaga (ele usa `coalesce`).
      5. `handleStatusSelect` ganha, logo abaixo do ramo de `perdido` e antes da chamada direta, o ramo: se o status novo for `ganho`, abrir `ganhoDialogOpen` e retornar sem gravar nada.
      6. No fim do arquivo, ao lado do `PerdaMotivoDialog` já renderizado sob a guarda `{cliente ? ... : null}`, renderizar o `GanhoFrequenciaDialog` com `open={ganhoDialogOpen}`, `onOpenChange={setGanhoDialogOpen}`, `razaoSocial={cliente.razaoSocial}` e `onConfirm={(frequencia) => handleStatusChange("ganho", undefined, frequencia)}`.

      O `SelectItem` de "ganho" continua desabilitado fora da etapa final, com o Tooltip que já existe — isso não muda.

    **(C) `tests/clientes/ganho-frequencia-dialog.test.tsx` (novo).** Primeira linha: `// @vitest-environment jsdom`. Imports explícitos de `describe`/`expect`/`it`/`vi` do `vitest` e de `render`/`screen`/`fireEvent` do `@testing-library/react`. Nenhum mock de Supabase é necessário — o diálogo não faz I/O; `onConfirm` e `onOpenChange` são `vi.fn()`.

    Casos exigidos, com estas palavras-chave (sem acento) nos nomes dos `it()`:
      - **`copy`**: renderizar com `razaoSocial="Padaria Central"` e `open`; afirmar que o título `Marcar Padaria Central como ganho?`, o label `Frequência de visita` e o texto de apoio `Escolha a frequência de visita para este cliente.` estão no documento.
      - **`desabilitado`**: com nada escolhido, o botão de nome acessível `Confirmar ganho` está desabilitado e o `Cancelar` não está.
      - **`variante`**: recuperar o botão `Confirmar ganho` e afirmar que o `className` dele NÃO contém `destructive` — trava a decisão da seção Color do UI-SPEC contra uma cópia distraída do molde.
      - **`cancelar`**: clicar em `Cancelar` chama `onOpenChange` com `false` e NUNCA chama `onConfirm`.
      - **`vocabulario`**: importar `FREQUENCIA_VISITA_ITEMS` de `@/lib/funil/frequencia` e afirmar que os rótulos, em ordem, são exatamente `["Semanal","Quinzenal","Mensal","Nenhuma"]` e os valores `["semanal","quinzenal","mensal","nenhuma"]`. É asserção pura, sem DOM — deliberadamente aqui e não no popup do Select, porque abrir o popup do Base UI dentro do jsdom é instável (ressalva já documentada em `estado-cidade-fields.test.tsx`).

    Não escrever teste de interação que dependa de abrir o popup do Select; a verificação de que escolher uma opção habilita o botão fica para a passagem manual do checkpoint humano do plano 13-03.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint components/clientes/GanhoFrequenciaDialog.tsx components/clientes/ClienteDetailSheet.tsx && npx vitest run tests/clientes/ganho-frequencia-dialog.test.tsx && node -e "const fs=require('fs');const d=fs.readFileSync('components/clientes/GanhoFrequenciaDialog.tsx','utf8');const copy=['Marcar ','como ganho?','Frequ\u00eancia de visita','Selecione a frequ\u00eancia','Escolha a frequ\u00eancia de visita para este cliente.','Selecione a frequ\u00eancia de visita antes de confirmar.','N\u00e3o foi poss\u00edvel salvar as altera\u00e7\u00f5es. Tente novamente.','Cancelar','Confirmar ganho','Salvando...'];for(const c of copy)if(!d.includes(c))throw new Error('copy do UI-SPEC ausente no dialogo: '+c);if(/variant\s*=\s*[\"']destructive[\"']/.test(d))throw new Error('UI-SPEC secao Color: o botao de confirmar ganho nao pode ser destrutivo');if(/useEffect|Carregando/.test(d))throw new Error('o dialogo nao tem carregamento assincrono: opcoes sao estaticas');if(!d.includes('FREQUENCIA_VISITA_ITEMS'))throw new Error('as opcoes tem que vir do modulo de vocabulario');if(!d.includes('gap-1.5'))throw new Error('UI-SPEC secao Spacing: bloco do campo usa gap-1.5');const s=fs.readFileSync('components/clientes/ClienteDetailSheet.tsx','utf8');for(const t of ['GanhoFrequenciaDialog','ganhoDialogOpen','setGanhoDialogOpen(false)','FrequenciaVisita'])if(!s.includes(t))throw new Error('ClienteDetailSheet: falta '+t);if(!/novoStatus\s*===\s*[\"']ganho[\"'][\s\S]{0,120}setGanhoDialogOpen\(true\)/.test(s))throw new Error('handleStatusSelect precisa do ramo ganho abrindo o dialogo antes de gravar');if(!/handleStatusChange\(\s*[\"']ganho[\"']\s*,\s*undefined\s*,/.test(s))throw new Error('onConfirm precisa chamar handleStatusChange com a frequencia no 3o argumento');const dis=(s.match(/eslint-disable/g)||[]).length;if(dis>2)throw new Error('nenhuma supressao de lint nova neste arquivo (debito documentado nao pode piorar), achei '+dis);const t=fs.readFileSync('tests/clientes/ganho-frequencia-dialog.test.tsx','utf8');if(!t.startsWith('// @vitest-environment jsdom'))throw new Error('o teste de render precisa da diretiva jsdom na 1a linha');const n=(t.match(/\bit\(\s*[\"'][^\"']+/g)||[]).map(x=>x.toLowerCase());for(const k of ['copy','desabilitado','variante','cancelar','vocabulario'])if(!n.some(x=>x.includes(k)))throw new Error('falta caso de teste com a palavra '+k);console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/clientes/GanhoFrequenciaDialog.tsx` existe, é Client Component, monta as opções a partir de `FREQUENCIA_VISITA_ITEMS`, e contém literalmente cada string de copy do Copywriting Contract do UI-SPEC.
    - O botão de confirmar do diálogo não usa a variante destrutiva, e o arquivo não tem nenhum `useEffect` nem estado de espera de carregamento.
    - O bloco do campo usa `gap-1.5` (token travado na seção Spacing do UI-SPEC).
    - `ClienteDetailSheet.tsx` declara `ganhoDialogOpen`, reseta esse estado no bloco de reset existente, tem o ramo `ganho` em `handleStatusSelect` abrindo o diálogo ANTES de qualquer gravação, e renderiza o diálogo novo com `onConfirm` chamando `handleStatusChange("ganho", undefined, frequencia)`.
    - O número de supressões de lint em `ClienteDetailSheet.tsx` não aumentou (continua em no máximo 2).
    - `tests/clientes/ganho-frequencia-dialog.test.tsx` existe, começa com a diretiva jsdom, e tem casos cujos nomes contêm `copy`, `desabilitado`, `variante`, `cancelar` e `vocabulario` — todos passando.
    - `npx tsc --noEmit` limpo e `npx eslint` limpo nos dois componentes.
  </acceptance_criteria>
  <done>Diálogo de frequência criado e ligado ao Select de Status; marcar "Ganho" na ficha agora pede a frequência antes de gravar, e o teste de render trava a copy e a variante do botão.</done>
</task>

</tasks>

<artifacts_produced>
## Artefatos deste plano (parte 2 de 3 — captura da frequência no momento do ganho)

| Símbolo / Arquivo | Tipo | Status |
|-------------------|------|--------|
| `lib/funil/frequencia.ts` | Módulo novo de vocabulário (`FREQUENCIAS_VISITA`, `FrequenciaVisita`, `FREQUENCIA_VISITA_LABELS`, `FREQUENCIA_VISITA_ITEMS`, `isFrequenciaVisita`) | Criado |
| `components/clientes/GanhoFrequenciaDialog.tsx` | Componente novo | Criado |
| `ClienteDetalhe.frequenciaVisita` | Campo novo no type de leitura da ficha | Adicionado |
| `marcarStatus(clienteId, novoStatus, motivoPerdaId?, frequenciaVisita?)` | Server Action estendida + código de erro `frequencia_obrigatoria` | Estendida |
| `components/clientes/ClienteDetailSheet.tsx` | Ramo `ganho` no Select de Status + render do diálogo novo | Estendido |
| `tests/clientes/ganho-frequencia-dialog.test.tsx` | Teste de render novo | Criado |

Nenhuma migration, nenhuma dependência npm nova, nenhum componente shadcn novo (Dialog/Select/Label/Button já instalados).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → Server Action `marcarStatus` | A frequência escolhida é entrada do usuário e atravessa o limite cliente→servidor. |
| Server Action → RPC `mover_card_funil` | A ação é o único caminho pelo qual a UI muda status de cliente. |
| UI condicional (item "Ganho" desabilitado fora da etapa final) | Esconder/desabilitar controle é cortesia, nunca fronteira de segurança. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-10 | Tampering (entrada não confiável) | `frequenciaVisita` chegando do navegador na Server Action | medium | mitigate | Re-validação no servidor com `isFrequenciaVisita` antes de chamar o RPC, e o parâmetro do RPC é tipado como enum do Postgres — qualquer valor fora dos quatro é recusado pelo banco mesmo que a checagem TypeScript seja contornada. Sem SQL dinâmico em nenhum ponto. |
| T-13-11 | Elevation of Privilege | `marcarStatus` alterando cliente de outro vendedor | high | mitigate | Nenhuma mudança no modelo de autorização: a ação continua roteando exclusivamente por `mover_card_funil`, que roda como o chamador e é filtrado pela RLS de `clientes` (T-02-13, já provado por teste desde a Fase 2). Este plano não introduz nenhum `.update()` cru nem nenhum uso de service role. |
| T-13-12 | Elevation of Privilege | contorno da exigência de frequência chamando a ação/RPC direto | medium | mitigate | A pré-checagem TypeScript é apenas mensagem amigável; o guard real vive no RPC (13-01) e já está provado por teste de integração (`obrigatoria`) chamando o RPC diretamente, sem passar pela UI. |
| T-13-13 | Information Disclosure | leitura da coluna nova na ficha do cliente | low | accept | `frequencia_visita` é lida no mesmo `select()` já escopado pela policy de SELECT de `clientes` — nenhuma superfície de leitura nova. As colunas da Fase 16 (`nome_fantasia`, `cnpj`, `frequencia_pedidos`) deliberadamente NÃO são lidas aqui. |
| T-13-14 | Spoofing / UX | item "Ganho" desabilitado fora da etapa final | low | accept | Já era assim antes desta fase; a garantia real continua sendo a CHECK constraint `chk_ganho_somente_etapa_final` mais o guard do RPC, ambos provados por teste. |
| T-13-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` nesta fase; nenhum componente shadcn novo é adicionado (todos já instalados, conforme a seção Registry Safety do UI-SPEC). |
</threat_model>

<verification>
Checks rodados pelo executor ao final do plano:
- `npx tsc --noEmit` limpo.
- `npx eslint` limpo nos quatro arquivos tocados.
- Check estrutural do Task 1: módulo de vocabulário puro, ordem e rótulos travados, coluna nova no `select()`, colunas da Fase 16 ausentes, `p_frequencia_visita` condicional ao ganho.
- Check estrutural do Task 2: cada string de copy do UI-SPEC presente no diálogo, ausência da variante destrutiva e de estado de carregamento, `gap-1.5`, ramo `ganho` em `handleStatusSelect` abrindo o diálogo antes de gravar, contagem de supressões de lint no `ClienteDetailSheet` não aumentada.
- `npx vitest run tests/clientes/ganho-frequencia-dialog.test.tsx` verde.
</verification>

<success_criteria>
- VIS-01 (tela): escolher "Ganho" no Select de Status abre o diálogo de frequência; cancelar não grava nada; confirmar grava status e frequência numa chamada só, pelo mesmo RPC.
- O botão de confirmar nasce desabilitado e usa a variante primária, não a destrutiva — travado por teste.
- Toda a copy do diálogo bate literalmente com o Copywriting Contract do `13-UI-SPEC.md`.
- O vocabulário das quatro frequências existe em um único módulo, na ordem travada, pronto para o plano 13-03 e as Fases 14-17.
- `ClienteDetalhe` carrega `frequenciaVisita` sem query extra, e o estado local do Sheet reflete a frequência logo após o ganho.
</success_criteria>

<output>
Create `.planning/phases/13-cliente-ativo-e-frequ-ncia-de-visita/13-02-SUMMARY.md` when done.
</output>
