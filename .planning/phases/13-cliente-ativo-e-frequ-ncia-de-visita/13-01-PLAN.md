---
phase: 13-cliente-ativo-e-frequ-ncia-de-visita
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/{proximo_numero_livre}_cliente_ativo_e_frequencia_visita.sql
  - tests/clientes/frequencia-visita.test.ts
  - tests/clientes/rls-visitas.test.ts
autonomous: false
requirements: [VIS-01, VIS-04, ATV-03]

must_haves:
  truths:
    - "VIS-01: chamar `mover_card_funil` com `p_novo_status = 'ganho'` SEM `p_frequencia_visita` levanta exceção com mensagem legível em português — mesma forma do guard de motivo-obrigatório-ao-perder que já existe na função"
    - "VIS-01: chamar `mover_card_funil` com `p_novo_status = 'ganho'` e uma frequência diferente de `nenhuma` grava `clientes.frequencia_visita` E cria EXATAMENTE UMA linha em `visitas` com `data_realizada` nula, na mesma transação"
    - "VIS-01: com `p_frequencia_visita = 'nenhuma'` a coluna é gravada com 'nenhuma' e NENHUMA linha de `visitas` é criada"
    - "VIS-01: marcar ganho duas vezes seguidas no mesmo cliente não duplica a visita pendente — o segundo ganho não cria uma segunda linha enquanto houver uma pendente"
    - "VIS-04: as quatro colunas novas de `clientes` (`nome_fantasia`, `cnpj`, `frequencia_pedidos`, `frequencia_visita`) são todas opcionais (nullable), sem constraint de validação — os clientes que já estavam 'ganho' em produção continuam válidos, com frequência nula, e continuam podendo ser lidos e editados normalmente"
    - "ATV-03: existe UMA única coluna `clientes.frequencia_visita` — o valor gravado pelo RPC no momento do ganho é exatamente o mesmo valor lido/alterado depois por um UPDATE comum; não existe nenhuma segunda coluna de frequência de visita em lugar nenhum do schema"
    - "A tabela `visitas` tem RLS habilitada e QUATRO policies (select/insert/update/delete), cada uma com o mesmo gate `exists (select 1 from clientes c where c.id = visitas.cliente_id and (c.responsavel = auth.uid() or is_supervisor()))` que `tarefas` já usa — Vendedor B não lê, não cria, não altera e não apaga visita de cliente do Vendedor A"
    - "`mover_card_funil` continua rodando como o usuário chamador (sem cláusula de elevação de privilégio), então tanto o UPDATE em `clientes` quanto o INSERT em `visitas` continuam filtrados pela RLS"
    - "A aritmética de data da próxima visita vive em UM único lugar (`proxima_data_visita`), calculada no Postgres com base no dia corrente no fuso de São Paulo, e faz clamp de fim de mês (31/01 + mensal = 28/02, ou 29/02 em ano bissexto)"
    - "Nenhuma policy de escrita nova foi criada sobre `historico`, e nenhuma trigger existente foi alterada"
    - "A migration foi aplicada no projeto Supabase hospedado via `supabase db push` com aprovação humana prévia, e a suíte completa (`npm test`) continua verde"
  artifacts:
    - "supabase/migrations/{proximo_numero_livre}_cliente_ativo_e_frequencia_visita.sql (número derivado por `ls` na execução, nunca copiado deste plano)"
    - "tipo `frequencia_visita_enum` com exatamente 4 valores: semanal, quinzenal, mensal, nenhuma"
    - "colunas novas nullable em `clientes`: nome_fantasia, cnpj, frequencia_pedidos, frequencia_visita"
    - "tabela `visitas` (id, cliente_id, data_prevista, data_realizada, resumo, criado_por, criado_em) com RLS + 4 policies + 2 índices"
    - "função `proxima_data_visita(p_base date, p_frequencia frequencia_visita_enum) returns date` — pura, immutable, sem acesso a tabela"
    - "`mover_card_funil` recriado com o 6º parâmetro `p_frequencia_visita` (a assinatura antiga de 5 parâmetros é removida no mesmo arquivo, para não deixar duas sobrecargas ambíguas)"
    - "tests/clientes/frequencia-visita.test.ts (novo)"
    - "tests/clientes/rls-visitas.test.ts (novo)"
  key_links:
    - "`drop function` da assinatura antiga de 5 parâmetros ANTES do `create function` de 6 — sem isso o Postgres cria uma SOBRECARGA em vez de substituir, e toda chamada existente do app (`moverCard`, `marcarStatus`) passa a falhar por ambiguidade no PostgREST"
    - "guard `if p_novo_status = 'ganho' and p_frequencia_visita is null then raise exception` — é a única coisa que torna VIS-01 inescapável, já que o schema é deliberadamente permissivo (Pitfall 3)"
    - "`get diagnostics ... row_count` depois do UPDATE: se a RLS não deixou o chamador alterar o cliente, nenhuma visita é semeada — é o que impede um vendedor de criar visita no cliente de outro"
    - "guard `not exists (visita pendente)` antes do INSERT — é o que torna o seed idempotente e evita duplicar visita ao remarcar ganho"
    - "`(now() at time zone 'America/Sao_Paulo')::date` como base do cálculo, nunca `current_date` cru — o banco roda em UTC e depois das 21h em São Paulo `current_date` já é o dia seguinte (Pitfall 1)"
    - "as 4 policies de `visitas` copiadas em forma das 4 policies de `tarefas` do 0002 — a RLS NÃO cascateia pela foreign key, cada tabela filha precisa do próprio gate"
---

<objective>
Criar toda a fundação de banco do marco v1.3 em uma migration só: as quatro colunas novas de "cliente ativo" em `clientes` (todas opcionais), o vocabulário de frequência de visita como enum, a tabela nova `visitas` com a mesma RLS de gate-pelo-pai que `tarefas` já usa, a função pura de cálculo da próxima data, e a extensão de `mover_card_funil` para exigir a frequência exatamente quando o status novo é "ganho" — semeando a primeira visita na mesma transação. Mais os dois arquivos de teste de integração que provam esse comportamento contra o banco real.

Purpose: sem isso nada do marco existe — a Fase 14 (Agenda) lê `visitas`, a Fase 15 (conclusão) escreve nela, e as Fases 16 e 17 leem as colunas novas. Toda a dificuldade deste plano é de segurança e de compatibilidade com dados que já existem em produção: a tabela nova precisa nascer com as quatro policies certas (RLS não cascateia por foreign key), e as colunas novas precisam nascer opcionais, senão a migration quebra contra os clientes que já estão "ganho" hoje.

Output: uma migration aplicada no banco hospedado com aprovação humana prévia, e dois arquivos de teste novos, verdes contra o banco real.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/skills/Supabase-conventions/SKILL.md
@supabase/migrations/0002_clientes_and_funil.sql
</context>

<source_audit>
## Multi-Source Coverage Audit — Fase 13 (conjunto completo de planos 13-01 a 13-03)

### GOAL (ROADMAP, Fase 13 — 4 critérios de sucesso)

| # | Item | Plano | Status |
|---|------|-------|--------|
| G1 | Ao mover para "ganho" o sistema pede a frequência antes de aceitar a mudança | 13-01 (guard no RPC) + 13-02 (diálogo) | COVERED |
| G2 | Frequência ≠ nenhuma → primeira visita nasce agendada; nenhuma → nenhuma visita criada | 13-01 | COVERED |
| G3 | Frequência trocável/cancelável a qualquer momento na ficha, com um valor só | 13-03 | COVERED |
| G4 | Clientes já "ganho" antes desta versão aparecem sem frequência e continuam funcionando | 13-01 (colunas nullable) + 13-03 (texto "Frequência ainda não definida.") | COVERED |

### REQ (REQUIREMENTS.md — phase_req_ids)

| ID | Plano | Status |
|----|-------|--------|
| VIS-01 | 13-01 (RPC + seed) + 13-02 (diálogo) | COVERED |
| VIS-02 | 13-03 | COVERED |
| VIS-04 | 13-01 + 13-03 | COVERED |
| ATV-03 | 13-01 (coluna única) + 13-03 (edição escreve a mesma coluna) | COVERED |

### RESEARCH (.planning/research/ARCHITECTURE.md + PITFALLS.md)

| Item | Plano | Status |
|------|-------|--------|
| Colunas novas em `clientes` (nome_fantasia, cnpj, frequencia_pedidos, frequencia_visita) | 13-01 | COVERED |
| Tabela `visitas` irmã de `tarefas`, RLS parent-gated | 13-01 | COVERED |
| `mover_card_funil` estendido com `p_frequencia_visita` obrigatório ao ganho (Pattern 4) | 13-01 | COVERED |
| Compute-and-store no write time, sem cron/worker (Pattern 1 / Anti-Pattern 1) | 13-01 | COVERED |
| Zero exceção `SECURITY DEFINER` nova (Anti-Pattern 2) | 13-01 | COVERED |
| Pitfall 1 — matemática de data/fuso, clamp de fim de mês, `parseISO` no cliente | 13-01 (`proxima_data_visita` + testes de clamp) | COVERED |
| Pitfall 3 — colunas graduadas nullable, regra no guard do RPC | 13-01 | COVERED |
| "RLS não cascateia por FK" (Security Mistakes) — 4 policies explícitas em `visitas` | 13-01 | COVERED |
| Índice de data em `visitas` | 13-01 | COVERED |
| Índice de data em `tarefas` (`idx_tarefas_data_conclusao`) | — | FORA DE ESCOPO: a research mapeia esse índice para a Fase 14 (feed unificado), junto da RPC que o consome |
| `resumo` em `tarefas`, `concluir_visita`, `agenda_do_vendedor`, trigger de historico estendida | — | FORA DE ESCOPO: Fases 14 e 15 |

### CONTEXT (decisões travadas)

Não existe `13-CONTEXT.md` — o dono pulou o Discuss porque as decisões já foram travadas no nível do marco. As decisões vinculantes vêm de `.planning/STATE.md` (Accumulated Context) e de `13-UI-SPEC.md`, e estão todas cobertas:

| Decisão travada | Plano | Status |
|-----------------|-------|--------|
| "Ativo" = `status_acompanhamento = 'ganho'`, não reabrir | 13-01 (nenhuma coluna/estado novo de ciclo de vida) | COVERED |
| Frequência é UM valor só, guardado uma vez em `clientes` | 13-01 + 13-03 | COVERED |
| Sem cron / sem worker de fundo | 13-01 | COVERED |
| Nenhuma exceção `SECURITY DEFINER` nova (a 4ª) | 13-01 | COVERED |
| Clientes já ganho ficam sem frequência até alguém definir | 13-01 + 13-03 | COVERED |
| UI-SPEC: `GanhoFrequenciaDialog` espelha `PerdaMotivoDialog`, botão primário (não destrutivo) | 13-02 | COVERED |
| UI-SPEC: copy exata (título, label, placeholder, opções, helper, erro, botões) | 13-02 + 13-03 | COVERED |
| UI-SPEC: controle permanente na seção Funil, salvamento imediato, sem diálogo de confirmação | 13-03 | COVERED |
| UI-SPEC: edição posterior NUNCA passa por `mover_card_funil` | 13-03 | COVERED |

**Resultado: 0 itens MISSING.** Nenhum item de fonte ficou fora dos três planos.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Escrever a migration completa e os dois arquivos de teste de integração (RED)</name>
  <files>supabase/migrations/{proximo_numero_livre}_cliente_ativo_e_frequencia_visita.sql, tests/clientes/frequencia-visita.test.ts, tests/clientes/rls-visitas.test.ts</files>
  <read_first>
    - supabase/migrations/0002_clientes_and_funil.sql — é a referência principal e já está no contexto. Extrair de lá: (a) o corpo COMPLETO de `mover_card_funil` e a ordem/tipos exatos dos seus 5 parâmetros atuais, que é a assinatura a remover; (b) as QUATRO policies de `tarefas` (`acesso a tarefas gated pelo cliente pai (select/insert/update/delete)`) — são o molde literal das policies de `visitas`; (c) o formato do `create table tarefas` (uso de `references clientes(id) on delete cascade`, `date` puro para data, `timestamptz not null default now()` para carimbo); (d) os dois CHECKs de `clientes` (`chk_ganho_somente_etapa_final`, `chk_perdido_exige_motivo`) — os testes precisam respeitá-los ao semear dados; (e) a lista de índices no fim do arquivo, como padrão de nomenclatura.
    - .planning/research/ARCHITECTURE.md — seções "Component Responsibilities" (linhas da tabela `visitas`, `clientes.frequencia_visita`, `mover_card_funil`), "Pattern 1" (compute-and-store no write time), "Pattern 4" (mover_card_funil como o momento atômico em que a cadência nasce), "Anti-Pattern 1" (nada de cron) e "Anti-Pattern 2" (nada de 4ª exceção de privilégio elevado).
    - .planning/research/PITFALLS.md — "Pitfall 1" (matemática de data e fuso; a exigência do teste de clamp 31/01 → 28/02 e 29/02), "Pitfall 3" (campos graduados contra tabela populada: por que as colunas entram opcionais e a regra vai no guard do RPC), e a tabela "Security Mistakes" (a linha "Assuming the new `visitas` table inherits `clientes`'s RLS because it has a `cliente_id` foreign key").
    - .planning/STATE.md — bloco "SECURITY DEFINER exceptions (3 no codebase)" no fim de Deferred Items (o v1.3 não pode adicionar uma quarta) e as convenções acumuladas sobre `mover_card_funil` e `historico`.
    - tests/clientes/rls-clientes.test.ts — o molde de teste de RLS deste projeto: `SEED_ACCOUNTS` importado de `../auth/rls-roles.test`, `signInAs`/`serviceClient` de `../helpers/supabase-test-clients`, helpers `uniqueRazaoSocial`/`baseClienteFields`/`getUserId`, e o array `createdClienteIds` + `afterEach` que limpa via service role.
    - tests/clientes/funil-status.test.ts — o helper `callMarcarStatusRpc` (a forma exata como o app chama o RPC, por nome de parâmetro) e o padrão "provar a Server Action exercitando a mesma chamada de RPC como usuário logado".
    - tests/clientes/funil-constraints.test.ts — como um cliente é levado até a etapa final antes de virar ganho, e como um `motivo_perda_id` ativo é buscado.
    - tests/helpers/supabase-test-clients.ts — assinaturas de `anonClient()`, `serviceClient()`, `signInAs(email, password)`.
    - vitest.config.ts — `fileParallelism: false` (os testes rodam serializados contra o banco real; não é preciso inventar isolamento extra além do já usado pelos outros arquivos).
  </read_first>
  <action>
    Duas entregas neste task: a migration e os dois arquivos de teste. Nada de código de aplicação (`app/`, `lib/`, `components/`) — isso é dos planos 02 e 03.

    (A) DERIVAR O NÚMERO DA MIGRATION EM TEMPO DE EXECUÇÃO. Rodar `ls supabase/migrations/` como PRIMEIRA ação e usar o próximo número livre de quatro dígitos no nome do arquivo novo: `supabase/migrations/{numero}_cliente_ativo_e_frequencia_visita.sql`. NÃO usar como literal nenhum número escrito neste plano ou na research — a Fase 11 teve uma colisão real de numeração corrigida ao vivo. Ao final do task o arquivo criado tem que ser o de MAIOR número da pasta. Nenhum arquivo de migration já existente pode ser aberto para edição.

    (B) Escrever a migration, nesta ordem, em seções numeradas com comentário de cabeçalho no estilo do `0002`:

    1. **Banner de cabeçalho.** Registrar em comentário: que este arquivo é a fundação de dados do marco v1.3; que as colunas novas de `clientes` entram deliberadamente opcionais porque a tabela já tem linhas "ganho" em produção e uma restrição de obrigatoriedade aplicada retroativamente abortaria o `supabase db push` (Pitfall 3); que a exigência "frequência obrigatória ao ganho" é imposta pelo guard do RPC, não pelo schema; que a função continua rodando como o chamador, sem elevação de privilégio, e que a RLS segue sendo a única fronteira de autorização; e que este marco não adiciona nenhuma exceção nova de privilégio elevado à lista de três já documentada no projeto.

    2. **Enum.** `create type frequencia_visita_enum as enum ('semanal', 'quinzenal', 'mensal', 'nenhuma');` — exatamente estes quatro valores, nesta ordem (é a ordem que a UI usa, travada no `13-UI-SPEC.md`).

    3. **Colunas novas em `clientes`.** Um único `alter table clientes` adicionando as quatro:
       - `nome_fantasia text`
       - `cnpj text`
       - `frequencia_pedidos text`
       - `frequencia_visita frequencia_visita_enum`
       TODAS opcionais. É PROIBIDO declarar qualquer uma como obrigatória, e é PROIBIDO adicionar qualquer constraint de validação (`check`) nesta migration, inclusive na forma `not valid`. Deixar um comentário SQL de uma linha acima do bloco registrando o motivo (Pitfall 3) e outro registrando que `frequencia_pedidos` é texto livre de propósito: ele é puramente informativo (ATV-02) e o vocabulário/validação dele é decisão da Fase 16, que ainda não aconteceu — não presumir aqui um enum que o dono nunca escolheu.
       <!-- planner-discipline-allow: not null -->
       <!-- planner-discipline-allow: check -->
       Comentar também que `frequencia_visita` é a ÚNICA coluna de frequência de visita do sistema (ATV-03): o que o RPC grava no momento do ganho e o que a ficha do cliente edita depois são o mesmo campo, nunca dois.

    4. **Tabela `visitas`.** Colunas exatamente estas:
       - `id uuid primary key default gen_random_uuid()`
       - `cliente_id uuid not null references clientes(id) on delete cascade`
       - `data_prevista date not null` — `date` puro, nunca `timestamptz`, espelhando `tarefas.data_conclusao` (Integration Gotchas da PITFALLS.md)
       - `data_realizada date` — nulo significa "pendente"; é o discriminador que a Agenda da Fase 14 vai filtrar
       - `resumo text` — deixado pronto aqui, preenchido só pela Fase 15 (CONC-01). Comentar isso explicitamente para não parecer coluna órfã: criar agora custa zero e evita um segundo `alter table` em produção depois.
       - `criado_por uuid references profiles(id)`
       - `criado_em timestamptz not null default now()`
       Comentar acima da tabela que ela é irmã de `tarefas`, não uma extensão dela (Anti-Pattern 3 da ARCHITECTURE.md): prospecção e pós-venda são conceitos diferentes, unificados só na leitura da Fase 14.

    5. **RLS de `visitas`.** `alter table visitas enable row level security;` seguido de EXATAMENTE QUATRO policies, uma por operação, copiadas em forma das quatro de `tarefas` no `0002`, trocando `tarefas.cliente_id` por `visitas.cliente_id` e nomeando em português no mesmo estilo (`"acesso a visitas gated pelo cliente pai (select)"` etc.). A de update precisa de `using` E `with check`, como a de `tarefas`. Deixar um comentário SQL registrando que a RLS não cascateia pela foreign key — é por isso que cada operação precisa do próprio gate. Não inventar policy extra, não criar policy de `service_role`, não usar `using (true)` em nenhuma delas.

    6. **Índices.** `create index idx_visitas_cliente_id on visitas (cliente_id);` e `create index idx_visitas_data_prevista on visitas (data_prevista);`. NÃO criar índice em `tarefas` — o índice de data de `tarefas` pertence à Fase 14, junto da RPC que o consome.

    7. **`proxima_data_visita`.** Função pura, sem nenhum acesso a tabela:
       `create or replace function proxima_data_visita(p_base date, p_frequencia frequencia_visita_enum) returns date language sql immutable as $$ ... $$;`
       Corpo: um `case p_frequencia` somando `interval '7 days'` para semanal, `interval '14 days'` para quinzenal e `interval '1 month'` para mensal, cada resultado convertido de volta para `date`; qualquer outro caso (incluindo `nenhuma` e nulo) devolve nulo. Comentar que ela é a ÚNICA autoridade sobre essa conta em todo o projeto (Pitfall 1: escolher uma autoridade e nunca duplicar a matemática no cliente), que a Fase 15 vai reusá-la em `concluir_visita`, e que `interval '1 month'` do Postgres já faz o clamp de fim de mês (31/01 + 1 mês = 28/02, ou 29/02 em ano bissexto) — que é justamente o caso coberto por teste.
       Ela é `immutable` e recebe a data-base por parâmetro justamente para poder ser testada com datas fixas; quem decide qual é "hoje" é o chamador.

    8. **`mover_card_funil` recriado.** ATENÇÃO — este é o ponto de maior risco do arquivo. No Postgres, `create or replace function` com um número diferente de parâmetros NÃO substitui a função: cria uma SOBRECARGA. Se as duas assinaturas coexistirem, toda chamada existente do app (que envia os parâmetros por nome) passa a falhar por ambiguidade no PostgREST. Então, nesta ordem, no mesmo arquivo:
       a. `drop function if exists mover_card_funil(uuid, etapa_funil, status_acompanhamento_enum, uuid, numeric);` — conferir os tipos exatos contra o `0002` antes de escrever, não confiar de cabeça. Nenhuma view/trigger depende dessa função, então o drop é seguro; ele e o create rodam na mesma transação da migration, sem janela em que a função não exista.
       b. `create function mover_card_funil(...)` com SEIS parâmetros: os cinco atuais, com os mesmos nomes, tipos, ordem e defaults, mais `p_frequencia_visita frequencia_visita_enum default null` no FIM. `returns void`, `language plpgsql`, e — como hoje — sem nenhuma cláusula de elevação de privilégio.

       Corpo, nesta ordem:
       - Os dois guards que já existem hoje, copiados sem alteração de texto: ganho só na etapa final, e perdido exige motivo.
       - Guard NOVO: se `p_novo_status = 'ganho'` e `p_frequencia_visita is null`, `raise exception` com uma mensagem em português no mesmo tom da de motivo-obrigatório (ex.: frequência de visita é obrigatória ao marcar um cliente como ganho). É este guard, e só ele, que torna VIS-01 inescapável — o schema é deliberadamente permissivo.
       - O mesmo `update clientes ... where id = p_cliente_id` de hoje, acrescentando `frequencia_visita = coalesce(p_frequencia_visita, frequencia_visita)` na lista de sets (mesma forma do `motivo_perda_id`, para que uma chamada de arrastar-card, que não manda frequência, jamais apague o valor existente).
       - Logo depois do update, `get diagnostics <var> = row_count;` numa variável inteira declarada no bloco `declare`.
       - Semeadura da primeira visita, só quando TODAS estas condições valem: `p_novo_status = 'ganho'`, o row_count do update foi maior que zero, a data devolvida por `proxima_data_visita((now() at time zone 'America/Sao_Paulo')::date, p_frequencia_visita)` não é nula, e NÃO existe já uma linha de `visitas` daquele cliente com `data_realizada` nula. O insert grava `cliente_id`, `data_prevista` (a data calculada) e `criado_por = (select auth.uid())`.
         - O row_count guard é de segurança: se a RLS não deixou o chamador alterar aquele cliente (cliente de outro vendedor), nada é semeado.
         - O guard de "já existe pendente" é de idempotência: remarcar ganho não pode criar uma segunda visita pendente.
         - A base do cálculo tem que ser o dia corrente no fuso de São Paulo, NUNCA `current_date` cru — o banco roda em UTC e, depois das 21h no horário local, `current_date` já é o dia seguinte (Pitfall 1). Deixar um comentário SQL de uma linha registrando exatamente isso.

    **Regras absolutas desta migration:**
      - Nenhuma cláusula de elevação de privilégio em nenhuma função nova.
      <!-- planner-discipline-allow: security definer -->
      - Nenhuma policy nova, de qualquer operação, sobre a tabela `historico`; nenhuma trigger existente recriada ou alterada; nenhum `alter table` em `tarefas` ou `historico`.
      - Nenhum `drop table`, `drop policy`, `truncate`, `delete from` ou `update` de dados.
      - Nenhuma dependência npm nova.
      - Exatamente um `create table` no arquivo (a `visitas`).

    (C) Criar os dois arquivos de teste. Os dois vão FALHAR (RED) até o push do Task 3. Padrão obrigatório em ambos: `SEED_ACCOUNTS` importado de `../auth/rls-roles.test`, sessões reais via `signInAs()`, `serviceClient()` usado SOMENTE para semear e limpar (nunca para afirmar comportamento de RLS — service role ignora RLS e daria falso positivo), e um `afterEach` limpando por id tudo que foi criado (apagar `visitas` antes de `clientes` não é necessário, o `on delete cascade` cobre, mas apagar os clientes é obrigatório).

    **`tests/clientes/frequencia-visita.test.ts` (novo).** Casos exigidos — os nomes dos `it()` precisam conter as palavras-chave marcadas, sem acento, porque as acceptance criteria filtram por elas:
      - **`clamp`**: chamar `rpc('proxima_data_visita', { p_base, p_frequencia })` como usuário logado e afirmar: `p_base = '2026-01-31'` + `mensal` devolve `'2026-02-28'`; `'2028-01-31'` + `mensal` devolve `'2028-02-29'` (ano bissexto); `'2026-03-31'` + `mensal` devolve `'2026-04-30'`. É o teste obrigatório da Pitfall 1.
      - **`intervalo`**: `'2026-01-31'` + `semanal` devolve `'2026-02-07'`; `'2026-01-31'` + `quinzenal` devolve `'2026-02-14'`.
      - **`nulo`**: `p_frequencia = 'nenhuma'` devolve nulo.
      - **`obrigatoria`**: cliente do Vendedor A levado até a etapa final (`primeira_venda`) e então `rpc('mover_card_funil', { p_cliente_id, p_nova_etapa: 'primeira_venda', p_novo_status: 'ganho' })` SEM `p_frequencia_visita` — a chamada retorna erro, e o cliente continua com `status_acompanhamento` diferente de ganho no banco. É VIS-01.
      - **`semeia`**: a mesma chamada COM `p_frequencia_visita: 'semanal'` tem sucesso; depois disso `clientes.frequencia_visita` vale `'semanal'`, e existe EXATAMENTE UMA linha em `visitas` daquele cliente com `data_realizada` nula e `data_prevista` igual a hoje-em-São-Paulo mais 7 dias. Para calcular a data esperada em JS: obter o dia local com `new Intl.DateTimeFormat('en-CA', { timeZone: 'America/Sao_Paulo' }).format(new Date())` (devolve `YYYY-MM-DD`) e somar 7 dias com `addDays(parseISO(hoje), 7)` do `date-fns`, formatando de volta com `format(..., 'yyyy-MM-dd')`. NUNCA construir a data com `new Date(stringDaData)` — é exatamente o bug de meia-noite-UTC que a Pitfall 1 descreve.
      - **`nenhuma`**: outro cliente, mesma chamada com `p_frequencia_visita: 'nenhuma'` — sucesso, `clientes.frequencia_visita` vale `'nenhuma'`, e a contagem de linhas em `visitas` daquele cliente é ZERO.
      - **`duplica`**: no cliente do caso `semeia`, voltar o status para `em_andamento` via RPC e marcar ganho de novo com `'semanal'` — continua existindo exatamente UMA visita pendente daquele cliente, não duas.
      - **`legado`**: simular uma linha anterior a esta versão inserindo via `serviceClient()` um cliente já com `etapa: 'primeira_venda'` e `status_acompanhamento: 'ganho'` (sem passar por RPC nenhum) e afirmar: `frequencia_visita` vem nula; `nome_fantasia`, `cnpj` e `frequencia_pedidos` vêm nulos; o Vendedor A dono consegue LER a linha e consegue fazer um UPDATE comum nela (ex.: mudar `observacao`) sem erro. É VIS-04 — nada quebra e nada é suposto.
      - **`coluna`**: no cliente do caso `semeia` (frequência gravada pelo RPC), fazer um UPDATE comum como o vendedor dono trocando `frequencia_visita` para `'mensal'`, reler e afirmar que vale `'mensal'` — provando que o RPC e a edição direta escrevem no MESMO campo. É ATV-03.

    **`tests/clientes/rls-visitas.test.ts` (novo).** Cenário base: Vendedor A cria um cliente e uma visita para ele (insert direto em `visitas`, permitido pela policy de insert); Vendedor B loga em seguida. Casos exigidos — palavras-chave nos nomes dos `it()`:
      - **`select`**: Vendedor B lê `visitas` filtrando pelo id da visita do A e recebe lista vazia sem erro; o Supervisor lê a mesma visita e a encontra.
      - **`insert`**: Vendedor B tenta inserir uma visita com `cliente_id` do cliente do Vendedor A — a chamada retorna erro (violação de policy) e nenhuma linha nova existe para aquele cliente.
      - **`update`**: Vendedor B tenta atualizar a visita do A (ex.: mudar `data_prevista`) — nenhuma linha é afetada e o valor no banco continua o original (reler via `serviceClient()` para confirmar).
      - **`delete`**: Vendedor B tenta apagar a visita do A — nenhuma linha é afetada e a visita continua existindo.
      - **`proprio`**: Vendedor A faz select, update e delete da própria visita com sucesso — prova que as policies não estão fechadas demais.
      - **`rpc`**: Vendedor B chama `mover_card_funil` com o `p_cliente_id` do cliente do Vendedor A, `p_novo_status: 'ganho'` e `p_frequencia_visita: 'semanal'` — depois disso, lendo via `serviceClient()`, o cliente do A continua sem estar ganho e NENHUMA visita nova existe para ele. É a prova do guard de row_count.

    Não usar `anonClient()` como sujeito de asserção a menos que queira cobrir também o caso não-autenticado; se cobrir, é um `it()` extra, não substitui nenhum dos acima.
  </action>
  <verify>
    <automated>test -z "$(git diff --name-only HEAD -- supabase/migrations/)" && npx tsc --noEmit && node -e "const fs=require('fs');const dir='supabase/migrations';const all=fs.readdirSync(dir).filter(n=>n.endsWith('.sql')).sort();const mig=all.filter(n=>n.includes('cliente_ativo_e_frequencia_visita'));if(mig.length!==1)throw new Error('esperado exatamente 1 arquivo _cliente_ativo_e_frequencia_visita.sql, achei '+mig.length);if(all[all.length-1]!==mig[0])throw new Error('a migration nova nao e a de maior numero da pasta: '+mig[0]+' vs '+all[all.length-1]);const raw=fs.readFileSync(dir+'/'+mig[0],'utf8');const body=raw.split('\n').filter(l=>!l.trim().startsWith('--')).join('\n').toLowerCase();for(const t of ['create type frequencia_visita_enum','semanal','quinzenal','mensal','nenhuma','alter table clientes','nome_fantasia','cnpj','frequencia_pedidos','frequencia_visita','create table visitas','data_prevista','data_realizada','criado_por','enable row level security','idx_visitas_cliente_id','idx_visitas_data_prevista','proxima_data_visita','immutable','america/sao_paulo','get diagnostics','not exists','drop function','mover_card_funil','p_frequencia_visita'])if(!body.includes(t))throw new Error('faltou no SQL: '+t);if(/security\s+definer/.test(body))throw new Error('nenhuma funcao desta migration pode elevar privilegio');if(/add\s+column\s+[a-z_]+[^,;]*not\s+null/.test(body))throw new Error('Pitfall 3: as colunas novas de clientes tem que ser opcionais');if(/check\s*\(/.test(body))throw new Error('Pitfall 3: nenhuma constraint de validacao nesta migration');if(/on\s+historico\s+for\s+/.test(body))throw new Error('nenhuma policy nova sobre historico');if(/alter\s+table\s+(tarefas|historico)/.test(body))throw new Error('esta fase nao mexe em tarefas nem historico');if(/drop\s+table|drop\s+policy|truncate|delete\s+from/.test(body))throw new Error('migration nao pode ser destrutiva');if(/create\s+index[^;]*on\s+tarefas/.test(body))throw new Error('o indice de data de tarefas pertence a Fase 14');const ct=(body.match(/create\s+table/g)||[]).length;if(ct!==1)throw new Error('esperado exatamente 1 create table, achei '+ct);const pol=(body.match(/create\s+policy/g)||[]).length;if(pol!==4)throw new Error('visitas precisa de exatamente 4 policies, achei '+pol);for(const op of ['for select','for insert','for update','for delete'])if(!body.includes(op))throw new Error('falta policy de visitas para: '+op);if((body.match(/using\s*\(\s*true\s*\)/g)||[]).length>0)throw new Error('nenhuma policy de visitas pode ser aberta');const df=(body.match(/drop\s+function/g)||[]).length;if(df!==1)throw new Error('esperado exatamente 1 drop function (a assinatura antiga de mover_card_funil), achei '+df);if(!/drop\s+function[^;]*mover_card_funil\s*\(\s*uuid\s*,\s*etapa_funil\s*,\s*status_acompanhamento_enum\s*,\s*uuid\s*,\s*numeric\s*\)/.test(body))throw new Error('o drop precisa citar a assinatura ANTIGA de 5 parametros de mover_card_funil');if(/current_date/.test(body.replace(/america\/sao_paulo/g,'')))throw new Error('Pitfall 1: usar o dia corrente no fuso de Sao Paulo, nunca current_date cru');const t1=fs.readFileSync('tests/clientes/frequencia-visita.test.ts','utf8');const n1=(t1.match(/\bit\(\s*[\"'][^\"']+/g)||[]).map(s=>s.toLowerCase());for(const k of ['clamp','intervalo','nulo','obrigatoria','semeia','nenhuma','duplica','legado','coluna'])if(!n1.some(t=>t.includes(k)))throw new Error('frequencia-visita.test.ts: falta caso com a palavra '+k);if(/new\s+Date\s*\(\s*[a-z]/i.test(t1))throw new Error('Pitfall 1: nao construir Date a partir de string, usar parseISO');if(!t1.includes('parseISO')||!t1.includes('America/Sao_Paulo'))throw new Error('o teste da data semeada precisa usar parseISO e o fuso de Sao Paulo');const t2=fs.readFileSync('tests/clientes/rls-visitas.test.ts','utf8');const n2=(t2.match(/\bit\(\s*[\"'][^\"']+/g)||[]).map(s=>s.toLowerCase());for(const k of ['select','insert','update','delete','proprio','rpc'])if(!n2.some(t=>t.includes(k)))throw new Error('rls-visitas.test.ts: falta caso com a palavra '+k);for(const f of [t1,t2]){if(!f.includes('SEED_ACCOUNTS')||!f.includes('signInAs'))throw new Error('os testes precisam usar sessoes reais via signInAs/SEED_ACCOUNTS')}console.log('OK '+mig[0])"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only HEAD -- supabase/migrations/` sai vazio: nenhuma migration já existente foi editada.
    - Existe exatamente um `supabase/migrations/*_cliente_ativo_e_frequencia_visita.sql` e ele é o de MAIOR número da pasta (número derivado por `ls` na execução).
    - O SQL (ignorando comentários) declara `frequencia_visita_enum` com os quatro valores, adiciona as quatro colunas em `clientes`, cria a tabela `visitas` com `data_prevista`/`data_realizada`/`criado_por`, habilita RLS nela e cria os dois índices.
    - Nenhuma das quatro colunas novas de `clientes` é declarada como obrigatória, e o arquivo inteiro não contém nenhuma constraint de validação.
    - Existem EXATAMENTE 4 `create policy` no arquivo, cobrindo select/insert/update/delete, e nenhuma delas é aberta (`using (true)`).
    - O arquivo contém exatamente um `create table` e exatamente um `drop function`, e o drop cita a assinatura ANTIGA de cinco parâmetros de `mover_card_funil`.
    - O SQL não contém cláusula de elevação de privilégio, não cria policy sobre `historico`, não altera `tarefas` nem `historico`, não contém `drop table`/`drop policy`/`truncate`/`delete from`, e não cria índice em `tarefas`.
    - `proxima_data_visita` existe, é `immutable`, e o corpo de `mover_card_funil` usa o dia corrente no fuso `America/Sao_Paulo` (nunca `current_date` cru), `get diagnostics` e um guard `not exists`.
    - `tests/clientes/frequencia-visita.test.ts` existe, usa `signInAs`/`SEED_ACCOUNTS`, usa `parseISO` e o fuso de São Paulo, nunca constrói `Date` a partir de string, e tem casos cujos nomes contêm `clamp`, `intervalo`, `nulo`, `obrigatoria`, `semeia`, `nenhuma`, `duplica`, `legado` e `coluna`.
    - `tests/clientes/rls-visitas.test.ts` existe, usa `signInAs`/`SEED_ACCOUNTS`, e tem casos cujos nomes contêm `select`, `insert`, `update`, `delete`, `proprio` e `rpc`.
    - `npx tsc --noEmit` limpo.
    - Os testes ainda FALHAM em runtime (RED) neste ponto — esperado até o Task 3.
  </acceptance_criteria>
  <done>Migration escrita (número derivado na execução) e os dois arquivos de teste criados; tudo ainda RED, aguardando aprovação e push.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Aprovação humana — aplicar a migration no banco Supabase de produção</name>
  <action>Pausar a execução. É a etapa de revisão antes de produção exigida pelo CLAUDE.md e já praticada em todas as fases que mexeram em schema (9, 10, 11, 12). Apresentar ao dono do projeto, em linguagem simples, o que a migration faz no banco real, e só rodar o `supabase db push` (Task 3) depois do sinal "approved". Não prosseguir sem aprovação.</action>
  <what-built>
    Foi escrita uma migration que prepara o banco para a "agenda do vendedor". Ela faz quatro coisas, e nenhuma delas apaga ou altera dado que já existe:

    1. **Quatro campos novos na ficha do cliente**: Nome Fantasia, CNPJ, frequência de pedidos e frequência de visita. Todos os quatro entram como OPCIONAIS de propósito. Isso importa: seus clientes que já estão marcados como "ganho" hoje continuam válidos e continuam abrindo normalmente, simplesmente com esses campos em branco. Se a gente tivesse tornado eles obrigatórios, a atualização do banco teria falhado logo de cara por causa desses clientes antigos.

    2. **Uma tabela nova chamada `visitas`**, para as visitas de pós-venda. Ela nasce com as mesmas travas de permissão que as tarefas já têm hoje: cada vendedor só enxerga, cria, edita e apaga visitas dos clientes que já são dele; o supervisor enxerga as de todo mundo. São quatro travas separadas, uma para cada tipo de operação, porque o banco não herda permissão automaticamente de uma tabela para outra.

    3. **Uma conta pronta que calcula a data da próxima visita** a partir da frequência escolhida (7 dias, 14 dias ou 1 mês). Ela é feita dentro do banco, e não no navegador, para não existir duas contas diferentes que possam discordar. Ela também trata o caso do fim do mês corretamente: 31 de janeiro + 1 mês cai em 28 de fevereiro (ou 29, em ano bissexto), não em uma data inválida.

    4. **A regra do "ganho" ganhou uma exigência nova**: a partir de agora, marcar um cliente como ganho sem escolher a frequência de visita é recusado pelo banco — exatamente como já acontece hoje quando alguém tenta marcar "perdido" sem escolher o motivo. Se a frequência escolhida for diferente de "nenhuma", a primeira visita já nasce agendada no mesmo instante, na mesma operação. Se for "nenhuma", nenhuma visita é criada.

    **Um aviso importante sobre a ordem das coisas:** entre este push e a conclusão do próximo plano (13-02), a tela vai ficar temporariamente sem conseguir marcar um cliente como "ganho" — o banco já vai exigir a frequência, mas a telinha que pergunta a frequência ainda não existe. Isso é esperado e dura só até o próximo plano subir, que é logo em seguida. Nenhum dado é perdido nesse intervalo.

    O que a migration NÃO faz: não apaga nada, não mexe nas tarefas nem no histórico, não cria nenhuma permissão especial que ignore as travas de acesso, não instala nenhuma biblioteca nova e não cria nenhum serviço que fique rodando em segundo plano.
  </what-built>
  <how-to-verify>
    1. Confirme que você está logado no Supabase CLI e com o projeto linkado (mesmo fluxo das migrations anteriores; se `supabase db push` pedir a senha do banco, é normal — tenha ela à mão).
    2. Se quiser conferir o arquivo: abra a migration nova em `supabase/migrations/` (é a de número mais alto, terminando em `_cliente_ativo_e_frequencia_visita.sql`). Procure e confirme que NÃO existe nenhum `drop table`, `delete from` ou `truncate` — só um `drop function`, que é a substituição controlada da regra de mover card (a regra é recriada logo abaixo, na mesma operação).
    3. Ponto que merece o seu olhar #1: **a primeira visita é agendada para hoje + a frequência escolhida**. Ou seja, escolher "semanal" no dia que o cliente vira ganho agenda a primeira visita para daqui a 7 dias, não para hoje. Se você esperava que a primeira visita fosse hoje mesmo, é melhor falar agora.
    4. Ponto que merece o seu olhar #2: **"frequência de pedidos" entrou como texto livre**, não como uma lista fechada de opções. Isso é de propósito — esse campo é só informativo e a decisão de quais opções ele terá (se tiver) é da Fase 16, que ainda não foi discutida com você. Se você já sabe que quer uma lista fechada, fale agora e isso muda o formato do campo.
    5. Ponto que merece o seu olhar #3: **os clientes que já estão "ganho" hoje ficam sem frequência definida**, e o sistema não vai chutar nenhuma para eles. Eles só entram na rotina de visitas quando alguém abrir a ficha e escolher uma (ou, mais para frente, pela planilha em massa da Fase 17). Isso é a decisão que você já tinha travado — confirme que continua valendo.
  </how-to-verify>
  <resume-signal>Digite "approved" para autorizar o `supabase db push` no banco de produção, ou descreva qualquer dúvida/ajuste antes de aplicar.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Aplicar a migration (supabase db push) e levar os testes a GREEN</name>
  <files>tests/clientes/frequencia-visita.test.ts, tests/clientes/rls-visitas.test.ts</files>
  <read_first>
    - o arquivo de migration criado no Task 1 (é o que será aplicado)
    - tests/helpers/supabase-test-clients.ts — conferir que `.env.local` tem `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` e `SUPABASE_SERVICE_ROLE_KEY` apontando para o projeto hospedado
    - .planning/STATE.md — Blockers/Concerns: o rate-limit conhecido de `signInWithPassword` no projeto de teste ao vivo (não repetir login desnecessariamente dentro do mesmo arquivo) e a linha de cliente com `estado='ZZ'` deixada de propósito na base
  </read_first>
  <action>
    Rodar `supabase db push` para aplicar a migration no projeto Supabase hospedado (mesmo fluxo das migrations 0002 a 0012). Se falhar por autenticação ou link, criar dinamicamente um gate de auth (pedir ao dono que rode `supabase login` / `supabase link` e forneça a senha do banco) e repetir — NÃO seguir adiante sem o push concluído: é o pré-requisito bloqueante dos planos 13-02 e 13-03 e de toda a Fase 14.

    Se o push acusar colisão de numeração, renomear o arquivo desta fase para o próximo número livre real e repetir — nunca renomear nem editar uma migration já aplicada.

    Depois do push, rodar os dois arquivos de teste contra o banco real e levá-los a GREEN. Se algum caso falhar, diagnosticar contra o SQL aplicado ANTES de mexer no teste. Suspeitos prováveis, em ordem:
      1. Erro de ambiguidade de função em QUALQUER chamada de `mover_card_funil` (inclusive nos testes antigos) — o `drop function` da assinatura antiga não pegou, e existem duas sobrecargas no banco. Corrigir com uma migration nova que remova a sobrecarga sobrando.
      2. `semeia` encontrando zero visitas — o guard de row_count está zerando indevidamente (o UPDATE não afetou linha por causa de RLS), ou o `not exists` está encontrando uma pendente de execução anterior do teste que não foi limpa.
      3. `semeia` com a data um dia adiantada/atrasada — a base do cálculo ficou em `current_date` cru em vez do dia no fuso de São Paulo, ou o teste construiu a data esperada com `new Date(string)`.
      4. Casos de `rls-visitas` passando indevidamente para o Vendedor B — alguma policy ficou com `using (true)` ou faltou uma das quatro operações. É o cenário mais grave: corrigir a policy, nunca afrouxar o teste.
      5. `legado` falhando no UPDATE comum do cliente antigo — alguma constraint ou obrigatoriedade acabou entrando nas colunas novas (Pitfall 3). Corrigir a migration.

    Corrigir SEMPRE pelo lado da migration/SQL, criando um arquivo novo se o push já tiver tido sucesso — nunca editando uma migration já aplicada, e nunca relaxando uma asserção de segurança para o teste passar.

    Rodar por último a suíte completa (`npm test`) para provar que a troca de assinatura de `mover_card_funil` não quebrou nenhum caminho existente — em especial `tests/clientes/funil-status.test.ts`, `tests/clientes/funil-constraints.test.ts` e as suítes de dashboard, que marcam clientes como ganho por UPDATE direto (e devem continuar funcionando justamente porque as colunas novas são opcionais).

    Este task não altera código de aplicação: só aplica a migration e valida.
  </action>
  <verify>
    <automated>npx vitest run tests/clientes/frequencia-visita.test.ts tests/clientes/rls-visitas.test.ts && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` aplicou a migration no projeto hospedado sem erro.
    - `npx vitest run tests/clientes/frequencia-visita.test.ts` passa inteiro (VIS-01, VIS-04, ATV-03 e o teste de clamp de data da Pitfall 1).
    - `npx vitest run tests/clientes/rls-visitas.test.ts` passa inteiro: Vendedor B é bloqueado nas quatro operações sobre visita de cliente alheio, Vendedor A consegue nas três próprias, e chamar `mover_card_funil` em cliente alheio não semeia visita nenhuma.
    - `npm test` (suíte completa) verde — nenhuma regressão nos testes de funil, clientes, importação ou dashboard, provando que a substituição da assinatura de `mover_card_funil` não deixou sobrecarga ambígua no banco.
    - Nenhum arquivo de migration já aplicado anteriormente foi editado.
  </acceptance_criteria>
  <done>Migration aplicada em produção com aprovação humana; tabela `visitas` com RLS provada por teste cross-vendedor nas quatro operações, guard de frequência-obrigatória-ao-ganho provado, semeadura da primeira visita provada, e clientes "ganho" legados provados intactos.</done>
</task>

</tasks>

<artifacts_produced>
## Artefatos deste plano (parte 1 de 3 — camada de banco)

| Símbolo / Arquivo | Tipo | Status |
|-------------------|------|--------|
| `supabase/migrations/{numero_derivado}_cliente_ativo_e_frequencia_visita.sql` | Migration nova (aditiva) | Criada (Task 1), aplicada (Task 3) |
| `frequencia_visita_enum` | Tipo enum: semanal / quinzenal / mensal / nenhuma | Criado |
| `clientes.nome_fantasia`, `clientes.cnpj`, `clientes.frequencia_pedidos`, `clientes.frequencia_visita` | Colunas novas, todas opcionais | Criadas |
| `visitas` | Tabela nova + RLS + 4 policies + 2 índices | Criada |
| `proxima_data_visita(date, frequencia_visita_enum) -> date` | Função pura `immutable`, autoridade única da conta de próxima data | Criada |
| `mover_card_funil(uuid, etapa_funil, status_acompanhamento_enum, uuid, numeric, frequencia_visita_enum)` | RPC recriada com 6º parâmetro; assinatura antiga de 5 removida | Substituída |
| `tests/clientes/frequencia-visita.test.ts` | Teste de integração novo | Criado (Task 1), GREEN (Task 3) |
| `tests/clientes/rls-visitas.test.ts` | Teste de RLS novo | Criado (Task 1), GREEN (Task 3) |

Nenhuma dependência npm nova. Nenhuma alteração em `tarefas`, `historico`, triggers existentes ou qualquer RPC de dashboard.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário autenticado (Vendedor) → tabela nova `visitas` | Tabela filha sem coluna de dono própria; a RLS não cascateia pela foreign key, então sem policy explícita qualquer autenticado leria/escreveria visita de qualquer cliente. |
| usuário autenticado → `mover_card_funil` estendido | O RPC agora escreve em DUAS tabelas (`clientes` e `visitas`) numa transação só; o parâmetro novo vem do navegador. |
| migration → linhas "ganho" já existentes em produção | O `ALTER TABLE` roda contra dados reais que ninguém pode perder nem invalidar. |
| usuário autenticado → `proxima_data_visita` | Função nova exposta como RPC pelo PostgREST. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Information Disclosure / Elevation of Privilege | tabela `visitas` | critical | mitigate | RLS habilitada + QUATRO policies explícitas (select/insert/update/delete), cada uma com o gate `exists`-no-`clientes` pai copiado de `tarefas`; nenhuma policy aberta. Provado por `tests/clientes/rls-visitas.test.ts` nas quatro operações como Vendedor B contra cliente do Vendedor A, e verificado estruturalmente no Task 1 (contagem exata de 4 policies, ausência de `using (true)`). |
| T-13-02 | Elevation of Privilege | `mover_card_funil` estendido escrevendo em duas tabelas | high | mitigate | A função continua sem cláusula de elevação de privilégio, então tanto o UPDATE quanto o INSERT passam pela RLS. Além disso, o `get diagnostics row_count` faz o insert de `visitas` só acontecer se o UPDATE em `clientes` realmente afetou linha — um vendedor não consegue nem tentar semear visita em cliente alheio. Caso de teste `rpc` em `rls-visitas.test.ts` prova. |
| T-13-03 | Tampering / Denial of Service | `alter table clientes` contra linhas "ganho" em produção | high | mitigate | As quatro colunas entram opcionais e a migration não contém nenhuma constraint de validação (Pitfall 3) — verificado mecanicamente no Task 1. A regra "obrigatória ao ganho" vive no guard do RPC, aplicável só a transições futuras. Caso `legado` prova que um cliente ganho anterior continua legível e editável. |
| T-13-04 | Tampering (trilha de auditoria) | `historico` | high | mitigate | A migration não cria nenhuma policy sobre `historico` nem recria nenhuma trigger — verificado mecanicamente. A escrita em `historico` continua exclusiva das triggers já existentes do `0002`, preservando a trilha à prova de adulteração pela API. |
| T-13-05 | Denial of Service / integridade | seed duplicado de visitas ao remarcar ganho | low | mitigate | Guard `not exists` de visita pendente antes do INSERT; caso de teste `duplica`. |
| T-13-06 | Tampering | quebra silenciosa de todas as chamadas de `mover_card_funil` por sobrecarga ambígua | high | mitigate | `drop function` explícito da assinatura antiga de 5 parâmetros antes do `create` de 6, na mesma transação; verificação estrutural exige exatamente um `drop function` citando a assinatura antiga, e o Task 3 roda `npm test` inteiro (que exercita os caminhos antigos) como alarme. |
| T-13-07 | Tampering | migration aplicada em produção sem revisão humana | medium | mitigate | Checkpoint bloqueante (Task 2) antes do `supabase db push`, exigido pelo CLAUDE.md e já praticado nas Fases 9-12. |
| T-13-08 | Information Disclosure | `proxima_data_visita` exposta como RPC a qualquer autenticado | low | accept | Função pura `immutable`, sem nenhum acesso a tabela: recebe uma data e um enum, devolve uma data. Não há dado do sistema a vazar e não há superfície de injeção (parâmetros tipados, sem SQL dinâmico). |
| T-13-09 | Tampering (entrada) | `p_frequencia_visita` vindo do navegador | low | mitigate | Parâmetro tipado como `frequencia_visita_enum`: qualquer valor fora dos quatro é recusado pelo próprio Postgres antes de chegar ao corpo da função. Sem SQL dinâmico em nenhum ponto. |
| T-13-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` neste plano nem nesta fase — zero pacotes novos, nada a auditar. Sem checkpoint de legitimidade aplicável. |
</threat_model>

<verification>
Checks rodados pelo executor ao final do plano:
- `git diff --name-only HEAD -- supabase/migrations/` vazio (nenhuma migration existente editada).
- `npx tsc --noEmit` limpo.
- Check estrutural do SQL (Task 1): enum, quatro colunas opcionais, tabela `visitas`, RLS + exatamente 4 policies não-abertas, dois índices, função pura `immutable`, um único `drop function` citando a assinatura antiga, uso do fuso de São Paulo, `get diagnostics` e guard `not exists`; ausência de cláusula de elevação de privilégio, de constraint de validação, de policy sobre `historico`, de DDL destrutivo e de índice em `tarefas`.
- Check estrutural dos testes (Task 1): palavras-chave obrigatórias nos nomes dos casos; uso de `signInAs`/`SEED_ACCOUNTS`; uso de `parseISO` e do fuso de São Paulo; ausência de construção de `Date` a partir de string.
- `npx vitest run tests/clientes/frequencia-visita.test.ts tests/clientes/rls-visitas.test.ts` verde contra o banco real, pós-push.
- `npm test` (suíte completa) verde — prova que não sobrou sobrecarga ambígua de `mover_card_funil`.
- Checkpoint humano (Task 2): dono aprovou aplicar a migration em produção antes do push.
</verification>

<success_criteria>
- VIS-01 (banco): marcar "ganho" sem frequência é recusado pelo RPC; com frequência ≠ nenhuma, a coluna é gravada e exatamente uma visita pendente nasce na mesma transação; com "nenhuma", nenhuma visita é criada; remarcar ganho não duplica a pendente.
- VIS-04: as quatro colunas novas são opcionais, sem constraint de validação; clientes que já estavam "ganho" continuam com frequência nula, legíveis e editáveis, provado por teste.
- ATV-03: existe uma única coluna `clientes.frequencia_visita`; o valor gravado pelo RPC e o valor alterado por UPDATE comum são o mesmo campo, provado por teste.
- `visitas` tem RLS com quatro policies parent-gated; Vendedor B é bloqueado nas quatro operações sobre visita alheia e o Supervisor não é, provado por teste.
- A conta de próxima data vive em um único lugar, no Postgres, com clamp de fim de mês provado inclusive em ano bissexto, e usa o dia corrente no fuso de São Paulo.
- Migration aplicada no banco hospedado com aprovação humana prévia; suíte completa verde.
</success_criteria>

<output>
Create `.planning/phases/13-cliente-ativo-e-frequ-ncia-de-visita/13-01-SUMMARY.md` when done.
</output>
