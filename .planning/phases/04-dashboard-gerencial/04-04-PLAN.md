---
phase: 04-dashboard-gerencial
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - components/dashboard/GanhosPerdidosCards.tsx
  - components/dashboard/DashboardClient.tsx
autonomous: true
requirements: [DSH-02, DSH-04]
must_haves:
  truths:
    - "The dashboard shows Ganhos, Perdidos, and Taxa de conversão as big-number stat tiles (not a pie/donut), all reacting to the period filter (D-05/D-02)."
    - "Ganhos/Perdidos count by the date the status changed within the selected period, sourced from the 04-01 historico-based function (D-02)."
    - "Taxa de conversão is ganho/(ganho+perdido), shown with one decimal in pt-BR, and reads as a dash when there is nothing to divide (DSH-04)."
    - "A Vendedor sees only their own ganho/perdido totals; a Supervisor sees the team total — RLS does the scoping (DSH-06/DSH-07)."
  artifacts:
    - "components/dashboard/GanhosPerdidosCards.tsx — 3 KPI stat tiles"
  key_links:
    - "GanhosPerdidosCards receives {inicio, fim} from DashboardClient and calls the 04-01 getGanhosPerdidos Server Action; conversão is computed with taxaConversao from lib/dashboard/periodo.ts."
---

## Phase Goal

**As a** supervisor ou vendedor, **I want to** enxergar a saúde do funil em números na medida da minha própria visão, **so that** eu tenha a informação gerencial que faltava para largar o CRM pago atual.

> Second visible slice: after this plan the dashboard shows the headline win/loss numbers and conversion rate for the selected period — the single most management-relevant view (DSH-02/DSH-04).

<objective>
Add the "ganhos x perdidos + taxa de conversão" KPI row to the dashboard: three big-number stat tiles (Ganhos in success green, Perdidos in destructive red, Taxa de conversão in neutral ink) that react to the period filter, per D-05's requirement that these read as "grandes, diretos" numbers rather than a chart. The conversion rate reuses the already-unit-tested taxaConversao helper from 04-01.

Purpose: Deliver the metric the Supervisor most needs to justify dropping the paid CRM (how many deals were won vs lost, and the conversion rate) as prominent hero figures.
Output: `components/dashboard/GanhosPerdidosCards.tsx`, wired into `DashboardClient.tsx`.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-gerencial/04-RESEARCH.md
@.planning/phases/04-dashboard-gerencial/04-UI-SPEC.md

# Analogs + 04-01/04-03 artifacts this plan uses
@components/dashboard/DashboardClient.tsx
@components/dashboard/ClientesPorEtapaChart.tsx
@app/actions/dashboard.ts
@lib/dashboard/periodo.ts
@components/ui/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ganhos/Perdidos/Conversão KPI stat tiles (DSH-02, DSH-04)</name>
  <read_first>
    - .planning/phases/04-dashboard-gerencial/04-UI-SPEC.md (Typography: the 36px "Stat" APPROVED WAIVER, reserved strictly for these 3 KPI numbers; Number formatting: pt-BR, integers for counts, exactly 1 decimal for conversão; Color: Success green Ganhos, Destructive red Perdidos, neutral-ink Taxa de conversão; Copywriting: labels "Ganhos"/"Perdidos"/"Taxa de conversão", empty "Nenhum dado no período selecionado.")
    - components/dashboard/ClientesPorEtapaChart.tsx (the loading/empty/error state pattern to mirror; Card usage)
    - app/actions/dashboard.ts (getGanhosPerdidos Server Action + its result type)
    - lib/dashboard/periodo.ts (taxaConversao — reuse, do not re-derive the division inline)
    - components/ui/card.tsx (Card/CardHeader/CardContent primitives)
  </read_first>
  <files>components/dashboard/GanhosPerdidosCards.tsx</files>
  <action>
    Create components/dashboard/GanhosPerdidosCards.tsx ("use client"). Accept the resolved period as an { inicio, fim } prop; on that prop changing, call the 04-01 getGanhosPerdidos Server Action and handle its discriminated-union result. Derive the two counts from the returned rows (status "ganho" / "perdido") and compute the rate with taxaConversao(ganho, perdido) from lib/dashboard/periodo.ts — never inline the division. Render three Cards in a md-gapped 3-column grid (stacked on mobile), lg internal padding: "Ganhos" (Success green Stat number + TrendingUp icon), "Perdidos" (Destructive red Stat number + TrendingDown icon), "Taxa de conversão" (neutral-foreground Stat number + Percent icon, muted). The Stat numbers use the 36px/600 waiver size (the only place in the project this size is allowed — do not reuse it elsewhere). Format all numbers with Intl.NumberFormat("pt-BR"): counts as plain integers, conversão with exactly one decimal and a "%" suffix; when ganho+perdido is 0, taxaConversao returns null → render the conversão value as "—" (an em dash), not "0,0%" or NaN. Own loading (Skeleton tiles), empty ("Nenhum dado no período selecionado."), and inline error/retry states, mirroring ClientesPorEtapaChart — a failure here must not blank the other blocks. Reuse the Success/Destructive tokens from Phase 2 status badges (no new hex). Keep TypeScript strict.
  </action>
  <acceptance_criteria>
    - The component imports taxaConversao from @/lib/dashboard/periodo and does not compute ganho/(ganho+perdido) inline (grep: no `/ (` division-on-counts expression in the file).
    - Number rendering uses Intl.NumberFormat("pt-BR") (grep confirms).
    - The three labels "Ganhos", "Perdidos", "Taxa de conversão" are present verbatim.
    - The conversão tile renders an em dash when there is no ganho/perdido data (guarded null branch present).
    - `npx tsc --noEmit` passes and `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'taxaConversao' components/dashboard/GanhosPerdidosCards.tsx && grep -q 'pt-BR' components/dashboard/GanhosPerdidosCards.tsx && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Three period-reactive KPI stat tiles render Ganhos, Perdidos, and Taxa de conversão as big numbers with correct pt-BR formatting and divide-by-zero handling.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the KPI row into DashboardClient</name>
  <read_first>
    - components/dashboard/DashboardClient.tsx (the metric-block slot ordering + how the resolved {inicio, fim} período is held and passed to period-filtered children)
    - .planning/phases/04-dashboard-gerencial/04-UI-SPEC.md (Screen-Specific Notes: the KPI row is block #2, directly below the etapa chart, above the desempenho/prospecção blocks)
  </read_first>
  <files>components/dashboard/DashboardClient.tsx</files>
  <action>
    Edit DashboardClient.tsx to render GanhosPerdidosCards in the second metric-block slot (directly below ClientesPorEtapaChart), passing the resolved { inicio, fim } período so the tiles react to the filter (D-02). Keep the etapa chart above it unaffected by the period (D-08). Leave the desempenho and prospecção slots as clearly-labeled placeholders for Wave 4. Do not change PeriodoFilter or ClientesPorEtapaChart.
  </action>
  <acceptance_criteria>
    - DashboardClient imports and renders GanhosPerdidosCards below ClientesPorEtapaChart, passing the período { inicio, fim } (grep confirms `GanhosPerdidosCards`).
    - ClientesPorEtapaChart still receives no período prop.
    - `npx tsc --noEmit` passes and `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'GanhosPerdidosCards' components/dashboard/DashboardClient.tsx && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>The KPI row appears on the dashboard below the etapa chart and updates when the period filter changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Server Action | KPI counts come from the RLS-scoped getGanhosPerdidos function; the period range crosses as typed timestamptz params. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Information Disclosure | GanhosPerdidosCards data path | high | mitigate | Counts derive from the RLS-scoped 04-01 function; a Vendedor's tiles physically only reflect their own clientes, with no client-side filtering. |
</threat_model>

<verification>
- Ganhos/Perdidos/Taxa de conversão tiles render as big numbers, period-reactive.
- Conversão shows one pt-BR decimal, and an em dash when there is no data.
- `npx tsc --noEmit` + `npm run build` clean.
</verification>

<success_criteria>
The dashboard shows win/loss counts and the conversion rate for the selected period as prominent hero figures, correctly scoped per role and formatted in pt-BR (DSH-02, DSH-04).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04-04)

- `components/dashboard/GanhosPerdidosCards.tsx` — 3 period-reactive KPI stat tiles (Ganhos / Perdidos / Taxa de conversão)
- `components/dashboard/DashboardClient.tsx` — now renders the KPI row in block slot 2
</artifacts_produced>

<output>
Create `.planning/phases/04-dashboard-gerencial/04-04-SUMMARY.md` when done.
</output>
