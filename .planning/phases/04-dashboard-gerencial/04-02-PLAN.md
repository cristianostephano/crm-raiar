---
phase: 04-dashboard-gerencial
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/chart.tsx
  - components/ui/skeleton.tsx
  - app/globals.css
  - package.json
  - package-lock.json
autonomous: false
requirements: [DSH-01, DSH-03, DSH-05]
user_setup: []
must_haves:
  truths:
    - "The shadcn Chart primitives (ChartContainer/ChartConfig/ChartTooltip/ChartTooltipContent) exist and the app still builds."
    - "Every bar chart in this phase renders in the project's blue accent, not the default grayscale ramp (--chart-1 = --primary)."
    - "recharts is only installed after a human confirms its legitimacy (SUS→approved disposition)."
  artifacts:
    - "components/ui/chart.tsx — shadcn Chart wrapper around Recharts"
    - "components/ui/skeleton.tsx — loading placeholder (if not already present)"
    - "app/globals.css — --chart-1 overridden to --primary in both :root and .dark"
  key_links:
    - "recharts@3.9.2 is pulled in as a dependency of `npx shadcn add chart`; the legitimacy checkpoint gates that install."
---

## Phase Goal

**As a** supervisor ou vendedor, **I want to** enxergar a saúde do funil em números na medida da minha própria visão, **so that** eu tenha a informação gerencial que faltava para largar o CRM pago atual.

> This plan installs the one new UI dependency the dashboard needs (the shadcn Chart component, which pulls in Recharts) and aligns its default color with the project's blue accent, so Waves 2-4 can render charts without any further install.

<objective>
Install the shadcn/ui `chart` component (and `skeleton`, if missing) and override the grayscale `--chart-1` CSS variable to the project's `--primary` blue, so every single-series bar chart in this phase renders in the established accent color instead of the neutral preset ramp. The `recharts` install is gated behind a blocking human legitimacy checkpoint per 04-RESEARCH.md's Package Legitimacy Audit (SUS→approved) and this project's Phase 2 precedent — the human confirmation is the control itself, not the research finding.

Purpose: This is the project's first data-visualization surface; the chart primitives and their color must exist and be verified before any chart component is written.
Output: `components/ui/chart.tsx`, `components/ui/skeleton.tsx`, updated `app/globals.css`, `recharts` in `package.json`.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-gerencial/04-RESEARCH.md
@.planning/phases/04-dashboard-gerencial/04-UI-SPEC.md

# The --chart-1..5 scaffold to override + the --primary values to match
@app/globals.css
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint — recharts</name>
  <files>none (gate only — no files are modified until Task 2)</files>
  <action>Pause for human legitimacy confirmation BEFORE any install runs. No automated work precedes this gate; it exists solely to require explicit human sign-off on the recharts dependency before Task 2 executes `npx shadcn@latest add chart`. Never auto-approve (legitimacy gates ignore workflow.auto_advance).</action>
  <what-built>
    Nothing yet — this checkpoint gates the install. 04-RESEARCH.md's Package Legitimacy Audit flagged `recharts` as SUS (the "too-new" heuristic fired on a recent patch release, 2026-07-04) then resolved to approved on strength of ~49M weekly downloads and the established `github.com/recharts/recharts` org. Per this project's protocol (Phase 2 plans 01-04/02-04 precedent), the human confirmation step is the control, independent of the research leaning approve. No postinstall script is present.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/recharts and confirm: publisher is the recharts org, weekly downloads are in the tens of millions, and the linked repo is github.com/recharts/recharts.
    2. Confirm the version to be installed is 3.9.2 (the version pinned in .claude/CLAUDE.md's Technology Stack table).
    3. Confirm this is the intended charting library for the dashboard (it is the CLAUDE.md-approved choice; shadcn's `chart` component wraps it).
  </how-to-verify>
  <acceptance_criteria>
    - A human has explicitly approved installing recharts@3.9.2 via `npx shadcn add chart`. This checkpoint is never auto-approved (workflow.auto_advance is ignored for legitimacy gates).
  </acceptance_criteria>
  <verify>
    <human-check>Human types "approved" (or names the concern to stop).</human-check>
  </verify>
  <resume-signal>Type "approved" to proceed with the install, or describe the concern.</resume-signal>
  <done>A human has confirmed recharts is legitimate and approved the install.</done>
</task>

<task type="auto">
  <name>Task 2: Install chart + skeleton, override --chart-1 to blue</name>
  <read_first>
    - app/globals.css (the --chart-1..--chart-5 grayscale scaffold at :root ~line 70 and .dark ~line 105; the --primary values at :root line 58 and .dark line 93)
    - .planning/phases/04-dashboard-gerencial/04-UI-SPEC.md (Color section: "override --chart-1 to the same value as --primary before wiring any chart"; Registry Safety section)
    - components.json (confirm the shadcn preset/registry so `add` uses the base-nova style)
  </read_first>
  <files>components/ui/chart.tsx, components/ui/skeleton.tsx, app/globals.css, package.json, package-lock.json</files>
  <action>
    First check whether components/ui/skeleton.tsx already exists (ls) — the UI-SPEC requires it for chart loading states and it may not be present yet. Run `npx shadcn@latest add chart skeleton` (drop `skeleton` from the argument list if it already exists) to generate components/ui/chart.tsx (ChartContainer/ChartConfig/ChartTooltip/ChartTooltipContent) and pull in recharts@3.9.2 as an npm dependency. If the shadcn CLI cannot resolve a base-nova chart variant, fall back to fetching the official chart component from ui.shadcn.com per 04-RESEARCH.md Assumption A3, keeping the same export names. Then edit app/globals.css: in BOTH the :root block and the .dark block, change the `--chart-1` line from its grayscale `oklch(...)` value to `var(--primary)` so bar fills reference the project's blue accent (light and dark stay in sync automatically). Leave --chart-2 through --chart-5 untouched — every chart this phase renders is single-series, so no categorical ramp is needed (UI-SPEC "never rainbow a single series"). Do not change any other token.
  </action>
  <acceptance_criteria>
    - components/ui/chart.tsx exists and exports ChartContainer and ChartTooltipContent (grep confirms).
    - components/ui/skeleton.tsx exists.
    - recharts appears in package.json dependencies.
    - app/globals.css sets `--chart-1: var(--primary);` in both the :root and .dark blocks (grep count of `--chart-1: var(--primary)` is 2).
    - `npx tsc --noEmit` passes and `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -c -- '--chart-1: var(--primary)' app/globals.css | grep -qx 2 && test -f components/ui/chart.tsx && npx tsc --noEmit && echo CHART-OK</automated>
  </verify>
  <done>The shadcn Chart + Skeleton primitives exist, recharts is installed, bar fills resolve to the project's blue accent, and the app still builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local dependency tree | A new third-party package (recharts) enters the build via the shadcn CLI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | npm install of recharts via `npx shadcn add chart` | high | mitigate | Blocking human legitimacy checkpoint (Task 1) verified against npmjs.com before install; never auto-approved. recharts has no postinstall script. |
</threat_model>

<verification>
- Human legitimacy checkpoint approved before install.
- components/ui/chart.tsx + components/ui/skeleton.tsx present.
- `--chart-1: var(--primary)` in both :root and .dark.
- `npx tsc --noEmit` and `npm run build` clean.
</verification>

<success_criteria>
The dashboard's charting primitives are installed and color-aligned to the project's blue accent, gated behind a human-approved package-legitimacy checkpoint, with the app still building.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04-02)

- `components/ui/chart.tsx` — shadcn Chart wrapper (ChartContainer, ChartConfig, ChartTooltip, ChartTooltipContent)
- `components/ui/skeleton.tsx` — loading placeholder (if newly added)
- `recharts@3.9.2` — new npm dependency
- `app/globals.css` — `--chart-1` overridden to `var(--primary)` in :root and .dark
</artifacts_produced>

<output>
Create `.planning/phases/04-dashboard-gerencial/04-02-SUMMARY.md` when done.
</output>
