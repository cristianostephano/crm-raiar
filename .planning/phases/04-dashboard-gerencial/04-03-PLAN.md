---
phase: 04-dashboard-gerencial
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - app/(app)/dashboard/page.tsx
  - app/(app)/layout.tsx
  - components/dashboard/DashboardClient.tsx
  - components/dashboard/PeriodoFilter.tsx
  - components/dashboard/ClientesPorEtapaChart.tsx
autonomous: true
requirements: [DSH-01, DSH-06, DSH-07]
must_haves:
  truths:
    - "A 'Dashboard' link appears in the top nav for both roles, and /dashboard is the screen it opens — the kanban (/clientes) stays the post-login landing (D-03)."
    - "The dashboard has a period filter (Últimos 30 dias / Este mês / Este ano / Personalizado); presets apply immediately, custom range needs an explicit Aplicar (D-01)."
    - "The 'Clientes por etapa do funil' chart shows one bar per etapa as a live snapshot, with a caption saying it is NOT affected by the period filter (D-04/D-08)."
    - "A Vendedor sees only their own per-etapa counts; a Supervisor sees the whole team — with no frontend filtering (RLS does it, DSH-06/DSH-07)."
  artifacts:
    - "app/(app)/dashboard/page.tsx — Server Component, role read, renders DashboardClient"
    - "components/dashboard/DashboardClient.tsx — Client orchestrator holding período state"
    - "components/dashboard/PeriodoFilter.tsx — Select presets + Popover/Calendar range"
    - "components/dashboard/ClientesPorEtapaChart.tsx — vertical BarChart (D-04)"
  key_links:
    - "page.tsx passes isSupervisor (from profile.role) as a prop into DashboardClient — the same profile?.role === 'supervisor' conditional used in layout.tsx."
    - "ClientesPorEtapaChart x-axis labels come from lib/funil/etapas.ts ETAPAS, never a second hardcoded map."
---

## Phase Goal

**As a** supervisor ou vendedor, **I want to** enxergar a saúde do funil em números na medida da minha própria visão, **so that** eu tenha a informação gerencial que faltava para largar o CRM pago atual.

> First visible slice: after this plan a real user can click "Dashboard" in the nav, land on /dashboard, choose a period, and see a real bar chart of clientes por etapa scoped to their own view. The remaining metrics arrive in Waves 3-4.

<objective>
Stand up the Dashboard screen end-to-end for its first metric: a protected `/dashboard` route with a role-aware header, a "Dashboard" nav link visible to both roles, a period filter (preset Select + custom range Popover/Calendar), and the "Clientes por etapa do funil" vertical bar chart wired to the Server Action from 04-01. This is the thinnest complete vertical slice — navigate, filter, see a real RLS-scoped chart — that the later KPI and horizontal-bar slices build onto.

Purpose: Deliver a working, user-navigable dashboard as early as possible (MVP vertical-slice rule) rather than deferring all UI to the end, and establish the DashboardClient orchestrator + PeriodoFilter that every remaining metric plugs into.
Output: `app/(app)/dashboard/page.tsx`, the "Dashboard" nav link in `app/(app)/layout.tsx`, `DashboardClient.tsx`, `PeriodoFilter.tsx`, `ClientesPorEtapaChart.tsx`.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-gerencial/04-RESEARCH.md
@.planning/phases/04-dashboard-gerencial/04-PATTERNS.md
@.planning/phases/04-dashboard-gerencial/04-UI-SPEC.md

# Analogs to mirror
@app/(app)/layout.tsx
@components/clientes/ClienteDetailSheet.tsx
@components/configuracoes/ConfiguracoesTabs.tsx
@lib/funil/etapas.ts
# 04-01 artifacts this plan calls
@app/actions/dashboard.ts
@lib/dashboard/periodo.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route, nav link, orchestrator + period filter</name>
  <read_first>
    - app/(app)/layout.tsx (auth guard already at layout level; the "Clientes" Link at lines 55-60 to copy for the new "Dashboard" link; the profile?.role === "supervisor" conditional at lines 61-76)
    - components/configuracoes/ConfiguracoesTabs.tsx (Client Component that holds shared state and passes it to independently-fetching children — the orchestration pattern to mirror; note the keepMounted lesson if any Tabs are ever used)
    - components/clientes/ClienteDetailSheet.tsx lines 1010-1026 (Popover + Calendar single-date composition and the exact PopoverTrigger className to reuse, extended to mode="range")
    - components/ui/select.tsx, components/ui/popover.tsx, components/ui/calendar.tsx (installed primitives)
    - lib/dashboard/periodo.ts (PeriodoPreset, resolvePeriodo — the source of {inicio, fim})
    - .planning/phases/04-dashboard-gerencial/04-UI-SPEC.md (Copywriting Contract: page title, role-aware subtitle, period labels; Screen-Specific Notes: layout order and gaps)
  </read_first>
  <files>app/(app)/dashboard/page.tsx, app/(app)/layout.tsx, components/dashboard/DashboardClient.tsx, components/dashboard/PeriodoFilter.tsx</files>
  <action>
    Create app/(app)/dashboard/page.tsx as a Server Component (mirror the auth/role read already in layout.tsx): call createClient(), getUser(), read profile.role from profiles, and render DashboardClient with an isSupervisor={profile?.role === "supervisor"} boolean prop — do not re-derive role client-side. Add a "Dashboard" Link to app/(app)/layout.tsx immediately after the "Clientes" link (before the Supervisor-only links), using the exact same className, visible to BOTH roles (D-03) — the kanban /clientes stays the landing screen, this only adds a nav entry. Create components/dashboard/DashboardClient.tsx ("use client"): render the page-level Display heading "Dashboard" and the role-aware subtitle ("Números da equipe" for Supervisor, "Seus números de vendas" for Vendedor), then the PeriodoFilter row, then a vertical stack of chart-block slots (for now only ClientesPorEtapaChart is wired — leave clearly-labeled placeholders/comments where the KPI, desempenho, and prospecção blocks land in later plans). Hold período state here: a preset value plus an optional committed custom {from, to}; compute the resolved {inicio, fim} via resolvePeriodo and pass it down as props to period-filtered children (ClientesPorEtapaChart does NOT receive it — it is a snapshot per D-08). Create components/dashboard/PeriodoFilter.tsx ("use client"): a Select with the four presets in order ("Últimos 30 dias" / "Este mês" / "Este ano" / "Personalizado") that applies immediately on change; when "Personalizado" is chosen, reveal a Popover whose trigger uses the "w-fit rounded-lg border border-input px-2.5 py-1.5 text-left text-sm" className from ClienteDetailSheet, containing a Calendar mode="range" numberOfMonths={2} and an "Aplicar período" (primary) / "Cancelar" (ghost) footer — only the committed range updates the parent's período (a half-selected range is not yet valid). Keep TypeScript strict.
  </action>
  <acceptance_criteria>
    - app/(app)/layout.tsx renders a Link to /dashboard with the same className as the Clientes link, placed before the supervisor-only links (grep confirms `href="/dashboard"`).
    - app/(app)/dashboard/page.tsx passes an isSupervisor boolean prop to DashboardClient derived from profile.role.
    - PeriodoFilter offers exactly the four preset labels from the UI-SPEC and only commits a custom range on "Aplicar período".
    - DashboardClient shows the role-aware subtitle text verbatim from the Copywriting Contract.
    - `npx tsc --noEmit` passes and `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'href="/dashboard"' 'app/(app)/layout.tsx' && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>A Dashboard nav link exists for both roles, /dashboard renders a role-aware header and a working period filter, and the orchestrator is ready to host metric charts.</done>
</task>

<task type="auto">
  <name>Task 2: Clientes por etapa chart (DSH-01) wired end-to-end</name>
  <read_first>
    - .planning/phases/04-dashboard-gerencial/04-RESEARCH.md (Pattern 5 vertical bars; Pitfall 3 sizing class h-[280px] w-full)
    - .planning/phases/04-dashboard-gerencial/04-UI-SPEC.md (Screen-Specific Notes block 1: etapa chart caption "Situação atual — não é afetado pelo filtro de período"; empty/loading/error copy; Color: single blue bar via --chart-1)
    - lib/funil/etapas.ts (ETAPAS — the etapa→label map to reuse for the x-axis)
    - components/ui/chart.tsx (ChartContainer/ChartConfig/ChartTooltip/ChartTooltipContent — created in 04-02)
    - components/ui/card.tsx, components/ui/skeleton.tsx
    - app/actions/dashboard.ts (the getClientesPorEtapa Server Action wrapper)
  </read_first>
  <files>components/dashboard/ClientesPorEtapaChart.tsx, components/dashboard/DashboardClient.tsx</files>
  <action>
    Create components/dashboard/ClientesPorEtapaChart.tsx ("use client"): on mount, call the 04-01 Server Action for clientes-por-etapa (no period argument — D-08 snapshot), handle its discriminated-union result. Render a Card with a Heading title "Clientes por etapa do funil" and a Meta caption "Situação atual — não é afetado pelo filtro de período" in the CardHeader; in CardContent render a Recharts vertical BarChart inside ChartContainer with className="h-[280px] w-full" and accessibilityLayer, one bar per etapa, x-axis labels mapped through ETAPAS from lib/funil/etapas.ts (never a second hardcoded label map), single bar fill via var(--color-total) resolving to the blue --chart-1. Own this component's states: Skeleton at h-[280px] while loading (never a bare empty div — Pitfall 3), the empty-state copy "Nenhum cliente cadastrado ainda." centered in the fixed-height area when zero clients exist, and the inline error card ("Não foi possível carregar os dados do dashboard. Tente novamente." + an outline "Tentar novamente" button) on fetch failure — an error here must not blank the rest of the page. Wire this component into DashboardClient's first chart-block slot. Keep TypeScript strict (no `any` without a justifying comment).
  </action>
  <acceptance_criteria>
    - ClientesPorEtapaChart imports ETAPAS from @/lib/funil/etapas and does NOT define its own etapa→label object (grep: no inline etapa label literals like "Aguardando contato" in the file).
    - ChartContainer carries a fixed height class (grep confirms `h-[280px]`).
    - The component renders distinct loading (Skeleton), empty, and error states with the UI-SPEC copy.
    - DashboardClient renders ClientesPorEtapaChart above the period-filtered blocks.
    - `npx tsc --noEmit` passes and `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'h-\[280px\]' components/dashboard/ClientesPorEtapaChart.tsx && grep -q 'from "@/lib/funil/etapas"' components/dashboard/ClientesPorEtapaChart.tsx && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>The dashboard shows a live, RLS-scoped "Clientes por etapa do funil" bar chart with the not-period-filtered caption, plus proper loading/empty/error states — the first complete on-screen metric (DSH-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Server Action | The chart fetches aggregated data via the 04-01 Server Actions; role/ownership scoping is already enforced server-side by RLS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Information Disclosure | ClientesPorEtapaChart data path | high | mitigate | No client-side filtering by responsavel — the count comes from the RLS-scoped dashboard_clientes_por_etapa function (04-01); the UI merely renders whatever the caller is allowed to see. |
| T-04-05 | Spoofing | isSupervisor prop from page.tsx | low | accept | The prop only toggles UX (which blocks render); it is never the authorization boundary — RLS is. A spoofed prop cannot reveal another vendedor's rows because the data is RLS-scoped regardless. |
</threat_model>

<verification>
- "Dashboard" nav link visible to both roles; /clientes remains the landing route.
- Period filter offers the four presets; custom range commits only on Aplicar.
- Clientes-por-etapa chart renders with the snapshot caption and blue bars.
- `npx tsc --noEmit` + `npm run build` clean.
</verification>

<success_criteria>
A user of either role can open /dashboard from the nav, select a period, and see a live RLS-scoped "Clientes por etapa" bar chart — the first working vertical slice of the dashboard (DSH-01, with the page shell for DSH-06/DSH-07).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04-03)

- `app/(app)/dashboard/page.tsx` — protected Server Component route, reads role, renders DashboardClient
- `app/(app)/layout.tsx` — adds the "Dashboard" nav Link (both roles)
- `components/dashboard/DashboardClient.tsx` — Client orchestrator; holds período state, hosts metric-block slots
- `components/dashboard/PeriodoFilter.tsx` — preset Select + custom-range Popover/Calendar
- `components/dashboard/ClientesPorEtapaChart.tsx` — vertical BarChart for DSH-01
</artifacts_produced>

<output>
Create `.planning/phases/04-dashboard-gerencial/04-03-SUMMARY.md` when done.
</output>
