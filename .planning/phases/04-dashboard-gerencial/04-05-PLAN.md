---
phase: 04-dashboard-gerencial
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - components/dashboard/DesempenhoVendedorChart.tsx
  - components/dashboard/ProspeccaoChart.tsx
  - components/dashboard/DashboardClient.tsx
autonomous: false
requirements: [DSH-03, DSH-05, DSH-07]
must_haves:
  truths:
    - "The Supervisor sees a 'Desempenho por vendedor' horizontal-bar chart (one bar per vendedor, ganhos no período, sorted descending); the Vendedor does not see this block at all (D-06/D-07/DSH-03)."
    - "The dashboard shows 'Prospecção por produto' and 'Prospecção por categoria' as horizontal-bar charts, counting clientes cadastrados by criado_em within the period (D-06/D-09/DSH-05)."
    - "A human has verified the complete dashboard as both a Supervisor and a Vendedor."
  artifacts:
    - "components/dashboard/DesempenhoVendedorChart.tsx — Supervisor-only horizontal bars"
    - "components/dashboard/ProspeccaoChart.tsx — reusable horizontal-bar chart (produto + categoria)"
  key_links:
    - "DesempenhoVendedorChart is rendered only when isSupervisor is true (mirrors layout.tsx's supervisor-only nav conditional) — a UX nicety over the RLS boundary, not the boundary itself."
    - "ProspeccaoChart is instantiated twice with different Server Actions/titles (produto, categoria)."
---

## Phase Goal

**As a** supervisor ou vendedor, **I want to** enxergar a saúde do funil em números na medida da minha própria visão, **so that** eu tenha a informação gerencial que faltava para largar o CRM pago atual.

> Final slice of the MVP: the Supervisor-only per-vendedor performance chart and the product/category prospecting charts complete the dashboard, and a human signs off on the whole screen for both roles. After this plan ships, all 7 dashboard requirements — and all 32 v1 requirements — are done.

<objective>
Complete the dashboard with its two remaining metric groups: the Supervisor-only "Desempenho por vendedor" horizontal-bar chart (one bar per vendedor, ganhos in the selected period, sorted descending — D-10's win-count metric) and the reusable "Prospecção por produto"/"Prospecção por categoria" horizontal-bar charts (clientes cadastrados by criado_em within the period — D-09). Then gate the phase on a human visual verification of the full dashboard as both a Supervisor and a Vendedor.

Purpose: Deliver the team-wide management view the Supervisor needs (who is winning deals, what is being prospected) while confirming a Vendedor sees only their own scoped view with the per-vendedor block correctly absent (D-07).
Output: `DesempenhoVendedorChart.tsx`, `ProspeccaoChart.tsx`, both wired into `DashboardClient.tsx`, plus a passing human-verify checkpoint.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-gerencial/04-RESEARCH.md
@.planning/phases/04-dashboard-gerencial/04-UI-SPEC.md

# Analogs + prior-plan artifacts this plan uses
@components/dashboard/DashboardClient.tsx
@components/dashboard/GanhosPerdidosCards.tsx
@app/actions/dashboard.ts
@app/(app)/layout.tsx
@components/ui/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Desempenho por vendedor + reusable Prospecção horizontal-bar charts (DSH-03, DSH-05)</name>
  <read_first>
    - .planning/phases/04-dashboard-gerencial/04-RESEARCH.md (Pattern 4 horizontal bars via layout="vertical"; Pattern 3 RLS auto-scoping desempenho; Pitfall 3 sizing)
    - .planning/phases/04-dashboard-gerencial/04-UI-SPEC.md (Spacing: variable-row-count chart height — min 280px, +40px per row, max 480px then internal scroll; Copywriting: titles/captions "Desempenho por vendedor"/"Ganhos no período, por responsável", "Prospecção por produto"/"Clientes cadastrados no período, por produto consumido", "Prospecção por categoria"/"Clientes cadastrados no período, por categoria"; empty "Nenhum dado no período selecionado.")
    - components/dashboard/GanhosPerdidosCards.tsx and ClientesPorEtapaChart.tsx (the fetch-on-período + loading/empty/error pattern to mirror)
    - app/actions/dashboard.ts (getDesempenhoVendedor, getProspeccaoPorProduto, getProspeccaoPorCategoria Server Actions + result types)
  </read_first>
  <files>components/dashboard/DesempenhoVendedorChart.tsx, components/dashboard/ProspeccaoChart.tsx</files>
  <action>
    Create components/dashboard/ProspeccaoChart.tsx ("use client") as a REUSABLE horizontal-bar chart: props are the resolved { inicio, fim } período, a title, a caption, and the Server Action to call (so the same component serves both produto and categoria — do not write two near-identical files). Render a Card (Heading title + Meta caption), then a Recharts BarChart with layout="vertical" inside ChartContainer, category axis = the produto/categoria nome, numeric axis = total, single blue bar fill via --chart-1, accessibilityLayer on. Size per the variable-row-count rule: base height 280px growing ~40px per row, capped at 480px with internal vertical scroll (compute height from the row count). Own loading (Skeleton)/empty ("Nenhum dado no período selecionado.")/error+retry states. Create components/dashboard/DesempenhoVendedorChart.tsx ("use client"): same horizontal-bar shape, calling getDesempenhoVendedor for the period, one bar per vendedor plotting their ganho count (D-10), sorted descending so the top performer reads first; title "Desempenho por vendedor", caption "Ganhos no período, por responsável". This component assumes it is only mounted for a Supervisor (the gating happens in DashboardClient) — it does not need its own role check, and it must never be the authorization boundary. Reuse ETAPAS-style label sourcing only where applicable (vendedor/produto/categoria names come from the Server Action rows, not a hardcoded map). Keep TypeScript strict.
  </action>
  <acceptance_criteria>
    - ProspeccaoChart is a single parameterized component accepting a title, caption, período, and an action to call (grep confirms a prop typed as a function / action).
    - Both charts use Recharts layout="vertical" for horizontal bars and a fixed/computed ChartContainer height (grep confirms `layout="vertical"`).
    - DesempenhoVendedorChart sorts vendedores by ganho descending (sort present) and carries the caption "Ganhos no período, por responsável".
    - Both own loading/empty/error states with the UI-SPEC copy.
    - `npx tsc --noEmit` passes and `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'layout="vertical"' components/dashboard/ProspeccaoChart.tsx && grep -q 'layout="vertical"' components/dashboard/DesempenhoVendedorChart.tsx && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>A reusable horizontal-bar Prospecção chart and a Supervisor-scoped Desempenho chart exist, each with proper sizing and state handling.</done>
</task>

<task type="auto">
  <name>Task 2: Wire desempenho (Supervisor-only) + prospecção grid into DashboardClient</name>
  <read_first>
    - components/dashboard/DashboardClient.tsx (isSupervisor prop; the remaining desempenho + prospecção slots; how período is passed to filtered children)
    - app/(app)/layout.tsx lines 61-76 (the profile?.role === "supervisor" conditional-render pattern to mirror for the desempenho block)
    - .planning/phases/04-dashboard-gerencial/04-UI-SPEC.md (Screen-Specific Notes: block #3 desempenho Supervisor-only; block #4 prospecção produto+categoria in a 2-column grid, stacked on mobile)
  </read_first>
  <files>components/dashboard/DashboardClient.tsx</files>
  <action>
    Edit DashboardClient.tsx: in the third metric-block slot, render DesempenhoVendedorChart ONLY when isSupervisor is true (omit it entirely for a Vendedor — do not render it disabled or blank, mirroring layout.tsx's supervisor-only nav links; D-07). In the fourth slot, render two ProspeccaoChart instances in a 2-column grid (stacked on mobile): one titled "Prospecção por produto" (caption "Clientes cadastrados no período, por produto consumido", action getProspeccaoPorProduto) and one titled "Prospecção por categoria" (caption "Clientes cadastrados no período, por categoria", action getProspeccaoPorCategoria) — both shown to both roles, both receiving the resolved { inicio, fim } período (D-09). Do not touch the etapa chart (snapshot) or the KPI wiring.
  </action>
  <acceptance_criteria>
    - DashboardClient renders DesempenhoVendedorChart inside an isSupervisor conditional (grep confirms both `isSupervisor` and `DesempenhoVendedorChart` co-located in a conditional branch).
    - DashboardClient renders two ProspeccaoChart instances with the produto and categoria titles/actions, both passed the período.
    - `npx tsc --noEmit` passes and `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'DesempenhoVendedorChart' components/dashboard/DashboardClient.tsx && grep -q 'ProspeccaoChart' components/dashboard/DashboardClient.tsx && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>The complete dashboard renders: etapa snapshot, KPI row, Supervisor-only desempenho, and the produto/categoria prospecção grid — all period-aware except the etapa snapshot.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the full dashboard (both roles)</name>
  <files>none (verification gate — no files are modified)</files>
  <action>After Tasks 1-2 have automated the complete dashboard, pause for a human to visually verify it end-to-end as both a Supervisor and a Vendedor using the steps below, then resume only on explicit approval.</action>
  <what-built>
    The complete Dashboard Gerencial: a "Dashboard" nav link (both roles), a period filter (presets + custom range), "Clientes por etapa do funil" (live snapshot), the Ganhos/Perdidos/Taxa de conversão KPI row, the Supervisor-only "Desempenho por vendedor" chart, and the "Prospecção por produto"/"Prospecção por categoria" charts — all RLS-scoped server-side.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev`. Log in as the Supervisor. Click "Dashboard" in the nav (the kanban /clientes should still be the landing screen).
    2. Confirm all blocks render: etapa bar chart with the caption "Situação atual — não é afetado pelo filtro de período"; the 3 KPI tiles (Ganhos green, Perdidos red, Taxa de conversão neutral); "Desempenho por vendedor" horizontal bars; "Prospecção por produto" and "Prospecção por categoria".
    3. Change the period (Últimos 30 dias → Este ano → Personalizado with a custom range + Aplicar) and confirm the KPI/desempenho/prospecção numbers change while the etapa chart stays put.
    4. Log out and log in as a Vendedor. Open the Dashboard and confirm: the "Desempenho por vendedor" block is ABSENT (not blank/disabled), and every visible number reflects only that vendedor's own clientes (cross-check against a couple of known clientes).
    5. Confirm numbers are in pt-BR (dot thousands, comma decimals) and the conversão shows one decimal (or an em dash when there is no data).
  </how-to-verify>
  <acceptance_criteria>
    - A human confirms every block renders correctly for the Supervisor, the desempenho block is absent for the Vendedor, the period filter drives the filtered blocks (not the etapa snapshot), and a Vendedor sees only their own numbers.
  </acceptance_criteria>
  <verify>
    <human-check>Human types "approved" or describes the issues to fix.</human-check>
  </verify>
  <resume-signal>Type "approved" to close the phase, or describe what looks wrong.</resume-signal>
  <done>A human has verified the full dashboard for both roles, including the D-07 role difference and the D-08 snapshot behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Server Action | Desempenho and prospecção data come from the RLS-scoped 04-01 functions; the isSupervisor prop only toggles which UI blocks render. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-07 | Information Disclosure | DesempenhoVendedorChart (per-vendedor breakdown) | high | mitigate | The data is RLS-scoped: a Vendedor calling getDesempenhoVendedor gets back only their own single row regardless of the UI. Hiding the block for Vendedores is a UX nicety over that boundary, verified by the human checkpoint (Vendedor sees no per-vendedor block). |
| T-04-08 | Elevation of Privilege | isSupervisor conditional render | low | accept | Even if the conditional were bypassed, a Vendedor's desempenho data is a single self-row via RLS — no other vendedor's numbers are reachable. |
</threat_model>

<verification>
- Supervisor sees all four block groups; period filter drives the filtered ones.
- Vendedor sees every block except "Desempenho por vendedor", scoped to own data.
- `npx tsc --noEmit` + `npm run build` clean.
- Human checkpoint approved.
</verification>

<success_criteria>
The full Dashboard Gerencial renders correctly for both roles — Supervisor sees the team-wide per-vendedor and prospecção breakdowns, Vendedor sees only their own scoped numbers with the per-vendedor block absent — completing DSH-03, DSH-05, DSH-07 and the phase (and the v1 milestone).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04-05)

- `components/dashboard/DesempenhoVendedorChart.tsx` — Supervisor-only horizontal-bar chart (ganhos por vendedor)
- `components/dashboard/ProspeccaoChart.tsx` — reusable horizontal-bar chart (instantiated for produto and categoria)
- `components/dashboard/DashboardClient.tsx` — now renders all four metric-block groups (desempenho gated by isSupervisor)
</artifacts_produced>

<output>
Create `.planning/phases/04-dashboard-gerencial/04-05-SUMMARY.md` when done.
</output>
