---
phase: 06-importa-o-upload-mapeamento-e-revis-o
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - lib/importacao/dedupe.ts
  - lib/validations/importacao.ts
  - lib/importacao/annotarLinha.ts
  - app/actions/importacao.ts
  - tests/importacao/dedupe.test.ts
  - tests/importacao/annotarLinha.test.ts
  - tests/importacao/rls-dedup-read.test.ts
autonomous: true
requirements: [IMP-04, IMP-05, IMP-07, IMP-10]
user_setup: []

must_haves:
  truths:
    - "Duas razões sociais que são a mesma empresa com formatação diferente (maiúsc/minúsc, acento, pontuação, sufixo Ltda/S.A.) produzem a mesma chave de deduplicação e são marcadas como possível duplicado (IMP-07)"
    - "Uma linha sem razão social, sem endereço (CEP/rua/número/cidade/estado) ou sem responsável é marcada como erro com motivo claro (IMP-05)"
    - "Uma linha que referencia uma categoria/produto inexistente é marcada como erro (ex: categoria 'X' não existe), nunca criada automaticamente nem gravada em branco (D-02)"
    - "A coluna de vendedor da planilha é resolvida contra profiles; um vendedor não encontrado vira erro na linha (IMP-04)"
    - "validarLoteImportacao rejeita um chamador não-Supervisor (IMP-10) e não grava NADA no banco"
  artifacts:
    - lib/importacao/dedupe.ts
    - lib/validations/importacao.ts
    - lib/importacao/annotarLinha.ts
    - app/actions/importacao.ts
    - tests/importacao/dedupe.test.ts
    - tests/importacao/annotarLinha.test.ts
    - tests/importacao/rls-dedup-read.test.ts
  key_links:
    - "A checagem de duplicado no banco é uma leitura RLS-scoped em clientes (Supervisor vê a base toda; nenhum filtro manual de responsavel), reusando o padrão de getClientesParaExportacao"
    - "annotarLinha é uma função pura (recebe as listas de lookup como parâmetro) — toda a lógica IMP-04/IMP-05/D-02 é unit-testável sem Supabase"
    - "Valores de célula do usuário passam por sanitizeCell (import de lib/clientes/exportacao.ts) antes de virar dado resolvido, fechando A4 no lado da importação"
    - "A precedência de status é erro > duplicado > ok"
---

<objective>
Construir o motor de validação read-only da importação: normalização/detecção de duplicados, o schema Zod de linha (regras mínimas do cadastro), a função pura que anota cada linha (ok/erro/duplicado + resolução de categoria/produto/vendedor), e a Server Action `validarLoteImportacao` que orquestra tudo com leituras RLS-scoped — sem escrever nada.

Purpose: É o coração testável da fase. IMP-05 (validação por linha), IMP-07 (duplicados), IMP-04 (coluna do vendedor) e o portão server-side do IMP-10 vivem aqui. Isolar a lógica em funções puras (annotarLinha, dedupe) deixa cada regra provada por teste unitário sem depender de UI nem de banco; a Server Action só junta as peças e busca os lookups já filtrados por RLS.

Output: `lib/importacao/dedupe.ts`, `lib/validations/importacao.ts`, `lib/importacao/annotarLinha.ts`, `app/actions/importacao.ts` (validarLoteImportacao) e a suíte de testes (dedupe + annotarLinha puros; rls-dedup-read com env).
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-importa-o-upload-mapeamento-e-revis-o/06-CONTEXT.md
@.planning/phases/06-importa-o-upload-mapeamento-e-revis-o/06-PATTERNS.md

# Reuse these exact shapes/paths — created by 06-01 and by earlier phases
@lib/importacao/types.ts
@lib/validations/cliente.ts
@app/actions/clientes.ts
@lib/supabase/queries/clientes.ts
@lib/clientes/exportacao.ts
@tests/clientes/rls-exportacao.test.ts
@tests/helpers/supabase-test-clients.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalização de razão social + detecção de duplicados (IMP-07 / D-03)</name>
  <files>lib/importacao/dedupe.ts, tests/importacao/dedupe.test.ts</files>
  <behavior>
    - normalizeRazaoSocial("Distribuidora ABC Ltda") === normalizeRazaoSocial("DISTRIBUIDORA ABC LTDA.") === normalizeRazaoSocial("Distribuidóra  ABC") — lowercase, remoção de acentos, remoção de pontuação, colapso de espaços, remoção de sufixos societários (ltda, s/a, s.a., sa, me, eireli, epp, mei) produzem a MESMA chave.
    - Duas empresas genuinamente diferentes ("Padaria do Zé" vs "Mercado do João") produzem chaves diferentes.
    - findDuplicates: dada uma lista de razões sociais do batch + uma lista de razões sociais já existentes, marca como duplicado toda linha cuja chave normalizada bate com uma existente OU com outra linha do próprio batch (D-03 — duplicado interno recebe o mesmo tratamento), devolvendo o nome "parecido" que casou (para o selo "Possível duplicado de '{nome}'").
    - Uma razão social única (nem no banco nem repetida no batch) não é marcada.
  </behavior>
  <action>
    Criar `lib/importacao/dedupe.ts`, módulo puro (sem Supabase, sem diretivas), na forma dependency-free de `lib/clientes/completude.ts` (o PATTERNS.md aponta essa como a referência de forma, não de lógica — não há algoritmo de dedup para copiar, é novo). Exportar:
    - `normalizeRazaoSocial(valor: string): string` — lowercase; normalizar acentos via `String.prototype.normalize("NFD")` + remover diacríticos; remover pontuação; colapsar espaços; remover sufixos societários comuns (ltda, s/a, s.a., sa, me, eireli, epp, mei) do fim; trim. Nunca sobrescrever a razão social original — isto é só a chave de comparação (Pitfall A1).
    - `findDuplicates(batch: { row: number; razaoSocial: string }[], existentes: string[]): Map<number, string>` — mapeia índice-de-linha → nome parecido que casou, considerando tanto `existentes` (banco) quanto colisões dentro do próprio batch (D-03). Retornar o nome legível (a razão social original do match), não a chave normalizada.

    Criar `tests/importacao/dedupe.test.ts` (estilo pure-function, sem env) cobrindo todos os casos do <behavior>, incluindo pelo menos um par "mesma empresa, formatação diferente" (case + acento + sufixo) e um duplicado puramente interno ao batch.
  </action>
  <verify>
    <automated>npm test -- dedupe</automated>
  </verify>
  <done>normalizeRazaoSocial colapsa variações de formatação/sufixo na mesma chave; findDuplicates flagueia contra banco E dentro do batch (D-03), devolvendo o nome parecido; testes de dedupe passam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema Zod de linha + anotação pura de cada linha (IMP-04 / IMP-05 / D-02)</name>
  <files>lib/validations/importacao.ts, lib/importacao/annotarLinha.ts, tests/importacao/annotarLinha.test.ts</files>
  <behavior>
    - createImportRowSchema aceita uma linha com razaoSocial/cep/rua/numero/cidade/estado/responsavel preenchidos → válida; faltando qualquer required → inválida, com o campo apontado (as mesmas regras mínimas de createClienteSchema, IMP-05).
    - annotarLinha(linhaMapeada, lookups) com todos os required presentes, categoria/produto/vendedor resolvíveis → { status: "ok", resolved: {...ids...} }.
    - annotarLinha com razão social/endereço/responsável faltando → { status: "erro", reasons: ["Razão social não informada", ...] } (mensagens do 06-UI-SPEC).
    - annotarLinha com categoria que não existe nos lookups → { status: "erro", reasons: ['Categoria "X" não existe'] } (D-02) — nunca resolve em branco, nunca "cria".
    - annotarLinha com um produto (multi-valor) inexistente → erro análogo para produto (D-02).
    - annotarLinha com um responsável (vendedor) não encontrado nos lookups → erro (IMP-04); um encontrado por email exato OU por nome completo normalizado resolve para o id do profile.
    - Todo valor de célula do usuário passa por sanitizeCell antes de entrar em `resolved` (A4).
  </behavior>
  <action>
    Criar `lib/validations/importacao.ts` reusando as regras de `lib/validations/cliente.ts`: exportar `createImportRowSchema` com os mesmos campos required (razaoSocial, cep, rua, numero, cidade, estado, responsavel) e opcionais (complemento, contato, telefone, email, numeroDeLojas). Usar `.superRefine` (não `.refine()` por campo) se precisar de checagem cross-field, pela mesma razão documentada em cliente.ts (ZodEffects quebra @hookform/resolvers no zod v4). Aqui o schema é validado direto com `.safeParse()` no servidor (sem react-hook-form), então a restrição só morde se o schema for reusado client-side.

    Criar `lib/importacao/annotarLinha.ts`, função PURA (sem Supabase, sem diretivas) — esta é a peça central testável de IMP-04/IMP-05/D-02. Assinatura sugerida: `annotarLinha(linha: MappedRow, lookups: { vendedores: {id,nome,sobrenome,email}[]; categorias: LookupOption[]; produtos: LookupOption[] }): AnnotatedRow`, onde `MappedRow` é `Partial<Record<SystemField, string>>` (de `lib/importacao/types.ts`) e `AnnotatedRow = { status: "ok" | "erro"; reasons: string[]; resolved: ResolvedRow }` (o status "duplicado" é aplicado depois pela Server Action, via dedupe — annotarLinha só decide ok vs erro). Passos, na ordem:
    1. Sanitizar cada valor de célula com `sanitizeCell` (import de `lib/clientes/exportacao.ts`) antes de qualquer uso — fecha A4 no lado da importação (uma célula começando com =/+/-/@ não vira fórmula viva num re-export futuro).
    2. Resolver categoria (se mapeada e não-vazia) contra `categorias` por nome normalizado (reusar normalização case/acento — pode reaproveitar de dedupe ou uma comparação case-insensitive simples); inexistente → reason 'Categoria "{valor}" não existe' (D-02).
    3. Resolver cada produto (multi-valor: separar por vírgula/`;`) contra `produtos`; qualquer inexistente → reason de produto (D-02).
    4. Resolver responsável contra `vendedores`: casar por email exato OU por "nome sobrenome" normalizado; não encontrado → reason "Responsável não informado"/"Vendedor '{valor}' não encontrado" (IMP-04).
    5. Validar os required com `createImportRowSchema.safeParse`; cada campo faltando vira uma reason legível (mensagens do 06-UI-SPEC: "Razão social não informada", "Endereço não informado" — mapear cep/rua/numero/cidade/estado para essa família —, "Responsável não informado").
    6. status = reasons.length > 0 ? "erro" : "ok"; `resolved` carrega os ids resolvidos + valores sanitizados, pronto para a Fase 7 (nunca gravado aqui).

    Criar `tests/importacao/annotarLinha.test.ts` (pure-function, sem env) passando listas de lookup fabricadas, cobrindo todos os casos do <behavior>. Nota: para produtos/categoria inexistentes, asserir a mensagem de erro exata do UI-SPEC.
  </action>
  <verify>
    <automated>npm test -- annotarLinha</automated>
  </verify>
  <done>createImportRowSchema exige os 7 campos mínimos; annotarLinha resolve categoria/produto/vendedor contra lookups passados, marca erro com mensagens do UI-SPEC para faltantes/inexistentes (D-02/IMP-04/IMP-05), sanitiza células (A4); testes puros passam sem .env.local.</done>
</task>

<task type="auto">
  <name>Task 3: Server Action validarLoteImportacao (read-only) + portão IMP-10 + leitura de dedup RLS-scoped</name>
  <files>app/actions/importacao.ts, tests/importacao/rls-dedup-read.test.ts</files>
  <action>
    Criar `app/actions/importacao.ts` com a Server Action `validarLoteImportacao`, seguindo a forma de `app/actions/clientes.ts` (`"use server"`, retorno discriminado `{ data } | { error: { code } }`):
    - Portão IMP-10 (server-side, o verdadeiro reforço desta fase read-only): buscar `user` via `supabase.auth.getUser()` (não autenticado → `{ error: { code: "unauthenticated" } }`), ler `profiles.role` do chamador e, se não for supervisor, retornar `{ error: { code: "forbidden" } }` ANTES de qualquer leitura de dados — mesma disciplina que `deleteCliente` usa para a exclusão supervisor-only. Documentar no comentário que este é o portão de feature server-side desta fase, e que o portão de ESCRITA real (RPC `importar_clientes_lote` com `is_supervisor()`) é escopo da Fase 7 (ARCHITECTURE.md Pattern 4).
    - Entrada: `validarLoteImportacao(linhas: MappedRow[]): Promise<...>`. As linhas mapeadas são DADO, nunca input de autorização — não confiar em nenhum campo delas para decisão de permissão (a única barreira é o portão is_supervisor acima), mesma postura de `getClientesParaExportacao`.
    - Buscar os lookups uma vez, todos RLS-scoped, sem ramificação manual de papel: `getCategoriasAtivas()`, `getProdutosAtivos()` (ambos read-open a qualquer autenticado) e a lista de vendedores via `profiles.select("id, nome, sobrenome, email")` (a policy SELECT de profiles é `using(true)`); e as razões sociais existentes via uma única query estreita `supabase.from("clientes").select("razao_social")` — para um Supervisor, a RLS de clientes retorna a base inteira, exatamente o conjunto contra o qual o dedup deve comparar (reusar o padrão narrow-select de getClientesParaExportacao; NUNCA adicionar filtro manual de responsavel).
    - Anotar cada linha com `annotarLinha(linha, lookups)`; depois aplicar `findDuplicates` (dedupe) sobre as razões sociais do batch + existentes; para cada linha ainda "ok" que casou um duplicado, promover status para "duplicado" com a reason 'Possível duplicado de "{nome}"' (precedência: uma linha "erro" continua erro; duplicado só se aplica a linhas que passaram na validação).
    - Retorno: `{ data: { linhas: ValidatedRow[] } }`, onde `ValidatedRow = { row: number; status: "ok" | "duplicado" | "erro"; reasons: string[]; similarTo?: string; resolved: ResolvedRow }`. NÃO gravar absolutamente nada (sem insert/update/rpc); sem `revalidatePath`. Exportar os tipos (`ValidatedRow`, `ValidateLoteResult`) para o 06-04 renderizar a tela de revisão.

    Criar `tests/importacao/rls-dedup-read.test.ts` espelhando `tests/clientes/rls-exportacao.test.ts` (usa `signInAs`/`serviceClient` de tests/helpers, SEED_ACCOUNTS): provar que a leitura de dedup (`clientes.select("razao_social")`) é RLS-scoped — Vendedor A não enxerga a razão social de Vendedor B (então o passo de dedup jamais vaza a base inteira a um não-Supervisor), enquanto o Supervisor enxerga ambos. Este é o teste negativo server-side (Pitfall 2/10) que ancora IMP-10 no nível de dados. Documentar no cabeçalho, como faz `tests/clientes/cliente-actions.test.ts`, que `validarLoteImportacao` em si não é invocável direto no Vitest (usa `next/headers` via createClient) — por isso o teste exercita a mesma forma de query/RLS que a action usa, e o portão app-layer `forbidden` espelha `deleteCliente`.
  </action>
  <verify>
    <automated>npm test -- rls-dedup-read</automated>
  </verify>
  <done>validarLoteImportacao rejeita não-Supervisor (forbidden) antes de ler dados, busca lookups RLS-scoped, anota cada linha (ok/erro/duplicado) e não grava nada; o teste RLS prova que a leitura de dedup é escopada por papel (Vendedor A não vê Vendedor B; Supervisor vê ambos).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → validarLoteImportacao (Server Action) | Linhas mapeadas chegam como JSON do cliente — dado não confiável, nunca input de autorização |
| Server Action → Postgres (leituras) | Dedup e lookups leem tabelas com RLS; o resultado precisa continuar escopado por papel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | validarLoteImportacao (portão de feature) | high | mitigate | Checagem `is_supervisor` app-layer retornando `forbidden` antes de qualquer leitura (espelha deleteCliente); portão de ESCRITA real é a RPC da Fase 7. Verificado indiretamente pelo teste RLS + documentação |
| T-06-02 | Information Disclosure | leitura de dedup em clientes | medium | mitigate | `clientes.select("razao_social")` é RLS-scoped, sem filtro manual de responsavel — reusa o padrão de getClientesParaExportacao; provado por tests/importacao/rls-dedup-read.test.ts |
| T-06-03 | Tampering | resolução de categoria/produto/vendedor | medium | mitigate | Valores da planilha são dado resolvido server-side contra lookups RLS-scoped; inexistente → erro na linha (D-02), nunca auto-criação nem gravação em branco |
| T-06-04 | Tampering | sanitização de célula (A4) | medium | mitigate | Todo valor de célula passa por sanitizeCell (reuso de lib/clientes/exportacao.ts) em annotarLinha antes de entrar em `resolved`, evitando que uma célula =/+/-/@ vire fórmula viva num re-export futuro |
</threat_model>

<verification>
- `npm test -- dedupe` e `npm test -- annotarLinha` passam sem .env.local (puros).
- `npm test -- rls-dedup-read` passa com .env.local + contas seedadas (prova IMP-10 no nível de dados).
- Nenhuma escrita no banco em `app/actions/importacao.ts` (sem insert/update/rpc/revalidatePath).
</verification>

<success_criteria>
- IMP-05: linha sem campo mínimo → erro com motivo (annotarLinha).
- IMP-07 + D-03: mesma empresa em formatos diferentes e duplicado interno ao batch → possível duplicado.
- D-02: categoria/produto inexistente → erro, nunca criado/em branco.
- IMP-04: coluna de vendedor resolvida contra profiles; não encontrado → erro.
- IMP-10: não-Supervisor rejeitado server-side; leitura de dedup RLS-scoped provada por teste.
</success_criteria>

<output>
Create `.planning/phases/06-importa-o-upload-mapeamento-e-revis-o/06-02-SUMMARY.md` when done
</output>
