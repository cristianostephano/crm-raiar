---
phase: 06-importa-o-upload-mapeamento-e-revis-o
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - app/(app)/clientes/importar/page.tsx
  - components/layout/AppSidebar.tsx
  - components/importacao/FileDropzone.tsx
  - components/importacao/ImportWizard.tsx
  - tests/importacao/AppSidebar.test.tsx
  - tests/importacao/FileDropzone.test.tsx
  - tests/e2e/importar-guard.spec.ts
autonomous: true
requirements: [IMP-02, IMP-10]
user_setup: []

must_haves:
  truths:
    - "Só o Supervisor acessa /clientes/importar; um Vendedor é redirecionado para / e nem vê o link no menu (IMP-10)"
    - "O Supervisor baixa o modelo de planilha a partir da tela de upload (IMP-02)"
    - "O Supervisor envia um .xlsx ou .csv e a tela confirma '{arquivo} selecionado — {N} linhas encontradas', ou mostra o empty state quando não há linhas"
    - "Um arquivo de tipo inválido ou grande demais é recusado com a mensagem de erro do UI-SPEC, sem parsing"
  artifacts:
    - app/(app)/clientes/importar/page.tsx
    - components/layout/AppSidebar.tsx
    - components/importacao/FileDropzone.tsx
    - components/importacao/ImportWizard.tsx
    - tests/e2e/importar-guard.spec.ts
  key_links:
    - "O guard da rota copia EXATAMENTE app/(app)/configuracoes/page.tsx (redirect /login sem user; redirect / se role != supervisor) — UX; a barreira real de escrita é a RPC da Fase 7"
    - "O link 'Importar clientes' entra no ADMIN_SECTION do AppSidebar (que só renderiza para supervisor), então o Vendedor nunca vê a opção"
    - "'Baixar modelo' embrulha buildModeloImportacao() (06-01) num Blob no cliente e dispara download — sem Route Handler novo"
    - "ImportWizard segura o estado do arquivo/linhas parseadas e é o shell dos 3 passos; os passos 2 e 3 são preenchidos no 06-04"
---

<objective>
Entregar a porta de entrada da importação: a rota Supervisor-only `/clientes/importar`, o link no menu (escondido de Vendedor), e o passo 1 do wizard (baixar modelo + enviar arquivo + ver "N linhas encontradas"/empty state), com o shell de 3 passos pronto para o 06-04 preencher mapeamento e revisão.

Purpose: IMP-10 (só Supervisor) e IMP-02 (baixar modelo) ganham sua manifestação de tela aqui. Fazer isso antes do mapeamento/revisão isola a prova do guard (redirect + link escondido) e o parsing client-side (já provado no 06-01) numa fatia navegável, sem tocar no banco.

Output: `app/(app)/clientes/importar/page.tsx`, link no `AppSidebar.tsx`, `FileDropzone.tsx`, `ImportWizard.tsx` (shell + passo 1), testes de render (nav + dropzone) e o E2E de guard.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-importa-o-upload-mapeamento-e-revis-o/06-UI-SPEC.md
@.planning/phases/06-importa-o-upload-mapeamento-e-revis-o/06-PATTERNS.md

# Copiar estas formas exatas
@app/(app)/configuracoes/page.tsx
@components/layout/AppSidebar.tsx
@components/configuracoes/EditableListTab.tsx
@lib/importacao/types.ts
@lib/importacao/parseArquivo.ts
@lib/importacao/modelo.ts
@tests/e2e/session-persistence.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rota Supervisor-only /clientes/importar + link no menu + E2E de guard (IMP-10)</name>
  <files>app/(app)/clientes/importar/page.tsx, components/layout/AppSidebar.tsx, tests/importacao/AppSidebar.test.tsx, tests/e2e/importar-guard.spec.ts</files>
  <action>
    Criar `app/(app)/clientes/importar/page.tsx` copiando a forma EXATA de `app/(app)/configuracoes/page.tsx` (Server Component): `createClient()`, `redirect("/login")` se não houver user, ler `profiles.role` e `redirect("/")` se `callerProfile?.role !== "supervisor"`. Documentar no comentário que este guard é UX-only e que a barreira de autorização real para qualquer escrita é a RLS/RPC da Fase 7 (mesmo raciocínio do configuracoes/page.tsx). A página renderiza `<ImportWizard />` (client) no lugar de `<ConfiguracoesTabs />`, com um cabeçalho seguindo o mesmo layout (`div flex flex-1 flex-col gap-8 p-6` + `h1`).

    Modificar `components/layout/AppSidebar.tsx`: adicionar `{ href: "/clientes/importar", label: "Importar clientes", icon: <um ícone lucide de upload, ex: Upload ou FileUp> }` ao `ADMIN_SECTION` (a seção que só é montada quando `role === "supervisor"`), então o Vendedor nunca vê a opção. Importar o ícone junto dos outros de `lucide-react`. Não mexer no PRINCIPAL_SECTION.

    Criar `tests/importacao/AppSidebar.test.tsx` (Vitest + @testing-library/react, já instalados): renderizar `<AppSidebar role="supervisor" .../>` e asserir que "Importar clientes" aparece; renderizar com `role="vendedor"` e asserir que NÃO aparece (metade "escondido do menu" do IMP-10). Passar props mínimas plausíveis (fullName/roleLabel/initials).

    Criar `tests/e2e/importar-guard.spec.ts` (Playwright, espelhando `tests/e2e/session-persistence.spec.ts` — o webServer roda `npm run dev` via playwright.config.ts): logar como Vendedor A (duplicar as credenciais no arquivo como o spec de sessão faz — `vendedor.a+test@raiar.local` / `TestVendedorA!2026` — em vez de importar do módulo Vitest), navegar para `/clientes/importar` e asserir redirecionamento para `/` (prova server-side do guard de rota, IMP-10 critério de sucesso #1). Opcional/complementar: logar como Supervisor e confirmar que a tela abre.
  </action>
  <verify>
    <automated>npm test -- AppSidebar && npm run test:e2e -- importar-guard</automated>
  </verify>
  <done>/clientes/importar redireciona não-Supervisor para / (E2E prova); "Importar clientes" aparece no menu só para supervisor (render test prova); o link vive no ADMIN_SECTION.</done>
</task>

<task type="auto">
  <name>Task 2: FileDropzone + shell do wizard (passo 1: baixar modelo + enviar + parse) — IMP-02</name>
  <files>components/importacao/FileDropzone.tsx, components/importacao/ImportWizard.tsx, tests/importacao/FileDropzone.test.tsx</files>
  <action>
    Criar `components/importacao/FileDropzone.tsx` (`"use client"`) — SEM analog no codebase (PATTERNS.md "No Analog Found"): um `<input type="file" accept=".xlsx,.csv">` mais eventos HTML5 de drag (dragover/drop) estilizados com os tokens de spacing/cor do `06-UI-SPEC.md`. Props controladas (PascalCase, controlled): recebe `onFileAccepted(file: File)` e `onFileRejected(motivo)`. Ao receber um arquivo (via input change OU drop), chamar `validateUploadFile` (de 06-01): se `{ ok: false }`, chamar onFileRejected com a cópia de erro certa do UI-SPEC ("Não foi possível ler este arquivo. Envie um arquivo .xlsx ou .csv." para tipo; "Este arquivo é maior que 10 MB..." para tamanho); se ok, chamar onFileAccepted. Copy da dropzone: "Arraste o arquivo aqui ou clique para selecionar" + helper "Aceita arquivos .xlsx ou .csv, até 10 MB". Botão-ícone "Remover arquivo" reusa o alvo de toque 44px (`size-11`) do ClienteCard (UI-SPEC Spacing).

    Criar `components/importacao/ImportWizard.tsx` (`"use client"`) — analog `components/configuracoes/EditableListTab.tsx` para o padrão de estado: `useState` por preocupação (arquivo, linhas parseadas, mensagem de erro, passo atual, label de progresso), constantes de copy no topo do arquivo (padrão de EditableListTab), banners `role="alert"`/`role="status"` reusados verbatim. Montar:
    - Um indicador de 3 passos não-interativo no topo ("1 Enviar planilha · 2 Mapear colunas · 3 Revisar"), passo atual destacado com `--primary` (UI-SPEC linha 163) — passos sequenciais e gated, NÃO um `Tabs`.
    - Passo 1 (Enviar planilha): título "Enviar planilha"; um link/botão "Baixar modelo de planilha" que chama `buildModeloImportacao()` (06-01), embrulha o Uint8Array num `Blob` (MIME `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`), cria um object URL e dispara um `<a download="modelo-importacao-clientes.xlsx">` — download client-side, sem Route Handler (IMP-02). Renderizar `<FileDropzone>`; ao aceitar um arquivo, mostrar o label de progresso "Lendo arquivo…" (reusar o padrão Skeleton de GanhosPerdidosCards, set síncrono como em EditableListTab) enquanto `parseArquivo(file)` roda; ao concluir, guardar {headers, rows} no estado e mostrar a confirmação "{nome do arquivo} selecionado — {N} linhas encontradas"; se `rows.length === 0`, mostrar o empty state ("Nenhuma linha encontrada nesta planilha" + corpo do UI-SPEC, reusando o padrão dashed-border de EditableListTab) e desabilitar "Continuar". CTA primário "Continuar" avança para o passo 2 (o corpo dos passos 2/3 é preenchido no 06-04 — deixar um placeholder mínimo/estrutura para eles, sem UI morta que aparente funcionalidade inexistente).
    - Rejeição de arquivo: renderizar a mensagem via banner `role="alert"` (UI-SPEC Error states).

    Criar `tests/importacao/FileDropzone.test.tsx` (Vitest + @testing-library/react): asserir que a dropzone renderiza a copy de prompt + helper de formatos; simular a seleção de um arquivo com extensão inválida e confirmar que `onFileRejected` é chamado com a mensagem de tipo do UI-SPEC (caminho de rejeição, sem parsing real — evita I/O de arquivo flaky no jsdom).
  </action>
  <verify>
    <automated>npm test -- FileDropzone</automated>
  </verify>
  <done>FileDropzone valida tipo/tamanho e expõe callbacks controlados; ImportWizard tem indicador de 3 passos, "Baixar modelo" (download client-side do modelo IMP-02), upload → parseArquivo → "N linhas encontradas"/empty state, e placeholder estrutural para os passos 2/3; teste de render do FileDropzone passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendedor → rota/menu de importação | Um Vendedor pode tentar abrir /clientes/importar por URL direta ou procurar o link |
| navegador → arquivo enviado | Conteúdo não confiável validado no cliente antes do parse |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | rota /clientes/importar + link do menu | high | mitigate | Guard de servidor redireciona não-Supervisor para / (copia configuracoes/page.tsx); link só no ADMIN_SECTION (escondido de Vendedor); provado por E2E de redirect + render test do sidebar. Reforço server-side do read path e da escrita ficam em 06-02/Fase 7 |
| T-06-05 | Denial of Service | FileDropzone | low | mitigate | validateUploadFile (06-01) recusa tipo/tamanho antes do parse; arquivo nunca sobe ao servidor |
</threat_model>

<verification>
- `npm test -- AppSidebar` prova a metade "escondido do menu" do IMP-10.
- `npm run test:e2e -- importar-guard` prova o redirect server-side (Vendedor → /).
- `npm test -- FileDropzone` prova a rejeição de tipo inválido com a cópia do UI-SPEC.
- Nada é gravado no banco nesta plan.
</verification>

<success_criteria>
- IMP-10: Vendedor redirecionado + link ausente do menu; Supervisor abre a tela.
- IMP-02: "Baixar modelo de planilha" entrega o .xlsx com as colunas esperadas.
- Upload de .xlsx/.csv mostra "{arquivo} selecionado — {N} linhas encontradas" ou o empty state.
- Shell de 3 passos pronto para o 06-04 preencher mapeamento/revisão.
</success_criteria>

<output>
Create `.planning/phases/06-importa-o-upload-mapeamento-e-revis-o/06-03-SUMMARY.md` when done
</output>
