---
phase: 06-importa-o-upload-mapeamento-e-revis-o
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified:
  - components/ui/table.tsx
  - lib/importacao/mapping.ts
  - components/importacao/ColumnMappingTable.tsx
  - components/importacao/ImportPreviewTable.tsx
  - lib/importacao/preview.ts
  - components/importacao/ImportWizard.tsx
  - tests/importacao/mapping.test.ts
  - tests/importacao/preview.test.ts
autonomous: false
requirements: [IMP-03, IMP-04, IMP-07, IMP-08]
user_setup: []

must_haves:
  truths:
    - "O Supervisor associa cada coluna da planilha a um campo do sistema (incluindo Responsável/vendedor) e pode marcar colunas como 'Não importar' (IMP-03/IMP-04)"
    - "Enquanto um campo obrigatório (ex: Responsável) não estiver mapeado, um aviso claro aparece e não dá para avançar"
    - "A tela de revisão mostra cada linha como OK / erro / possível duplicado, com o motivo, e a linha de resumo '{N} linhas — {ok} OK · {erros} com erro · {dups} possíveis duplicados' (IMP-08)"
    - "Cada linha de possível duplicado mostra um selo citando o nome parecido e tem 'Importar mesmo assim' / 'Pular' com default Pular — decisão granular por linha, nunca ação em massa (D-04); linhas de erro não têm ação (IMP-07)"
    - "Nenhum cliente é criado nesta fase — a revisão só decide o estado das linhas em memória"
  artifacts:
    - lib/importacao/mapping.ts
    - components/importacao/ColumnMappingTable.tsx
    - components/importacao/ImportPreviewTable.tsx
    - lib/importacao/preview.ts
    - tests/importacao/mapping.test.ts
    - tests/importacao/preview.test.ts
  key_links:
    - "ColumnMappingTable é puramente controlado (padrão ClienteToolbar) — todo o estado de mapeamento vive no ImportWizard e sobrevive à navegação entre passos"
    - "A revisão chama validarLoteImportacao (06-02) com as linhas já mapeadas via applyMapping; o status/motivo por linha vem da Server Action, a tabela só renderiza"
    - "Cor de status da linha reusa o vocabulário TASK_STATUS_BORDER de ClienteCard (ok=verde, duplicado=âmbar, erro=vermelho) — nunca inventar"
    - "Paginação 50/página é feita com estado React simples sobre o primitivo Table do shadcn — NÃO adicionar @tanstack/react-table (não está no package.json e exigiria outro checkpoint de legitimidade; slice simples não precisa de dependência nova)"
    - "O botão 'Confirmar importação' existe visualmente mas seu onClick de gravação é escopo da Fase 7 (confirmarLoteImportacao)"
---

<objective>
Completar o wizard: o passo 2 (mapear cada coluna do arquivo para um campo do sistema, com "não importar" e aviso de obrigatório) e o passo 3 (tela de revisão paginada mostrando OK/erro/possível duplicado por linha, com decisão granular Importar/Pular nos duplicados), ligando a revisão à Server Action de validação — sem gravar nada.

Purpose: Fecha IMP-03 (mapeamento + não importar), IMP-04 (coluna do vendedor como alvo de mapeamento), IMP-07 (duplicado mostrado com decisão por linha) e IMP-08 (tela de revisão com status por linha). É a fatia que torna a fase inteira usável e verificável por um humano de ponta a ponta.

Output: primitivo `table` do shadcn, `lib/importacao/mapping.ts` (suggestMapping + applyMapping puros), `ColumnMappingTable.tsx`, `ImportPreviewTable.tsx`, `lib/importacao/preview.ts` (contagens + cor de status puras), extensão do `ImportWizard`, testes puros e um checkpoint humano do fluxo completo.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-importa-o-upload-mapeamento-e-revis-o/06-UI-SPEC.md
@.planning/phases/06-importa-o-upload-mapeamento-e-revis-o/06-PATTERNS.md

# Copiar estas formas exatas
@components/clientes/ClienteToolbar.tsx
@components/configuracoes/EditableListTab.tsx
@components/clientes/ClienteCard.tsx
@lib/importacao/types.ts
@app/actions/importacao.ts
@components/importacao/ImportWizard.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Instalar Table (shadcn) + lógica de mapeamento pura + ColumnMappingTable (IMP-03/IMP-04)</name>
  <files>components/ui/table.tsx, lib/importacao/mapping.ts, components/importacao/ColumnMappingTable.tsx, components/importacao/ImportWizard.tsx, tests/importacao/mapping.test.ts</files>
  <behavior>
    - suggestMapping(header, SYSTEM_FIELDS): dado "razao_social" ou "Razão Social" sugere o campo razaoSocial; "vendedor"/"responsavel" sugere responsavel; um header sem correspondência boa sugere "não importar".
    - applyMapping(headers, rows, mapping): transforma as linhas parseadas (string[][]) em MappedRow[] (Partial<Record<SystemField,string>>), ignorando colunas marcadas "não importar"; produtos (multi) preservado como string única para annotarLinha dividir depois.
    - requiredFieldsFaltando(mapping, SYSTEM_FIELDS): devolve a lista de campos required (ex: Responsável) ainda não associados a nenhuma coluna.
  </behavior>
  <action>
    Instalar o primitivo Table via `npx shadcn@latest add table` (registro oficial shadcn — sem portão de vetting, per UI-SPEC Registry Safety). Gera `components/ui/table.tsx`. Não é dependência npm (é componente copiado), então não precisa de checkpoint de legitimidade.

    Criar `lib/importacao/mapping.ts`, módulo puro, exportando `suggestMapping`, `applyMapping` e `requiredFieldsFaltando` conforme o <behavior>, usando `SYSTEM_FIELDS`/`SystemField`/`MappedRow` de `lib/importacao/types.ts` (06-01). O auto-sugerir casa por label normalizado (case/acento/underscore) contra os labels de SYSTEM_FIELDS; sem match → sentinela "não importar".

    Criar `components/importacao/ColumnMappingTable.tsx` (`"use client"`), PURAMENTE CONTROLADA (padrão `components/clientes/ClienteToolbar.tsx` — "controlled-input shell", sem estado próprio): props `columns` (headers detectados), `previews` (2-3 primeiros valores por coluna, `text-sm text-muted-foreground` truncados), `mapping`, `onMappingChange`. Uma linha por coluna do arquivo: [Coluna do arquivo] · [Prévia] · [Campo do sistema] (um `Select` de `components/ui/select.tsx`, uma opção por SYSTEM_FIELD + "Não importar esta coluna" sempre por último, per UI-SPEC linha 116/165). Todo o estado de mapeamento vive no ImportWizard (sobrevive à navegação entre passos).

    Estender `components/importacao/ImportWizard.tsx`: preencher o passo 2 ("Mapear colunas") com o título + helper do UI-SPEC, `<ColumnMappingTable>` (mapping inicial = `suggestMapping` por coluna), o aviso inline de obrigatório-não-mapeado ('O campo "{nome}" é obrigatório e ainda não foi associado a nenhuma coluna.') vindo de `requiredFieldsFaltando`, CTA "Continuar" (desabilitado enquanto houver required faltando) e "Voltar". Ao continuar, calcular `applyMapping(headers, rows, mapping)` e guardar as MappedRow[] no estado para o passo 3.

    Criar `tests/importacao/mapping.test.ts` (puro, sem env) cobrindo os 3 casos do <behavior>, incluindo o auto-sugerir de "responsavel"/"vendedor" → responsavel (IMP-04) e o descarte de colunas "não importar".
  </action>
  <verify>
    <automated>npm test -- mapping</automated>
  </verify>
  <done>Table do shadcn instalado; suggestMapping/applyMapping/requiredFieldsFaltando puros e testados; ColumnMappingTable controlado com Select por coluna + "Não importar"; passo 2 do wizard mapeia (auto-sugerido), avisa obrigatório faltando e bloqueia "Continuar" até resolver.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ImportPreviewTable (revisão paginada) + ligação com validarLoteImportacao (IMP-07/IMP-08)</name>
  <files>components/importacao/ImportPreviewTable.tsx, lib/importacao/preview.ts, components/importacao/ImportWizard.tsx, tests/importacao/preview.test.ts</files>
  <behavior>
    - summaryCounts(linhas validadas): devolve { total, ok, erros, duplicados } corretos para uma mistura de status.
    - statusBorderClass(status): "ok" → border-l-4 border-l-green-500; "duplicado" → border-l-4 border-l-amber-500; "erro" → border-l-4 border-l-red-500 (mesmo vocabulário de TASK_STATUS_BORDER).
    - paginação: dado > 50 linhas, a página 1 mostra as primeiras 50 e existe navegação para as demais (fatia por estado React, sem lib nova).
    - decisão por linha: uma linha "duplicado" tem estado "importar"|"pular" com default "pular"; alternável; linha "erro" não expõe ação.
  </behavior>
  <action>
    Criar `lib/importacao/preview.ts`, módulo puro, exportando `summaryCounts` e `statusBorderClass` (reusando o vocabulário exato de cor de `components/clientes/ClienteCard.tsx` `TASK_STATUS_BORDER`, mapeado para ok/duplicado/erro — UI-SPEC linhas 84-91; nunca inventar sistema de cor novo).

    Criar `components/importacao/ImportPreviewTable.tsx` (`"use client"`) usando o primitivo `Table` do shadcn (Task 1). Paginação client-side de 50 linhas por página com estado React simples (fatia `rows.slice(page*50, ...)`) — NÃO adicionar `@tanstack/react-table`: o UI-SPEC o citou como "já aprovado", mas ele NÃO está no package.json e adicioná-lo exigiria outro checkpoint de legitimidade de pacote; uma fatia simples cobre a mesma UX de 50/página sem dependência nova (coerente com a postura zero-dependência/free-tier do projeto). Cada linha mostra: número da linha, os valores mapeados, o selo de status + motivo, e a borda esquerda de cor via `statusBorderClass`. Para linhas "duplicado", o selo "Possível duplicado de '{nome}'" mais os dois botões `outline`/`ghost` "Importar mesmo assim" / "Pular" (default Pular, per UI-SPEC linha 132; decisão granular por linha, nunca ação em massa automática — D-04; estado row-keyed `useState<Record<number,"importar"|"pular">>` como o `editingId` de EditableListTab). Linhas "erro" só mostram o texto do motivo, sem botão. Linha de resumo acima da tabela via `summaryCounts`. Empty state reusa o dashed-border de EditableListTab.

    Estender `components/importacao/ImportWizard.tsx`: preencher o passo 3 ("Revisar antes de importar"). Ao entrar no passo, mostrar o label de progresso "Validando linhas…" (padrão Skeleton) e chamar a Server Action `validarLoteImportacao(mappedRows)` (06-02); em erro `forbidden`/`generic`, mostrar o banner `role="alert"` com a cópia genérica do UI-SPEC ("Não foi possível validar a planilha agora. Tente novamente."). Com sucesso, renderizar `<ImportPreviewTable>` com as ValidatedRow[]. Incluir o CTA primário "Confirmar importação" e "Voltar" — MAS o onClick de "Confirmar importação" NÃO grava nada nesta fase: deixar explicitamente marcado (comentário) que a ação de escrita `confirmarLoteImportacao` é escopo da Fase 7; nesta fase o botão só carrega o estado de linhas decididas (quais duplicados importar/pular). Nada é escrito no banco.

    Criar `tests/importacao/preview.test.ts` (puro, sem env) cobrindo summaryCounts (mistura de status) e statusBorderClass (as 3 cores).
  </action>
  <verify>
    <automated>npm test -- preview</automated>
  </verify>
  <done>summaryCounts/statusBorderClass puros e testados; ImportPreviewTable pagina 50/linha (sem @tanstack), colore a borda por status, e oferece Importar/Pular (default Pular) só nos duplicados; passo 3 chama validarLoteImportacao e renderiza a revisão; "Confirmar importação" não grava (escopo Fase 7).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana do fluxo completo de importação (upload → mapear → revisar)</name>
  <what-built>O wizard Supervisor-only completo em /clientes/importar: baixar modelo, enviar planilha (.xlsx e .csv), mapear colunas (com auto-sugestão + "não importar" + aviso de obrigatório), e a tela de revisão classificando cada linha como OK / erro / possível duplicado — sem nada gravado no banco.</what-built>
  <how-to-verify>
    1. Logado como Supervisor, abra o menu → "Importar clientes" (confirme que o link existe). Acesse /clientes/importar.
    2. Clique "Baixar modelo de planilha" e confirme que o .xlsx baixado tem exatamente as colunas esperadas + 1 linha de exemplo (IMP-02).
    3. Prepare 2 arquivos de teste a partir do modelo: (a) um .xlsx e (b) um .csv EXPORTADO do Excel em pt-BR (delimitado por ponto-e-vírgula, "CSV UTF-8" com BOM). Em cada um inclua propositalmente: uma linha OK completa; uma linha sem endereço; uma linha com categoria inexistente; e duas linhas com a mesma razão social em grafias diferentes (ex: "Distribuidora ABC Ltda" e "DISTRIBUIDORA ABC"). Envie cada arquivo e confirme "{arquivo} selecionado — {N} linhas encontradas" (o CSV pt-BR deve separar colunas certo, sem tudo numa coluna só, e o primeiro cabeçalho não pode vir com lixo de BOM).
    4. No passo Mapear colunas: confirme a auto-sugestão por coluna, marque uma coluna como "Não importar", e verifique que deixar Responsável sem mapear mostra o aviso e bloqueia "Continuar" (IMP-03/IMP-04).
    5. No passo Revisar: confirme o resumo "{N} linhas — {ok} OK · {erros} com erro · {dups} possíveis duplicados"; a linha sem endereço aparece como Erro (com motivo); a de categoria inexistente como Erro ("Categoria 'X' não existe"); as duas de mesma razão social como Possível duplicado (com o nome parecido citado) e cada uma com Importar mesmo assim / Pular (default Pular); linhas OK em verde (IMP-05/IMP-07/IMP-08).
    6. Confirme que NADA foi criado: abra /clientes e veja que nenhum cliente novo apareceu (critério de sucesso #5 — nada gravado nesta fase).
  </how-to-verify>
  <resume-signal>Digite "approved" se todos os passos conferem, ou descreva o que divergiu.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client (mapeamento/decisões) → validarLoteImportacao | O mapeamento e as linhas são dado do cliente; a validação/resolução é re-feita server-side (06-02) |
| célula da planilha → tabela de revisão | Valores do usuário são renderizados na prévia/revisão |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Tampering | mapeamento de colunas | medium | mitigate | O mapeamento é dado; a resolução real de categoria/produto/vendedor e a validação de obrigatórios são re-checadas server-side em validarLoteImportacao (06-02), nunca confiadas no que a UI mapeou |
| T-06-04 | Tampering | ImportPreviewTable (render de valores do usuário) | medium | mitigate | Os valores exibidos vêm de `resolved`, já sanitizados por sanitizeCell no server (06-02), então uma célula =/+/-/@ não reabre como fórmula num re-export futuro |
</threat_model>

<verification>
- `npm test -- mapping` e `npm test -- preview` passam (puros, sem env).
- Checkpoint humano (Task 3) aprova o fluxo completo em .xlsx e .csv pt-BR.
- Nenhuma escrita no banco — /clientes não ganha cliente novo após a revisão.
</verification>

<success_criteria>
- IMP-03: mapeamento por coluna com "Não importar" + aviso de obrigatório não mapeado.
- IMP-04: coluna do vendedor mapeável (auto-sugerida) e resolvida.
- IMP-07: possíveis duplicados sinalizados com decisão granular Importar/Pular (default Pular).
- IMP-08: tela de revisão mostra OK/erro/possível duplicado por linha + resumo, sem gravar.
</success_criteria>

<output>
Create `.planning/phases/06-importa-o-upload-mapeamento-e-revis-o/06-04-SUMMARY.md` when done
</output>
