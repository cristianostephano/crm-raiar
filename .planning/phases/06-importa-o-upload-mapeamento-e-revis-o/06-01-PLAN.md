---
phase: 06-importa-o-upload-mapeamento-e-revis-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - lib/importacao/types.ts
  - lib/importacao/parseArquivo.ts
  - lib/importacao/modelo.ts
  - tests/importacao/parseArquivo.test.ts
  - tests/importacao/modelo.test.ts
autonomous: false
requirements: [IMP-02]
user_setup: []

must_haves:
  truths:
    - "O Supervisor baixa um modelo de planilha (.xlsx) com exatamente as colunas que o sistema espera + uma linha de exemplo (IMP-02)"
    - "Um arquivo .xlsx OU .csv (D-01 — a importação aceita ambos os formatos, incluindo CSV pt-BR delimitado por ponto-e-vírgula e com BOM UTF-8) é lido no navegador e produz {headers, rows} corretos"
    - "Um arquivo com tipo/extensão inválida ou acima de 10 MB é rejeitado antes de qualquer parsing"
  artifacts:
    - lib/importacao/types.ts
    - lib/importacao/parseArquivo.ts
    - lib/importacao/modelo.ts
    - tests/importacao/parseArquivo.test.ts
    - tests/importacao/modelo.test.ts
  key_links:
    - "parseArquivo e modelo usam @e965/xlsx no modo browser-safe (type: 'array' → Uint8Array), NUNCA o Buffer Node de lib/clientes/exportacao.ts — ambos rodam no navegador (Pattern 5 passo 1)"
    - "SYSTEM_FIELDS em types.ts é a fonte única do vocabulário de campos, reusada por modelo (col do template), annotarLinha (06-02) e mapeamento (06-04)"
    - "papaparse só é instalado após o checkpoint humano de legitimidade (convenção estabelecida: recharts Fase 4, @e965/xlsx Fase 5)"
---

<objective>
Construir o "motor" de leitura de planilha do lado do navegador e o gerador do modelo de planilha, mais o vocabulário de campos do sistema — tudo puro e testável em isolamento, sem UI e sem tocar no banco.

Purpose: As três telas do wizard (06-03, 06-04) e a validação server-side (06-02) dependem de: (a) transformar um arquivo .xlsx/.csv em linhas normalizadas, (b) conhecer a lista canônica de campos do sistema, e (c) oferecer um modelo para download (IMP-02). Isolar isso primeiro deixa os pontos frágeis (delimitador pt-BR, BOM UTF-8, tipo/tamanho de arquivo) provados por teste antes de qualquer tela existir.

Output: `lib/importacao/types.ts` (SYSTEM_FIELDS + tipos), `lib/importacao/parseArquivo.ts` (parse + guarda de upload), `lib/importacao/modelo.ts` (modelo IMP-02), papaparse instalado atrás de checkpoint, e testes unitários com fixtures pt-BR reais.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-importa-o-upload-mapeamento-e-revis-o/06-CONTEXT.md
@.planning/phases/06-importa-o-upload-mapeamento-e-revis-o/06-PATTERNS.md
@.planning/phases/06-importa-o-upload-mapeamento-e-revis-o/06-UI-SPEC.md

# Reuse these exact shapes/paths — do NOT reinvent
@lib/clientes/exportacao.ts
@lib/supabase/queries/clientes.ts
@tests/clientes/exportacao.test.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verificação humana de legitimidade do pacote papaparse</name>
  <what-built>Nada foi instalado ainda. Este é o portão de legitimidade de pacote exigido por este projeto sempre que uma dependência npm nova entra (mesma convenção usada em recharts na Fase 4 e @e965/xlsx na Fase 5). `papaparse` é o único pacote NOVO desta fase — `@e965/xlsx` já foi aprovado e instalado na Fase 5 e NÃO precisa de novo checkpoint.</what-built>
  <how-to-verify>
    1. Abra https://www.npmjs.com/package/papaparse e confirme: mantido (última publicação recente, ~5.x), milhões de downloads semanais, repositório GitHub oficial (mholt/PapaParse), licença MIT.
    2. Abra https://www.npmjs.com/package/@types/papaparse e confirme que é o pacote de tipos community-maintained correspondente (DefinitelyTyped).
    3. Confirme que a versão a instalar bate com a pesquisada em `.planning/research/STACK.md` (v1.1 Addendum): `papaparse@5.5.4` + `@types/papaparse@5.5.2` (ou patch atual equivalente do mesmo publisher).
    4. Custo/infra: papaparse é open-source, roda 100% no navegador, zero custo de infraestrutura — coerente com a restrição free-tier do projeto.
  </how-to-verify>
  <resume-signal>Digite "approved" para liberar a instalação, ou descreva o problema encontrado.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vocabulário de campos + parser client-side de .xlsx/.csv (papaparse + @e965/xlsx)</name>
  <files>package.json, lib/importacao/types.ts, lib/importacao/parseArquivo.ts, tests/importacao/parseArquivo.test.ts</files>
  <behavior>
    - validateUploadFile: arquivo .xlsx ou .csv até 10 MB → { ok: true }; extensão fora do allowlist → { ok: false, motivo: "tipo" }; > 10 MB → { ok: false, motivo: "tamanho" }.
    - parseArquivo(.xlsx): lê o ArrayBuffer via XLSX.read(data, { type: "array" }) e devolve { headers, rows } com header:1; toda célula coagida a string.
    - parseArquivo(.csv comma US-locale): devolve as mesmas colunas na ordem certa.
    - parseArquivo(.csv pt-BR): um CSV delimitado por ponto-e-vírgula (`;`) e prefixado com BOM UTF-8 (o code point U+FEFF) devolve o PRIMEIRO header como "razao_social" (BOM removido, Pitfall A3) e o número correto de colunas (delimitador detectado, Pitfall A2) — NÃO tudo colado numa coluna só.
    - parseArquivo(arquivo só com cabeçalho, 0 linhas de dados): devolve headers preenchidos e rows: [] (o wizard usa isso para o empty state).
  </behavior>
  <action>
    Após o "approved" da Task 1, instalar as dependências novas com o gerenciador do projeto: `papaparse` (runtime) e `@types/papaparse` (dev), nas versões confirmadas no checkpoint. Não usar `npx --yes`; usar `npm install`.

    Criar `lib/importacao/types.ts` com o vocabulário canônico de campos do sistema, fonte única reusada por modelo (esta plan), annotarLinha (06-02) e mapeamento (06-04). Exportar:
    - Um union `SystemField` e um array `SYSTEM_FIELDS: { key: SystemField; label: string; required: boolean; multi?: boolean }[]`. Os labels/ordem seguem `06-UI-SPEC.md` linha 165: Razão social, CEP, Rua, Número, Complemento, Cidade, Estado, Categoria, Contato, Telefone, Email, Produtos consumidos, Responsável (vendedor), Número de lojas. `required: true` apenas para razaoSocial, cep, rua, numero, cidade, estado, responsavel (as mesmas regras mínimas de `lib/validations/cliente.ts`, per IMP-05). `multi: true` apenas em produtos. NÃO incluir etapa/estágio do funil — todo cliente importado sempre nasce em "Aguardando contato", então essa coluna não existe no vocabulário.
    - Os tipos compartilhados `ParsedFile = { headers: string[]; rows: string[][] }` e `UploadValidation = { ok: true } | { ok: false; motivo: "tipo" | "tamanho" }`.

    Criar `lib/importacao/parseArquivo.ts` como módulo PURO — sem `"use client"`/`"use server"`, sem import de Supabase — seguindo a forma de `lib/clientes/exportacao.ts` (dependency-isolated, unit-testável), mas rodando no NAVEGADOR (Pattern 5 passo 1 do ARCHITECTURE.md: parsing acontece client-side, nunca sobe o arquivo pro servidor). O parser aceita os DOIS formatos por D-01 (.xlsx e .csv). Exportar:
    - `validateUploadFile(file: File): UploadValidation` — guarda por extensão (allowlist .xlsx/.csv) e tamanho (limite 10 MB), a defesa do Pitfall A7 aplicável aqui (o arquivo nunca chega ao servidor, então checar tamanho/tipo no cliente é suficiente; não há parsing server-side de bytes não confiáveis nesta fase).
    - `parseArquivo(file: File): Promise<ParsedFile>` — despacha por extensão: `.xlsx` → `file.arrayBuffer()` + `XLSX.read(data, { type: "array" })` + `XLSX.utils.sheet_to_json(sheet, { header: 1, defval: "" })` na primeira sheet; `.csv` → `await file.text()`, remover um BOM inicial (o code point U+FEFF, referenciado no código pela escape backslash-u-F-E-F-F) antes de tudo (Pitfall A3), e `Papa.parse(texto, { skipEmptyLines: true })` deixando o delimitador em auto-detecção (Pitfall A2 — Excel pt-BR exporta com `;`). Normalizar: `headers` = primeira linha com cada cabeçalho trimado e o primeiro com BOM já removido; coagir toda célula de dado a `String(cell ?? "")`; `rows` = todas as linhas de dados (exclui o cabeçalho). Importante: usar `type: "array"` do @e965/xlsx (retorna Uint8Array, browser-safe), NUNCA `type: "buffer"` (Node-only, usado só no export server-side).

    Criar `tests/importacao/parseArquivo.test.ts` no estilo pure-function de `tests/clientes/exportacao.test.ts` (sem Supabase, sem contexto de servidor). Para o fixture .xlsx, gerar o buffer no próprio teste via `XLSX.write(wb, { type: "array" })` e reconstituir um `File`/`Blob`; para o CSV pt-BR, montar a string inline com um BOM (escape backslash-u-F-E-F-F) na frente e `;` como delimitador. Cobrir todos os casos do bloco <behavior> acima.
  </action>
  <verify>
    <automated>npm test -- parseArquivo</automated>
  </verify>
  <done>papaparse + @types/papaparse instalados nas versões aprovadas; types.ts exporta SYSTEM_FIELDS com os 7 required corretos; parseArquivo lê .xlsx e .csv (incl. o fixture pt-BR ;+BOM com o primeiro header = "razao_social"); validateUploadFile rejeita tipo/tamanho; todos os testes de parseArquivo passam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gerador do modelo de planilha para download (IMP-02)</name>
  <files>lib/importacao/modelo.ts, tests/importacao/modelo.test.ts</files>
  <behavior>
    - buildModeloImportacao() devolve um workbook (Uint8Array browser-safe) cuja primeira sheet tem uma linha de cabeçalho com EXATAMENTE os labels de SYSTEM_FIELDS, na mesma ordem, sem a coluna de etapa do funil.
    - O workbook contém exatamente UMA linha de exemplo preenchida com valores plausíveis (ex: razão social de exemplo, CEP, cidade/estado, um nome de vendedor de exemplo), demonstrando o formato esperado — "vazio + uma linha de exemplo" do IMP-02.
    - Reler o workbook (XLSX.read type:"array") reproduz o cabeçalho e a linha de exemplo intactos.
  </behavior>
  <action>
    Criar `lib/importacao/modelo.ts`, módulo puro (sem Supabase/diretivas), espelhando a forma de `buildClientesWorkbook` em `lib/clientes/exportacao.ts`, mas: (1) derivando os cabeçalhos de `SYSTEM_FIELDS` (labels, na ordem), para o modelo nunca divergir do vocabulário de mapeamento; (2) escrevendo no modo browser-safe `XLSX.write(wb, { type: "array" })` → Uint8Array (o botão "Baixar modelo de planilha" do 06-03 embrulha isso num Blob no cliente; nada de Route Handler nem Buffer Node). Exportar `buildModeloImportacao(): Uint8Array`. Incluir uma única linha de exemplo (IMP-02: "vazio + uma linha de exemplo") com valores de exemplo coerentes com cada campo. Não sanitizar aqui (o modelo é gerado pelo sistema, não é input do usuário) — a sanitização de formula-injection é do lado de quem lê valores do usuário (06-02).

    Criar `tests/importacao/modelo.test.ts` no estilo de `tests/clientes/exportacao.test.ts`: reconstituir o workbook com `XLSX.read(buffer, { type: "array" })` e asserir (a) a linha de cabeçalho == labels de SYSTEM_FIELDS na ordem, (b) exatamente 1 linha de exemplo, (c) que a coluna de etapa do funil NÃO aparece.
  </action>
  <verify>
    <automated>npm test -- modelo</automated>
  </verify>
  <done>buildModeloImportacao devolve um .xlsx (Uint8Array) com o cabeçalho == labels de SYSTEM_FIELDS + 1 linha de exemplo, sem coluna de etapa; testes de modelo passam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador do Supervisor → parsing client-side | O arquivo enviado é conteúdo não confiável; é lido na própria máquina do Supervisor, nunca sobe ao servidor nesta fase |
| npm registry → build | Uma dependência nova (papaparse) entra na árvore de build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | instalação npm de papaparse/@types/papaparse | high | mitigate | Checkpoint humano de legitimidade bloqueante (Task 1) ANTES do install; verificar em npmjs.com/package; checkpoint nunca auto-aprovável |
| T-06-05 | Denial of Service | parseArquivo / validateUploadFile | low | mitigate | validateUploadFile impõe teto de 10 MB + allowlist de extensão antes do parse; o parsing é client-side (máquina do próprio Supervisor), o arquivo não atinge o servidor, então não há parse server-side de bytes não confiáveis nesta fase (Pitfall A7 mitigado no cliente) |
</threat_model>

<verification>
- `npm test -- parseArquivo` e `npm test -- modelo` passam (sem necessidade de .env.local — são testes puros).
- `package.json` lista papaparse (dependencies) e @types/papaparse (devDependencies).
- Fixture pt-BR (BOM + `;`) prova A2/A3: primeiro header = "razao_social", colunas corretamente separadas.
</verification>

<success_criteria>
- SYSTEM_FIELDS existe com exatamente 7 campos required (razaoSocial, cep, rua, numero, cidade, estado, responsavel), sem etapa de funil.
- parseArquivo lê .xlsx e .csv (US + pt-BR ;+BOM) devolvendo {headers, rows} corretos; empty file → rows:[].
- buildModeloImportacao gera o modelo IMP-02 (cabeçalho == labels + 1 linha exemplo).
- Toda dependência nova passou pelo checkpoint de legitimidade.
</success_criteria>

<output>
Create `.planning/phases/06-importa-o-upload-mapeamento-e-revis-o/06-01-SUMMARY.md` when done
</output>
