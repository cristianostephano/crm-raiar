---
phase: 12-comparativo-por-vendedor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/{proximo_numero_livre}_dashboard_comparativo_vendedor.sql
  - tests/dashboard/comparativo-vendedor.test.ts
  - tests/dashboard/rls-dashboard.test.ts
autonomous: false
requirements: [VEND-01]

must_haves:
  truths:
    - "VEND-01: existe a RPC dashboard_comparativo_vendedor(), sem nenhum argumento, devolvendo uma linha por vendedor ATIVO com responsavel, responsavel_nome, negocios_iniciados, ganho, perdido e ciclo_medio_dias"
    - "VEND-01 (D-02): a RPC não recebe nem usa parâmetro de período — todas as métricas são de histórico completo, exatamente como as duas RPCs da Fase 11"
    - "VEND-01 (D-01): negocios_iniciados é a contagem de TODOS os clientes atribuídos ao vendedor desde sempre (coluna clientes.responsavel), sem recorte de data"
    - "VEND-01 (D-04): ciclo_medio_dias é a média de dias de clientes.criado_em até o evento de fechamento contando SÓ os negócios ganhos — um negócio perdido nunca entra nessa média, e o valor fica NULL (nunca 0) para quem não tem nenhum ganho"
    - "VEND-01 (D-03): a RPC devolve ganho e perdido como contagens cruas e NUNCA calcula taxa de conversão no SQL — essa conta é feita uma vez só, no TypeScript, pelo plano 02"
    - "VEND-01: o filtro ativo = true aparece UMA única vez em toda a migration, dentro da CTE que monta a lista de linhas (vendedores_ativos); a agregação sobre clientes/historico nunca é filtrada por ele, então um vendedor desativado perde a LINHA nesta tabela mas mantém todos os números históricos intactos em qualquer outra RPC de dashboard"
    - "VEND-01: um vendedor ativo sem nenhum cliente atribuído ainda assim aparece como linha completa (zeros nas contagens, NULL no ciclo médio), nunca é escondido"
    - "VEND-01: a RPC roda como o usuário chamador (sem cláusula de elevação de privilégio), então a RLS de clientes/historico continua sendo a única fronteira de autorização — Vendedor B nunca vê números reais do Vendedor A"
    - "A migration é puramente aditiva: um único create or replace function, nenhuma tabela/coluna/índice/policy nova e nenhuma alteração em qualquer RPC de dashboard já existente"
    - "A migration foi aplicada no projeto Supabase hospedado via supabase db push, com aprovação humana prévia, e os testes de integração provam o comportamento contra o banco real"
  artifacts:
    - "supabase/migrations/{proximo_numero_livre}_dashboard_comparativo_vendedor.sql (número derivado em tempo de execução por ls, nunca copiado deste plano)"
    - "dashboard_comparativo_vendedor() — RPC sem argumentos, language sql stable, returns table(responsavel uuid, responsavel_nome text, negocios_iniciados bigint, ganho bigint, perdido bigint, ciclo_medio_dias numeric)"
    - "tests/dashboard/comparativo-vendedor.test.ts (novo — casos ativo / desativado / iniciados / ciclo / conversao)"
    - "tests/dashboard/rls-dashboard.test.ts (estendido — a RPC nova entra nos arrays antes/depois e ganha um bloco dedicado de isolamento cross-vendedor)"
  key_links:
    - "CTE vendedores_ativos lendo profiles com role = 'vendedor' e ativo = true — é a ÚNICA fonte da lista de linhas, e o LEFT JOIN dela para a agregação é o que garante linha completa para vendedor sem cliente"
    - "CTE ultimo_status_change copiada sem mudanças estruturais do 0003/0009 — dedup distinct on (cliente_id) por criado_em desc, cruzada com clientes.status_acompanhamento para confirmar que o evento é o fechamento vigente"
    - "um único group by c.responsavel produzindo negocios_iniciados, ganho e perdido a partir do MESMO conjunto de linhas de clientes — é o que garante por construção que ganho + perdido nunca passa de negocios_iniciados"
    - "o filter de ganho aplicado ao avg do ciclo médio (D-04) — é o que separa 'tempo até fechar uma venda' de 'tempo até desistir'"
    - "ausência de qualquer cláusula de elevação de privilégio na função nova: é o único mecanismo que entrega o isolamento por papel, e o teste de RLS estendido é o alarme se alguém adicionar uma no futuro"
---

<objective>
Criar a única peça de backend desta fase: uma migration nova com uma função Postgres somente-leitura — `dashboard_comparativo_vendedor()` — que devolve, para cada vendedor ATIVO, quantos negócios ele iniciou desde sempre (D-01), quantos ganhou, quantos perdeu, e o ciclo médio em dias contando só os ganhos (D-04); mais os testes de integração que provam esse comportamento e o isolamento por papel contra o banco real.

Purpose: a tabela do VEND-01 não existe sem estes números. Toda a dificuldade da fase está aqui, e ela é de precisão, não de algoritmo: separar "quem aparece como linha" (só vendedor ativo) de "quais dados históricos contam" (todos, inclusive os de um vendedor desativado), sem deixar o filtro de ativo vazar para a agregação. Os planos 02 a 04 são consumo puro do que este plano produz.

Output: uma migration aplicada no banco hospedado com aprovação humana, um arquivo de teste de integração novo e o teste de RLS existente estendido para cobrir a RPC nova.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-comparativo-por-vendedor/12-CONTEXT.md
@.planning/phases/12-comparativo-por-vendedor/12-RESEARCH.md
@.planning/phases/12-comparativo-por-vendedor/12-PATTERNS.md
@.planning/phases/12-comparativo-por-vendedor/12-VALIDATION.md
@.claude/skills/Supabase-conventions/SKILL.md
@supabase/migrations/0003_dashboard_aggregates.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever a migration com a RPC nova e criar/estender os testes de integração (RED)</name>
  <files>supabase/migrations/{proximo_numero_livre}_dashboard_comparativo_vendedor.sql, tests/dashboard/comparativo-vendedor.test.ts, tests/dashboard/rls-dashboard.test.ts</files>
  <read_first>
    - .planning/phases/12-comparativo-por-vendedor/12-PATTERNS.md — bloco `supabase/migrations/00XX_dashboard_comparativo_vendedor.sql`: traz o banner de cabeçalho a copiar e o SQL COMPLETO da função, já sintetizado contra este schema. É o texto concreto a usar como rascunho inicial, não só inspiração. Ler também os blocos finais sobre estender `tests/dashboard/rls-dashboard.test.ts` e criar `tests/dashboard/comparativo-vendedor.test.ts`.
    - .planning/phases/12-comparativo-por-vendedor/12-RESEARCH.md — seções "Architecture Patterns" (Pattern 1 invoker por omissão, Pattern 2 lista de ativos separada da agregação, Pattern 3 dedup do último evento de status), "Anti-Patterns to Avoid" e os Pitfalls 1 a 4 (política de SELECT aberta de `profiles`; confundir "quem ganha linha" com "cujos dados contam"; erro de matemática de percentual do 0010; bigint chegando como string).
    - .planning/phases/12-comparativo-por-vendedor/12-VALIDATION.md — "Per-Task Verification Map": os comandos `-t` travados fixam as PALAVRAS que os nomes dos testes precisam conter.
    - supabase/migrations/0003_dashboard_aggregates.sql — o banner de cabeçalho a copiar em espírito, a CTE `ultimo_status_change` de `dashboard_ganhos_perdidos` (a copiar sem mudanças estruturais), o precedente de `group by c.responsavel` e o `left join profiles` da função de desempenho por vendedor, e os índices já criados no fim do arquivo.
    - supabase/migrations/0001_profiles_and_roles.sql — a tabela `profiles` (colunas `id`, `nome`, `sobrenome`, `role`) e a policy de SELECT aberta (`using (true)`) que explica por que a CTE de listagem nunca é estreitada pelo papel de quem chama.
    - supabase/migrations/0008_desativacao_membro_equipe.sql — a coluna `profiles.ativo` e sua semântica.
    - supabase/migrations/0002_clientes_and_funil.sql — tabela `clientes` (`responsavel`, `criado_em`, `status_acompanhamento`, `etapa`, `motivo_perda_id`), tabela `historico`, a trigger que escreve `historico` no UPDATE e o formato exato de `descricao`, e as constraints `chk_ganho_somente_etapa_final` / `chk_perdido_exige_motivo` (os testes precisam respeitá-las).
    - tests/dashboard/funil-detalhado.test.ts — o padrão mais recente de teste de comportamento desta pasta: mutação via `serviceClient()` (é a trigger que escreve `historico`), backdate de `clientes.criado_em`/`historico.criado_em` para controlar durações, leitura via cliente logado.
    - tests/equipe/reassignment.test.ts — o ciclo de vida de fixture descartável: `createTestMember("vendedor", label)` / `deleteTestMember(id)` e a ordem de teardown exigida pela FK (apagar os `clientes` ANTES do membro).
    - tests/dashboard/rls-dashboard.test.ts — arquivo a ESTENDER (nunca duplicar): o comentário de topo, os arrays `Promise.all([...])` de antes (~linha 213) e de depois (~linha 278), os helpers de normalização, e o bloco `it()` dedicado adicionado na Fase 11 (~linha 387) que é o molde exato do bloco novo.
    - tests/helpers/supabase-test-clients.ts — assinaturas de `serviceClient()`, `signInAs(email, password)`, `createTestMember(role, label?)`, `deleteTestMember(id)` e `SEED_ACCOUNTS`.
  </read_first>
  <action>
    Três entregas neste task.

    (A) DERIVAR O NÚMERO DA MIGRATION EM TEMPO DE EXECUÇÃO. Rodar `ls supabase/migrations/` como PRIMEIRA ação do task e usar o próximo número livre (quatro dígitos, com zeros à esquerda) no nome do arquivo novo: `supabase/migrations/{numero}_dashboard_comparativo_vendedor.sql`. NÃO usar como literal nenhum número escrito neste plano, no 12-RESEARCH.md ou no 12-PATTERNS.md — o número correto só é conhecido no momento da execução (a Fase 11 teve uma colisão real de numeração que precisou ser corrigida ao vivo). O arquivo criado tem que ser o de MAIOR número da pasta ao final do task.

    (B) Escrever a migration. Um único `create or replace function`, nada mais.

    Começar pelo banner de cabeçalho: copiar em espírito o bloco de comentário do topo de `0003_dashboard_aggregates.sql` — o que registra que toda função do arquivo roda como o usuário chamador por omissão de cláusula de elevação de privilégio, e que adicionar essa cláusula quebraria silenciosamente a RLS — adaptando o texto para citar VEND-01 e os dois arquivos de teste desta fase como o alarme. Acrescentar ao banner o parágrafo do 12-PATTERNS.md registrando que `profiles` tem policy de SELECT aberta para qualquer usuário autenticado (comportamento pré-existente do `0001`, também usado por `lib/equipe/membros.ts`), de modo que a CTE de listagem de vendedores nunca é estreitada pelo papel de quem chama — e que por isso NÃO se deve adicionar checagem de papel dentro da função.

    A função: `dashboard_comparativo_vendedor()`, SEM NENHUM argumento, `language sql` + `stable`, `returns table(responsavel uuid, responsavel_nome text, negocios_iniciados bigint, ganho bigint, perdido bigint, ciclo_medio_dias numeric)`. Corpo: usar como rascunho o SQL completo do 12-PATTERNS.md, com estas CTEs, nesta ordem e com estes nomes:

      1. `vendedores_ativos` — lê só `profiles`: `p.id` e o nome montado por `trim(both ' ' from coalesce(p.nome, '') || ' ' || coalesce(p.sobrenome, ''))`, filtrando `p.role = 'vendedor'` e `p.ativo = true`. Esta é a ÚNICA CTE do arquivo autorizada a mencionar `ativo`. Deixar um comentário SQL de uma linha registrando isso explicitamente.

      2. `ultimo_status_change` — copiar sem mudanças estruturais a CTE homônima de `0003_dashboard_aggregates.sql`: `distinct on (h.cliente_id)` sobre `historico`, filtrando `h.tipo = 'status_acompanhamento'` e a descrição contendo ganho ou perdido, com `order by h.cliente_id, h.criado_em desc`, devolvendo `cliente_id`, `criado_em` e o `status_evento` derivado. Não reinventar a lógica de comparação de texto nem trocar o operador — é a terceira aplicação literal do mesmo trecho.

      3. `fechamentos` — `clientes c join ultimo_status_change u on u.cliente_id = c.id`, mantendo apenas os casos em que `c.status_acompanhamento::text = u.status_evento` (confirma que o evento deduplicado é o fechamento vigente, e não um status que já foi revertido depois). Projeta `cliente_id`, `status_evento`, `c.criado_em` e o `criado_em` do evento como `fechado_em`.

      4. `agregados` — `from clientes c left join fechamentos f on f.cliente_id = c.id group by c.responsavel`. Um ÚNICO group by sobre UM único conjunto de linhas de `clientes` produz as três contagens, o que garante por construção que ganho + perdido nunca ultrapassa negocios_iniciados (é a mesma guarda de consistência que o `0010` teve que aplicar depois de um bug real de percentual acima de 100%). Colunas:
         - `negocios_iniciados` = `count(*)` — D-01: todos os clientes atualmente atribuídos ao vendedor pela coluna `clientes.responsavel`, sem NENHUM recorte de data. Deixar um comentário SQL de uma linha registrando que "desde sempre" aqui significa "sem recorte de data", e que a base é a atribuição vigente em `clientes.responsavel`, não uma reconstrução do histórico de reatribuições.
         - `ganho` = `count(*) filter (...)` sobre `f.status_evento` igual a ganho.
         - `perdido` = `count(*) filter (...)` sobre `f.status_evento` igual a perdido.
         - `ciclo_medio_dias` = média de `extract(epoch from (f.fechado_em - f.criado_em))` dividida por `86400.0`, com `filter` restringindo aos ganhos, arredondada a 1 casa. D-04: o `filter` de ganho é a decisão travada com o dono — um negócio perdido nunca entra nessa média. O valor fica NULL naturalmente quando o vendedor não tem nenhum ganho; NÃO envolver em coalesce para zero. Deixar um comentário SQL de uma linha registrando a decisão D-04 e que NULL é o resultado correto (não um bug) para quem ainda não fechou nada.
         Esta CTE NÃO pode mencionar `ativo`, nem juntar com `vendedores_ativos`, nem filtrar por papel. Deixar um comentário SQL de uma linha explicando que isso é deliberado: os clientes de um vendedor desativado continuam contando em todas as outras agregações do sistema, e reaplicar o filtro aqui quebraria essa garantia.

      5. `select` final — `from vendedores_ativos v left join agregados a on a.responsavel = v.id`, com `coalesce(..., 0)` nas três contagens (para o vendedor ativo sem nenhum cliente virar uma linha de zeros em vez de sumir), `a.ciclo_medio_dias` puro (sem coalesce — NULL é o valor correto), e `order by v.nome` (ordem alfabética, travada pelo 12-UI-SPEC.md).

    Regras absolutas desta migration:
      - NENHUMA cláusula de elevação de privilégio na função.
      - NENHUM parâmetro — nem de período, nem de vendedor, nem de papel (D-02).
      - NENHUMA coluna de taxa de conversão no retorno (D-03) — a RPC devolve só as contagens cruas de ganho e perdido; a razão entre elas é calculada uma vez só, no TypeScript, pelo plano 02.
      - NENHUMA referência a qualquer outra RPC de dashboard já existente, e nenhum `alter table`, `drop`, `create table`, `create index`, `create policy` — a fase inteira é aditiva e somente-leitura. Os índices criados no `0003` já cobrem os caminhos de acesso destas queries.
      - NENHUM arquivo de migration já existente pode ser editado.
      <!-- planner-discipline-allow: dashboard_ganhos_perdidos -->
      <!-- planner-discipline-allow: dashboard_desempenho_vendedor -->

    (C) Criar `tests/dashboard/comparativo-vendedor.test.ts` e estender `tests/dashboard/rls-dashboard.test.ts`. Os dois vão FALHAR (RED) até o push do Task 3, porque a função ainda não existe no banco.

      `tests/dashboard/comparativo-vendedor.test.ts` (novo). Estratégia de isolamento, exigida pelo 12-RESEARCH.md: NÃO usar as contas semente compartilhadas como sujeito das asserções de contagem — o projeto de teste ao vivo já tem clientes históricos de quantidade desconhecida nelas, o que tornaria qualquer asserção de número exato instável. Em vez disso, criar um vendedor descartável com `createTestMember("vendedor", "comparativo")` e semear para ele um conjunto pequeno e conhecido de clientes via `serviceClient()`. Quem CHAMA a RPC nos testes é o Supervisor (`signInAs` com `SEED_ACCOUNTS.supervisor`), que é quem enxerga as linhas de todos os vendedores; as asserções sempre localizam a linha do vendedor de fixture pelo `responsavel` (id), nunca por posição no array.

      Seed determinístico sugerido para o vendedor de fixture: 3 clientes — um levado a ganho, um levado a perdido, um deixado em andamento. Toda mudança de etapa/status é feita por UPDATE via `serviceClient()` (é a trigger que escreve `historico`; nunca inserir em `historico` na mão). Respeitar as constraints: ganho só é permitido na etapa final do funil, e perdido exige um `motivo_perda_id` (buscar um motivo ativo, como `tests/dashboard/ganhos-perdidos.test.ts` já faz). Para controlar durações, ajustar `clientes.criado_em` e `historico.criado_em` via `serviceClient()` — nenhuma trigger reage a essas colunas.

      Teardown em `afterAll`: apagar primeiro os `clientes` criados (por id), só depois chamar `deleteTestMember(fixtureVendedorId)` — a ordem inversa falha na foreign key de `clientes.responsavel`, conforme `tests/equipe/reassignment.test.ts` já documenta. Se o teste desativar o vendedor de fixture, reativá-lo não é necessário (ele é apagado no teardown).

      Casos exigidos. Os nomes dos testes (`it("...")`) precisam conter EXATAMENTE estas palavras, sem acento, porque o mapa de comandos do 12-VALIDATION.md filtra por elas — em particular o nome do caso de taxa de conversão precisa conter a forma SEM acento `conversao`:
        - "ativo": chamando a RPC como Supervisor, o vendedor de fixture (recém-criado, `ativo = true`) aparece como linha. Provar também que um vendedor ativo sem nenhum cliente atribuído aparece como linha completa de zeros (criar um segundo membro de fixture sem clientes, ou conferir isso no mesmo membro antes de semear os clientes): `negocios_iniciados`, `ganho` e `perdido` valem 0 e `ciclo_medio_dias` vem nulo — a linha nunca é escondida. Este nome de teste NÃO pode conter a palavra "desativado".
        - "desativado": depois de semear os clientes e ler os números, virar `profiles.ativo` para false no vendedor de fixture via `serviceClient()` e chamar a RPC de novo — a linha dele some COMPLETAMENTE do resultado. Na mesma leitura de antes/depois, chamar também a RPC de ganhos/perdidos (a que recebe janela de datas) como Supervisor com uma janela larga que cubra os eventos semeados, e afirmar que os totais dela ficaram INALTERADOS pela desativação. É a prova direta do limite da fase: desativar tira a LINHA desta tabela, nunca os números históricos de qualquer outro lugar.
        <!-- planner-discipline-allow: dashboard_ganhos_perdidos -->
        - "iniciados": `negocios_iniciados` do vendedor de fixture vale exatamente o número de clientes semeados (3 na sugestão acima), incluindo o que está em andamento e sem nenhum recorte de data — backdatar o `criado_em` de um dos clientes em vários meses e conferir que ele continua contando (D-01/D-02, ausência de filtro de período).
        - "ciclo": backdatar o `clientes.criado_em` do cliente GANHO em ~10 dias, deixando o evento de ganho no instante do teste, e backdatar o `clientes.criado_em` do cliente PERDIDO em ~30 dias com o evento de perda no instante do teste. Afirmar que `ciclo_medio_dias` fica próximo de 10 (tolerância de ~0,2 dia) e claramente NÃO próximo de 20 — é a prova de que a média é ganho-only (D-04) e que o perdido não entra. Afirmar também que um vendedor ativo sem nenhum ganho tem `ciclo_medio_dias` nulo, nunca 0.
        - "conversao": importar `taxaConversao` de `@/lib/dashboard/periodo` e afirmar (a) que a linha crua devolvida pela RPC NÃO traz nenhuma chave de taxa de conversão — a SQL nunca calcula isso (D-03); (b) que `taxaConversao` aplicada às contagens cruas da linha do vendedor de fixture (1 ganho e 1 perdido no seed sugerido) devolve 0.5; e (c) que para um vendedor com 0 ganhos e 0 perdidos a mesma função devolve `null`, nunca `NaN` nem 0.

      `tests/dashboard/rls-dashboard.test.ts` (ESTENDER, nunca criar arquivo novo para isso):
        - Atualizar o comentário de topo mencionando que o arquivo agora cobre também `dashboard_comparativo_vendedor()` (VEND-01), sem reescrever o texto já existente.
        - Acrescentar `vendedorB.rpc("dashboard_comparativo_vendedor")` (zero argumentos) aos DOIS arrays `Promise.all([...])` do primeiro describe (o de antes e o de depois), às checagens de `result.error`, e à comparação de igualdade das linhas normalizadas — o cliente criado e movido pelo Vendedor A não pode alterar NENHUM número visto pelo Vendedor B.
        - Acrescentar um bloco `it()` dedicado, no segundo describe, copiado em forma do bloco equivalente da Fase 11: Supervisor, Vendedor A e Vendedor B logados; leitura da RPC nova antes e depois de o Vendedor A criar um cliente; afirmar que a linha do Vendedor A vista pelo SUPERVISOR cresce em `negocios_iniciados`, enquanto o resultado visto pelo Vendedor B fica idêntico ao de antes. O nome do teste deve citar VEND-01. Comentar no bloco que a linha do Vendedor A APARECE no resultado do Vendedor B (a lista de nomes vem de `profiles`, que tem SELECT aberto) mas com contagens que não refletem os clientes alheios — é exatamente essa distinção que o teste trava.

    NÃO tocar em `lib/`, `app/` ou `components/` neste plano — são dos planos 02 a 04.
  </action>
  <verify>
    <automated>test -z "$(git diff --name-only HEAD -- supabase/migrations/)" && npx tsc --noEmit && node -e "const fs=require('fs'); const dir='supabase/migrations'; const all=fs.readdirSync(dir).filter(n=>n.endsWith('.sql')).sort(); const mig=all.filter(n=>n.includes('comparativo_vendedor')); if(mig.length!==1) throw new Error('esperado exatamente 1 arquivo _dashboard_comparativo_vendedor.sql, achei '+mig.length); if(all[all.length-1]!==mig[0]) throw new Error('a migration nova nao e a de maior numero da pasta: '+mig[0]+' vs '+all[all.length-1]); const sql=fs.readFileSync(dir+'/'+mig[0],'utf8'); const body=sql.split('\n').filter(l=>!l.trim().startsWith('--')).join('\n').toLowerCase(); for (const t of ['dashboard_comparativo_vendedor','vendedores_ativos','ultimo_status_change','fechamentos','agregados','negocios_iniciados','ciclo_medio_dias','responsavel_nome','language sql','stable','86400']) { if(!body.includes(t)) throw new Error('faltou no SQL: '+t) } if(/security\s+definer/.test(body)) throw new Error('a RPC nova NAO pode elevar privilegio - quebraria o isolamento por papel'); if(/p_inicio|p_fim/.test(body)) throw new Error('a RPC desta fase nao tem parametro de periodo (D-02)'); if(/taxa_conversao/.test(body)) throw new Error('D-03: taxa de conversao nunca e calculada no SQL'); const fn=(body.match(/create\s+or\s+replace\s+function/g)||[]).length; if(fn!==1) throw new Error('a migration deve declarar exatamente 1 funcao, achei '+fn); for (const outra of ['dashboard_ganhos_perdidos','dashboard_desempenho_vendedor','dashboard_funil_detalhado','dashboard_tempo_ate_fechamento','dashboard_clientes_por_etapa','dashboard_prospeccao']) { if(body.includes(outra)) throw new Error('esta migration nao pode tocar em outra RPC de dashboard: '+outra) } if(/alter\s+table|drop\s+|create\s+table|create\s+index|create\s+policy|alter\s+policy/.test(body)) throw new Error('migration tem que ser puramente aditiva e somente-leitura'); const at=(body.match(/ativo\s*=\s*true/g)||[]).length; if(at!==1) throw new Error('o filtro de ativo tem que aparecer EXATAMENTE 1 vez, so na CTE de listagem, achei '+at); const pa=(body.match(/\bp\.ativo\b/g)||[]).length; if(pa!==1) throw new Error('p.ativo tem que aparecer exatamente 1 vez, achei '+pa); const m=/ativo\s*=\s*true/.exec(body); if(m.index>body.indexOf('ultimo_status_change')) throw new Error('o filtro de ativos tem que estar na PRIMEIRA CTE, antes da agregacao'); const g=(body.match(/status_evento\s*=\s*'ganho'/g)||[]).length; if(g<2) throw new Error('D-04: o ciclo medio precisa do filter de ganho alem da contagem de ganhos'); const p=(body.match(/status_evento\s*=\s*'perdido'/g)||[]).length; if(p!==1) throw new Error('D-04: perdido so pode aparecer na contagem, nunca na media do ciclo, achei '+p); const nt=fs.readFileSync('tests/dashboard/comparativo-vendedor.test.ts','utf8'); const titles=(nt.match(/\bit\(\s*\"[^\"]+\"/g)||[]).map(s=>s.toLowerCase()); if(titles.length<5) throw new Error('esperado ao menos 5 casos de teste, achei '+titles.length); for (const k of ['desativado','iniciados','ciclo','conversao']) { if(!titles.some(t=>t.includes(k))) throw new Error('falta caso de teste com a palavra: '+k) } if(!titles.some(t=>t.includes('ativo') && !t.includes('desativ'))) throw new Error('falta o caso de teste ativo (lista so vendedores ativos), separado do caso desativado'); if(!nt.includes('createTestMember')||!nt.includes('deleteTestMember')) throw new Error('o teste tem que usar o padrao de fixture descartavel'); const rls=fs.readFileSync('tests/dashboard/rls-dashboard.test.ts','utf8'); const c=(rls.match(/dashboard_comparativo_vendedor/g)||[]).length; if(c<4) throw new Error('rls-dashboard.test.ts precisa citar a RPC nova nos dois arrays antes/depois e no bloco dedicado, achei '+c); console.log('OK '+mig[0])"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only HEAD -- supabase/migrations/` sai vazio: nenhum arquivo de migration já existente foi editado.
    - Existe exatamente um arquivo `supabase/migrations/*_dashboard_comparativo_vendedor.sql` e ele é o de MAIOR número da pasta (número derivado por `ls` na execução, não copiado deste plano).
    - A migration declara exatamente UMA função (`create or replace function` uma única vez) e não cita nenhuma outra RPC de dashboard no corpo sem comentários.
    - O corpo SQL (ignorando comentários) contém `dashboard_comparativo_vendedor`, as CTEs `vendedores_ativos`, `ultimo_status_change`, `fechamentos` e `agregados`, as colunas `negocios_iniciados`, `ciclo_medio_dias` e `responsavel_nome`, `language sql`, `stable` e o divisor `86400`.
    - O corpo SQL (ignorando comentários) NÃO contém cláusula de elevação de privilégio, NÃO contém parâmetro de período, NÃO contém coluna de taxa de conversão, e não contém `alter table` / `drop` / `create table` / `create index` / `create policy`.
    - O filtro de vendedor ativo aparece EXATAMENTE uma vez (`ativo = true` uma vez, `p.ativo` uma vez) e está posicionado antes da CTE de dedup do último status — ou seja, dentro de `vendedores_ativos`, nunca dentro de `agregados`.
    - O ciclo médio é ganho-only (D-04): a expressão de ganho aparece pelo menos 2 vezes (contagem + filtro do avg) e a de perdido aparece exatamente 1 vez (só na contagem).
    - `tests/dashboard/comparativo-vendedor.test.ts` existe, usa `createTestMember`/`deleteTestMember`, e tem ao menos 5 testes cujos nomes contêm "ativo" (sem ser "desativado"), "desativado", "iniciados", "ciclo" e "conversao" (sem acento).
    - `tests/dashboard/rls-dashboard.test.ts` foi estendido (não duplicado) e cita `dashboard_comparativo_vendedor` ao menos 4 vezes.
    - `npx tsc --noEmit` limpo.
    - Os testes ainda FALHAM em runtime (RED) neste ponto — esperado até o Task 3.
  </acceptance_criteria>
  <done>Migration com a RPC nova escrita (número derivado em execução), teste de comportamento novo criado e teste de RLS estendido; tudo ainda RED, aguardando o push.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Aprovação humana — aplicar a migration no banco Supabase de produção</name>
  <action>Pausar a execução. É a etapa de revisão antes de produção exigida pelo CLAUDE.md ("não fazer deploy direto em produção sem passar por uma etapa de revisão") e a prática já estabelecida nas Fases 9, 10 e 11 para toda migration que mexe no schema. Apresentar ao dono do projeto, em linguagem simples, o que a migration faz no banco real e só rodar o `supabase db push` (Task 3) depois do sinal "approved". Não prosseguir sem aprovação.</action>
  <what-built>
    Foi escrita uma migration nova que adiciona UMA "conta pronta" ao seu banco de dados — nada mais. Ela não cria, não apaga e não muda nenhum dado: só lê o que já existe e devolve números.

    Ela devolve uma linha para cada vendedor ATIVO da equipe, com quatro informações:
    1. Quantos negócios aquele vendedor iniciou desde sempre (todos os clientes que estão sob a responsabilidade dele, sem recorte de data nenhum).
    2. Quantos ele ganhou.
    3. Quantos ele perdeu.
    4. Quantos dias, em média, ele leva para fechar uma venda.

    Dois pontos que você decidiu e que estão travados no código:
    - A média de dias conta SÓ os negócios ganhos. Um negócio perdido não entra nessa média — a ideia é medir "quanto tempo leva pra fechar uma venda de verdade", não quanto tempo se gasta até desistir.
    - Vendedor desativado NÃO aparece como linha nessa tabela. Mas os clientes dele continuam existindo e continuam contando em todos os outros números do sistema, exatamente como antes. A migration foi escrita de propósito com o filtro de "ativo" em um lugar só (a lista de quem aparece), justamente para não contaminar nada além disso.

    Um vendedor ativo que ainda não tem nenhum cliente aparece na tabela mesmo assim, com zeros — de propósito, para você conseguir ver quem ainda não começou.

    Continua valendo a regra de sempre: a função roda "no crachá" de quem pergunta, então o banco já filtra sozinho quem vê o quê. Nenhuma regra de permissão nova foi programada.
  </what-built>
  <how-to-verify>
    1. Confirme que você está logado no Supabase CLI e com o projeto linkado (mesmo fluxo das migrations anteriores; se `supabase db push` pedir a senha do banco, é normal — tenha ela à mão).
    2. Se quiser conferir o arquivo: abra a migration nova em `supabase/migrations/` (é a de número mais alto, terminando em `_dashboard_comparativo_vendedor.sql`) e confirme que existe um único `create or replace function` e nenhum `drop`, `alter table` ou `delete`.
    3. Ponto que merece o seu olhar: "negócios iniciados" conta os clientes que estão HOJE sob a responsabilidade do vendedor. Se um cliente já foi transferido de um vendedor para outro (como acontece na desativação da Fase 10), ele conta para quem está com ele agora, não para quem o cadastrou. Se essa não for a leitura que você quer, é melhor falar agora do que depois de a tabela estar no ar.
  </how-to-verify>
  <resume-signal>Digite "approved" para autorizar o `supabase db push` no banco de produção, ou descreva qualquer dúvida/ajuste antes de aplicar.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Aplicar a migration no banco hospedado (supabase db push) e levar os testes a GREEN</name>
  <files>tests/dashboard/comparativo-vendedor.test.ts, tests/dashboard/rls-dashboard.test.ts</files>
  <read_first>
    - o arquivo de migration criado no Task 1 (o que será aplicado)
    - tests/helpers/supabase-test-clients.ts — conferir que `.env.local` tem `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` e `SUPABASE_SERVICE_ROLE_KEY` apontando para o projeto hospedado
    - .planning/STATE.md — Blockers/Concerns: rate-limit conhecido do `signInWithPassword` no projeto de teste ao vivo (evitar logins repetidos desnecessários dentro do mesmo arquivo de teste) e a linha de cliente com estado inválido deixada de propósito na base
  </read_first>
  <action>
    Rodar `supabase db push` para aplicar a migration no projeto Supabase hospedado (mesmo fluxo das migrations 0002 a 0010). Se falhar por autenticação ou link, criar dinamicamente um gate de auth (pedir ao dono que rode `supabase login` / `supabase link` e forneça a senha do banco) e repetir — NÃO seguir adiante sem o push concluído, porque este é o pré-requisito bloqueante de todos os planos seguintes desta fase (nada consegue chamar a RPC antes disso).

    Se o push acusar colisão de numeração (outra migration com o mesmo prefixo), renomear o arquivo desta fase para o próximo número livre real e repetir o push — nunca renomear nem editar uma migration já aplicada.

    Depois do push, rodar os dois arquivos de teste contra o banco real e levá-los a GREEN. Se algum caso falhar, diagnosticar contra a query aplicada ANTES de mexer no teste. Suspeitos prováveis, em ordem:
      1. `negocios_iniciados` menor que o esperado — provável filtro de ativo tendo vazado para a agregação, ou o `left join` de fechamentos tendo virado `join`.
      2. `ciclo_medio_dias` puxando para cima e ficando entre o valor do ganho e o do perdido — o `filter` de ganho não foi aplicado ao avg (D-04 quebrado).
      3. Linha do vendedor de fixture sumindo mesmo com ele ativo — o `left join` do select final invertido (agregados como base em vez de vendedores_ativos), o que também esconderia o vendedor sem clientes.
      4. Totais da RPC de ganhos/perdidos mudando depois da desativação — o filtro de ativo vazou para fora desta fase; é o cenário mais grave e exige corrigir a migration.
      <!-- planner-discipline-allow: dashboard_ganhos_perdidos -->
    Corrigir sempre pela MIGRATION (editando o mesmo arquivo se o push ainda não teve sucesso, ou criando uma nova se já teve) — nunca alterar um arquivo de migration já aplicado no banco.

    Este task não altera código de aplicação: só aplica a migration e valida.
  </action>
  <verify>
    <automated>npx vitest run tests/dashboard/comparativo-vendedor.test.ts tests/dashboard/rls-dashboard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` aplicou a migration no projeto hospedado sem erro; `dashboard_comparativo_vendedor()` existe no banco real.
    - `npx vitest run tests/dashboard/comparativo-vendedor.test.ts -t "ativo"` passa.
    - `npx vitest run tests/dashboard/comparativo-vendedor.test.ts -t "desativado"` passa.
    - `npx vitest run tests/dashboard/comparativo-vendedor.test.ts -t "iniciados"` passa.
    - `npx vitest run tests/dashboard/comparativo-vendedor.test.ts -t "ciclo"` passa.
    - `npx vitest run tests/dashboard/comparativo-vendedor.test.ts -t "conversao"` passa.
    - `npx vitest run tests/dashboard/rls-dashboard.test.ts` passa (GREEN): Vendedor B não enxerga números do Vendedor A na RPC nova, e o Supervisor enxerga.
    - `npm test` (suíte completa) continua verde — nenhuma regressão nos testes de dashboard existentes, em especial os de ganhos/perdidos e desempenho por vendedor.
    - Nenhum arquivo de migration já aplicado anteriormente foi editado.
  </acceptance_criteria>
  <done>Migration aplicada em produção com aprovação humana; a RPC nova provada por teste de integração contra o banco real, incluindo o isolamento por papel e a garantia de que a desativação não mexe em nenhum número histórico fora desta tabela.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (parte 1 de 4 — camada de banco)

| Símbolo / Arquivo | Tipo | Status |
|-------------------|------|--------|
| `supabase/migrations/{numero_derivado}_dashboard_comparativo_vendedor.sql` | Migration nova (aditiva, somente-leitura) | Criada (Task 1), aplicada (Task 3) |
| `dashboard_comparativo_vendedor()` | RPC sem argumentos, `language sql stable`, invoker; devolve `responsavel`, `responsavel_nome`, `negocios_iniciados`, `ganho`, `perdido`, `ciclo_medio_dias` — uma linha por vendedor ativo | Criada |
| `tests/dashboard/comparativo-vendedor.test.ts` | Teste de integração novo (ativo / desativado / iniciados / ciclo / conversao) | Criado (Task 1), GREEN (Task 3) |
| `tests/dashboard/rls-dashboard.test.ts` | Teste existente ESTENDIDO com a RPC nova (isolamento por papel, VEND-01) | Estendido (Task 1), GREEN (Task 3) |

Nenhuma tabela, view, coluna, trigger, índice, policy ou dependência npm nova. Nenhuma RPC de dashboard existente alterada.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário autenticado (Vendedor) → RPC nova de dashboard | Qualquer usuário logado pode chamar a função nova; o que ele recebe de volta tem que estar limitado aos clientes que a RLS já deixa ele ver. |
| `profiles` → lista de linhas da tabela comparativa | A lista de nomes de vendedores vem de uma tabela com policy de SELECT aberta para qualquer autenticado. |
| `historico.descricao` (escrito por trigger) → comparação de texto dentro da RPC | Um texto vindo do banco é interpretado para derivar ganho/perdido. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Information Disclosure / Elevation of Privilege | `dashboard_comparativo_vendedor()` | critical | mitigate | Declarada SEM cláusula de elevação de privilégio (invoker por omissão, convenção do `0003`), de modo que toda leitura interna de `clientes`/`historico` é filtrada pelas policies de RLS de `0001`/`0002`. Zero código de permissão novo. Regressão futura é pega pelo `tests/dashboard/rls-dashboard.test.ts` estendido e pelo check estrutural do Task 1, que falham alto se uma cláusula de elevação aparecer. |
| T-12-02 | Information Disclosure | roster de vendedores (`profiles`) visível a qualquer autenticado | low | accept | Comportamento pré-existente do `0001` (policy `using (true)`), já explorado hoje por `lib/equipe/membros.ts` e pela RPC de desempenho por vendedor. Um Vendedor que chame a RPC nova diretamente vê os NOMES dos colegas (já visíveis para ele hoje) com contagens zeradas, porque `clientes`/`historico` seguem escopados por RLS. Nenhuma exposição nova é introduzida por esta fase; adicionar checagem de papel dentro da função seria quebrar a convenção sem ganho real. |
| T-12-03 | Tampering (regressão de dados históricos) | filtro `ativo = true` vazando para a agregação | high | mitigate | O filtro aparece exatamente uma vez, na CTE de listagem, verificado mecanicamente pelo check estrutural do Task 1 (contagem exata de ocorrências + posição antes da CTE de dedup) e comportamentalmente pelo caso de teste "desativado", que afirma que os totais da RPC de ganhos/perdidos ficam inalterados após uma desativação. |
| T-12-04 | Tampering (injeção via dado) | derivação de ganho/perdido a partir de `historico.descricao` | low | accept | `descricao` nunca é fornecido pelo usuário: é escrito exclusivamente pelas triggers de `0002` sobre um cast de enum. A RPC não recebe parâmetro nenhum, então não há superfície de injeção pelo chamador. |
| T-12-05 | Tampering | migration aplicada direto em produção sem revisão | medium | mitigate | Checkpoint humano bloqueante (Task 2) antes do `supabase db push`, exigido pelo CLAUDE.md e já praticado nas Fases 9, 10 e 11. A migration é puramente aditiva (um único `create or replace function`), sem `drop`/`alter table`/`delete`, verificado pelo check estrutural do Task 1. |
| T-12-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` nesta fase — 12-RESEARCH.md registra zero pacotes novos ("Package Legitimacy Audit: not applicable"). Sem checkpoint de legitimidade aplicável. |
</threat_model>

<verification>
Checks rodados pelo executor ao final do plano:
- `git diff --name-only HEAD -- supabase/migrations/` vazio (nenhuma migration existente editada).
- `npx tsc --noEmit` limpo.
- Check estrutural do SQL (Task 1): função única, CTEs e colunas esperadas presentes; ausência de cláusula de elevação de privilégio, de parâmetro de período, de coluna de taxa de conversão e de DDL destrutivo; filtro de ativo com contagem exata 1 e posicionado na primeira CTE; ciclo médio ganho-only.
- `npx vitest run tests/dashboard/comparativo-vendedor.test.ts tests/dashboard/rls-dashboard.test.ts` verde contra o banco real, pós-push.
- `npm test` (suíte completa) verde.
- Checkpoint humano (Task 2): dono aprovou aplicar a migration em produção antes do push.
</verification>

<success_criteria>
- VEND-01: `dashboard_comparativo_vendedor()` devolve uma linha por vendedor ativo com negócios iniciados de histórico completo (D-01/D-02), ganhos, perdidos e ciclo médio em dias ganho-only (D-04), sem calcular taxa de conversão no SQL (D-03).
- Vendedor desativado some da lista de linhas desta RPC e NÃO afeta nenhum número histórico de nenhuma outra RPC de dashboard, provado por teste automatizado.
- Vendedor ativo sem clientes aparece como linha completa de zeros, nunca escondido.
- A RPC roda como o chamador; Vendedor B não enxerga números do Vendedor A, provado por teste automatizado.
- Migration aplicada no banco hospedado com aprovação humana prévia; suíte completa verde.
</success_criteria>

<output>
Create `.planning/phases/12-comparativo-por-vendedor/12-01-SUMMARY.md` when done.
</output>
