---
phase: 12-comparativo-por-vendedor
plan: 03
type: execute
wave: 3
depends_on: [12-02]
files_modified:
  - components/dashboard/ComparativoVendedorTable.tsx
  - tests/dashboard/comparativo-vendedor-table.test.tsx
autonomous: true
requirements: [VEND-01]

must_haves:
  truths:
    - "VEND-01: existe uma tabela dentro de um Card com o título 'Comparativo por vendedor' e cinco colunas nesta ordem exata — Vendedor, Taxa de conversão, Negócios iniciados, Negócios ganhos, Ciclo médio (dias)"
    - "VEND-01: cada linha corresponde a um vendedor ativo devolvido pela RPC, renderizada NA ORDEM RECEBIDA — o componente nunca reordena nem refiltra as linhas, porque a lista de ativos e a ordem alfabética já são decididas no SQL"
    - "VEND-01 (D-03): a taxa de conversão exibida vem do campo já calculado por taxaConversao() no plano 02, multiplicado por 100 só na hora de formatar — exatamente como GanhosPerdidosCards já faz hoje; nenhuma fórmula é reescrita aqui"
    - "VEND-01 (D-04): a coluna Ciclo médio (dias) tem um ícone de informação com o tooltip explicando que considera só os negócios ganhos, e não inclui os perdidos"
    - "Taxa de conversão e Ciclo médio renderizam travessão quando o valor é nulo — nunca um '0,0%' ou '0,0 dias' fabricado"
    - "Negócios iniciados e Negócios ganhos renderizam SEMPRE um inteiro, inclusive 0 — nunca travessão, porque zero é uma resposta real e informativa nessas duas colunas"
    - "Um vendedor ativo sem nenhum cliente aparece como linha completa (dois zeros e dois travessões), nunca é escondido"
    - "Quando a RPC devolve zero linhas (nenhum vendedor ativo), o componente mostra a mensagem de estado vazio em vez de uma tabela sem linhas"
    - "O componente tem estados de carregando, erro (com botão de nova tentativa) e vazio próprios e independentes, de modo que uma falha aqui nunca apaga o resto do dashboard"
    - "Nenhuma linha recebe destaque condicional: todas renderizam com o estilo padrão de linha, sem selo, borda colorida ou ícone de alerta — este componente não tem conceito de gargalo nem de sinalização de baixo desempenho"
    - "O componente não faz nenhuma checagem de papel — ele assume que só é montado para o Supervisor, decisão que pertence ao DashboardClient (plano 04)"
  artifacts:
    - "components/dashboard/ComparativoVendedorTable.tsx (novo)"
    - "tests/dashboard/comparativo-vendedor-table.test.tsx (novo — teste de render com a Server Action mockada)"
  key_links:
    - "multiplicação por 100 no formatador de percentual — sem ela a razão 0,625 renderizaria '0,6%' em vez de '62,5%'; é o mesmo formatador que GanhosPerdidosCards já usa"
    - "guarda de nulo devolvendo travessão nas duas colunas de razão/média, e ausência dessa guarda nas duas colunas de contagem"
    - "condição de estado vazio baseada no número de linhas recebidas (nenhum vendedor ativo), não na soma das contagens — um time inteiro com zero clientes ainda tem linhas para mostrar"
    - "chave de linha usando o id do responsavel, nunca o índice do array"
---

<objective>
Criar o componente de UI do VEND-01: `ComparativoVendedorTable.tsx`, uma tabela de cinco colunas dentro de um Card, com estados próprios de carregando/erro/vazio, alimentada pela Server Action do plano 02 — mais um teste de render que trava o formato dos números, as strings de copy e a ausência de reordenação no cliente.

Purpose: é a entrega visível da fase. Todo o cuidado aqui é de precisão de formatação: distinguir "zero de verdade" (contagem, mostra 0) de "indefinido" (razão/média sem denominador, mostra travessão), e converter a razão de conversão em percentual sem reescrever a fórmula.

Output: um componente novo e um teste de render novo. Nada é montado no dashboard ainda — isso é o plano 04.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-comparativo-por-vendedor/12-UI-SPEC.md
@.planning/phases/12-comparativo-por-vendedor/12-PATTERNS.md
@.planning/phases/12-comparativo-por-vendedor/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar components/dashboard/ComparativoVendedorTable.tsx</name>
  <files>components/dashboard/ComparativoVendedorTable.tsx</files>
  <read_first>
    - components/dashboard/FunilDetalhadoTable.tsx — arquivo inteiro. É o transplante estrutural quase exato: bloco de imports, os dois formatadores `Intl.NumberFormat("pt-BR")`, a união `FetchState`, o efeito de busca com guarda `cancelled` e contador `reloadKey`, o comentário `eslint-disable-next-line react-hooks/set-state-in-effect`, a casca `Card`/`CardHeader`/`CardTitle`/`CardDescription`/`CardContent` (linhas 129-139), o encadeamento carregando/erro/vazio (linhas 140-159, altura `h-[320px]`) e o padrão de coluna com ícone de informação e tooltip (linhas 164-176).
    - components/dashboard/GanhosPerdidosCards.tsx — a função `formatConversao` (linhas 29-34). É a referência OBRIGATÓRIA para a coluna de taxa de conversão: `taxaConversao()` devolve uma RAZÃO entre 0 e 1, e o formatador multiplica por 100 antes de anexar o símbolo de porcentagem. O trecho equivalente citado no 12-PATTERNS.md omite essa multiplicação — o 12-PATTERNS.md está errado nesse detalhe e o comportamento correto é o do `GanhosPerdidosCards`.
    - .planning/phases/12-comparativo-por-vendedor/12-UI-SPEC.md — seções "Copywriting Contract" (as strings exatas de título, legenda, cabeçalhos, tooltip, vazio e erro) e "Component-Specific Implementation Notes / 1." (ordem das colunas, ordem das linhas, ausência de destaque condicional, alturas dos estados).
    - .planning/phases/12-comparativo-por-vendedor/12-PATTERNS.md — bloco "components/dashboard/ComparativoVendedorTable.tsx (new)": imports, efeito de busca, casca do Card, ramos de estado e markup da tabela, prontos para adaptar (com a ressalva do formatador de percentual acima).
    - lib/supabase/queries/dashboard.ts — o tipo `ComparativoVendedorRow` criado no plano 02, para importar o tipo com os nomes de campo certos.
    - app/actions/dashboard.ts — a assinatura de `getComparativoVendedorAction()` criada no plano 02.
  </read_first>
  <action>
    Criar `components/dashboard/ComparativoVendedorTable.tsx` como Client Component (`"use client"` na primeira linha), exportando a função nomeada `ComparativoVendedorTable`, sem nenhuma prop.

    Imports: `useEffect`/`useState` de react; `CircleHelp` de lucide-react; `getComparativoVendedorAction` de `@/app/actions/dashboard`; `Button`; `Card`, `CardContent`, `CardDescription`, `CardHeader`, `CardTitle`; `Skeleton`; `Table`, `TableBody`, `TableCell`, `TableHead`, `TableHeader`, `TableRow`; `Tooltip`, `TooltipContent`, `TooltipTrigger`; e o tipo `ComparativoVendedorRow` de `@/lib/supabase/queries/dashboard`. Nada além disso — em particular, nenhum componente de selo, nenhum ícone de alerta e nenhuma lista fixa de etapas (este componente não tem conceito de gargalo nem conjunto fixo de linhas).

    Formatadores no escopo do módulo, copiados de `FunilDetalhadoTable.tsx`:
      - `integerFormatter` = `new Intl.NumberFormat("pt-BR")`.
      - `percentFormatter` = `new Intl.NumberFormat("pt-BR", { minimumFractionDigits: 1, maximumFractionDigits: 1 })`.
      - `formatConversao(rate: number | null): string` — devolve `"—"` (travessão) quando `rate` é `null`; caso contrário `percentFormatter.format(rate * 100)` seguido do símbolo de porcentagem. A MULTIPLICAÇÃO POR 100 É OBRIGATÓRIA: o valor que chega é a razão crua de `taxaConversao()`, então sem ela a taxa de 62,5% apareceria como "0,6%". Nomear a função `formatConversao` (e não `formatPct`) justamente para deixar claro que ela é a gêmea da de `GanhosPerdidosCards`, não a de `FunilDetalhadoTable`. Deixar um comentário de uma linha registrando isso.
      - `formatDias(value: number | null): string` — devolve `"—"` quando `value` é `null`; caso contrário `percentFormatter.format(value)` seguido de `" dias"`. Sem multiplicação aqui: o valor já vem em dias.

    Estado: a união `FetchState` com `{ status: "loading" }` | `{ status: "error" }` | `{ status: "ready"; rows: ComparativoVendedorRow[] }`, mais o contador `reloadKey`. Efeito de busca copiado de `FunilDetalhadoTable.tsx`: flag `cancelled`, `setState({ status: "loading" })` no corpo do efeito com o mesmo comentário de desativação de regra do eslint, chamada de `getComparativoVendedorAction()`, retorno antecipado se `cancelled`, `setState({ status: "error" })` quando `result.error`, `setState({ status: "ready", rows: result.data })` caso contrário, e cleanup marcando `cancelled`. Lista de dependências: apenas `[reloadKey]` — a action não recebe nenhum argumento.

    Casca: `Card` > `CardHeader` > `CardTitle` com `className="text-[20px] font-semibold"` e o texto exato `Comparativo por vendedor`; `CardDescription` com o texto exato `Histórico completo — não é afetado pelo filtro de período` (reuso verbatim da legenda do `FunilDetalhadoTable`, travado pelo Copywriting Contract). Depois `CardContent` com o encadeamento de ramos:
      1. carregando → `<Skeleton className="h-[320px] w-full" />`.
      2. erro → `div` com `className="flex h-[320px] w-full flex-col items-center justify-center gap-3 text-center"`, um `p` com `className="text-sm text-muted-foreground"` e o texto exato `Não foi possível carregar os dados do dashboard. Tente novamente.`, e um `Button` com `type="button"`, `variant="outline"`, rótulo exato `Tentar novamente` e `onClick` incrementando `reloadKey`.
      3. vazio → quando o array de linhas recebido tem comprimento zero, um `div` com `className="flex h-[320px] w-full items-center justify-center text-center text-sm text-muted-foreground"` e o texto exato `Nenhum vendedor ativo no momento.`. A condição é o NÚMERO DE LINHAS recebidas, nunca a soma das contagens: um time inteiro com zero clientes ainda tem linhas para mostrar (é justamente o sinal de que ninguém começou).
      4. caso contrário → a tabela.

    Tabela: `Table` > `TableHeader` > `TableRow` com cinco `TableHead`, nesta ordem exata e com estes textos exatos: `Vendedor`, `Taxa de conversão`, `Negócios iniciados`, `Negócios ganhos`, `Ciclo médio (dias)`. O quinto cabeçalho envolve o texto e o ícone num `div` com `className="flex items-center gap-1"`, seguido de um `Tooltip` cujo `TooltipTrigger` tem `className="inline-flex shrink-0 items-center bg-transparent p-0"` e `aria-label` igual à string do tooltip, contendo um `CircleHelp` com `className="size-3.5 text-muted-foreground"`, e cujo `TooltipContent` traz o texto exato `Considera só os negócios ganhos — não inclui os negócios perdidos.` — a string aparece nos dois lugares (rótulo acessível e conteúdo visual), exatamente como o cabeçalho de Quantidade do `FunilDetalhadoTable` já faz. Não acrescentar uma segunda linha de texto de ajuda visível abaixo do cabeçalho.

    Corpo: `TableBody` iterando as linhas recebidas DIRETAMENTE, na ordem em que vieram, com `key={row.responsavel}` (o id, nunca o índice). Cada linha tem cinco `TableCell`:
      1. `row.responsavelNome` com `className="font-medium"` (a coluna que identifica a linha é a única com peso destacado).
      2. `formatConversao(row.taxaConversao)`.
      3. `integerFormatter.format(row.negociosIniciados)` — sempre um inteiro, inclusive 0, nunca travessão.
      4. `integerFormatter.format(row.ganho)` — mesma regra.
      5. `formatDias(row.cicloMedioDias)`.

    Proibições explícitas neste componente:
      - Não reordenar as linhas por nenhum critério e não oferecer cabeçalho clicável para ordenar — a ordem alfabética vem pronta do SQL.
      - Não refiltrar as linhas por nenhum critério — a lista de vendedores ativos já vem pronta do SQL.
      - Não aplicar destaque condicional em nenhuma linha: nada de borda colorida à esquerda, selo ou ícone de alerta. Esta fase não define nenhum conceito de sinalização de baixo desempenho, e inventar um seria ampliar o escopo além do que foi decidido.
      - Não fazer nenhuma checagem de papel do usuário dentro do componente, e não receber prop de papel: quem decide se ele é montado é o `DashboardClient` (plano 04).
      - Não receber prop de período: o componente é de histórico completo (D-02).

    Comentário JSDoc no topo do componente citando VEND-01, registrando que é uma seção somente-leitura de histórico completo, que a decisão de mostrá-lo só ao Supervisor mora no `DashboardClient` apoiada na RLS, e que a lista/ordem das linhas é responsabilidade exclusiva da RPC.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && node -e "const s=require('fs').readFileSync('components/dashboard/ComparativoVendedorTable.tsx','utf8'); if(!s.startsWith('\"use client\"')) throw new Error('faltou a diretiva use client na primeira linha'); for (const t of ['ComparativoVendedorTable','getComparativoVendedorAction','ComparativoVendedorRow','formatConversao','formatDias','integerFormatter','percentFormatter','reloadKey','h-[320px]','size-3.5 text-muted-foreground','inline-flex shrink-0 items-center bg-transparent p-0','row.responsavel','font-medium','Comparativo por vendedor','Tentar novamente','Nenhum vendedor ativo no momento.','Vendedor']) { if(!s.includes(t)) throw new Error('faltou: '+t) } for (const re of [/Hist.rico completo . n.o . afetado pelo filtro de per.odo/, /N.o foi poss.vel carregar os dados do dashboard\. Tente novamente\./, /Taxa de convers.o/, /Neg.cios iniciados/, /Neg.cios ganhos/, /Ciclo m.dio \(dias\)/, /Considera s. os neg.cios ganhos . n.o inclui os neg.cios perdidos\./]) { if(!re.test(s)) throw new Error('string do Copywriting Contract ausente ou reescrita: '+re) } if(!/\*\s*100/.test(s)) throw new Error('a taxa de conversao precisa ser multiplicada por 100 na formatacao - taxaConversao devolve uma razao, nao um percentual'); const ints=(s.match(/integerFormatter\.format/g)||[]).length; if(ints<2) throw new Error('as duas colunas de contagem precisam usar integerFormatter, achei '+ints); for (const proibido of ['Badge','TriangleAlert','border-l-4','isSupervisor','ETAPAS']) { if(s.includes(proibido)) throw new Error('nao permitido neste componente: '+proibido) } if(/\.sort\(/.test(s)) throw new Error('a ordem das linhas vem do SQL - nao reordenar no cliente'); if(/\.filter\(/.test(s)) throw new Error('a lista de vendedores ativos vem do SQL - nao refiltrar no cliente'); const trav=(s.match(/\u2014\"/g)||[]).length; if(trav<2) throw new Error('as duas colunas anulaveis precisam devolver travessao quando o valor e nulo, achei '+trav); console.log('OK componente')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/dashboard/ComparativoVendedorTable.tsx` existe, começa com a diretiva de Client Component e exporta `ComparativoVendedorTable` sem props.
    - As cinco colunas aparecem com os textos exatos do Copywriting Contract, nesta ordem: Vendedor, Taxa de conversão, Negócios iniciados, Negócios ganhos, Ciclo médio (dias).
    - O título, a legenda, a mensagem de erro, o rótulo do botão de nova tentativa, a mensagem de estado vazio e o texto do tooltip são as strings exatas do 12-UI-SPEC.md, sem reescrita.
    - A formatação da taxa de conversão multiplica o valor por 100 (a razão crua de `taxaConversao()` vira percentual), e `formatConversao`/`formatDias` devolvem travessão quando o valor é nulo.
    - As duas colunas de contagem usam `integerFormatter` e nunca produzem travessão.
    - Os três estados (carregando, erro com botão de nova tentativa, vazio) usam a altura `h-[320px]` e são independentes do resto do dashboard.
    - O componente não importa nem usa selo, ícone de alerta, borda colorida à esquerda, lista fixa de etapas ou checagem de papel; não chama `.sort(` nem `.filter(` sobre as linhas; usa `row.responsavel` como chave.
    - `npx tsc --noEmit` e `npm run lint` limpos.
  </acceptance_criteria>
  <done>Componente criado, espelhando estruturalmente o FunilDetalhadoTable, com as strings de copy travadas e a distinção entre zero real e valor indefinido implementada coluna a coluna.</done>
</task>

<task type="auto">
  <name>Task 2: Criar o teste de render tests/dashboard/comparativo-vendedor-table.test.tsx</name>
  <files>tests/dashboard/comparativo-vendedor-table.test.tsx</files>
  <read_first>
    - tests/dashboard/funil-detalhado-table.test.tsx — arquivo inteiro. É o molde exato: o comentário `// @vitest-environment jsdom` na primeira linha, o `vi.mock` da action seguido do import dela e do `vi.mocked(...)`, o helper que constrói uma linha a partir de valores parciais, o wrapper com `TooltipProvider`, e as asserções via `screen.findByRole("table")` + `within(table).getAllByRole("row")`.
    - components/dashboard/ComparativoVendedorTable.tsx — o componente criado no Task 1.
    - vitest.config.ts — confirmar que o ambiente jsdom está restrito aos arquivos `.test.tsx` (não mudar nada aqui).
  </read_first>
  <action>
    Criar `tests/dashboard/comparativo-vendedor-table.test.tsx` seguindo o molde de `funil-detalhado-table.test.tsx`: primeira linha com a diretiva de ambiente jsdom, `vi.mock("@/app/actions/dashboard", ...)` expondo `getComparativoVendedorAction` como mock, import da action mockada logo depois do `vi.mock`, `vi.mocked(...)` para tipar, um helper `buildRow(partial)` que devolve uma `ComparativoVendedorRow` completa com valores padrão de "vendedor sem nada ainda" (contagens em 0, ciclo e taxa nulos, `responsavel` distinto por linha), e um helper de render que envolve o componente em `TooltipProvider`.

    Casos exigidos:
      1. Ordem preservada: mockar três linhas em uma ordem deliberadamente NÃO alfabética e não ordenada por nenhuma métrica, e afirmar que as linhas aparecem na tabela exatamente nessa ordem. É a prova de que o componente não reordena nada — a ordem é responsabilidade do SQL.
      2. Formatação da taxa de conversão: uma linha com `taxaConversao` igual a 0.625 renderiza `62,5%`. É o caso que pega o erro de esquecer a multiplicação por 100 (que produziria `0,6%`).
      3. Zero real versus indefinido, na mesma linha: um vendedor com `negociosIniciados` 0, `ganho` 0, `taxaConversao` nula e `cicloMedioDias` nulo renderiza `0` nas duas colunas de contagem e travessão nas duas colunas de razão/média — e a linha EXISTE (não é escondida). Afirmar explicitamente que as células de contagem não contêm travessão.
      4. Ciclo médio formatado: uma linha com `cicloMedioDias` igual a 12.5 renderiza `12,5 dias`.
      5. Tooltip do ciclo médio: o cabeçalho da quinta coluna expõe um elemento com o rótulo acessível exato `Considera só os negócios ganhos — não inclui os negócios perdidos.` (buscar por `getByLabelText` ou pelo `role` do gatilho, o que for estável com o primitivo já usado no teste da Fase 11).
      6. Estado vazio: quando a action devolve um array vazio, a tabela NÃO é renderizada (`queryByRole("table")` nulo) e o texto `Nenhum vendedor ativo no momento.` aparece.
      7. Estado de erro: quando a action devolve `{ error: ... }`, aparecem a mensagem padrão do dashboard e o botão `Tentar novamente`.

    Usar `mockResolvedValueOnce` por caso (como o teste da Fase 11 faz), para cada `it()` controlar seu próprio retorno sem vazamento entre testes.

    NÃO testar a RPC nem a Server Action reais aqui — esse é o papel do `tests/dashboard/comparativo-vendedor.test.ts` do plano 01. Este arquivo é só de render.
  </action>
  <verify>
    <automated>npx vitest run tests/dashboard/comparativo-vendedor-table.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `tests/dashboard/comparativo-vendedor-table.test.tsx` existe, começa com a diretiva de ambiente jsdom e mocka `getComparativoVendedorAction`.
    - Existe um caso que prova que a ordem das linhas recebidas é preservada (mock em ordem não alfabética).
    - Existe um caso que prova que `taxaConversao` 0.625 renderiza `62,5%`.
    - Existe um caso que prova que uma linha toda zerada renderiza `0` nas duas colunas de contagem e travessão nas duas colunas de razão/média, e que a linha não é escondida.
    - Existe um caso que prova o formato `12,5 dias` do ciclo médio.
    - Existe um caso que prova o rótulo acessível exato do tooltip do ciclo médio.
    - Existem casos para o estado vazio (sem tabela + mensagem de nenhum vendedor ativo) e para o estado de erro (mensagem padrão + botão de nova tentativa).
    - `npx vitest run tests/dashboard/comparativo-vendedor-table.test.tsx` verde.
    - `npm test` (suíte completa) continua verde.
  </acceptance_criteria>
  <done>Teste de render criado, travando ordem, formatação de percentual/dias, a distinção entre zero real e indefinido, e os três estados auxiliares.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (parte 3 de 4 — camada de UI)

| Símbolo / Arquivo | Tipo | Status |
|-------------------|------|--------|
| `components/dashboard/ComparativoVendedorTable.tsx` | Client Component novo, sem props | Criado (Task 1) |
| `ComparativoVendedorTable` | Componente exportado — Card + tabela de 5 colunas com estados de carregando/erro/vazio próprios | Criado (Task 1) |
| `formatConversao` / `formatDias` / `integerFormatter` / `percentFormatter` | Formatadores no escopo do módulo do componente (não exportados) | Criados (Task 1) |
| `tests/dashboard/comparativo-vendedor-table.test.tsx` | Teste de render novo, com a Server Action mockada | Criado (Task 2) |

Nenhum componente do shadcn novo, nenhuma dependência npm nova, nenhum token de design novo.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → Client Component | Dados agregados de todo o time atravessam para o navegador de quem tiver o componente montado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-10 | Information Disclosure | `ComparativoVendedorTable` montado para um papel indevido | medium | mitigate | O componente não é a fronteira de autorização e não tenta ser: ele não faz checagem de papel (verificado pelo check estrutural, que falha se uma aparecer). A fronteira real é a RLS na RPC (T-12-01) — mesmo que o componente fosse montado para um Vendedor por engano, as contagens dos colegas viriam zeradas. A ocultação por papel no `DashboardClient` (plano 04) é conforto visual sobre essa fronteira, nunca a garantia. |
| T-12-11 | Tampering (integridade de exibição) | formatação de percentual sem multiplicação por 100 | medium | mitigate | Um número gerencial exibido errado é um defeito de integridade, não só cosmético: um Supervisor leria 0,6% onde a taxa real é 62,5%. Mitigado pelo check estrutural (exige a multiplicação) e pelo caso de teste de render que fixa `0.625` renderizando `62,5%`. |
| T-12-12 | Tampering (integridade de exibição) | zero fabricado onde o valor é indefinido | low | mitigate | As duas colunas de razão/média devolvem travessão quando nulas, provado pelo caso de teste da linha zerada; as duas colunas de contagem nunca devolvem travessão, também provado no mesmo caso. |
| T-12-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` neste plano — todos os primitivos de UI usados já estão instalados (12-UI-SPEC.md, "New shadcn components added by this phase: none"). |
</threat_model>

<verification>
Checks rodados pelo executor ao final do plano:
- `npx tsc --noEmit` e `npm run lint` limpos.
- Check estrutural do componente: diretiva de client, símbolos e classes esperadas, todas as strings do Copywriting Contract presentes sem reescrita, multiplicação por 100 presente, ausência de selo/ícone de alerta/borda colorida/checagem de papel/reordenação/refiltro.
- `npx vitest run tests/dashboard/comparativo-vendedor-table.test.tsx` verde.
- `npm test` (suíte completa) verde.
</verification>

<success_criteria>
- VEND-01: existe a tabela de cinco colunas com o título e a legenda exatos, uma linha por vendedor ativo, na ordem recebida da RPC.
- D-03: a taxa de conversão exibida vem do campo já calculado no plano 02 e é convertida em percentual só na formatação.
- D-04: o tooltip da coluna de ciclo médio explica que a média considera só os negócios ganhos.
- Zero real e valor indefinido são visualmente distintos e testados.
- Os três estados auxiliares existem, são independentes e usam as strings de copy já padronizadas no projeto.
</success_criteria>

<output>
Create `.planning/phases/12-comparativo-por-vendedor/12-03-SUMMARY.md` when done.
</output>
