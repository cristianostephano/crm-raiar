---
phase: 12-comparativo-por-vendedor
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - lib/supabase/queries/dashboard.ts
  - app/actions/dashboard.ts
autonomous: true
requirements: [VEND-01]

must_haves:
  truths:
    - "VEND-01: existe o leitor tipado getComparativoVendedor() que chama dashboard_comparativo_vendedor() sem argumento e devolve ComparativoVendedorRow[] em camelCase"
    - "VEND-01 (Pitfall 4): negociosIniciados, ganho e perdido passam por Number() porque o PostgREST serializa bigint como string; cicloMedioDias preserva null explicitamente (a checagem de null acontece ANTES de qualquer Number()), então um vendedor sem nenhum ganho nunca vira 0 nem NaN"
    - "VEND-01 (D-03): a taxa de conversão é calculada UMA única vez, aqui, chamando taxaConversao(ganho, perdido) importada de lib/dashboard/periodo.ts — a fórmula nunca é reescrita, nem no SQL, nem no componente"
    - "VEND-01 (D-03): o campo taxaConversao da linha guarda a RAZÃO crua devolvida por taxaConversao() (um número entre 0 e 1, ou null), exatamente como GanhosPerdidosCards já consome hoje — a multiplicação por 100 é responsabilidade exclusiva do formatador de exibição, no plano 03"
    - "VEND-01: existe a Server Action getComparativoVendedorAction(), sem argumento, com o mesmo guard de sessão e a mesma união {data}|{error} das sete actions já existentes do arquivo, reusando os códigos de erro e as mensagens já existentes sem inventar texto novo"
    - "Nenhuma das funções novas filtra por responsavel, por ativo ou por papel no TypeScript — a RLS da RPC continua sendo a única fronteira de autorização, e a lista de vendedores ativos continua sendo decidida só no SQL"
    - "Nenhuma das funções novas recebe parâmetro de período (D-02)"
  artifacts:
    - "ComparativoVendedorRow — tipo exportado de lib/supabase/queries/dashboard.ts"
    - "getComparativoVendedor() — leitor tipado sem argumento em lib/supabase/queries/dashboard.ts"
    - "GetComparativoVendedorResult — tipo de resultado {data}|{error} em app/actions/dashboard.ts"
    - "getComparativoVendedorAction() — Server Action sem argumento em app/actions/dashboard.ts"
  key_links:
    - "import de taxaConversao vindo de @/lib/dashboard/periodo dentro de lib/supabase/queries/dashboard.ts — é o elo que entrega o D-03; sem ele a fórmula seria reescrita em algum lugar"
    - "checagem de null em ciclo_medio_dias antes do Number() — é o elo que faz a coluna renderizar travessão em vez de 0,0 dias para quem nunca ganhou"
    - "bloco de import único de @/lib/supabase/queries/dashboard em app/actions/dashboard.ts — estendido, nunca duplicado"
---

<objective>
Construir a ponte entre a RPC criada no plano 01 e a UI do plano 03: o leitor tipado `getComparativoVendedor()` (que normaliza os números e calcula a taxa de conversão reusando a função já existente, per D-03) e a Server Action `getComparativoVendedorAction()` (sem argumento, com o guard de sessão e as mensagens de erro padrão do dashboard).

Purpose: é a única camada onde a taxa de conversão pode ser calculada sem duplicar a fórmula — a SQL de propósito não a devolve e o componente de propósito não a recalcula. Também é onde os `bigint` que chegam como string do PostgREST viram números de verdade, e onde o `null` de "ciclo médio indefinido" é preservado em vez de virar zero.

Output: dois símbolos novos em `lib/supabase/queries/dashboard.ts` e dois em `app/actions/dashboard.ts`, sem tocar em nada do que já existe nos dois arquivos.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-comparativo-por-vendedor/12-CONTEXT.md
@.planning/phases/12-comparativo-por-vendedor/12-PATTERNS.md
@.planning/phases/12-comparativo-por-vendedor/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar o tipo e o leitor tipado da RPC nova em lib/supabase/queries/dashboard.ts</name>
  <files>lib/supabase/queries/dashboard.ts</files>
  <read_first>
    - lib/supabase/queries/dashboard.ts — arquivo inteiro. Prestar atenção no comentário de topo (linhas 4-19: "NO manual responsavel/role filtering anywhere in this file"), no comentário sobre `Number()` (linhas 21-23: `bigint` volta do PostgREST como string), em `getFunilDetalhado` (o análogo mais próximo: sem argumento e com várias colunas anuláveis), em `getDesempenhoVendedor` (o análogo do formato de linha `responsavel` + `responsavel_nome`), e no bloco de re-export `export { ETAPA_KEYS }` no fim do arquivo.
    - lib/dashboard/periodo.ts — a função `taxaConversao(ganho, perdido)` (linhas 40-44). Reparar que ela devolve a RAZÃO (`ganho / total`, um número entre 0 e 1), não um percentual, e devolve `null` quando o total é 0.
    - .planning/phases/12-comparativo-por-vendedor/12-PATTERNS.md — bloco "lib/supabase/queries/dashboard.ts (extend)": traz o texto exato do tipo e da função a acrescentar, além do import a adicionar.
    - .planning/phases/12-comparativo-por-vendedor/12-01-SUMMARY.md — confirmar os nomes finais das colunas devolvidas pela RPC (caso algo tenha sido ajustado durante a execução do plano 01) antes de escrever o mapeamento.
  </read_first>
  <action>
    Acrescentar em `lib/supabase/queries/dashboard.ts`, logo depois do último leitor existente e ANTES do bloco de re-export `export { ETAPA_KEYS }` que fecha o arquivo (sem reordenar nem reescrever nada do que já existe):

    1. Um import novo no topo do arquivo: `taxaConversao` vindo de `@/lib/dashboard/periodo`, posicionado alfabeticamente entre os dois imports já existentes. Deve haver exatamente UM import desse módulo no arquivo.

    2. O tipo `ComparativoVendedorRow` com os campos `responsavel: string`, `responsavelNome: string | null`, `negociosIniciados: number`, `ganho: number`, `perdido: number`, `cicloMedioDias: number | null` e `taxaConversao: number | null`. Deixar um comentário de uma linha no campo `taxaConversao` registrando que ele é calculado aqui pela função reusada e que a RPC nunca o devolve (D-03), e que o valor guardado é a razão crua (0 a 1), não um percentual.

    3. A função `getComparativoVendedor(): Promise<ComparativoVendedorRow[]>` — `createClient()`, `supabase.rpc("dashboard_comparativo_vendedor")` sem argumento, e no caminho de erro `throw new Error(...)` com a mensagem `Falha ao carregar comparativo por vendedor: ` concatenada ao `error.message` (mesma postura throw-on-error do arquivo; é a action que converte isso em estado de erro). Depois um `.map()` de snake_case para camelCase com a linha crua tipada explicitamente inline (sem tipo dinâmico não justificado, conforme o CLAUDE.md), no mesmo estilo do parâmetro tipado inline de `getFunilDetalhado`. Regras do mapeamento:
       - `responsavel` passa direto (uuid como string) e `responsavel_nome` vira `responsavelNome` preservando `null`.
       - `negocios_iniciados`, `ganho` e `perdido` passam por `Number()` — o PostgREST manda `bigint` como string, e sem isso uma soma viraria concatenação de texto.
       - `ciclo_medio_dias` vira `cicloMedioDias` com a checagem `=== null` ANTES de chamar `Number()`; se o `Number()` vier primeiro, o `null` vira `0` e a tabela renderizaria "0,0 dias" onde deve renderizar travessão.
       - `taxaConversao` recebe o retorno de `taxaConversao(ganho, perdido)` usando as variáveis JÁ normalizadas por `Number()` (extrair `ganho` e `perdido` em constantes antes de montar o objeto, para não chamar `Number()` duas vezes nem passar string para a função).

    A função ganha um comentário JSDoc de uma linha citando VEND-01 e registrando que é um retrato de histórico completo, sem parâmetro de período — no mesmo formato do comentário `/** DSH-01/DSH-08: live snapshot, no period parameters. */` já presente no arquivo.

    Estender o comentário de topo do arquivo mencionando que agora são oito funções de dashboard (as cinco do `0003`, as duas da Fase 11 e a desta fase) — sem reescrever o parágrafo existente sobre ausência de filtro manual de responsavel/papel, que continua valendo integralmente. Acrescentar a esse parágrafo uma frase registrando que a lista de vendedores ATIVOS também é decidida só no SQL: este arquivo nunca filtra por `ativo`.

    NÃO adicionar filtro por `responsavel`, por `ativo` ou por papel. NÃO adicionar parâmetro nenhum. NÃO recalcular a razão de conversão à mão. NÃO tocar em `app/actions/dashboard.ts` neste task.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && node -e "const s=require('fs').readFileSync('lib/supabase/queries/dashboard.ts','utf8'); for (const t of ['ComparativoVendedorRow','getComparativoVendedor','dashboard_comparativo_vendedor','responsavelNome','negociosIniciados','cicloMedioDias','taxaConversao']) { if(!s.includes(t)) throw new Error('faltou: '+t) } const imp=(s.match(/from \"@\/lib\/dashboard\/periodo\"/g)||[]).length; if(imp!==1) throw new Error('esperado exatamente 1 import de lib/dashboard/periodo, achei '+imp); const chunk=s.split('getComparativoVendedor').slice(1).join(''); if(!/ciclo_medio_dias\s*===\s*null/.test(chunk)) throw new Error('cicloMedioDias tem que checar null ANTES de Number()'); if(!/taxaConversao\(/.test(chunk)) throw new Error('D-03: taxaConversao tem que ser CHAMADA, nunca reimplementada'); if(/p_inicio|p_fim/.test(chunk)) throw new Error('o leitor novo nao pode ter parametro de periodo'); if(/\.filter\(/.test(chunk)) throw new Error('a lista de vendedores ativos ja vem pronta do SQL - este arquivo nao filtra linhas'); console.log('OK leitor')"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/supabase/queries/dashboard.ts` exporta `ComparativoVendedorRow` e `getComparativoVendedor`.
    - `getComparativoVendedor` chama `supabase.rpc("dashboard_comparativo_vendedor")` sem nenhum argumento.
    - Existe exatamente UM import de `@/lib/dashboard/periodo` no arquivo, e `taxaConversao` é chamada (não reimplementada) para preencher o campo homônimo da linha.
    - O campo `taxaConversao` guarda a razão crua devolvida pela função (0 a 1, ou `null`) — nenhuma multiplicação por 100 acontece neste arquivo.
    - `cicloMedioDias` preserva `null`: a comparação `ciclo_medio_dias === null` aparece antes de qualquer `Number()` sobre esse campo.
    - `negociosIniciados`, `ganho` e `perdido` passam por `Number()`.
    - Nenhum filtro por `responsavel`, por vendedor ativo ou por papel, e nenhum parâmetro de período no trecho novo.
    - `npx tsc --noEmit` e `npm run lint` limpos.
  </acceptance_criteria>
  <done>Tipo e leitor tipado adicionados, espelhando `getFunilDetalhado`, com preservação de null no ciclo médio e a taxa de conversão calculada por reuso (D-03).</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar a Server Action sem argumento em app/actions/dashboard.ts</name>
  <files>app/actions/dashboard.ts</files>
  <read_first>
    - app/actions/dashboard.ts — arquivo inteiro. O bloco de import único de `@/lib/supabase/queries/dashboard`, a união `DashboardErrorCode`, e `getFunilDetalhadoAction` (o análogo exato, por não receber argumento).
    - .planning/phases/12-comparativo-por-vendedor/12-PATTERNS.md — bloco "app/actions/dashboard.ts (extend)": traz o texto exato do import estendido e da action a acrescentar.
    - lib/supabase/queries/dashboard.ts — os dois símbolos criados no Task 1, para importar com os nomes certos.
  </read_first>
  <action>
    Acrescentar ao FINAL de `app/actions/dashboard.ts`:

    1. `GetComparativoVendedorResult` — a união `{ data: ComparativoVendedorRow[]; error?: undefined } | { data?: undefined; error: { code: DashboardErrorCode; message: string } }`, exatamente o formato de `GetFunilDetalhadoResult`.

    2. `getComparativoVendedorAction(): Promise<GetComparativoVendedorResult>` — sem argumento. Corpo copiado de `getFunilDetalhadoAction`: `createClient()`, `supabase.auth.getUser()`, se não houver `user` devolve `{ error: { code: "unauthenticated", message: "Sessão expirada." } }`, senão `try { return { data: await getComparativoVendedor() } } catch { ... }` devolvendo `{ code: "fetch_falhou", message: "Não foi possível carregar os dados do dashboard. Tente novamente." }`. A mensagem tem que ser essa string exata: o componente do plano 03 a renderiza verbatim e o Copywriting Contract do 12-UI-SPEC.md a trava explicitamente como reuso, não como texto novo.

    Estender o bloco de import já existente de `@/lib/supabase/queries/dashboard`, acrescentando `getComparativoVendedor` e `type ComparativoVendedorRow` em ordem alfabética junto dos que já estão lá — NÃO criar um segundo `import ... from` para o mesmo módulo.

    A action ganha um JSDoc de uma linha citando VEND-01, registrando que não tem parâmetro de período e que a restrição de visibilidade ao Supervisor NÃO mora aqui: ela é uma decisão de montagem em `DashboardClient` (plano 04) apoiada na RLS, exatamente como já acontece com a action de desempenho por vendedor.

    NÃO criar valor novo em `DashboardErrorCode`: a action reusa `"unauthenticated" | "fetch_falhou"`. NÃO chamar `revalidatePath` (este arquivo inteiro é somente-leitura, como o comentário de topo registra). NÃO adicionar checagem de papel nem de vendedor ativo.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && node -e "const s=require('fs').readFileSync('app/actions/dashboard.ts','utf8'); for (const t of ['GetComparativoVendedorResult','getComparativoVendedorAction','getComparativoVendedor','ComparativoVendedorRow']) { if(!s.includes(t)) throw new Error('faltou: '+t) } const imports=(s.match(/from \"@\/lib\/supabase\/queries\/dashboard\"/g)||[]).length; if(imports!==1) throw new Error('deve haver exatamente 1 import do modulo de queries, achei '+imports); const codes=(s.match(/fetch_falhou/g)||[]).length; if(codes!==9) throw new Error('esperado 9 ocorrencias de fetch_falhou (1 na uniao de tipos + 7 actions antigas + 1 nova), achei '+codes); const msgs=(s.match(/Tente novamente\./g)||[]).length; if(msgs!==8) throw new Error('a mensagem de erro padrao nao foi reusada verbatim na action nova, achei '+msgs); const sess=(s.match(/Sess..o expirada\./g)||[]).length; if(sess!==8) throw new Error('o guard de sessao nao foi reusado verbatim na action nova, achei '+sess); if(s.includes('revalidatePath')) throw new Error('arquivo somente-leitura nao deve revalidar'); if(/isSupervisor|is_supervisor/.test(s)) throw new Error('a restricao ao Supervisor nao mora nesta camada'); console.log('OK action')" && npx vitest run tests/dashboard/comparativo-vendedor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/actions/dashboard.ts` exporta `GetComparativoVendedorResult` e `getComparativoVendedorAction`.
    - Existe exatamente UM `import ... from "@/lib/supabase/queries/dashboard"` no arquivo (o bloco existente foi estendido, não duplicado).
    - A action não recebe argumento, faz o guard de sessão com `"unauthenticated"` / `"Sessão expirada."` e devolve no catch o código `"fetch_falhou"` com a mensagem padrão do dashboard, sem inventar código nem texto de erro novo (contagens: 9 ocorrências de `fetch_falhou`, 8 de `Tente novamente.`, 8 de `Sessão expirada.`).
    - Nenhum `revalidatePath` e nenhuma checagem de papel ou de vendedor ativo no arquivo.
    - `npx tsc --noEmit` e `npm run lint` limpos.
    - `npx vitest run tests/dashboard/comparativo-vendedor.test.ts` continua verde (nenhuma regressão no backend do plano 01).
  </acceptance_criteria>
  <done>Server Action sem argumento adicionada, com a união {data}|{error} e as strings de erro reusadas verbatim; ponte pronta para o componente do plano 03.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (parte 2 de 4 — camada de acesso)

| Símbolo / Arquivo | Tipo | Status |
|-------------------|------|--------|
| `ComparativoVendedorRow` | Tipo exportado de `lib/supabase/queries/dashboard.ts` (`responsavel`, `responsavelNome`, `negociosIniciados`, `ganho`, `perdido`, `cicloMedioDias`, `taxaConversao`) | Criado (Task 1) |
| `getComparativoVendedor()` | Leitor tipado da RPC `dashboard_comparativo_vendedor`, sem argumento, com `Number()` nos bigint, `null` preservado no ciclo médio e `taxaConversao()` reusada (D-03) | Criado (Task 1) |
| `GetComparativoVendedorResult` | Tipo de resultado `{data}\|{error}` em `app/actions/dashboard.ts` | Criado (Task 2) |
| `getComparativoVendedorAction()` | Server Action sem argumento (VEND-01) | Criado (Task 2) |

Nenhum arquivo novo, nenhuma dependência npm nova, nenhum código de permissão novo.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → Server Action | Entrada de rede atravessa aqui, ainda que esta action não receba nenhum parâmetro. |
| Server Action → RPC do Postgres | A sessão do usuário é propagada ao banco, onde a RLS decide o escopo real. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06 | Spoofing / Broken Authentication | `getComparativoVendedorAction()` | high | mitigate | Guard `supabase.auth.getUser()` no início da action, devolvendo `{ code: "unauthenticated" }` sem chamar a RPC quando não há sessão — mesmo padrão das sete actions já existentes do arquivo. Verificado mecanicamente pela contagem exata de ocorrências do guard no check estrutural. |
| T-12-07 | Information Disclosure | escopo dos dados devolvidos pela action | high | mitigate | Nenhum filtro de autorização é escrito nesta camada de propósito: a action apenas repassa a chamada, e a RLS de `clientes`/`historico` (T-12-01, plano 01) faz o escopo. O check estrutural falha se `isSupervisor`/`is_supervisor` aparecer neste arquivo, para impedir que alguém mova a fronteira de autorização para um lugar contornável. |
| T-12-08 | Information Disclosure | vazamento de detalhe interno na mensagem de erro | low | mitigate | O `catch` descarta o erro real e devolve a mensagem genérica padrão do dashboard; a mensagem técnica do Postgres nunca chega ao navegador. Contagem exata da string padrão verificada no check estrutural. |
| T-12-09 | Elevation of Privilege | superfície de parâmetro | low | accept | A action não recebe nenhum argumento — não existe entrada do usuário para validar nem para injetar. |
| T-12-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` neste plano — só edição de dois arquivos existentes. |
</threat_model>

<verification>
Checks rodados pelo executor ao final do plano:
- `npx tsc --noEmit` e `npm run lint` limpos.
- Check estrutural de `lib/supabase/queries/dashboard.ts`: símbolos presentes, import único de `periodo`, `null` do ciclo médio checado antes de `Number()`, sem parâmetro de período, sem filtro de vendedor ativo.
- Check estrutural de `app/actions/dashboard.ts`: import único do módulo de queries, contagens exatas das strings de erro reusadas, sem `revalidatePath`, sem checagem de papel.
- `npx vitest run tests/dashboard/comparativo-vendedor.test.ts` verde (backend do plano 01 sem regressão).
</verification>

<success_criteria>
- VEND-01: `getComparativoVendedor()` devolve `ComparativoVendedorRow[]` com os números normalizados, o ciclo médio preservando `null` e a taxa de conversão calculada por reuso de `taxaConversao()` (D-03).
- VEND-01: `getComparativoVendedorAction()` existe, não recebe argumento, protege a sessão e devolve a união `{data}|{error}` com as mensagens padrão do dashboard.
- Nenhuma lógica de permissão ou de filtro de vendedor ativo foi movida para o TypeScript.
</success_criteria>

<output>
Create `.planning/phases/12-comparativo-por-vendedor/12-02-SUMMARY.md` when done.
</output>
