---
phase: 22-conclus-o-remota-com-motivo
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - app/actions/agenda.ts
  - app/(app)/agenda/page.tsx
  - components/agenda/ConcluirItemDialog.tsx
  - components/agenda/AgendaList.tsx
  - tests/agenda/concluir-item-dialog.test.tsx
  - tests/agenda/agenda-list.test.tsx
autonomous: false
requirements: [CONC-02, CONC-04, CONC-05]

must_haves:
  truths:
    - "CONC-02 (D-01): na janela única de conclusão, para tarefa de prospecção E para visita, o vendedor pode marcar que não foi presencial e escolher um motivo de uma lista fechada"
    - "CONC-02 (D-01): o resumo continua obrigatório do mesmo jeito nos dois casos — marcar 'não foi presencial' não afrouxa nada; motivo é a categoria, resumo é o texto livre"
    - "CONC-02: com a marcação ligada, o botão de confirmar fica indisponível enquanto nenhum motivo estiver escolhido"
    - "CONC-02 (D-06): o identificador do motivo chega às DUAS ações de servidor e às DUAS funções do banco — prospecção e visita — nunca só a uma"
    - "CONC-04 (D-07): a seção de próxima visita continua aparecendo e funcionando do mesmo jeito para visita de cliente com frequência, com ou sem a marcação de não-presencial ligada; as duas seções convivem, uma nunca substitui a outra"
    - "Uma conclusão presencial continua exatamente como hoje: nenhuma interação nova é exigida, nenhum campo novo obrigatório, e o texto que vai para o Diário é idêntico ao de antes"
    - "Desmarcar 'não foi presencial' depois de já ter escolhido um motivo limpa a escolha — nunca sobra motivo escondido numa conclusão marcada como presencial"
    - "CONC-03 (prova de ponta): o Vendedor (não só o Supervisor) vê a lista de motivos preenchida no campo de escolha"
  artifacts:
    - "app/actions/agenda.ts — as duas ações de conclusão com o parâmetro novo no fim, repassado à função correspondente do banco"
    - "app/(app)/agenda/page.tsx — a busca do catálogo de motivos ativos, ao lado das duas que a página já faz"
    - "components/agenda/ConcluirItemDialog.tsx — a marcação de não-presencial e o campo de escolha condicional"
    - "components/agenda/AgendaList.tsx — o catálogo repassado à janela e o identificador do motivo roteado para a ação certa conforme a origem do item"
    - "tests/agenda/concluir-item-dialog.test.tsx e tests/agenda/agenda-list.test.tsx — casos novos cobrindo presencial, remoto, desmarcar, convivência com a próxima visita, lista vazia e o roteamento para as duas origens"
  key_links:
    - "as duas ações de conclusão são funções SEPARADAS chamadas por uma janela ÚNICA. Estender só uma delas quebra metade da funcionalidade em silêncio — o vendedor testa concluir uma tarefa, dá certo, e a conclusão de visita falha só quando alguém tentar. As duas precisam ser alteradas juntas e as duas precisam ser exercitadas na mesma verificação"
    - "o campo de escolha só fica preenchido se a regra de leitura da tabela nova estiver aberta ao Vendedor. Uma lista vazia aqui NÃO é erro de tela — é o sintoma da falha silenciosa de permissão, e por isso a verificação humana precisa ser feita com conta de Vendedor, nunca só de Supervisor"
    - "o item de agenda não muda de forma nem ganha campo novo: o motivo é uma escolha feita no momento da conclusão, não um atributo do item — quem carrega o valor entre a janela e a ação é o parâmetro novo, e mais nada"
    - "o botão desabilitado da janela é cortesia de interface, nunca fronteira: a fronteira real é a guarda dentro da função do banco (plano 22-01), que recusa motivo inexistente ou desativado"
---

<objective>
Colocar a conclusão remota na mão do vendedor: uma marcação de "não foi presencial" na janela única de conclusão, um campo de escolha do motivo que só aparece quando ela está ligada, e o valor escolhido percorrendo o caminho inteiro até o banco pelas DUAS origens de item.

Purpose: os planos 22-01 e 22-02 deixaram o banco pronto e a lista administrável, mas nada disso é alcançável pela tela. Este plano fecha a fase e é o único que o usuário final enxerga. É também onde mora o risco travado da fase: a janela é uma só, mas as ações e as funções do banco são duas — mexer em metade do caminho quebra em silêncio.

Output: a janela de conclusão estendida, as duas ações e a página da Agenda fiadas, a cobertura de tela dos dois caminhos, e a verificação humana no navegador que prova a fase inteira de ponta a ponta com as duas contas (Supervisor e Vendedor).
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@components/agenda/ConcluirItemDialog.tsx
@components/agenda/AgendaList.tsx
@app/actions/agenda.ts
@app/(app)/agenda/page.tsx
@tests/agenda/concluir-item-dialog.test.tsx
</context>

<interface_context>
Superfície que este plano CONSOME (publicada pelos planos 22-01 e 22-02):

```sql
concluir_tarefa_prospeccao(p_tarefa_id uuid, p_resumo text, p_motivo_conclusao_remota_id uuid default null)
concluir_visita(p_visita_id uuid, p_resumo text, p_proxima_data date default null, p_motivo_conclusao_remota_id uuid default null)
```

```ts
getMotivosConclusaoRemotaAtivos(): Promise<LookupOption[]>   // { id, nome }[], so ativos, ordenado por nome
```

Nomes que ESTE plano estabelece — usados de forma idêntica nos quatro arquivos de origem para que a verificação mecânica consiga segui-los:

```ts
// app/actions/agenda.ts — parametro novo SEMPRE no fim, sempre opcional
concluirTarefaProspeccao(tarefaId, resumo, motivoConclusaoRemotaId?: string | null)
concluirVisita(visitaId, resumo, frequenciaVisita, proximaData, motivoConclusaoRemotaId?: string | null)

// components/agenda/ConcluirItemDialog.tsx
motivoOptions: { id: string; nome: string }[]
onConfirm: (resumo: string, proximaData: string | null, motivoConclusaoRemotaId: string | null) => ...

// components/agenda/AgendaList.tsx (propriedade nova, vinda da pagina)
motivoConclusaoRemotaOptions: { id: string; nome: string }[]
```

NÃO alterado por este plano: `lib/agenda/itens.ts` e o tipo do item de agenda (o motivo não é atributo do item), qualquer arquivo do calendário das Fases 20/21, `AgendaItemRow`, a ficha do cliente e o Diário (que já renderiza o texto vindo do banco sem caso especial).
</interface_context>

<source_audit>
A auditoria completa das quatro fontes para a Fase 22 está no plano `22-01-PLAN.md`, bloco `<source_audit>` — este plano não a repete. Itens que ESTE plano fecha:

| Item | Fonte | Situação neste plano |
|------|-------|----------------------|
| G1 — concluir tarefa de prospecção ou visita permite marcar "não foi presencial" + escolher motivo; resumo continua obrigatório | GOAL / crit. 1 | COBERTO (Tasks 1 e 2) |
| G2 (metade do consumo) — o Vendedor vê a lista preenchida no diálogo de conclusão | GOAL / crit. 2 | COBERTO (Task 2 + verificação humana ponto b) |
| G3 — conclusão remota vale como conclusão normal: sai da Agenda, entra no diário, e para visita ativa a próxima data continua sendo sugerida | GOAL / crit. 3 | COBERTO (Task 2 + verificação humana pontos c e e) |
| G4 — no Diário o motivo aparece em texto legível junto do resumo | GOAL / crit. 4 | COBERTO (verificação humana ponto c; a lógica em si é do 22-01) |
| CONC-02 — vendedor pode marcar que não foi presencial e escolher um motivo | REQ | COBERTO junto com o 22-01 |
| CONC-04 — conclusão remota conta como conclusão normal, inclusive a sugestão de próxima data | REQ | COBERTO junto com o 22-01 |
| CONC-05 — o motivo aparece como texto no Diário | REQ | COBERTO junto com o 22-01 (prova de ponta aqui) |
| Pitfall 3 — as duas funções/ações são separadas mas a janela é única; alterar só uma quebra metade em silêncio | RESEARCH/PITFALLS 3 | COBERTO (Task 1 com contagem mecânica + Task 2 com caso de roteamento + verificação humana ponto c nas duas origens) |
| Conflito resolvido nº 3 (22-01) — as opções vêm da busca da página, entregues como propriedade, não de uma busca dentro da janela | RESEARCH/ARCHITECTURE x leitura direta do código | COBERTO (Task 2) |

Este plano NÃO fecha sozinho nenhum dos três requisitos: a camada de banco de todos eles é do plano 22-01.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: As duas ações de conclusão repassam o motivo — juntas, nunca só uma</name>
  <files>app/actions/agenda.ts</files>
  <read_first>
    - `app/actions/agenda.ts` INTEIRO — as duas ações de conclusão (a de prospecção e a de visita), o comentário de topo que explica por que a atomicidade e a obrigatoriedade do resumo moram no banco e nunca aqui, a revalidação de rotas no fim de cada uma, e a união de códigos de erro.
    - `.planning/phases/22-conclus-o-remota-com-motivo/22-01-PLAN.md`, bloco `<interface_context>` — a ordem exata dos parâmetros das duas funções do banco depois da migration 0022.
    - `.planning/STATE.md` — Blockers/Concerns, item "RPCs gêmeas (Fase 22)": as duas funções são separadas e a janela é uma só; estender só uma quebra silenciosamente metade da funcionalidade.
  </read_first>
  <action>
    Acrescentar às DUAS ações de conclusão um parâmetro novo, opcional, SEMPRE no fim da lista, chamado `motivoConclusaoRemotaId` e do tipo "texto ou vazio". Na ação de prospecção ele entra depois do resumo; na de visita, depois da data de próxima visita. Nenhuma reordenação dos parâmetros existentes — as duas chamadas atuais em `AgendaList.tsx` continuam válidas até o Task 2 atualizá-las.

    Em cada ação, normalizar o valor antes de usar: texto vazio ou só espaços vira vazio de verdade. Repassar o resultado dessa normalização à função do banco correspondente, sob o nome de parâmetro publicado pelo plano 22-01. As duas chamadas ao banco ganham essa chave a mais no objeto de argumentos — e as duas ganham, não uma.

    O que NÃO fazer aqui, deliberadamente:
    - Não consultar a tabela de motivos para validar o identificador. A validação real (existe E está ativo) já vive dentro das funções do banco, criada no plano 22-01; repetir a consulta aqui seria uma segunda ida ao banco a cada conclusão, sem ganhar fronteira nenhuma.
    - Não criar código de erro novo. "Marcou não-presencial mas não escolheu motivo" não tem representação possível no que chega até aqui — o que chega é o identificador ou nada, e é justamente essa a razão de não existir indicador booleano separado (D-05). Se o banco recusar o identificador, a ação devolve o mesmo erro genérico de salvar que já devolve hoje, sem repassar a mensagem crua do banco para a tela.
    - Não mexer no bloco que decide se a data de próxima visita deve ou não ser enviada. Ele continua exatamente como está (D-07).
    - Não mexer na revalidação de rotas nem na revalidação do resumo.

    Atualizar o comentário de bloco de cada uma das duas ações para registrar o parâmetro novo em uma frase, e acrescentar ao comentário de topo do arquivo uma nota curta: a partir desta fase as duas conclusões carregam também o motivo de conclusão remota, e as DUAS precisam ser mantidas em sincronia porque a janela que as chama é uma só.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint app/actions/agenda.ts && node -e "const fs=require('fs');const a=fs.readFileSync('app/actions/agenda.ts','utf8');if((a.match(/p_motivo_conclusao_remota_id/g)||[]).length!==2)throw new Error('o motivo precisa chegar as DUAS chamadas ao banco, prospeccao E visita');if((a.match(/motivoConclusaoRemotaId/g)||[]).length<4)throw new Error('as duas acoes precisam declarar e usar o parametro novo');const p=a.indexOf('export async function concluirTarefaProspeccao');const v=a.indexOf('export async function concluirVisita');if(p<0||v<0)throw new Error('as duas acoes precisam continuar existindo com os mesmos nomes');if(!a.slice(p,v).includes('motivoConclusaoRemotaId'))throw new Error('a acao de prospeccao nao recebeu o parametro novo');if(!a.slice(v).includes('motivoConclusaoRemotaId'))throw new Error('a acao de visita nao recebeu o parametro novo');if(!/precisaDeData \? proximaData : null/.test(a))throw new Error('D-07: o bloco que decide o envio da proxima data nao pode mudar');console.log('OK acoes de conclusao')"</automated>
  </verify>
  <acceptance_criteria>
    - As duas ações de conclusão têm o parâmetro novo, opcional, no FIM da lista, com o mesmo nome nas duas.
    - As duas repassam o valor normalizado à função do banco correspondente — a chave do parâmetro do banco aparece exatamente duas vezes no arquivo, uma por ação.
    - Nenhum código de erro novo foi criado, nenhuma consulta nova ao banco foi acrescentada, e o bloco que decide o envio da data de próxima visita ficou intacto.
    - Os parâmetros existentes não foram reordenados.
    - `npx tsc --noEmit` e `npx eslint app/actions/agenda.ts` limpos.
  </acceptance_criteria>
  <done>O motivo tem caminho aberto da tela até o banco pelas duas origens de item, sem nenhuma fronteira nova inventada na camada intermediária.</done>
</task>

<task type="auto">
  <name>Task 2: Marcação "não foi presencial" na janela de conclusão, catálogo vindo da página e roteamento para as duas origens</name>
  <files>components/agenda/ConcluirItemDialog.tsx, components/agenda/AgendaList.tsx, app/(app)/agenda/page.tsx, tests/agenda/concluir-item-dialog.test.tsx, tests/agenda/agenda-list.test.tsx</files>
  <read_first>
    - `components/agenda/ConcluirItemDialog.tsx` INTEIRO — os cinco estados locais, o comentário de cabeçalho que explica ser a janela ÚNICA parametrizada por origem, o efeito que semeia a data sugerida na abertura, a função que limpa o estado ao fechar (e por que ela vive no manipulador de abertura e não num efeito), a revalidação do resumo antes de confirmar, a condição que desabilita o botão, e a seção condicional de próxima visita — que é o molde estrutural exato da seção nova.
    - `components/agenda/AgendaList.tsx` INTEIRO — as propriedades que a tela já recebe, o roteamento da confirmação para uma das duas ações conforme a origem do item, e a montagem da janela no fim do arquivo.
    - `app/(app)/agenda/page.tsx` INTEIRO — a busca conjunta dos dois catálogos que a página já faz e o repasse deles como propriedades.
    - `components/clientes/FiltersPopover.tsx` — um uso real da caixa de marcação já instalada no projeto (não instalar nada).
    - `components/clientes/PerdaMotivoDialog.tsx` — o precedente do projeto para "campo de escolha obrigatório com botão desabilitado até escolher", incluindo o comentário que registra que o botão desabilitado é cortesia e o banco é a fronteira real.
    - `tests/agenda/concluir-item-dialog.test.tsx` INTEIRO e `tests/agenda/agenda-list.test.tsx` (o começo) — os moldes de teste a estender, incluindo como o segundo simula o módulo de ações de servidor.
    - `.planning/STATE.md` — Blockers/Concerns e decisões: o campo de escolha da biblioteca de base exige a lista de itens também como propriedade do componente de escolha; e, em teste de navegador simulado, escolher um item que não seja o primeiro da lista exige disparar o evento de ponteiro antes do clique (achado registrado na Fase 20-05).
  </read_first>
  <action>
    **1. Página da Agenda.** Acrescentar a leitura de catálogo dos motivos ativos (publicada pelo plano 22-02) ao conjunto de buscas que a página já faz em paralelo, e repassar o resultado à tela como a propriedade nova publicada no bloco de interface. Uma linha em cada lugar; nenhuma outra mudança na página.

    **2. Janela de conclusão.** Acrescentar a propriedade nova com o catálogo de motivos e crescer a assinatura da função de confirmação em um terceiro argumento (o identificador do motivo ou vazio), conforme o bloco de interface.

    Dois estados locais novos: um indicador de "não foi presencial" e o identificador do motivo escolhido. Os dois entram na limpeza que já acontece quando a janela fecha — junto do resumo e da data — pelo mesmo caminho já usado (o manipulador de abertura, nunca um efeito reagindo ao fechamento).

    Seção nova na janela, posicionada DEPOIS do bloco de resumo e ANTES do bloco de próxima visita, e válida para as DUAS origens (não condicionar por origem — é a decisão D-01: vale para tarefa de prospecção e para visita):
    - uma caixa de marcação com rótulo "Não foi presencial", desmarcada por padrão, com um texto de apoio curto explicando que marcar não dispensa o resumo;
    - quando marcada, um campo de escolha rotulado "Motivo" alimentado pelo catálogo recebido, sem valor pré-selecionado;
    - quando marcada E o catálogo vier vazio, em vez do campo de escolha, uma mensagem explicando que ainda não há motivos cadastrados e que o supervisor precisa cadastrá-los em Configurações. Este caminho existe de propósito: lista vazia aqui é o sintoma visível de um problema de permissão de leitura, e é melhor mostrá-lo do que oferecer um campo de escolha mudo;
    - desmarcar a caixa limpa o motivo escolhido, sempre. Nunca pode sobrar identificador escolhido numa conclusão que o vendedor decidiu marcar como presencial.

    Condição de botão desabilitado: manter as três condições atuais e somar uma quarta — marcação ligada e nenhum motivo escolhido. Na função de confirmar, revalidar a mesma condição antes de chamar (mesma defesa em profundidade que a função já faz para o resumo e para a data), e passar como terceiro argumento o identificador escolhido quando a marcação estiver ligada, ou vazio quando não estiver — nunca o valor de estado cru sem passar por essa decisão.

    O bloco de próxima visita fica exatamente como está: mesma condição, mesma posição relativa, mesmo texto. As duas seções CONVIVEM — numa visita de cliente com frequência, marcar não-presencial não faz a seção de próxima data sumir nem virar opcional. É o critério CONC-04.

    Atualizar o comentário de cabeçalho do arquivo com um parágrafo curto sobre a seção nova, deixando registrado que ela vale para as duas origens e que o botão desabilitado é cortesia de interface — a fronteira real é a guarda dentro da função do banco (plano 22-01), que recusa motivo inexistente ou desativado.

    **3. Tela da Agenda.** Receber a propriedade nova do catálogo e repassá-la à janela. Crescer a função que trata a confirmação em um terceiro parâmetro (o identificador do motivo) e repassá-lo às DUAS ações — à de prospecção e à de visita, cada uma no fim da sua lista de argumentos. Este é o ponto exato em que o projeto já se avisou que é fácil errar: são dois pontos de chamada, não um.

    **4. Testes da janela** (estender `tests/agenda/concluir-item-dialog.test.tsx`; editar o auxiliar de renderização para passar a propriedade nova é esperado e permitido). Casos novos, com estas palavras-chave nos identificadores:
    - `presencial`: sem tocar na marcação, confirmar chama a função de confirmação com vazio no terceiro argumento, e nenhum campo de escolha de motivo está visível.
    - `remoto`: marcar faz o campo de escolha aparecer; com resumo válido e sem motivo escolhido o botão continua indisponível; escolhido o motivo, confirmar entrega o identificador no terceiro argumento.
    - `desmarcar`: marcar, escolher um motivo, desmarcar, confirmar — o terceiro argumento volta vazio.
    - `proximavisita`: origem de visita com frequência real e marcação ligada — a seção de próxima visita continua presente, e confirmar entrega data E motivo juntos.
    - `semlista`: marcação ligada com catálogo vazio mostra a mensagem de "nenhum motivo cadastrado" e mantém o botão indisponível.

    **5. Teste de roteamento** (estender `tests/agenda/agenda-list.test.tsx`; editar o auxiliar de renderização para a propriedade nova é esperado e permitido). Um caso novo com a palavra-chave `roteia`: com um item de prospecção, a confirmação com motivo chega à ação de prospecção com o identificador no fim; com um item de visita, chega à ação de visita com o identificador no fim. As duas metades no MESMO caso (ou dois casos, ambos com a palavra-chave) — o que não pode acontecer é só uma origem ser exercitada.

    Não editar nenhum dos quatro arquivos de teste de conclusão listados como prova de não-regressão no plano 22-01.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint app components && npx vitest run tests/agenda/concluir-item-dialog.test.tsx tests/agenda/agenda-list.test.tsx && node -e "const fs=require('fs');const d=fs.readFileSync('components/agenda/ConcluirItemDialog.tsx','utf8');if(!d.includes('motivoOptions'))throw new Error('a janela precisa receber o catalogo de motivos');if(!/components\/ui\/checkbox/.test(d))throw new Error('a caixa de marcacao precisa usar o primitivo ja instalado');if(!/components\/ui\/select/.test(d))throw new Error('o campo de escolha precisa usar o primitivo ja instalado');if((d.match(/motivoConclusaoRemotaId/g)||[]).length<2)throw new Error('o terceiro argumento da confirmacao precisa existir e ser usado');const cd=d.indexOf('confirmDisabled');if(cd<0||!/motivo/i.test(d.slice(cd,cd+260)))throw new Error('o botao precisa ficar indisponivel enquanto faltar motivo');if(!d.includes('temCadencia'))throw new Error('a secao de proxima visita nao pode ter sido removida');const l=fs.readFileSync('components/agenda/AgendaList.tsx','utf8');if(!l.includes('motivoConclusaoRemotaOptions'))throw new Error('a tela precisa receber e repassar o catalogo');if((l.match(/motivoConclusaoRemotaId/g)||[]).length!==3)throw new Error('o identificador precisa ser declarado uma vez e repassado as DUAS acoes');const pg=fs.readFileSync('app/(app)/agenda/page.tsx','utf8');if(!pg.includes('getMotivosConclusaoRemotaAtivos'))throw new Error('a pagina precisa buscar o catalogo');if(!pg.includes('motivoConclusaoRemotaOptions'))throw new Error('a pagina precisa repassar o catalogo para a tela');const t=fs.readFileSync('tests/agenda/concluir-item-dialog.test.tsx','utf8');for(const k of ['presencial','remoto','desmarcar','proximavisita','semlista'])if(!t.includes(k))throw new Error('falta o caso de janela '+k);const al=fs.readFileSync('tests/agenda/agenda-list.test.tsx','utf8');if(!al.includes('roteia'))throw new Error('falta o caso de roteamento das duas origens');const {execSync}=require('child_process');const diff=execSync('git diff --name-only HEAD -- tests/agenda/concluir-rpc.test.ts tests/agenda/rls-conclusao.test.ts tests/agenda/conclusao-validacao.test.ts tests/agenda/agenda-rpc.test.ts').toString().trim();if(diff)throw new Error('os testes de conclusao de banco nao podem ser editados: '+diff);console.log('OK tela de conclusao remota')"</automated>
  </verify>
  <acceptance_criteria>
    - A página da Agenda busca o catálogo de motivos ativos junto das buscas que já fazia e o repassa à tela.
    - A janela de conclusão tem a marcação "Não foi presencial" (desmarcada por padrão, válida para as duas origens), o campo de escolha condicional alimentado pelo catálogo, e a mensagem alternativa quando o catálogo vem vazio.
    - Desmarcar limpa o motivo escolhido; fechar a janela limpa os dois estados novos junto dos que já eram limpos.
    - O botão de confirmar fica indisponível enquanto a marcação estiver ligada sem motivo escolhido, e a mesma condição é revalidada antes de chamar.
    - A seção de próxima visita continua presente e funcional junto da seção nova, sem virar opcional.
    - A tela roteia o identificador do motivo para as DUAS ações conforme a origem (verificado mecanicamente pela contagem de três ocorrências: declaração + duas chamadas).
    - Cinco casos novos na suíte da janela e o caso de roteamento na suíte da tela, todos verdes.
    - Os quatro arquivos de teste de conclusão de banco continuam com zero diff.
    - `npx tsc --noEmit` e `npx eslint app components` limpos.
  </acceptance_criteria>
  <done>O vendedor consegue, na janela de sempre, marcar que não foi presencial e escolher o motivo, nas duas origens de item — e o valor escolhido chega ao banco pelo caminho certo em cada uma.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana no navegador — as duas contas, as duas origens, e a conclusão presencial intacta</name>
  <read_first>
    - `.planning/phases/22-conclus-o-remota-com-motivo/22-01-PLAN.md`, bloco `<interface_context>` — o formato exato do texto que deve aparecer no Diário (nome do motivo entre colchetes, espaço, resumo), que é o que o ponto (c) procura.
    - `.planning/STATE.md` — Blockers/Concerns: o limite conhecido de tentativas de login do Supabase Auth (evitar entrar e sair das contas mais vezes do que o necessário).
  </read_first>
  <action>Pausar a execução e conduzir o dono do projeto pelos cinco pontos abaixo, no navegador, com a aplicação rodando. Preparar o ambiente antes de pausar (subir a aplicação, deixar claro qual conta usar em cada etapa) para que ele só precise clicar. Não encerrar a fase sem o sinal explícito.</action>
  <what-built>
    A fase inteira está no ar: a lista nova, a aba de Configurações, a marcação na janela de concluir e o motivo aparecendo no Diário. O que falta é você ver funcionando com os seus próprios olhos, nas duas contas.

    Por que a verificação é com DUAS contas: a parte mais frágil desta fase não é a tela, é a permissão de leitura da lista nova. Se ela tivesse sido escrita errado, tudo funcionaria perfeitamente na sua conta de Supervisor e o campo de escolha apareceria eternamente vazio para o vendedor — sem mensagem de erro nenhuma. Este projeto já passou por exatamente isso uma vez. Por isso o ponto (b) é obrigatório com conta de Vendedor.

    Por que as DUAS origens: concluir uma tarefa de prospecção e concluir uma visita são duas operações diferentes por baixo, mesmo compartilhando a mesma janela. Testar só uma não prova a outra.
  </what-built>
  <how-to-verify>
    **(a) Supervisor administra a lista nova.** Entre como Supervisor, vá em Configurações e confirme que existe uma sexta aba, "Motivos de conclusão remota". Nela: acrescente um motivo novo qualquer (por exemplo "Pedido pelo representante"), renomeie um dos existentes e desative outro. Confirme que os três gestos funcionam igual às outras cinco abas e que o rótulo da aba nova cabe direito na barra sem quebrar o visual — se ficar apertado, diga; encurtar o rótulo é decisão sua, não do sistema.

    **(b) Vendedor vê a lista preenchida.** Entre como Vendedor (não como Supervisor). Vá na Agenda, clique em concluir qualquer item e marque "Não foi presencial". O campo de escolha do motivo PRECISA aparecer com opções — inclusive a que você acabou de criar no ponto (a), e SEM a que você desativou. Se ele aparecer vazio, pare aqui: é o problema de permissão descrito acima, não é detalhe visual.

    **(c) Concluir remotamente pelas duas origens.** Ainda como Vendedor, no mesmo dia:
    1. Conclua uma **tarefa de prospecção** com "Não foi presencial" marcado, um motivo escolhido e um resumo escrito. Confirme que o item saiu da Agenda.
    2. Conclua uma **visita** de um cliente ativo do mesmo jeito. Confirme que ela também saiu da Agenda.
    3. Abra a ficha de cada um desses dois clientes e olhe o Diário. As duas entradas novas precisam aparecer no formato `[Nome do motivo] seu resumo` — o nome do motivo por extenso, entre colchetes, seguido do que você escreveu. Se aparecer um código estranho de letras e números no lugar do nome, ou se o motivo não aparecer, pare: é problema real.

    **(d) Conclusão presencial continua igual.** Conclua mais um item SEM tocar na marcação — só resumo, como você sempre fez. Confirme três coisas: que nada novo foi exigido de você, que o item saiu da Agenda normalmente, e que a entrada no Diário desse cliente ficou só com o seu resumo, sem colchete nenhum.

    **(e) A próxima visita continua sendo sugerida na conclusão remota.** Volte na visita que você concluiu remotamente no ponto (c-2), do cliente ativo com frequência definida: confirme que, no momento em que você concluiu, a janela ofereceu a data da próxima visita normalmente (não sumiu por causa da marcação), e que agora existe uma visita nova agendada para esse cliente na Agenda. Este ponto é o mais importante da fase: conclusão remota não pode fazer o cliente ativo "sumir" do ciclo de visitas.
  </how-to-verify>
  <acceptance_criteria>
    - (a) A sexta aba existe e os três gestos (adicionar, renomear, desativar) funcionam como nas outras cinco.
    - (b) Com conta de VENDEDOR, o campo de escolha aparece preenchido, incluindo o motivo criado no ponto (a) e excluindo o desativado.
    - (c) Uma tarefa de prospecção E uma visita foram concluídas remotamente; as duas saíram da Agenda; e o Diário dos dois clientes mostra o nome do motivo por extenso entre colchetes seguido do resumo — nunca um identificador interno.
    - (d) Uma conclusão presencial não exigiu nenhuma interação nova e gerou entrada de Diário idêntica ao padrão de antes, sem colchetes.
    - (e) Na conclusão remota da visita, a data da próxima visita foi oferecida normalmente e a próxima visita existe na Agenda depois.
    - Qualquer ajuste levantado pelo dono foi tratado ANTES de encerrar a fase, não registrado como pendência.
  </acceptance_criteria>
  <resume-signal>Digite "approved" se os cinco pontos passaram, ou descreva exatamente o que você viu de diferente em cada ponto que falhou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → ação de servidor de conclusão | Ação de servidor é endpoint público; o identificador de motivo e o resumo vêm do navegador e não são confiáveis. |
| ação de servidor → função do banco | A guarda real (resumo entre 10 e 500, motivo existente e ativo, RLS sobre a linha) vive na função, criada no plano 22-01. |
| botão desabilitado da janela | NÃO é fronteira. É cortesia de interface, exatamente como o projeto já registrou no diálogo de motivo de perda. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-12 | Tampering | identificador de motivo forjado ou desativado | high | mitigate | A ação repassa sem confiar; a guarda dentro da função do banco (plano 22-01) recusa identificador inexistente ou desativado e derruba a conclusão inteira. Provado pelo caso `invalido` do plano 22-01. |
| T-22-13 | Denial of service (funcional) | uma das duas ações esquecida | critical | mitigate | Verificação mecânica exige o parâmetro do banco exatamente duas vezes na camada de ações e o identificador exatamente três vezes na tela (declaração + duas chamadas); caso de teste `roteia` exercita as duas origens; verificação humana ponto (c) conclui uma tarefa E uma visita. |
| T-22-14 | Information disclosure | mensagem crua do banco vazando para a tela | medium | mitigate | As duas ações continuam devolvendo a mensagem genérica de salvar já usada hoje; nenhuma mensagem do banco é repassada. Nenhuma linha nova de tratamento de erro foi criada. |
| T-22-15 | Spoofing | conclusão de item de outro vendedor | high | mitigate | Inalterado por este plano: a atualização dentro da função do banco continua sob a RLS, e item que o usuário não pode tocar simplesmente não muda linha nenhuma. Nenhuma checagem de dono foi escrita na tela nem na ação. |
| T-22-16 | Repudiation | motivo escondido numa conclusão marcada como presencial | medium | mitigate | Desmarcar limpa o motivo escolhido e a confirmação decide o terceiro argumento pela marcação, nunca pelo estado cru. Provado pelo caso `desmarcar`. |
| T-22-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` neste plano — a caixa de marcação e o campo de escolha já estão instalados no projeto. Sem checkpoint de legitimidade aplicável. |
</threat_model>

<verification>
Checks rodados pelo executor ao final do plano:
- Camada de ações: parâmetro novo no fim das duas, chave do parâmetro do banco exatamente duas vezes, bloco de decisão da próxima data intacto.
- Camada de tela: catálogo buscado na página e repassado; marcação e campo de escolha usando os primitivos já instalados; condição de botão desabilitado cobrindo a falta de motivo; seção de próxima visita preservada; identificador do motivo repassado às duas ações (contagem de três).
- Suítes: cinco casos novos na janela e o caso de roteamento na tela, todos verdes; os quatro arquivos de teste de conclusão de banco com zero diff.
- `npx tsc --noEmit` e `npx eslint app components` limpos.
- Checkpoint humano: os cinco pontos (a) a (e) aprovados no navegador, com conta de Supervisor E de Vendedor.
</verification>

<success_criteria>
- Na janela de sempre, o vendedor marca "não foi presencial" e escolhe um motivo — para tarefa de prospecção e para visita.
- O resumo continua obrigatório do mesmo jeito, e o botão só libera quando marcação e motivo estão coerentes.
- Concluir presencialmente não mudou em nada: nenhuma interação nova, mesma entrada de Diário de sempre.
- Concluir uma visita remotamente continua sugerindo e criando a próxima visita pela frequência do cliente.
- O Diário do cliente mostra o nome do motivo por extenso junto do resumo, nas duas origens.
- O Vendedor — não só o Supervisor — vê a lista de motivos preenchida.
</success_criteria>

<output>
Create `.planning/phases/22-conclus-o-remota-com-motivo/22-03-SUMMARY.md` when done
</output>
