---
phase: 22-conclus-o-remota-com-motivo
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - app/actions/listas.ts
  - components/configuracoes/ConfiguracoesTabs.tsx
  - lib/supabase/queries/clientes.ts
  - tests/configuracoes/configuracoes-tabs.test.tsx
  - tests/configuracoes/motivos-conclusao-remota-catalogo.test.ts
autonomous: true
requirements: [CONC-03]

must_haves:
  truths:
    - "CONC-03 (D-02/D-09): a tela de Configurações passa a ter SEIS abas; a sexta é 'Motivos de conclusão remota' e o Supervisor cadastra, renomeia e desativa valores nela exatamente com os mesmos gestos das outras cinco"
    - "CONC-03 (D-09): nenhuma lógica de CRUD nova foi escrita — a sexta aba reusa o mesmo componente genérico e as mesmas quatro ações genéricas das outras cinco; a única mudança nas ações é o nome da tabela nova entrar na união de nomes permitidos"
    - "D-04: a lista nova não tem nenhum tratamento especial em relação às outras cinco — sem validação de formato, sem trava em valor já usado, sem confirmação diferente ao desativar"
    - "Editar a lista pela tela de Configurações passa a atualizar também a tela da Agenda: as três ações de escrita de lista revalidam a rota da Agenda além das duas que já revalidavam"
    - "Existe uma leitura de catálogo dos motivos ATIVOS, ordenada por nome, no mesmo formato das leituras de catálogo já existentes (categorias/produtos/tipos de tarefa/motivos de perda) — é ela que o plano 22-03 usa para preencher o campo de escolha do vendedor"
    - "Um Vendedor logado consegue LER o catálogo de motivos ativos; um valor desativado some do catálogo"
  artifacts:
    - "app/actions/listas.ts — nome da tabela nova na união + revalidação da rota da Agenda nas três ações de escrita"
    - "components/configuracoes/ConfiguracoesTabs.tsx — a sexta entrada da barra de abas, com o painel mantido montado como as outras cinco"
    - "lib/supabase/queries/clientes.ts — a leitura de catálogo dos motivos ativos, irmã literal da leitura de motivos de perda"
    - "tests/configuracoes/configuracoes-tabs.test.tsx — prova de que as seis abas existem, na ordem certa, e que cada uma está ligada à sua própria tabela"
    - "tests/configuracoes/motivos-conclusao-remota-catalogo.test.ts — prova, contra o banco real e com sessão de Vendedor, do comportamento da leitura de catálogo (só ativos, ordem alfabética, colunas estreitas, efeito de desativar)"
  key_links:
    - "registrar uma lista editável nova exige TRÊS lugares e o TypeScript só cobra um deles sozinho: a migration (feita no 22-01), a união de nomes de tabela nas ações, e a aba. Esquecer a aba não quebra compilação nenhuma — a lista simplesmente fica invisível para o Supervisor, e o campo de escolha do vendedor fica preso nos cinco valores iniciais para sempre"
    - "o painel de cada aba PRECISA continuar sendo mantido montado. Sem isso a biblioteca de abas desmonta o painel inativo e cada clique refaz a busca do zero, com piscada de 'Carregando' que o teste manual já leu uma vez neste projeto como 'a aba não troca'"
    - "a leitura de catálogo tem que filtrar por ativo e selecionar só as duas colunas usadas — copiar a leitura de motivos de perda literalmente evita reinventar isso errado"
    - "sem a revalidação da rota da Agenda, o motivo recém-criado pelo Supervisor pode não aparecer no campo de escolha do vendedor até uma recarga completa da página — e é exatamente esse sintoma que a verificação humana do plano 22-03 procura"
---

<objective>
Fazer a lista de motivos de conclusão remota existir para as pessoas: uma sexta aba em Configurações para o Supervisor administrar, e uma leitura de catálogo para a tela de conclusão consumir no plano seguinte.

Purpose: a tabela nasceu no plano 22-01, mas uma tabela sem aba é invisível — o Supervisor não teria como acrescentar nem renomear motivo nenhum, e o time ficaria preso nos cinco valores iniciais da migration. Este é o plano mais barato da fase de propósito: todo o trabalho de CRUD já existe e é genérico; o que falta é registro em dois lugares e uma leitura de catálogo.

Output: a sexta aba funcionando com os mesmos gestos das outras cinco, a rota da Agenda entrando na revalidação das escritas de lista, e a leitura de catálogo publicada para o plano 22-03.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/actions/listas.ts
@components/configuracoes/ConfiguracoesTabs.tsx
@components/configuracoes/EditableListTab.tsx
@tests/configuracoes/frequencias-pedido-catalogo.test.ts
</context>

<interface_context>
Superfície que este plano CONSOME (publicada pelo 22-01, migration 0022):

```sql
motivos_conclusao_remota (id uuid, nome text unique, ativo boolean, created_at timestamptz)
-- leitura aberta a qualquer autenticado; escrita atras de is_supervisor()
```

Superfície que este plano PUBLICA. O plano 22-03 consome exatamente estes nomes:

```ts
// app/actions/listas.ts — a uniao ganha o sexto nome
type ListaTabela =
  | "categorias" | "produtos_consumidos" | "tipos_tarefa"
  | "motivos_perda" | "frequencias_pedido"
  | "motivos_conclusao_remota"

// lib/supabase/queries/clientes.ts — leitura de catalogo, irma literal de getMotivosPerdaAtivos
getMotivosConclusaoRemotaAtivos(): Promise<LookupOption[]>   // { id, nome }[], so ativos, ordenado por nome
```

NÃO alterado por este plano: o componente genérico de aba de lista editável (`EditableListTab`), as quatro ações genéricas de CRUD (só a união de nomes muda), e as cinco entradas de aba que já existem.
</interface_context>

<source_audit>
A auditoria completa das quatro fontes para a Fase 22 está no plano `22-01-PLAN.md`, bloco `<source_audit>` — este plano não a repete. Itens que ESTE plano fecha:

| Item | Fonte | Situação neste plano |
|------|-------|----------------------|
| G2 (metade da administração) — Supervisor cadastra/renomeia/desativa os motivos numa 6ª aba de Configurações, no mesmo formato das outras cinco | GOAL / crit. 2 | COBERTO (Task 1) |
| CONC-03 — motivos formam uma 6ª lista editável pelo Supervisor | REQ | COBERTO junto com o 22-01 (tabela + regras de acesso lá, aba e registro aqui) |
| R6 / D-09 — registrar a 6ª lista exige 3 lugares; o TypeScript só pega um sozinho; a aba precisa do painel mantido montado | RESEARCH/PITFALLS 6 + STATE | COBERTO (Task 1) |
| D-04 — sem validação de formato, sem trava de imutabilidade, sem tratamento especial em relação às outras cinco listas | CONTEXT (travada) | COBERTO (Task 1: nenhuma lógica própria, tudo genérico) |
| Preparação do consumo pela tela de conclusão — leitura de catálogo de ativos no molde das já existentes | RESEARCH/ARCHITECTURE + conflito resolvido nº 3 do 22-01 | COBERTO (Task 2) |

Este plano NÃO fecha sozinho o CONC-03: a tabela e as regras de acesso são do 22-01, e a prova de que o Vendedor vê a lista preenchida no diálogo é do 22-03.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Registrar a 6ª lista editável — união de nomes, aba de Configurações e revalidação da Agenda</name>
  <files>app/actions/listas.ts, components/configuracoes/ConfiguracoesTabs.tsx, tests/configuracoes/configuracoes-tabs.test.tsx</files>
  <read_first>
    - `app/actions/listas.ts` INTEIRO — a união de nomes de tabela no topo (com o comentário que hoje diz "as 5 tabelas"), e as três ações de escrita, cada uma terminando com o mesmo par de revalidações de rota.
    - `components/configuracoes/ConfiguracoesTabs.tsx` INTEIRO — o arranjo de entradas de aba (cada uma com nome de tabela, rótulo, texto de exemplo do campo e legenda de contagem no plural correto), o comentário que explica a ordem das abas, e o comentário longo que explica por que o painel precisa continuar montado. As duas últimas entradas ("Motivos de perda" e "Frequência de pedidos") são o molde direto.
    - `components/configuracoes/EditableListTab.tsx`, só o topo — as três propriedades que ele recebe e o efeito de busca na montagem que dispara a leitura por tabela (é o que o teste novo vai observar).
    - `tests/agenda/agenda-list.test.tsx`, só o começo — o precedente do projeto de simular um módulo de ações de servidor inteiro num teste de renderização em ambiente de navegador simulado.
  </read_first>
  <action>
    **1. União de nomes de tabela (`app/actions/listas.ts`).** Acrescentar `"motivos_conclusao_remota"` como sexta alternativa da união `ListaTabela`, no fim. Atualizar o comentário do bloco: hoje ele fala em "as 5 tabelas" e narra a chegada da quinta na Fase 16; passar a falar em seis e narrar a chegada da sexta nesta fase (motivo da conclusão remota, migration 0022), deixando explícito — como o comentário atual já faz para a quinta — que ela entra na mesma união genérica SEM nenhuma mudança nas quatro ações abaixo, porque a forma da tabela é idêntica.

    Verificar, sem alterar: as quatro ações genéricas (listar, criar, renomear, ativar/desativar) já são parametrizadas pelo nome da tabela e não precisam de nenhuma linha nova. Se alguma delas tiver qualquer condição escrita por nome de tabela específico, isso é um achado a registrar no resumo — mas não deve existir.

    **2. Revalidação da rota da Agenda.** Nas TRÊS ações de escrita (criar, renomear, ativar/desativar), acrescentar a revalidação da rota `/agenda` ao lado das duas que já existem. Comentar, uma vez só e no lugar mais alto que fizer sentido no arquivo, o porquê: a partir desta fase a tela da Agenda também consome uma dessas listas (o motivo de conclusão remota é buscado na página da Agenda e entregue ao diálogo de conclusão), então sem essa revalidação um motivo recém-criado pelo Supervisor pode continuar invisível para o vendedor até uma recarga completa. Aplicar às três, não só à de criar — renomear e desativar mudam o que o vendedor vê tanto quanto criar.

    **3. Sexta aba (`components/configuracoes/ConfiguracoesTabs.tsx`).** Acrescentar uma sexta entrada ao arranjo de abas, no FIM (depois de "Frequência de pedidos"), com: nome de tabela `motivos_conclusao_remota`; rótulo exatamente `Motivos de conclusão remota` (texto literal do requisito — decisão travada no conflito resolvido nº 5 do plano 22-01; não encurtar por conta própria); texto de exemplo do campo `Nome do motivo de conclusão remota` (deliberadamente diferente do usado pela aba de motivos de perda, para as duas não se confundirem); e legenda de contagem no plural `motivos de conclusão remota ativos`.

    Nada mais muda neste arquivo: a renderização já percorre o arranjo, o painel já é criado mantido montado para toda entrada, e o componente genérico já recebe as três propriedades. Atualizar o comentário do topo, que hoje descreve cinco abas e a ordem delas, para descrever seis e explicar por que a nova entra por último: ela não é atributo do cliente nem lista do funil, é vocabulário de COMO um item da Agenda foi concluído — a mais periférica das seis para o uso diário do Supervisor.

    **4. Teste de renderização (`tests/configuracoes/configuracoes-tabs.test.tsx`, arquivo novo).** Teste em ambiente de navegador simulado (o arquivo precisa ter extensão de componente para o ambiente correto ser escolhido pela configuração do Vitest). Simular o módulo inteiro de ações de lista, fazendo a ação de listar devolver uma lista vazia com sucesso, e as outras três nunca serem chamadas. Renderizar a barra de abas SEM nenhuma interação de clique (a biblioteca de abas tem quirks conhecidos de ponteiro no ambiente simulado neste projeto — clicar não é necessário para o que este teste precisa provar, já que o painel é mantido montado).

    Três casos, com estas palavras-chave nos identificadores:
    - `seisabas`: os seis rótulos aparecem, e o rótulo da aba nova é o texto literal do requisito.
    - `ordem`: os seis rótulos aparecem NA ORDEM esperada, com o novo por último — comparar a sequência de rótulos encontrados, não só a presença de cada um.
    - `ligada`: a ação de listar foi chamada uma vez para CADA um dos seis nomes de tabela — o que prova ao mesmo tempo que a aba nova está ligada à tabela certa e que o painel continua mantido montado (se não estivesse, só o painel ativo teria buscado).

    Não escrever caso nenhum sobre criar/renomear/desativar aqui: esse comportamento já é do componente genérico e já está provado; duplicá-lo por aba é ruído.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint app/actions/listas.ts components/configuracoes tests/configuracoes && npx vitest run tests/configuracoes/configuracoes-tabs.test.tsx && node -e "const fs=require('fs');const a=fs.readFileSync('app/actions/listas.ts','utf8');if(!/\|\s*\x22motivos_conclusao_remota\x22/.test(a))throw new Error('a uniao ListaTabela nao ganhou o sexto nome');if((a.match(/revalidatePath\(\x22\/agenda\x22\)/g)||[]).length!==3)throw new Error('as TRES acoes de escrita precisam revalidar a rota da Agenda');const c=fs.readFileSync('components/configuracoes/ConfiguracoesTabs.tsx','utf8');if((c.match(/tabela:/g)||[]).length!==6)throw new Error('a barra de abas precisa ter exatamente 6 entradas');if(!c.includes('motivos_conclusao_remota'))throw new Error('a aba nova nao aponta para a tabela nova');if(!/Motivos de conclus/.test(c))throw new Error('rotulo literal da aba nova ausente');if(!c.includes('keepMounted'))throw new Error('o painel precisa continuar mantido montado');const t=fs.readFileSync('tests/configuracoes/configuracoes-tabs.test.tsx','utf8');for(const k of ['seisabas','ordem','ligada'])if(!t.includes(k))throw new Error('falta o caso '+k);console.log('OK 6a lista registrada')"</automated>
  </verify>
  <acceptance_criteria>
    - `ListaTabela` tem exatamente seis alternativas, com o nome da tabela nova por último, e o comentário do bloco fala em seis listas.
    - As quatro ações genéricas de CRUD continuam sem nenhuma condição por nome de tabela específico (nenhuma linha de lógica nova foi escrita para a lista nova).
    - As três ações de escrita revalidam a rota da Agenda, além das duas rotas que já revalidavam.
    - O arranjo de abas tem exatamente seis entradas; a sexta aponta para a tabela nova, tem o rótulo literal do requisito, e o painel continua sendo criado mantido montado.
    - `tests/configuracoes/configuracoes-tabs.test.tsx` verde nos três casos, incluindo o que prova que as seis abas buscam cada uma a sua própria tabela.
    - `npx tsc --noEmit` e o lint dos arquivos tocados limpos.
  </acceptance_criteria>
  <done>O Supervisor tem uma sexta aba de Configurações funcionando com os mesmos gestos das outras cinco, e editar qualquer lista passa a refletir também na tela da Agenda.</done>
</task>

<task type="auto">
  <name>Task 2: Leitura de catálogo dos motivos ativos + prova contra o banco real</name>
  <files>lib/supabase/queries/clientes.ts, tests/configuracoes/motivos-conclusao-remota-catalogo.test.ts</files>
  <read_first>
    - `lib/supabase/queries/clientes.ts`, a região das leituras de catálogo (a de motivos de perda e as duas seguintes, de categorias e produtos) — a leitura nova é cópia literal dessa forma: cliente do servidor, seleção estreita de duas colunas, filtro de ativo, ordenação por nome, e retorno de lista vazia em caso de erro em vez de exceção. Copiar também o tipo de retorno já usado por essas leituras.
    - `tests/configuracoes/frequencias-pedido-catalogo.test.ts` INTEIRO — molde literal do arquivo de teste novo, incluindo a explicação de por que o teste exercita a MESMA consulta em vez de invocar a função diretamente (a função depende do contexto de requisição do Next e não pode ser chamada do Vitest), o login de cada identidade uma única vez, e o uso do cliente de serviço só para semear/limpar.
  </read_first>
  <action>
    **1. A leitura de catálogo.** Acrescentar `getMotivosConclusaoRemotaAtivos()` em `lib/supabase/queries/clientes.ts`, imediatamente ao lado da leitura de motivos de perda (é a irmã mais próxima), com a MESMA forma: sem argumentos, devolvendo a lista de valores ativos com identificador e nome, ordenada por nome, e devolvendo lista vazia se a consulta falhar. Nenhuma checagem de papel, nenhum filtro de dono: a regra de leitura da tabela (migration 0022) já é a fronteira, e ela é aberta a qualquer autenticado de propósito.

    Comentar, em duas frases: que esta leitura serve ao campo de escolha do diálogo de conclusão da Agenda (plano 22-03) e que ela filtra por ativo justamente porque um motivo desativado não deve mais ser oferecido — mas continua aparecendo no Diário de conclusões antigas, porque aquele texto é retrato congelado (D-04).

    Não criar ação de servidor nova para envolvê-la: quem chama é a página da Agenda, que já é Server Component e já chama duas leituras de catálogo desse mesmo jeito (decisão do conflito resolvido nº 3 do plano 22-01). A fiação em si é do plano 22-03.

    **2. Teste de catálogo (`tests/configuracoes/motivos-conclusao-remota-catalogo.test.ts`, arquivo novo).** Cópia estrutural do teste de catálogo de frequências: identidades logadas uma vez cada no bloco de preparação, cliente de serviço só para semear e limpar, gerador de nome único, limpeza no fim. Exercitar a MESMA consulta que a leitura nova faz (mesma tabela, mesmas duas colunas, mesmo filtro de ativo, mesma ordenação), não a função em si.

    Quatro casos, com estas palavras-chave nos identificadores:
    - `sementes`: os cinco valores iniciais da migration aparecem e estão ativos.
    - `vendedor`: uma sessão de VENDEDOR recebe o catálogo NÃO vazio — a mesma prova de fundo do plano 22-01, repetida aqui na forma exata em que a tela vai consumir.
    - `ordem`: o resultado vem em ordem alfabética por nome.
    - `desativado`: um valor semeado e depois desativado pelo Supervisor SOME do catálogo, enquanto continua existindo na tabela (releitura pelo cliente de serviço confirma que a linha não foi apagada, só desativada).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint lib/supabase/queries/clientes.ts tests/configuracoes && npx vitest run tests/configuracoes/motivos-conclusao-remota-catalogo.test.ts && node -e "const fs=require('fs');const q=fs.readFileSync('lib/supabase/queries/clientes.ts','utf8');if(!/export async function getMotivosConclusaoRemotaAtivos/.test(q))throw new Error('leitura de catalogo ausente');const i=q.indexOf('getMotivosConclusaoRemotaAtivos');const corpo=q.slice(i,i+700);if(!corpo.includes('motivos_conclusao_remota'))throw new Error('a leitura precisa consultar a tabela nova');if(!/eq\(\x22ativo\x22, true\)/.test(corpo))throw new Error('a leitura precisa filtrar por ativo');if(!/order\(\x22nome\x22/.test(corpo))throw new Error('a leitura precisa ordenar por nome');if(/is_supervisor|role/.test(corpo))throw new Error('nenhuma checagem de papel pode existir nesta leitura');const t=fs.readFileSync('tests/configuracoes/motivos-conclusao-remota-catalogo.test.ts','utf8');for(const k of ['sementes','vendedor','ordem','desativado'])if(!t.includes(k))throw new Error('falta o caso '+k);if((t.match(/signInAs\(/g)||[]).length>2)throw new Error('logar uma vez por identidade no bloco de preparacao');console.log('OK catalogo')"</automated>
  </verify>
  <acceptance_criteria>
    - `getMotivosConclusaoRemotaAtivos()` existe em `lib/supabase/queries/clientes.ts`, consulta a tabela nova, filtra por ativo, ordena por nome e devolve identificador e nome — sem nenhuma checagem de papel.
    - Nenhuma ação de servidor nova foi criada para envolvê-la.
    - `tests/configuracoes/motivos-conclusao-remota-catalogo.test.ts` verde nos quatro casos contra o banco real, incluindo o do Vendedor que recebe o catálogo não vazio e o do valor desativado que some sem ser apagado.
    - `npx tsc --noEmit` e o lint dos arquivos tocados limpos.
  </acceptance_criteria>
  <done>A tela de conclusão do plano 22-03 tem de onde tirar as opções de motivo, com o comportamento de "só ativos, em ordem" provado contra o banco real e com sessão de Vendedor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → ação de servidor de lista | As quatro ações genéricas de CRUD de lista são endpoints públicos; o nome da tabela vem do componente, mas o papel de quem chama vem da sessão. |
| RLS de `motivos_conclusao_remota` | Fronteira real de autorização das escritas (criada no plano 22-01). A checagem de Supervisor dentro das ações é defesa em profundidade, nunca a fronteira. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-09 | Elevation of privilege | ações genéricas de CRUD de lista | high | mitigate | Nenhuma linha nova de autorização é escrita: a lista nova entra na união e herda exatamente as mesmas checagens de Supervisor já existentes nas três ações de escrita, com a RLS da migration 0022 como fronteira real. Provado pelo caso `vendedorescreve` do plano 22-01. |
| T-22-10 | Tampering | nome de tabela vindo do componente | medium | mitigate | A união fechada de seis nomes literais continua sendo o único conjunto aceito pelas ações — nenhuma tabela arbitrária pode ser alcançada por essa via, e a verificação mecânica exige exatamente seis entradas na barra de abas. |
| T-22-11 | Information disclosure | leitura de catálogo | low | accept | O catálogo é vocabulário compartilhado do time, deliberadamente legível por qualquer autenticado (é o requisito CONC-03). Nenhum dado de cliente atravessa esta leitura. |
| T-22-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` neste plano — zero pacotes novos. Sem checkpoint de legitimidade aplicável. |
</threat_model>

<verification>
Checks rodados pelo executor ao final do plano:
- União de nomes com exatamente seis alternativas; barra de abas com exatamente seis entradas; painel mantido montado preservado.
- Revalidação da rota da Agenda presente nas três ações de escrita.
- Leitura de catálogo consultando a tabela nova, filtrando por ativo, ordenando por nome e sem nenhuma checagem de papel.
- `tests/configuracoes/configuracoes-tabs.test.tsx` verde (três casos) e `tests/configuracoes/motivos-conclusao-remota-catalogo.test.ts` verde (quatro casos, contra o banco real).
- `npx tsc --noEmit` e lint limpos nos arquivos tocados.
</verification>

<success_criteria>
- A tela de Configurações mostra seis abas; na sexta o Supervisor adiciona, renomeia e desativa motivos de conclusão remota com os mesmos gestos das outras cinco.
- Nenhuma lógica de CRUD nova foi escrita — só registro em dois lugares.
- Um valor desativado deixa de ser oferecido, sem ser apagado e sem afetar o histórico já gravado.
- A leitura de catálogo que a tela de conclusão vai consumir existe e está provada com sessão de Vendedor contra o banco real.
</success_criteria>

<output>
Create `.planning/phases/22-conclus-o-remota-com-motivo/22-02-SUMMARY.md` when done
</output>
