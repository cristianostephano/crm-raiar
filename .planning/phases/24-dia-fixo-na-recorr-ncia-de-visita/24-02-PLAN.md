---
phase: 24-dia-fixo-na-recorr-ncia-de-visita
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
autonomous: false
requirements: [ANCORA-01, ANCORA-02]
files_modified:
  - lib/funil/diaFixo.ts
  - tests/clientes/dia-fixo-vocabulario.test.ts
  - lib/supabase/queries/clientes.ts
  - app/actions/clientes.ts
  - components/clientes/ClienteDetailSheet.tsx

must_haves:
  truths:
    - "Na ficha de um cliente ativo com frequência semanal ou quinzenal, o vendedor escolhe o dia da semana logo abaixo do Select de frequência que já existe, e a escolha é salva na hora (ANCORA-01, D-02)."
    - "Com frequência mensal, aparecem DOIS campos — semana do mês (1ª/2ª/3ª/4ª/Última) e dia da semana — e os dois são salvos (ANCORA-02)."
    - "Escolher a frequência `Nenhuma` limpa o dia fixo gravado; trocar de mensal para semanal limpa a semana do mês, para o cliente nunca ficar com um resto de âncora que não se aplica."
    - "Enquanto o dia fixo não estiver completo, a ficha explica em uma linha que a próxima visita continua sendo sugerida pelo intervalo antigo (D-01)."
    - "A caixinha de marcar ganho continua exatamente como está — nenhum campo novo nela (GANHO-03 da Fase 23, e D-02 desta fase)."
    - "Uma próxima visita já marcada não muda quando o vendedor define o dia fixo agora (D-04)."
  artifacts:
    - lib/funil/diaFixo.ts
    - tests/clientes/dia-fixo-vocabulario.test.ts
  key_links:
    - "`lib/funil/diaFixo.ts` é a autoridade ÚNICA do vocabulário de dia fixo (rótulos, guards de tipo e a regra de quais campos de âncora se aplicam a cada frequência). Nem a ficha nem a ação de servidor podem repetir essa decisão com texto solto."
    - "A regra de quais campos de âncora valem para cada frequência é aplicada nos DOIS lados: na ficha, antes de enviar, e na ação de servidor, antes de gravar. Ação de servidor é endpoint público e nunca confia no que veio do navegador, mesmo tipado — é a mesma postura que `isFrequenciaVisita` já tem hoje."
    - "A ação continua sendo um UPDATE comum em `clientes`, nunca uma chamada para a RPC de mover card. A RPC existe para transição de etapa/status e reusá-la aqui reintroduziria guards que não fazem sentido numa edição de preferência."
    - "A ação não toca em `visitas` — é isso, e só isso, que entrega o D-04: a visita já marcada continua valendo, o dia fixo só rege a próxima."
    - "Os dois campos novos só aparecem dentro do bloco que já é condicionado ao cliente estar ganho. Fora dali o conceito não existe."
---

<objective>
Dar ao vendedor o lugar onde o dia fixo é definido: dois Selects novos na ficha do cliente, ao lado do Select de frequência de visita que já existe (D-02), salvando na hora e gravando nas duas colunas que a migration 0026 criou.

Purpose: a migration da Fase 24 já sabe calcular mirando o dia fixo, mas hoje nenhuma tela grava esse dia — todo cliente do sistema continuaria no cálculo antigo para sempre. Este plano é o que liga a capacidade nova a um humano.

Output:
- Módulo puro novo com o vocabulário de dia da semana e semana do mês (rótulos em português, guards de tipo, e a regra de quais campos valem para cada frequência), com teste unitário próprio.
- Leitura da ficha trazendo as duas colunas novas.
- Ação de servidor de frequência estendida para gravar também a âncora, revalidando o que veio do navegador.
- Ficha do cliente com os dois Selects condicionais e a linha de explicação enquanto o dia fixo não estiver completo.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.claude/CLAUDE.md
@.planning/phases/24-dia-fixo-na-recorr-ncia-de-visita/24-01-SUMMARY.md

Arquivos-fonte que o executor precisa ler antes de escrever qualquer linha:
@lib/funil/frequencia.ts
@app/actions/clientes.ts
@lib/supabase/queries/clientes.ts
@components/clientes/ClienteDetailSheet.tsx
@supabase/migrations/0026_dia_fixo_visita.sql
</context>

<decisoes_travadas>
- **D-02 (onde definir):** o dia fixo se define num ÚNICO lugar — ao lado do Select de frequência de visita que já existe na ficha do cliente. Nunca um formulário paralelo, nunca dentro da Agenda, nunca dentro da caixinha de ganho.
- **D-01 (fallback):** enquanto o dia fixo não existir, o cliente continua recebendo a sugestão pelo cálculo antigo. A tela precisa dizer isso em uma linha, em português simples, sem jargão.
- **D-04 (sem recálculo retroativo):** definir o dia fixo agora NÃO muda uma próxima visita já marcada. Este plano respeita isso por construção: a ação grava apenas colunas de `clientes` e nunca toca em `visitas`.
- **Layout (discrição do Claude, já decidida aqui):** os campos novos ficam ABAIXO do Select de frequência, dentro do mesmo bloco, um por linha — não ao lado em duas colunas. Motivo: o bloco já é estreito (fica dentro de um painel lateral) e o rótulo "Semana do mês" não caberia lado a lado sem truncar.

Fora de escopo, deliberadamente:
- `GanhoFrequenciaDialog.tsx` continua intocado. A caixinha de ganho pede CNPJ e frequência e não cresce (GANHO-03, travado na Fase 23). Um cliente recém-marcado como ganho fica sem dia fixo, cai no cálculo antigo (D-01) e aparece no aviso da Agenda (plano 24-03) — esse é o desenho, não uma lacuna.
- A seção nova da Agenda: plano 24-03.
- Qualquer planilha de importação em massa.
- Refatorar a supressão de lint já documentada perto do efeito de reset deste arquivo. Não piorá-la também: se algum setState novo precisar entrar naquele efeito, parar e reportar.
</decisoes_travadas>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Módulo puro do vocabulário de dia fixo + teste unitário</name>
  <files>lib/funil/diaFixo.ts, tests/clientes/dia-fixo-vocabulario.test.ts</files>
  <behavior>
    - Os sete dias da semana existem, na mesma ordem e com os mesmos rótulos internos do tipo enumerado da migration 0026 (começando no domingo), e cada um tem um rótulo de tela em português com acentuação correta.
    - As cinco semanas do mês existem, com rótulos de tela "1ª semana", "2ª semana", "3ª semana", "4ª semana" e "Última semana" — nunca uma quinta opção.
    - Os dois guards de tipo aceitam qualquer rótulo válido e recusam texto desconhecido, valor ausente, número e objeto.
    - A pergunta "esta frequência pede semana do mês?" responde verdadeiro só para mensal.
    - A normalização de âncora por frequência devolve: sem cadência (ausente ou `nenhuma`) → os dois campos vazios; semanal ou quinzenal → dia da semana preservado e semana do mês vazia; mensal → os dois preservados.
    - A pergunta "a âncora está completa?" responde verdadeiro quando não há cadência (nada a definir), verdadeiro para semanal/quinzenal com dia da semana, falso para semanal/quinzenal sem dia da semana, verdadeiro para mensal só quando os DOIS campos estão preenchidos.
  </behavior>
  <action>
Criar `lib/funil/diaFixo.ts`, espelhando a forma de `lib/funil/frequencia.ts` (lista `as const`, tipo derivado da lista, mapa de rótulos, lista de itens para o Select derivada do mapa, guard de tipo). O módulo é PURO: nada de `next/*`, nada de `@supabase/*`, nada de React — ele é importado tanto por Componente de Cliente quanto por Ação de Servidor, e qualquer dependência de servidor vazaria para o pacote do navegador.

Conteúdo:

1. **Dia da semana.** Lista com os sete rótulos internos exatamente iguais aos do tipo enumerado criado na migration 0026, começando no domingo. Tipo derivado. Mapa de rótulos de tela em português, com acentuação correta e a forma que o vendedor lê ("Segunda-feira", "Terça-feira", ... , "Sábado", "Domingo"). Lista de itens derivada do mapa. Guard de tipo no molde exato de `isFrequenciaVisita`.

2. **Semana do mês.** Lista com os cinco rótulos internos da migration 0026. Tipo derivado. Mapa de rótulos de tela: as quatro primeiras como número ordinal feminino seguido da palavra semana, e a última como "Última semana". Lista de itens. Guard de tipo.

3. **Uma pergunta só sobre semana do mês.** Função que responde se aquela frequência exige semana do mês — verdadeiro apenas para mensal. Esta é a única fonte dessa decisão no código de aplicação; a ficha e a ação de servidor chamam esta função em vez de comparar texto.

4. **Normalização de âncora por frequência.** Função que recebe frequência, dia da semana e semana do mês e devolve o par de valores que de fato deve ser gravado, aplicando três regras: sem cadência limpa os dois; semanal/quinzenal mantém o dia da semana e limpa a semana do mês; mensal mantém os dois. É esta função que impede um cliente semanal de carregar uma semana do mês vestigial de quando era mensal.

5. **Âncora completa.** Função que responde se o dia fixo está completamente definido para aquela frequência, conforme descrito no bloco de comportamento acima. É ela que a ficha usa para decidir se mostra a linha explicando que o cálculo antigo ainda vale.

**Reuso obrigatório, não duplicar:** a pergunta "esta frequência gera próxima visita?" já existe em `lib/funil/frequencia.ts` e é a mesma pergunta que "esta frequência pede dia da semana?". Importar e usar a função existente; NÃO escrever uma segunda função com o mesmo significado neste módulo. Registrar isso em comentário no cabeçalho.

Cabeçalho do módulo, em comentário, no estilo de `lib/funil/frequencia.ts`: que os rótulos internos precisam bater exatamente com os do tipo enumerado da migration 0026 (uma divergência de letra vira erro de tipo do Postgres em tempo de execução, não erro de compilação), e o aviso de que os rótulos `segunda` e `quarta` existem nas DUAS listas com significados diferentes — dia da semana numa, ordem da semana no mês na outra — então nenhuma função deste módulo pode receber um dos dois sem o tipo correspondente.

Criar `tests/clientes/dia-fixo-vocabulario.test.ts` cobrindo cada item do bloco de comportamento acima. Teste puro, ambiente padrão (sem DOM, sem banco), sem nenhuma autenticação — roda verde em qualquer máquina, sempre.

Escrever o teste ANTES do módulo, rodá-lo vermelho, e só então implementar até ficar verde.
  </action>
  <verify>
    <automated>npx vitest run tests/clientes/dia-fixo-vocabulario.test.ts</automated>
    <automated>test "$(grep -c 'next/\|@supabase/\|from \"react\"' lib/funil/diaFixo.ts)" = "0" &amp;&amp; echo OK</automated>
    <automated>test "$(grep -cE '^\s*it\(' tests/clientes/dia-fixo-vocabulario.test.ts)" -ge "8" &amp;&amp; echo OK</automated>
  </verify>
  <done>O módulo existe, é puro, exporta as duas listas com sete e cinco rótulos, os dois guards, a pergunta de semana do mês, a normalização por frequência e a checagem de âncora completa; o teste unitário passa e não usa banco nem DOM.</done>
</task>

<task type="auto">
  <name>Task 2: Leitura, gravação e os dois Selects na ficha do cliente</name>
  <files>lib/supabase/queries/clientes.ts, app/actions/clientes.ts, components/clientes/ClienteDetailSheet.tsx</files>
  <action>
Três arquivos, nesta ordem (o de baixo depende do de cima).

**1. `lib/supabase/queries/clientes.ts` — trazer as duas colunas novas.**
Acrescentar `dia_semana_visita` e `semana_do_mes_visita` à lista de colunas da leitura da ficha, ao tipo da linha crua e ao tipo do detalhe do cliente (nomes em camelo: dia da semana e semana do mês da visita), e ao mapeamento entre os dois. Ambas opcionais, podendo vir vazias — é o estado de TODO cliente hoje. Nenhuma outra leitura deste arquivo muda.

**2. `app/actions/clientes.ts` — a ação de frequência passa a gravar a âncora.**
Estender a ação existente que edita a frequência de visita, sem criar uma segunda ação. Dois parâmetros novos, SEMPRE no fim da lista e ambos com valor padrão vazio (convenção travada do projeto: parâmetro novo entra no fim, com padrão, para que nenhuma chamada existente quebre): dia da semana e semana do mês.

Ordem do corpo:
- as checagens que já existem (sessão e frequência válida) ficam onde estão, sem mudar;
- duas checagens novas logo depois: se o dia da semana veio preenchido e não é um rótulo válido, recusar com o mesmo código de erro de valor inválido que já existe; idem para a semana do mês. Ação de servidor é endpoint público — mesmo com tipagem, o que chega do navegador é revalidado, exatamente como a frequência já é hoje;
- normalizar o par de âncora chamando a função do módulo da Task 1, nunca decidindo aqui com comparação de texto;
- o UPDATE passa a gravar TRÊS colunas (frequência + as duas da âncora) em vez de uma, na mesma chamada;
- as duas saídas de erro já existentes (falha genérica e nenhuma linha afetada) ficam como estão. O comentário que explica que zero linhas significa que a regra de acesso do banco recusou a edição continua valendo palavra por palavra.

Acrescentar também a revalidação da rota da agenda ao lado da revalidação da rota de clientes que já existe: a partir do plano 24-03 a Agenda passa a listar quem está sem dia fixo, e gravar a âncora aqui muda aquela lista. Precedente direto: a Fase 22 acrescentou essa mesma revalidação às ações de lista quando a Agenda passou a consumir uma lista editável.

Atualizar o comentário de cabeçalho da ação registrando dois fatos: que ela continua sendo um UPDATE comum e nunca uma chamada à RPC de mover card; e que ela NÃO toca em `visitas` — é isso que entrega o D-04, a próxima visita já marcada continua exatamente como está e o dia fixo só rege a seguinte.

**3. `components/clientes/ClienteDetailSheet.tsx` — os dois Selects.**

Estrutura atual a preservar: dentro do bloco que só existe quando o cliente está ganho, há hoje um rótulo, um Select de frequência, e um parágrafo que mostra ou o erro ou o aviso de frequência não definida. Os campos novos entram DEPOIS desse parágrafo, dentro do MESMO bloco condicional — nunca fora dele.

Refatoração mínima do handler: hoje existe um handler que salva a frequência na hora. Extrair dele uma função única de salvamento que recebe os três valores (frequência, dia da semana, semana do mês), normaliza pelo módulo da Task 1, chama a ação e, no sucesso, atualiza os TRÊS campos no estado local do cliente. Os três handlers de tela (frequência, dia da semana, semana do mês) chamam essa função única — nenhum deles fala com a ação diretamente. Reusar o mesmo indicador de "salvando" e o mesmo estado de erro que a frequência já tem; não criar um segundo par.

Quando a frequência muda, a função de salvamento recebe os valores de âncora atuais do cliente e a normalização decide o que sobrevive — trocar para `Nenhuma` limpa os dois campos, trocar de mensal para semanal limpa a semana do mês. Nenhuma dessas regras é escrita aqui; todas vêm do módulo.

Campos novos:
- **Dia da semana** — aparece quando a frequência atual do cliente gera próxima visita (usar a função que já existe em `lib/funil/frequencia.ts`, nunca comparar texto). Mesmo formato visual do Select de frequência logo acima: rótulo, Select de largura cheia, desabilitado enquanto salva. Identificador do controle: `cliente-dia-semana-visita-select`. Texto de espera quando ainda não há valor: "Selecione o dia da semana".
- **Semana do mês** — aparece somente quando a função do módulo da Task 1 disser que a frequência exige. Mesmo formato. Identificador: `cliente-semana-do-mes-visita-select`. Texto de espera: "Selecione a semana do mês". Renderizado ACIMA do dia da semana, porque na frase que o vendedor pensa a ordem é "última semana, na quinta" e não o contrário.
- **Linha de explicação** — quando a âncora ainda não estiver completa (função do módulo da Task 1), mostrar em texto pequeno e discreto, no mesmo estilo do aviso de frequência não definida que já existe ali: que sem o dia fixo a próxima visita continua sendo sugerida contando os dias a partir da conclusão, e que definindo o dia fixo as próximas passam a cair sempre nele. Linguagem simples, sem jargão técnico e sem citar nome de função nem de coluna — quem lê não programa.

Proibições explícitas neste arquivo:
- não tocar em `GanhoFrequenciaDialog.tsx` nem no que a ficha passa para ele;
- não acrescentar setState ao efeito de reset que já carrega a supressão de lint documentada;
- não introduzir uma segunda maneira de decidir quais campos aparecem — as duas perguntas (gera próxima visita? exige semana do mês?) vêm de funções, sempre.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>test "$(grep -c 'dia_semana_visita' lib/supabase/queries/clientes.ts)" -ge "2" &amp;&amp; test "$(grep -c 'semana_do_mes_visita' lib/supabase/queries/clientes.ts)" -ge "2" &amp;&amp; echo OK</automated>
    <automated>test "$(grep -c 'isDiaSemanaVisita' app/actions/clientes.ts)" -ge "1" &amp;&amp; test "$(grep -c 'isSemanaDoMesVisita' app/actions/clientes.ts)" -ge "1" &amp;&amp; echo OK</automated>
    <automated>test "$(grep -c 'revalidatePath("/agenda")' app/actions/clientes.ts)" -ge "1" &amp;&amp; echo OK</automated>
    <automated>test "$(grep -c 'cliente-dia-semana-visita-select' components/clientes/ClienteDetailSheet.tsx)" = "1" &amp;&amp; test "$(grep -c 'cliente-semana-do-mes-visita-select' components/clientes/ClienteDetailSheet.tsx)" = "1" &amp;&amp; echo OK</automated>
    <automated>git diff --quiet HEAD -- components/clientes/GanhoFrequenciaDialog.tsx &amp;&amp; echo OK</automated>
    <automated>npx vitest run tests/clientes/dia-fixo-vocabulario.test.ts tests/agenda/agenda-calendario-integracao.test.tsx</automated>
    <automated>npx eslint lib/funil/diaFixo.ts app/actions/clientes.ts components/clientes/ClienteDetailSheet.tsx lib/supabase/queries/clientes.ts</automated>
  </verify>
  <done>A leitura da ficha traz as duas colunas, a ação grava as três colunas revalidando os dois valores novos, a ficha mostra os dois Selects condicionais com os identificadores acima, a caixinha de ganho está sem uma linha alterada, e a verificação de tipos e o lint passam limpos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Conferir os campos de dia fixo na ficha do cliente</name>
  <action>PAUSAR. Esta task não escreve código — o dono do projeto confere na tela, no navegador, os dez passos descritos abaixo, e responde com o resultado antes de o plano 24-03 começar.</action>
  <what-built>A ficha de um cliente ativo passou a ter, logo abaixo do Select de frequência de visita, os campos de dia fixo: dia da semana para semanal e quinzenal, e semana do mês + dia da semana para mensal. Tudo salva na hora, sem botão de salvar.</what-built>
  <how-to-verify>
1. Rodar o projeto (`npm run dev`) e entrar com uma conta que enxergue pelo menos um cliente ativo (status "ganho"). Se não houver nenhum, marcar um cliente como ganho pelo fluxo normal — vai pedir CNPJ e frequência, exatamente como antes.

2. Abrir a ficha desse cliente ativo e olhar o bloco de frequência de visita. Esperado: o Select de frequência que já existia, e abaixo dele o campo novo de dia da semana. Abaixo, uma linha discreta explicando que, sem o dia fixo, a próxima visita continua sendo sugerida contando os dias a partir da conclusão.

3. Escolher frequência **Semanal** e depois escolher **Quinta-feira** no dia da semana. Esperado: salva sozinho (sem botão), a linha de explicação some, e a semana do mês NÃO aparece.

4. Fechar a ficha e abrir de novo. Esperado: os dois valores continuam escolhidos — é isso que prova que gravou de verdade e não só na tela.

5. Trocar a frequência para **Mensal**. Esperado: aparece o campo de semana do mês, com as cinco opções (1ª, 2ª, 3ª, 4ª e Última semana) e NENHUMA quinta opção. O dia da semana continua em Quinta-feira. Enquanto a semana do mês não for escolhida, a linha de explicação volta a aparecer.

6. Escolher **Última semana**. Esperado: salva, a linha de explicação some.

7. Trocar a frequência de volta para **Semanal**. Fechar e reabrir a ficha. Esperado: a semana do mês sumiu da tela e não voltou "guardada" — o cliente não pode ficar com um resto de mensal.

8. Trocar a frequência para **Nenhuma**. Esperado: os dois campos de dia fixo somem. Reabrir a ficha e confirmar que continuam sumidos.

9. Marcar um cliente NOVO como ganho pelo fluxo normal. Esperado: a caixinha de ganho continua pedindo só CNPJ e frequência — nenhum campo de dia fixo apareceu ali. Isso é intencional: o dia fixo se define na ficha.

10. Só se houver um cliente ativo com visita já agendada: anotar a data da próxima visita dele na Agenda ANTES de definir o dia fixo, definir o dia fixo, e conferir na Agenda que aquela data NÃO mudou. O dia fixo só vale para a visita seguinte.
  </how-to-verify>
  <resume-signal>Responda "aprovado" ou descreva o que ficou diferente do esperado, indicando o número do passo.</resume-signal>
  <verify>
    <automated>npx vitest run tests/clientes/dia-fixo-vocabulario.test.ts</automated>
  </verify>
  <done>Os dez passos se comportam como descrito, com atenção especial aos passos 7 (nada de resto de mensal), 9 (caixinha de ganho intocada) e 10 (visita já marcada não muda).</done>
</task>

</tasks>

<source_audit>
A auditoria completa das quatro fontes da Fase 24 está no plano 24-01 (`<source_audit>`) e vale para os três planos. Itens cobertos especificamente por ESTE plano: G-SC1, G-SC2, REQ-1 (parte de tela), REQ-2 (parte de tela), RES-10, CTX-2, CTX-4.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → Ação de Servidor | A ação de frequência é um endpoint público: qualquer sessão autenticada pode chamá-la com qualquer valor, sem passar pela ficha |
| Ação de Servidor → Postgres | UPDATE comum em `clientes`; a RLS da tabela é a única fronteira de autorização |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-07 | Tampering | valor fora do vocabulário enviado à ação de frequência | medium | mitigate | Dois guards de tipo novos na ação, no molde do guard de frequência já existente, recusando antes de chegar ao banco; e o tipo enumerado do Postgres recusa de novo na fronteira do banco (defesa em profundidade) |
| T-24-08 | Elevation of Privilege | vendedor gravando âncora no cliente de outro | high | mitigate | A ação continua sem nenhuma checagem de papel própria: a policy de UPDATE de `clientes` decide, e zero linhas afetadas vira erro (falha fechada), exatamente como já acontece hoje com a frequência |
| T-24-09 | Tampering | âncora vestigial de uma frequência anterior | medium | mitigate | A normalização por frequência roda nos dois lados (tela e servidor) e é a única autoridade sobre quais campos sobrevivem a uma troca de frequência; provada por teste unitário |
| T-24-10 | Denial of Service | mudança retroativa de compromisso já combinado com o cliente | high | mitigate | A ação grava apenas colunas de `clientes` e nunca toca em `visitas` — nenhuma visita já agendada pode ser alterada por este caminho (D-04); confirmado no passo 10 do checkpoint |
| T-24-SC | Tampering | instalações npm/pip/cargo | low | accept | Este plano não instala nenhum pacote novo. Se o executor concluir que algum pacote é necessário, deve PARAR e reportar — instalação exige checkpoint humano bloqueante de legitimidade |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo e lint limpo nos quatro arquivos tocados.
- `components/clientes/GanhoFrequenciaDialog.tsx` sem uma linha alterada.
- Nenhum arquivo de migration tocado por este plano.
- O módulo novo não importa nada de servidor nem de React.
- Nenhuma segunda função com o significado de "esta frequência gera próxima visita" foi criada.
</verification>

<success_criteria>
1. Com frequência semanal ou quinzenal, a ficha oferece o dia da semana e grava a escolha.
2. Com frequência mensal, a ficha oferece semana do mês (cinco opções, nunca uma sexta) e dia da semana, e grava as duas.
3. Trocar de mensal para semanal, ou para nenhuma, limpa o que não se aplica mais.
4. Enquanto o dia fixo não está completo, a ficha explica em uma linha que o cálculo antigo ainda vale.
5. A caixinha de marcar ganho continua idêntica.
6. Uma visita já agendada não muda de data quando o dia fixo é definido.
</success_criteria>

<output>
Create `.planning/phases/24-dia-fixo-na-recorr-ncia-de-visita/24-02-SUMMARY.md` when done
</output>
