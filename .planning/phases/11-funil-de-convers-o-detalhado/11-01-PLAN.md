---
phase: 11-funil-de-convers-o-detalhado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/{proximo_numero_livre}_dashboard_funil_detalhado.sql
  - tests/dashboard/funil-detalhado.test.ts
  - tests/dashboard/rls-dashboard.test.ts
autonomous: false
requirements: [FNL-01, FNL-02, FNL-03]

must_haves:
  truths:
    - "FNL-01: a RPC dashboard_funil_detalhado() devolve exatamente 7 linhas (uma por valor de etapa_funil, mesmo com zero clientes), cada uma com quantidade, avancou_count, avancou_pct, perdidos_count, perdidos_pct, tempo_medio_dias e gargalo"
    - "FNL-01 (D-02): avancou_pct usa como denominador o total de clientes que JÁ PASSARAM pela etapa (inclui quem está parado nela agora) — quem está parado conta como 'ainda não avançou', nunca é excluído do denominador"
    - "FNL-01 (Pitfall 5): quantidade é a contagem histórica 'já entrou nesta etapa alguma vez', não o snapshot vivo do ClientesPorEtapaChart — é o mesmo denominador de avancou_pct/perdidos_pct"
    - "FNL-01: o tempo médio parado usa a cadeia COALESCE(próxima entrada, evento de fechamento ganho/perdido, now()) — now() é o ÚLTIMO recurso, só para quem ainda está em_andamento e genuinamente parado; um cliente já perdido para de acumular tempo no instante da perda (Pitfall 2)"
    - "FNL-01 (D-03): gargalo é TRUE quando tempo_medio_dias da etapa é maior que 1,5x a média das OUTRAS etapas — comparação relativa entre as 7 etapas, nunca um limite fixo de dias"
    - "FNL-02: a RPC dashboard_tempo_ate_fechamento() devolve a média de dias de clientes.criado_em até o evento de fechamento, separada por status ganho e perdido"
    - "FNL-03: as duas RPCs novas rodam como o usuário chamador (SECURITY INVOKER por omissão), então a RLS de clientes/historico já limita Vendedor aos próprios clientes e deixa Supervisor ver todos — sem nenhuma linha de código de permissão nova"
    - "As duas RPCs não têm NENHUM parâmetro (filtro de período está fora do escopo desta fase, por PROJECT.md)"
    - "A migration foi aplicada no projeto Supabase hospedado via supabase db push e os testes de integração provam o comportamento contra o banco real"
  artifacts:
    - "supabase/migrations/{proximo_numero_livre}_dashboard_funil_detalhado.sql (as duas RPCs novas, número derivado em tempo de execução)"
    - "dashboard_funil_detalhado() — RPC sem argumentos, returns table(etapa, quantidade, avancou_count, avancou_pct, perdidos_count, perdidos_pct, tempo_medio_dias, gargalo)"
    - "dashboard_tempo_ate_fechamento() — RPC sem argumentos, returns table(status text, media_dias numeric)"
    - "tests/dashboard/funil-detalhado.test.ts (novo — casos quantidade / ainda parado / perdido / gargalo / fechamento)"
    - "tests/dashboard/rls-dashboard.test.ts (estendido — as duas RPCs novas entram nas asserções cross-vendedor e supervisor-vê-tudo)"
  key_links:
    - "linha sintética de primeira entrada: (clientes.id, 'aguardando_contato', clientes.criado_em) UNION ALL as linhas de historico com tipo='etapa' — sem ela a primeira etapa fica invisível (Pitfall 1)"
    - "extração do valor da etapa via substring(descricao from '\"(.*)\"')::etapa_funil, nunca uma cadeia de 7 ilike (Pitfall 3)"
    - "ordem do funil vem da ordem de declaração do enum etapa_funil (comparação com o operador maior-que), nunca de um mapa ordinal escrito à mão (Pattern 2)"
    - "clientes.etapa_alterada_em NÃO é usado em lugar nenhum desta fase — só guarda o início da etapa atual, não o histórico"
    - "nenhuma cláusula de elevação de privilégio nas funções novas: é o único mecanismo que entrega FNL-03, e o teste de RLS estendido é o alarme que dispara se alguém adicionar uma no futuro"
---

<objective>
Criar a única peça de backend desta fase: uma migration nova com duas funções Postgres somente-leitura — `dashboard_funil_detalhado()` (FNL-01, incluindo o cálculo de "% avançou" do D-02 e a marcação de gargalo do D-03) e `dashboard_tempo_ate_fechamento()` (FNL-02) — mais os testes de integração que provam o comportamento e a visibilidade por papel (FNL-03) contra o banco real.

Purpose: nenhuma das duas seções novas do dashboard (a tabela por etapa e os dois cartões de tempo até fechamento) existe sem estes números. Toda a dificuldade técnica da fase está aqui: reconstruir "quanto tempo cada cliente ficou em cada etapa" a partir de um log de eventos (`historico`) que só registra MUDANÇAS, nunca durações. Os planos 02 a 05 são consumo puro do que este plano produz.

Output: uma migration aplicada em produção com aprovação humana, um arquivo de teste de integração novo e o teste de RLS existente estendido para cobrir as duas RPCs novas.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-funil-de-convers-o-detalhado/11-CONTEXT.md
@.planning/phases/11-funil-de-convers-o-detalhado/11-RESEARCH.md
@.planning/phases/11-funil-de-convers-o-detalhado/11-PATTERNS.md
@.planning/phases/11-funil-de-convers-o-detalhado/11-VALIDATION.md
@.claude/skills/Supabase-conventions/SKILL.md
@supabase/migrations/0002_clientes_and_funil.sql
@supabase/migrations/0003_dashboard_aggregates.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever a migration com as duas RPCs novas e criar/estender os testes de integração (RED)</name>
  <files>supabase/migrations/{proximo_numero_livre}_dashboard_funil_detalhado.sql, tests/dashboard/funil-detalhado.test.ts, tests/dashboard/rls-dashboard.test.ts</files>
  <read_first>
    - .planning/phases/11-funil-de-convers-o-detalhado/11-RESEARCH.md — seção "Code Examples" traz o SQL completo das duas funções, já sintetizado contra este schema; é o texto concreto a copiar, não só inspiração. Ler também "Architecture Patterns" (Pattern 1 linha sintética + LEAD, Pattern 2 ordenação nativa do enum, Pattern 3 COALESCE de três vias), "Anti-Patterns to Avoid" e os Pitfalls 1 a 6.
    - .planning/phases/11-funil-de-convers-o-detalhado/11-PATTERNS.md — bloco "supabase/migrations/00XX_dashboard_funil_detalhado.sql" (banner de cabeçalho a copiar, formato exato da assinatura sem argumentos, CTE de dedupe a reusar) e o bloco final sobre estender `tests/dashboard/rls-dashboard.test.ts`.
    - supabase/migrations/0003_dashboard_aggregates.sql — as cinco funções existentes: convenção de invoker por omissão, `dashboard_ganhos_perdidos` (CTE `ultimo_status_change` que a FNL-02 reaproveita), e os índices `idx_historico_tipo_criado` / `idx_clientes_criado_em` já criados no fim do arquivo.
    - supabase/migrations/0002_clientes_and_funil.sql — declaração do enum `etapa_funil` (linhas 28-36, a ordem do funil), tabela `historico` (linhas 159-166), a trigger `clientes_after_update_historico()` (linhas 412-445, formatos exatos de `descricao`: `Etapa alterada para "X"` e `Status alterado para "X"`), e as constraints `chk_ganho_somente_etapa_final` / `chk_perdido_exige_motivo` (os testes precisam respeitá-las).
    - tests/dashboard/ganhos-perdidos.test.ts — padrão de teste de comportamento desta pasta: `uniqueRazaoSocial`, `baseClienteFields`, `createdClienteIds` + `afterEach`, mutação via `serviceClient()` (para a trigger escrever `historico`), leitura via cliente logado.
    - tests/dashboard/rls-dashboard.test.ts — arquivo a ESTENDER (nunca duplicar): os dois `describe` existentes, os arrays `Promise.all([...])` de antes/depois, os helpers `toMap`/`toResponsavelMap`/`getUserId`/`testWindow`.
    - tests/helpers/supabase-test-clients.ts — `serviceClient()` / `signInAs()` e os nomes das variáveis de ambiente.
    - lib/funil/etapas.ts — ordem canônica das 7 etapas, que deve bater com a ordem do enum.
  </read_first>
  <action>
    Três entregas neste task.

    (A) DERIVAR O NÚMERO DA MIGRATION EM TEMPO DE EXECUÇÃO. Rodar `ls supabase/migrations/` como PRIMEIRA ação do task e usar o próximo número livre (quatro dígitos, com zeros à esquerda) no nome do arquivo novo: `supabase/migrations/{numero}_dashboard_funil_detalhado.sql`. NÃO usar nenhum número escrito neste plano nem em 11-RESEARCH.md/11-PATTERNS.md como literal — a Fase 10 tem uma migration própria que pode ou não ter aterrissado no `master` antes desta, então o número correto só é conhecido no momento da execução. O arquivo criado tem que ser o de maior número da pasta ao final do task.

    (B) Escrever a migration. Copiar primeiro o banner de cabeçalho de `0003_dashboard_aggregates.sql` (o bloco de comentário que registra que TODA função do arquivo roda como o usuário chamador por omissão de cláusula de elevação, e que adicionar essa cláusula quebraria silenciosamente a RLS), adaptando o texto para citar FNL-03 e os dois arquivos de teste desta fase como o alarme. Depois as duas funções, ambas `language sql` + `stable`, ambas SEM NENHUM argumento e SEM cláusula de elevação de privilégio:
      <!-- planner-discipline-allow: security definer -->
      <!-- planner-discipline-allow: p_inicio -->

      1. `dashboard_funil_detalhado()` — `returns table (etapa etapa_funil, quantidade bigint, avancou_count bigint, avancou_pct numeric, perdidos_count bigint, perdidos_pct numeric, tempo_medio_dias numeric, gargalo boolean)`. Corpo: copiar a query completa da seção "Code Examples" do 11-RESEARCH.md, com estas CTEs, nesta ordem e com estes nomes: `stage_events` (linha sintética `(c.id, 'aguardando_contato'::etapa_funil, c.criado_em)` vinda de `clientes`, UNION ALL com `historico` onde `tipo = 'etapa'`, extraindo a etapa via `substring(h.descricao from '"(.*)"')` com cast para `etapa_funil`); `stage_visits` (`lead(entrada) over (partition by cliente_id order by entrada)` como `proxima_entrada`); `status_events` e `ultimo_fechamento` (dedupe `distinct on (cliente_id)` cruzando com `clientes.status_acompanhamento` para confirmar que o evento é o fechamento vigente); `duracoes` (`saida = coalesce(v.proxima_entrada, f.criado_em, now())`, com o `left join ultimo_fechamento` guardado por `and v.proxima_entrada is null` para que só a ÚLTIMA visita em aberto de cada cliente consulte o evento de fechamento); `maior_etapa`; `etapas_base` (`unnest(enum_range(null::etapa_funil))`); `alcancados`; `avancados`; `perdidos_por_etapa`; `tempo_medio`; `resumo`; `media_outras`.

         Decisões travadas que o corpo tem que refletir exatamente:
         - `quantidade` vem de `alcancados` (contagem "já entrou nesta etapa alguma vez"), NÃO da contagem viva por etapa. Deixar um comentário SQL de uma linha explicando que é a base histórica e que é de propósito o mesmo denominador de `avancou_pct` e `perdidos_pct` (Pitfall 5). A CTE `quantidade_atual` do rascunho da research pode ser omitida — ela existe só como alternativa e ficaria sem consumidor.
         - `avancou_pct` = 100 * `avancou_count` / `quantidade`, arredondado a 1 casa, e NULL (não zero) quando `quantidade` é 0. Quem está parado na etapa entra no denominador e não no numerador — é literalmente o D-02.
         - `perdidos_pct` segue a mesma regra de arredondamento e de NULL.
         - `tempo_medio_dias` = média de `extract(epoch from (saida - entrada))` dividida por 86400, arredondada a 1 casa.
         - `gargalo` = TRUE quando `tempo_medio_dias` não é nulo, a média das OUTRAS etapas não é nula nem zero, e `tempo_medio_dias` é maior que essa média multiplicada por 1.5; envolvido em `coalesce(..., false)` para nunca devolver NULL. O multiplicador 1.5 é a fórmula do D-03 (comparação relativa entre as 7 etapas). Deixar um comentário SQL de uma linha registrando que é um valor deliberadamente ajustável e onde ele fica, para o dono poder pedir para afrouxar/apertar depois de ver a tabela com dados reais.
         - `order by r.etapa` no final, para as 7 linhas saírem sempre na ordem do funil.

      2. `dashboard_tempo_ate_fechamento()` — `returns table(status text, media_dias numeric)`. Copiar a segunda query da seção "Code Examples" do 11-RESEARCH.md: reusa a CTE `ultimo_status_change` de `dashboard_ganhos_perdidos` sem mudanças estruturais, trocando `count(*)` pela média de `extract(epoch from (u.criado_em - c.criado_em))` dividida por 86400, arredondada a 1 casa, agrupada por `status_evento`. O `join clientes c` aqui é necessário de verdade (precisa de `c.criado_em` e de `c.status_acompanhamento`), diferente das CTEs que só leem `historico` e já são limitadas pela RLS sem join (Pitfall 4).

      NÃO adicionar índice novo neste arquivo: `idx_historico_tipo_criado` e `idx_clientes_criado_em`, criados no `0003`, já cobrem os caminhos de acesso destas queries. NÃO criar tabela, view materializada, coluna ou trigger nova — a fase inteira é aditiva e somente-leitura.

    (C) Criar `tests/dashboard/funil-detalhado.test.ts` e estender `tests/dashboard/rls-dashboard.test.ts`. Os dois vão FALHAR (RED) até o push do Task 3, porque as funções ainda não existem no banco.

      `tests/dashboard/funil-detalhado.test.ts` (novo) — copiar os helpers de `tests/dashboard/ganhos-perdidos.test.ts` (`uniqueRazaoSocial`, `baseClienteFields`, `createdClienteIds` + `afterEach`, `getUserId`, `serviceClient`/`signInAs`, `SEED_ACCOUNTS`). Como as duas RPCs não têm filtro de período, a comparação antes/depois usada pelos testes existentes não serve para médias — a base precisa ser vazia e determinística. Por isso: adicionar um `beforeEach` que, via `serviceClient()`, apaga TODOS os `clientes` cujo `responsavel` é o id da conta semente `vendedorB` (conta de teste dedicada `@raiar.local`; `historico` cai junto por `on delete cascade`), e usar `vendedorB` como a identidade que LÊ as RPCs em todos os casos deste arquivo. Deixar um comentário no topo do arquivo explicando essa escolha de isolamento, já que ela difere do padrão de delta dos outros testes desta pasta. Toda mutação de etapa/status é feita via `serviceClient()` (é a trigger que escreve `historico`, nunca um insert direto em `historico`); para controlar durações, o teste pode ajustar `clientes.criado_em` e `historico.criado_em` via `serviceClient()` (nenhuma trigger dispara nessas colunas — a trigger só reage a mudanças de `etapa`/`status_acompanhamento`).

      Casos exigidos, com os nomes de teste contendo EXATAMENTE estas palavras (o mapa de comandos do 11-VALIDATION.md filtra por elas):
        - "quantidade": a RPC devolve 7 linhas, uma por etapa, mesmo com etapas de contagem zero; um cliente novo criado e movido até a 3ª etapa faz `quantidade` das etapas 1, 2 e 3 valer 1 cada (contagem "já passou por aqui"), e `avancou_pct` das etapas 1 e 2 valer 100 enquanto a 3ª vale 0 — porque ele está parado nela. Com base vazia, as etapas 4 a 7 têm `quantidade` 0 e `avancou_pct`/`perdidos_pct`/`tempo_medio_dias` nulos.
        - "ainda parado": um cliente em `em_andamento` que nunca avançou conta como NÃO avançado (entra no denominador de `avancou_pct` da etapa onde está, com numerador 0) e ainda assim tem `tempo_medio_dias` não nulo para essa etapa — provando que `now()` é usado como saída provisória (regra travada no ROADMAP). Backdatar `clientes.criado_em` em alguns dias via `serviceClient()` e conferir que `tempo_medio_dias` da primeira etapa fica próximo desse número de dias (tolerância de ~0,2 dia).
        - "perdido": um cliente marcado `perdido` enquanto estava na etapa N (respeitando `chk_perdido_exige_motivo` — buscar um `motivos_perda` ativo, como faz `ganhos-perdidos.test.ts`) aparece em `perdidos_count` da etapa N e em nenhuma outra; e o `tempo_medio_dias` dessa etapa PARA de crescer no instante da perda — provar backdatando o `historico.criado_em` do evento de perda e conferindo que o tempo medido bate com (evento de perda menos entrada na etapa), e não com (agora menos entrada na etapa).
        - "gargalo": montar duas etapas com tempos muito diferentes (uma com permanência de poucas horas, outra com permanência de dezenas de dias, via backdate dos timestamps) e conferir que a etapa lenta vem com `gargalo` true e a rápida com `gargalo` false — comparação relativa, sem nenhum limite fixo de dias no teste.
        - "fechamento": `dashboard_tempo_ate_fechamento()` devolve linhas separadas para `ganho` e `perdido`, cada `media_dias` calculada de `clientes.criado_em` até o respectivo evento de fechamento (backdatar os timestamps e conferir os dois valores esperados). Lembrar que `ganho` só é permitido em `primeira_venda` (`chk_ganho_somente_etapa_final`).

      `tests/dashboard/rls-dashboard.test.ts` (estender, NUNCA criar arquivo novo para isso): no primeiro `describe` (cross-vendedor), acrescentar `vendedorB.rpc("dashboard_funil_detalhado")` e `vendedorB.rpc("dashboard_tempo_ate_fechamento")` aos dois arrays `Promise.all([...])` (antes e depois) e às listas de checagem de `result.error`, e afirmar que o retorno de depois é igual ao de antes — o cliente criado e movido pelo Vendedor A não pode alterar NENHUM número visto pelo Vendedor B (comparar por igualdade profunda das linhas normalizadas, no mesmo espírito dos `toMap` já existentes). No segundo `describe` (supervisor), acrescentar uma asserção de que o Supervisor, chamando `dashboard_funil_detalhado()`, enxerga o cliente do Vendedor A (a `quantidade` da etapa dele cresce em relação a uma leitura feita antes da criação), enquanto o Vendedor B continua sem enxergar. As duas RPCs não recebem argumento nenhum, então as chamadas são mais simples que as existentes com janela de período.

    NÃO tocar em `lib/`, `app/` ou `components/` neste plano — são dos planos 02 a 05.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const fs=require('fs'); const dir='supabase/migrations'; const all=fs.readdirSync(dir).filter(n=>n.endsWith('.sql')).sort(); const mig=all.filter(n=>n.includes('funil_detalhado')); if(mig.length!==1) throw new Error('esperado exatamente 1 arquivo de migration funil_detalhado, achei '+mig.length); if(all[all.length-1]!==mig[0]) throw new Error('a migration nova nao e a de maior numero da pasta: '+mig[0]+' vs '+all[all.length-1]); const sql=fs.readFileSync(dir+'/'+mig[0],'utf8'); const body=sql.split('\n').filter(l=>!l.trim().startsWith('--')).join('\n').toLowerCase(); for (const t of ['dashboard_funil_detalhado','dashboard_tempo_ate_fechamento','stage_events','stage_visits','proxima_entrada','ultimo_fechamento','enum_range','tempo_medio_dias','gargalo','media_dias','language sql','stable']) { if(!body.includes(t)) throw new Error('faltou no SQL: '+t) } if(/security\s+definer/.test(body)) throw new Error('as RPCs novas NAO podem elevar privilegio - quebraria FNL-03'); if(/p_inicio|p_fim/.test(body)) throw new Error('as RPCs desta fase nao tem parametro de periodo'); if(/etapa_alterada_em/.test(body)) throw new Error('etapa_alterada_em nao pode ser usado nesta fase'); const nt=fs.readFileSync('tests/dashboard/funil-detalhado.test.ts','utf8'); for (const t of ['quantidade','ainda parado','perdido','gargalo','fechamento']) { if(!nt.includes(t)) throw new Error('caso de teste ausente: '+t) } const rls=fs.readFileSync('tests/dashboard/rls-dashboard.test.ts','utf8'); for (const t of ['dashboard_funil_detalhado','dashboard_tempo_ate_fechamento']) { if(!rls.includes(t)) throw new Error('rls-dashboard.test.ts nao cobre '+t) } console.log('OK '+mig[0])"</automated>
  </verify>
  <acceptance_criteria>
    - Existe exatamente um arquivo `supabase/migrations/*_dashboard_funil_detalhado.sql` e ele é o de MAIOR número da pasta (número derivado por `ls` no momento da execução, não copiado deste plano).
    - O corpo SQL (ignorando linhas de comentário) contém `dashboard_funil_detalhado`, `dashboard_tempo_ate_fechamento`, as CTEs `stage_events`, `stage_visits`, `ultimo_fechamento`, a coluna `proxima_entrada`, `enum_range`, e as colunas de saída `tempo_medio_dias`, `gargalo`, `media_dias`.
    - O corpo SQL (ignorando comentários) NÃO contém nenhuma cláusula de elevação de privilégio, nenhum parâmetro de período, e nenhuma referência a `etapa_alterada_em`.
    - As duas funções são `language sql` + `stable` e declaradas com lista de parâmetros vazia.
    - `gargalo` é calculado comparando `tempo_medio_dias` da etapa com 1,5x a média das outras etapas, com guarda de nulo/zero e `coalesce(..., false)`.
    - `quantidade` vem da CTE de "já entrou nesta etapa" e é o mesmo denominador de `avancou_pct`/`perdidos_pct`; ambos os percentuais são NULL (não 0) quando `quantidade` é 0.
    - `tests/dashboard/funil-detalhado.test.ts` existe, tem um `beforeEach` que zera os clientes do `vendedorB` via `serviceClient()`, e tem testes cujos nomes contêm "quantidade", "ainda parado", "perdido", "gargalo" e "fechamento".
    - `tests/dashboard/rls-dashboard.test.ts` foi estendido (não duplicado) e cita as duas RPCs novas.
    - `npx tsc --noEmit` limpo.
    - Os testes ainda FALHAM em runtime (RED) neste ponto — esperado até o Task 3.
  </acceptance_criteria>
  <done>Migration com as duas RPCs escrita (número derivado em execução), teste de comportamento novo criado e teste de RLS estendido; tudo ainda RED, aguardando o push.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Aprovação humana — aplicar a migration no banco Supabase de produção</name>
  <action>Pausar a execução. É a etapa de revisão antes de produção exigida pelo CLAUDE.md ("não fazer deploy direto em produção sem passar por uma etapa de revisão") e a prática já estabelecida nas Fases 9 e 10 para toda migration que muda o schema. Apresentar ao dono do projeto, em linguagem simples, o que a migration faz no banco real e só rodar o `supabase db push` (Task 3) depois do sinal "approved". Não prosseguir sem aprovação.</action>
  <what-built>
    Foi escrita uma migration nova que adiciona DUAS "contas prontas" ao seu banco de dados — nada mais. Elas não criam, apagam nem mudam nenhum dado: só leem o que já existe e devolvem números.

    1. A primeira devolve, para cada uma das 7 etapas do funil: quantos clientes já passaram por ela, qual percentual desses já avançou para a etapa seguinte, quantos foram perdidos ali (número e percentual), quantos dias em média as pessoas ficam paradas nessa etapa, e uma marca de "gargalo" quando uma etapa demora bem mais que as outras.
    2. A segunda devolve dois números: a média de dias entre cadastrar um cliente e ele virar "ganho", e a média de dias até ele virar "perdido".

    Um detalhe que vale saber: o sistema nunca guardou "quanto tempo cada cliente ficou em cada etapa". Essa informação está sendo RECONSTRUÍDA a partir do histórico de mudanças que o sistema já registra desde o começo. É a primeira vez que essa reconstrução roda contra os seus dados de verdade — por isso a revisão antes de aplicar.

    Continua valendo a mesma regra de sempre: vendedor só vê os próprios clientes nesses números, supervisor vê os de todo o time. Isso não foi programado de novo — as funções novas rodam "no crachá" de quem pergunta, então o banco já filtra sozinho.
  </what-built>
  <how-to-verify>
    1. Confirme que você está logado no Supabase CLI e com o projeto linkado (mesmo fluxo das migrations anteriores; se `supabase db push` pedir a senha do banco, é normal — tenha ela à mão).
    2. Se quiser conferir o arquivo: abra a migration nova em `supabase/migrations/` (é a de número mais alto, terminando em `_dashboard_funil_detalhado.sql`) e confirme que só existem dois `create or replace function` e nenhum `drop`, `alter table` ou `delete`.
    3. Ponto que merece seu olhar: a regra de "gargalo" está travada em "a etapa demora mais que 1,5x a média das outras". Depois que você vir a tabela com dados reais, se ela marcar etapas demais ou de menos, esse número é fácil de ajustar — é só pedir.
  </how-to-verify>
  <resume-signal>Digite "approved" para autorizar o `supabase db push` no banco de produção, ou descreva qualquer dúvida/ajuste antes de aplicar.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Aplicar a migration no banco hospedado (supabase db push) e levar os testes a GREEN</name>
  <files>tests/dashboard/funil-detalhado.test.ts, tests/dashboard/rls-dashboard.test.ts</files>
  <read_first>
    - o arquivo de migration criado no Task 1 (o que será aplicado)
    - tests/helpers/supabase-test-clients.ts — conferir que `.env.local` tem `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` e `SUPABASE_SERVICE_ROLE_KEY` apontando para o projeto hospedado
    - .planning/STATE.md — Blockers/Concerns: rate-limit conhecido do `signInWithPassword` no projeto de teste ao vivo (evitar logins repetidos desnecessários dentro do mesmo arquivo de teste)
  </read_first>
  <action>
    Rodar `supabase db push` para aplicar a migration no projeto Supabase hospedado (mesmo fluxo das migrations 0002 a 0007). Se falhar por autenticação ou link, criar dinamicamente um gate de auth (pedir ao dono que rode `supabase login` / `supabase link` e forneça a senha do banco) e repetir — NÃO seguir adiante sem o push concluído, porque este é o pré-requisito bloqueante de todos os planos seguintes desta fase (nada consegue chamar as RPCs antes disso).

    Se o push acusar colisão de numeração (outra migration com o mesmo prefixo, cenário da Fase 10), renomear o arquivo desta fase para o próximo número livre real e repetir o push — nunca renomear ou editar uma migration já aplicada.

    Depois do push, rodar os dois arquivos de teste contra o banco real e levá-los a GREEN. Se algum caso falhar, diagnosticar contra a query aplicada antes de mexer no teste: os suspeitos prováveis, em ordem, são (1) a linha sintética de primeira entrada faltando, o que derruba a contagem da etapa `aguardando_contato`; (2) a cadeia COALESCE caindo em `now()` para clientes já fechados, o que infla o tempo médio; (3) o guard `and v.proxima_entrada is null` ausente no join com `ultimo_fechamento`, o que faz visitas antigas consultarem o evento de fechamento indevidamente. Corrigir a MIGRATION criando uma nova (ou, se ainda não tiver sido aplicada com sucesso, editando a mesma antes do push bem-sucedido) — nunca alterar um arquivo de migration já aplicado no banco.

    Este task não altera código de aplicação: só aplica a migration e valida.
  </action>
  <verify>
    <automated>npx vitest run tests/dashboard/funil-detalhado.test.ts tests/dashboard/rls-dashboard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` aplicou a migration no projeto hospedado sem erro; `dashboard_funil_detalhado()` e `dashboard_tempo_ate_fechamento()` existem no banco real.
    - `npx vitest run tests/dashboard/funil-detalhado.test.ts` passa (GREEN): os casos "quantidade", "ainda parado", "perdido", "gargalo" e "fechamento" todos verdes.
    - `npx vitest run tests/dashboard/rls-dashboard.test.ts` passa (GREEN): Vendedor B não enxerga nada do Vendedor A nas duas RPCs novas, e o Supervisor enxerga.
    - `npm test` (suíte completa) continua verde — nenhuma regressão nos testes de dashboard existentes.
    - Nenhum arquivo de migration já aplicado anteriormente foi editado.
  </acceptance_criteria>
  <done>Migration aplicada em produção com aprovação humana; as duas RPCs novas provadas por teste de integração contra o banco real, incluindo o isolamento por papel (FNL-03).</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (parte 1 de 5 — camada de banco)

| Símbolo / Arquivo | Tipo | Status |
|-------------------|------|--------|
| `supabase/migrations/{numero_derivado}_dashboard_funil_detalhado.sql` | Migration nova (aditiva, somente-leitura) | Criada (Task 1), aplicada (Task 3) |
| `dashboard_funil_detalhado()` | RPC sem argumentos, `language sql stable`, invoker; devolve 7 linhas com etapa, quantidade, avancou_count, avancou_pct, perdidos_count, perdidos_pct, tempo_medio_dias, gargalo | Criada |
| `dashboard_tempo_ate_fechamento()` | RPC sem argumentos, `language sql stable`, invoker; devolve `status` + `media_dias` para ganho e perdido | Criada |
| `tests/dashboard/funil-detalhado.test.ts` | Teste de integração novo (quantidade / ainda parado / perdido / gargalo / fechamento) | Criado (Task 1), GREEN (Task 3) |
| `tests/dashboard/rls-dashboard.test.ts` | Teste existente ESTENDIDO com as duas RPCs novas (FNL-03) | Estendido (Task 1), GREEN (Task 3) |

Nenhuma tabela, view, coluna, trigger, índice ou dependência npm nova.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário autenticado (Vendedor) → RPCs de dashboard | Qualquer usuário logado pode chamar as duas funções novas; o que ele recebe de volta tem que estar limitado aos clientes que a RLS já deixa ele ver. |
| `historico.descricao` (escrito por trigger) → regex de extração dentro da RPC | Um texto vindo do banco é interpretado por `substring(... from '"(.*)"')` e convertido para enum. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Information Disclosure / Elevation of Privilege | `dashboard_funil_detalhado()` e `dashboard_tempo_ate_fechamento()` | critical | mitigate | Ambas declaradas SEM cláusula de elevação de privilégio (invoker por omissão, convenção do `0003`), de modo que toda leitura interna de `clientes`/`historico` é filtrada pelas policies de RLS de `0001`/`0002`. Zero código de permissão novo. Regressão futura é pega pelo `tests/dashboard/rls-dashboard.test.ts` estendido, que falha alto se o Vendedor B passar a enxergar dado do Vendedor A. |
| T-11-02 | Tampering (injeção via dado) | extração de etapa/status a partir de `historico.descricao` | low | accept | `descricao` nunca é fornecido pelo usuário: é escrito exclusivamente pelas triggers de `0002` via `format()` sobre um cast de enum. As duas RPCs não recebem parâmetro nenhum, então não há superfície de injeção pelo chamador. O cast final para `etapa_funil` rejeitaria qualquer valor fora do enum com erro, nunca com leitura silenciosa errada. |
| T-11-03 | Denial of Service | reconstrução com window function sobre todo o `historico` a cada carga do dashboard | low | accept | Volume do free tier é de centenas de clientes; `idx_historico_tipo_criado` e `idx_clientes_criado_em` (criados em `0003`) já cobrem os caminhos de acesso. Função `stable`, sem escrita. Se algum dia doer, a alternativa (view materializada) está registrada em 11-RESEARCH.md "Alternatives Considered". |
| T-11-04 | Tampering | migration aplicada direto em produção sem revisão | medium | mitigate | Checkpoint humano bloqueante (Task 2) antes do `supabase db push`, exigido pelo CLAUDE.md e já praticado nas Fases 9 e 10. A migration é puramente aditiva (dois `create or replace function`), sem `drop`/`alter table`/`delete`. |
| T-11-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` nesta fase — 11-RESEARCH.md "Package Legitimacy Audit" registra zero pacotes novos. Sem checkpoint de legitimidade aplicável. |
</threat_model>

<verification>
Checks rodados pelo executor ao final do plano:
- `npx tsc --noEmit` limpo.
- Check estrutural do SQL (Task 1): presença das CTEs e colunas esperadas; ausência de cláusula de elevação de privilégio, de parâmetro de período e de `etapa_alterada_em` no corpo sem comentários; a migration nova é a de maior número da pasta.
- `npx vitest run tests/dashboard/funil-detalhado.test.ts tests/dashboard/rls-dashboard.test.ts` verde contra o banco real, pós-push.
- `npm test` (suíte completa) verde.
- Checkpoint humano (Task 2): dono aprovou aplicar a migration em produção antes do push.
</verification>

<success_criteria>
- FNL-01: `dashboard_funil_detalhado()` devolve as 7 etapas com quantidade histórica, % avançou (denominador incluindo quem está parado — D-02), perdidos em número e taxa, tempo médio parado (com `now()` como saída provisória só para quem segue em andamento) e a marca de gargalo relativa (D-03, 1,5x a média das outras etapas).
- FNL-02: `dashboard_tempo_ate_fechamento()` devolve as médias de dias até ganho e até perdido, separadas.
- FNL-03: as duas RPCs rodam como o chamador; Vendedor B não enxerga nada do Vendedor A e o Supervisor enxerga todos, provado por teste automatizado.
- Migration aplicada no banco hospedado com aprovação humana prévia; suíte completa verde.
</success_criteria>

<output>
Create `.planning/phases/11-funil-de-convers-o-detalhado/11-01-SUMMARY.md` when done.
</output>
