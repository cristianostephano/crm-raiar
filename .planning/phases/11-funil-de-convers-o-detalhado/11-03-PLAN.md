---
phase: 11-funil-de-convers-o-detalhado
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - components/dashboard/FunilDetalhadoTable.tsx
  - tests/dashboard/funil-detalhado-table.test.tsx
autonomous: true
requirements: [FNL-01]

must_haves:
  truths:
    - "FNL-01 (D-01): existe uma tabela com uma linha por etapa do funil e cinco colunas — Etapa, Quantidade, % Avançou, Perdidos, Tempo médio parado — dentro de um Card com o título 'Funil de conversão detalhado'; é uma seção NOVA, não substitui o ClientesPorEtapaChart"
    - "FNL-01: as 7 etapas são sempre renderizadas na ordem fixa de lib/funil/etapas.ts, nunca reordenadas por valor de coluna, mesmo quando uma etapa tem zero clientes"
    - "FNL-01 (D-03): uma linha com gargalo true ganha DUAS marcas simultâneas — a borda esquerda âmbar e um badge com ícone de alerta e o texto 'Gargalo' — nunca só cor, para não excluir quem não distingue cores"
    - "FNL-01 (Pitfall 5): a coluna Quantidade traz um ícone de informação com o tooltip que explica que é o total histórico de quem já passou pela etapa, incluindo quem está parado nela agora"
    - "Valor nulo é sempre renderizado como travessão, nunca como '0,0%' ou '0,0 dias' fabricado"
    - "O componente tem estados de carregando, erro (com botão de nova tentativa) e vazio próprios e independentes, de modo que uma falha aqui nunca apaga o resto do dashboard"
    - "FNL-03: o componente não recebe nem consulta papel do usuário — Vendedor e Supervisor renderizam exatamente o mesmo componente, porque a visibilidade já foi resolvida pela RLS na RPC"
  artifacts:
    - "components/dashboard/FunilDetalhadoTable.tsx (novo Client Component)"
    - "tests/dashboard/funil-detalhado-table.test.tsx (novo teste de render)"
  key_links:
    - "getFunilDetalhadoAction() do plano 02 é a única fonte de dados; o componente não chama Supabase diretamente"
    - "ETAPAS de lib/funil/etapas.ts define a ordem e os rótulos das linhas; as linhas da RPC entram por um Map indexado por etapa, com fallback para linha ausente"
    - "condição de estado vazio: soma de quantidade nas 7 linhas igual a zero (mesma semântica do ClientesPorEtapaChart)"
    - "as strings de erro, de nova tentativa e de estado vazio são reuso verbatim das já usadas por ClientesPorEtapaChart — nunca reescritas"
---

<objective>
Construir o `FunilDetalhadoTable`, a seção nova de tabela do dashboard que entrega o D-01: uma linha por etapa do funil com quantidade histórica, % que avançou, perdidos (número e taxa) e tempo médio parado, mais o destaque relativo de gargalo do D-03.

Purpose: é a superfície onde FNL-01 fica visível para o time de vendas. Toda a matemática já veio pronta da RPC (plano 01) — este plano é composição de primitivos que já existem no repositório (`Table`, `Card`, `Badge`, `Tooltip`, `Skeleton`, `Button`), sem nenhum componente shadcn novo, nenhum token de cor novo e nenhuma dependência npm nova.

Output: um Client Component novo com estados de carregando/erro/vazio próprios, e um teste de render que trava as regras visuais que mais fácil se perdem numa edição futura (ordem das 7 linhas, travessão para nulo, badge de gargalo junto da borda).
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-funil-de-convers-o-detalhado/11-UI-SPEC.md
@.planning/phases/11-funil-de-convers-o-detalhado/11-PATTERNS.md
@.planning/phases/11-funil-de-convers-o-detalhado/11-02-SUMMARY.md
@components/dashboard/ClientesPorEtapaChart.tsx
@components/importacao/ImportPreviewTable.tsx
@lib/funil/etapas.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar components/dashboard/FunilDetalhadoTable.tsx</name>
  <files>components/dashboard/FunilDetalhadoTable.tsx</files>
  <read_first>
    - .planning/phases/11-funil-de-convers-o-detalhado/11-UI-SPEC.md — seções "Copywriting Contract" (as strings exatas) e "Component-Specific Implementation Notes" §0 e §1 (colunas, ordem, formatos de célula, destaque de gargalo, alturas de skeleton, estados). É o contrato aprovado; não parafrasear nenhuma string dele.
    - .planning/phases/11-funil-de-convers-o-detalhado/11-PATTERNS.md — bloco "components/dashboard/FunilDetalhadoTable.tsx": scaffold do efeito de fetch, shell do Card, ramos de loading/erro/vazio, markup da tabela, padrão de "sempre 7 linhas", padrão do badge e dos formatadores.
    - components/dashboard/ClientesPorEtapaChart.tsx — arquivo inteiro: o `FetchState` discriminado, o `reloadKey`, o comentário `eslint-disable-next-line react-hooks/set-state-in-effect`, o guard `cancelled`, e as strings de erro/vazio a reusar verbatim.
    - components/importacao/ImportPreviewTable.tsx — markup de `Table`/`TableHeader`/`TableHead`/`TableBody`/`TableRow`/`TableCell` e a convenção de que só a coluna identificadora leva `font-medium`.
    - components/dashboard/GanhosPerdidosCards.tsx — os formatadores `integerFormatter` e `percentFormatter` e o padrão de guarda de nulo devolvendo travessão (`formatConversao`, linhas 23-34).
    - components/clientes/ClienteCard.tsx (trecho por volta das linhas 210-225) — uso de `Tooltip`/`TooltipTrigger`/`TooltipContent` com `TriangleAlert` e `aria-label` no trigger.
    - lib/funil/etapas.ts — `ETAPAS` (chave + rótulo, ordem fixa) e o tipo `EtapaKey`.
    - lib/supabase/queries/dashboard.ts — o tipo `FunilDetalhadoRow` criado no plano 02.
  </read_first>
  <action>
    Criar `components/dashboard/FunilDetalhadoTable.tsx` como Client Component (`"use client"` no topo), exportando `FunilDetalhadoTable` sem nenhuma prop.

    ESTRUTURA E ESTADO — copiar o scaffold de `ClientesPorEtapaChart`: `type FetchState` discriminado com `{status:"loading"}`, `{status:"error"}` e `{status:"ready"; rows: FunilDetalhadoRow[]}`; `useState` do estado e do `reloadKey`; `useEffect` com dependência `[reloadKey]` (só isso — a action não tem argumento), guard `cancelled`, `setState({status:"loading"})` síncrono no corpo do efeito precedido do mesmo comentário `eslint-disable-next-line react-hooks/set-state-in-effect`, chamada de `getFunilDetalhadoAction()`, e cleanup marcando `cancelled`.

    FORMATADORES — declarar no módulo (fora do componente) duas instâncias de `Intl.NumberFormat`: uma sem opções para inteiros e uma com `minimumFractionDigits: 1` e `maximumFractionDigits: 1` para percentuais e dias, exatamente como `GanhosPerdidosCards` faz. Declarar também helpers puros de formatação de célula que devolvem o travessão (caractere U+2014) quando o valor é nulo — nunca um zero fabricado.

    SHELL — `Card` > `CardHeader` com `CardTitle` de className `text-[20px] font-semibold` e o texto exato `Funil de conversão detalhado`, e `CardDescription` com o texto exato `Histórico completo — não é afetado pelo filtro de período` > `CardContent`.

    RAMOS DENTRO DO CardContent, nesta ordem:
      - carregando: `Skeleton` com className `h-[320px] w-full`.
      - erro: div com className `flex h-[320px] w-full flex-col items-center justify-center gap-3 text-center`, contendo um `p` de className `text-sm text-muted-foreground` com o texto exato `Não foi possível carregar os dados do dashboard. Tente novamente.` e um `Button` `type="button"` `variant="outline"` com `onClick` que incrementa o `reloadKey` e o rótulo exato `Tentar novamente`.
      - vazio: quando a soma de `quantidade` nas 7 linhas é zero, div com className `flex h-[320px] w-full items-center justify-center text-center text-sm text-muted-foreground` e o texto exato `Nenhum cliente cadastrado ainda.` — renderizar ISSO NO LUGAR da tabela, nunca uma tabela cheia de travessões.
      - pronto: a tabela.

    TABELA — `Table` > `TableHeader` > `TableRow` com cinco `TableHead` nesta ordem e com estes textos exatos: `Etapa`, `Quantidade`, `% Avançou`, `Perdidos`, `Tempo médio parado`. O cabeçalho de `Quantidade` é onde mora o afordance de informação: um ícone (`CircleHelp` do lucide-react) com className `size-3.5 text-muted-foreground`, dentro de `Tooltip`/`TooltipTrigger`/`TooltipContent`, com o `TooltipContent` carregando o texto exato `Total de clientes que já passaram por esta etapa, incluindo os que estão parados nela agora.` e o `TooltipTrigger` com um `aria-label` equivalente (mesmo padrão do trigger de `ClienteCard`). Não adicionar uma segunda linha de texto de ajuda visível embaixo do número — o tooltip é a única superfície de esclarecimento.

    CORPO — construir um `Map` indexado por `etapa` a partir das linhas recebidas e iterar `ETAPAS.map(...)` na ordem fixa do array, NUNCA reordenando por valor de coluna. Para cada etapa, recuperar a linha correspondente (pode ser `undefined` se a RPC omitir alguma; tratar como quantidade zero e demais campos nulos). Células:
      1. Etapa: `TableCell` com className `font-medium` e o `label` da etapa.
      2. Quantidade: o número formatado pelo formatador de inteiros.
      3. % Avançou: percentual com uma casa seguido de `%`, ou o travessão quando `avancouPct` é nulo.
      4. Perdidos: se `quantidade` é zero, travessão; senão, se `perdidosCount` é zero, apenas `0`; senão, a contagem seguida do percentual entre parênteses com uma casa e o sinal de porcentagem (formato `3 (12,5%)`).
      5. Tempo médio parado: o número de dias com uma casa seguido de ` dias`, ou o travessão quando `tempoMedioDias` é nulo. Esta célula também hospeda o badge de gargalo.

    DESTAQUE DE GARGALO (D-03) — quando `gargalo` é verdadeiro na linha, aplicar DUAS marcas ao mesmo tempo, nunca só uma: (a) no `TableRow`, a className `border-l-4 border-l-amber-500`; (b) dentro da célula de tempo médio, um `Badge` com `variant="outline"` e className `gap-1 border-amber-500/40 text-amber-500`, contendo um `TriangleAlert` de className `size-3` e o texto exato `Gargalo`. Linha sem gargalo fica com o `TableRow` padrão, sem override de borda e sem badge — a distinção tem que existir SÓ nas linhas marcadas. O mesmo tom âmbar vale em tema claro e escuro; não inventar variante de dark mode.

    PROIBIÇÕES — o componente não recebe prop de papel, não lê papel de lugar nenhum e não tem ramo condicional de aparência por papel (a visibilidade já foi resolvida pela RLS na RPC).
    <!-- planner-discipline-allow: isSupervisor -->
    Também não recebe `inicio`/`fim` nem qualquer parâmetro de período. Não adicionar componente shadcn novo (todos os primitivos usados já existem em `components/ui/`), não adicionar dependência npm, e não tocar em `DashboardClient.tsx` (é do plano 05).

    Comentário de topo do arquivo, no mesmo estilo do de `ClientesPorEtapaChart`: citar FNL-01/D-01/D-02/D-03, registrar que `Quantidade` é a contagem histórica de quem já passou pela etapa (e não o snapshot vivo do gráfico acima), e que a visibilidade por papel vem inteiramente da RLS.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && node -e "const s=require('fs').readFileSync('components/dashboard/FunilDetalhadoTable.tsx','utf8'); for (const t of ['\"use client\"','FunilDetalhadoTable','getFunilDetalhadoAction','ETAPAS.map(','text-[20px] font-semibold','Funil de convers','rico completo','Quantidade','% Avan','Perdidos','dio parado','Total de clientes que j','Nenhum cliente cadastrado ainda.','Tentar novamente','h-[320px] w-full','border-l-4 border-l-amber-500','border-amber-500/40','text-amber-500','TriangleAlert','size-3','Gargalo','Tooltip','reloadKey','\u2014']) { if(!s.includes(t)) throw new Error('faltou no componente: '+t) } const code=s.split('\n').filter(l=>!l.trim().startsWith('*')&&!l.trim().startsWith('//')&&!l.trim().startsWith('/*')).join('\n'); if(/isSupervisor/.test(code)) throw new Error('o componente nao pode ramificar por papel'); if(!code.includes('function FunilDetalhadoTable()')) throw new Error('o componente nao pode receber props (nem papel, nem periodo)'); if(/\.sort\(/.test(code)) throw new Error('as 7 etapas nunca podem ser reordenadas'); console.log('OK componente tabela')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/dashboard/FunilDetalhadoTable.tsx` existe, é `"use client"`, exporta `FunilDetalhadoTable` sem props e busca dados só via `getFunilDetalhadoAction()`.
    - As strings do Copywriting Contract aparecem verbatim: título `Funil de conversão detalhado`, legenda `Histórico completo — não é afetado pelo filtro de período`, cabeçalhos `Etapa` / `Quantidade` / `% Avançou` / `Perdidos` / `Tempo médio parado`, tooltip `Total de clientes que já passaram por esta etapa, incluindo os que estão parados nela agora.`, badge `Gargalo`, vazio `Nenhum cliente cadastrado ainda.`, erro `Não foi possível carregar os dados do dashboard. Tente novamente.` + botão `Tentar novamente`.
    - As 7 linhas vêm de `ETAPAS.map(...)` na ordem fixa; nenhum `.sort(` no arquivo.
    - Linha com gargalo tem simultaneamente `border-l-4 border-l-amber-500` no `TableRow` e um `Badge` com `TriangleAlert` + `Gargalo` na célula de tempo médio; linha sem gargalo não tem nenhum dos dois.
    - Valores nulos renderizam o travessão U+2014; a célula de Perdidos com contagem zero renderiza `0` sem percentual entre parênteses.
    - Skeleton, ramo de erro e ramo de vazio usam a altura `h-[320px]`.
    - Nenhuma referência a papel de usuário e nenhum parâmetro de período no código (fora de comentários).
    - `npx tsc --noEmit` e `npm run lint` limpos; nenhum componente shadcn novo instalado e nenhuma dependência npm nova.
  </acceptance_criteria>
  <done>Tabela do funil detalhado construída com as 5 colunas do D-01, o destaque de gargalo do D-03 em cor + ícone + texto, e os três estados próprios (carregando/erro/vazio).</done>
</task>

<task type="auto">
  <name>Task 2: Criar o teste de render tests/dashboard/funil-detalhado-table.test.tsx</name>
  <files>tests/dashboard/funil-detalhado-table.test.tsx</files>
  <read_first>
    - tests/importacao/import-summary.test.tsx — padrão de teste de render desta base de código: diretiva `// @vitest-environment jsdom` na primeira linha, `render`/`screen` do `@testing-library/react`, asserções por texto visível.
    - tests/clientes/kanban-scroll-column.test.tsx — padrão de asserção por className quando a regra visual é a coisa sendo travada (`container.querySelector` + `toContain` no className).
    - vitest.config.ts — `environmentMatchGlobs` já roteia `tests/**/*.test.tsx` para jsdom e `include` já cobre `.tsx`; nenhuma configuração nova é necessária.
    - components/dashboard/FunilDetalhadoTable.tsx — o componente criado no Task 1.
    - app/layout.tsx — confirma que `TooltipProvider` envolve a árvore em produção; o teste precisa envolver o componente do mesmo jeito.
  </read_first>
  <action>
    Criar `tests/dashboard/funil-detalhado-table.test.tsx` com a diretiva `// @vitest-environment jsdom` na primeira linha.

    Mockar o módulo de Server Actions com `vi.mock("@/app/actions/dashboard", () => ({ getFunilDetalhadoAction: vi.fn() }))` e, em cada teste, configurar o retorno resolvido antes de renderizar. Como o componente busca no `useEffect`, todas as asserções de conteúdo usam as variantes assíncronas (`findByText`/`findAllByRole`) ou um `await screen.findBy...` inicial que sincroniza a árvore antes das asserções síncronas seguintes. Renderizar sempre dentro de `TooltipProvider` (importado de `@/components/ui/tooltip`), espelhando `app/layout.tsx`.

    Escrever um helper local que monta uma linha de `FunilDetalhadoRow` a partir de valores parciais, para os casos ficarem legíveis.

    Casos exigidos:
      1. "renderiza as 7 etapas na ordem fixa": com dados para só algumas etapas, afirmar que existem 7 linhas no corpo da tabela e que os rótulos aparecem exatamente na ordem de `ETAPAS` (comparar o array de textos da primeira célula de cada linha com os `label` de `ETAPAS`). Incluir nos dados uma etapa com tempo médio propositalmente maior que as outras para provar que a ordem NÃO segue o valor da coluna.
      2. "marca a linha de gargalo com borda e badge": uma linha com `gargalo: true` e as demais com `false`. Afirmar que existe exatamente UM elemento com a className `border-l-4` combinada com `border-l-amber-500`, que ele é a linha da etapa esperada, e que dentro dele aparece o texto `Gargalo`. Afirmar também que uma linha sem gargalo não tem esse texto nem essa borda.
      3. "usa travessao para valor nulo": uma etapa com `quantidade: 0`, `avancouPct: null`, `perdidosPct: null` e `tempoMedioDias: null` renderiza o travessão nessas células e nunca a string `0,0%` nem `0,0 dias`.
      4. "formata perdidos com contagem e taxa": uma etapa com `perdidosCount: 3` e `perdidosPct: 12.5` renderiza `3 (12,5%)`; outra com `perdidosCount: 0` e `quantidade` maior que zero renderiza apenas `0`.
      5. "mostra o estado vazio quando ninguem passou por nenhuma etapa": todas as 7 linhas com `quantidade: 0` renderizam `Nenhum cliente cadastrado ainda.` e NÃO renderizam a tabela (nenhum elemento com role `table`).
      6. "mostra o estado de erro com botao de nova tentativa": a action resolve com `{ error: { code: "fetch_falhou", message: "..." } }` e o componente renderiza a mensagem padrão de erro e o botão `Tentar novamente`.

    Usar as strings exatas do Copywriting Contract nas asserções — o valor deste teste é justamente travar essas strings contra reescrita acidental. Não testar o tooltip abrindo por hover (Base UI depende de posicionamento real, que o jsdom não fornece): basta afirmar que o `aria-label` do trigger de informação existe na coluna Quantidade.
  </action>
  <verify>
    <automated>npx vitest run tests/dashboard/funil-detalhado-table.test.tsx && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `tests/dashboard/funil-detalhado-table.test.tsx` existe, começa com `// @vitest-environment jsdom` e mocka `@/app/actions/dashboard`.
    - Os seis casos acima existem e passam.
    - O teste renderiza o componente dentro de `TooltipProvider`.
    - As asserções usam as strings exatas do Copywriting Contract (título, cabeçalhos, `Gargalo`, `Nenhum cliente cadastrado ainda.`, mensagem de erro, `Tentar novamente`).
    - `npx vitest run tests/dashboard/funil-detalhado-table.test.tsx` verde; `npx tsc --noEmit` e `npm run lint` limpos.
  </acceptance_criteria>
  <done>Teste de render travando ordem das 7 linhas, marcação dupla de gargalo, travessão para nulo, formato de perdidos e os estados vazio/erro.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (parte 3 de 5 — tabela do funil)

| Símbolo / Arquivo | Tipo | Status |
|-------------------|------|--------|
| `components/dashboard/FunilDetalhadoTable.tsx` | Client Component novo (Card + Table, 7 linhas fixas, 5 colunas) | Criado (Task 1) |
| `FunilDetalhadoTable` | Export nomeado, sem props | Criado (Task 1) |
| `tests/dashboard/funil-detalhado-table.test.tsx` | Teste de render novo (jsdom) | Criado (Task 2) |

Nenhum componente shadcn novo, nenhum token de cor novo, nenhuma dependência npm nova.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → Client Component | Os números do funil chegam ao browser e são renderizados; tudo que chega aqui já foi filtrado pela RLS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-08 | Information Disclosure | `FunilDetalhadoTable` | medium | mitigate | O componente renderiza exatamente o que a action devolveu, sem nenhum filtro de papel próprio — o que impede a armadilha inversa (esconder no cliente algo que o servidor mandaria, criando falsa sensação de segurança). A fronteira real é a RLS da RPC, provada em `tests/dashboard/rls-dashboard.test.ts` (plano 01). O check de aceite proíbe qualquer ramificação por papel no arquivo. |
| T-11-09 | Repudiation / erro silencioso | ramo de erro do componente | low | mitigate | O estado de erro é explícito e oferece nova tentativa, em vez de renderizar uma tabela vazia que o usuário leria como "não tem gargalo nenhum". Coberto pelo caso 6 do teste de render. |
| T-11-10 | Information Disclosure | mensagem de erro exibida | low | accept | O componente ignora `error.message` e mostra a string fixa do Copywriting Contract; nenhum detalhe técnico chega à tela. |
| T-11-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` e nenhum `npx shadcn add` neste plano — todos os primitivos já existem em `components/ui/`. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo.
- `npm run lint` limpo.
- Check estrutural do componente: strings do Copywriting Contract presentes, classes de gargalo presentes, `ETAPAS.map(` presente, nenhum `.sort(`, nenhuma referência a papel ou período fora de comentários.
- `npx vitest run tests/dashboard/funil-detalhado-table.test.tsx` verde.
</verification>

<success_criteria>
- FNL-01 (D-01): a tabela existe como seção nova, com uma linha por etapa e as cinco colunas contratadas, sem substituir o gráfico existente.
- FNL-01 (D-03): etapas marcadas como gargalo se distinguem por borda âmbar E por badge com ícone e texto, nunca só por cor.
- Nulos aparecem como travessão; contagem zero de perdidos aparece como `0` sem percentual fabricado.
- Carregando, erro e vazio são estados próprios do componente e não afetam o resto do dashboard.
- Nenhuma lógica de papel no componente.
</success_criteria>

<output>
Create `.planning/phases/11-funil-de-convers-o-detalhado/11-03-SUMMARY.md` when done.
</output>
