---
phase: 11-funil-de-convers-o-detalhado
plan: 04
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - components/dashboard/TempoAteFechamentoCards.tsx
  - tests/dashboard/tempo-ate-fechamento-cards.test.tsx
autonomous: true
requirements: [FNL-02]

must_haves:
  truths:
    - "FNL-02: o dashboard mostra dois cartões de número grande — 'Média de dias até ganho' e 'Média de dias até perdido' — como duas métricas separadas, nunca somadas nem combinadas"
    - "FNL-02: quando ainda não existe nenhum ganho (ou nenhum perdido), o cartão correspondente mostra um travessão, nunca '0,0 dias'"
    - "O par de cartões tem estados de carregando e de erro próprios e independentes da tabela do funil — uma falha aqui não apaga a outra seção, e vice-versa"
    - "FNL-03: o componente não recebe nem consulta papel do usuário; Vendedor e Supervisor renderizam exatamente o mesmo componente, com a visibilidade já resolvida pela RLS na RPC"
    - "O componente não recebe período — as médias são sempre de todo o histórico (filtro de período está fora do escopo desta fase)"
  artifacts:
    - "components/dashboard/TempoAteFechamentoCards.tsx (novo Client Component)"
    - "tests/dashboard/tempo-ate-fechamento-cards.test.tsx (novo teste de render)"
  key_links:
    - "getTempoAteFechamentoAction() do plano 02 é a única fonte de dados"
    - "cada valor sai de rows.find(r => r.status === 'ganho'/'perdido')?.mediaDias, com fallback nulo quando a RPC não devolveu a linha daquele status"
    - "o cartão de ganho reusa a borda verde e o cartão de perdido a borda destructive já usados pelos tiles Ganhos/Perdidos do GanhosPerdidosCards — mesmo vocabulário visual, nenhuma cor nova"
---

<objective>
Construir o `TempoAteFechamentoCards`: dois cartões de estatística que entregam FNL-02 — a média de dias entre a entrada do cliente no funil e o momento em que ele virou "ganho", separada da média de dias até virar "perdido".

Purpose: é a resposta direta à pergunta que originou a fase ("quanto tempo em média demora o avanço do nosso funil?"). O cálculo já veio pronto da RPC do plano 01; aqui é só o transplante estrutural do `GanhosPerdidosCards` existente, trocando 3 tiles por 2 e a busca reativa a período por uma busca sem argumento.

Output: um Client Component novo com estados de carregando e erro próprios, e um teste de render que trava os rótulos, o formato do valor e o travessão para "ainda não houve fechamento desse tipo".
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-funil-de-convers-o-detalhado/11-UI-SPEC.md
@.planning/phases/11-funil-de-convers-o-detalhado/11-PATTERNS.md
@.planning/phases/11-funil-de-convers-o-detalhado/11-02-SUMMARY.md
@components/dashboard/GanhosPerdidosCards.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar components/dashboard/TempoAteFechamentoCards.tsx</name>
  <files>components/dashboard/TempoAteFechamentoCards.tsx</files>
  <read_first>
    - .planning/phases/11-funil-de-convers-o-detalhado/11-UI-SPEC.md — "Copywriting Contract" (linhas dos rótulos dos dois tiles e do formato do valor) e "Component-Specific Implementation Notes" §2 (grid, bordas, ícones, tipografia, alturas de skeleton, estado de erro).
    - .planning/phases/11-funil-de-convers-o-detalhado/11-PATTERNS.md — bloco "components/dashboard/TempoAteFechamentoCards.tsx": scaffold do efeito com a ÚNICA diferença em relação ao análogo (array de dependência só com `reloadKey`, porque a RPC não tem argumento), ramos de loading/erro e markup dos dois tiles.
    - components/dashboard/GanhosPerdidosCards.tsx — arquivo inteiro. É o transplante estrutural quase exato: `FetchState`, `reloadKey`, formatadores, ramo de loading com skeletons no grid, ramo de erro dentro de um `Card` com `CardContent`, e o markup dos tiles Ganhos (borda verde, `TrendingUp`) e Perdidos (borda destructive, `TrendingDown`).
    - components/dashboard/ClientesPorEtapaChart.tsx — o formato do efeito com dependência só em `reloadKey` (busca sem argumento), que é o que muda em relação ao análogo principal.
    - lib/supabase/queries/dashboard.ts — o tipo `TempoAteFechamentoRow` criado no plano 02.
  </read_first>
  <action>
    Criar `components/dashboard/TempoAteFechamentoCards.tsx` como Client Component (`"use client"` no topo), exportando `TempoAteFechamentoCards` sem nenhuma prop.

    ESTADO E BUSCA — copiar o scaffold de `GanhosPerdidosCards`: `type FetchState` discriminado com `{status:"loading"}`, `{status:"error"}` e `{status:"ready"; rows: TempoAteFechamentoRow[]}`; `useState` do estado e do `reloadKey`; `useEffect` com guard `cancelled`, `setState({status:"loading"})` síncrono precedido do mesmo comentário `eslint-disable-next-line react-hooks/set-state-in-effect`, chamada de `getTempoAteFechamentoAction()` e cleanup. A ÚNICA divergência em relação ao análogo é o array de dependência: apenas `[reloadKey]`, sem `inicio`/`fim`, porque esta RPC não recebe período. Deixar um comentário de uma linha nessa dependência registrando o porquê, para ninguém "consertar" isso depois achando que faltou um filtro.

    FORMATADOR — uma instância de `Intl.NumberFormat("pt-BR", { minimumFractionDigits: 1, maximumFractionDigits: 1 })` no escopo do módulo, e um helper puro que devolve o travessão (caractere U+2014) quando o valor é nulo, e o número formatado seguido de ` dias` quando não é.

    DERIVAÇÃO DOS VALORES — cada tile lê `state.rows.find(...)` pelo `status` correspondente e cai para nulo quando a linha não existe (mesmo idioma `.find(...) ?? fallback` de `GanhosPerdidosCards`). Zero linhas devolvidas pela RPC é resultado válido: significa que ainda não houve nenhum ganho nem nenhum perdido, e os dois tiles mostram o travessão. Nunca converter ausência de linha em `0`.

    RAMOS, na ordem:
      - carregando: uma div com className `grid grid-cols-1 gap-4 md:grid-cols-2` contendo DOIS `Skeleton` de className `h-[140px] w-full`.
      - erro: um `Card` único com `CardContent` de className `flex flex-col items-center justify-center gap-3 py-6 text-center`, com o `p` de className `text-sm text-muted-foreground` e o texto exato `Não foi possível carregar os dados do dashboard. Tente novamente.`, mais um `Button` `type="button"` `variant="outline"` com `onClick` incrementando o `reloadKey` e o rótulo exato `Tentar novamente`.
      - pronto: a div com className `grid grid-cols-1 gap-4 md:grid-cols-2` contendo os dois tiles.

    TILE 1 (ganho) — `Card` de className `border-l-4 border-l-green-600` > `CardContent` de className `flex flex-col gap-2 p-6` > uma div de className `flex items-center gap-2 text-sm font-semibold text-muted-foreground` contendo um `TrendingUp` de className `size-4 text-muted-foreground` e o texto exato `Média de dias até ganho`, seguida de um `p` de className `text-[36px] leading-[1.1] font-semibold text-foreground` com o valor formatado.

    TILE 2 (perdido) — idêntico, com `Card` de className `border-l-4 border-l-destructive`, ícone `TrendingDown` e o texto exato `Média de dias até perdido`.

    Não colocar título de seção acima do grid — cada rótulo já é autoexplicativo, mesma convenção do `GanhosPerdidosCards`. Não usar `md:grid-cols-3` (são duas métricas, não três).

    PROIBIÇÕES — o componente não recebe prop nenhuma, não lê papel do usuário, não ramifica aparência por papel e não recebe período.
    <!-- planner-discipline-allow: isSupervisor -->
    Nenhum componente shadcn novo, nenhuma dependência npm nova, e não tocar em `DashboardClient.tsx` (é do plano 05).

    Comentário de topo no estilo do análogo: citar FNL-02, registrar que as médias são de todo o histórico (sem filtro de período, por decisão de escopo), que o travessão significa "ainda não houve fechamento desse tipo" e que a visibilidade por papel vem inteiramente da RLS.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && node -e "const s=require('fs').readFileSync('components/dashboard/TempoAteFechamentoCards.tsx','utf8'); for (const t of ['\"use client\"','TempoAteFechamentoCards','getTempoAteFechamentoAction','grid grid-cols-1 gap-4 md:grid-cols-2','h-[140px] w-full','border-l-4 border-l-green-600','border-l-4 border-l-destructive','TrendingUp','TrendingDown','size-4 text-muted-foreground','text-[36px] leading-[1.1] font-semibold','dias at','ganho','perdido',' dias','Tentar novamente','reloadKey','\u2014']) { if(!s.includes(t)) throw new Error('faltou no componente: '+t) } const code=s.split('\n').filter(l=>!l.trim().startsWith('*')&&!l.trim().startsWith('//')&&!l.trim().startsWith('/*')).join('\n'); if(/isSupervisor/.test(code)) throw new Error('o componente nao pode ramificar por papel'); if(!code.includes('function TempoAteFechamentoCards()')) throw new Error('o componente nao pode receber props (nem papel, nem periodo)'); if(code.includes('md:grid-cols-3')) throw new Error('sao 2 tiles, nao 3'); const sk=(code.match(/h-\[140px\] w-full/g)||[]).length; if(sk!==2) throw new Error('esperado exatamente 2 skeletons, achei '+sk); console.log('OK componente cartoes')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/dashboard/TempoAteFechamentoCards.tsx` existe, é `"use client"`, exporta `TempoAteFechamentoCards` sem props e busca dados só via `getTempoAteFechamentoAction()`.
    - Os rótulos aparecem verbatim: `Média de dias até ganho` e `Média de dias até perdido`; o valor sai como número com uma casa decimal em pt-BR seguido de ` dias`, ou o travessão U+2014.
    - Grid `grid grid-cols-1 gap-4 md:grid-cols-2` (nunca `md:grid-cols-3`), exatamente dois `Skeleton` de `h-[140px] w-full` no ramo de carregando.
    - Tile de ganho com `border-l-4 border-l-green-600` + `TrendingUp`; tile de perdido com `border-l-4 border-l-destructive` + `TrendingDown`; valor em `text-[36px] leading-[1.1] font-semibold text-foreground`.
    - Ramo de erro com a mensagem padrão do dashboard e o botão `Tentar novamente`.
    - O array de dependência do efeito contém apenas `reloadKey`.
    - Nenhuma referência a papel de usuário e nenhuma prop; `npx tsc --noEmit` e `npm run lint` limpos.
  </acceptance_criteria>
  <done>Par de cartões de tempo até fechamento construído sobre o vocabulário visual já existente do GanhosPerdidosCards, com busca sem argumento e estados próprios.</done>
</task>

<task type="auto">
  <name>Task 2: Criar o teste de render tests/dashboard/tempo-ate-fechamento-cards.test.tsx</name>
  <files>tests/dashboard/tempo-ate-fechamento-cards.test.tsx</files>
  <read_first>
    - tests/importacao/import-summary.test.tsx — padrão de teste de render (diretiva `// @vitest-environment jsdom`, `render`/`screen`, asserção por texto visível).
    - tests/clientes/kanban-scroll-column.test.tsx — padrão de asserção por className quando a regra travada é visual.
    - components/dashboard/TempoAteFechamentoCards.tsx — o componente criado no Task 1.
    - vitest.config.ts — `environmentMatchGlobs` já roteia `tests/**/*.test.tsx` para jsdom; nenhuma configuração nova é necessária.
  </read_first>
  <action>
    Criar `tests/dashboard/tempo-ate-fechamento-cards.test.tsx` com a diretiva `// @vitest-environment jsdom` na primeira linha e `vi.mock("@/app/actions/dashboard", () => ({ getTempoAteFechamentoAction: vi.fn() }))`. Configurar o retorno resolvido em cada teste antes de renderizar e usar as variantes assíncronas de consulta (`findByText`) para sincronizar com o `useEffect`.

    Casos exigidos:
      1. "mostra as duas medias separadas": a action devolve `[{status:"ganho", mediaDias: 18.4}, {status:"perdido", mediaDias: 7}]` e o componente renderiza `Média de dias até ganho` com `18,4 dias` e `Média de dias até perdido` com `7,0 dias` — provando o formato pt-BR de uma casa decimal e que as duas métricas são independentes.
      2. "mostra travessao quando ainda nao houve ganho": a action devolve só a linha de `perdido`; o cartão de ganho renderiza o travessão e o de perdido renderiza o número. Afirmar explicitamente que a string `0,0 dias` NÃO aparece.
      3. "mostra travessao nos dois quando nao houve fechamento nenhum": a action devolve array vazio; os dois cartões renderizam o travessão e os dois rótulos continuam visíveis (os tiles nunca somem, mesmo sem dado).
      4. "mostra o estado de erro com botao de nova tentativa": a action resolve com `{ error: { code: "fetch_falhou", message: "..." } }` e o componente renderiza a mensagem padrão de erro e o botão `Tentar novamente`, e nenhum dos dois rótulos de métrica.
      5. "usa a borda verde no ganho e a destructive no perdido": afirmar via `container.querySelector` que existe um elemento com `border-l-green-600` e outro com `border-l-destructive`, e que o rótulo de ganho está dentro do primeiro e o de perdido dentro do segundo.

    Usar as strings exatas do Copywriting Contract nas asserções.
  </action>
  <verify>
    <automated>npx vitest run tests/dashboard/tempo-ate-fechamento-cards.test.tsx && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `tests/dashboard/tempo-ate-fechamento-cards.test.tsx` existe, começa com `// @vitest-environment jsdom` e mocka `@/app/actions/dashboard`.
    - Os cinco casos acima existem e passam.
    - O caso 2 afirma explicitamente a ausência de `0,0 dias`.
    - As asserções usam os rótulos exatos `Média de dias até ganho` e `Média de dias até perdido`, a mensagem padrão de erro e `Tentar novamente`.
    - `npx vitest run tests/dashboard/tempo-ate-fechamento-cards.test.tsx` verde; `npx tsc --noEmit` e `npm run lint` limpos.
  </acceptance_criteria>
  <done>Teste de render travando os dois rótulos, o formato pt-BR de uma casa, o travessão para ausência de fechamento e o estado de erro.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (parte 4 de 5 — cartões de tempo até fechamento)

| Símbolo / Arquivo | Tipo | Status |
|-------------------|------|--------|
| `components/dashboard/TempoAteFechamentoCards.tsx` | Client Component novo (2 tiles de estatística) | Criado (Task 1) |
| `TempoAteFechamentoCards` | Export nomeado, sem props | Criado (Task 1) |
| `tests/dashboard/tempo-ate-fechamento-cards.test.tsx` | Teste de render novo (jsdom) | Criado (Task 2) |

Nenhum componente shadcn novo, nenhuma cor nova, nenhuma dependência npm nova.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action → Client Component | As duas médias chegam ao browser já filtradas pela RLS da RPC. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-11 | Information Disclosure | `TempoAteFechamentoCards` | medium | mitigate | Sem nenhum filtro de papel próprio: o componente renderiza o que a action devolveu, e a fronteira real é a RLS da RPC (provada em `tests/dashboard/rls-dashboard.test.ts`, plano 01). O check de aceite proíbe qualquer referência a papel no arquivo. |
| T-11-12 | Repudiation / número enganoso | tile sem dado | medium | mitigate | Ausência de linha vira travessão, nunca `0,0 dias` — um zero fabricado leria como "fechamos no mesmo dia", que é o oposto de "ainda não houve fechamento". Travado pelos casos 2 e 3 do teste de render. |
| T-11-13 | Information Disclosure | mensagem de erro exibida | low | accept | O componente ignora `error.message` e mostra a string fixa do Copywriting Contract. |
| T-11-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` e nenhum `npx shadcn add` neste plano. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo.
- `npm run lint` limpo.
- Check estrutural do componente: rótulos e classes do UI-SPEC presentes, exatamente 2 skeletons, sem `md:grid-cols-3`, sem props, sem referência a papel.
- `npx vitest run tests/dashboard/tempo-ate-fechamento-cards.test.tsx` verde.
</verification>

<success_criteria>
- FNL-02: as duas médias (até ganho e até perdido) aparecem como métricas separadas, com uma casa decimal em pt-BR.
- Ausência de fechamento de um tipo aparece como travessão, nunca como zero.
- Carregando e erro são estados próprios, isolados da tabela do funil.
- Nenhuma lógica de papel e nenhum parâmetro de período no componente.
</success_criteria>

<output>
Create `.planning/phases/11-funil-de-convers-o-detalhado/11-04-SUMMARY.md` when done.
</output>
