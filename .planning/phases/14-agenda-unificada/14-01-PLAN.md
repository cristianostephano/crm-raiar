---
phase: 14-agenda-unificada
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/{proximo_numero_livre}_agenda_do_vendedor.sql
  - tests/agenda/agenda-rpc.test.ts
  - tests/agenda/rls-agenda.test.ts
autonomous: false
requirements: [AGD-01, AGD-04]

must_haves:
  truths:
    - "AGD-01: existe UMA função `agenda_do_vendedor()` que devolve, numa chamada só, as tarefas de prospecção pendentes E as visitas de pós-venda pendentes, cada linha carimbada com uma coluna `origem` que vale 'prospeccao' ou 'visita' — nunca duas consultas separadas"
    - "AGD-01: as linhas voltam ordenadas por data crescente (a mais antiga primeiro), que é a ordenação que produz atrasado → hoje → próximos dias; a ordenação é decidida no SQL e em nenhum outro lugar"
    - "AGD-01: cada linha identifica o cliente (`cliente_id` + `razao_social`), o dono do cliente (`responsavel` + `responsavel_nome`), o rótulo do item (`titulo` = o nome do tipo de tarefa para prospecção, o literal 'Visita' para visita) e a data (`data`)"
    - "AGD-01: item já concluído nunca aparece — tarefa com `concluida = true` e visita com `data_realizada` preenchida ficam de fora; tarefa sem data também fica de fora, porque não teria seção onde cair"
    - "AGD-04: a função NÃO contém nenhuma checagem de papel no corpo — o Vendedor só recebe itens dos próprios clientes e o Supervisor recebe os do time inteiro exclusivamente por causa da RLS já existente em `clientes`/`tarefas`/`visitas`, provado por teste cross-vendedor"
    - "A função roda como o usuário chamador (sem cláusula de elevação de privilégio), igual a toda função `dashboard_*` do projeto — este marco continua com exatamente três exceções de privilégio elevado, nenhuma nova"
    - "AGD-06 (contrato do contador): chamar a mesma função pedindo só a contagem devolve exatamente o mesmo número de linhas que a chamada normal devolve — é isso que garante que o número no menu nunca discorde da lista da tela"
    - "Existe índice de data em `tarefas` para sustentar a ordenação do feed unificado; o índice de data de `visitas` já existe desde a migration anterior e não é recriado"
    - "A migration é puramente aditiva: nenhuma tabela nova, nenhuma policy nova, nenhuma alteração de tabela existente, nenhum dado tocado, nenhuma trigger recriada, nenhum agendador/processo de fundo"
    - "A migration foi aplicada no projeto Supabase hospedado via `supabase db push` com aprovação humana prévia, e os testes das fases anteriores continuam verdes"
  artifacts:
    - "supabase/migrations/{proximo_numero_livre}_agenda_do_vendedor.sql (número derivado por `ls` na execução, nunca copiado deste plano)"
    - "função `agenda_do_vendedor()` — `language sql`, `stable`, sem elevação de privilégio, `union all` de `tarefas` pendentes + `visitas` pendentes"
    - "índice `idx_tarefas_data_conclusao` em `tarefas (data_conclusao)`"
    - "tests/agenda/agenda-rpc.test.ts (novo)"
    - "tests/agenda/rls-agenda.test.ts (novo)"
  key_links:
    - "ausência de qualquer checagem de papel dentro do corpo da função — é exatamente isso que faz AGD-04 sair de graça da RLS; qualquer filtro manual de dono ali dentro quebraria a visão do Supervisor ou duplicaria a autorização fora da fronteira real"
    - "ausência de cláusula de elevação de privilégio — com ela a função devolveria a agenda do time inteiro para qualquer vendedor, que é a falha mais grave possível nesta fase"
    - "`where t.concluida = false and t.data_conclusao is not null` e `where v.data_realizada is null` aplicados ANTES do `union all`, não depois — é o que mantém o conjunto de trabalho proporcional ao que está em aberto, não ao histórico inteiro"
    - "o `union all` normaliza as duas metades para a MESMA lista de colunas, na mesma ordem e com os mesmos tipos — se as duas metades divergirem, o Postgres recusa a função na hora do push"
    - "`order by` posicional sobre a coluna de data no final da união (não dentro de cada metade) — ordenar dentro de cada select antes da união não ordena o resultado combinado"
---

<objective>
Criar a camada de banco inteira da Agenda: uma única função `agenda_do_vendedor()` que devolve, numa chamada só, as tarefas de prospecção pendentes e as visitas de pós-venda pendentes já unificadas, ordenadas por data e carimbadas com a origem de cada item — mais o índice de data em `tarefas` que sustenta essa ordenação. Mais os dois arquivos de teste de integração que provam o formato do feed e provam que Vendedor e Supervisor enxergam exatamente o que devem enxergar.

Purpose: é a decisão de maior alavancagem da fase inteira. Toda a tela da Agenda, o destaque de atrasado e o contador do menu se apoiam neste único caminho de leitura. Se a lista for montada com duas consultas separadas e um merge no navegador, a lógica de ordenação passa a existir em dois lugares, o custo de tráfego dobra e a regra de quem-vê-o-quê deixa de ser uma coisa só. E se a função for criada com elevação de privilégio, qualquer vendedor passa a ver a agenda do time inteiro.

Output: uma migration aditiva aplicada no banco hospedado com aprovação humana prévia, e dois arquivos de teste novos, verdes contra o banco real.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/skills/Supabase-conventions/SKILL.md
@supabase/migrations/0011_dashboard_comparativo_vendedor.sql
@supabase/migrations/0013_cliente_ativo_e_frequencia_visita.sql
</context>

<source_audit>
## Multi-Source Coverage Audit — Fase 14 (conjunto completo de planos 14-01 a 14-04)

### GOAL (ROADMAP, Fase 14 — 4 critérios de sucesso)

| # | Item | Plano | Status |
|---|------|-------|--------|
| G1 | Item "Agenda" no topo do menu, acima de "Clientes", com a contagem de pendentes | 14-04 (menu + selo) + 14-02 (leitor da contagem) | COVERED |
| G2 | Tarefas de prospecção e visitas numa lista só, ordenada atrasado → hoje → próximos, cada item identificado pelo cliente e pela origem | 14-01 (união + ordenação + origem) + 14-02 (repartição em seções) + 14-03 (renderização) | COVERED |
| G3 | Itens atrasados destacados no mesmo padrão do kanban | 14-02 (classificação) + 14-03 (borda vermelha + triângulo âmbar, classes idênticas às do `ClienteCard`) | COVERED |
| G4 | Vendedor vê só a própria agenda; Supervisor vê a do time e filtra por vendedor | 14-01 (RLS provada por teste cross-vendedor) + 14-03 (filtro de vendedor) | COVERED |

### REQ (REQUIREMENTS.md — phase_req_ids)

| ID | Plano | Status |
|----|-------|--------|
| AGD-01 | 14-01 + 14-02 + 14-03 | COVERED |
| AGD-02 | 14-04 | COVERED |
| AGD-03 | 14-02 + 14-03 | COVERED |
| AGD-04 | 14-01 | COVERED |
| AGD-05 | 14-03 (+ helpers puros em 14-02) | COVERED |
| AGD-06 | 14-02 (leitor de contagem) + 14-04 (selo no menu) | COVERED |

### RESEARCH (.planning/research/ARCHITECTURE.md + PITFALLS.md + FEATURES.md + SUMMARY.md)

| Item | Plano | Status |
|------|-------|--------|
| Pattern 2 — feed unificado por RPC `language sql stable`, sem elevação de privilégio | 14-01 | COVERED |
| Anti-Pattern (ARCHITECTURE) — nenhuma checagem de papel dentro da função; AGD-04 sai da RLS | 14-01 | COVERED |
| Anti-Pattern 4 — nunca duas consultas do cliente mescladas no navegador | 14-01 (a união é a única fonte) + 14-02 (o leitor chama uma RPC só) | COVERED |
| Pitfall 5 — índice de data em cada tabela de origem | 14-01 (`idx_tarefas_data_conclusao`; o de `visitas` já veio na 0013) | COVERED |
| Pitfall 5 — seleção estreita de colunas (nunca tudo) | 14-01 (lista fechada de 8 colunas) | COVERED |
| Pitfall 5 — paginação/limite no SQL | 14-01 | COVERED POR DESENHO: a tela não tem conceito de página — ela renderiza as três seções com contagem ao vivo, então um `limit` tornaria as contagens mentirosas. O conjunto de trabalho já é limitado ao que está EM ABERTO (filtros aplicados antes da união) e o índice de data está criado. Ponto de revisita explícito registrado no checkpoint humano do Task 2. |
| Pitfall 1 — nunca construir data a partir de string crua; usar `parseISO` | 14-02 (autoridade da repartição) + 14-03 (formatação `dd/MM`) | COVERED |
| Anti-Pattern 1 / Pitfall 4 — nada de agendador ou processo de fundo | 14-01 (verificação mecânica na migration) | COVERED |
| Zero exceção nova de privilégio elevado | 14-01 | COVERED |
| Distinção visual entre as duas frentes (UX Pitfalls) | 14-03 (selo de origem) | COVERED |
| `concluir_visita`, `resumo`, trigger de histórico estendida, diário do cliente | — | FORA DE ESCOPO: Fases 15 e 16, e o `14-UI-SPEC.md` proíbe explicitamente trazer qualquer um deles para esta fase |

### CONTEXT (decisões travadas)

Não existe `14-CONTEXT.md` — o dono pulou o Discuss porque as decisões já foram travadas no nível do marco. As decisões vinculantes vêm de `.planning/STATE.md` (Accumulated Context) e de `14-UI-SPEC.md` (aprovado, 6/6 dimensões), e estão todas cobertas:

| Decisão travada | Plano | Status |
|-----------------|-------|--------|
| Toda função de leitura é SECURITY INVOKER por omissão; RLS é a única fronteira | 14-01 | COVERED |
| Nenhuma exceção nova de privilégio elevado (as três já documentadas continuam sendo três) | 14-01 | COVERED |
| Sem cron / sem worker de fundo em nenhum ponto do marco | 14-01 | COVERED |
| UI-SPEC: fase de LEITURA PURA — sem botão "Concluir", sem campo de resumo, sem próxima data | 14-01 (nenhuma escrita) + 14-03 (nenhum controle de escrita) | COVERED |
| UI-SPEC: ordem fixa das seções Atrasado / Hoje / Próximos dias, seção vazia é omitida inteira | 14-03 | COVERED |
| UI-SPEC: copy exata (título, subtítulo, cabeçalhos, selos, tooltip, estados vazios, erro) | 14-03 + 14-04 | COVERED |
| UI-SPEC: `border-l-4 border-l-red-500` e `TriangleAlert size-3.5 text-amber-500` reusados literalmente do `ClienteCard` | 14-03 | COVERED |
| UI-SPEC: clicar na linha abre o `ClienteDetailSheet` existente | 14-03 | COVERED |
| UI-SPEC: item "Agenda" primeiro, antes de "Clientes"; nunca renderizar o literal "Agenda (0)" | 14-04 | COVERED |
| UI-SPEC: contagem lida uma vez por carregamento de página, sem tempo real | 14-02 + 14-04 | COVERED |
| UI-SPEC: nenhum componente shadcn novo, nenhuma dependência npm nova | 14-02 + 14-03 + 14-04 | COVERED |

**Resultado: 0 itens MISSING.** Nenhum item de fonte ficou fora dos quatro planos.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Escrever a migration da RPC unificada e os dois arquivos de teste de integração (RED)</name>
  <files>supabase/migrations/{proximo_numero_livre}_agenda_do_vendedor.sql, tests/agenda/agenda-rpc.test.ts, tests/agenda/rls-agenda.test.ts</files>
  <read_first>
    - supabase/migrations/0011_dashboard_comparativo_vendedor.sql (já no contexto) — é o molde exato desta migration. Extrair: (a) o formato do banner de comentário que explica por que a função NÃO eleva privilégio e por que isso nunca deve mudar; (b) a forma `create or replace function nome() returns table(...) language sql stable as $$ ... $$;`; (c) o `trim(both ' ' from coalesce(p.nome, '') || ' ' || coalesce(p.sobrenome, ''))` usado para montar o nome do vendedor; (d) o comentário que registra que `profiles` tem policy de SELECT aberta para qualquer autenticado — é pré-existente, não é vazamento novo, e não se deve adicionar checagem de papel por causa disso.
    - supabase/migrations/0013_cliente_ativo_e_frequencia_visita.sql (já no contexto) — a forma real da tabela `visitas` (colunas `id`, `cliente_id`, `data_prevista`, `data_realizada`, `resumo`, `criado_por`, `criado_em`), as quatro policies dela, e a lista de índices que ela já criou (importante: o índice de data de `visitas` JÁ EXISTE, não recriar).
    - supabase/migrations/0002_clientes_and_funil.sql — a forma real de `tarefas` (`id`, `cliente_id`, `tipo_tarefa_id`, `data_conclusao`, `concluida`, `concluida_em`, `criado_em`), de `tipos_tarefa` (`id`, `nome`, `ativo`) e de `clientes` (em especial `responsavel`, `razao_social`), mais a lista de índices no fim do arquivo como padrão de nomenclatura.
    - .planning/research/ARCHITECTURE.md — "Pattern 2" (o corpo de exemplo da função e por que RPC em vez de view ou merge no cliente), a linha de `agenda_do_vendedor` na tabela "Component Responsibilities", e o "Anti-Pattern 4".
    - .planning/research/PITFALLS.md — "Pitfall 5" inteiro (feed unificado: índices de data, seleção estreita de colunas, filtrar o pendente ANTES da união) e a tabela "Performance Traps".
    - .planning/STATE.md — o bloco de exceções de privilégio elevado no fim de Deferred Items (continuam sendo três; esta fase não pode criar a quarta) e o Blocker do rate limit de login nos testes.
    - tests/dashboard/rls-dashboard.test.ts — o molde de teste "prova que a função respeita a RLS": como Vendedor A e Vendedor B são logados, como o Supervisor é logado, e como se afirma que um vê e o outro não.
    - tests/clientes/rls-visitas.test.ts — o molde mais recente de teste de integração desta fase do projeto: import de `SEED_ACCOUNTS` de `../auth/rls-roles.test`, `signInAs`/`serviceClient` de `../helpers/supabase-test-clients`, o array de ids criados e o `afterEach` que limpa via service role, e como uma visita é semeada para um cliente.
    - tests/clientes/funil-constraints.test.ts — como um cliente de teste é criado respeitando os CHECKs de `clientes`, e como um `tipo_tarefa` ativo é buscado para criar uma tarefa.
    - tests/helpers/supabase-test-clients.ts — assinaturas de `anonClient()`, `serviceClient()`, `signInAs(email, password)`.
    - vitest.config.ts — `fileParallelism: false` e `include: ["tests/**/*.test.{ts,tsx}"]` (a pasta `tests/agenda/` nova já é coberta pelo padrão, nenhuma configuração precisa mudar).
  </read_first>
  <action>
    Duas entregas neste task: a migration e os dois arquivos de teste. Nada de código de aplicação (`app/`, `lib/`, `components/`) — isso é dos planos 02, 03 e 04.

    (A) DERIVAR O NÚMERO DA MIGRATION EM TEMPO DE EXECUÇÃO. Rodar `ls supabase/migrations/` como PRIMEIRA ação e usar o próximo número livre de quatro dígitos no nome do arquivo novo: `supabase/migrations/{numero}_agenda_do_vendedor.sql`. NÃO usar como literal nenhum número escrito neste plano ou na research. Ao final do task o arquivo criado tem que ser o de MAIOR número da pasta. Nenhum arquivo de migration já existente pode ser aberto para edição.

    (B) Escrever a migration, nesta ordem, em seções numeradas com comentário de cabeçalho no estilo do `0011`:

    1. **Banner de cabeçalho.** Registrar em comentário: que este arquivo é a camada de leitura da Fase 14 e que a fase inteira é de LEITURA PURA (nenhuma escrita, nenhuma tabela, nenhuma policy); que a função nasce sem cláusula de elevação de privilégio de propósito, espelhando toda função `dashboard_*` do projeto, e que adicionar uma passaria a devolver a agenda do time inteiro para qualquer vendedor; que a regra "vendedor vê o próprio, supervisor vê o time" (AGD-04) vem inteiramente da RLS já existente em `clientes`, `tarefas` e `visitas`, e que por isso é PROIBIDO escrever qualquer filtro de dono ou checagem de papel dentro do corpo da função; e que este marco continua com exatamente três exceções de privilégio elevado no projeto, sem nenhuma nova.
       <!-- planner-discipline-allow: security definer -->
       Registrar também, como o `0011` já faz, que `profiles` tem policy de SELECT aberta para qualquer autenticado desde a migration 0001 — o join com `profiles` para trazer o nome do vendedor é comportamento pré-existente, não um vazamento novo, e não justifica adicionar checagem de papel nenhuma.

    2. **Índice.** `create index idx_tarefas_data_conclusao on tarefas (data_conclusao);` — é o índice que a Pitfall 5 pede para sustentar a ordenação do feed. Deixar um comentário registrando que o índice equivalente de `visitas` já foi criado pela migration anterior e por isso NÃO é recriado aqui.
       <!-- planner-discipline-allow: idx_visitas_data_prevista -->
       Esta é a única instrução da migration que não é a criação da função. Ela NÃO é um `alter table` — é um `create index`, que é aditivo e não reescreve a tabela.

    3. **`agenda_do_vendedor()`.** `create or replace function agenda_do_vendedor() returns table(...) language sql stable as $$ ... $$;` — sem parâmetros, sem cláusula de elevação de privilégio.

       Colunas de saída, exatamente estas oito, nesta ordem (nomes em snake_case, como toda função do projeto):
       1. `origem text` — `'prospeccao'` ou `'visita'`
       2. `item_id uuid` — o id da linha de origem (`tarefas.id` ou `visitas.id`)
       3. `cliente_id uuid`
       4. `razao_social text`
       5. `responsavel uuid` — `clientes.responsavel`
       6. `responsavel_nome text` — nome completo montado a partir de `profiles`, no mesmo formato do `0011`
       7. `titulo text` — o `nome` do `tipos_tarefa` para a metade de prospecção; o literal `'Visita'` para a metade de visita
       8. `data date` — `tarefas.data_conclusao` numa metade, `visitas.data_prevista` na outra

       Corpo: exatamente um `union all` entre duas metades normalizadas para essas mesmas oito colunas, na mesma ordem e com os mesmos tipos (usar cast explícito `::text` nos literais `'prospeccao'`, `'visita'` e `'Visita'`, senão o Postgres infere `unknown` e a união pode recusar).

       Metade 1 (prospecção): `from tarefas t join clientes c on c.id = t.cliente_id join tipos_tarefa tt on tt.id = t.tipo_tarefa_id left join profiles p on p.id = c.responsavel where t.concluida = false and t.data_conclusao is not null`.
         - O join com `tipos_tarefa` é interno (todo `tarefas.tipo_tarefa_id` é obrigatório e referencia a tabela), o join com `profiles` é externo (um cliente pode, em tese, estar sem responsável).
         - O filtro `t.data_conclusao is not null` é DELIBERADO e precisa de um comentário SQL explicando: uma tarefa sem data não teria em qual das três seções da tela cair (atrasado / hoje / próximos dias), então ela não entra no feed. Este é o ponto que o checkpoint humano do Task 2 leva ao dono para confirmar.

       Metade 2 (visita): `from visitas v join clientes c on c.id = v.cliente_id left join profiles p on p.id = c.responsavel where v.data_realizada is null`.
         - `data_realizada` nula é o que significa "pendente" nesta tabela (definido na migration anterior).

       Os dois `where` ficam DENTRO de cada metade, antes da união — nunca um filtro só depois do `union all`. É isso que mantém o conjunto de trabalho proporcional ao que está em aberto e não ao histórico inteiro (Pitfall 5).

       Ordenação: um único `order by` posicional no FIM da união, ordenando primeiro pela coluna de data (posição 8) e depois pela razão social (posição 4) como desempate determinístico. Ordenar dentro de cada metade não ordena o resultado combinado. Deixar um comentário SQL dizendo em texto quais são as posições usadas (8 = data, 4 = razão social) e que a ordenação é decidida AQUI e em nenhum outro lugar — nem o leitor tipado nem o componente de tela reordenam o que recebem, exatamente como o `ComparativoVendedorTable` já documenta para o comparativo de vendedor.

       Selecionar SOMENTE as oito colunas listadas. Nunca `select *` em nenhuma das metades (Pitfall 5).

    **Regras absolutas desta migration:**
      - Nenhuma cláusula de elevação de privilégio em nenhuma função.
      - Nenhuma chamada a `is_supervisor()` nem a `auth.uid()` dentro do corpo da função — é a chamada que faria AGD-04 deixar de sair da RLS.
        <!-- planner-discipline-allow: is_supervisor -->
        <!-- planner-discipline-allow: auth.uid -->
      - Nenhuma tabela nova, nenhuma policy nova (de qualquer operação, sobre qualquer tabela), nenhuma alteração de estrutura de tabela existente, nenhuma trigger criada ou recriada.
      - Nenhuma instrução que apague, trunque, insira ou altere dado.
      - Nenhum agendador, extensão de agendamento ou processo de fundo de qualquer tipo.
      - Nenhuma dependência npm nova.
      - Exatamente uma função criada no arquivo e exatamente um índice.

    (C) Criar a pasta `tests/agenda/` e os dois arquivos de teste. Os dois vão FALHAR (RED) até o push do Task 3. Padrão obrigatório em ambos: `SEED_ACCOUNTS` importado de `../auth/rls-roles.test`, sessões reais via `signInAs()`, `serviceClient()` usado SOMENTE para semear e limpar (nunca para afirmar comportamento de RLS — service role ignora RLS e daria falso positivo), e um `afterEach` limpando por id os clientes criados (o `on delete cascade` já leva tarefas e visitas junto). Para respeitar o rate limit de login registrado no STATE.md, logar cada identidade UMA vez por arquivo e reusar o cliente autenticado entre os casos, nunca chamar `signInAs` dentro de cada `it()`.

    **`tests/agenda/agenda-rpc.test.ts` (novo).** Cenário base montado uma vez: Vendedor A com um cliente, uma tarefa em aberto com data e uma visita pendente. Casos exigidos — os nomes dos `it()` precisam conter as palavras-chave marcadas, sem acento, porque as acceptance criteria filtram por elas:
      - **`origem`**: `rpc('agenda_do_vendedor')` devolve as duas linhas do cliente semeado; a da tarefa tem `origem` igual a `'prospeccao'` e `titulo` igual ao `nome` do `tipos_tarefa` usado; a da visita tem `origem` igual a `'visita'` e `titulo` igual ao literal `'Visita'`.
      - **`cliente`**: as duas linhas trazem `cliente_id` e `razao_social` do cliente semeado, e `responsavel` igual ao id do Vendedor A, com `responsavel_nome` não vazio.
      - **`ordem`**: semear três itens do mesmo cliente com datas deliberadamente fora de ordem (por exemplo daqui a 10 dias, ontem, e hoje) e afirmar que a sequência devolvida para esses três ids é da data mais antiga para a mais recente. NÃO reordenar no teste antes de comparar — é justamente a ordenação do SQL que está sendo provada.
      - **`pendente`**: uma tarefa com `concluida = true` e uma visita com `data_realizada` preenchida NÃO aparecem no resultado (afirmar pela ausência dos ids delas).
      - **`semdata`**: uma tarefa em aberto com `data_conclusao` nula NÃO aparece no resultado — é a decisão deliberada registrada na migration.
      - **`contagem`**: chamar `rpc('agenda_do_vendedor', {}, { count: 'exact', head: true })` e afirmar que o `count` devolvido é exatamente igual ao `length` do array devolvido pela chamada normal. É o contrato do selo do menu (AGD-06): o número do menu e a lista da tela vêm da mesma fonte e não podem discordar. Se a combinação `head`/`count` não devolver contagem nesta versão do supabase-js, NÃO relaxar o teste: registrar o achado no SUMMARY como uma restrição real, e o plano 14-02 usará o `length` da leitura completa como fonte da contagem.

    **`tests/agenda/rls-agenda.test.ts` (novo).** Cenário base: Vendedor A com um cliente e um item pendente; Vendedor B com um cliente e um item pendente (semear via `serviceClient()` para não depender de RLS na semeadura). Casos exigidos — palavras-chave nos nomes dos `it()`:
      - **`vendedor`**: a agenda do Vendedor A contém o item do próprio cliente e NÃO contém nenhuma linha cujo `cliente_id` seja o do cliente do Vendedor B. E o simétrico: a agenda do Vendedor B não contém nada do cliente do Vendedor A. É AGD-04, a metade do vendedor.
      - **`supervisor`**: a agenda do Supervisor contém os itens dos DOIS vendedores, e as linhas de cada um trazem o `responsavel_nome` correspondente. É AGD-04, a metade do supervisor, e é também a prova de que a ausência de checagem de papel dentro da função é o que faz isso funcionar.
      - **`anonimo`**: um cliente não autenticado (`anonClient()`) chamando a função não recebe nenhuma linha dos clientes semeados (seja por erro, seja por lista vazia — aceitar as duas formas, afirmar apenas que nenhum dos ids semeados volta).
  </action>
  <verify>
    <automated>test -z "$(git diff --name-only HEAD -- supabase/migrations/)" && npx tsc --noEmit && node -e "const fs=require('fs');const dir='supabase/migrations';const all=fs.readdirSync(dir).filter(n=>n.endsWith('.sql')).sort();const mig=all.filter(n=>n.includes('agenda_do_vendedor'));if(mig.length!==1)throw new Error('esperado exatamente 1 arquivo _agenda_do_vendedor.sql, achei '+mig.length);if(all[all.length-1]!==mig[0])throw new Error('a migration nova nao e a de maior numero da pasta: '+mig[0]+' vs '+all[all.length-1]);const raw=fs.readFileSync(dir+'/'+mig[0],'utf8');const body=raw.split('\n').filter(l=>!l.trim().startsWith('--')).join('\n').toLowerCase();for(const t of ['create or replace function agenda_do_vendedor','returns table','language sql','stable','union all','origem','prospeccao','item_id','cliente_id','razao_social','responsavel_nome','titulo','from tarefas','from visitas','tipos_tarefa','concluida = false','data_conclusao is not null','data_realizada is null','order by','idx_tarefas_data_conclusao'])if(!body.includes(t))throw new Error('faltou no SQL: '+t);if(/security\s+definer/.test(body))throw new Error('a funcao nao pode elevar privilegio');if(/is_supervisor/.test(body))throw new Error('AGD-04 sai da RLS: nenhuma checagem de papel dentro da funcao');if(/auth\.uid/.test(body))throw new Error('AGD-04 sai da RLS: nenhum filtro manual de dono dentro da funcao');if(/create\s+table|create\s+policy|alter\s+table|create\s+trigger|drop\s+/.test(body))throw new Error('esta migration e puramente aditiva: so uma funcao e um indice');if(/truncate|delete\s+from|insert\s+into|\bupdate\s+[a-z_]+\s+set\b/.test(body))throw new Error('esta migration nao pode tocar em dado');if(/pg_cron|schedule\(/.test(body))throw new Error('nenhum agendador/processo de fundo neste marco');if(/select\s+\*/.test(body))throw new Error('Pitfall 5: selecao estreita de colunas, nunca select *');const ua=(body.match(/union\s+all/g)||[]).length;if(ua!==1)throw new Error('esperado exatamente 1 union all, achei '+ua);const fn=(body.match(/create\s+or\s+replace\s+function/g)||[]).length;if(fn!==1)throw new Error('esperado exatamente 1 create function, achei '+fn);const ix=(body.match(/create\s+index/g)||[]).length;if(ix!==1)throw new Error('esperado exatamente 1 create index (o de tarefas), achei '+ix);if(/create\s+index[^;]*on\s+visitas/.test(body))throw new Error('o indice de data de visitas ja existe desde a migration anterior');const t1=fs.readFileSync('tests/agenda/agenda-rpc.test.ts','utf8');const n1=(t1.match(/\bit\(\s*[\"'][^\"']+/g)||[]).map(s=>s.toLowerCase());for(const k of ['origem','cliente','ordem','pendente','semdata','contagem'])if(!n1.some(t=>t.includes(k)))throw new Error('agenda-rpc.test.ts: falta caso com a palavra '+k);const t2=fs.readFileSync('tests/agenda/rls-agenda.test.ts','utf8');const n2=(t2.match(/\bit\(\s*[\"'][^\"']+/g)||[]).map(s=>s.toLowerCase());for(const k of ['vendedor','supervisor','anonimo'])if(!n2.some(t=>t.includes(k)))throw new Error('rls-agenda.test.ts: falta caso com a palavra '+k);for(const f of [t1,t2]){if(!f.includes('SEED_ACCOUNTS')||!f.includes('signInAs'))throw new Error('os testes precisam usar sessoes reais via signInAs/SEED_ACCOUNTS');if(!f.includes('agenda_do_vendedor'))throw new Error('os testes precisam chamar a RPC agenda_do_vendedor')}console.log('OK '+mig[0])"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only HEAD -- supabase/migrations/` sai vazio: nenhuma migration já existente foi editada.
    - Existe exatamente um `supabase/migrations/*_agenda_do_vendedor.sql` e ele é o de MAIOR número da pasta (número derivado por `ls` na execução).
    - O SQL (ignorando comentários) cria exatamente uma função `agenda_do_vendedor()` — `returns table`, `language sql`, `stable` — e exatamente um índice, o `idx_tarefas_data_conclusao`; nenhum índice novo sobre `visitas`.
    - O corpo tem exatamente um `union all`, lê de `tarefas` (com join em `tipos_tarefa`) numa metade e de `visitas` na outra, filtra `concluida = false` e `data_conclusao is not null` numa e `data_realizada is null` na outra, e termina com um `order by`.
    - A saída expõe as colunas `origem`, `item_id`, `cliente_id`, `razao_social`, `responsavel_nome` e `titulo`, e nenhuma metade usa seleção aberta de colunas.
    - O arquivo não contém cláusula de elevação de privilégio, não chama a função de checagem de papel do projeto nem a função de identidade do usuário logado dentro do corpo, não cria/altera tabela, policy ou trigger, não toca em nenhum dado, e não introduz agendador.
    - `tests/agenda/agenda-rpc.test.ts` existe, chama `agenda_do_vendedor`, usa `signInAs`/`SEED_ACCOUNTS`, e tem casos cujos nomes contêm `origem`, `cliente`, `ordem`, `pendente`, `semdata` e `contagem`.
    - `tests/agenda/rls-agenda.test.ts` existe, chama `agenda_do_vendedor`, usa `signInAs`/`SEED_ACCOUNTS`, e tem casos cujos nomes contêm `vendedor`, `supervisor` e `anonimo`.
    - `npx tsc --noEmit` limpo.
    - Os testes ainda FALHAM em runtime (RED) neste ponto — esperado até o Task 3.
  </acceptance_criteria>
  <done>Migration escrita (número derivado na execução) e os dois arquivos de teste criados; tudo ainda RED, aguardando aprovação e push.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Aprovação humana — aplicar a migration da Agenda no banco Supabase de produção</name>
  <action>Pausar a execução. É a etapa de revisão antes de produção exigida pelo CLAUDE.md e já praticada em todas as fases que mexeram no banco (9, 10, 11, 12, 13). Apresentar ao dono do projeto, em linguagem simples, o que a migration faz no banco real, e só rodar o `supabase db push` (Task 3) depois do sinal "approved". Não prosseguir sem aprovação.</action>
  <what-built>
    Foi escrita uma migration que prepara o banco para a tela de Agenda. Ela é bem menor e mais inofensiva que a da fase anterior: **não cria tabela, não apaga nada, não altera nenhum dado e não muda nenhuma regra de permissão**. Ela faz só duas coisas:

    1. **Uma "consulta pronta" chamada Agenda.** É uma receita guardada no banco que, quando a tela pergunta "o que está pendente?", devolve numa lista só as tarefas de prospecção que ainda não foram concluídas E as visitas de pós-venda que ainda não aconteceram — já misturadas, já ordenadas da data mais antiga para a mais nova, e cada item vem marcado com de onde veio (prospecção ou visita) e de qual cliente é.

       Por que uma receita no banco e não duas buscas no navegador: se a tela buscasse as duas coisas separadamente e juntasse na hora, a regra de "o que está atrasado e o que vem primeiro" passaria a existir em dois lugares diferentes, que é exatamente como esse tipo de tela começa a mostrar números que não batem entre si. Assim existe uma fonte só.

    2. **Um índice na coluna de data das tarefas.** É o equivalente do índice remissivo de um livro: sem ele, o banco precisa percorrer tudo para ordenar por data. Não muda nada visível, só mantém a tela rápida conforme o volume cresce. O índice equivalente das visitas já foi criado na fase anterior.

    **Sobre quem vê o quê:** a receita não tem nenhuma regra de permissão escrita dentro dela — de propósito. Ela se apoia inteiramente nas travas que já existem hoje nas tabelas de clientes, tarefas e visitas. Na prática: um vendedor perguntando "o que está pendente?" recebe só os itens dos clientes dele; o supervisor recebe os do time inteiro. Isso é testado com dois vendedores diferentes antes de a tela ser construída.

    O que a migration NÃO faz: não cria tabela, não cria nem altera nenhuma permissão, não mexe em tarefas, visitas, clientes ou histórico, não apaga nem altera nenhum dado, não instala biblioteca nova e não cria nenhum processo que fique rodando sozinho em segundo plano.
  </what-built>
  <how-to-verify>
    1. Confirme que você está logado no Supabase CLI e com o projeto linkado (mesmo fluxo das migrations anteriores; se `supabase db push` pedir a senha do banco, é normal — tenha ela à mão).
    2. Se quiser conferir o arquivo: abra a migration nova em `supabase/migrations/` (é a de número mais alto, terminando em `_agenda_do_vendedor.sql`). Ele deve ter só duas instruções de verdade: um índice e uma função de leitura. Não deve haver nenhum `delete`, `drop table`, `truncate` ou `create policy` em lugar nenhum.
    3. Ponto que merece o seu olhar #1: **tarefas sem data marcada não vão aparecer na Agenda.** A Agenda é organizada em três blocos — Atrasado, Hoje, Próximos dias — e uma tarefa sem data não tem em qual bloco cair. Ela continua existindo normalmente na ficha do cliente, só não aparece nessa tela. Se você preferir que ela apareça (por exemplo num quarto bloco "sem data"), fale agora, porque isso muda a consulta.
    4. Ponto que merece o seu olhar #2: **a Agenda mostra TUDO que está pendente, inclusive coisas marcadas para daqui a muitos meses.** Não existe um corte do tipo "só os próximos 30 dias". Para o volume de hoje isso é o comportamento mais honesto (nada some da vista), mas se você quiser um corte de horizonte, é melhor decidir agora do que depois.
    5. Ponto que merece o seu olhar #3: **o supervisor vê a agenda do time inteiro por padrão**, com um filtro por vendedor na tela (isso vem no plano seguinte). Ou seja, ao abrir a Agenda, o supervisor vê tudo de todo mundo misturado até escolher um vendedor no filtro. Confirme que é o que você espera.
  </how-to-verify>
  <resume-signal>Digite "approved" para autorizar o `supabase db push` no banco de produção, ou descreva qualquer dúvida/ajuste antes de aplicar.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Aplicar a migration (supabase db push) e levar os testes a GREEN</name>
  <files>tests/agenda/agenda-rpc.test.ts, tests/agenda/rls-agenda.test.ts</files>
  <read_first>
    - o arquivo de migration criado no Task 1 (é o que será aplicado)
    - tests/helpers/supabase-test-clients.ts — conferir que `.env.local` tem `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` e `SUPABASE_SERVICE_ROLE_KEY` apontando para o projeto hospedado
    - .planning/STATE.md — Blockers/Concerns: o rate limit conhecido de `signInWithPassword` no projeto ao vivo (a suíte completa `npm test` NÃO termina limpa numa execução única por causa disso; rodar em lotes pequenos é o procedimento correto, e falha com a mensagem de rate limit não é sinal de regressão), o pin do Supabase CLI em `2.111.0` usado no push da fase anterior, e a linha de cliente com `estado='ZZ'` deixada de propósito na base
  </read_first>
  <action>
    Rodar `supabase db push` para aplicar a migration no projeto Supabase hospedado (mesmo fluxo das migrations 0002 a 0013; se a versão do CLI der problema de validação de schema, usar o pin `2.111.0` registrado no STATE.md). Se falhar por autenticação ou link, criar dinamicamente um gate de auth (pedir ao dono que rode `supabase login` / `supabase link` e forneça a senha do banco) e repetir — NÃO seguir adiante sem o push concluído: é o pré-requisito bloqueante dos planos 14-02, 14-03 e 14-04.

    Se o push acusar colisão de numeração, renomear o arquivo desta fase para o próximo número livre real e repetir — nunca renomear nem editar uma migration já aplicada.

    Depois do push, rodar os dois arquivos de teste contra o banco real e levá-los a GREEN. Se algum caso falhar, diagnosticar contra o SQL aplicado ANTES de mexer no teste. Suspeitos prováveis, em ordem:
      1. Erro de tipo na união ("UNION types ... cannot be matched"): os literais de texto não receberam cast explícito, ou as duas metades divergiram em ordem/quantidade de colunas. Corrigir o SQL.
      2. Caso `ordem` falhando: o `order by` ficou dentro de uma das metades em vez de no fim da união, ou apontou para a posição errada. Corrigir o SQL, nunca reordenar no teste.
      3. Caso `supervisor` devolvendo só os itens de um vendedor: a RLS de `visitas`/`tarefas` está correta, mas alguma metade da união pode ter ganhado um filtro de dono indevido no corpo da função. Remover o filtro — AGD-04 sai da RLS.
      4. Caso `vendedor` devolvendo item do outro vendedor: é o cenário MAIS GRAVE. Significa que a função ficou com elevação de privilégio ou que uma policy foi afrouxada. Parar, corrigir a função (nunca o teste) e reportar no SUMMARY.
      5. Caso `contagem` sem número: a combinação `head`/`count` do supabase-js não trouxe contagem para função que retorna tabela. Confirmar com uma chamada isolada antes de concluir; se for restrição real da biblioteca, registrar no SUMMARY como achado (o plano 14-02 então usa o tamanho da lista completa como fonte da contagem) e ajustar SÓ este caso do teste para afirmar a fonte efetivamente disponível.

    Corrigir SEMPRE pelo lado da migration/SQL, criando um arquivo novo se o push já tiver tido sucesso — nunca editando uma migration já aplicada, e nunca relaxando uma asserção de segurança para o teste passar.

    Rodar por último a regressão em lote pequeno (não `npm test` inteiro, por causa do rate limit registrado no STATE.md): as suítes de funil, de visitas e de RLS de dashboard, que são as que poderiam ser afetadas por um índice novo em `tarefas` ou por qualquer mexida acidental nas tabelas de base.

    Este task não altera código de aplicação: só aplica a migration e valida.
  </action>
  <verify>
    <automated>npx vitest run tests/agenda/ && npx vitest run tests/clientes/funil-status.test.ts tests/clientes/funil-constraints.test.ts tests/clientes/rls-visitas.test.ts tests/dashboard/rls-dashboard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` aplicou a migration no projeto hospedado sem erro.
    - `npx vitest run tests/agenda/agenda-rpc.test.ts` passa inteiro: origem/título das duas frentes, identificação do cliente e do responsável, ordenação por data crescente decidida no SQL, exclusão de itens concluídos, exclusão de tarefa sem data, e contagem coerente com o tamanho da lista.
    - `npx vitest run tests/agenda/rls-agenda.test.ts` passa inteiro: Vendedor A e Vendedor B não enxergam itens um do outro, o Supervisor enxerga os dois, e o não autenticado não enxerga nenhum.
    - As suítes de regressão de funil, visitas e RLS de dashboard continuam verdes.
    - Nenhum arquivo de migration já aplicado anteriormente foi editado.
    - O SUMMARY registra explicitamente qual fonte de contagem ficou provada (contagem exata da própria RPC, ou tamanho da lista completa), porque o plano 14-02 depende dessa resposta.
  </acceptance_criteria>
  <done>Função `agenda_do_vendedor()` aplicada em produção com aprovação humana; formato do feed unificado provado por teste e AGD-04 provado por teste cross-vendedor contra o banco real.</done>
</task>

</tasks>

<artifacts_produced>
## Artefatos deste plano (parte 1 de 4 — camada de banco)

| Símbolo / Arquivo | Tipo | Status |
|-------------------|------|--------|
| `supabase/migrations/{numero_derivado}_agenda_do_vendedor.sql` | Migration nova (aditiva, só leitura) | Criada (Task 1), aplicada (Task 3) |
| `agenda_do_vendedor()` | RPC `language sql stable`, sem elevação de privilégio; devolve `origem, item_id, cliente_id, razao_social, responsavel, responsavel_nome, titulo, data` | Criada |
| `idx_tarefas_data_conclusao` | Índice em `tarefas (data_conclusao)` | Criado |
| `tests/agenda/agenda-rpc.test.ts` | Teste de integração novo (formato e ordenação do feed) | Criado (Task 1), GREEN (Task 3) |
| `tests/agenda/rls-agenda.test.ts` | Teste de RLS novo (AGD-04 cross-vendedor) | Criado (Task 1), GREEN (Task 3) |

Contrato para os planos seguintes: `agenda_do_vendedor()` é a ÚNICA fonte de leitura da Agenda e a ÚNICA autoridade de ordenação. Nenhum plano posterior pode reordenar as linhas recebidas nem buscar `tarefas`/`visitas` separadamente.

Nenhuma dependência npm nova. Nenhuma alteração em tabela, policy, trigger ou dado.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário autenticado (Vendedor) → `agenda_do_vendedor()` | Função nova exposta pelo PostgREST a qualquer autenticado; ela lê de três tabelas com RLS (`clientes`, `tarefas`, `visitas`) e de `profiles`. É a única superfície nova da fase. |
| usuário autenticado → `profiles` via join de nome do vendedor | `profiles` tem policy de SELECT aberta desde a 0001; o nome do vendedor sai por esse caminho. |
| migration → banco de produção | DDL aplicado contra dados reais em uso. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Information Disclosure / Elevation of Privilege | `agenda_do_vendedor()` com elevação de privilégio | critical | mitigate | A função nasce sem cláusula de elevação, espelhando toda função `dashboard_*`; verificação mecânica no Task 1 recusa o arquivo se a cláusula aparecer, e o caso `vendedor` de `rls-agenda.test.ts` falha alto se um vendedor enxergar item de cliente alheio. |
| T-14-02 | Information Disclosure | filtro de dono ou checagem de papel escrito DENTRO da função | high | mitigate | Proibição explícita + verificação mecânica que recusa o arquivo se a função de checagem de papel ou a de identidade do usuário aparecerem no corpo. Um filtro manual ali criaria uma segunda fronteira de autorização fora da RLS, que é justamente o que o CLAUDE.md proíbe; o caso `supervisor` prova que a visão do time continua inteira. |
| T-14-03 | Information Disclosure | vazamento de item de outro vendedor pela metade de `visitas` da união | high | mitigate | As quatro policies de `visitas` (criadas e provadas na fase anterior) continuam valendo dentro da união, porque o Postgres aplica RLS nas tabelas de base antes do `union all`. Provado de novo aqui pelo caso `vendedor`, agora pelo caminho da Agenda e não pelo acesso direto à tabela. |
| T-14-04 | Information Disclosure | nome de vendedor visível a qualquer autenticado pelo join com `profiles` | low | accept | Comportamento pré-existente desde a 0001 (`profiles` tem policy de SELECT aberta), já explorado por `dashboard_comparativo_vendedor` e por `lib/equipe/membros.ts`. Nada novo é exposto: as LINHAS continuam restritas pela RLS de `clientes`, só o nome de quem já é dono de uma linha visível aparece. Não adicionar checagem de papel para "corrigir" isso — seria um filtro fora da fronteira real. |
| T-14-05 | Tampering | migration aplicada em produção sem revisão humana | medium | mitigate | Checkpoint bloqueante (Task 2) antes do `supabase db push`, exigido pelo CLAUDE.md e praticado nas Fases 9-13. |
| T-14-06 | Tampering (trilha de auditoria) | `historico` | high | mitigate | A migration não cria policy alguma, não recria trigger alguma e não escreve em lugar nenhum — verificado mecanicamente. `historico` continua escrito só pelas triggers já existentes. |
| T-14-07 | Denial of Service | varredura sem índice ao ordenar o feed por data | low | mitigate | `idx_tarefas_data_conclusao` criado aqui; o índice equivalente de `visitas` já existe. Os filtros de "ainda pendente" são aplicados ANTES da união, mantendo o conjunto de trabalho proporcional ao que está em aberto. Sem `limit` de propósito (a tela mostra contagens ao vivo das três seções); ponto de revisita levado ao dono no checkpoint. |
| T-14-08 | Denial of Service | processo de fundo / agendador introduzido para pré-calcular atraso | low | mitigate | Proibição explícita + verificação mecânica; o atraso é calculado na leitura, nunca gravado nem pré-computado (Anti-Pattern 1 da research). |
| T-14-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` neste plano nem nesta fase inteira — zero pacotes novos, nada a auditar. Sem checkpoint de legitimidade aplicável. |
</threat_model>

<verification>
Checks rodados pelo executor ao final do plano:
- `git diff --name-only HEAD -- supabase/migrations/` vazio (nenhuma migration existente editada).
- `npx tsc --noEmit` limpo.
- Check estrutural do SQL (Task 1), ignorando comentários: exatamente uma função `stable` `language sql`, exatamente um índice (o de `tarefas`), exatamente um `union all`, filtros de pendência dentro de cada metade, `order by` presente, colunas de saída obrigatórias presentes; ausência de cláusula de elevação de privilégio, de checagem de papel, de filtro manual de dono, de DDL de tabela/policy/trigger, de manipulação de dado, de agendador e de seleção aberta de colunas.
- Check estrutural dos testes (Task 1): palavras-chave obrigatórias nos nomes dos casos e uso de sessões reais.
- `npx vitest run tests/agenda/` verde contra o banco real, pós-push.
- Regressão em lote pequeno (funil, visitas, RLS de dashboard) verde — a suíte completa não é rodada de uma vez por causa do rate limit de login registrado no STATE.md.
- Checkpoint humano (Task 2): dono aprovou aplicar a migration em produção antes do push.
</verification>

<success_criteria>
- AGD-01 (banco): uma única chamada devolve tarefas de prospecção pendentes e visitas pendentes unificadas, com coluna de origem, título, cliente e data, ordenadas por data crescente; itens concluídos e tarefas sem data ficam de fora.
- AGD-04: Vendedor A não enxerga item de cliente do Vendedor B (e vice-versa), Supervisor enxerga os dois, não autenticado não enxerga nenhum — tudo provado contra o banco real, sem nenhuma linha de checagem de papel dentro da função.
- A função não eleva privilégio e o projeto continua com exatamente três exceções de privilégio elevado.
- Existe índice de data em `tarefas`; o de `visitas` não foi recriado.
- A contagem de pendentes lida pela mesma função bate exatamente com o tamanho da lista — contrato do selo do menu.
- Migration aplicada no banco hospedado com aprovação humana prévia; suítes de regressão verdes.
</success_criteria>

<output>
Create `.planning/phases/14-agenda-unificada/14-01-SUMMARY.md` when done.
</output>
