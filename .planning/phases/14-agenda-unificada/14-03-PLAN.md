---
phase: 14-agenda-unificada
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - components/agenda/AgendaItemRow.tsx
  - components/agenda/AgendaList.tsx
  - app/(app)/agenda/page.tsx
  - tests/agenda/agenda-item-row.test.tsx
  - tests/agenda/agenda-list.test.tsx
autonomous: false
requirements: [AGD-01, AGD-03, AGD-05]

must_haves:
  truths:
    - "AGD-01: a rota `/agenda` existe dentro da área protegida e mostra tarefas de prospecção e visitas numa lista só, cada linha identificando o cliente pela razão social e a origem por um selo — `Prospecção` para tarefa, `Visita` para visita"
    - "AGD-01: a tela renderiza exatamente três seções, sempre nesta ordem: Atrasado, Hoje, Próximos dias; cada cabeçalho mostra a contagem viva da própria seção, e uma seção sem nenhum item é omitida inteira (nem cabeçalho, nem espaço vazio)"
    - "AGD-01: a ordem das linhas dentro de cada seção é exatamente a ordem recebida da leitura — a tela nunca reordena nem refiltra por conta própria"
    - "AGD-03: linhas da seção Atrasado ganham borda esquerda vermelha e um triângulo de alerta âmbar com tooltip, usando as MESMAS classes utilitárias que o card do kanban já usa hoje para o mesmo sinal; linhas de Hoje e Próximos dias não têm borda colorida nem triângulo"
    - "AGD-05: quando o usuário é Supervisor, existe um controle de Vendedor no cabeçalho da tela, com a opção `Todos os vendedores` mais um item por vendedor presente na agenda carregada; escolher um vendedor estreita as linhas E as contagens das três seções, sem nova ida ao servidor"
    - "AGD-05: quando o usuário é Vendedor, o controle de filtro não é renderizado — e isso é conveniência de tela, não permissão: a RLS já entregou só os itens dele"
    - "A tela tem estado de carregamento (blocos de esqueleto, sem texto), estado de erro com a copy exata do UI-SPEC e um botão de tentar de novo que refaz a leitura, e dois estados de vazio distintos: agenda inteira vazia e resultado vazio depois de filtrar por um vendedor"
    - "Clicar (ou acionar pelo teclado) uma linha abre a ficha do cliente já existente, no mesmo padrão controlado que o kanban usa; salvar ou apagar pela ficha refaz a leitura da agenda"
    - "Fase de leitura pura respeitada: nenhum botão de concluir, nenhum campo de resumo, nenhuma sugestão de próxima data e nenhuma escrita em lugar nenhum desta tela"
    - "Nenhum componente shadcn novo é instalado e nenhuma dependência npm nova é adicionada — Card, Badge, Select, Tooltip, Skeleton e Button já existem no projeto"
  artifacts:
    - "components/agenda/AgendaItemRow.tsx — linha apresentacional de um item da agenda"
    - "components/agenda/AgendaList.tsx — Client Component que faz a leitura, reparte em seções, oferece o filtro de vendedor e abre a ficha do cliente"
    - "app/(app)/agenda/page.tsx — Server Component da rota, com guarda de sessão e leitura dos catálogos que a ficha do cliente precisa"
    - "tests/agenda/agenda-item-row.test.tsx (novo)"
    - "tests/agenda/agenda-list.test.tsx (novo)"
  key_links:
    - "a rota vive em `app/(app)/agenda/page.tsx`, DENTRO do grupo `(app)` — fora dele a página perderia a guarda de sessão e o menu lateral"
    - "`AgendaList` importa `agruparAgenda`/`filtrarPorVendedor`/`vendedoresDaAgenda` de `lib/agenda/itens.ts` e não reimplementa nenhuma dessas decisões"
    - "as classes de atraso são copiadas literalmente de `ClienteCard.tsx` — o critério de sucesso 3 exige o MESMO padrão visual do kanban, então classe equivalente-mas-diferente não atende"
    - "o `Select` do filtro precisa receber a lista de opções também pela prop `items`, senão o valor selecionado não é exibido (bug real já corrigido no filtro de período do dashboard)"
    - "formatação de data com `format(parseISO(item.data), \"dd/MM\")` — converter a string do banco com o construtor de data cru mostra o dia anterior no horário de São Paulo"
---

<objective>
Construir a tela da Agenda: a rota `/agenda`, o componente de lista que lê, reparte em Atrasado / Hoje / Próximos dias e oferece o filtro de vendedor ao Supervisor, e a linha apresentacional de cada item com o selo de origem e o destaque de atraso idêntico ao do kanban. Mais os dois arquivos de teste de renderização que provam a estrutura, a copy e os estados.

Purpose: é a entrega visível da fase — a tela que responde "o que eu preciso fazer hoje?". Toda a decisão de negócio já foi resolvida nos planos anteriores (o SQL decide a ordem, o arquivo puro decide o que é atrasado), então este plano é deliberadamente apresentacional: se aparecer regra de negócio nova aqui, é sinal de que ela está sendo duplicada.

Output: a rota `/agenda` funcionando ponta a ponta contra o banco real, com verificação humana no navegador.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-agenda-unificada/14-UI-SPEC.md
@components/clientes/ClienteCard.tsx
@.planning/phases/14-agenda-unificada/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: `AgendaItemRow` — a linha apresentacional, com selo de origem e destaque de atraso idêntico ao do kanban</name>
  <files>components/agenda/AgendaItemRow.tsx, tests/agenda/agenda-item-row.test.tsx</files>
  <read_first>
    - components/clientes/ClienteCard.tsx (já no contexto) — é o molde literal desta linha. Extrair e reusar VERBATIM: (a) o mapa de borda esquerda por status de tarefa, em especial a entrada que produz `border-l-4 border-l-red-500`; (b) o bloco do triângulo de alerta — `TriangleAlert` com `size-3.5 shrink-0 text-amber-500` dentro de `Tooltip`/`TooltipTrigger`/`TooltipContent`, com `aria-label` no trigger e `onClick={(event) => event.stopPropagation()}` para que o tooltip não dispare a abertura da linha; (c) o padrão de linha clicável: `Card size="sm"` com `role="button"`, `tabIndex={0}`, `onClick` e `onKeyDown` tratando Enter e barra de espaço com `preventDefault`; (d) o `CardTitle` com `truncate text-base leading-tight font-semibold` e `title={...}` para o nome longo; (e) o uso de `Badge variant="outline"` e o espaçamento `gap-1.5`/`gap-2`/`px-3` internos.
    - .planning/phases/14-agenda-unificada/14-UI-SPEC.md (já no contexto) — seção "Item row": as quatro linhas de conteúdo, a copy exata dos dois selos, a escolha de ícones, a regra de borda por seção, o formato de data e o texto do tooltip de atraso, e a regra da linha do responsável.
    - lib/agenda/itens.ts — o tipo `AgendaItem` (campos exatos e quais são anuláveis).
    - components/ui/badge.tsx — as variantes disponíveis (`default`, `secondary`, `outline`, `destructive`, `ghost`, `link`) para confirmar que `outline` e `secondary` existem com esses nomes.
    - tests/dashboard/comparativo-vendedor-table.test.tsx — o molde de teste de renderização deste projeto: o comentário `// @vitest-environment jsdom` no topo, o wrapper `TooltipProvider` quando o componente usa tooltip, e o uso de `screen`/`within` do testing-library.
  </read_first>
  <action>
    Criar `components/agenda/AgendaItemRow.tsx` — Client Component (`"use client"`), puramente apresentacional: nenhuma leitura de dados, nenhum estado além do que o React exige, nenhuma decisão de negócio.

    Props: `{ item: AgendaItem; atrasado: boolean; showResponsavel: boolean; onOpen?: () => void }`.
      - `atrasado` chega PRONTO de quem renderiza (a seção em que a linha está). Este componente NÃO calcula atraso e NÃO importa `bucketDoItem` — a autoridade disso é o `lib/agenda/itens.ts` e ela é aplicada uma vez, na lista.

    Raiz: `Card size="sm"` com `role="button"`, `tabIndex={0}`, `onClick={onOpen}` e `onKeyDown` tratando Enter e barra de espaço — o mesmo trecho do `ClienteCard`, incluindo o `preventDefault`. Aplicar `cursor-pointer` quando houver `onOpen`. Classe de espaçamento interno `gap-1.5`, como o `ClienteCard`.

    Borda de atraso: quando `atrasado` for verdadeiro, acrescentar `border-l-4 border-l-red-500` — exatamente essas duas classes, copiadas do mapa de borda do `ClienteCard`. Quando falso, nenhuma classe de borda colorida. O critério de sucesso 3 da fase exige o MESMO padrão visual já usado no kanban, então usar uma classe equivalente-mas-diferente (outro tom de vermelho, outra espessura) não atende.

    Conteúdo, nesta ordem:
      1. **Título + selo de origem** numa linha só: a razão social do cliente em `truncate text-base leading-tight font-semibold` com `title` para o nome inteiro, e à direita o selo de origem:
         - origem de prospecção → `Badge variant="outline"` com o ícone `ClipboardCheck` e o texto `Prospecção`
         - origem de visita → `Badge variant="secondary"` com o ícone `Repeat` e o texto `Visita`
         Os dois selos levam `shrink-0`. Os textos são a copy travada no UI-SPEC — com acento, exatamente assim.
      2. **Título do item** (`item.titulo`) em `text-sm text-muted-foreground`. Para prospecção esse valor é o nome do tipo de tarefa que o Supervisor cadastra (Visitar, Mandar mensagem...); para visita é o literal que a RPC devolve. O componente só exibe o que recebeu, nunca decide esse texto.
      3. **Data**: `format(parseISO(item.data), "dd/MM")` do `date-fns`, em `text-sm text-muted-foreground`. JAMAIS converter `item.data` com o construtor de data cru do JavaScript — a string vem do banco no formato ano-mês-dia e o construtor cru a interpreta em fuso zero, mostrando o dia anterior no horário de São Paulo (é a Pitfall 1 da research, e vale para exibição também).
         Quando `atrasado` for verdadeiro, logo depois da data, o bloco de triângulo de alerta copiado do `ClienteCard`: `TriangleAlert` com `size-3.5 shrink-0 text-amber-500`, dentro de `Tooltip`/`TooltipTrigger`/`TooltipContent`, com o `onClick` que interrompe a propagação, e com o conteúdo e o `aria-label` valendo `Atrasado desde {dd/MM}.` (a mesma data formatada, seguida de ponto final).
      4. **Responsável**: quando `showResponsavel` for verdadeiro E `item.responsavelNome` existir, uma linha `truncate text-sm text-muted-foreground` com o nome. Caso contrário, não renderizar nada (não renderizar espaço vazio).

    PROIBIDO neste componente, porque pertence à Fase 15 e o UI-SPEC proíbe explicitamente antecipar: qualquer botão de concluir item, qualquer campo de resumo, qualquer sugestão ou seletor de próxima data, qualquer chamada de Server Action.
    <!-- planner-discipline-allow: Concluir -->
    <!-- planner-discipline-allow: resumo -->

    Criar `tests/agenda/agenda-item-row.test.tsx` com `// @vitest-environment jsdom` no topo e `TooltipProvider` em volta do componente. Casos exigidos — palavras-chave nos nomes dos `it()`:
      - **`prospeccao`**: item de prospecção renderiza a razão social, o `titulo` recebido e um selo com o texto `Prospecção`.
      - **`visita`**: item de visita renderiza um selo com o texto `Visita`.
      - **`atrasado`**: com `atrasado` verdadeiro, o cartão tem as classes `border-l-4` e `border-l-red-500`, e existe um elemento acessível cujo rótulo começa com `Atrasado desde` — inspecionar via `container.querySelector` ou pelo `aria-label`, não por snapshot.
      - **`normal`**: com `atrasado` falso, NÃO existe a classe `border-l-red-500` e NÃO existe nenhum rótulo contendo `Atrasado desde`.
      - **`data`**: um item com data `2026-01-31` renderiza o texto `31/01` (prova que a formatação não desloca o dia).
      - **`responsavel`**: com `showResponsavel` falso, o nome do responsável não aparece; com verdadeiro, aparece.
      - **`teclado`**: com `onOpen` fornecido, disparar Enter no cartão chama `onOpen` uma vez.
  </action>
  <verify>
    <automated>npx vitest run tests/agenda/agenda-item-row.test.tsx && npx tsc --noEmit && node -e "const fs=require('fs');const c=fs.readFileSync('components/agenda/AgendaItemRow.tsx','utf8');for(const t of ['use client','border-l-4','border-l-red-500','text-amber-500','TriangleAlert','size-3.5','Prospecção','Visita','Atrasado desde','parseISO','stopPropagation','role=\"button\"','tabIndex'])if(!c.includes(t))throw new Error('faltou em AgendaItemRow.tsx: '+t);if(/new\s+Date\s*\(\s*[a-z_]/i.test(c))throw new Error('Pitfall 1: usar parseISO, nunca construir data a partir da string do banco');if(/bucketDoItem/.test(c))throw new Error('a linha nao decide atraso: recebe pronto de quem renderiza');if(/Concluir|resumo|proxima_data|próxima data/i.test(c))throw new Error('fase de leitura pura: sem fluxo de conclusao (Fase 15)');if(/actions\/agenda|use server/.test(c))throw new Error('a linha e apresentacional: nao chama Server Action');const t=fs.readFileSync('tests/agenda/agenda-item-row.test.tsx','utf8');if(!t.includes('@vitest-environment jsdom'))throw new Error('teste de render precisa do comentario de ambiente jsdom');const names=(t.match(/\bit\(\s*[\"'][^\"']+/g)||[]).map(s=>s.toLowerCase());for(const k of ['prospeccao','visita','atrasado','normal','data','responsavel','teclado'])if(!names.some(n=>n.includes(k)))throw new Error('agenda-item-row.test.tsx: falta caso com a palavra '+k);console.log('OK AgendaItemRow')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/agenda/AgendaItemRow.tsx` existe, é Client Component, e contém literalmente as classes `border-l-4`, `border-l-red-500`, `size-3.5` e `text-amber-500` (mesmas do `ClienteCard`).
    - Os textos `Prospecção`, `Visita` e `Atrasado desde` aparecem com a grafia exata do UI-SPEC.
    - O componente usa `parseISO` para formatar a data e não constrói data a partir da string do banco com o construtor cru.
    - O componente não importa `bucketDoItem` (não decide atraso), não chama Server Action e não contém nenhum elemento do fluxo de conclusão da Fase 15.
    - O cartão é acionável por teclado (`role`, `tabIndex`, Enter/espaço) e o tooltip interrompe a propagação do clique.
    - `tests/agenda/agenda-item-row.test.tsx` existe, roda em ambiente de DOM e tem casos cujos nomes contêm `prospeccao`, `visita`, `atrasado`, `normal`, `data`, `responsavel` e `teclado`; todos passam.
    - `npx tsc --noEmit` limpo.
  </acceptance_criteria>
  <done>Linha da agenda pronta e testada: selo de origem por frente, destaque de atraso com as classes idênticas às do kanban, data sem deslocamento de fuso e acionável por teclado.</done>
</task>

<task type="auto">
  <name>Task 2: `AgendaList` + a rota `/agenda`</name>
  <files>components/agenda/AgendaList.tsx, app/(app)/agenda/page.tsx, tests/agenda/agenda-list.test.tsx</files>
  <read_first>
    - .planning/phases/14-agenda-unificada/14-UI-SPEC.md (já no contexto) — seções "Page structure", "Vendedor filter", "Copywriting Contract" (título, subtítulo, cabeçalhos das três seções, rótulo e placeholder do filtro, os dois estados de vazio, o estado de erro e o botão de tentar de novo) e "What this phase does NOT render".
    - components/dashboard/ComparativoVendedorTable.tsx — o molde exato do padrão de leitura sob demanda deste projeto: o tipo `FetchState` como união discriminada, o `useEffect` com `setState` síncrono no corpo (com o comentário de supressão de lint já usado no projeto), a guarda `cancelled`, o contador `reloadKey` bumpado pelo botão de tentar de novo, e a estrutura carregando → erro → vazio → conteúdo.
    - components/clientes/KanbanBoard.tsx — o padrão controlado da ficha do cliente: o estado `selectedClienteId`, a função `handleOpenCliente`, e como `ClienteDetailSheet` recebe `clienteId`/`open`/`onOpenChange`/`isSupervisor`/`categoriaOptions`/`produtoOptions`/`vendedorOptions`/`onSaved`/`onDeleted`. Extrair a LISTA EXATA de props obrigatórias, porque a página nova precisa fornecer todas.
    - components/clientes/FiltersPopover.tsx — o trecho do `Select` de Vendedor (por volta das linhas 217-244): a constante sentinela usada como valor de "sem filtro", a conversão de/para `null` no `onValueChange`, e a composição `Select`/`SelectTrigger`/`SelectValue`/`SelectContent`/`SelectItem`.
    - components/dashboard/PeriodoFilter.tsx — atenção especial: o `Select` deste projeto (Base UI) precisa receber também a prop `items` com a lista de opções, senão o valor selecionado não é exibido. É um bug real já corrigido no dashboard; o filtro novo tem que nascer com a prop.
    - app/(app)/dashboard/page.tsx — o molde da página: guarda de sessão com `redirect("/login")`, releitura do `role` do perfil só para passar `isSupervisor` como prop, e o comentário explicando que a guarda real já está no layout.
    - app/(app)/clientes/page.tsx — de onde vêm `getCategoriasAtivas()`/`getProdutosAtivos()` e como a lista de membros do time é buscada quando o usuário é Supervisor (a ficha do cliente precisa dessas três listas).
    - lib/agenda/itens.ts — assinaturas de `agruparAgenda`, `filtrarPorVendedor` e `vendedoresDaAgenda`.
    - app/actions/agenda.ts — assinatura e formato de resultado de `getAgendaAction()`.
  </read_first>
  <action>
    (A) Criar `components/agenda/AgendaList.tsx` — Client Component. Ele é o dono do cabeçalho da tela inteiro (título, subtítulo e filtro), do mesmo jeito que `DashboardClient` é dono do cabeçalho do dashboard.

    Props: `{ isSupervisor: boolean; categoriaOptions: { id: string; nome: string }[]; produtoOptions: { id: string; nome: string }[]; vendedorOptions: { id: string; nome: string }[] }`. As três listas de catálogo existem só para alimentar a ficha do cliente; nenhuma delas alimenta o filtro da agenda.

    Estado: `FetchState` como união discriminada (`carregando` / `erro` / `pronto` com os itens), `reloadKey` numérico, `vendedorFiltroId: string | null`, `selectedClienteId: string | null`.

    Leitura: `useEffect` dependente de `reloadKey`, marcando carregando de forma síncrona no corpo do efeito (com o mesmo comentário de supressão de lint que o `ComparativoVendedorTable` já usa), chamando `getAgendaAction()`, com guarda `cancelled` no cleanup. Em erro da action, ir para o estado de erro. Uma chamada só — nada de buscar `tarefas` ou `visitas` por fora.

    Derivações (todas com as funções puras do plano 14-02, nunca reimplementadas aqui):
      - `vendedorOpcoesDoFiltro = vendedoresDaAgenda(itens)` — as opções do filtro saem dos itens carregados, não das props de catálogo.
      - `itensFiltrados = filtrarPorVendedor(itens, vendedorFiltroId)`
      - `secoes = agruparAgenda(itensFiltrados)`
      - `showResponsavel = isSupervisor && vendedorFiltroId === null` — o nome do vendedor aparece na linha só para o Supervisor e só enquanto ele está vendo todos; escolhido um vendedor específico, repetir o nome em toda linha é ruído. (Resolução explícita da frase do UI-SPEC "Supervisor view only, or when the Vendedor filter is set to Todos os vendedores".)

    Layout:
      1. **Cabeçalho**, uma linha com o bloco de texto à esquerda e o filtro à direita: título `Agenda` em `text-xl font-semibold`, subtítulo `O que você precisa fazer, do mais urgente ao mais distante.` em `text-sm text-muted-foreground`.
      2. **Filtro de Vendedor**, renderizado somente quando `isSupervisor` for verdadeiro: um `Select` autônomo (sem popover em volta — a agenda tem uma dimensão de filtro só), com `Label` `Vendedor`, a opção sentinela de "sem filtro" com o texto `Todos os vendedores` e um item por entrada de `vendedorOpcoesDoFiltro`. Usar a mesma constante sentinela e a mesma conversão de/para `null` do `FiltersPopover`. Passar também a prop `items` com a lista completa de opções (incluindo a sentinela), senão o valor escolhido não aparece no gatilho.
      3. **Estado de carregando**: blocos `Skeleton`, sem texto nenhum.
      4. **Estado de erro**: o texto `Não foi possível carregar sua agenda. Tente novamente.` e um `Button variant="outline"` com o rótulo `Tentar novamente` que incrementa `reloadKey`.
      5. **Estado pronto**: as três seções na ordem fixa Atrasado → Hoje → Próximos dias, separadas por `gap-6`. Cada seção só é renderizada se tiver ao menos um item — seção vazia não gera cabeçalho nem espaço reservado. Cabeçalho de cada seção em `text-xs font-semibold tracking-wide text-muted-foreground uppercase` com a contagem viva entre parênteses: `Atrasado ({n})`, `Hoje ({n})` e `Próximos dias ({n})`. Corpo de cada seção em `flex flex-col gap-2`, uma `AgendaItemRow` por item, com `atrasado` verdadeiro apenas na seção Atrasado, `showResponsavel` conforme derivado acima, e `onOpen` abrindo a ficha do cliente daquele item.
      6. **Vazio total** (nenhum item em nenhuma seção e nenhum filtro aplicado): título `Sua agenda está em dia` e corpo `Nenhuma tarefa de prospecção ou visita pendente no momento.`
      7. **Vazio por filtro** (há itens carregados, mas o vendedor escolhido não tem nenhum): `Nenhum item pendente para {nome do vendedor}.` — com o nome vindo da opção selecionada.
      8. **Ficha do cliente**: `ClienteDetailSheet` controlado por `selectedClienteId`, recebendo `isSupervisor` e as três listas de catálogo vindas das props. Em `onSaved` e em `onDeleted`, incrementar `reloadKey` para a agenda refletir a mudança (um cliente apagado ou com dados alterados pode mudar o que aparece aqui).

    PROIBIDO nesta tela: qualquer botão de concluir item, campo de resumo, seletor ou sugestão de próxima data, e qualquer chamada de escrita — tudo isso é da Fase 15 e o UI-SPEC proíbe antecipar.
    <!-- planner-discipline-allow: Concluir -->
    <!-- planner-discipline-allow: resumo -->

    (B) Criar `app/(app)/agenda/page.tsx` — Server Component, dentro do grupo de rotas `(app)` (fora dele a página perde a guarda de sessão e o menu lateral). Molde: `app/(app)/dashboard/page.tsx` para a guarda e a releitura do papel; `app/(app)/clientes/page.tsx` para as três leituras de catálogo.
      - Obter o usuário; sem usuário, `redirect("/login")`.
      - Ler `role` do perfil e derivar `isSupervisor`.
      - Ler as categorias ativas e os produtos ativos; quando `isSupervisor`, ler os membros do time e mapear para o formato `{ id, nome }` que a ficha do cliente espera (quando não for Supervisor, lista vazia — é o que o `KanbanBoard` já faz).
      - Renderizar um contêiner com o mesmo espaçamento de página das outras telas e, dentro dele, `<AgendaList ... />` com as quatro props. A página NÃO renderiza título nem filtro — quem faz isso é o `AgendaList`.
      - Nenhuma chamada a `getAgenda()` aqui: a leitura acontece no cliente, via Server Action, porque o componente precisa refazê-la ao trocar filtro/tentar de novo/salvar a ficha.

    (C) Criar `tests/agenda/agenda-list.test.tsx`, com `// @vitest-environment jsdom` no topo. Mockar `@/app/actions/agenda` (só `getAgendaAction`) e mockar `@/lib/supabase/client` (a ficha do cliente e componentes internos criam o cliente de navegador; seguir o padrão de mock já usado em `tests/clientes/filters-popover.test.tsx`). Envolver em `TooltipProvider`. Casos exigidos — palavras-chave nos nomes dos `it()`:
      - **`carregando`**: no primeiro render, nem a copy de erro nem a de vazio aparecem (estado de esqueleto).
      - **`secoes`**: com itens de datas passada, de hoje e futura, os três cabeçalhos aparecem com as contagens certas e na ordem Atrasado → Hoje → Próximos dias (conferir a ORDEM comparando as posições no DOM, não só a presença).
      - **`omite`**: com itens só de hoje, os cabeçalhos de Atrasado e de Próximos dias não existem no documento.
      - **`vazio`**: com lista vazia, aparece `Sua agenda está em dia`.
      - **`erro`**: com a action devolvendo erro, aparece `Não foi possível carregar sua agenda. Tente novamente.` e um botão `Tentar novamente`; clicar nele chama a action de novo.
      - **`filtro`**: com `isSupervisor` verdadeiro e itens de dois vendedores diferentes, o rótulo `Vendedor` e a opção `Todos os vendedores` aparecem, e o controle oferece uma opção por vendedor presente na agenda.
      - **`vendedor`**: com `isSupervisor` falso, nem o rótulo `Vendedor` nem a opção `Todos os vendedores` são renderizados.
      Para fixar "hoje" nos casos que dependem de data, montar as datas dos itens a partir do relógio real do teste com `format(addDays(new Date(), n), "yyyy-MM-dd")` do `date-fns` (n negativo para atrasado, 0 para hoje, positivo para próximos) em vez de datas literais fixas — assim o teste não apodrece com o passar do tempo.
      Se a abertura do menu suspenso do `Select` se mostrar inviável no ambiente de DOM simulado, NÃO forçar: o estreitamento por vendedor já está provado por teste unitário em `tests/agenda/itens.test.ts`; aqui basta provar que o controle existe com as opções certas. Registrar a limitação no SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run tests/agenda/agenda-list.test.tsx tests/agenda/agenda-item-row.test.tsx && npx tsc --noEmit && npm run lint && node -e "const fs=require('fs');const l=fs.readFileSync('components/agenda/AgendaList.tsx','utf8');for(const t of ['use client','getAgendaAction','agruparAgenda','filtrarPorVendedor','vendedoresDaAgenda','AgendaItemRow','ClienteDetailSheet','Skeleton','reloadKey','cancelled','Agenda','O que você precisa fazer, do mais urgente ao mais distante.','Atrasado (','Hoje (','Próximos dias (','Sua agenda está em dia','Nenhuma tarefa de prospecção ou visita pendente no momento.','Nenhum item pendente para','Não foi possível carregar sua agenda. Tente novamente.','Tentar novamente','Todos os vendedores','items='])if(!l.includes(t))throw new Error('faltou em AgendaList.tsx: '+t);if(/from\s*\(\s*[\"'](tarefas|visitas)/.test(l))throw new Error('Anti-Pattern 4: a agenda vem de uma leitura so');if(/differenceInCalendarDays|bucketDoItem\s*\(/.test(l))throw new Error('a decisao de atraso mora em lib/agenda/itens.ts, nao aqui');if(/\.sort\s*\(/.test(l))throw new Error('a ordem e decidida no SQL: a tela nao reordena');if(/Agenda \(0\)/.test(l))throw new Error('nunca renderizar contagem zero');if(/Concluir|proxima_data|próxima data/i.test(l))throw new Error('fase de leitura pura: sem fluxo de conclusao (Fase 15)');const p=fs.readFileSync('app/(app)/agenda/page.tsx','utf8');for(const t of ['AgendaList','redirect','getUser','isSupervisor'])if(!p.includes(t))throw new Error('faltou em app/(app)/agenda/page.tsx: '+t);if(/getAgenda\s*\(/.test(p))throw new Error('a leitura da lista acontece no cliente via Server Action, nao na pagina');if(/:\s*any\b/.test(l+p))throw new Error('TypeScript estrito: sem any');const t=fs.readFileSync('tests/agenda/agenda-list.test.tsx','utf8');if(!t.includes('@vitest-environment jsdom'))throw new Error('teste de render precisa do comentario de ambiente jsdom');const names=(t.match(/\bit\(\s*[\"'][^\"']+/g)||[]).map(s=>s.toLowerCase());for(const k of ['carregando','secoes','omite','vazio','erro','filtro','vendedor'])if(!names.some(n=>n.includes(k)))throw new Error('agenda-list.test.tsx: falta caso com a palavra '+k);console.log('OK AgendaList + rota /agenda')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/(app)/agenda/page.tsx` existe DENTRO do grupo `(app)`, tem guarda de sessão, deriva `isSupervisor` do perfil, busca os catálogos que a ficha do cliente exige e renderiza `AgendaList`; ela mesma não chama `getAgenda()`.
    - `components/agenda/AgendaList.tsx` faz UMA leitura via `getAgendaAction()`, com guarda de cancelamento e contador de nova tentativa, e não consulta `tarefas`/`visitas` por fora.
    - Toda a copy travada no UI-SPEC aparece literalmente: título, subtítulo, os três cabeçalhos de seção com contagem, os dois estados de vazio, a mensagem de erro, o rótulo do botão de nova tentativa e a opção `Todos os vendedores`.
    - O `Select` do filtro recebe a prop `items`.
    - O componente usa `agruparAgenda`, `filtrarPorVendedor` e `vendedoresDaAgenda` e não reimplementa nem a decisão de atraso nem a ordenação (não contém comparação de dias nem `.sort()`).
    - Nenhum elemento do fluxo de conclusão da Fase 15 aparece na tela.
    - `tests/agenda/agenda-list.test.tsx` existe, roda em ambiente de DOM, tem casos cujos nomes contêm `carregando`, `secoes`, `omite`, `vazio`, `erro`, `filtro` e `vendedor`, e todos passam junto com os do Task 1.
    - `npx tsc --noEmit` limpo e `npm run lint` limpo.
  </acceptance_criteria>
  <done>Rota `/agenda` renderizando as três seções na ordem fixa, com filtro de vendedor para o Supervisor, todos os estados de carregamento/erro/vazio, e a ficha do cliente abrindo pela linha.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação humana no navegador — a Agenda contra dados reais</name>
  <action>Pausar a execução. Rodar `npm run dev` e pedir ao dono do projeto que confira a tela nova no navegador, com os dados reais dele. Só marcar o plano como concluído depois do sinal "approved". Se ele apontar diferença, corrigir e pedir nova conferida antes de seguir.</action>
  <what-built>
    A tela nova de **Agenda** está no ar em `/agenda`. Por enquanto ela ainda não aparece no menu lateral — o item de menu é o próximo plano (14-04), então por ora é preciso digitar o endereço na barra do navegador.

    O que a tela faz: junta numa lista só as **tarefas de prospecção** que ainda não foram concluídas e as **visitas de pós-venda** que ainda não aconteceram, separadas em três blocos — **Atrasado**, **Hoje** e **Próximos dias**, nessa ordem. Cada linha mostra o cliente, um selo dizendo se aquilo é Prospecção ou Visita, o que é o item, e a data.

    Os itens atrasados ficam com a **barra vermelha na lateral esquerda e o triângulo amarelo** — de propósito são exatamente os mesmos sinais que os cards atrasados do kanban já usam hoje, para você não precisar aprender dois códigos de cor diferentes.

    Se você entrar como **supervisor**, aparece um seletor de **Vendedor** no canto superior direito. Ele começa em "Todos os vendedores" (a agenda do time inteiro) e, ao escolher alguém, tanto as linhas quanto os números entre parênteses de cada bloco se ajustam na hora, sem recarregar a página.

    **Clicar em qualquer linha abre a ficha do cliente**, a mesma ficha que já abre pelo kanban.

    O que a tela NÃO faz ainda, de propósito: **não tem botão para marcar um item como feito**. Concluir uma tarefa ou visita (com o resuminho do que aconteceu e a sugestão da próxima data) é a Fase 15, a próxima. Esta fase é só de visualização.
  </what-built>
  <how-to-verify>
    1. Com `npm run dev` rodando, entre como **vendedor** e acesse `http://localhost:3000/agenda`.
    2. Confira que os itens que aparecem são só dos SEUS clientes, e que estão separados nos blocos certos: um item com data de ontem tem que estar em "Atrasado", um com a data de hoje em "Hoje", e um com data futura em "Próximos dias".
    3. Confira que os itens de "Atrasado" têm a barra vermelha na esquerda e o triângulo amarelo, e que passar o mouse no triângulo mostra "Atrasado desde" com a data.
    4. Confira que as datas mostradas são as datas CERTAS — em especial, que um item marcado para hoje aparece com a data de hoje e não com a de ontem. (Esse é o erro mais comum nesse tipo de tela e o que estamos mais preocupados em não deixar passar.)
    5. Clique numa linha e confirme que a ficha do cliente correto abre.
    6. Saia e entre como **supervisor**. Confira que agora aparecem itens de mais de um vendedor, que cada linha mostra o nome do vendedor dono, e que o seletor "Vendedor" aparece no topo à direita. Escolha um vendedor e confirme que a lista e os números entre parênteses encolhem juntos.
    7. Se algum dos três blocos estiver vazio, confirme que ele simplesmente **não aparece** (não deve haver um bloco com o cabeçalho e nada embaixo).
    8. Ponto que merece o seu olhar: se a sua agenda estiver toda em dia, a tela mostra "Sua agenda está em dia". Se você filtrar por um vendedor que não tem nada pendente, aparece "Nenhum item pendente para {nome}". Confirme que esses dois textos fazem sentido para você.
  </how-to-verify>
  <resume-signal>Digite "approved" se a tela estiver correta, ou descreva o que está diferente do esperado (em especial qualquer data que apareça deslocada em um dia).</resume-signal>
</task>

</tasks>

<artifacts_produced>
## Artefatos deste plano (parte 3 de 4 — a tela)

| Símbolo / Arquivo | Tipo | Status |
|-------------------|------|--------|
| `components/agenda/AgendaItemRow.tsx` | Client Component apresentacional | Criado |
| `components/agenda/AgendaList.tsx` | Client Component: leitura, seções, filtro, ficha do cliente | Criado |
| `app/(app)/agenda/page.tsx` | Server Component da rota `/agenda` | Criado |
| `tests/agenda/agenda-item-row.test.tsx` | Teste de renderização | Criado, verde |
| `tests/agenda/agenda-list.test.tsx` | Teste de renderização | Criado, verde |

A rota `/agenda` passa a existir e é o alvo do item de menu que o plano 14-04 adiciona. Nenhum componente shadcn novo, nenhuma dependência npm nova.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → Server Action de leitura da agenda | Fronteira de rede; a tela é Client Component e a leitura acontece do lado do servidor. |
| tela → decisão de qual vendedor é exibido (filtro) | Escolha feita no navegador sobre uma lista que já chegou escopada. |
| tela → ficha do cliente (edição/exclusão) | A linha da agenda abre um componente que JÁ escreve — a fronteira de escrita dele é a que ele sempre teve. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-15 | Elevation of Privilege | filtro de vendedor confundido com controle de acesso | high | mitigate | O filtro só estreita uma lista que a RLS já entregou, e suas opções saem dos próprios itens carregados — não existe como pedir um vendedor que o chamador não enxergaria. A ocultação do controle para o Vendedor é conveniência de tela, nunca a garantia; a garantia é a RLS provada no plano 14-01 (mesma postura que `app/(app)/clientes/page.tsx` já documenta). |
| T-14-16 | Information Disclosure | nome de outro vendedor exibido na linha para um Vendedor comum | low | mitigate | `showResponsavel` só é verdadeiro quando o chamador é Supervisor; e mesmo se fosse verdadeiro para um Vendedor, as linhas que ele recebe são só dos clientes dele, então o nome exibido seria o dele mesmo. |
| T-14-17 | Tampering | tela escrevendo no banco numa fase de leitura pura | medium | mitigate | Verificação mecânica recusa qualquer elemento do fluxo de conclusão nos dois componentes; a única escrita alcançável a partir daqui é a da ficha do cliente, que é pré-existente e mantém as próprias validações e RLS. |
| T-14-18 | Tampering (integridade da informação) | data exibida deslocada em um dia | medium | mitigate | Formatação obrigatória via `parseISO`, verificação mecânica recusa construção de data a partir da string do banco, caso de teste `data` fixa `31/01` a partir de `2026-01-31`, e o passo 4 do checkpoint humano pede conferência explícita disso contra os dados reais. |
| T-14-19 | Repudiation / integridade da informação | contagem do cabeçalho de seção divergindo das linhas mostradas | low | mitigate | As contagens são derivadas do MESMO array já filtrado que alimenta as linhas (`agruparAgenda` sobre `itensFiltrados`), nunca de uma segunda contagem paralela; o caso `secoes` compara contagem e linhas. |
| T-14-20 | Denial of Service | falha de leitura deixando a tela em branco sem explicação | low | mitigate | Estado de erro explícito com a copy do UI-SPEC e botão de nova tentativa que refaz a leitura, mesmo padrão de `ComparativoVendedorTable`; caso de teste `erro` prova. |
| T-14-SC | Tampering (supply chain) | instalação de pacotes | low | accept | Nenhum `npm install` neste plano — todos os componentes de interface usados já estão instalados desde fases anteriores, conforme registrado no UI-SPEC. Sem checkpoint de legitimidade aplicável. |
</threat_model>

<verification>
Checks rodados pelo executor ao final do plano:
- `npx vitest run tests/agenda/agenda-item-row.test.tsx tests/agenda/agenda-list.test.tsx` verde.
- Check estrutural de `AgendaItemRow.tsx`: classes de atraso idênticas às do `ClienteCard`, copy exata dos selos e do tooltip, uso de `parseISO`, ausência de decisão de atraso, de Server Action e de qualquer elemento do fluxo de conclusão.
- Check estrutural de `AgendaList.tsx`: uma leitura só, uso das funções puras do plano 14-02, ausência de comparação de dias e de reordenação, copy exata de todos os estados, prop `items` no filtro.
- Check estrutural de `app/(app)/agenda/page.tsx`: dentro do grupo `(app)`, com guarda de sessão, sem chamar o leitor diretamente.
- `npx tsc --noEmit` limpo; `npm run lint` limpo.
- Checkpoint humano (Task 3): dono conferiu a tela no navegador como Vendedor e como Supervisor, com atenção especial às datas.
</verification>

<success_criteria>
- AGD-01: `/agenda` mostra prospecção e visitas numa lista só, em três seções na ordem fixa Atrasado → Hoje → Próximos dias, com contagem viva por seção, seção vazia omitida, e cada linha identificando cliente e origem.
- AGD-03: linhas atrasadas têm borda esquerda vermelha e triângulo âmbar com tooltip, usando as mesmas classes utilitárias do card do kanban.
- AGD-05: Supervisor tem um filtro de Vendedor com `Todos os vendedores` mais um item por vendedor presente na agenda; escolher um estreita linhas e contagens sem nova ida ao servidor; Vendedor não vê o controle.
- A tela tem carregando, erro com nova tentativa, vazio total e vazio por filtro, todos com a copy exata do UI-SPEC.
- Clicar numa linha abre a ficha do cliente; salvar ou apagar por ela refaz a leitura da agenda.
- Nenhum elemento do fluxo de conclusão da Fase 15 foi antecipado; nenhuma dependência npm nova.
</success_criteria>

<output>
Create `.planning/phases/14-agenda-unificada/14-03-SUMMARY.md` when done.
</output>
