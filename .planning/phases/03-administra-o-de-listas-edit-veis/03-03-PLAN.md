---
phase: 03-administra-o-de-listas-edit-veis
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - components/configuracoes/ConfiguracoesTabs.tsx
autonomous: false
requirements: [ADM-02, ADM-03, ADM-04]
must_haves:
  truths:
    - "A tela Configurações tem as 4 abas (Categoria / Produtos consumidos / Tipos de tarefa / Motivos de perda) na ordem do D-01, cada uma com o mesmo CRUD completo"
    - "Adicionar/editar/desativar um produto reflete no cadastro de cliente; um tipo de tarefa reflete na criação de tarefas do card; um motivo de perda reflete no diálogo de marcar como perdido"
    - "Cada aba usa o mesmo EditableListTab genérico, parametrizado pela tabela e pelos labels da UI-SPEC"
  artifacts:
    - components/configuracoes/ConfiguracoesTabs.tsx
  key_links:
    - "ConfiguracoesTabs -> EditableListTab (tabela='produtos_consumidos' | 'tipos_tarefa' | 'motivos_perda')"
    - "produtos_consumidos -> checklist de produtos no cadastro/detalhe do cliente (getProdutosAtivos, ativo=true live)"
    - "tipos_tarefa -> Select de tipo de tarefa no card (getTiposTarefaAtivos); motivos_perda -> PerdaMotivoDialog (getMotivosPerdaAtivos)"
---

<objective>
Estender a tela Configurações das 1 aba (Categoria, já completa após 03-02) para as 4 abas do D-01, ligando **Produtos consumidos** (ADM-02), **Tipos de tarefa** (ADM-03) e **Motivos de perda** (ADM-04) — cada uma reusando o mesmo EditableListTab genérico, só parametrizado por tabela e labels. Depois, verificar de ponta a ponta que cada mudança reflete no lugar onde a lista é consumida (cadastro de cliente, criação de tarefa, diálogo de perda).

Purpose: O objetivo da fase é manter as QUATRO listas pela interface. Com o componente pronto (03-02), cada aba nova é uma capacidade real a mais para a Supervisor (mais uma lista administrável), fechando ADM-02/03/04.

Output: ConfiguracoesTabs com as 4 abas wired + verificação humana de reflexo downstream e da barreira de acesso do Vendedor.

Nota de escopo (MVP): cada aba adicionada é uma fatia vertical nova (nova lista que a Supervisor passa a administrar), não uma camada técnica. O componente subjacente já foi provado na aba Categoria.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-administra-o-de-listas-edit-veis/03-CONTEXT.md
@.planning/phases/03-administra-o-de-listas-edit-veis/03-UI-SPEC.md

# Artefatos dos planos anteriores + consumidores downstream a verificar
@components/configuracoes/ConfiguracoesTabs.tsx
@components/configuracoes/EditableListTab.tsx
@app/actions/listas.ts
</context>

<phase_goal>

**As a** Supervisor, **I want to** manter as quatro listas editáveis (categoria, produtos consumidos, tipos de tarefa, motivos de perda) direto pela interface, **so that** eu não preciso pedir uma alteração de código a cada novo valor.

(Este plano cobre: ligar as abas **Produtos consumidos**, **Tipos de tarefa** e **Motivos de perda**, e verificar o reflexo downstream de todas.)

</phase_goal>

<tasks>

<task type="auto">
  <name>Task 1: Ligar as 3 abas restantes no ConfiguracoesTabs</name>
  <read_first>
    - components/configuracoes/ConfiguracoesTabs.tsx (versão do 03-01: só a aba Categoria wired)
    - components/configuracoes/EditableListTab.tsx (componente genérico completo após 03-02 — props tabela + labels)
    - app/actions/listas.ts (ListaTabela union: "categorias" | "produtos_consumidos" | "tipos_tarefa" | "motivos_perda")
    - .planning/phases/03-administra-o-de-listas-edit-veis/03-UI-SPEC.md secção "Copywriting Contract" (rótulos das abas na ordem D-01, placeholders por aba, substantivos)
  </read_first>
  <files>components/configuracoes/ConfiguracoesTabs.tsx</files>
  <action>
    Estender ConfiguracoesTabs.tsx para renderizar as 4 abas na ordem exata do D-01/Copywriting Contract: "Categoria" (tabela="categorias"), "Produtos consumidos" (tabela="produtos_consumidos"), "Tipos de tarefa" (tabela="tipos_tarefa"), "Motivos de perda" (tabela="motivos_perda"). Adicionar os 3 TabsTrigger que faltam à TabsList e, para cada, um TabsContent renderizando `<EditableListTab tabela=... />` com os labels da UI-SPEC:
    - Produtos consumidos: placeholder do input "Nome do produto", substantivo "produto".
    - Tipos de tarefa: placeholder "Nome da tarefa", substantivo "tarefa" (tipo de tarefa).
    - Motivos de perda: placeholder "Nome do motivo", substantivo "motivo".
    A aba Categoria continua com placeholder "Nome da categoria", substantivo "categoria". Como o EditableListTab é genérico (parametrizado por tabela + labels) e as 4 tabelas têm o mesmo shape { id, nome, ativo }, isto é só configuração — nenhuma lógica nova de CRUD. Manter o indicador de aba ativa em `--primary` (herdado) e a ordem conceitual do domínio (atributos do cliente primeiro, depois listas do funil), conforme UI-SPEC linha 119.

    Considerar uma pequena tabela/constante de configuração das abas (chave da tabela + rótulo + placeholder + substantivo) mapeada sobre a union ListaTabela, para evitar 4 blocos repetidos e manter a ordem D-01 explícita.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - ConfiguracoesTabs.tsx renderiza 4 TabsTrigger com os textos exatos "Categoria", "Produtos consumidos", "Tipos de tarefa", "Motivos de perda" nessa ordem (grep confirma os 4 rótulos).
    - Cada aba renderiza `<EditableListTab>` com tabela ∈ {categorias, produtos_consumidos, tipos_tarefa, motivos_perda} (grep confirma os 4 literais de tabela no arquivo).
    - Nenhuma duplicação de lógica de CRUD (só reuso do EditableListTab); `npx tsc --noEmit` passa.
  </acceptance_criteria>
  <done>As 4 abas do D-01 estão na tela, cada uma com o CRUD completo herdado do EditableListTab — ADM-02/ADM-03/ADM-04 wired.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [CHECKPOINT] Verificação humana das 4 abas + reflexo downstream + barreira do Vendedor</name>
  <what-built>
    A tela Configurações completa: 4 abas (Categoria, Produtos consumidos, Tipos de tarefa, Motivos de perda), cada uma com adicionar/editar/desativar/reativar + "Mostrar inativos", protegida para Supervisor. As mudanças refletem live nos consumidores porque eles já leem `ativo = true` do banco a cada carga (getCategoriasAtivas/getProdutosAtivos/getTiposTarefaAtivos/getMotivosPerdaAtivos — nenhuma camada de cache a invalidar, além do revalidatePath("/clientes") que as actions já chamam).
  </what-built>
  <action>
    Toda a automação já foi feita na Task 1 (as 4 abas wired) e nos planos 03-01/03-02 (rota, actions, componente). Aqui não há código a escrever: conduzir a verificação manual abaixo com o dono do projeto e aguardar aprovação explícita antes de fechar a fase. Se algo divergir, registrar o sintoma observável e abrir gap para replanejar.
  </action>
  <how-to-verify>
    Rodar `npm run dev` e, logado como Supervisor (SEED_ACCOUNTS.supervisor):
    1. Abrir /configuracoes. Confirmar as 4 abas na ordem: Categoria / Produtos consumidos / Tipos de tarefa / Motivos de perda.
    2. Em "Produtos consumidos": adicionar "Teste Produto X". Abrir o cadastro/detalhe de um cliente e confirmar que "Teste Produto X" aparece na lista de produtos consumidos. Voltar às Configurações, desativar "Teste Produto X" e confirmar que ele some das opções de produto no cadastro (mas um cliente que já o tinha continua mostrando). Reativar com "Mostrar inativos".
    3. Em "Tipos de tarefa": adicionar "Teste Tarefa Y". Abrir um card no funil e confirmar que "Teste Tarefa Y" aparece no seletor de tipo de tarefa ao adicionar uma tarefa. Desativar e confirmar que some do seletor.
    4. Em "Motivos de perda": adicionar "Teste Motivo Z". Marcar um card como perdido e confirmar que "Teste Motivo Z" aparece no diálogo de motivo. Desativar e confirmar que some.
    5. Em "Categoria": editar o nome de uma categoria no lugar (lápis -> Salvar) e confirmar que o novo nome aparece no filtro/dropdown de categoria do cadastro.
    6. Barreira de acesso: sair, logar como Vendedor (SEED_ACCOUNTS.vendedorA). Confirmar que o link "Configurações" NÃO aparece no menu e que acessar /configuracoes direto redireciona para "/".
    Observar que nenhuma ação usa estilo destrutivo (vermelho) ao desativar (D-03) e que os ícones são Pencil/EyeOff/Eye (nunca lixeira).
  </how-to-verify>
  <verify>
    <human-check>O dono do projeto navegou pelas 4 abas, confirmou que adicionar/editar/desativar em cada lista reflete no consumidor correto (cadastro, seletor de tarefa, diálogo de perda, filtro de categoria) e que o Vendedor não vê o link e é redirecionado ao acessar /configuracoes direto.</human-check>
  </verify>
  <resume-signal>Digite "approved" se as 4 abas administram as listas e cada mudança reflete no consumidor correto e o Vendedor é bloqueado; senão descreva o que divergiu.</resume-signal>
  <done>As 4 abas administram as listas com CRUD completo; cada mudança reflete no consumidor downstream correto; o Vendedor é bloqueado da tela. Fase 3 pronta para verificação/ship.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendedor (autenticado) → /configuracoes (todas as 4 abas) | Mesmo limite Supervisor-only do 03-01/03-02, agora abrangendo as 4 tabelas |
| ConfiguracoesTabs → EditableListTab (3 novas tabelas) | Reuso do componente já provado; superfície de escrita idêntica |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | escrita em produtos_consumidos / tipos_tarefa / motivos_perda | high | mitigate | Mesmas políticas RLS Supervisor-only da migração 0002 nas 4 tabelas, provadas pelo teste do 03-01 (parametrizado sobre as 4); defense-in-depth nas actions (forbidden) já em vigor. Nenhuma superfície nova — só mais abas para o mesmo componente/actions |
| T-03-03 | Information Disclosure | rota /configuracoes | low | mitigate | Redirect Supervisor-only por profiles.role (03-01), verificado no checkpoint humano com login Vendedor |
| T-03-04 | Denial of Service / perda de dados | 3 novas listas (D-03) | medium | mitigate | Soft-delete via `ativo` também nas 3 abas novas (mesmo EditableListTab); checkpoint confirma que desativar não apaga dado de cliente existente |
| T-03-SC | Tampering (supply chain) | dependências | low | accept | Nenhuma instalação npm/pip/cargo neste plano (Switch já foi instalado no 03-02). Sem gate de legitimidade aplicável |
</threat_model>

<artifacts>
## Artifacts this phase produces (deste plano)

- `components/configuracoes/ConfiguracoesTabs.tsx` (estendido) — as 4 abas do D-01, cada uma renderizando o EditableListTab genérico por tabela.
- Verificação humana de que ADM-02/03/04 refletem nos consumidores downstream e de que o Vendedor é bloqueado.
</artifacts>

<verification>
- `npx tsc --noEmit` sem erros.
- Grep confirma os 4 rótulos de aba e os 4 literais de tabela em ConfiguracoesTabs.tsx.
- Checkpoint humano aprovado (reflexo downstream das 4 listas + barreira de acesso do Vendedor).
</verification>

<success_criteria>
- As 4 abas (Categoria/Produtos/Tipos de tarefa/Motivos de perda) administram suas listas com CRUD completo (ADM-01..04).
- Adicionar/editar/desativar em cada lista reflete no consumidor correto: produtos no cadastro, tipos de tarefa no card, motivos de perda no diálogo de perda, categoria no cadastro/filtro.
- Vendedor não vê o link e é redirecionado ao acessar /configuracoes.
</success_criteria>

<output>
Create `.planning/phases/03-administra-o-de-listas-edit-veis/03-03-SUMMARY.md` when done
</output>
