---
phase: 20-calend-rio-da-agenda-m-s-semana-e-dia
plan: 05
type: execute
wave: 4
depends_on: ["20-02", "20-03", "20-04"]
files_modified:
  - components/agenda/AgendaList.tsx
  - tests/agenda/agenda-calendario-integracao.test.tsx
autonomous: true
requirements: [AGD-07, AGD-14]

must_haves:
  truths:
    - "AGD-07 (D-01): na tela da Agenda existe um alternador entre Lista e Calendário; a visão inicial é a Lista, e um clique troca para qualquer um dos três modos de calendário e de volta"
    - "AGD-07 (D-01): a Lista continua se comportando exatamente como antes — mesmas três seções Atrasado/Hoje/Próximos dias com as mesmas contagens, mesmo cartão, mesmo botão de concluir, mesmos dois estados de vazio (agenda em dia x nenhum item para o vendedor escolhido), mesmo botão de exportar diário, mesmo estado de carregando e de erro"
    - "AGD-14 (D-08): o filtro de vendedor do Supervisor é aplicado UMA vez, acima das duas visões; trocar o vendedor muda o que a Lista mostra e o que o calendário mostra, sempre em concordância"
    - "AGD-14: o filtro continua sendo o mesmo controle no mesmo lugar do cabeçalho, alimentado pela mesma extração de vendedores a partir dos itens já carregados — nenhuma consulta nova"
    - "O calendário consome exatamente a mesma lista já estreitada que a Lista usa; não existe segunda busca de dados na tela"
    - "Concluir um item pelo calendário abre a mesma janela de conclusão da Lista e, ao dar certo, recarrega a agenda pelo mesmo contador de recarga já existente"
    - "Abrir um item pelo calendário abre a mesma ficha do cliente da Lista"
    - "A alternância de visão sobrevive ao carregamento e ao erro: a barra aparece quando há dados, e os estados de carregando/erro continuam iguais aos de hoje"
    - "Nenhum arquivo além de AgendaList.tsx foi modificado; os testes antigos da Agenda passam sem uma linha de edição"
  artifacts:
    - "components/agenda/AgendaList.tsx — estado de visão novo e composição do calendário como irmão da Lista"
    - "tests/agenda/agenda-calendario-integracao.test.tsx — alternância, coerência com o filtro de vendedor e roteamento das ações"
  key_links:
    - "o filtro por vendedor precisa continuar sendo aplicado UMA vez, no ponto onde já é aplicado hoje, e o resultado alimentar as duas visões — aplicar de novo dentro do calendário, ou passar a lista crua, faz as contagens da grade divergirem das da Lista (Pitfall 10, e é o critério de sucesso 5 da fase)"
    - "o corte que hoje encerra a renderização quando não há nenhum item precisa passar a valer só para a Lista — se continuar valendo para a tela inteira, o Supervisor filtra um vendedor sem itens e o calendário desaparece junto com a barra, deixando o usuário sem caminho de volta"
    - "a visão inicial precisa ser a Lista, senão os testes existentes da Agenda quebram e o comportamento padrão da tela muda — o que a decisão D-01 proíbe"
    - "a janela de conclusão e a ficha do cliente continuam montadas no mesmo lugar de sempre, no fim do componente, servindo às duas visões; duplicá-las dentro do calendário criaria duas janelas concorrendo pelo mesmo estado"
---

<objective>
Plugar o calendário na tela da Agenda: acrescentar o estado de visão, renderizar a barra/calendário como irmão da Lista, e garantir que o filtro de vendedor do Supervisor continue sendo aplicado uma única vez, acima das duas visões. Depois, verificar tudo no navegador com o dono do projeto.

Purpose: é o plano que entrega os dois requisitos que faltam (AGD-07 e AGD-14) e, ao mesmo tempo, o de maior risco de regressão da fase — a Agenda é uma tela que a equipe já usa todo dia, e a decisão D-01 é explícita: a Lista não pode mudar de comportamento em nada. Por isso a mudança aqui é deliberadamente cirúrgica, e a prova principal é negativa: os testes antigos da Agenda passam sem uma linha de edição.

Output: uma alteração contida em `AgendaList.tsx`, um arquivo de teste de integração novo, e a verificação humana no navegador que fecha a fase.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@components/agenda/AgendaList.tsx
@tests/agenda/agenda-list.test.tsx
@lib/agenda/itens.ts
@.planning/phases/20-calend-rio-da-agenda-m-s-semana-e-dia/20-01-PLAN.md
@.planning/phases/20-calend-rio-da-agenda-m-s-semana-e-dia/20-04-PLAN.md
</context>

<interface_context>
**Consumido** (plano 20-04):

```tsx
export function AgendaCalendario(props: {
  visao: AgendaVisao
  onVisaoChange: (visao: AgendaVisao) => void
  itens: AgendaItem[]                       // JÁ estreitados pelo filtro de vendedor
  showResponsavel: boolean
  onOpenCliente: (clienteId: string) => void
  onConcluirItem: (item: AgendaItem) => void
  now?: Date
}): JSX.Element
```

Este componente renderiza **sempre** a barra de ferramentas e, quando a visão não é a Lista, também a grade. Na visão de Lista ele devolve só a barra.

**Consumido** (plano 20-01): `AgendaVisao` (`"lista" | "dia" | "semana" | "mes"`), de `lib/agenda/itens.ts`.

**Já existente em `AgendaList.tsx`, REUSADO sem alteração de semântica:**
- `itensFiltrados` — o resultado de `filtrarPorVendedor(itens, vendedorFiltroId)`, calculado uma vez, hoje já consumido pelo agrupamento em seções. Passa a alimentar também o calendário.
- `showResponsavel` — `isSupervisor && vendedorFiltroId === null`.
- `handleOpenCliente(clienteId)` e `handleOpenConcluir(item)` — os dois tratadores que a Lista já passa a cada linha.
- `handleRecarregar()` e o contador de recarga.
- `ClienteDetailSheet` e `ConcluirItemDialog`, montados no fim do componente, servindo as duas visões.

**Forma esperada da mudança em `AgendaList.tsx`** (o resto do arquivo fica como está):

```tsx
const [visao, setVisao] = useState<AgendaVisao>("lista")   // padrão Lista: D-01
...
) : (
  <>
    <AgendaCalendario
      visao={visao}
      onVisaoChange={setVisao}
      itens={itensFiltrados}
      showResponsavel={showResponsavel}
      onOpenCliente={handleOpenCliente}
      onConcluirItem={handleOpenConcluir}
    />
    {visao === "lista" ? (/* bloco de vazio OU as três seções, exatamente como hoje */) : null}
  </>
)
```
</interface_context>

<source_audit>
Ver `<source_audit>` completo em `20-01-PLAN.md`. Este plano cobre G1, G5, AGD-07, AGD-14, R7, D-01 e D-08 — e é onde a fase inteira é verificada de ponta a ponta no navegador.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estado de visão e composição do calendário na tela da Agenda</name>
  <files>components/agenda/AgendaList.tsx, tests/agenda/agenda-calendario-integracao.test.tsx</files>
  <read_first>
    - `components/agenda/AgendaList.tsx` INTEIRO, com atenção especial a: o comentário de cabeçalho (quem é dono do quê); o bloco de estados (linhas ~85-99); o cálculo de `vendedorOpcoesDoFiltro`/`itensFiltrados`/`secoes`/`showResponsavel` (linhas ~125-131); o encadeamento de renderização carregando → erro → vazio → seções (linhas ~305-364); e os dois diálogos no fim (linhas ~366-389).
    - `tests/agenda/agenda-list.test.tsx` INTEIRO — cada caso ali é uma promessa de comportamento da Lista que esta tarefa não pode quebrar. Reparar em especial nos casos de estado vazio (os dois textos diferentes) e nos de filtro de vendedor.
    - O bloco `<interface_context>` deste plano — a forma exata da mudança.
    - `.planning/research/PITFALLS.md`, Pitfall 10 — por que o filtro tem que ser aplicado uma vez só, acima das duas visões.
  </read_first>
  <behavior>
    - A tela abre na visão de Lista: as três seções aparecem como sempre, e nenhuma grade de calendário é renderizada.
    - A barra de ferramentas aparece assim que os dados carregam, também na visão de Lista.
    - Clicar em "Mês" esconde as seções da Lista e mostra a grade de mês; clicar em "Lista" traz as seções de volta, com as mesmas contagens de antes.
    - Trocar de visão não dispara nova busca de dados (a ação de leitura da agenda continua tendo sido chamada o mesmo número de vezes).
    - Para o Supervisor, escolher um vendedor no filtro enquanto o calendário está ativo muda o que a grade mostra; voltar para "Todos os vendedores" restaura.
    - Com um vendedor escolhido, o conjunto que a Lista mostra e o que o calendário mostra são o mesmo — um item de outro vendedor não aparece em nenhuma das duas.
    - Com o filtro escolhendo um vendedor sem nenhum item, a Lista mostra a mensagem própria daquele caso E a barra de ferramentas continua visível (dá para voltar/trocar de visão).
    - Concluir um item a partir da visão de dia abre a mesma janela de conclusão que a Lista abre.
    - Abrir um item a partir da visão de semana abre a mesma ficha de cliente que a Lista abre.
    - Os estados de carregando e de erro continuam exatamente como hoje.
  </behavior>
  <action>
    Escrever os casos de teste primeiro (RED), no arquivo NOVO `tests/agenda/agenda-calendario-integracao.test.tsx`, depois a alteração. **Não editar `tests/agenda/agenda-list.test.tsx`** — ele é a prova de que a Lista não mudou; adaptá-lo destruiria a prova.

    Em `components/agenda/AgendaList.tsx`, fazer exatamente três mudanças e mais nenhuma:

    1. **Um estado novo**, irmão dos que já existem, guardando a visão ativa, iniciado na Lista. Comentar por que o padrão é a Lista: a decisão D-01 diz que o Calendário é uma alternativa, não uma substituição — a tela precisa continuar abrindo do jeito que a equipe já conhece.

    2. **Compor o calendário** logo antes do bloco que hoje decide entre o estado de vazio e as seções, dentro do mesmo ramo de "dados prontos". O calendário recebe a visão, o repasse de troca de visão, `itensFiltrados` (o MESMO valor que o agrupamento em seções já consome — não recalcular, não passar a lista crua), o indicador de mostrar responsável, e os dois tratadores que a Lista já usa para abrir ficha e abrir conclusão. Comentar que passar `itensFiltrados` é o que garante que as contagens da grade nunca discordem das da Lista quando o Supervisor troca o filtro.

    3. **Escopar à Lista** o que hoje encerra a renderização quando não há item nenhum. Hoje o teste de "nenhum item" decide o corpo inteiro da tela; ele passa a decidir apenas o bloco da Lista, e esse bloco inteiro passa a ser renderizado só quando a visão ativa é a Lista. Os DOIS textos de vazio, as três seções, os títulos com contagem e o mapeamento de linhas ficam **caractere por caractere como estão** — só mudam de lugar dentro da árvore, envolvidos por uma condição. Comentar por que: com o corte valendo para a tela inteira, um Supervisor que filtrasse um vendedor sem itens perderia a barra de ferramentas junto com a Lista e ficaria sem caminho de volta.

    Atualizar o comentário de cabeçalho do componente para registrar o papel novo (dono também da visão ativa), mantendo tudo o que ele já diz.

    **Não mexer em nada mais:** botão de exportar diário, cabeçalho de título, o controle de filtro de vendedor, os estados de carregando e erro, os dois diálogos no fim, e o efeito de busca ficam exatamente como estão. Nenhuma segunda busca, nenhum segundo filtro, nenhum segundo diálogo.

    No arquivo de teste novo, reusar o molde de `tests/agenda/agenda-list.test.tsx` (mesma simulação das ações de servidor e do cliente de navegador, mesmo construtor auxiliar de item, mesma função de data relativa ao relógio do teste) e cobrir todos os itens do bloco de comportamento. O caso de coerência entre Lista e calendário sob filtro é obrigatório — é o critério de sucesso 5 da fase.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint components/agenda tests/agenda && npx vitest run tests/agenda/ && npx next build && node -e "const fs=require('fs');const raw=fs.readFileSync('components/agenda/AgendaList.tsx','utf8');const src=raw.replace(/\/\*[\s\S]*?\*\//g,'').replace(/^[^\n]*\/\/.*$/gm,'');if(!src.includes('AgendaCalendario'))throw new Error('o calendario nao foi composto na tela');if(!/useState<AgendaVisao>\(\s*[\\\"']lista[\\\"']\s*\)/.test(src))throw new Error('a visao inicial precisa ser a Lista (D-01)');if((src.match(/filtrarPorVendedor\s*\(/g)||[]).length!==1)throw new Error('o filtro por vendedor precisa ser aplicado UMA vez so (D-08/Pitfall 10)');if((src.match(/getAgendaAction\s*\(/g)||[]).length!==1)throw new Error('nao pode existir segunda busca de dados na tela');if((src.match(/<ConcluirItemDialog/g)||[]).length!==1||(src.match(/<ClienteDetailSheet/g)||[]).length!==1)throw new Error('os dois diaogos continuam montados uma vez so, servindo as duas visoes');if(!/itens=\{itensFiltrados\}/.test(src))throw new Error('o calendario precisa consumir a MESMA lista ja estreitada');for(const t of ['Sua agenda esta em dia'.replace(/esta/,'está'),'Atrasado (','Hoje (','Próximos dias (','Exportar meu diário','Tentar novamente'])if(!raw.includes(t))throw new Error('texto da Lista alterado ou perdido: '+t);const agrup=src.indexOf('agruparAgenda');if(agrup<0)throw new Error('o agrupamento em secoes da Lista foi removido');const t1=fs.readFileSync('tests/agenda/agenda-list.test.tsx','utf8');if(/AgendaCalendario|visao|Calendário/.test(t1))throw new Error('tests/agenda/agenda-list.test.tsx foi adaptado — ele e a prova de que a Lista nao mudou, precisa passar SEM edicao');for(const f of ['components/agenda/AgendaItemRow.tsx','components/agenda/ConcluirItemDialog.tsx'])if(fs.readFileSync(f,'utf8').includes('AgendaCalendario'))throw new Error('nao era para editar '+f);const t2=fs.readFileSync('tests/agenda/agenda-calendario-integracao.test.tsx','utf8');if(!/vendedor/i.test(t2))throw new Error('falta o caso de coerencia entre Lista e calendario sob o filtro de vendedor (criterio de sucesso 5)');console.log('OK fiacao da tela')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/agenda/AgendaList.tsx` ganhou exatamente um estado novo (a visão ativa, iniciada na Lista) e compõe `AgendaCalendario` passando `itensFiltrados`, o indicador de responsável e os dois tratadores já existentes.
    - `filtrarPorVendedor` e a busca da agenda continuam aparecendo uma única vez cada no arquivo; a ficha do cliente e a janela de conclusão continuam montadas uma vez cada.
    - O bloco da Lista (dois estados de vazio, três seções, títulos com contagem, mapeamento de linhas) está caractere por caractere igual, apenas envolvido pela condição de visão.
    - `tests/agenda/agenda-list.test.tsx`, `agenda-item-row.test.tsx` e `concluir-item-dialog.test.tsx` passam SEM nenhuma edição, e o primeiro não menciona nada de calendário.
    - `tests/agenda/agenda-calendario-integracao.test.tsx` verde, cobrindo todos os itens do bloco de comportamento, incluindo o caso de coerência Lista x calendário sob filtro de vendedor e o caso do vendedor sem itens com a barra ainda visível.
    - `npx vitest run tests/agenda/` inteiro verde.
    - `npx next build` conclui sem erro; `npx tsc --noEmit` e `npx eslint components/agenda tests/agenda` limpos.
    - `git diff --stat` mostra exatamente dois arquivos: `AgendaList.tsx` alterado e o teste de integração novo.
  </acceptance_criteria>
  <done>A Agenda tem os dois modos de olhar os mesmos dados, com o filtro de vendedor mandando nos dois, e a Lista provadamente intacta.</done>
</task>

<task type="auto">
  <name>Task 2: Fechamento da fase — checagem de regressão ampla e verificação humana no navegador</name>
  <files>components/agenda/AgendaList.tsx</files>
  <read_first>
    - `.planning/ROADMAP.md`, Fase 20, os cinco critérios de sucesso — é exatamente o que esta verificação precisa confirmar com os olhos.
    - `.planning/STATE.md`, seção Blockers/Concerns — em especial o limite conhecido de tentativas de login do serviço de autenticação, que impede rodar a bateria inteira de testes de uma vez, e a convenção do projeto (Fases 13/18/19) de provar isoladamente os arquivos de maior risco de regressão.
    - `.planning/sketches/003-agenda-calendario/index.html`, Variante A — a referência visual contra a qual o resultado é comparado a olho.
    - Os arquivos `SUMMARY.md` dos planos 20-01 a 20-04 — o que já foi provado por teste automatizado e não precisa ser reconferido à mão.
  </read_first>
  <action>
    **Parte A — checagem de regressão ampla, automatizada.**

    Rodar a bateria de testes da Agenda inteira e, além dela, os arquivos de maior risco indireto. Se a bateria completa do projeto não fechar limpa por causa do limite conhecido de tentativas de login (concern já registrado no estado do projeto), seguir a convenção das Fases 13/18/19: provar isoladamente os arquivos de risco e registrar no resumo quais foram, com o resultado de cada um. Não marcar a fase como concluída com base numa bateria que não rodou.

    Rodar também a construção de produção e a checagem de tipos, já cobertas pela verificação automatizada abaixo.

    **Parte B — verificação humana no navegador.**

    Subir o ambiente de desenvolvimento e conduzir o dono do projeto — que não programa — pela verificação, explicando cada passo em linguagem simples. Antes de chamá-lo, semear dados de teste que tornem os casos interessantes visíveis: pelo menos um dia com mais de 3 itens (para o "+N mais" aparecer), pelo menos um item atrasado, pelo menos um item de prospecção e um de visita no mesmo dia, e — se houver conta de Supervisor — itens de dois vendedores diferentes. Semear pelo caminho que o projeto já usa para dados de teste (cliente com credencial de serviço, nunca pela interface), para não esbarrar no limite de tentativas de login.

    O roteiro da conversa, na ordem, cobrindo os cinco critérios de sucesso da fase:

    1. Abrir `/agenda`. Confirmar que a tela abre igualzinha ao que era antes: as mesmas seções Atrasado / Hoje / Próximos dias, o mesmo botão de exportar diário, os mesmos cartões com o botão "Concluir". A única novidade visível é a barra com Lista / Dia / Semana / Mês.
    2. Clicar em "Mês". Confirmar: a grade tem 7 colunas, a primeira coluna é **segunda-feira**, o dia de hoje está circulado, os dias do mês anterior e do seguinte aparecem apagadinhos nas pontas, e um dia cheio mostra 3 itens e um "+N mais".
    3. Clicar num dia. Confirmar que abre uma janelinha com a lista completa daquele dia, com os mesmos cartões da tela de Lista, inclusive o botão "Concluir". Fechar.
    4. Usar as setas para ir para o mês seguinte e voltar; depois clicar em "Hoje" e confirmar que volta para o mês atual. Repetir em "Semana" e em "Dia", conferindo que a seta anda uma semana e um dia, respectivamente.
    5. Em "Semana", confirmar as 7 colunas (segunda a domingo), a coluna de hoje destacada, e que dá para diferenciar a olho um item de prospecção (cinza, ícone de prancheta) de um de visita (azul, ícone de repetição). Confirmar que um item atrasado tem o risquinho vermelho na lateral **e** ainda dá para saber se é prospecção ou visita.
    6. Ir para um mês em que um item atrasado caia numa daquelas pontas apagadinhas de mês vizinho e confirmar que ele continua legível como atrasado.
    7. Clicar num item da semana e confirmar que abre a ficha do cliente, igual à Lista.
    8. Concluir um item pelo calendário (pela janelinha do dia ou pela visão de Dia) e confirmar que a janela de conclusão é a mesma de sempre e que, depois de concluir, o item some das duas visões.
    9. **Se houver conta de Supervisor:** entrar como Supervisor, escolher um vendedor no filtro e confirmar que a grade muda junto com a Lista — e que os números do "+N mais" acompanham o filtro, não continuam mostrando o total de todo mundo. Voltar para "Todos os vendedores".
    10. Voltar para "Lista" e confirmar, uma última vez, que nada mudou nela.

    Se qualquer passo falhar, descrever o problema pelo sintoma observável ("o dia 3 aparece na coluna errada") antes de qualquer detalhe técnico, corrigir, e refazer o passo. Registrar no resumo da fase o que foi conferido, o que falhou e o que foi corrigido.

    **Não** implementar nada da Fase 21 (mostrar itens já concluídos ao navegar para datas passadas) durante esta verificação, mesmo que o dono pergunte por isso ao ver um mês passado vazio — a resposta correta é explicar que essa é a próxima fase, já roteirizada, e registrar o comentário dele no resumo.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint . && npx vitest run tests/agenda/ tests/clientes/ && npx next build && node -e "const fs=require('fs');const esperados=['components/agenda/AgendaCalendarioDia.tsx','components/agenda/AgendaCalendarioSemana.tsx','components/agenda/AgendaCalendarioMes.tsx','components/agenda/AgendaCalendarioToolbar.tsx','components/agenda/AgendaCalendario.tsx','tests/agenda/agenda-calendario-dia.test.tsx','tests/agenda/agenda-calendario-semana.test.tsx','tests/agenda/agenda-calendario-mes.test.tsx','tests/agenda/agenda-calendario-toolbar.test.tsx','tests/agenda/agenda-calendario.test.tsx','tests/agenda/agenda-calendario-integracao.test.tsx'];for(const f of esperados)if(!fs.existsSync(f))throw new Error('artefato da fase ausente: '+f);const pkg=JSON.parse(fs.readFileSync('package.json','utf8'));const deps=Object.keys({...pkg.dependencies,...pkg.devDependencies});const antes=['date-fns','lucide-react','react','next'];for(const d of antes)if(!deps.includes(d))throw new Error('dependencia esperada sumiu: '+d);const suspeitas=deps.filter(d=>/calendar|scheduler|fullcalendar|dnd|big-calendar/i.test(d));if(suspeitas.length)throw new Error('dependencia nova de calendario detectada — esta fase e zero instalacao: '+suspeitas.join(','));const supa=fs.readdirSync('supabase/migrations');console.log('OK fase completa | migrations: '+supa.length+' (nenhuma nova era esperada)')"</automated>
    <human-check>
      Verificação no navegador com o dono do projeto, seguindo os 10 passos do roteiro acima. Aprovada quando os cinco critérios de sucesso da Fase 20 no ROADMAP forem confirmados a olho:
      1. Alterna Lista ↔ Calendário num clique e a Lista continua exatamente como era.
      2. Escolhe mês/semana/dia, avança/volta e o botão "Hoje" traz de volta; a semana começa na segunda-feira nas três visões.
      3. Mês mostra até 3 itens por dia com "+N mais" e o clique no dia abre a lista completa daquele dia; semana mostra 7 colunas; dia mostra o mesmo cartão da Lista.
      4. Prospecção em cinza e Visita em azul em todas as visões; item atrasado continua legível como atrasado inclusive numa célula de mês vizinho.
      5. (Se houver Supervisor) o filtro de vendedor manda no calendário igual manda na Lista, e as contagens "+N" acompanham o filtro.
      Resposta esperada: "aprovado" ou a descrição do que ficou errado.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/agenda/ tests/clientes/` verde; se a bateria completa do projeto não puder rodar limpa por causa do limite conhecido de tentativas de login, o resumo registra quais arquivos de risco foram provados isoladamente e o resultado de cada um.
    - `npx next build`, `npx tsc --noEmit` e `npx eslint .` limpos.
    - Os onze artefatos da fase existem; nenhuma dependência nova em `package.json`; nenhuma migration nova em `supabase/migrations`.
    - A verificação humana no navegador foi conduzida e os cinco critérios de sucesso do ROADMAP foram confirmados, com o resultado (aprovado ou os ajustes feitos) registrado no resumo da fase.
    - Nada da Fase 21 (itens já concluídos em datas passadas) foi implementado.
  </acceptance_criteria>
  <done>A Fase 20 está entregue e conferida no navegador pelo dono do projeto: a Agenda tem calendário de mês, semana e dia, e a Lista continua sendo exatamente o que era.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → Server Action → Postgres | A leitura da agenda continua sendo a mesma chamada de sempre, sob RLS. Esta fase não acrescenta nenhuma travessia nova dessa fronteira. |
| filtro de vendedor no navegador | Estreitamento LOCAL sobre uma lista que a RLS já escopou. Não é, e não pode virar, checagem de permissão — regra já documentada na própria função de filtro. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-13 | Elevation of Privilege | filtro de vendedor confundido com autorização | high | mitigate | O filtro continua sendo o mesmo estreitamento local de hoje, aplicado uma única vez (verificado mecanicamente: a função de filtro aparece exatamente uma vez no arquivo). A fronteira real continua sendo a RLS em `agenda_do_vendedor()`, intocada nesta fase — nenhuma migration, nenhum RPC novo (verificado mecanicamente). Um vendedor não passa a enxergar item de outro por trocar de visão, porque as duas visões consomem o mesmo arranjo que o servidor entregou. |
| T-20-14 | Information Disclosure | segunda busca de dados na tela | medium | mitigate | Uma busca paralela dentro do calendário poderia escapar do recorte que a tela aplica. Mitigado pela proibição mecânica de mais de uma chamada da ação de leitura em `AgendaList.tsx` e pela proibição, nos planos 20-02 a 20-04, de qualquer importação de ação de servidor nos componentes de calendário. |
| T-20-15 | Tampering | regressão silenciosa na Lista | high | mitigate | A Lista é usada diariamente pela equipe; uma mudança de comportamento aqui é dano real. Mitigado por uma prova negativa: `tests/agenda/agenda-list.test.tsx` precisa passar SEM edição, e a verificação mecânica recusa o arquivo caso ele mencione qualquer coisa de calendário (sinal de que foi adaptado em vez de respeitado). Somam-se as conferências de texto literal dos rótulos das seções e dos dois estados de vazio. |
| T-20-16 | Repudiation | conclusão de item disparada pelo calendário | low | accept | O caminho de escrita é o mesmo de hoje (as duas ações de servidor e os gatilhos de auditoria já existentes, intocados); a trilha no diário continua sendo escrita pelos mesmos gatilhos com privilégio elevado que já existem. Nada novo a mitigar. |
| T-20-SC | Tampering | instalação de pacotes (npm) | high | mitigate | A fase termina com zero dependência nova — verificado mecanicamente na Task 2, que recusa qualquer pacote de calendário/agendamento/arrastar-e-soltar em `package.json`. Nenhum portão humano de legitimidade foi necessário porque nenhuma instalação ocorreu. |
</threat_model>

<verification>
- `npx vitest run tests/agenda/` verde por inteiro, com `agenda-list.test.tsx`, `agenda-item-row.test.tsx` e `concluir-item-dialog.test.tsx` passando sem uma linha de edição.
- `npx vitest run tests/clientes/` verde (a ficha do cliente é aberta a partir do calendário).
- `npx next build`, `npx tsc --noEmit` e `npx eslint .` limpos.
- `package.json` sem dependência nova; `supabase/migrations` sem arquivo novo.
- Verificação humana no navegador conduzida, cobrindo os cinco critérios de sucesso da Fase 20.
</verification>

<success_criteria>
A tela da Agenda oferece Lista e Calendário como duas leituras dos mesmos dados: um clique alterna, três modos de calendário navegam por data com botão de volta ao presente, a semana começa na segunda-feira em todas as visões, a sinalização Prospecção/Visita e o acento de atraso vieram junto, o filtro de vendedor do Supervisor manda nas duas visões — e a Lista continua provadamente idêntica ao que era antes da fase.
</success_criteria>

<output>
Create `.planning/phases/20-calend-rio-da-agenda-m-s-semana-e-dia/20-05-SUMMARY.md` when done
</output>
