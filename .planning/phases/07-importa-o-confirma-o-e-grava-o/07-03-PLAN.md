---
phase: 07-importa-o-confirma-o-e-grava-o
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - components/importacao/ImportPreviewTable.tsx
  - components/importacao/ImportSummary.tsx
  - components/importacao/ImportWizard.tsx
  - tests/importacao/import-summary.test.tsx
autonomous: false
requirements: [IMP-01, IMP-06, IMP-09]
must_haves:
  truths:
    - "Clicking 'Confirmar importação' calls confirmarLoteImportacao; while in flight the button is disabled and relabels to 'Importando clientes…'."
    - "On success the wizard body is replaced by the summary screen and the numbered step indicator is hidden (D-01)."
    - "The summary shows 'Clientes importados' and 'Linhas puladas' tiles, plus a reason-grouped breakdown only when puladas > 0."
    - "'Ver clientes' navigates to /clientes (imported clientes appear at 'Aguardando contato'); 'Importar outra planilha' resets the wizard to Step 1."
    - "The per-row Importar/Pular decisions made in Step 3 drive exactly which rows are confirmed (decisions lifted out of ImportPreviewTable into ImportWizard)."
  artifacts:
    - "components/importacao/ImportSummary.tsx"
    - "components/importacao/ImportWizard.tsx (confirm wiring + summary step)"
    - "components/importacao/ImportPreviewTable.tsx (decisions lifted to props)"
    - "tests/importacao/import-summary.test.tsx"
  key_links:
    - "ImportPreviewTable decisions (controlled props) ↔ ImportWizard state ↔ confirmarLoteImportacao(linhas, decisions)"
    - "confirmResult !== null → render <ImportSummary/> instead of the step body"
    - "'Ver clientes' → /clientes (kanban shows new clientes at aguardando_contato, IMP-09)"
---

<objective>
Wire the review screen's existing "Confirmar importação" no-op button to the real `confirmarLoteImportacao` action, lift the per-row Importar/Pular decisions out of `ImportPreviewTable` so the wizard can pass them to the action, add the post-confirm summary screen (`ImportSummary.tsx`, D-01), and verify the whole write path end-to-end against the live project with a human checkpoint.

Purpose: This closes the milestone — the Supervisor can now actually complete a bulk import and see "X importados, Y puladas". Because it is the first real database write in the milestone, the final task is a human-verify checkpoint importing a real file.
Output: wired `ImportWizard`, controlled `ImportPreviewTable`, new `ImportSummary`, a render test, and a human sign-off.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/07-importa-o-confirma-o-e-grava-o/07-CONTEXT.md
@.planning/phases/07-importa-o-confirma-o-e-grava-o/07-UI-SPEC.md
@.planning/phases/07-importa-o-confirma-o-e-grava-o/07-PATTERNS.md
@components/importacao/ImportWizard.tsx
@components/importacao/ImportPreviewTable.tsx
@components/dashboard/GanhosPerdidosCards.tsx
@app/actions/importacao.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift the per-row Importar/Pular decisions from ImportPreviewTable to ImportWizard</name>
  <read_first>
    - components/importacao/ImportPreviewTable.tsx — the internal `decisions` state (lines 44-66): `Record<number,"importar"|"pular">`, default "pular" via `decisionFor`, only rendered for `status === "duplicado"` rows. Currently nothing outside the component reads it.
    - components/importacao/ImportWizard.tsx — where `<ImportPreviewTable linhas={validatedRows} />` is rendered (line 330) and where `validatedRows` lives (Step 3 state).
    - .planning/phases/07-importa-o-confirma-o-e-grava-o/07-PATTERNS.md — ImportWizard section (lines 227-285) on lifting state.
  </read_first>
  <files>components/importacao/ImportPreviewTable.tsx, components/importacao/ImportWizard.tsx</files>
  <action>
Make `ImportPreviewTable` a controlled component for its decisions: add props `decisions: Record<number, "importar" | "pular">` and `onDecisionChange: (row: number, decision: "importar" | "pular") => void`. Remove the internal `useState` for `decisions`; keep the `decisionFor(row)` default-"pular" read (now reading from the `decisions` prop) and route `setDecision` clicks through `onDecisionChange`. Leave pagination state internal (unchanged). This is the integration point 07-02's `confirmarLoteImportacao(linhas, decisions)` depends on — without lifting, the wizard cannot tell the action which duplicado rows the Supervisor chose to import.

In `ImportWizard`, add `const [decisions, setDecisions] = useState<Record<number, "importar" | "pular">>({})`, a `handleDecisionChange(row, decision)` that merges into it, and pass both to `<ImportPreviewTable linhas={validatedRows} decisions={decisions} onDecisionChange={handleDecisionChange} />`. Reset `decisions` to `{}` wherever `validatedRows` is reset to null (handleContinueFromMapping, handleRemoveFile) so a re-validated batch starts fresh.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/importacao/preview.test.ts</automated>
  </verify>
  <done>ImportPreviewTable no longer owns decisions state; ImportWizard owns it and passes it down; existing preview tests and typecheck pass.</done>
  <acceptance_criteria>
    - ImportPreviewTable takes `decisions` + `onDecisionChange` props and has no internal decisions useState.
    - ImportWizard owns the decisions map, resets it alongside validatedRows, and passes it to the table.
    - `npx tsc --noEmit` passes; tests/importacao/preview.test.ts still green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ImportSummary component + wire confirm action and summary step in ImportWizard</name>
  <read_first>
    - components/dashboard/GanhosPerdidosCards.tsx — the stat-tile Card layout (lines 117-152) and the `new Intl.NumberFormat("pt-BR")` instance (line 23). Reuse the tile shape but with the 20px number (see UI-SPEC deviation).
    - .planning/phases/07-importa-o-confirma-o-e-grava-o/07-UI-SPEC.md — the entire Copywriting Contract + Layout & Interaction Notes: tile colors (green-600 success / amber-500 puladas via `border-l-4`), icons (CircleCheck / TriangleAlert, size-4 text-muted-foreground), `text-xl leading-[1.2] font-semibold` number (NOT the 36px Dashboard waiver), breakdown `<ul>`/`<li>` only when puladas > 0, buttons row (Ver clientes default variant, Importar outra planilha outline), in-flight relabel to "Importando clientes…", CONFIRM_ERROR banner copy.
    - .planning/phases/07-importa-o-confirma-o-e-grava-o/07-PATTERNS.md — ImportSummary section (lines 173-224) and ImportWizard section (lines 227-285): handleConfirmar/confirming/confirmError/confirmResult state, error-banner + top-of-file constant convention, the confirmResult !== null render branch, handleImportarOutra reset.
    - app/actions/importacao.ts — `confirmarLoteImportacao(linhas, decisions)` signature and its `{ data: { importados, puladas } }` / `{ error }` result (from 07-02).
    - components/importacao/ImportWizard.tsx — the existing no-op "Confirmar importação" button (lines 337-348) this replaces, and the router: this file is a Client Component, so use `useRouter().push("/clientes")` (next/navigation) for "Ver clientes".
  </read_first>
  <files>components/importacao/ImportSummary.tsx, components/importacao/ImportWizard.tsx, tests/importacao/import-summary.test.tsx</files>
  <action>
Create `components/importacao/ImportSummary.tsx` (Client Component, pure presentational — no fetch effect, unlike GanhosPerdidosCards). Props: `importadosCount: number`, `puladas: { motivo: string; quantidade: number }[]`, `onVerClientes: () => void`, `onImportarOutra: () => void`. Render per 07-UI-SPEC exactly: heading "Importação concluída"; a `grid grid-cols-1 gap-4 md:grid-cols-2` of two `Card`s — "Clientes importados" with `border-l-green-600` + `CircleCheck`, "Linhas puladas" with `border-l-amber-500` + `TriangleAlert`; each `CardContent className="flex flex-col gap-2 p-6"`, icon+label row `text-sm font-semibold text-muted-foreground` with `size-4 text-muted-foreground` icon, and the count as `text-xl leading-[1.2] font-semibold text-foreground` formatted with `new Intl.NumberFormat("pt-BR")`. Do NOT use the 36px size — it is the Dashboard's non-precedential waiver (UI-SPEC Typography). Below the tiles, render the breakdown ONLY when `puladas.length > 0`: heading "Motivos das linhas puladas" + a plain `<ul>`/`<li>` (`text-sm text-muted-foreground`), one line per group as `{motivo} — {quantidade} linha(s)`. Then a `flex gap-2` buttons row: `<Button onClick={onVerClientes}>Ver clientes</Button>` (default/accent variant, the only blue element) and `<Button variant="outline" onClick={onImportarOutra}>Importar outra planilha</Button>`.

Wire `ImportWizard`:
  - Add top-of-file `const CONFIRM_ERROR = "Não foi possível concluir a importação agora. Nenhum cliente foi criado. Tente novamente."` (same convention as VALIDATION_ERROR/PARSE_ERROR).
  - Add state: `confirming` (bool), `confirmError` (string|null), `confirmResult` (the action's `data` type | null). Add `const router = useRouter()`.
  - `handleConfirmar()`: guard on `validatedRows`, set `confirming=true`, clear `confirmError`, call `confirmarLoteImportacao(validatedRows, decisions)`; on `.error` set `confirmError = CONFIRM_ERROR`; on success set `confirmResult = result.data`; always clear `confirming`.
  - Replace the no-op button (lines 337-348, remove the "/* Fase 7 scope */" comment entirely) with a disabled+relabel button: `disabled={!validatedRows || confirming}`, label `{confirming ? "Importando clientes…" : "Confirmar importação"}`. Add the confirmError banner (reuse the existing `role="alert"` destructive banner markup already used 3x in this file).
  - Add a top-level `confirmResult !== null` branch BEFORE the `step === 1/2/3` chain: when set, render `<ImportSummary importadosCount={confirmResult.importados.length} puladas={confirmResult.puladas} onVerClientes={() => router.push("/clientes")} onImportarOutra={handleImportarOutra} />` and hide the numbered `<ol>` step indicator (do not leave "3 Revisar" highlighted under a finished summary).
  - `handleImportarOutra()`: reuse handleRemoveFile's reset, plus `setValidatedRows(null); setDecisions({}); setConfirmResult(null); setConfirmError(null); setStep(1)`.

Write `tests/importacao/import-summary.test.tsx` (jsdom, @testing-library/react — jsdom is already wired via environmentMatchGlobs for *.test.tsx per 06-03): render ImportSummary with `importadosCount={5}` and one puladas group → asserts both tile labels + the count "5" + the breakdown line render; render with `puladas={[]}` → asserts the "Motivos das linhas puladas" heading is NOT in the document (zero-puladas case).
  </action>
  <verify>
    <automated>npx vitest run tests/importacao/import-summary.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>ImportSummary renders the two tiles + conditional breakdown + buttons per the UI-SPEC; ImportWizard's confirm button calls confirmarLoteImportacao, shows the in-flight/error states, and swaps to the summary on success with the step indicator hidden; render test + typecheck pass.</done>
  <acceptance_criteria>
    - ImportSummary uses `text-xl leading-[1.2]` (not 36px), green-600/amber-500 left borders, CircleCheck/TriangleAlert muted icons, Intl.NumberFormat("pt-BR").
    - Breakdown section renders only when puladas.length > 0 (verified by the render test's zero-puladas case).
    - "Confirmar importação" is disabled+relabeled while in flight; the CONFIRM_ERROR banner shows on failure; the old no-op comment is removed.
    - confirmResult !== null replaces the wizard body and hides the step `<ol>`; "Ver clientes" pushes /clientes; "Importar outra planilha" resets to Step 1.
    - `npx vitest run tests/importacao/import-summary.test.tsx` and `npx tsc --noEmit` pass.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the full bulk-import write path (real file)</name>
  <what-built>
    The full import write path is now wired: the Supervisor-only /clientes/importar wizard parses a spreadsheet (Phase 6), reviews rows (OK/erro/duplicado with per-row Importar/Pular), and "Confirmar importação" now calls the real `confirmarLoteImportacao` → `importar_clientes_lote` RPC, then shows the "Importação concluída" summary (X importados / Y puladas + reasons). Migration 0004 is applied on the live project (07-01) and all automated tests are green.
  </what-built>
  <action>
    This is a manual verification gate — the coordinator imports a REAL test spreadsheet against the live project and confirms the end-to-end write path (imported clientes appear at "Aguardando contato", the summary counts are correct, and a bad/duplicate row did not block the good rows). Follow the numbered steps below; do not auto-approve.
  </action>
  <how-to-verify>
    1. Start the app: `npm run dev`. Log in as the Supervisor (cristiano.stephano@raiarorganicos.com.br).
    2. Open http://localhost:3000/clientes/importar. Download the model (Baixar modelo), and prepare a small real test spreadsheet with, deliberately: (a) 2-3 clearly valid new rows with unique razão sociais and a real vendedor in the responsável column, (b) 1 row missing endereço or responsável (should be flagged "Erro"), and (c) 1 row whose razão social matches an existing cliente already in the funil (should be flagged "Possível duplicado", leave it as "Pular").
    3. Upload the file, map the columns, continue to Revisar. Confirm the review screen shows the valid rows as OK, the incomplete row as Erro, and the existing-name row as Possível duplicado.
    4. Click "Confirmar importação". Confirm the button relabels to "Importando clientes…" briefly, then the screen swaps to "Importação concluída" with: Clientes importados = the count of valid rows, Linhas puladas = the erro + skipped-duplicado count, and a "Motivos das linhas puladas" list naming each reason.
    5. Click "Ver clientes" → confirm the newly imported clientes appear in the funil kanban in the FIRST column "Aguardando contato" (IMP-09), and that the bad row and the skipped duplicate did NOT create clientes (IMP-06 — a bad/duplicate row did not block or corrupt the good rows).
    6. Optional: re-open the wizard, import another file, and confirm "Importar outra planilha" resets cleanly to Step 1.
  </how-to-verify>
  <resume-signal>Type "approved" if the imported clientes appear at "Aguardando contato", the summary counts are correct, and the bad/duplicate rows were skipped without blocking the good ones — otherwise describe what was off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review UI → confirm action | The per-row decisions + validatedRows the wizard sends are untrusted client data; the Server Action (07-02) and RPC (07-01) re-gate and re-validate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09 | Elevation of Privilege | /clientes/importar UI + confirm button | medium | mitigate | The page already redirects non-Supervisors (existing app-layer guard); the confirm button ultimately hits the Supervisor-gated action (07-02) and RPC (07-01) — UI hiding is never the boundary. |
| T-07-10 | Tampering (double-submit) | Confirmar importação button | low | mitigate | Button is `disabled` + relabeled "Importando clientes…" while the request is in flight (single-submit guard, no dialog — UI-SPEC decision), so a double-click cannot fire two imports. |
| T-07-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | No new packages — Card/Button/Skeleton/lucide-react already installed since Phase 4/6 (UI-SPEC Registry Safety: "No install, no registry gate"). |
</threat_model>

<verification>
- `npx vitest run tests/importacao/import-summary.test.tsx` and `npx tsc --noEmit` pass.
- Human checkpoint approved: imported clientes land at "Aguardando contato", summary counts correct, bad/duplicate rows skipped without blocking the batch.
- Full suite still green: `npm test`.
</verification>

<success_criteria>
- A Supervisor completes a real bulk import end-to-end and sees an accurate "X importados / Y puladas" summary (D-01).
- Imported clientes are visible in the funil at "Aguardando contato" (IMP-09); a bad/duplicate row does not block the rest (IMP-06).
- Per-row Importar/Pular decisions correctly drive the confirmed set.
</success_criteria>

<output>
Create `.planning/phases/07-importa-o-confirma-o-e-grava-o/07-03-SUMMARY.md` when done.
</output>
