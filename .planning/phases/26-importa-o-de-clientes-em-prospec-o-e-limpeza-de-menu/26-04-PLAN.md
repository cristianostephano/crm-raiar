---
phase: 26-importa-o-de-clientes-em-prospec-o-e-limpeza-de-menu
plan: 04
type: execute
wave: 3
depends_on: ["26-02", "26-03"]
files_modified:
  - components/layout/AppSidebar.tsx
  - app/(app)/clientes/importar/page.tsx
  - app/(app)/clientes/importar-ativos/page.tsx
  - tests/importacao/AppSidebar.test.tsx
  - tests/e2e/importar-guard.spec.ts
  - tests/importacao/mapping.test.ts
  - tests/importacao/column-mapping-table.test.tsx
  - lib/clientes/nomeExibicao.ts
  - components/clientes/ClienteCard.tsx
  - components/clientes/KanbanBoard.tsx
  - components/clientes/ClienteDetailSheet.tsx
  - tests/clientes/nome-exibicao.test.ts
autonomous: false
requirements: [PROSP-01, MENU-01, MENU-02, PROSP-02]

must_haves:
  truths:
    - "No menu Administração, a entrada da planilha de prospecção se chama 'Importar Clientes em Prospecção' (PROSP-01)."
    - "As entradas 'Importar CNPJ' e 'Importar frequências' não aparecem em lugar nenhum do menu, e as rotas antigas deixam de existir (MENU-01/MENU-02)."
    - "O projeto compila e a suíte de testes roda sem nenhum import apontando para arquivo apagado."
    - "Os dois testes de infraestrutura genérica de mapeamento continuam existindo e passando, sem depender de nenhum arquivo apagado."
    - "Um cliente sem razão social preenchida aparece no cartão do funil e na ficha com o Nome Fantasia; sem nenhum dos dois, com um rótulo único de ausência."
  artifacts:
    - "components/layout/AppSidebar.tsx — seção Administração com 4 entradas: Gerenciar equipe, Configurações, Importar Clientes em Prospecção, Importar Clientes Ativos"
    - "lib/clientes/nomeExibicao.ts — autoridade única do nome exibido de um cliente"
    - "tests/clientes/nome-exibicao.test.ts — prova mecânica das três quedas de exibição"
  key_links:
    - "Dois arquivos de teste de infraestrutura GENÉRICA de mapeamento tomam emprestado o vocabulário de frequências só como 'lista alternativa menor' — apagar esse vocabulário sem substituir o empréstimo quebra testes que precisam continuar existindo."
    - "O teste de ordem do menu que ancora na entrada de CNPJ precisa de âncora nova, senão a remoção derruba um teste que não fala de CNPJ."
    - "O arquivo da rota de clientes ativos cita a rota removida num comentário — não quebra compilação, mas deixa referência morta para a próxima pessoa."
    - "O cartão do funil recebe hoje uma forma achatada que NÃO carrega Nome Fantasia — sem acrescentar esse campo à forma e ao ponto onde ela é montada, o rótulo novo não teria de onde tirar o nome."
---

<objective>
Fechar a fase pela camada de tela: renomear a entrada de menu da planilha de prospecção (PROSP-01), remover por completo as duas planilhas avulsas que a Fase 25 tornou redundantes (MENU-01/MENU-02) e dar um nome visível aos clientes que agora podem ser importados sem razão social.

Purpose: o menu hoje oferece quatro planilhas de importação, duas das quais viraram caminho morto quando "Importar Clientes Ativos" entrou em produção — isso confunde quem usa e mantém código, rota e teste vivos sem motivo. E, depois do relaxamento de PROSP-02, um cliente importado só com Nome Fantasia apareceria com o título do cartão em branco no funil, que é uma regressão visível criada por esta própria fase.

Output: menu limpo com quatro entradas na seção Administração, 29 arquivos removidos sem nenhuma referência solta, e um rótulo de nome de cliente com fonte única.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-importa-o-de-clientes-em-prospec-o-e-limpeza-de-menu/26-RESEARCH.md
@.planning/phases/26-importa-o-de-clientes-em-prospec-o-e-limpeza-de-menu/26-02-SUMMARY.md

# Arquivos editados por este plano
@components/layout/AppSidebar.tsx
@tests/importacao/AppSidebar.test.tsx

# Molde de "rótulo único quando o campo está vazio" já aceito pelo dono do projeto
@lib/clientes/rotuloLocalizacao.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: PROSP-01 — renomear a planilha de prospecção no menu, na tela e nos testes</name>
  <files>components/layout/AppSidebar.tsx, app/(app)/clientes/importar/page.tsx, tests/importacao/AppSidebar.test.tsx, tests/e2e/importar-guard.spec.ts</files>
  <action>
O texto final travado é, literalmente, **Importar Clientes em Prospecção** — exatamente como aparece nos requisitos e no `PROJECT.md`. Não inventar variação curta: a classe de truncamento já existe em todo rótulo de link da barra lateral e cuida de rótulo longo no modo compacto.

**1. `components/layout/AppSidebar.tsx`**
Trocar o rótulo da entrada cuja rota é `/clientes/importar` pelo texto travado. Não mexer na rota, no ícone nem na posição dela na lista. Reescrever os comentários de diferenciação de ícone que citam o rótulo ANTIGO pelo nome, para citarem o novo — comentário que cita rótulo inexistente vira armadilha para a próxima pessoa.

**2. `app/(app)/clientes/importar/page.tsx`**
Trocar o texto do título principal da página (o cabeçalho de nível 1) pelo mesmo texto travado, para o menu e a tela dizerem a mesma coisa. Atualizar também a menção ao nome da tela no comentário de cabeçalho do arquivo.

**3. `tests/importacao/AppSidebar.test.tsx`**
- Os dois casos que hoje verificam a visibilidade dessa entrada para Supervisor e para Vendedor passam a procurar pelo texto novo.
- Acrescentar, no caso do Supervisor, uma asserção de que NENHUM link com o rótulo antigo é renderizado — é isso que impede a renomeação de ficar pela metade.
- O caso que verifica a ordem no documento e usa essa entrada como âncora passa a usar o rótulo novo.
- Não apagar nenhum caso nesta task; a remoção das entradas de CNPJ e de frequências é a Task 2.

**4. `tests/e2e/importar-guard.spec.ts`**
Este arquivo NÃO é apagado — ele cobre a rota de prospecção, que está sendo renomeada e não removida. Trocar a asserção de cabeçalho da página pelo texto novo. Não mexer em mais nada dele.
  </action>
  <verify>
    <automated>npx vitest run tests/importacao/AppSidebar.test.tsx --reporter=basic && [ "$(grep -c 'Importar Clientes em Prospecção' components/layout/AppSidebar.tsx)" -ge 1 ] && [ "$(grep -c 'Importar Clientes em Prospecção' 'app/(app)/clientes/importar/page.tsx')" -ge 1 ] && [ "$(grep -c 'Importar Clientes em Prospecção' tests/e2e/importar-guard.spec.ts)" -ge 1 ] && npx tsc --noEmit</automated>
  </verify>
  <done>Os testes da barra lateral passam com o rótulo novo e provam que o rótulo antigo não é mais renderizado. O título da página, o rótulo do menu e a asserção do teste de ponta a ponta usam o mesmo texto travado. A verificação de tipos passa.</done>
</task>

<task type="auto">
  <name>Task 2: MENU-01 e MENU-02 — remover "Importar CNPJ" e "Importar frequências" do sistema por completo</name>
  <files>components/layout/AppSidebar.tsx, tests/importacao/AppSidebar.test.tsx, tests/importacao/mapping.test.ts, tests/importacao/column-mapping-table.test.tsx, app/(app)/clientes/importar-ativos/page.tsx</files>
  <action>
Seguir EXATAMENTE o inventário de remoção levantado pela pesquisa da fase — não improvisar outra lista. Remover com `git rm`.

**A. Reparar PRIMEIRO os dois testes que tomam emprestado o vocabulário de frequências**

Antes de apagar qualquer arquivo, consertar o empréstimo — senão a suíte quebra por módulo não encontrado, um erro barulhento que parece bem pior do que é.

`tests/importacao/mapping.test.ts` e `tests/importacao/column-mapping-table.test.tsx` importam o vocabulário de frequências apenas como "lista alternativa MENOR", para provar que a infraestrutura genérica de mapeamento respeita a lista de campos recebida. Eles não testam o recurso de frequências e precisam continuar existindo.

Em cada um dos dois arquivos: substituir esse import por uma lista de campos declarada localmente no próprio arquivo de teste, tipada com a interface genérica de definição de campo que vem de `lib/importacao/types.ts`, contendo poucos campos e usando ao menos um rótulo que NÃO exista no vocabulário de prospecção. O vocabulário de clientes ativos NÃO serve de substituto: ele tem as mesmas 16 chaves e os mesmos rótulos do de prospecção, então não conseguiria provar as asserções de "a lista de opções é realmente menor/diferente". Ajustar as asserções que derivam da lista emprestada para derivarem da lista local.

**B. Remover "Importar CNPJ" (MENU-01) — 14 arquivos**

`app/(app)/clientes/importar-cnpj/page.tsx`; `app/actions/importacaoCnpj.ts`; `lib/importacao/typesCnpj.ts`; `lib/importacao/annotarLinhaCnpj.ts`; `lib/importacao/confirmarCnpj.ts`; `lib/importacao/modeloCnpj.ts`; `components/importacao/CnpjImportWizard.tsx`; `components/importacao/CnpjPreviewTable.tsx`; `components/importacao/CnpjImportSummary.tsx`; `tests/importacao/annotarLinhaCnpj.test.ts`; `tests/importacao/confirmarCnpj.test.ts`; `tests/importacao/cnpj-preview-table.test.tsx`; `tests/importacao/cnpj-import-summary.test.tsx`; `tests/importacao/cnpj-vocabulario.test.ts`.

O 15º item do inventário da pesquisa (o teste de integração ao vivo da função de gravação em massa de CNPJ) já foi removido pelo plano 26-01 — confirmar que não existe mais, em vez de tentar apagá-lo de novo. Não existe teste de ponta a ponta do Playwright para essa rota (confirmado por busca exaustiva na pesquisa); não criar um nem procurar por um.

**C. Remover "Importar frequências" (MENU-02) — 15 arquivos**

`app/(app)/clientes/importar-frequencias/page.tsx`; `app/actions/importacaoFrequencia.ts`; `lib/importacao/typesFrequencia.ts`; `lib/importacao/annotarLinhaFrequencia.ts`; `lib/importacao/confirmarFrequencia.ts`; `lib/importacao/modeloFrequencia.ts`; `components/importacao/FrequenciaImportWizard.tsx`; `components/importacao/FrequenciaPreviewTable.tsx`; `components/importacao/FrequenciaImportSummary.tsx`; `tests/importacao/annotarLinhaFrequencia.test.ts`; `tests/importacao/confirmarFrequencia.test.ts`; `tests/importacao/frequencia-preview-table.test.tsx`; `tests/importacao/frequencia-import-summary.test.tsx`; `tests/importacao/frequencia-vocabulario.test.ts`; `tests/e2e/importar-frequencias-guard.spec.ts`.

Também aqui, o teste de integração ao vivo correspondente já saiu no plano 26-01.

**D. `components/layout/AppSidebar.tsx`**

Remover as duas entradas de link e os comentários de diferenciação de ícone que existiam só por causa delas. A seção Administração fica com quatro entradas, nesta ordem: Gerenciar equipe, Configurações, Importar Clientes em Prospecção, Importar Clientes Ativos — ou seja, a ordem relativa de hoje preservada, com as duas do meio simplesmente sumindo (opção de menor diferença, recomendada pela pesquisa). Nenhum ícone das entradas que ficam pode mudar. Nenhum comentário pode continuar citando as duas telas removidas.

**E. `tests/importacao/AppSidebar.test.tsx`**

- Apagar os seis casos que existem só para as duas entradas removidas (visibilidade para Supervisor, ocultação para Vendedor e ordem no documento, de cada uma).
- ATENÇÃO antes de apagar o caso de ocultação de frequências para Vendedor: ele também afirma que "Gerenciar equipe" e "Configurações" ficam escondidas para o Vendedor — isso é cobertura da seção inteira, não da entrada removida. Conferir se outro caso do arquivo ainda garante isso; se não garantir, mover essas asserções para um caso de ocultação que sobreviva, em vez de perder a cobertura.
- O caso de ordem no documento da entrada de clientes ativos ancora hoje na entrada de CNPJ. Reancorar em "Importar Clientes em Prospecção", que passa a ser a entrada imediatamente anterior.
- Acrescentar um caso novo provando que, para o Supervisor, nenhum link com os dois rótulos removidos é renderizado.

**F. `app/(app)/clientes/importar-ativos/page.tsx`**

O comentário de cabeçalho desse arquivo cita a rota removida como precedente estrutural. Não quebra nada, mas vira referência morta: reescrever a frase para citar a rota de prospecção, que continua existindo.

**G. Conferência final de referência solta**

Rodar uma busca no repositório pelos nomes de módulo e pelos caminhos de rota removidos e confirmar zero resultado em `app/`, `components/`, `lib/` e `tests/`. Import apontando para arquivo apagado é quebra de compilação e não é aceitável nem por um commit. Fechar com a construção completa do projeto, que é a prova mais forte disso.
<!-- planner-discipline-allow: importar-cnpj -->
<!-- planner-discipline-allow: importar-frequencias -->
<!-- planner-discipline-allow: typesCnpj -->
<!-- planner-discipline-allow: typesFrequencia -->
<!-- planner-discipline-allow: importacaoCnpj -->
<!-- planner-discipline-allow: importacaoFrequencia -->
  </action>
  <verify>
    <automated>[ ! -d "app/(app)/clientes/importar-cnpj" ] && [ ! -d "app/(app)/clientes/importar-frequencias" ] && [ ! -f lib/importacao/typesCnpj.ts ] && [ ! -f lib/importacao/typesFrequencia.ts ] && [ ! -f app/actions/importacaoCnpj.ts ] && [ ! -f app/actions/importacaoFrequencia.ts ] && [ ! -f tests/e2e/importar-frequencias-guard.spec.ts ] && [ "$(grep -rn 'clientes/importar-cnpj\|clientes/importar-frequencias' app components lib tests 2>/dev/null | wc -l)" = "0" ] && [ "$(grep -rn '@/lib/importacao/typesCnpj\|@/lib/importacao/typesFrequencia\|@/lib/importacao/annotarLinhaCnpj\|@/lib/importacao/annotarLinhaFrequencia\|@/lib/importacao/confirmarCnpj\|@/lib/importacao/confirmarFrequencia\|@/lib/importacao/modeloCnpj\|@/lib/importacao/modeloFrequencia\|@/app/actions/importacaoCnpj\|@/app/actions/importacaoFrequencia\|@/components/importacao/Cnpj\|@/components/importacao/Frequencia' app components lib tests 2>/dev/null | wc -l)" = "0" ] && npx vitest run tests/importacao/AppSidebar.test.tsx tests/importacao/mapping.test.ts tests/importacao/column-mapping-table.test.tsx tests/importacao/modelo.test.ts tests/importacao/prospeccao-vocabulario.test.ts --reporter=basic && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Os 29 arquivos do inventário não existem mais. Nenhum arquivo em `app/`, `components/`, `lib/` ou `tests/` referencia as rotas ou os módulos removidos. Os dois testes de infraestrutura de mapeamento passam com a lista de campos local. A seção Administração tem exatamente 4 entradas na ordem esperada e os testes da barra lateral provam isso. A construção completa do projeto conclui sem erro.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Nome exibido de um cliente sem razão social</name>
  <files>lib/clientes/nomeExibicao.ts, components/clientes/ClienteCard.tsx, components/clientes/KanbanBoard.tsx, components/clientes/ClienteDetailSheet.tsx, tests/clientes/nome-exibicao.test.ts</files>
  <behavior>
    - Cliente com razão social preenchida → exibe a razão social.
    - Cliente sem razão social (nula, ausente ou só com espaços) e com Nome Fantasia → exibe o Nome Fantasia.
    - Cliente sem razão social e sem Nome Fantasia → exibe o rótulo único de ausência.
    - A função tolera nulo e indefinido nos dois argumentos, sem estourar.
  </behavior>
  <action>
Esta task fecha uma regressão visível criada pela própria fase: com razão social opcional (PROSP-02), um cliente importado só com Nome Fantasia apareceria com o título do cartão do funil em branco.

**1. Módulo novo `lib/clientes/nomeExibicao.ts`**

Espelhar literalmente a forma de `lib/clientes/rotuloLocalizacao.ts` — módulo puro, sem import de Supabase nem de cabeçalhos de requisição, importável tanto de componente de servidor quanto de componente de cliente. Exportar:

- uma constante com o rótulo de ausência, em português, na mesma família dos rótulos "Sem cidade"/"Sem estado" já aceitos pelo dono do projeto
- uma função que recebe razão social e Nome Fantasia (ambos podendo ser texto, nulo ou indefinido) e devolve o primeiro dos dois que não estiver vazio, caindo para o rótulo de ausência quando os dois estiverem vazios. Texto só com espaços conta como vazio, exatamente como o módulo de localização já trata.

Comentário de cabeçalho com a mesma advertência que o módulo de localização carrega: todo lugar que exibe o nome de um cliente deve ler daqui, nunca escrever o texto de ausência à mão — dois lugares escrevendo o mesmo texto é como a tela e a ficha acabam discordando.

**2. Teste novo `tests/clientes/nome-exibicao.test.ts`**

Cobrir cada item do bloco de comportamento acima. Espelhar a forma de `tests/clientes/rotulo-localizacao.test.ts`.

**3. `components/clientes/ClienteCard.tsx` + `components/clientes/KanbanBoard.tsx`**

A forma achatada que o cartão recebe hoje NÃO carrega Nome Fantasia. Acrescentar esse campo (texto ou nulo) a essa forma e preenchê-lo em todo ponto onde ela é montada — buscar por todas as construções dessa forma antes de editar, para não deixar nenhuma de fora. O campo já existe na leitura de clientes que alimenta a tela, então é repasse, não consulta nova.

No cartão, o título e o atributo de dica de texto do título passam a usar a função nova, alimentada pela razão social e pelo Nome Fantasia. Nenhuma outra mudança visual.

Na tela do funil: se a ordenação por nome e/ou o filtro de busca por texto lerem a razão social diretamente, trocar pelo nome exibido — senão os clientes sem razão social ficariam impossíveis de encontrar pela busca e cairiam todos juntos no fim da ordenação. Se esses trechos já lidarem com mais de um campo, apenas incluir o nome exibido, sem reescrever a lógica.

**4. `components/clientes/ClienteDetailSheet.tsx`**

Dois pontos de exibição passam a usar a função nova: o título do cabeçalho da ficha e o texto da caixa de confirmação de exclusão. Não mexer nos campos de formulário — o campo de edição da razão social continua mostrando o valor real (que pode estar vazio), porque é um campo editável, não um rótulo. Conferir se a forma de cliente que este componente recebe já traz o Nome Fantasia; se não trouxer, repassar do mesmo lugar de onde vem a razão social.

Cuidado com a supressão de regra de lint já documentada por volta da linha 227 deste arquivo (efeito de reinicialização com muitos setters): não ampliar nem piorar essa supressão.
  </action>
  <verify>
    <automated>npx vitest run tests/clientes/nome-exibicao.test.ts tests/clientes/rotulo-localizacao.test.ts --reporter=basic && [ "$(grep -c 'nomeExibicao' components/clientes/ClienteCard.tsx)" -ge 1 ] && [ "$(grep -c 'nomeExibicao' components/clientes/ClienteDetailSheet.tsx)" -ge 1 ] && npx tsc --noEmit && npm run lint && npm run build</automated>
  </verify>
  <done>O módulo novo existe, passa nos testes e cobre as três quedas. O cartão do funil e a ficha leem o nome exibido dele, e o Nome Fantasia chega até o cartão. A verificação de tipos, o lint e a construção do projeto passam.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — conferir o menu limpo, as rotas removidas e o nome exibido</name>
  <files>—</files>
  <action>
Parar a execução e apresentar ao dono do projeto o roteiro de conferência abaixo. Não marcar o plano nem a fase como concluídos antes do sinal de retomada. Se algum passo divergir, tratar como defeito deste plano e corrigir antes de seguir.
  </action>
  <what-built>
O menu da seção Administração agora tem quatro entradas: Gerenciar equipe, Configurações, "Importar Clientes em Prospecção" (nome novo da planilha que era só "Importar clientes") e "Importar Clientes Ativos". As telas "Importar CNPJ" e "Importar frequências" foram removidas do sistema inteiro — menu, tela, rota, código e testes — e as funções de banco delas já tinham sido apagadas no plano 26-01. Além disso, clientes sem razão social preenchida passam a aparecer pelo Nome Fantasia no cartão do funil e na ficha.
  </what-built>
  <how-to-verify>
1. Rode o sistema (`npm run dev`) e entre com a conta de Supervisor.
2. Abra o menu lateral e confira a seção Administração: precisa ter EXATAMENTE quatro entradas, nesta ordem — Gerenciar equipe, Configurações, Importar Clientes em Prospecção, Importar Clientes Ativos. Nada de "Importar CNPJ" nem "Importar frequências".
3. Clique em "Importar Clientes em Prospecção": o título no alto da tela precisa ser o mesmo texto do menu.
4. Na barra de endereço, tente abrir `/clientes/importar-cnpj` e depois `/clientes/importar-frequencias`. As duas precisam devolver "página não encontrada". Se qualquer uma abrir, pare e avise.
5. Ainda como Supervisor, abra "Importar Clientes Ativos" e confirme que essa tela continua funcionando normalmente (ela NÃO foi removida).
6. Vá para a tela de Clientes (funil) e olhe os clientes de prospecção importados no checkpoint do plano 26-03, os que estão sem razão social: o título do cartão agora precisa mostrar o Nome Fantasia, e não ficar em branco.
7. Abra a ficha de um desses clientes (clique no cartão): o título da ficha também precisa mostrar o Nome Fantasia. Dentro da ficha, o campo "Razão social" continua vazio e editável — isso é o correto, é um campo de digitação, não um rótulo.
8. Use a busca da tela de Clientes com um pedaço do Nome Fantasia de um desses clientes: ele precisa aparecer no resultado.
9. Entre com uma conta de Vendedor e confirme que a seção Administração inteira continua invisível para ele.
  </how-to-verify>
  <resume-signal>Escreva "aprovado" se os 9 passos se comportaram como descrito, ou diga qual passo divergiu e o que apareceu na tela.</resume-signal>
  <done>O dono do projeto confirmou no navegador: seção Administração com as 4 entradas na ordem esperada, rótulo novo no menu e no título da tela, as duas rotas antigas devolvendo página não encontrada, a tela de clientes ativos intacta, o Nome Fantasia aparecendo no cartão e na ficha de quem não tem razão social, a busca encontrando esses clientes, e a seção inteira invisível para o Vendedor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → rota protegida | Cada tela de importação faz a checagem de papel no servidor antes de renderizar; remover a tela remove essa superfície inteira |
| menu (tela) → regra de acesso do banco | A visibilidade do menu é só reflexo de experiência de uso; a regra de linha do banco continua sendo a fronteira real de autorização |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-12 | Elevation of Privilege | Rota antiga de importação continuando acessível por endereço direto depois de sumir do menu | high | mitigate | As pastas de rota são removidas de fato (não só o link); a checagem mecânica confirma a ausência dos diretórios e o passo 4 do checkpoint humano confirma "página não encontrada" no navegador |
| T-26-13 | Denial of Service | Import solto apontando para arquivo apagado derrubando a construção do projeto | high | mitigate | Busca dirigida por cada caminho de módulo removido, mais a construção completa do projeto na checagem mecânica das Tasks 2 e 3 |
| T-26-14 | Information Disclosure | Perda silenciosa da cobertura de "a seção de Administração inteira fica escondida para o Vendedor" ao apagar o teste que carregava essa asserção | medium | mitigate | A ação da Task 2 obriga a conferir e, se necessário, mover essas asserções para um caso sobrevivente; o passo 9 do checkpoint humano confirma no navegador |
| T-26-15 | Spoofing | Cliente sem nome visível dificultando distinguir um registro de outro na tela | low | mitigate | Task 3 dá fonte única ao nome exibido, com queda para Nome Fantasia e depois para rótulo explícito de ausência — nunca título em branco |
| T-26-SC | Tampering | instalações npm/pip/cargo | low | accept | Este plano não instala nenhum pacote; nenhum checkpoint de legitimidade é necessário |
</threat_model>

<verification>
1. `npm run build` conclui sem erro — prova mais forte de que nenhum import ficou apontando para arquivo apagado.
2. `npx tsc --noEmit` e `npm run lint` sem erros.
3. `npx vitest run tests/importacao/AppSidebar.test.tsx tests/importacao/mapping.test.ts tests/importacao/column-mapping-table.test.tsx tests/clientes/nome-exibicao.test.ts tests/clientes/rotulo-localizacao.test.ts --reporter=basic` — todos verdes.
4. `git diff --stat` mostra 29 arquivos removidos (14 da planilha de CNPJ, 15 da de frequências) e nenhum arquivo removido fora desse inventário.
5. `ls tests/e2e/` ainda lista `importar-guard.spec.ts` (a rota de prospecção foi renomeada, não removida) e já não lista o de frequências.
6. Checkpoint humano (Task 4) aprovado.

Observação de contexto: os testes de ponta a ponta do Playwright não são executados neste plano — conforme os Blockers em `.planning/STATE.md`, as contas semente foram apagadas e o login automatizado não sobe. A asserção do arquivo de ponta a ponta é atualizada por texto (Task 1) e a verificação equivalente é feita à mão nos passos 3 e 4 do checkpoint humano.
</verification>

<success_criteria>
- A entrada de menu da planilha de prospecção se chama "Importar Clientes em Prospecção", e o título da tela diz o mesmo (PROSP-01).
- "Importar CNPJ" e "Importar frequências" sumiram do menu, e as duas rotas devolvem página não encontrada (MENU-01/MENU-02).
- Os 29 arquivos do inventário foram removidos, zero referência solta, projeto compila e constrói.
- Os dois testes de infraestrutura genérica de mapeamento continuam existindo e verdes, com lista de campos local.
- Cliente sem razão social aparece pelo Nome Fantasia no cartão e na ficha, e continua encontrável pela busca (PROSP-02).
- O dono do projeto confirmou os 9 passos no navegador.
</success_criteria>

<output>
Create `.planning/phases/26-importa-o-de-clientes-em-prospec-o-e-limpeza-de-menu/26-04-SUMMARY.md` when done
</output>
