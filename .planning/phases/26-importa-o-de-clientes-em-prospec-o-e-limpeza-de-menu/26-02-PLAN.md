---
phase: 26-importa-o-de-clientes-em-prospec-o-e-limpeza-de-menu
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/importacao/types.ts
  - lib/validations/importacao.ts
  - lib/importacao/annotarLinha.ts
  - lib/importacao/modelo.ts
  - lib/importacao/dedupe.ts
  - lib/importacao/confirmar.ts
  - lib/importacao/confirmarAtivo.ts
  - app/actions/importacao.ts
  - tests/importacao/prospeccao-vocabulario.test.ts
  - tests/importacao/mapping.test.ts
  - tests/importacao/annotarLinha.test.ts
  - tests/importacao/modelo.test.ts
  - tests/importacao/dedupe.test.ts
  - tests/importacao/confirmar.test.ts
autonomous: true
requirements: [PROSP-02, PROSP-03]

must_haves:
  truths:
    - "Uma linha de planilha de prospecção com APENAS Nome Fantasia e Responsável preenchidos é aceita na revisão (situação 'ok', zero motivos de recusa) — PROSP-02."
    - "Uma linha SEM Nome Fantasia é recusada com uma frase própria, mesmo que a razão social esteja preenchida — PROSP-02."
    - "Razão social em branco chega à camada de gravação como valor NULO, nunca como texto vazio — sem isso, a segunda empresa sem razão social do sistema é classificada como duplicada por engano (Bug A da pesquisa)."
    - "A comparação de duplicados não quebra quando um cliente já existente tem razão social nula (Bug B da pesquisa)."
    - "Duas linhas da mesma planilha sem razão social e com o mesmo Nome Fantasia são reconhecidas como possível duplicado uma da outra."
    - "O modelo de planilha para download marca visualmente quais colunas são obrigatórias, e um modelo preenchido e reenviado continua sendo mapeado automaticamente coluna a coluna."
    - "Nada neste plano toca em etapa/situação do funil — cliente importado continua nascendo em 'Aguardando contato' (PROSP-03)."
  artifacts:
    - "lib/importacao/types.ts — SYSTEM_FIELDS com obrigatoriedade em exatamente nomeFantasia + responsavel"
    - "lib/validations/importacao.ts — createImportRowSchema declarando nomeFantasia pela primeira vez e razaoSocial como opcional"
    - "lib/importacao/annotarLinha.ts — ResolvedRow.razaoSocial nulável e ramo de campo obrigatório trocado"
    - "lib/importacao/dedupe.ts — normalização tolerante a nulo + chave de reserva por Nome Fantasia"
    - "lib/importacao/confirmar.ts — RpcClienteRow.razao_social nulável"
    - "tests/importacao/prospeccao-vocabulario.test.ts — invariante mecânica de quais campos são obrigatórios"
  key_links:
    - "São TRÊS superfícies independentes de obrigatoriedade que hoje só concordam por convenção (SYSTEM_FIELDS, createImportRowSchema e o ramo literal de mensagens dentro de annotarLinha) — mexer em uma sem as outras produz uma tela que promete uma coisa e um servidor que faz outra."
    - "Razão social nula e razão social em branco são a MESMA coisa para o usuário e coisas DIFERENTES para o Postgres: dois nulos nunca conflitam numa restrição de unicidade, dois textos vazios sempre conflitam."
    - "A chave de reserva por Nome Fantasia é o que garante que duas linhas sem razão social sejam distinguíveis uma da outra — o plano 26-03 depende dessa garantia."
---

<objective>
Deixar a planilha de prospecção minimalista: só Nome Fantasia e Responsável passam a ser obrigatórios, razão social e todo o resto viram opcionais (PROSP-02), corrigindo no mesmo passo os dois defeitos reais que a pesquisa da fase encontrou lendo o código linha a linha e que uma implementação ingênua de PROSP-02 dispararia.

Purpose: hoje o time não consegue importar prospects porque a planilha exige razão social — dado que quase nunca existe antes do primeiro contato. Relaxar a exigência sem os dois consertos criaria um problema pior e silencioso: a partir da segunda empresa sem razão social, toda empresa nova sem razão social seria recusada como "duplicada", e a validação inteira do lote passaria a estourar assim que qualquer cliente do banco ficasse com razão social nula.

Output: o vocabulário de campos, o esquema de validação, a anotação linha a linha, o gerador do modelo de planilha, a comparação de duplicados e a camada de confirmação, todos coerentes entre si, com testes que provam cada comportamento novo.
</objective>

<execution_context>
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/Cristiano/workspace/crm-raiar/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-importa-o-de-clientes-em-prospec-o-e-limpeza-de-menu/26-RESEARCH.md

# Arquivos editados por este plano
@lib/importacao/types.ts
@lib/validations/importacao.ts
@lib/importacao/annotarLinha.ts
@lib/importacao/dedupe.ts
@lib/importacao/confirmar.ts
@lib/importacao/modelo.ts

# Molde vivo de como este projeto declara "obrigatório + frase de recusa" numa lista de campos
@lib/importacao/typesAtivo.ts

# Precedente de teste de invariante de vocabulário (a forma a espelhar no arquivo de teste novo)
@tests/importacao/ativos-vocabulario.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trocar a obrigatoriedade — Nome Fantasia entra, razão social sai (as três superfícies no mesmo passo)</name>
  <files>lib/importacao/types.ts, lib/validations/importacao.ts, lib/importacao/annotarLinha.ts, tests/importacao/prospeccao-vocabulario.test.ts, tests/importacao/mapping.test.ts, tests/importacao/annotarLinha.test.ts</files>
  <behavior>
    - Linha com Nome Fantasia + Responsável preenchidos e TUDO o mais em branco → situação "ok", lista de motivos vazia.
    - Linha com razão social preenchida mas Nome Fantasia em branco → situação "erro", com a frase própria de Nome Fantasia entre os motivos.
    - Linha com Nome Fantasia preenchido e Responsável em branco → situação "erro", com a frase de Responsável entre os motivos.
    - Linha com Nome Fantasia e Responsável ambos em branco → situação "erro", com as duas frases.
    - `SYSTEM_FIELDS` tem exatamente dois campos marcados como obrigatórios, e são `nomeFantasia` e `responsavel`.
    - A função que lista campos obrigatórios ainda não mapeados (`requiredFieldsFaltando`) passa a cobrar Nome Fantasia e a não cobrar razão social.
  </behavior>
  <action>
Esta task muda as TRÊS superfícies independentes de obrigatoriedade da importação de prospecção no mesmo commit. A pesquisa da fase (Pitfall 1) documenta que elas não são derivadas umas das outras — mexer em uma só produz uma tela que promete uma regra e um servidor que aplica outra.

**1. `lib/importacao/types.ts` (vocabulário / superfície da tela de mapeamento)**

Na lista `SYSTEM_FIELDS`, trocar a marcação de obrigatório: o campo `razaoSocial` passa a ser opcional e o campo `nomeFantasia` passa a ser obrigatório. `responsavel` continua obrigatório, todos os demais continuam opcionais. Não mudar a ordem, os rótulos, as chaves nem a quantidade de campos (continuam 16). Não criar nenhum tipo ou lista nova — a pesquisa confirma que, depois da Fase 25, `SYSTEM_FIELDS` é usada exclusivamente pelo fluxo de prospecção, então a edição direta é segura e nenhuma abstração de "perfil de obrigatoriedade" é necessária (a Fase 25 já resolveu o problema do segundo vocabulário forkando `typesAtivo.ts`).

Reescrever o comentário de cabeçalho do arquivo para descrever a regra NOVA (dois obrigatórios: Nome Fantasia e Responsável; razão social opcional desde esta fase por PROSP-02, apoiada na coluna já nulável desde a migration 0024) e por que ela diverge do cadastro manual. O comentário antigo descreve a regra antiga e ficaria mentindo.

**2. `lib/validations/importacao.ts` (esquema de validação usado pelo servidor)**

Este esquema é mantido à mão e hoje NÃO declara `nomeFantasia` de forma nenhuma — não basta virar uma chave, o campo precisa ser acrescentado pela primeira vez:

- acrescentar `nomeFantasia` como texto exigido com comprimento mínimo 1, cuja mensagem seja a frase de recusa "Nome Fantasia não informado"
- trocar `razaoSocial` para texto opcional (sem exigência de comprimento mínimo)
- não mexer em `responsavel` nem em nenhum dos demais campos opcionais

Atualizar o comentário de cabeçalho pelo mesmo motivo do item 1.

**3. `lib/importacao/annotarLinha.ts` (ramo literal de mensagens)**

O bloco que percorre os problemas devolvidos pelo esquema tem hoje um encadeamento literal de duas condições, uma para razão social e outra para responsável. Trocar a condição de razão social por uma condição de `nomeFantasia`, apontando para uma constante nova de frase de recusa de Nome Fantasia declarada no topo do arquivo junto da que já existe para responsável; remover a constante de frase de razão social, que deixa de ter uso.

No objeto entregue ao esquema para validação, acrescentar a chave `nomeFantasia` alimentada pela célula já saneada (mesma forma das outras: valor saneado, ou texto vazio quando ausente), senão o campo recém-exigido nunca seria avaliado.

NÃO mexer nesta task na forma resolvida da razão social (ela continua caindo para texto vazio por enquanto) — isso é a Task 3, junto com a camada de confirmação, para que cada task termine compilando.

**4. Teste novo `tests/importacao/prospeccao-vocabulario.test.ts`**

Espelhar a forma de `tests/importacao/ativos-vocabulario.test.ts`. Provar mecanicamente:
- `SYSTEM_FIELDS` continua com 16 campos, mesmas chaves e mesma ordem de hoje
- exatamente 2 definições são obrigatórias, e o conjunto de chaves obrigatórias é `nomeFantasia` + `responsavel`
- o esquema de validação aceita uma entrada só com Nome Fantasia e Responsável
- o esquema recusa uma entrada com Nome Fantasia em branco, e a mensagem do problema é a frase de recusa de Nome Fantasia
- o esquema aceita razão social ausente E razão social em branco

**5. `tests/importacao/mapping.test.ts`**

O bloco de testes de campos obrigatórios ainda não mapeados assume a lista antiga. Ajustar os dois casos: o que lista campos faltando passa a esperar Nome Fantasia entre os cobrados; o que espera lista vazia passa a mapear também a coluna de Nome Fantasia. Não tocar em nenhum outro bloco deste arquivo nesta task.

**6. `tests/importacao/annotarLinha.test.ts`**

Inverter o caso existente que hoje espera recusa por razão social ausente: com razão social em branco, Nome Fantasia e Responsável preenchidos, a linha passa a ser aceita. Acrescentar os quatro casos descritos no bloco de comportamento acima. Preservar todos os demais casos do arquivo (categoria/produto/cidade/estado/responsável não encontrado) sem alteração.
  </action>
  <verify>
    <automated>npx vitest run tests/importacao/prospeccao-vocabulario.test.ts tests/importacao/mapping.test.ts tests/importacao/annotarLinha.test.ts --reporter=basic && npx tsc --noEmit</automated>
  </verify>
  <done>Os três arquivos de teste passam. `SYSTEM_FIELDS` tem exatamente 2 obrigatórios (`nomeFantasia`, `responsavel`). O esquema de validação declara `nomeFantasia` como exigido e `razaoSocial` como opcional. Uma linha só com Nome Fantasia e Responsável é aceita de ponta a ponta pela anotação. A verificação de tipos do projeto passa sem erros.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modelo de planilha marca os obrigatórios + comparação de duplicados tolerante a nulo e com chave de reserva</name>
  <files>lib/importacao/modelo.ts, lib/importacao/dedupe.ts, tests/importacao/modelo.test.ts, tests/importacao/dedupe.test.ts</files>
  <behavior>
    - O cabeçalho do modelo de planilha marca cada coluna obrigatória com um sufixo visível, e deixa as opcionais sem marca.
    - Todo cabeçalho gerado pelo modelo continua sendo reconhecido automaticamente pela sugestão de mapeamento (o usuário baixa o modelo, preenche e reenvia sem precisar mapear coluna a coluna na mão).
    - A normalização de nome devolve texto vazio para entrada nula/indefinida, em vez de estourar.
    - Uma lista de nomes já existentes contendo valores nulos não derruba a comparação de duplicados.
    - Duas linhas da mesma planilha sem razão social e com o mesmo Nome Fantasia são marcadas como duplicadas uma da outra.
    - Duas linhas sem razão social e com Nome Fantasia diferente NÃO são marcadas como duplicadas.
    - Todo comportamento já existente para linhas COM razão social continua idêntico.
  </behavior>
  <action>
**1. `lib/importacao/modelo.ts` — marcar visualmente os campos obrigatórios**

Hoje o cabeçalho do modelo é o rótulo puro de cada campo. Passar a acrescentar um sufixo visível de obrigatoriedade (um espaço seguido de asterisco) ao rótulo de todo campo marcado como obrigatório, deixando os opcionais exatamente como estão. Declarar esse sufixo como uma constante única no próprio arquivo e derivar o cabeçalho dela — nunca digitar a marca solta em mais de um lugar.

Registrar no comentário de cabeçalho por que essa marca é segura: a sugestão automática de mapeamento normaliza o cabeçalho descartando tudo que não é letra ou número antes de comparar, então um cabeçalho marcado continua batendo com o rótulo do campo. Um modelo baixado, preenchido e reenviado continua sendo mapeado sozinho.

Não mexer em `lib/importacao/modeloAtivo.ts` (modelo da planilha de ativos, Fase 25) — fora do escopo desta fase.

**2. `tests/importacao/modelo.test.ts`**

Ajustar a asserção de cabeçalho para esperar o rótulo com marca nos campos obrigatórios e sem marca nos opcionais (derivando de `SYSTEM_FIELDS`, nunca listando os 16 textos à mão). Acrescentar um caso novo de ida e volta: para cada cabeçalho do modelo gerado, a sugestão automática de mapeamento devolve exatamente a chave do campo correspondente. Preservar os demais casos do arquivo.

**3. `lib/importacao/dedupe.ts` — tolerância a nulo (Bug B) e chave de reserva**

a) A função de normalização passa a aceitar texto, nulo ou indefinido, devolvendo texto vazio para os dois últimos ANTES de qualquer chamada de método de texto. Isso conserta o defeito real descrito na pesquisa: a coluna de razão social é nulável no banco desde a migration 0024, e a lista de nomes existentes é montada com uma conversão de tipo não checada — no primeiro cliente com razão social nula, a comparação de duplicados estoura e derruba a validação do lote inteiro, não só daquela linha.

b) A função de comparação de duplicados passa a aceitar, na entrada do lote, um Nome Fantasia opcional ao lado da razão social; e passa a aceitar nulos tanto na razão social do lote quanto na lista de nomes existentes.

c) Cálculo de chave, com espaço de nomes explícito para os dois lados (lote e existentes), de forma que uma chave de razão social nunca colida com uma chave de Nome Fantasia:
   - chave primária = normalização da razão social, num espaço de nomes próprio
   - quando a chave primária fica vazia, cai para a normalização do Nome Fantasia, num espaço de nomes DIFERENTE
   - quando as duas ficam vazias, a linha continua sendo pulada pela comparação, como hoje

d) Acrescentar um terceiro parâmetro OPCIONAL para a lista de Nomes Fantasia já existentes no banco, com lista vazia como valor padrão, indexado no espaço de nomes de Nome Fantasia. O parâmetro precisa ser opcional para que as três chamadas existentes (duas ações de servidor e o planejamento da confirmação) continuem compilando sem alteração — quem passa esse argumento de fato é o plano 26-03.

e) Reusar a mesma função de normalização para o Nome Fantasia: a pesquisa confirma que ela não tem nada de específico de razão social (é só minúsculas, remoção de acento, remoção de pontuação e remoção de sufixo societário) e o projeto já a reusa assim para nome de categoria, produto e vendedor.

**4. `tests/importacao/dedupe.test.ts`**

Acrescentar casos para: normalização de nulo e de indefinido devolvendo texto vazio; lista de existentes contendo nulo sem estourar; duas linhas sem razão social com o mesmo Nome Fantasia marcadas como duplicadas; duas linhas sem razão social com Nome Fantasia diferente não marcadas; uma linha sem razão social e sem Nome Fantasia continuar sendo ignorada. Preservar todos os casos existentes sem alteração — eles são a prova mecânica de que o comportamento de linhas com razão social não regrediu.
  </action>
  <verify>
    <automated>npx vitest run tests/importacao/modelo.test.ts tests/importacao/dedupe.test.ts tests/importacao/mapping.test.ts --reporter=basic && npx tsc --noEmit</automated>
  </verify>
  <done>Os testes de modelo, de comparação de duplicados e de mapeamento passam. O cabeçalho do modelo marca os obrigatórios e todo cabeçalho gerado continua sendo reconhecido pela sugestão automática de mapeamento. A normalização aceita nulo sem estourar. A chave de reserva por Nome Fantasia funciona dentro do lote e os casos antigos seguem verdes. A verificação de tipos passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Razão social em branco vira valor nulo de ponta a ponta (Bug A)</name>
  <files>lib/importacao/annotarLinha.ts, lib/importacao/confirmar.ts, lib/importacao/confirmarAtivo.ts, app/actions/importacao.ts, tests/importacao/annotarLinha.test.ts, tests/importacao/confirmar.test.ts</files>
  <behavior>
    - Linha com razão social ausente → a forma resolvida traz valor nulo na razão social (nunca texto vazio).
    - Linha com razão social só de espaços em branco → também vira valor nulo.
    - Linha com razão social preenchida → continua trazendo o texto aparado, idêntico a hoje.
    - A carga entregue à função de gravação carrega valor nulo na razão social dessas linhas.
    - A conciliação do que foi gravado conta corretamente várias linhas com razão social nula no mesmo lote.
    - O fluxo de clientes ativos (Fase 25) continua compilando e com o comportamento intacto.
  </behavior>
  <action>
Este é o conserto do Bug A da pesquisa. Rastro do defeito, confirmado linha a linha: a anotação de linha resolve razão social ausente para texto vazio (é o ÚNICO campo opcional do arquivo que não segue o padrão "valor aparado ou nulo" já usado por CNPJ, Nome Fantasia e endereço); esse texto vazio atravessa a camada de confirmação sem alteração e é gravado como texto vazio na coluna. Dois valores nulos nunca conflitam numa restrição de unicidade do Postgres, mas dois textos vazios sempre conflitam — então, a partir da SEGUNDA empresa sem razão social do sistema, toda empresa nova sem razão social é descartada na gravação e reportada como duplicada, o que é falso e destrói justamente o caso de uso que PROSP-02 existe para atender.

**1. `lib/importacao/annotarLinha.ts`**

- Na forma resolvida (`ResolvedRow`), a razão social passa a admitir valor nulo além de texto.
- No cálculo do objeto resolvido, trocar a queda para texto vazio pelo mesmo padrão que CNPJ e Nome Fantasia já usam neste arquivo: apara o valor saneado e devolve valor nulo quando o resultado fica vazio.
- Registrar em comentário curto, ao lado, POR QUE é nulo e não texto vazio (semântica de unicidade do Postgres), para ninguém "simplificar" isso de volta depois.

**2. `lib/importacao/confirmar.ts`**

- Na forma da carga enviada à função de gravação (`RpcClienteRow`), a razão social passa a admitir valor nulo, alinhando-se a todos os outros campos opcionais dessa mesma forma.
- No planejamento da confirmação, o lote entregue à comparação de duplicados passa a levar também o Nome Fantasia de cada candidato (a chave de reserva criada na Task 2), além da razão social.
- Na conciliação do que foi gravado, a lista de nomes devolvidos passa a admitir valores nulos e a forma devolvida passa a admitir razão social nula. Acrescentar comentário explicando o raciocínio de contagem: como valores nulos nunca conflitam na restrição de unicidade, TODA linha com razão social nula enviada é sempre gravada, então casar por presença do valor no conjunto devolvido conta certo — inclusive com várias linhas nulas no mesmo lote.

**3. `lib/importacao/confirmarAtivo.ts`**

Ajuste mínimo de compilação, sem mudança de comportamento: na conciliação do fluxo de clientes ativos, a razão social do item passa por uma queda para texto vazio ao ser registrada na lista de importados. O vocabulário de ativos EXIGE razão social, então esse caminho nunca recebe nulo na prática — o ajuste existe só para o tipo agora nulável não vazar para dentro daquele fluxo. Registrar isso em comentário de uma linha.

**4. `app/actions/importacao.ts`**

Ajuste mínimo de tipo, sem mudança de comportamento: a forma do resultado da ação de confirmação declara hoje que cada item importado tem razão social em texto; passa a admitir também valor nulo, acompanhando a conciliação. Nenhuma outra linha deste arquivo é tocada nesta task (a limpeza da lista de existentes e a passagem do Nome Fantasia são do plano 26-03).

**5. Testes**

Em `tests/importacao/annotarLinha.test.ts`, acrescentar os casos de razão social ausente e de razão social só com espaços, ambos provando valor nulo na forma resolvida, e um caso de razão social preenchida provando que o texto aparado continua chegando igual.

Em `tests/importacao/confirmar.test.ts`, acrescentar: uma carga com duas linhas de razão social nula e Nome Fantasia diferente entre si produz duas entradas para gravação, cada uma com razão social nula; a conciliação dessa carga contra um retorno com dois valores nulos conta 2 importados e 0 pulados. Preservar todos os casos existentes.
  </action>
  <verify>
    <automated>npx vitest run tests/importacao/annotarLinha.test.ts tests/importacao/confirmar.test.ts tests/importacao/confirmarAtivo.test.ts tests/importacao/dedupe.test.ts --reporter=basic && npx tsc --noEmit</automated>
  </verify>
  <done>Razão social ausente ou só com espaços resolve para valor nulo em toda a cadeia até a carga de gravação. A conciliação conta corretamente múltiplas linhas de razão social nula. O fluxo de clientes ativos continua verde e sem mudança de comportamento. A verificação de tipos do projeto passa sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planilha enviada pelo usuário → anotação de linha | Toda célula é entrada não confiável; o saneamento contra injeção de fórmula já existe e não pode ser contornado pelas mudanças deste plano |
| navegador (tela de mapeamento) → ação de servidor | A tela decide quais campos cobrar; o servidor precisa cobrar de novo, sem confiar no que a tela mandou |
| aplicação → restrição de unicidade do Postgres | A semântica de nulo x texto vazio nessa fronteira é o que este plano conserta |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-04 | Tampering | Obrigatoriedade cobrada só na tela de mapeamento e não no servidor | high | mitigate | A Task 1 muda as três superfícies no mesmo commit; a anotação de linha (servidor) valida com o mesmo esquema, e o teste novo de vocabulário prova o par tela/servidor mecanicamente |
| T-26-05 | Denial of Service | Validação do lote inteiro estourando ao encontrar um cliente com razão social nula (Bug B) | high | mitigate | Task 2: a normalização de nome passa a tolerar nulo/indefinido antes de qualquer chamada de método de texto; teste dedicado com nulo na lista de existentes |
| T-26-06 | Tampering | Texto vazio gravado onde deveria haver valor nulo, produzindo classificação falsa de duplicado e descarte de linha (Bug A) | high | mitigate | Task 3 alinha razão social ao padrão "aparado ou nulo" do próprio arquivo, com comentário explicando o porquê e testes que provam o valor nulo em toda a cadeia |
| T-26-07 | Information Disclosure | Saneamento de célula contornado por algum caminho novo | low | accept | Nenhuma mudança deste plano mexe no saneamento de célula; todo valor resolvido continua vindo do mesmo passo de saneamento existente |
| T-26-SC | Tampering | instalações npm/pip/cargo | low | accept | Este plano não instala nenhum pacote (pesquisa da fase: nenhum pacote é instalado nesta fase); nenhum checkpoint de legitimidade é necessário |
</threat_model>

<verification>
1. `npx vitest run tests/importacao/ --reporter=basic` — os arquivos tocados por este plano passam. Observação de contexto: conforme os Blockers em `.planning/STATE.md`, os testes de integração contra o banco ao vivo dessa pasta já falham hoje por contas semente apagadas — falha nesses arquivos específicos é anterior a este plano e não é regressão dele. Provar isoladamente os arquivos alterados, convenção do projeto desde a Fase 13.
2. `npx tsc --noEmit` sem erros.
3. PROSP-03 (sem regressão de etapa do funil): confirmar mecanicamente que a instrução de gravação da função de importação de prospecção não define etapa nem situação de acompanhamento, continuando a depender dos valores padrão das colunas — `sed -n '/insert into clientes/,/on conflict/p' supabase/migrations/0019_importar_clientes_lote_cnpj_nome_fantasia.sql | grep -ciE '\betapa\b|status_acompanhamento'` devolve `0`. Nenhum arquivo de migration é editado por este plano.
4. `git diff --stat` não lista `lib/importacao/typesAtivo.ts`, `lib/importacao/annotarLinhaAtivo.ts` nem `lib/importacao/modeloAtivo.ts` — o fluxo de clientes ativos da Fase 25 não é tocado.
</verification>

<success_criteria>
- Uma linha de prospecção com apenas Nome Fantasia e Responsável é aceita; sem Nome Fantasia é recusada com frase própria (PROSP-02).
- Razão social em branco vira valor nulo em toda a cadeia, e a comparação de duplicados nunca estoura com nulo (Bugs A e B fechados).
- Duas linhas sem razão social e com o mesmo Nome Fantasia são reconhecidas como possível duplicado.
- O modelo de planilha marca os obrigatórios e continua sendo remapeado automaticamente ao ser reenviado.
- Cliente importado continua nascendo em "Aguardando contato" — nenhuma migration tocada, checagem mecânica no bloco de verificação (PROSP-03).
- A verificação de tipos passa e o fluxo de clientes ativos segue intacto.
</success_criteria>

<output>
Create `.planning/phases/26-importa-o-de-clientes-em-prospec-o-e-limpeza-de-menu/26-02-SUMMARY.md` when done
</output>
